---
phase: 260720-02i-support-greenhouse-eu-board-hosts-in-url
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/detect.ts
  - web/tests/detect.test.ts
autonomous: true
requirements: [QUICK-260720-02i]

must_haves:
  truths:
    - "Pasting https://job-boards.eu.greenhouse.io/imc/ into Watchlist add-company succeeds instead of returning the unsupported-URL error."
    - "An EU Greenhouse URL and the equivalent US Greenhouse URL for the same board produce the identical source key greenhouse:global:imc, so no job is ingested twice."
    - "Lookalike hosts such as evil-greenhouse.io and greenhouse.io.attacker.com remain unsupported."
    - "Strict-slug rejection (multi-segment paths, dotted slugs, encoded separators) still applies to the new EU hosts."
  artifacts:
    - supabase/functions/_shared/detect.ts
    - web/tests/detect.test.ts
  key_links:
    - "detect.ts greenhouseBoard Set -> connectors.ts deterministicSourceKey: the absence of a region field on Greenhouse detections is what keeps one board = one source key."
    - "detect.ts buildEndpoint -> boards-api.greenhouse.io: unified API host serves EU boards, so no endpoint change is required."
---

<objective>
Add Greenhouse's EU display hosts (`job-boards.eu.greenhouse.io`, `boards.eu.greenhouse.io`) to the detect.ts host allowlist as exact matches, without introducing a `region` field on Greenhouse detections.

Purpose: `https://job-boards.eu.greenhouse.io/imc/` is a valid public Greenhouse board but is rejected as unsupported, blocking the user from adding IMC to the Watchlist. Greenhouse's Job Board API is unified (there is no `boards-api.eu.greenhouse.io` — DNS does not resolve), so EU is a display-host difference only.

Output: Two EU hosts accepted by `detectAts`, a comment recording why Greenhouse deliberately has no region branch while Lever does, regression tests, and a follow-up report on regional-host gaps for other providers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@supabase/functions/_shared/detect.ts
@web/tests/detect.test.ts

Read-only reference (DO NOT MODIFY):
@supabase/functions/_shared/connectors.ts
</context>

<diagnosis_already_complete>
Investigation is finished. Do NOT re-probe the network, do NOT re-diagnose.

Root cause: `supabase/functions/_shared/detect.ts` line 19 — the `greenhouseBoard` Set contains only `boards.greenhouse.io` and `job-boards.greenhouse.io`. EU display hosts fall through to `unsupported`. Browser-side `web/src/lib/watchlist.ts` `safeCareersUrl` only checks HTTPS and absence of credentials, so it is not the rejection point; the server's verify-board -> detect.ts is. detect.ts is the single point of fix.

Live probes already run (do not repeat):
- `https://boards-api.eu.greenhouse.io/v1/boards/imc/jobs` -> DNS failure, host does not exist
- `https://boards-api.greenhouse.io/v1/boards/imc/jobs` -> HTTP 200, returns IMC's real jobs
- `https://job-boards.eu.greenhouse.io/imc` -> 200
- `https://boards.eu.greenhouse.io/imc` -> 301
- `https://job-boards.greenhouse.io/imc` -> 200
</diagnosis_already_complete>

<concurrency_constraints>
Another agent is executing Phase 03 gap plans in this repo right now. These paths are OFF LIMITS — do not read for modification, do not edit, do not stage:

- supabase/migrations/0025_scoring_freshness.sql
- supabase/functions/_shared/filters.ts, supabase/functions/_shared/scoring-input.ts
- supabase/functions/score-tick/index.ts, supabase/functions/discovery-sweep/index.ts
- web/src/lib/feed.ts, web/src/lib/feed.test.ts, web/src/lib/preferences.test.ts
- web/src/pages/Preferences.tsx, web/src/pages/Preferences.test.tsx
- web/tests/filters.test.ts, scoring-input.test.ts, company-name-ingestion.test.ts, company-name-feed.integration.test.ts, preference-refilter-feed.integration.test.ts
- scripts/verify-scoring-*, scripts/verify-expected-red.mjs
- .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/03-*

`web/tests/detect.test.ts` IS yours despite sharing a directory with off-limits files.

Stage only the two files in `files_modified` plus this plan's SUMMARY. Never use `git add -A` or `git commit -a`.
</concurrency_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Accept Greenhouse EU display hosts as exact allowlist entries</name>
  <files>supabase/functions/_shared/detect.ts, web/tests/detect.test.ts</files>

  <behavior>
Write these tests in `web/tests/detect.test.ts` FIRST and confirm they fail, then implement.

Add to the existing `detectAts` positive `it.each` table:
  - `https://job-boards.eu.greenhouse.io/imc` -> `{ ats: 'greenhouse', slug: 'imc' }`
  - `https://job-boards.eu.greenhouse.io/imc/` -> `{ ats: 'greenhouse', slug: 'imc' }` (trailing slash, the exact URL the user pasted)
  - `https://boards.eu.greenhouse.io/imc` -> `{ ats: 'greenhouse', slug: 'imc' }`
  - `https://www.job-boards.eu.greenhouse.io/imc` -> `{ ats: 'greenhouse', slug: 'imc' }` (www stripping already applies)
`toEqual` performs exact object comparison, so these assertions fail if a `region` key is present. That is the intended guard — do not relax them to `toMatchObject`.

Add to the existing `detectAts` unsupported `it.each` table:
  - `https://evil-greenhouse.io/imc` (lookalike registrable domain)
  - `https://greenhouse.io.attacker.example/imc` (suffix-confusion host)
  - `https://job-boards.eu.greenhouse.io.evil.example/imc`
  - `https://job-boards.eu.greenhouse.io/imc/jobs/123` (multi-segment path)
  - `https://job-boards.eu.greenhouse.io/bad.slug` (dotted slug)
  - `https://job-boards.eu.greenhouse.io/bad%2Fslug` (encoded separator)
  - `http://job-boards.eu.greenhouse.io/imc` (non-HTTPS)
  - `https://user:password@job-boards.eu.greenhouse.io/imc`
  - `https://job-boards.eu.greenhouse.io:444/imc`

Add a new `describe('greenhouse EU source identity')` block proving one board yields one identity. `deterministicSourceKey` is module-private in connectors.ts, so reach it through the exported `verifyConnector(detected, fetchImpl)`, which accepts an injected fetch. Stub the fetch to resolve a Response-like object with `ok: true` and `json()` returning `{ jobs: [{ company_name: 'IMC' }] }`. Assert:
  - `verifyConnector(detectAts('https://job-boards.eu.greenhouse.io/imc/'), stub)` returns `sourceKey === 'greenhouse:global:imc'`
  - that value is strictly equal to the `sourceKey` from `detectAts('https://job-boards.greenhouse.io/imc')`
  - `region` on the returned verification is `null` for both
Add one assertion that `buildEndpoint(detectAts('https://job-boards.eu.greenhouse.io/imc'))` equals `https://boards-api.greenhouse.io/v1/boards/imc/jobs` — the same US API host — documenting that the API is unified.
  </behavior>

  <action>
In `supabase/functions/_shared/detect.ts`, extend the `greenhouseBoard` Set (line 19) so it contains four exact host strings: the two existing entries plus `job-boards.eu.greenhouse.io` and `boards.eu.greenhouse.io`.

SSRF boundary requirement (hard): these MUST be added as literal members of the existing `Set`, matched by the existing `supportedHosts.greenhouseBoard.has(host)` exact lookup. Do NOT introduce any suffix, wildcard, regex, or `endsWith` matching against the greenhouse registrable domain — that would admit attacker-controlled lookalike and suffix-confusion hosts. The `recruiteeSuffix` pattern elsewhere in this file is NOT a precedent to copy here.

Do NOT set a `region` field on the returned Greenhouse detection, even though the adjacent Lever branch does exactly that for its EU host. `connectors.ts` `deterministicSourceKey` builds the key as ats, region-or-global, and slug joined by colons; adding a region would give one IMC board two different source keys depending on which host the user pasted, and since the uniqueness constraint is source plus external id, every job would be ingested twice. Greenhouse's unified API means one board equals one identity.

Add a short comment at the Greenhouse board branch (near line 74) explaining that asymmetry: Greenhouse EU is display-only and its API host is unified, so all Greenhouse boards share a single global identity, whereas Lever genuinely has a separate regional API host and therefore does carry a region. State plainly that adding a region here would split one board into two source keys and duplicate ingestion. Without this comment someone will later "fix" the asymmetry and reintroduce the bug.

Make NO other changes to detect.ts. `buildEndpoint` stays exactly as-is: it already returns the unified `boards-api.greenhouse.io` host for every Greenhouse detection, which the live probe confirmed serves EU boards. Make NO changes to `connectors.ts`: `canonicalCareersUrl` already emits the US `job-boards.greenhouse.io` careers URL, which returns 200 for EU boards, so it stays correct and unmodified. `DetectResult`'s optional `region` type stays as-is for Lever's benefit.
  </action>

  <verify>
    <automated>cd web &amp;&amp; npx vitest run tests/detect.test.ts</automated>
    <automated>grep -c "job-boards.eu.greenhouse.io" supabase/functions/_shared/detect.ts | grep -qv '^0$'</automated>
    <automated>grep -c "boards.eu.greenhouse.io" supabase/functions/_shared/detect.ts | grep -qv '^0$'</automated>
    <automated>test "$(grep -v '^\s*//' supabase/functions/_shared/detect.ts | grep -c 'endsWith(supportedHosts.greenhouse')" -eq 0</automated>
    <automated>git diff --name-only -- supabase/functions/_shared/connectors.ts | wc -l | grep -q '^0$'</automated>
    <automated>git diff -- supabase/functions/_shared/detect.ts | grep '^+' | grep -v '^+++' | grep -c 'buildEndpoint' | grep -q '^0$'</automated>
  </verify>

  <done>
`npx vitest run tests/detect.test.ts` passes with every new positive and negative case. `detectAts` accepts both EU hosts with and without a trailing slash and returns no `region` key. Lookalike and suffix-confusion hosts still return unsupported. EU and US URLs for the same board produce the identical `greenhouse:global:imc` source key. `connectors.ts` shows zero diff and `buildEndpoint` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Report regional-host gaps for other providers as follow-up candidates</name>
  <files>(read-only investigation — findings go in the SUMMARY, no source files change)</files>

  <action>
Investigation only. Add NO code, modify NO files in this task.

Re-read `supabase/functions/_shared/detect.ts` (already in context from Task 1) and inspect the host handling for the remaining providers: `ashby` (`jobs.ashbyhq.com`), `smartrecruiters` (`jobs.smartrecruiters.com`), and `recruitee` (`.recruitee.com` suffix). For each, note whether the matching strategy could plausibly miss a regional or alternate display host in the same way Greenhouse's did, and whether such a host — if it exists — would be display-only (safe to fold into one identity, like Greenhouse) or API-distinct (requiring a region, like Lever).

Do NOT make live network calls to confirm whether any such host exists; that is what makes this a follow-up candidate rather than part of this task. Base the assessment purely on the code's matching strategy and the endpoint each provider's `buildEndpoint` branch constructs.

Record the findings in the SUMMARY under a `## Follow-up candidates` heading, one short entry per provider, each stating: the current matching strategy, whether a gap is plausible, and the identity implication (unified versus regional) if a gap is later confirmed. Explicitly do NOT expand this task's scope to fix any gap found.
  </action>

  <verify>
    <automated>grep -q "Follow-up candidates" .planning/quick/260720-02i-support-greenhouse-eu-board-hosts-in-url/260720-02i-SUMMARY.md</automated>
    <automated>git status --porcelain -- supabase/functions/_shared/connectors.ts | wc -l | grep -q '^0$'</automated>
  </verify>

  <done>
SUMMARY contains a `## Follow-up candidates` section with one entry each for Ashby, SmartRecruiters, and Recruitee, stating matching strategy, gap plausibility, and unified-versus-regional identity implication. No source file was modified by this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-pasted URL -> detect.ts | Fully attacker-controlled string; detect.ts is the authoritative SSRF gate |
| detect.ts -> buildEndpoint -> outbound fetch | Server makes an outbound request to whatever host the allowlist blesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02i-01 | Spoofing | `greenhouseBoard` host allowlist in detect.ts | critical | mitigate | EU hosts added as exact `Set` members only; no suffix/wildcard/`endsWith`/regex match on `greenhouse.io`. Tests assert `evil-greenhouse.io`, `greenhouse.io.attacker.example`, and `job-boards.eu.greenhouse.io.evil.example` all return unsupported. |
| T-02i-02 | Information disclosure | outbound fetch target from `buildEndpoint` | high | mitigate | `buildEndpoint` remains unmodified and hardcodes `boards-api.greenhouse.io`; only the slug is interpolated and it is `encodeURIComponent`-wrapped behind an existing strict-slug regex. Verify gate asserts zero added `buildEndpoint` lines. |
| T-02i-03 | Tampering | path/slug parsing on the new hosts | high | mitigate | New hosts reuse the existing `singlePathSegment` + `strictSlug` path; negative tests cover multi-segment, dotted, and percent-encoded-separator slugs on the EU hosts. |
| T-02i-04 | Tampering | duplicate job ingestion via split source key | medium | mitigate | No `region` on Greenhouse detections; test asserts EU and US URLs yield the identical `greenhouse:global:imc` source key through `verifyConnector`, plus an explanatory code comment to prevent regression. |
| T-02i-05 | Elevation of privilege | scheme/credential/port bypass on new hosts | medium | accept | Existing pre-host guards already reject non-HTTPS, credentialed, ported, and fragment URLs before any host branch; negative tests confirm they apply to the EU hosts. No new code path. |

No package-manager installs in this plan, so no supply-chain checkpoint is required.
</threat_model>

<verification>
1. `cd web && npx vitest run tests/detect.test.ts` — all detect tests pass, including new EU positives, negatives, and the source-identity block.
2. `cd web && npx vitest run tests/public-connectors.integration.test.ts` — existing connector integration tests still pass, confirming the detection change did not disturb the registry contract.
3. `git status --porcelain` shows changes limited to `supabase/functions/_shared/detect.ts`, `web/tests/detect.test.ts`, and this quick task's planning directory. No off-limits Phase 03 path appears.
4. `supabase/functions/_shared/connectors.ts` shows zero diff.
</verification>

<success_criteria>
- `detectAts('https://job-boards.eu.greenhouse.io/imc/')` returns `{ ats: 'greenhouse', slug: 'imc' }` with no `region` key.
- `detectAts('https://boards.eu.greenhouse.io/imc')` returns the same.
- EU and US URLs for the IMC board resolve to the identical source key `greenhouse:global:imc`.
- Lookalike and suffix-confusion greenhouse hosts remain unsupported.
- `buildEndpoint` and `connectors.ts` `canonicalCareersUrl` are provably unmodified.
- A comment in detect.ts explains why Greenhouse has no region branch while Lever does.
- SUMMARY reports Ashby/SmartRecruiters/Recruitee regional-host findings as follow-up candidates only.
</success_criteria>

<output>
Create `.planning/quick/260720-02i-support-greenhouse-eu-board-hosts-in-url/260720-02i-SUMMARY.md` when done.
</output>
