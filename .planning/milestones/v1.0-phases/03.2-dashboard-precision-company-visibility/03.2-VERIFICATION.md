---
phase: "03.2-dashboard-precision-company-visibility"
verified: "2026-07-22T23:51:27Z"
status: passed
score: "12/12 must-haves verified"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 8/10
  gaps_closed:
    - "The redundant All jobs mode was removed; the Dashboard now has one current preference-pass scope whose score boundary is owned by Strong, Good, and Weak."
    - "All six authenticated production UAT checks, including staged company state and real-browser column resizing, were passed by the owner."
  gaps_remaining: []
  regressions: []
---

# Phase 03.2: Dashboard Precision & Company Visibility Verification Report

**Phase Goal:** Users can tune the current dashboard feed by company, score tier, and explicit required-experience cap while retaining truthful future ingestion and gaining a full-width, accessible, resizable results table.
**Verified:** 2026-07-22T23:51:27Z
**Status:** passed
**Re-verification:** Yes — after Plan 04 gap closure and owner production UAT

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Current-feed company controls are truthful, staged, session-only, and leave newly refreshed companies visible by default. | ✓ VERIFIED | `dashboardCompanyOptions`, hidden-key state, draft copy/toggle/reset, and `filterDashboardRows` are pure in `web/src/lib/dashboard.ts:18-95`; Dashboard owns company state only in React state (`web/src/pages/Dashboard.tsx:153-155`). Unit tests cover truthful normalization, staging, reset, and new-company default. UAT Tests 1-2 passed the authenticated remount, search, Escape, and Show results flows. |
| 2 | The Dashboard has one current preference-pass scope, no redundant mode, and Strong/Good/Weak are the only score-boundary controls. | ✓ VERIFIED | `preferenceVisible` is the sole nondismissed base (`web/src/lib/dashboard.ts:74-91`); `ALL_SCORE_TIERS` initializes all three controls and `tierPresentation(null)` maps scoreless survivors to Weak. No `viewAll`, `defaultVisible`, or `All jobs` remains in production source. Unit/integration tests prove low-score and eligible failed/deferred scoreless rows leave when Weak is deselected. UAT Test 2 passed on exact production release `895356f`. |
| 3 | Applied company visibility and score tiers use AND semantics while Show dismissed remains separate. | ✓ VERIFIED | `filterDashboardRows` applies base eligibility, hidden-company membership, then selected-tier membership conjunctively (`web/src/lib/dashboard.ts:83-92`). Dismissed state is handled separately by `baseDashboardVisible`. Regression tests cover company+tier AND and dismissed independence; the owner passed the production check in UAT Test 2. |
| 4 | A nullable integer 0-20 maximum required-experience preference persists per user and reuses the existing refilter path. | ✓ VERIFIED | Migration 0030 adds the bounded nullable column. `web/src/lib/preferences.ts:3-67` loads/upserts it and calls `mark_recent_jobs_for_refilter`; `Preferences.tsx:100-153` hydrates, validates, and submits blank or integer values. Save-order and cache/refilter tests pass; UAT Test 3 passed. |
| 5 | Experience semantics are conservative: exact threshold passes, only explicit required minima above it fail, ranges use the minimum, and title/optional/ambiguous text remains eligible. | ✓ VERIFIED | `experienceMinimumRequired` normalizes Unicode and extracts mandatory lower bounds (`supabase/functions/_shared/filters.ts:29-125`); `cheapFilter` rejects only `minimum > cap` (`:254-264`) without reading title seniority. Table-driven tests cover 38 parser cases and 21 candidate-local filter cases. UAT Test 3 passed the deployed form and boundary behavior. |
| 6 | The experience gate is provider-neutral, precedes routing/hash/budget/AI, participates in scoring freshness, and uses a database-safe reason. | ✓ VERIFIED | `score-tick` maps the preference and calls `cheapFilter` before routing and hashing (`supabase/functions/score-tick/index.ts:243-307`); budget reservation and the single AI call occur later. `scoring-input.ts:58-78` hashes the cap with `filter-v3`; migration 0030 and `feed.ts` agree on `experience_above_max`. Isolation tests reject provider bypass and pin ordering. |
| 7 | Dashboard alone uses the full remaining browser width while shared chrome and non-Dashboard routes remain centered. | ✓ VERIFIED | `shellMainClass` widens only pathname `/` (`web/src/components/Shell.tsx:16-20,97-99`); header and heartbeat retain `max-w-6xl`. Route tests cover Dashboard, detail, Preferences, and shared chrome. UAT Test 4 passed. |
| 8 | The table shows Location and omits Match reason, while job detail retains complete Match reasons. | ✓ VERIFIED | Feed list/detail projections retain `jobs.location` and detail `reasons`; Dashboard renders Location with an em dash fallback and has no Match reason header (`Dashboard.tsx:554-684`). `JobDetail.tsx:228-241` renders Match reasons. Static tests and UAT Test 4 pass. |
| 9 | Column widths use a versioned, bounded same-origin persistence model and invalid storage falls back safely. | ✓ VERIFIED | `web/src/lib/dashboardColumns.ts:1-175` defines all nine exact bounds, validates known finite in-range values, catches storage denial, and persists only the versioned width map. Unit tests cover malformed, wrong-version, unknown, non-finite, out-of-range, denied, clamped, and keyboard cases. UAT Test 5 passed reload persistence and company-Reset independence. |
| 10 | Resizing is accessible and isolated from Score sorting across pointer, keyboard, cancellation, and narrow/coarse-pointer use. | ✓ VERIFIED | `ColumnResizeHandle.tsx:29-141` implements focusable separator semantics, numeric ARIA values, pointer capture, rollback, document-style restoration, and keyboard commits. Dashboard renders eight sibling handles, a separate Score sort button, and a labeled focusable overflow region. Static/reducer tests pass; owner UAT Tests 5-6 provide the required real-browser evidence. |
| 11 | Deferred title-keyword exclusions, company bulk actions, and a tier dropdown were not introduced by this gap closure. | ✓ VERIFIED | Source scan finds no title-exclusion implementation, Select all/Clear all actions, tier `<select>`, or removal of the existing company Reset. The experience cap and three explicit tier buttons remain intact, matching the Plan 04 scope fence. |
| 12 | The reviewed implementation is deployed as an exact identified production release and all phase UAT is complete. | ✓ VERIFIED | Release `895356f76edd8bbdb34a253460ad5152fbc16310` is bound to Cloudflare deployment `7c804087-c656-499f-a3ec-3ad91a71fac4`; immutable and production assets match SHA-256 `d085764d3d54c78f8668ee593608cb99b39161ec4bc93da1af15bbe930d8301e`. `03.2-UAT.md` records 6/6 passed with zero issues on that release. |

**Score:** 12/12 truths verified; 0 behavior-unverified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `supabase/migrations/0030_dashboard_precision.sql` | Nullable 0-20 cap and fourth filter reason | ✓ VERIFIED | Exists, substantive, and aligned with browser/worker/feed types. |
| `supabase/functions/_shared/filters.ts` | Conservative provider-neutral experience parser/gate | ✓ VERIFIED | Exported parser and gate are called by `score-tick`; focused behavioral tests pass. |
| `supabase/functions/_shared/scoring-input.ts` | Semantic cap invalidation | ✓ VERIFIED | Cap and filter revision participate in the canonical hash. |
| `web/src/lib/preferences.ts`, `web/src/pages/Preferences.tsx` | Load/save/validate/explain the cap | ✓ VERIFIED | Select, payload, form validation, refilter RPC, and cache convergence are wired. |
| `web/src/lib/feed.ts` | Single current preference-pass feed contract and tier presentation | ✓ VERIFIED | RLS-scoped query output is filtered by truthful company identity and `preferenceVisible`. |
| `web/src/lib/dashboard.ts` | Pure one-scope company/tier/dismissed pipeline | ✓ VERIFIED | Imported and used by Dashboard; no network or persistence side effects. |
| `web/src/pages/Dashboard.tsx` | Full Dashboard controls/table composition | ✓ VERIFIED | Real query data flows through filters, sorting, table, links, and actions. |
| `web/src/lib/dashboardColumns.ts` | Typed width/persistence model | ✓ VERIFIED | Imported by Dashboard and resize handle; untrusted storage is validated. |
| `web/src/components/ColumnResizeHandle.tsx` | Accessible pointer/keyboard separator | ✓ VERIFIED | Rendered for every non-final column and wired to preview/commit state. |
| `web/src/components/Shell.tsx` | Dashboard-only width branch | ✓ VERIFIED | Current pathname changes only main-content width. |
| `web/src/pages/JobDetail.tsx` | Detail-only match reasons | ✓ VERIFIED | Real detail query renders stored reason strings. |
| `scripts/verify-paylocity.ts` | Release evidence aligned to one-scope Dashboard contract | ✓ VERIFIED | Requires `preferenceVisible`, `tierPresentation`, and truthful company identity; stale dual-mode assertions are absent. |

Plan 04's machine artifact query passed 6/6 declared artifacts. Earlier plans used descriptive rather than path-structured artifact frontmatter, so they were verified manually at all four levels above.

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Preferences form | Recent jobs | upsert -> `mark_recent_jobs_for_refilter` | ✓ WIRED | Upsert succeeds before the RPC; tests pin success/failure ordering. |
| Database preference | Pre-AI filter | worker select/map -> `cheapFilter` | ✓ WIRED | Cap is selected per user and applied before routing, budget, or AI. |
| Experience cap | Score freshness | `scoringInputHash` + `filter-v3` | ✓ WIRED | Semantic changes cannot reuse the old scoring hash. |
| RLS-scoped feed | Dashboard base | `listFeed` -> `preferenceVisible` | ✓ WIRED | Confirmed current preference passes are the sole client-visible base. |
| Feed score | Tier control | `tierPresentation` -> `filterDashboardRows` | ✓ WIRED | Null and low scores map to Weak and are removed when Weak is unselected. |
| Dashboard helper | Dashboard page | `filterDashboardRows` with dismissed/company/tier state | ✓ WIRED | No retired mode boolean remains. |
| Width storage | Rendered table | hydrate -> state -> colgroup/min-width -> persist | ✓ WIRED | Width changes drive all nine columns and completion writes the safe map. |
| Resize separator | Score header | sibling targets with propagation guards | ✓ WIRED | Resizing cannot activate the separate sort button. |
| Job row | Detail reasons | `/jobs/:id` -> `getFeedJob` -> `row.reasons` | ✓ WIRED | Removing the list column did not remove detail reasons. |

Plan 04's machine key-link query passed 3/3 declared links.

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Dashboard | `feedQuery.data` | RLS-scoped Supabase `user_jobs` joined to jobs/companies | Yes; up to 200 current confirmed preference passes | ✓ FLOWING |
| Company panel | `companyOptions` | `companyName(row)` over loaded feed | Yes; no hardcoded/user-created options | ✓ FLOWING |
| Results table | `rows` | preference-pass -> dismissed -> company -> tier -> sort | Yes; rendered from the live query result | ✓ FLOWING |
| Preferences | `maxRequiredExperience` | own-row preferences select/upsert | Yes; nullable server field hydrates and saves | ✓ FLOWING |
| Width layout | `columnWidths` | validated localStorage or exact defaults | Yes; state drives colgroup and table minimum width | ✓ FLOWING |
| Job detail | `row.reasons` | detail-only `user_jobs` projection | Yes; stored reasons render on detail | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command/Evidence | Result | Status |
|---|---|---|---|
| Complete automated regression suite | `cd web && npm test` | 38 files, 571/571 tests passed | ✓ PASS |
| Production compile | `cd web && npm run build` | TypeScript/Vite passed; emitted `index-B44mvuSI.js` | ✓ PASS |
| Static quality gate | `cd web && npm run lint` | Passed with only the pre-existing `AuthProvider.tsx` Fast Refresh warning | ✓ PASS |
| Authenticated interaction contract | `03.2-UAT.md` on exact release `895356f` | 6/6 tests passed, 0 issues | ✓ PASS |

### Probe Execution

No phase probe script was declared or found. Hosted verification used the approved schema/function/web deployment path, exact source/asset identity, automated suite, and owner UAT.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| PREF-01 | 03.2-01 | Persist maximum required experience with user preferences | ✓ SATISFIED | Bounded database column, browser projection/form, own-row upsert, and refilter RPC are wired and UAT-passed. |
| SCOR-01 | 03.2-01 | Cheap filters discard irrelevant postings before AI | ✓ SATISFIED | Provider-neutral experience gate executes before routing, budget, and AI and records a bounded reason. |
| SCOR-03 | 03.2-01, 03.2-02, 03.2-04 | Plain-language match reasons remain available | ✓ SATISFIED | Filter reason has a label; full scored reasons remain on job detail after list-column removal. |
| SCOR-04 | 03.2-02, 03.2-03, 03.2-04 | Dashboard feed, score controls, direct apply access, and accessible table | ✓ SATISFIED | One-scope feed, truthful company/tier filters, Location, links, resizing, persistence, and responsive behavior pass tests and UAT. |

No additional requirement is assigned to Phase 03.2; there are no orphaned requirement IDs.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| Phase-modified production source | - | No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers | None | No blocker or incomplete implementation marker. |
| `web/src/pages/Dashboard.test.tsx` | source assertions | Some accessibility/event wiring is checked statically | Info | Real-browser gaps are closed by owner UAT Tests 5-6. |

Disconfirmation checks found no partial phase requirement, no misleading test that remains the sole evidence for a runtime behavior, and no uncovered phase-critical error path. Static interaction assertions are supplemented by owner UAT; storage denial, malformed data, query errors, preference-save failures, drag cancellation, and empty filter states all have explicit handling or tests.

### Human Verification Required

None. All six previously deferred authenticated/visual interaction checks are recorded passed in `03.2-UAT.md` on the exact production release.

### Gaps Summary

No implementation or verification gap remains. The phase goal is present, substantive, wired to real data, regression-tested, built, linted, deployed as the approved exact release, and accepted through 6/6 production UAT checks.

---

_Verified: 2026-07-22T23:51:27Z_
_Verifier: the agent (gsd-verifier)_
