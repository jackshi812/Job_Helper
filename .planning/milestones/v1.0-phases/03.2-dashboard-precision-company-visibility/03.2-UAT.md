---
status: complete
phase: 03.2-dashboard-precision-company-visibility
source: [03.2-VERIFICATION.md]
started: 2026-07-22T22:28:51Z
updated: 2026-07-22T23:41:35Z
---

## Current Test

[testing complete]

## Tests

### 1. Fresh defaults and session reset
expected: All loaded companies and Strong, Good, and Weak start selected; hidden companies return after reload/navigation; newly refreshed companies are visible.
result: pass

### 2. Staged company search and AND filtering
expected: Search only narrows checklist options; checkbox edits wait for Show results; Reset changes the draft; Escape cancels; company and tier filters combine with AND semantics and announce the count.
result: pass
notes: "The redundant All jobs mode was removed on exact release 895356f76edd8bbdb34a253460ad5152fbc16310. On Cloudflare deployment 7c804087-c656-499f-a3ec-3ad91a71fac4, the owner confirmed after a hard refresh that the button is absent, all three tiers start selected, Weak toggling changes qualifying rows, and company plus dismissed filtering remain independent."

### 3. Maximum required-experience preference
expected: Blank, 0, 3, and 20 save; decimal, negative, and 21 are blocked; cap 3 retains explicit minimum 3, filters required minimums above 3, and does not reject optional years or senior title words.
result: pass

### 4. Layout, Location, and detail reasons
expected: Only Dashboard uses full width; Location shows real data or an em dash; Match reason is absent from the list and Match reasons remain on job detail; other routes stay centered.
result: pass

### 5. Column resize and persistence
expected: Pointer drag, cancel, Arrow/Shift+Arrow/Home/End, bounds, Score-sort isolation, and reload persistence work; company Reset does not reset widths.
result: pass

### 6. Narrow and coarse-pointer use
expected: The complete table remains available through labeled horizontal scrolling and resize hit areas remain usable without hiding columns.
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "The All jobs button changes the Dashboard from its normal view to the complete job view."
  status: resolved
  reason: "User reported: All jobs button shows the same jobs as the normal Dashboard and has no visible effect"
  severity: major
  test: 2
  root_cause: "Phase 03.2 changed normal eligibility from score >= 50 to every completed scored row so Weak could be selected by default. All jobs now adds only exceptional preference-pass rows whose scoring failed, is deferred, or remains scoreless; a healthy settled feed therefore makes both modes identical."
  artifacts:
    - path: "web/src/lib/feed.ts"
      issue: "Normal eligibility now accepts every non-null scored tier, collapsing the ordinary difference from all-mode eligibility."
    - path: "web/src/lib/dashboard.ts"
      issue: "viewAll still switches predicates, but the predicates coincide for settled scored rows."
    - path: "web/src/pages/Dashboard.tsx"
      issue: "The All jobs affordance remains even though its residual failed/deferred-only meaning is not communicated."
    - path: "web/src/lib/dashboard.test.ts"
      issue: "Coverage preserves Weak-by-default without asserting a meaningful All jobs transition for settled data."
  missing:
    - "Resolved by adopting one current preference-pass Dashboard scope, removing the redundant mode, and making Strong/Good/Weak the sole score-boundary controls."
    - "Regression coverage now proves Weak owns both low-score and eligible scoreless membership while company and dismissed filtering retain their existing composition."
  debug_session: ".planning/debug/resolved/all-jobs-no-visible-effect.md"
