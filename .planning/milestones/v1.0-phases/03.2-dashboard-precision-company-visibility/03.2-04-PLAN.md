---
phase: 03.2-dashboard-precision-company-visibility
plan: 04
type: execute
wave: 4
depends_on: [03.2-03]
files_modified:
  - web/src/lib/feed.ts
  - web/src/lib/dashboard.ts
  - web/src/pages/Dashboard.tsx
  - web/src/lib/feed.test.ts
  - web/src/lib/dashboard.test.ts
  - web/src/pages/Dashboard.test.tsx
  - web/tests/company-name-feed.integration.test.ts
  - web/tests/preference-refilter-feed.integration.test.ts
  - scripts/verify-paylocity.ts
  - web/tests/paylocity-verifier-safety.test.ts
  - .planning/phases/03.2-dashboard-precision-company-visibility/03.2-CONTEXT.md
  - .planning/phases/03.2-dashboard-precision-company-visibility/03.2-UI-SPEC.md
  - .planning/phases/03.2-dashboard-precision-company-visibility/03.2-UAT.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [SCOR-03, SCOR-04]
must_haves:
  truths:
    - "D-12: Strong, Good, and Weak start selected, and explicit tier selection is the only score-boundary control."
    - "The Dashboard has one current preference-pass job scope; it does not advertise a second mode that can be an observational no-op."
    - "A Weak scored row and a current eligible scoreless failed/deferred row are both reachable on first render and both leave the result set when Weak is deselected."
    - "D-13: Applied company visibility and selected tiers retain AND semantics, while Show dismissed remains a separate state filter."
    - "The title-keyword exclusion, company bulk-action, and tier-dropdown follow-ups are not implemented in this gap closure."
  artifacts:
    - path: "web/src/lib/dashboard.ts"
      provides: "Single-scope Dashboard eligibility composed with company, tier, and dismissed filters"
    - path: "web/src/pages/Dashboard.tsx"
      provides: "Dashboard controls without the redundant mode affordance"
    - path: "web/src/lib/dashboard.test.ts"
      provides: "Weak and exceptional scoreless regression coverage for the single-scope contract"
    - path: "web/tests/preference-refilter-feed.integration.test.ts"
      provides: "Preference-save convergence coverage against the one-scope preferenceVisible contract"
    - path: "scripts/verify-paylocity.ts"
      provides: "Release verifier assertions and evidence copy aligned to the one-scope Dashboard contract"
    - path: ".planning/phases/03.2-dashboard-precision-company-visibility/03.2-UI-SPEC.md"
      provides: "Updated approved interaction contract matching the shipped control model"
  key_links:
    - from: "web/src/lib/feed.ts"
      to: "web/src/lib/dashboard.ts"
      via: "preferenceVisible supplies the current authorized preference-pass base before client filters"
      pattern: "preferenceVisible"
    - from: "web/src/lib/dashboard.ts"
      to: "web/src/pages/Dashboard.tsx"
      via: "filterDashboardRows consumes selected tiers, hidden company keys, and dismissed state without viewAll"
      pattern: "filterDashboardRows"
    - from: "web/src/lib/feed.ts"
      to: "web/src/lib/dashboard.ts"
      via: "tierPresentation maps null scores to Weak so the Weak control owns exceptional scoreless visibility"
      pattern: "tierPresentation"
---

<objective>
Close the diagnosed All jobs UAT gap by removing the redundant dual-mode Dashboard contract and making the current preference-pass feed the single base scope filtered by company and explicit score tiers.

Purpose: Preserve the locked Phase 03.2 decision that Strong, Good, and Weak all start selected and tier selection owns score boundaries, while retaining access to every confirmed current preference pass rather than restoring the superseded score-at-least-50 default.
Output: Simplified Dashboard eligibility and controls, regression coverage for Weak and eligible scoreless rows, reconciled project/UI documentation, and exact-release deployment plus targeted UAT evidence.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-dashboard-precision-company-visibility/03.2-CONTEXT.md
@.planning/phases/03.2-dashboard-precision-company-visibility/03.2-UI-SPEC.md
@.planning/phases/03.2-dashboard-precision-company-visibility/03.2-UAT.md
@.planning/debug/all-jobs-no-visible-effect.md
@.planning/phases/03.2-dashboard-precision-company-visibility/03.2-02-SUMMARY.md
@.planning/phases/03.2-dashboard-precision-company-visibility/03.2-03-SUMMARY.md
@web/src/lib/feed.ts
@web/src/lib/dashboard.ts
@web/src/pages/Dashboard.tsx
@scripts/verify-paylocity.ts
@web/tests/paylocity-verifier-safety.test.ts
@web/tests/preference-refilter-feed.integration.test.ts

<interfaces>
From web/src/lib/feed.ts:
- `preferenceVisible(row: FeedRow): boolean` admits only open, current confirmed preference passes, including completed scores, post-filter failures, and paid-deferred survivors.
- `tierPresentation(score: number | null): TierPresentation` derives Strong at 75+, Good at 50-74, and Weak below 50 or when score is null.
- `listFeed(): Promise<FeedRow[]>` is RLS-scoped and already returns only truthful-company rows that satisfy `preferenceVisible`.

From web/src/lib/dashboard.ts before this plan:
- `DashboardFilterState` contains `viewAll`, `showDismissed`, `appliedHiddenKeys`, and `selectedTiers`; this plan removes only the obsolete `viewAll` dimension.
- `filterDashboardRows(rows, state): FeedRow[]` is the pure company-plus-tier composition point used by Dashboard.tsx.
</interfaces>
</context>

<scope_fence>
Do not replace the maximum-experience preference with title-keyword exclusions, do not add company Clear all or Select all actions or remove the company Reset action, and do not consolidate Strong/Good/Weak into a dropdown. Those newly captured ideas explicitly follow Phase 03.2 gap closure.
</scope_fence>

<scope_rationale>
The 15-path impact set is intentionally bounded and test/document-heavy: only three production UI/library files change behavior (`feed.ts`, `dashboard.ts`, and `Dashboard.tsx`); seven paths are unit/integration/release-verifier coverage; and five paths are contract, state, or UAT records. Task 1 owns the predicate removal plus every directly coupled regression and Paylocity verifier assertion, Task 2 owns the control removal plus every canonical decision/UI ledger, and Task 3 owns deployed acceptance evidence. Splitting those atomic contract updates into another plan would create cross-plan predicate/document drift without reducing production scope. No schema, backend function, provider, package, preference form, company bulk action, or tier-control redesign is included.
</scope_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse Dashboard eligibility to one tier-owned preference-pass scope</name>
  <files>web/src/lib/feed.ts, web/src/lib/dashboard.ts, web/src/lib/feed.test.ts, web/src/lib/dashboard.test.ts, web/tests/company-name-feed.integration.test.ts, web/tests/preference-refilter-feed.integration.test.ts, scripts/verify-paylocity.ts, web/tests/paylocity-verifier-safety.test.ts</files>
  <behavior>
    - Test 1: With Strong, Good, and Weak selected, the visible nondismissed set includes Strong, Good, Weak, eligible failed scoreless, and eligible deferred scoreless rows from the current preference-pass pool.
    - Test 2: Deselecting Weak removes both the scored Weak row and scoreless eligible rows, while Strong and Good remain; this proves the tier helper owns the boundary.
    - Test 3: Unknown pending, filtered, closed, stale-current-revision, and identity-less rows remain excluded by the existing feed contract.
    - Test 4: Show dismissed returns the dismissed pool without reintroducing a second score mode, and company-plus-tier filtering remains conjunctive per D-13.
    - Test 5: The preference-refilter integration imports `preferenceVisible`; a deferred stale scored row and a deferred eligible scoreless survivor are in the single base scope, an ordinary unknown pending row is not, and convergence leaves only the current preference pass.
    - Test 6: Paylocity dry-run and safety coverage require the one-scope `preferenceVisible`/`tierPresentation`/truthful-company contract and contain no assertion or evidence copy that depends on the removed default predicate or All-versus-Focused modes.
  </behavior>
  <action>Write the failing predicate and integration cases first. Remove `viewAll` from `DashboardFilterState` and from `baseDashboardVisible`; for the ordinary nondismissed path use the existing `preferenceVisible` contract as the single base, then apply truthful-company and tier filters. Remove the now-dead `defaultVisible` export and its scored-only tests/usages so production and tests cannot preserve a hidden competing Focused contract. In `preference-refilter-feed.integration.test.ts`, replace the removed predicate import and calls with `preferenceVisible`, explicitly expect the deferred stale scored fixture and deferred eligible scoreless fixture to remain in the one current preference-pass scope, retain `Updating` only for the stale scored fixture, and retain the converged assertion that the filtered old title leaves only the current title. In `scripts/verify-paylocity.ts`, change `assertDashboardFeed` failure/evidence wording to the single current preference-pass scope, remove the unused synthetic Focused boolean assertion, and change the dry-run feed source gate to require `preferenceVisible`, `tierPresentation`, and `companyName` without requiring the removed predicate; describe the proof as one preference-pass scope with tier-owned boundaries. Revise `paylocity-verifier-safety.test.ts` source assertions to pin that new dry-run/evidence contract and reject stale dual-mode assumptions. Keep `listFeed` query/RLS/current-revision filtering unchanged, and keep `tierPresentation(null)` mapped to Weak so eligible failed/deferred scoreless rows remain controlled by the Weak selection. Do not change tier thresholds, dismissal persistence, scoring, preferences, or backend ingestion.</action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- src/lib/feed.test.ts src/lib/dashboard.test.ts tests/company-name-feed.integration.test.ts tests/preference-refilter-feed.integration.test.ts tests/paylocity-verifier-safety.test.ts</automated>
  </verify>
  <done>The pure pipeline has no mode boolean, every confirmed current preference pass is reachable with all tiers selected, Weak selection deterministically owns both low-score and scoreless membership, preference-save convergence uses the one-scope predicate, Paylocity verification no longer asserts the retired dual-mode contract, and all focused tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the redundant control and reconcile the locked UI/product contract</name>
  <files>web/src/pages/Dashboard.tsx, web/src/pages/Dashboard.test.tsx, .planning/phases/03.2-dashboard-precision-company-visibility/03.2-CONTEXT.md, .planning/phases/03.2-dashboard-precision-company-visibility/03.2-UI-SPEC.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <behavior>
    - Test 1: First render has no All jobs control, while Strong, Good, and Weak are pressed and a Weak fixture renders.
    - Test 2: Dashboard passes only dismissed state, applied hidden company keys, and selected tiers into `filterDashboardRows`; no stale mode state or memo dependency remains.
    - Test 3: Show dismissed, Companies, staged Show results, result counts, score sorting, and resize controls retain their existing accessible markup.
  </behavior>
  <action>Write the static-render/source regression first, then remove `viewAll` state, handler, button, predicate argument, and memo dependency from Dashboard.tsx. Keep Show dismissed as the first control and leave all company/tier/table interactions unchanged. Update D-12 in CONTEXT.md to state that the complete current preference-pass feed is the single Dashboard scope and Strong/Good/Weak are the sole score-boundary controls. In UI-SPEC.md remove the stale All jobs page-order and interaction references and describe company/tier filtering after current preference-pass and dismissed eligibility. In PROJECT.md supersede the Phase 3 All-versus-Focused wording with the current one-scope, all-tier-selected contract while preserving the requirement that confirmed preference passes remain inspectable. In STATE.md preserve the full earlier Phase 3 All-versus-Focused decision as historical text, mark it explicitly superseded for Dashboard presentation by Phase 03.2, and append the active Phase 03.2 decision that the Dashboard has one current preference-pass scope whose score boundary is owned only by Strong/Good/Weak selection. Keep the pending todo names and all other decision history intact. Do not edit or implement any follow-up preference or company/tier-control requests.</action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- src/pages/Dashboard.test.tsx src/lib/dashboard.test.ts src/lib/feed.test.ts</automated>
  </verify>
  <done>The deployed UI contract has no unexplained expansion affordance, all three tier controls remain selected on mount, existing company/dismiss/sort/resize behavior stays intact, and canonical product/UI documentation matches the implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve exact web release and targeted gap-closure UAT</name>
  <files>.planning/phases/03.2-dashboard-precision-company-visibility/03.2-UAT.md</files>
  <action>After Tasks 1 and 2 pass focused verification, run the complete web suite, production build, and lint. Present the exact implementation/documentation diff and release commit for owner approval before pushing or deploying. Use the established GitHub-to-Cloudflare exact-SHA process, record the deployment ID/URL and immutable asset hash match, and do not deploy any schema, Edge function, provider, polling, preference, or paid-AI change. On that exact release, perform one proportional signed-in Dashboard check: the redundant control is absent; Strong, Good, and Weak begin selected; a Weak selection change visibly changes qualifying rows when such rows exist; Show dismissed and company filtering still operate independently. The deterministic automated fixture is the required evidence for eligible scoreless membership, so do not create or mutate production job rows merely to manufacture that state. Record the result against UAT Test 2 and close the diagnosed gap only if both automated and deployed checks pass.</action>
  <verify>
    <automated>cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
    <human-check>On the exact deployed release, confirm there is one Dashboard job scope with no All jobs button, all three tier controls start selected, Weak toggling affects qualifying rows when present, and company plus dismissed controls retain their prior behavior.</human-check>
  </verify>
  <done>The full 517-test-or-higher baseline, build, and lint pass; the owner approves the exact web-only release; Cloudflare serves the matching artifact; and UAT Test 2 records the reconciled contract as passed without production fixture mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| RLS-scoped feed → client filters | The browser may narrow only already-authorized, truthful-company, current preference-pass rows. |
| Score/status data → tier presentation | Stored nullable scores and status determine visible tier membership through bounded presentation helpers. |
| Git release → Cloudflare asset | Production acceptance must be tied to the reviewed exact commit and immutable built asset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.2-08 | Information disclosure | `listFeed` → `filterDashboardRows` | medium | mitigate | Reuse `preferenceVisible`, RLS-scoped `listFeed`, open/current-revision checks, and truthful company identity; add excluded-row regressions before widening the one client-visible scope. |
| T-03.2-09 | Tampering | null/low scores → tier selection | medium | mitigate | Derive tiers only through `tierPresentation`, pin 75/50 boundaries and null-to-Weak behavior, and test membership as tiers toggle. |
| T-03.2-10 | Repudiation | product/UI contract | low | mitigate | Update CONTEXT, UI-SPEC, PROJECT, and UAT evidence together so the removed mode and retained preference-pass visibility are auditable. |
| T-03.2-SC | Tampering | web deployment | high | mitigate | Require blocking owner approval, exact commit/deployment identity, and immutable asset-hash verification; install no packages. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Tune current feed by company and score tier while preserving truthful ingestion and the accessible table | 03.2-02, 03.2-03, 03.2-04 | COVERED | Plan 04 removes the contradictory mode while retaining all shipped controls. |
| REQ | PREF-01 | Per-user preferences | 03.2-01 | COVERED | Unchanged; full verification protects it. |
| REQ | SCOR-01 | Cheap filtering before AI | 03.2-01 | COVERED | Unchanged; the browser continues to consume confirmed preference passes only. |
| REQ | SCOR-03 | Match information remains available | 03.2-02, 03.2-04 | COVERED | Project wording is reconciled; detail reasons remain untouched. |
| REQ | SCOR-04 | Dashboard feed with scores and direct job access | 03.2-02, 03.2-03, 03.2-04 | COVERED | Single-scope eligibility and targeted UAT close the diagnosed feed-control gap. |
| RESEARCH | compatibility correction | Explicit tiers must own the score boundary and Weak must be reachable | 03.2-02, 03.2-04 | COVERED | Plan 04 removes the residual competing All/Focused contract. |
| CONTEXT | D-01–D-06 | Session-only truthful staged company controls | 03.2-02, 03.2-04 | COVERED | Existing behavior retained and regression-checked; captured bulk-action follow-up excluded. |
| CONTEXT | D-07–D-11 | Explicit required-years preference | 03.2-01 | COVERED | Existing implementation remains; captured replacement idea excluded. |
| CONTEXT | D-12 | All tiers selected; tier selection owns score boundaries | 03.2-02, 03.2-04 | COVERED | Direct target of this gap closure. |
| CONTEXT | D-13 | Company and tier filters use AND semantics | 03.2-02, 03.2-04 | COVERED | Pure regression retains composition. |
| CONTEXT | D-14–D-17 | Full-width detail/location/resizable table contract | 03.2-02, 03.2-03, 03.2-04 | COVERED | Unchanged and protected by full verification/UAT. |
</source_audit>

<verification>
1. Focused predicate, feed, integration, and static Dashboard tests pass after each behavior change.
2. The complete `cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint` gate passes with no new warning class.
3. Source review confirms no preference schema/parser, company bulk-action, tier-dropdown, backend, package, or ingestion change entered the gap closure.
4. Exact-SHA Cloudflare deployment and the targeted signed-in UAT check are recorded in 03.2-UAT.md.
</verification>

<success_criteria>
- The reported no-visible-effect control no longer exists because there is no redundant Dashboard mode.
- Every confirmed current preference pass remains inspectable in the single Dashboard scope when its company and tier are selected.
- Strong, Good, and Weak begin selected; Weak alone controls low-score and eligible scoreless rows.
- Company, dismissed, sorting, column resizing, detail reasons, Location, and Dashboard-only width behavior regressions pass.
- The three captured post-03.2 follow-up ideas remain outside this plan.
- The exact web-only release passes targeted authenticated UAT.
</success_criteria>

<output>
Create `.planning/phases/03.2-dashboard-precision-company-visibility/03.2-04-SUMMARY.md` when done.
</output>
