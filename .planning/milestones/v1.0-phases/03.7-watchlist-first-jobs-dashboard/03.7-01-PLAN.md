---
phase: 03.7-watchlist-first-jobs-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/dashboard.ts
  - web/src/lib/dashboard.test.ts
  - web/src/pages/Dashboard.tsx
  - web/src/pages/Dashboard.test.tsx
  - web/src/components/Shell.tsx
  - web/src/components/Shell.test.tsx
  - web/src/main.tsx
autonomous: true
requirements: [SCOR-04]
must_haves:
  truths:
    - "The default route displays only preference-eligible jobs with an existing normalized watchlist-company relationship."
    - "The combined feed remains available at a clearly labeled All Jobs route."
    - "Both views reuse the existing Dashboard scoring, filter, lifecycle, detail, apply, queue, table, accessibility, and responsive behavior."
    - "No database, ingestion, provider, or persistence contract changes."
  artifacts:
    - path: web/src/lib/dashboard.ts
      provides: Pure fail-closed watched-job source classification
    - path: web/src/pages/Dashboard.tsx
      provides: One reusable Dashboard with watchlist and all scopes
    - path: web/src/main.tsx
      provides: Default watchlist route and secondary all-jobs route
    - path: web/src/components/Shell.tsx
      provides: Watchlist Jobs and All Jobs navigation
---

<objective>
Reuse the current Dashboard to make watched-source postings the default while retaining the current combined feed as All Jobs.

Purpose: Prioritize jobs from career URLs the owner intentionally watches without introducing a second backend feed contract.
Output: Source-scoped Dashboard rendering, routes, navigation, and regression coverage.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add a fail-closed watchlist source scope and reusable Dashboard routes</name>
  <files>web/src/lib/dashboard.ts, web/src/pages/Dashboard.tsx, web/src/main.tsx, web/src/components/Shell.tsx</files>
  <action>Add a pure watchlist classifier based only on the existing nested company relationship. Reuse Dashboard through a scope prop, make Watchlist Jobs the index route, retain the existing combined feed at /all-jobs, keep both routes full-width, derive watchlist company options from visible watched rows, and preserve lifecycle queue refill behavior without changing the server RPC.</action>
  <verify>cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint</verify>
  <done>The default route is watchlist-only, All Jobs preserves the combined feed, and no backend contract changes.</done>
</task>

<task type="auto">
  <name>Task 2: Pin source separation, routes, navigation, and regressions</name>
  <files>web/src/lib/dashboard.test.ts, web/src/pages/Dashboard.test.tsx, web/src/components/Shell.test.tsx</files>
  <action>Add pure watched-versus-external row tests, static rendered Dashboard scope tests, and route/navigation/width source tests while retaining the complete existing suite.</action>
  <verify>cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint</verify>
  <done>Automated coverage proves source separation and all existing web tests remain green.</done>
</task>

</tasks>

<success_criteria>
- The default Watchlist Jobs view excludes external rows.
- All Jobs renders both watched and external rows.
- The company filter on Watchlist Jobs contains only watched companies in the bounded result set.
- All existing deterministic ranking and Dashboard behavior remains intact.
- No schema, ingestion, provider, package, or production-data mutation occurs.
</success_criteria>
