---
phase: 03.3-dashboard-filter-refinements
verified: 2026-07-28T21:05:51Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 7/11
  gaps_closed:
    - "Clear all and Select all now use a stable unfiltered Watchlist option source and remain recoverable after the zero-company state."
    - "Watchlist company disabled states and newly refreshed company defaults remain truthful after Clear all."
    - "Current production UAT confirms an explicitly stored empty title-exclusion array survives save, ranking convergence, and reload."
    - "Current production UAT confirms zero-to-three score-tier selections and the required Escape, outside-pointer, and focus behavior."
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "The original filter-v4 semantic-hash worker and mark_recent_jobs_for_refilter wiring remain the active implementation."
    addressed_in: "Phase 03.4"
    evidence: "Phase 03.4 explicitly replaced automatic/background AI scoring with captured deterministic ranking revisions and save_preferences_and_start_ranking while preserving title exclusions and Dashboard controls."
behavior_unverified_items: []
human_verification: []
---

# Phase 03.3: Dashboard Filter Refinements Verification Report

**Phase Goal:** Simplify Dashboard filtering by replacing the required-experience cap with persisted job-title exclusions, adding staged company bulk-selection actions, and consolidating score tiers into one accessible multi-select dropdown.
**Verified:** 2026-07-28T21:05:51Z
**Status:** passed
**Re-verification:** Yes — automated gaps closed by commit `5850bba`; current production behavior accepted in `03.3-UAT.md`

## Verification Scope and Supersession

This report verifies the current repository, not the Phase 03.3 summaries. Plan 04 is explicitly `superseded`: the owner chose mark-and-skip on 2026-07-25, its partial identity record is preserved, and its missing post-release counters and `03.3-UAT.md` are not fabricated or replayed.

The original filter-v4/refilter mechanics were later replaced by Phase 03.4's deterministic-ranking architecture. That replacement is treated as an intentional later-phase implementation change because the user outcome remains title exclusions before ranking, persisted preference revisioning, and atomic feed publication. The later optional maximum-experience field is a scoring signal whose own UI says, “This never filters a job”; it is not the retired Phase 03.2 eligibility cap.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | The active required-experience eligibility cap is retired; persisted title exclusions are the active hard title gate. | ✓ VERIFIED | `cheapFilter` takes no experience cap and runs title exclusions first (`filters.ts:292-299`). The deterministic evaluator runs that hard filter before the later experience scoring row (`deterministic-ranking.ts:443-465,511-524`). The reintroduced preference is explicitly score-only (`Preferences.tsx:427-432`). |
| 2 | Title exclusions are seeded as `president`, `PhD`, bounded to 50 entries/4,096 bytes, and validated across schema/browser/worker boundaries. | ✓ VERIFIED | Migration 0031 supplies the exact default and constraints (`0031_dashboard_filter_refinements.sql:3-7`); browser defaults/validation are in `preferences.ts:6,25-50,190-226`; `filters.ts:182-202` fails closed before tokenization. Boundary tests passed. |
| 3 | A deliberately saved empty exclusion list remains empty through real persistence, convergence, and reload. | ✓ VERIFIED | Source distinguishes `null` from an existing row and forwards `[]` to the atomic RPC (`Preferences.tsx:135-138`; `preferences.ts:431-446`). Current production UAT saved an empty list, waited for convergence, reloaded Preferences, and confirmed defaults did not return. |
| 4 | `president` matches case-insensitive whole words/phrases anywhere, including Vice/Senior Vice President, without matching `presidential`. | ✓ VERIFIED | Literal token-sequence matching is in `filters.ts:156-179,204-227`; the focused test matrix includes President, Vice President, Senior Vice-President, case variants, and the `presidential` negative. |
| 5 | `PhD`, `Ph.D.`, and `Ph D` are equivalent title tokens while doctorate/doctoral remain distinct, and description-only hits do not exclude. | ✓ VERIFIED | The bounded fixed PhD fold is in `filters.ts:204-214`; focused filter tests exercise punctuated, spaced, parenthetical, title-only, and negative variants. |
| 6 | Title exclusions remain provider-neutral and are captured before deterministic ranking publication; preference saves initiate the current atomic rebuild lifecycle. | ✓ VERIFIED | `save_preferences_and_start_ranking` receives the title array (`preferences.ts:431-446`; migration 0032 captures it), the worker passes it to `evaluateDeterministicRanking` (`deterministic-worker.ts:213-235`), and the evaluator hard-filters before scoring. Phase 03.4 release evidence records one real preference run completing revision 2 with 2,116 items and a complete active snapshot. |
| 7 | Clear all and Select all operate on the complete current-company draft and only Show results applies it. | ✓ VERIFIED | Commit `5850bba` adds a separate Watchlist option request with no hidden companies and all score tiers (`Dashboard.tsx:402-410,433-442`). `dashboardWatchlistCompanyOptions` scopes those loaded rows to watched jobs (`dashboard.ts:84-88`), while draft changes remain separate until Show results (`Dashboard.tsx:655-670`). The exact Clear → apply → reopen → Select-all test passed. |
| 8 | Zero selected companies is recoverable, disabled states stay truthful, and newly arriving companies remain selected by default. | ✓ VERIFIED | The named regression test exercises the zero-result state, stable option source, reopened disabled predicates, Select all recovery, and a newly refreshed company remaining visible by default (`dashboard.test.ts:192-241`). It passed in isolation and in the focused suite. |
| 9 | Strong, Good, and Weak are presented in one compact native accessible multi-select instead of standalone tier buttons. | ✓ VERIFIED | One trigger controls a labeled fieldset of native checkboxes with `aria-expanded`, `aria-controls`, Strong/Good/Weak order, 44px rows, and visible focus styles (`Dashboard.tsx:798-848`). Static rendering confirms no standalone pressed tier buttons. |
| 10 | All tiers default selected; zero-to-three states apply immediately, remain session-only, and compose with company filters. | ✓ VERIFIED | Local state, immutable toggling, query-key composition, and no filter persistence are present (`Dashboard.tsx:346-350,393-418,673-681,818-840`; `dashboard.ts:139-152,211-235`). Current production UAT confirms all zero-to-three states plus Escape, outside-pointer, and focus-return behavior. |
| 11 | Result counts, sorting, detail data, lifecycle controls, responsive table, and width persistence remain available. | ✓ VERIFIED | Focused regressions passed, and exact-release Phase 03.4/03.6/03.7 UAT records Dashboard filters, result counts, sorting, detail/apply actions, keyboard access, horizontal scrolling, and resizing. |

**Score:** 11/11 truths verified

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | Original filter-v4 hash/refilter wiring is no longer active | Phase 03.4 | ROADMAP goal and verified implementation replace automatic/background scoring with captured deterministic revisions, atomic preference-save ranking, and a provider-free worker while retaining title exclusions. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `supabase/migrations/0031_dashboard_filter_refinements.sql` | Persisted seeded bounded title exclusions | ✓ VERIFIED | Additive field/default/checks and compatible historical reason are substantive. |
| `supabase/functions/_shared/filters.ts` | Literal title-only gate | ✓ VERIFIED | Validator, tokenizer, PhD fold, code-point detail bound, and first-gate `cheapFilter` wiring are substantive. |
| `supabase/functions/_shared/deterministic-ranking.ts` / `deterministic-worker.ts` | Current title-exclusion consumer | ✓ VERIFIED | Captured real preference data flows through the worker into the hard filter before ranking/staging. |
| `web/src/lib/preferences.ts` / `pages/Preferences.tsx` | Persisted accessible title-exclusion editor | ✓ VERIFIED | Substantive and wired to the current atomic RPC; current production UAT confirms the real empty-save/convergence/reload transition. |
| `web/src/lib/dashboard.ts` | Immutable company/tier helpers | ✓ VERIFIED | Helpers are substantive; pure tests cover immutability and canonical query construction. |
| `web/src/pages/Dashboard.tsx` | Staged company actions and score popover | ✓ VERIFIED | Watchlist options now come from a separate unfiltered/all-tier loaded-row query and are scoped to watched rows; the filtered feed no longer destroys the option universe. |
| `web/src/lib/feed.ts` / current `dashboard_feed_page` SQL | Server-authoritative tier/company query | ✓ VERIFIED | Tier and hidden-company arrays flow to the RLS-scoped RPC, which applies both filters. |
| `scripts/verify-phase-03-3-release.mjs` / test | Fail-closed historical identity verifier | ✓ VERIFIED | Parser/negative paths passed 13/13 locally. Live mode was not replayed because it uses hosted Supabase/Cloudflare/network probes. |
| `03.3-04-ROLLOUT-EVIDENCE.md` | Historical release identity | ⚠️ PARTIAL BY SUPERSESSION | Records migration 0031, worker v9/filter-v4, exact Cloudflare SHA, and matching asset hashes; contains no post-release mutation ledger. |
| `03.3-UAT.md` | Current-release signed-in acceptance | ✓ VERIFIED | Two of two targeted checks pass against deployed commit `3901b90`: score-tier interaction/focus and explicit-empty title-exclusion persistence. The absent historical release ledger remains disclosed rather than reconstructed. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| migration 0031 | atomic preference save | `title_exclude_keywords` column/RPC argument | ✓ WIRED | Browser projection and migration 0032's current save RPC agree on the field. |
| atomic preference save | deterministic ranking worker | captured preference revision | ✓ WIRED | Migration 0032 stores captured exclusions; worker forwards them to the evaluator. |
| evaluator | title hard filter | `cheapFilter` before scoring rows | ✓ WIRED | Title exclusions return an ineligible result before experience/other scoring. |
| Dashboard company controls | server feed and stable Watchlist option source | `appliedHiddenKeys` → feed RPC; independent unfiltered request → option helper | ✓ WIRED | Applied hidden keys affect feed rows, while the separate option query supplies watched-company choices independently of company/tier filters. |
| Dashboard score controls | server feed | `selectedTiers` → query key/RPC `p_tiers` | ✓ WIRED | Changes create a new server query; zero is handled locally by disabling the fetch and rendering zero rows. |
| Phase 03.3 release commit | Cloudflare asset | stored exact SHA and SHA-256 | ⚠️ HISTORICAL IDENTITY ONLY | Identity evidence exists; post-release counters and exact Phase 03.3 UAT do not. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Preferences | `titleExcludeKeywords` | RLS preference row / no-row defaults | Yes | ✓ FLOWING |
| deterministic worker | `captured_title_exclude_keywords` | atomic captured preference revision | Yes | ✓ FLOWING |
| Dashboard tiers | `selectedTiers` | component-local checkbox state → feed RPC | Yes | ✓ FLOWING |
| All Jobs companies | `companyOptionsQuery.data` | unfiltered server company-options RPC | Yes | ✓ FLOWING |
| Watchlist companies | `watchlistCompanyRowsQuery.data?.rows` | separate feed request with no hidden keys and all tiers, then watchlist scoping | Yes, within the bounded loaded-row contract | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Current title exclusions, preferences, deterministic ranking, Dashboard, and feed regressions | `cd web && npm test --` with 9 focused files | 9 files, 522 tests passed | ✓ PASS |
| Historical fail-closed release-evidence contract | `node --test scripts/verify-phase-03-3-release.test.mjs` | 13/13 passed | ✓ PASS |
| Watchlist Clear-all recovery | `cd web && npm test -- src/lib/dashboard.test.ts -t "recovers after Clear all is applied, reopened, and followed by Select all"` | 1 named test passed; 31 unrelated tests skipped | ✓ PASS |
| Live Phase 03.3 identity/post-release verifier | Not run | Network/live probes and obsolete rollout replay are prohibited; evidence is inspected as release-bound history only | ? SKIP |

### Probe Execution

No `probe-*.sh` is declared for this phase. The declared Node release verifier's local test suite was run; its live `--identity`/`--post-release` modes were intentionally not run because they contact Supabase/Cloudflare and the post-release evidence schema is absent.

### Release-Bound Evidence

| Evidence | What it proves | Limitation |
|---|---|---|
| Phase 03.3 identity record (`fd36fb4`) | Migration 0031 parity, score-tick v9/filter-v4 identity, production Cloudflare SHA, and matching asset hashes | No two-save counters and no `03.3-UAT.md` |
| Phase 03.4 exact-release UAT (`7642fde8…`) | Real preference revision converged; Preferences and Dashboard controls rendered and worked generically | Does not state explicit-empty round trip or exact score-popover transitions |
| Phase 03.6 exact-release UAT (`0e24023…`) | Company/tier filters, keyboard reachability, responsive table, resizing, and counts passed | This release still used the independent full company-options query |
| Phase 03.7 exact-release UAT (`baf81fca…`) | Watchlist/All Jobs routing and generic preserved interactions passed | That released source contained the later-fixed Watchlist option-source regression; the checklist did not exercise Clear all → reopen → Select all |
| Current repair (`5850bba`) | Stable unfiltered Watchlist option source plus passing Clear-all recovery regressions | Included in deployed commit `3901b90`; current production UAT completes the remaining Phase 03.3 behavior checks |

### Requirements Coverage

| Requirement | Source Plans | Current Description | Status | Evidence |
|---|---|---|---|---|
| PREF-01 | 01, 02, 04 | Persisted title exclusions plus current optional experience/rubric preferences | ✓ SATISFIED | Schema/browser/current save path are wired, and current production UAT confirms explicit-empty save, convergence, and reload. |
| SCOR-01 | 01, 04 | Deterministic title/location/title-exclusion/literal keyword eligibility | ✓ SATISFIED | Current provider-neutral evaluator runs title exclusions as a hard gate before deterministic ranking publication. |
| SCOR-03 | 01, 03, 04 | Stored six-category deterministic evidence | ✓ SATISFIED | Phase 03.4 implementation, tests, exact release, and UAT verify the current replacement contract. |
| SCOR-04 | 03, 04 | Dashboard feed with company controls, tiers, sorting, and apply path | ✓ SATISFIED | Company Clear-all recovery has a passing named regression, Dashboard table paths pass focused tests, and current production UAT confirms the exact score-popover transition/focus behavior. |

`REQUIREMENTS.md` currently remaps PREF-01 and SCOR-01..04 to Phase 03.4 rather than listing Phase 03.3 separately. All four IDs declared by the Phase 03.3 plans were nevertheless evaluated; no additional Phase 03.3-mapped orphan ID exists.

### Anti-Patterns and Disconfirmation

No unreferenced `TBD`, `FIXME`, or `XXX` marker and no user-visible placeholder/stub was found in the reviewed release-critical files.

The previous disconfirmation finding is closed. Commit `5850bba` replaces the filtered-row Watchlist option source with a separate unfiltered/all-tier request and adds a behavioral test that covers Clear all → Show results → zero rows → reopen → Select all, including search-narrowed targeting and newly refreshed companies. The focused regression suite found no regression in the previously passing truths.

No unreferenced debt marker, user-visible placeholder, hollow option source, or remaining automated blocker was found in the changed files. The separate Watchlist option query remains intentionally bounded to the loaded-row contract established by the later Dashboard scope work.

### Human Verification Completed

Current-release UAT passed both remaining human-only checks: stored-empty title
exclusions survive convergence/reload, and score-tier zero-to-three selection
preserves its Escape, outside-pointer, and focus behavior.

### Gaps Summary

No automated or human-verification gaps remain. Commit `5850bba` closes the
Watchlist company-option source and Clear-all recovery failures; 528 focused
tests pass, and the two targeted production checks pass on deployed commit
`3901b90`.

No obsolete rollout replay or fabricated historical UAT was used. Current
behavior is verified against the repaired production release.

---

_Verified: 2026-07-28T21:05:51Z_
_Verifier: the agent (gsd-verifier)_
