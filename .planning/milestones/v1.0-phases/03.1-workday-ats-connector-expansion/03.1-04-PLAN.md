---
phase: 03.1-workday-ats-connector-expansion
plan: "04"
type: execute
wave: 4
depends_on: ["03.1-03"]
files_modified: []
autonomous: false
requirements: [DISC-09]
must_haves:
  truths:
    - "No production schema, Edge bundle, scheduler state, provider state, auth row, job row, or paid scoring state changes without explicit owner approval."
    - "The owner sees the exact deploy commands, durable effects, temporary effects, cleanup guarantees, and one-paid-call ceiling before deciding."
    - "Declining approval stops the phase with every local implementation and test intact and production untouched."
  artifacts:
    - path: ".planning/phases/03.1-workday-ats-connector-expansion/03.1-04-SUMMARY.md"
      provides: "Explicit owner approval or decline record for the bounded production mutation inventory"
  key_links:
    - from: "03.1-03-SUMMARY.md"
      to: "03.1-04-SUMMARY.md"
      via: "all local code, migration, tests, and verifier gates precede approval"
      pattern: "approval"
---

<objective>
Pause at the production boundary and obtain explicit owner authorization for the exact schema push, Edge deployments, activation windows, provider polling, and disposable-user scoring/feed proof.

Purpose: D-22 requires production schema, deployment, scheduling, live state mutation, and paid verification to remain owner-controlled after all local work passes.
Output: A blocking approval decision whose summary is the prerequisite for every hosted mutation in Plan 05.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-03-SUMMARY.md
@supabase/migrations/0029_paylocity_connector.sql
@scripts/verify-paylocity.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve or decline the bounded production rollout and proof</name>
  <files>None</files>
  <read_first>03.1-03-SUMMARY.md; migration 0029; verifier dry-run output; current git status; linked Supabase migration/function inventory.</read_first>
  <decision>Whether to authorize the exact Phase 03.1 production mutation inventory.</decision>
  <context>All local implementation, focused/full tests, build, lint, migration structural assertions, verifier dry-run, and safety tests must pass first. The approved non-TTY workflow is `cd web &amp;&amp; npx supabase db push --linked`, followed by `npx supabase functions deploy verify-board --project-ref fjcsvajkkztvlrpdplwx` and `npx supabase functions deploy poll-tick --project-ref fjcsvajkkztvlrpdplwx --no-verify-jwt`; hosted proof then uses `node --env-file=scripts/.env --experimental-strip-types scripts/verify-paylocity.ts --resume` across eligible server windows.</context>
  <action>Run read-only preflight only: show the exact implementation commits and dirty-file exclusions; full test/build/lint results; checksum/path for migration 0029; linked migration list proving 0028 is remote and 0029 pending; current verify-board/poll-tick function inventory and JWT posture; exact commands above; and verifier dry-run/safety results. Present the mutation envelope: migration 0029 changes constraints, activation RPC eligibility, and claim authorization; Edge deploys replace verify-board and poll-tick bundles; repeated real-user verification creates or reconciles The Only Facial and three immutable activation observations; activation enables existing scheduled claims; poll-tick makes Paylocity network calls and durably inserts/updates/reopens/closes provider jobs under closure-safe rules; health and heartbeat timestamps advance; the scoring/feed proof pauses/drains and later restores the score cron, creates a disposable auth/profile/preferences/resume/extract/user_jobs set, may make at most one paid OpenAI request against one actual Paylocity job, proves the common feed projection, and deletes all disposable state. State that the exact Paylocity company, activation evidence, legitimate jobs, health, and heartbeats are durable expected effects; temporary claims/cron state require CAS restoration; real-user rows are never modified; SuccessFactors remains evidence-only; Capital One remains untouched except legitimate concurrent polling. Exclude `.DS_Store`, agent-dashboard files, and unrelated user changes from every commit or push.</action>
  <options>
    <option id="approve">
      <name>Approve exact rollout</name>
      <pros>Plan 05 may apply only migration 0029, deploy the two named functions, activate the exact Paylocity identity, and run the bounded proof.</pros>
      <cons>Production will gain durable Paylocity company/evidence/jobs and may incur one paid scoring call.</cons>
    </option>
    <option id="decline">
      <name>Decline production mutation</name>
      <pros>Production remains unchanged while all local implementation and tests are preserved.</pros>
      <cons>Phase 03.1 cannot claim hosted activation, polling, or dashboard delivery.</cons>
    </option>
  </options>
  <verify>
    <automated>git diff --check &amp;&amp; cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; npx supabase migration list --linked &amp;&amp; npx supabase functions list</automated>
    <human-check>Confirm the inventory and explicitly choose approve or decline; silence or ambiguous assent is not approval.</human-check>
  </verify>
  <acceptance_criteria>The checkpoint records an explicit decision after presenting exact commands, durable/temporary mutations, one-paid-call ceiling, cleanup, Workday non-change, and unrelated-file exclusions.</acceptance_criteria>
  <done>Owner types `approve` to authorize only the stated Plan 05 actions, or `decline` to stop before every external mutation.</done>
  <resume-signal>Type `approve` or `decline`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local implementation → hosted Supabase | Schema and Edge deployment changes production authorization and execution. |
| verifier approval → hosted mutations | A vague approval could be misread as authorization for broader or paid actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-17 | Elevation of privilege | production deployment | high | mitigate | Blocking explicit approval after read-only inventory; authorization is limited to migration 0029 and the two named functions. |
| T-03.1-18 | Repudiation | owner decision | high | mitigate | Record exact commands/effects and an unambiguous approve/decline signal in the plan summary. |
| T-03.1-19 | Tampering | unrelated worktree files | medium | mitigate | Name excluded files, stage explicit paths only, and show git status before approval. |
| T-03.1-20 | Financial abuse | paid scoring proof | high | mitigate | Inventory the one-physical-request ceiling and disposable-user scope before approval. |
</threat_model>

## Artifacts this phase produces

| Artifact | New symbol/content |
|---|---|
| `03.1-04-SUMMARY.md` | exact owner approval/decline signal, approved command inventory, and production mutation boundary |

<verification>
All automated preflight is read-only with respect to production. No `db push`, function deploy, verify-board invocation, poll-tick invocation, cron mutation, auth mutation, provider request, or OpenAI request may occur in this plan.
</verification>

<success_criteria>
- The owner can evaluate the complete production and paid-effect envelope before deciding.
- Approval is explicit, scoped, and recorded; decline safely stops the phase.
- Plan 05 cannot execute without the approved Plan 04 summary.
</success_criteria>

<output>
Create `.planning/phases/03.1-workday-ats-connector-expansion/03.1-04-SUMMARY.md` only after an explicit decision.
</output>
