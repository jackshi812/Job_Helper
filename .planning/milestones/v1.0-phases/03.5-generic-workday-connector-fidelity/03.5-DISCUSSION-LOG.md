# Phase 03.5: Generic Workday Connector & Fidelity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-23
**Phase:** 03.5-generic-workday-connector-fidelity
**Areas discussed:** Generalization scope, How a company is added, Ingestion breadth + caps, Activation & health

---

## Generalization scope

| Option | Description | Selected |
|--------|-------------|----------|
| Both known URL shapes | Parse+validate myworkdayjobs.com (subdomain-tenant) and myworkdaysite.com (path-tenant); reject unresolvable | ✓ |
| Fidelity-only hardcode | Add Fidelity as a second hardcoded tenant like Capital One | |
| Fully generic + proxies | Also handle vanity/proxy domains and bot-wall headers | |

**User's choice:** Both known URL shapes
**Notes:** Covers Fidelity + most direct Workday boards without the edge-case cost of proxy/vanity resolution.

---

## How a company is added

| Option | Description | Selected |
|--------|-------------|----------|
| Paste URL, auto-detect | Watchlist paste-URL UI; verify-board detects Workday, parses tenant/region/site | ✓ |
| Migration-seeded | Seed each tenant via migration like Capital One (0028) | |

**User's choice:** Paste URL, auto-detect
**Notes:** Makes future Workday adds a runtime action, consistent with other ATS.

---

## Ingestion breadth + caps

| Option | Description | Selected |
|--------|-------------|----------|
| Whole board, recency fallback | Ingest all; recency-filter if over cap | |
| Whole board, reject if over cap | Ingest all; Degraded if over cap | |
| Facet-scoped roles | Filter to relevant job families at source (like Capital One) | ✓ |

**User's choice:** Facet-scoped roles
**Notes:** Exclude Sales + Customer Service (+ Sales Support) job families. Experience "<3 years" rule added — resolved to downstream enforcement (see below).

---

## Activation & health

| Option | Description | Selected |
|--------|-------------|----------|
| Active, reuse health model | Fidelity Active immediately | |
| Experimental until proven | Land Experimental, promote later | |
| Match Capital One | Same activation model as Capital One (Experimental → Active after 3 clean observations) | ✓ |

**User's choice:** Match Capital One ("like capital one, filter jobs")
**Notes:** User specified filtering rules — no Sales/Customer Service categories; experience required < 3 years.

---

## Experience-filter enforcement (follow-up clarification)

| Option | Description | Selected |
|--------|-------------|----------|
| Post-fetch parse + drop | Parse min-years from JD text, drop 3+ at ingestion | |
| Leave to downstream filters | Ingest all non-Sales/CS; existing dashboard filters apply the <3yr cut | ✓ |
| Drop the rule for now | Only category facet exclusion this phase | |

**User's choice:** Leave to downstream filters
**Notes:** Years-of-experience is not a Workday facet; keeping it downstream avoids fragile free-text parsing and keeps one filtering surface.

---

## Claude's Discretion

- Exclusion mechanism (inclusion-only facets): fetch-whole-board-then-drop vs apply-all-other-family-facets — deferred to research/planner (Fidelity fits under cap, so both viable).

## Deferred Ideas

- Vanity/proxy-domain Workday boards.
- Bot-wall / header-gated tenants.
- Over-cap recency-fallback for large tenants.
- Broadening the Workday pilot beyond Fidelity (folded todo's remaining breadth).
