---
phase: 03.5-generic-workday-connector-fidelity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0035_generic_workday_fidelity.sql
  - web/src/lib/watchlist.ts
  - web/tests/migration-0035-parity.test.ts
autonomous: true
requirements: [connector-expansion]
must_haves:
  truths:
    - "SC3: A new forward-only migration 0035 relaxes the 0028 single-identity guardrail to an allowlist that keeps Capital One admitted byte-identically and adds workday:wd1:fmr:FidelityCareers, without editing any deployed migration"
    - "SC3: 0035 relaxes companies_region_check to admit workday wd1 while still rejecting arbitrary regions"
    - "SC5: 0035 makes Fidelity auto-promote from experimental to active after 3 clean server-timed observation windows, preserving Capital One's promotion"
    - "SC5/SC4: The Fidelity source_coverage_catalog row is UPDATEd (not INSERTed) to experimental with source_key workday:wd1:fmr:FidelityCareers; the Watchlist shows a Fidelity filter note (excludes Sales/Customer Service/Sales Support; experience filtered downstream) and OK/Degraded/Unsupported health badges"
  artifacts:
    - "supabase/migrations/0035_generic_workday_fidelity.sql (forward-only, authored + committed, NOT applied)"
    - "Fidelity filter note in web/src/lib/watchlist.ts activationPresentation keyed on the Fidelity source key"
    - "web/tests/migration-0035-parity.test.ts asserting the SQL contract via ?raw"
  key_links:
    - "0035 companies_workday_identity_check allowlist -> claim_due_companies disjunct -> pollable Fidelity identity"
    - "0035 catalog UPDATE (experimental + source_key + null reason) satisfies truthful_disposition_check + UNIQUE company_name"
  prohibitions:
    - "NEVER edit deployed migrations 0013/0016/0028/0029/0034 — 0035 is forward-only create-or-replace + ALTER only"
    - "DO NOT copy 0028's zero-pre-existing-workday-jobs guard (0028 L9-15) — Capital One is now live in prod; the guard would abort (A5)"
    - "0035 must UPDATE the existing Fidelity catalog row, NEVER INSERT (UNIQUE company_name + truthful_disposition_check)"
    - "Capital One tuple, its catalog row, and its promotion parity assertions MUST stay intact byte-identical"
    - "NO supabase db push / apply in this plan — 0035 is authored and committed only; application is the owner-gated Plan 04"
---

<objective>
Author (do NOT apply) the forward-only migration 0035 that admits the Fidelity Workday identity into the database — relaxing the 0028 single-tenant guardrail to an allowlist, widening the region check to admit wd1, wiring Fidelity into the 3-clean-window auto-promotion, and UPDATEing Fidelity's existing catalog row from unsupported to experimental. Add the Watchlist health/filter presentation for Fidelity.

Purpose: SC3 (DB admits non–Capital-One Workday identities via a forward-only migration) and SC5 (Fidelity activation model + existing health badges). Because of the absolute production boundary, this plan writes and commits the migration and frontend note but never applies/deploys — application is deferred to owner-gated Plan 04.

Output: `0035_generic_workday_fidelity.sql` (authored), the Fidelity filter note in watchlist.ts, and a `?raw` SQL parity test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-CONTEXT.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-RESEARCH.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — migration 0035 parity test (?raw)</name>
  <files>web/tests/migration-0035-parity.test.ts</files>
  <read_first>
    - web/tests/workday-connector.integration.test.ts L545-560 — the existing ?raw SQL parity assertion pattern (import '.sql?raw', regex asserts)
    - supabase/migrations/0028_capital_one_recent_active.sql L9-15 (zero-jobs guard — must NOT be copied), L19-28 (identity check), L35-78 (promotion trigger), L111-120 (catalog parity DO-block)
    - supabase/migrations/0029_paylocity_connector.sql L120 (workday allowed in RPC), L212-216 (stable-promotion set), L244-277 (claim_due_companies), L286-400 (parity DO-block)
    - supabase/migrations/0016 L6-11 (region check), 0013 L16-19/L35-46/L176-184 (catalog regex + truthful_disposition_check + Fidelity row)
    - RESEARCH.md § 4; PATTERNS.md § 0035 migration
  </read_first>
  <behavior>
    - Importing supabase/migrations/0035_generic_workday_fidelity.sql?raw, the SQL text asserts: relaxes companies_region_check to admit workday wd1; relaxes companies_workday_identity_check to an allowlist that still contains the exact Capital One tuple AND workday:wd1:fmr:FidelityCareers; wires Fidelity into 3-window auto-promotion; widens claim_due_companies to admit the Fidelity/allowlist disjunct; UPDATEs (never INSERTs) the Fidelity source_coverage_catalog row to experimental with source_key workday:wd1:fmr:FidelityCareers and null unsupported_reason
    - The SQL text does NOT contain a zero-pre-existing-workday-jobs guard
    - The SQL text preserves the Capital One catalog/promotion parity assertions
  </behavior>
  <action>
    Create the parity test in its own file (avoids contention with Plan 01/02 which edit workday-connector.integration.test.ts). Mirror the ?raw import + regex-assert style from workday-connector.integration.test.ts L545-560. Assert the presence of: the Fidelity source key literal in both the identity allowlist and the catalog UPDATE; an UPDATE ... source_coverage_catalog statement (and the ABSENCE of any INSERT ... source_coverage_catalog for Fidelity); the Capital One tuple still present; region check admitting wd1; and the ABSENCE of a "count(*) ... jobs ... = 0" style workday zero-jobs guard. The migration file does not exist yet, so the import fails — RED.
  </action>
  <acceptance_criteria>
    - `cd web && npm run test -- migration-0035` FAILS because the .sql file does not exist yet (RED confirmed)
    - Test asserts UPDATE (not INSERT) for the Fidelity catalog row and the absence of a zero-jobs guard
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test -- migration-0035 2>&1 | grep -qiE 'fail|cannot find|no such file|0035' && echo RED-OK</automated>
  </verify>
  <done>A dedicated ?raw parity test encodes the 0035 contract and fails pending the migration.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 0035 (forward-only, not applied)</name>
  <files>supabase/migrations/0035_generic_workday_fidelity.sql</files>
  <read_first>
    - supabase/migrations/0016_workday_experimental.sql L6-11 (companies_region_check)
    - supabase/migrations/0028_capital_one_recent_active.sql L9-15 (zero-jobs guard to AVOID), L19-28 (identity check), L35-78 (promote_capital_one_after_observation trigger), L111-120 (catalog parity)
    - supabase/migrations/0029_paylocity_connector.sql L28-32 (provider check), L120 (workday allowed), L136-172 (window logic, 30-min for workday), L212-216 (stable-promotion set), L244-277 (claim_due_companies), L286-400 (parity DO-block)
    - supabase/migrations/0013 L16-19 (careers_url regex), L35-46 (truthful_disposition_check), L176-184 (existing Fidelity unsupported row)
    - RESEARCH.md § 4 + Assumptions A3/A5 + Pitfall 5; PATTERNS.md § 0035
  </read_first>
  <action>
    Write supabase/migrations/0035_generic_workday_fidelity.sql, forward-only (ALTER ... DROP/ADD CONSTRAINT and CREATE OR REPLACE only; never edit 0013/0016/0028/0029/0034). Include: (1) Relax companies_region_check (from 0016 L6-11) to additionally admit workday region matching ^wd\d+$ while keeping the lever-eu and null cases. (2) Relax companies_workday_identity_check (from 0028 L19-28) to an allowlist disjunction that keeps the exact Capital One tuple (board_token capitalone / region wd12 / site_token Capital_One / source_key workday:wd12:capitalone:Capital_One) admitted byte-identically AND adds Fidelity (board_token fmr / region wd1 / site_token FidelityCareers / source_key workday:wd1:fmr:FidelityCareers), both activation_state in ('experimental','active'). (3) Auto-promotion (D-08, A3 — pick option a): add 'workday' to the RPC stable-promotion set (0029 L212-216) so admitted workday tenants promote at progress=3 using the existing 30-min window logic; keep the Capital One promotion trigger intact so Capital One's path is unchanged. (4) CREATE OR REPLACE claim_due_companies (latest def 0029 L244-277) widening the workday disjunct to admit the allowlist/Fidelity tuple. (5) UPDATE (never INSERT) public.source_coverage_catalog WHERE company_name='Fidelity': set disposition='experimental', source_key='workday:wd1:fmr:FidelityCareers', unsupported_reason=NULL, provider='Workday', careers_url='https://wd1.myworkdaysite.com/en-US/recruiting/fmr/FidelityCareers' (must match the 0013 L16-19 regex and satisfy truthful_disposition_check). (6) A parity DO-block mirroring 0029 L286-400 asserting: Capital One tuple still pinned/admitted, region check admits wd1, Fidelity catalog row experimental. DO NOT copy 0028's zero-pre-existing-workday-jobs guard (0028 L9-15) — Capital One is live in prod (A5). Do NOT run supabase db push — author + commit only.
  </action>
  <acceptance_criteria>
    - `cd web && npm run test -- migration-0035` PASSES (GREEN) against the authored SQL
    - File is numbered 0035; no deployed migration file is modified (git status shows only 0035 added)
    - Catalog row is UPDATEd not INSERTed; no zero-workday-jobs guard present
    - Capital One tuple + region wd12 + its source key remain admitted byte-identically
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test -- migration-0035 2>&1 | tail -5; git status --porcelain supabase/migrations | grep -qv '0035' && echo "WARN: other migration touched" || echo "only-0035-added"</automated>
  </verify>
  <done>Forward-only 0035 admits Fidelity + generalizes the allowlist/region/promotion/claim, UPDATEs the catalog row, and passes the ?raw parity test — authored and committed, never applied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fidelity filter note + health badges in watchlist.ts</name>
  <files>web/src/lib/watchlist.ts</files>
  <read_first>
    - web/src/lib/watchlist.ts — CompanyRecord.ats_type union L16, region L18, activationPresentation L258-275 (Capital One filter note keyed on source_key L259-260)
    - PATTERNS.md § watchlist.ts assignments; RESEARCH.md § 6 (activationPresentation)
    - CONTEXT.md D-05/D-07 (excluded families; experience filtered downstream)
  </read_first>
  <action>
    In activationPresentation (L258-275) add a Fidelity filter note parallel to the Capital One one, keyed on row.source_key === 'workday:wd1:fmr:FidelityCareers', describing the kept scope: excludes Sales, Customer Service, and Sales Support families; required-experience is applied by the downstream dashboard filters (D-07), not the connector. Keep the Capital One note (L259-262) byte-identical. Verify ats_type union (L16) already includes 'workday' and region (L18) is string | null (no narrowing needed); if any narrower workday/region literal type exists on this path, widen it to admit wd1. Fidelity reuses the existing OK/Degraded/Unsupported badge presentation — add no new badge logic.
  </action>
  <acceptance_criteria>
    - Rows with source_key workday:wd1:fmr:FidelityCareers show the Fidelity filter note; Capital One rows keep their existing note verbatim
    - No new health-badge component/logic introduced (reuses existing OK/Degraded/Unsupported)
    - region/ats_type types on this path admit the Fidelity identity
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test 2>&1 | tail -5</automated>
  </verify>
  <done>Watchlist presents a truthful Fidelity filter note and reuses existing health badges; Capital One presentation unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB identity guardrail | The allowlist constraint is the DB-tier admission gate (defense-in-depth with edge checks) |
| catalog disposition → frontend truthfulness | source_coverage_catalog disposition must stay truthful (truthful_disposition_check) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-03 | Elevation of privilege | companies_workday_identity_check + claim_due_companies | high | mitigate | Allowlist admits only the exact Capital One + Fidelity tuples; arbitrary tenants remain rejected at the DB tier |
| T-03.5-08 | Tampering | catalog UPDATE truthfulness | medium | mitigate | UPDATE satisfies truthful_disposition_check (experimental requires non-null source_key + null reason); no INSERT (UNIQUE) collision |
| T-03.5-09 | Denial of service | migration abort against live prod | medium | mitigate | Omit 0028's zero-workday-jobs guard (A5) so 0035 does not abort against Capital One's live jobs |
| T-03.5-SC | Tampering | npm/deno installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `cd web && npm run test` full suite green (parity test + watchlist)
- `git status` shows only supabase/migrations/0035_generic_workday_fidelity.sql added; 0013/0016/0028/0029/0034 unmodified
- No `supabase db push` executed; migration authored + committed only
</verification>

<success_criteria>
Maps to ROADMAP SC3 (forward-only migration admits non–Capital-One Workday identities without editing deployed migrations), SC5 (Fidelity activation model + existing health badges), and SC4 (truthful Fidelity filter note; experience filtered downstream).
</success_criteria>

<artifacts_produced>
- NEW `supabase/migrations/0035_generic_workday_fidelity.sql`: relaxed companies_region_check (admits wd\d+), relaxed companies_workday_identity_check (Capital One + workday:wd1:fmr:FidelityCareers allowlist), workday added to RPC stable-promotion set, CREATE OR REPLACE claim_due_companies, UPDATE of the Fidelity catalog row, parity DO-block
- MODIFIED `web/src/lib/watchlist.ts`: Fidelity filter note keyed on the Fidelity source key
- NEW `web/tests/migration-0035-parity.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/03.5-generic-workday-connector-fidelity/03.5-03-SUMMARY.md` when done
</output>
