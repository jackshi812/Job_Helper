---
phase: 03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat
plan: 02
type: execute
wave: 2
depends_on: [03.6-01]
files_modified:
  - supabase/migrations/0037_us_workday_dashboard_queue.sql
  - web/tests/migration-0037-us-workday-dashboard-queue.test.ts
  - web/src/lib/feed.ts
  - web/src/lib/feed.test.ts
autonomous: true
requirements:
  - "Source-coverage extension with a bounded precursor to Phase 4 application tracking; no formal requirement IDs are remapped"
must_haves:
  truths:
    - "D-01/D-03/D-04: the database admits and schedules only the six exact Workday identities, with four new sources independently progressing Experimental to Active."
    - "D-07/D-09/D-10: `applied_at` is RLS-protected, reversible, mutually exclusive with `dismissed_at`, and lifecycle mutations never update/delete shared jobs."
    - "D-12 through D-17: one lifecycle and every active filter are applied server-side before a 200-row limit, with lifecycle-specific stable cursor ordering and no duplicates."
    - "D-18/D-19: the feed API exposes one-row continuation/backfill plus truthful exhaustion metadata."
  artifacts:
    - path: "supabase/migrations/0037_us_workday_dashboard_queue.sql"
      provides: "Forward-only exact Workday DB allowlist/catalog/claims plus lifecycle field, RLS-safe grants, indexes, and dashboard RPCs"
    - path: "web/tests/migration-0037-us-workday-dashboard-queue.test.ts"
      provides: "Raw SQL parity, grant/RLS/constraint, exact identity, pre-limit, and cursor contract assertions"
    - path: "web/src/lib/feed.ts"
      provides: "Typed server-authoritative paged lifecycle feed, company options, cursor validation, backfill, and reversible mutations"
    - path: "web/src/lib/feed.test.ts"
      provides: "Lifecycle/filter/order/cursor/mutation and held-out negative tests"
  key_links:
    - from: "web/src/lib/feed.ts"
      to: "public.dashboard_feed_page"
      via: "validated lifecycle/filter/order/cursor parameters under the authenticated Supabase session"
      pattern: "dashboard_feed_page"
    - from: "authenticated lifecycle helpers"
      to: "public.user_jobs"
      via: "column-limited UPDATE plus own-row RLS; shared public.jobs is read-only"
      pattern: "applied_at|dismissed_at"
---

<objective>
Create the forward-only database and typed feed boundary for exact source admission, mutually exclusive per-user lifecycle state, and stable filtered 200-row Dashboard pages.

Purpose: Make filter-before-limit, RLS, cursor ordering, backfill, and exhaustion server-authoritative before the UI consumes them.
Output: Migration 0037, raw SQL parity coverage, and a tested paged feed API.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-CONTEXT.md
@.planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-PATTERNS.md
@.planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-UI-SPEC.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-03-SUMMARY.md

<interfaces>
`public.user_jobs` owns per-user lifecycle fields under `user_jobs_update_own`. Authenticated writes are column-granted. Migration 0036 owns the current NULL-safe Workday tuple constraint; migration 0035 owns the current observation/claim definitions. `FeedRow` mirrors snake_case database fields. The new page contract returns rows, a validated opaque continuation cursor, and `hasMore`/caught-up state without exposing shared-job mutation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author migration 0037 with exact admission, lifecycle invariants, and RLS-safe paging</name>
  <files>web/tests/migration-0037-us-workday-dashboard-queue.test.ts, supabase/migrations/0037_us_workday_dashboard_queue.sql</files>
  <read_first>
    - supabase/migrations/0019_user_jobs_scoring.sql
    - supabase/migrations/0032_deterministic_ranking.sql
    - supabase/migrations/0035_generic_workday_fidelity.sql
    - supabase/migrations/0036_workday_fidelity_integrity_repair.sql
    - web/tests/migration-0036-workday-fidelity-repair.test.ts
    - web/src/lib/feed.ts
    - .planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-CONTEXT.md
  </read_first>
  <behavior>
    - "Migration is transactional and forward-only; migrations 0019/0032/0035/0036 are not edited."
    - "The installed NULL-safe companies_workday_identity_check and claim_due_companies allow exactly Capital One, Fidelity, Nasdaq, S&P Global, Morningstar, and State Street."
    - "The TypeScript registry, companies_workday_identity_check, source_coverage_catalog, and claim_due_companies share these byte-identical D-01 mappings: workday:wd1:nasdaq:Global_External_Site → tenant nasdaq / region wd1 / site Global_External_Site; workday:wd5:spgi:SPGI_Careers → tenant spgi / region wd5 / site SPGI_Careers; workday:wd5:morningstar:morningstar → tenant morningstar / region wd5 / site morningstar; workday:wd1:statestreet:Global → tenant statestreet / region wd1 / site Global."
    - "Four source_coverage_catalog rows become truthful Workday experimental entries without seeding public.companies or bypassing live verification."
    - "user_jobs.applied_at is nullable; a named CHECK rejects rows where applied_at and dismissed_at are both non-null."
    - "Authenticated update grant is exactly seen_at, dismissed_at, applied_at; own-row RLS remains the authority and anon/public execute is revoked."
    - "dashboard_feed_page filters open/current deterministic eligibility, one lifecycle, tiers, and normalized company selection before p_limit; limit is bounded at 200 and all orderings end with user_jobs.id."
    - "dashboard_company_options sees the complete authorized lifecycle/tier scope before page limiting and keeps hidden companies re-selectable."
  </behavior>
  <action>
    Write the raw SQL test first, then create `0037_us_workday_dashboard_queue.sql`. Use one ordered literal expected table in the test for these exact D-01 mappings and no aliases: `workday:wd1:nasdaq:Global_External_Site` / `nasdaq` / `wd1` / `Global_External_Site` / `https://nasdaq.wd1.myworkdayjobs.com/Global_External_Site`; `workday:wd5:spgi:SPGI_Careers` / `spgi` / `wd5` / `SPGI_Careers` / `https://spgi.wd5.myworkdayjobs.com/SPGI_Careers`; `workday:wd5:morningstar:morningstar` / `morningstar` / `wd5` / `morningstar` / `https://morningstar.wd5.myworkdayjobs.com/morningstar`; `workday:wd1:statestreet:Global` / `statestreet` / `wd1` / `Global` / `https://statestreet.wd1.myworkdayjobs.com/Global`. Read both `supabase/functions/_shared/workday-identities.ts` and the migration as raw test inputs; extract each TypeScript `sourceKey/tenant/region/site/publicBoard` mapping and each SQL constraint, catalog, and claim mapping, serialize the key/tenant/region/site bytes in that order, and require exact string equality across the registry, constraint, catalog, and scheduler plus exact canonical URL equality between registry and catalog. This parity assertion must run without importing TypeScript constants into SQL and must fail for case changes, swapped tenant/region/site values, missing keys, or aliases.

    Extend the 0036 NULL-safe tuple constraint with explicit branches for those four literal mappings and installed-constraint negative probes for null/unknown/lookalike tuples. Rebuild `claim_due_companies` from the latest definition with all six exact keys; preserve existing provider branches and three-window `record_connector_observation`. Insert the four absent source-catalog rows as `experimental` with the literal canonical URLs above, null unsupported reason, truthful read-only evidence, and no company seed. Add `applied_at`, `user_jobs_lifecycle_mutual_exclusion`, lifecycle/cursor indexes, and reissue only the three authenticated lifecycle columns while preserving own-row policies per D-07/D-09/D-10.

    Create `public.dashboard_feed_page` and `public.dashboard_company_options` as `security invoker`, fixed `search_path`, authenticated/RLS-scoped functions. The page function must validate lifecycle (`active|applied|dismissed`), order (`newest|score_desc|score_asc` only where Active), tiers, hidden normalized company keys, cursor object fields/types/version/query signature, and a limit fixed/bounded to 200. Apply lifecycle and deterministic/open/company/tier predicates before the limit. Order Active by the established posted/first-seen or score tuple plus unique `user_jobs.id`; Applied by `applied_at DESC,id DESC`; Dismissed by `dismissed_at DESC,id DESC`. Return one extra internal row or equivalent truthful continuation evidence, never duplicate it into results. Revoke public/anon execution and grant authenticated only. Treat cursor/filter drift as a bounded error, never a fallback first page.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run test -- migration-0037-us-workday-dashboard-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "D-01/D-03/D-04: raw tests assert the four literal source-key/tenant/region/site/URL mappings above, byte-for-byte registry/constraint/catalog/claim parity, all six exact keys, exact scheduling, NULL/unknown rejection, and no seeded company."
    - "D-07/D-09/D-10: tests assert applied_at, named mutual-exclusion CHECK, own-row RLS, exact three-column grant, and no shared jobs UPDATE/DELETE."
    - "D-13 through D-17: tests prove lifecycle/company/tier/deterministic/open predicates precede LIMIT, page size is 200, and every cursor ordering ends with id."
    - "Held-out SQL cases reject anonymous execution, wrong user, invalid lifecycle/order/limit/cursor, mismatched filter signature, and lifecycle timestamps that conflict."
    - "Migration starts with BEGIN and ends with COMMIT; no deployed migration is modified."
  </acceptance_criteria>
  <done>Migration 0037 is locally authored and test-proven, but not pushed; exact admission, lifecycle authorization, and paging invariants are forward-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the typed lifecycle page, cursor, backfill, and mutation API</name>
  <files>web/src/lib/feed.ts, web/src/lib/feed.test.ts</files>
  <read_first>
    - web/src/lib/feed.ts
    - web/src/lib/feed.test.ts
    - supabase/migrations/0037_us_workday_dashboard_queue.sql
    - web/tests/migration-0037-us-workday-dashboard-queue.test.ts
    - web/src/lib/dashboard.ts
    - web/src/pages/Dashboard.tsx
  </read_first>
  <behavior>
    - "Initial and subsequent listFeedPage calls request exactly 200 qualifying rows for one lifecycle/query signature; a backfill call requests exactly one next row through the same server predicate/order."
    - "A validated opaque cursor carries version, lifecycle, order, filter signature, full order tuple, and unique id; malformed, stale, mismatched, oversized, or noncanonical cursors fail closed."
    - "Page append deduplicates by user_jobs.id without reordering already-rendered rows and reports truthful hasMore/nextCursor/caught-up state."
    - "markJobApplied sets applied_at and clears dismissed_at; undoJobApplied clears only applied_at; dismissJob sets dismissed_at and clears applied_at; undismissJob clears only dismissed_at."
    - "safeApplyUrl remains navigation-only and never calls any lifecycle helper."
  </behavior>
  <action>
    Add `applied_at` to list/detail projections and `FeedRow`. Define `LifecycleView`, `DashboardFeedQuery`, `DashboardFeedCursor`, and `DashboardFeedPage` in the data layer. Implement the exported boundaries `listFeedPage`, `listDashboardCompanyOptions`, `encodeDashboardFeedCursor`, `decodeDashboardFeedCursor`, `mergeDashboardFeedPages`, and `backfillDashboardFeedRow`. Use the two migration RPCs for pages and company options so lifecycle, current deterministic/open eligibility, tier, and company selection execute before limit. Encode/decode cursor state with strict schema, bounds, canonical base64url representation, and exact current query-signature comparison; cursor data conveys position only and never authorization. Make `backfillDashboardFeedRow` request one continuation row through the same query and make `mergeDashboardFeedPages` deduplicate IDs while preserving prior order. Keep lifecycle mutations explicitly named `markJobApplied`, `undoJobApplied`, `dismissJob`, and `undismissJob`, with mutually exclusive payloads that throw on errors and RLS-safe `.eq('id', userJobId)` updates only. Keep detail visibility, company truth, deterministic ranking, and HTTPS Apply behavior unchanged per D-05/D-06.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run test -- feed.test.ts migration-0037-us-workday-dashboard-queue.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - "D-06: tests prove safeApplyUrl/opening an Apply href performs no Mark Applied mutation."
    - "D-07/D-09/D-10: exact update payload tests prove mutual exclusion, reversibility, own user_jobs targeting, and no shared job mutation."
    - "D-12 through D-16: RPC argument/call-order tests cover all lifecycle predicates, all filters, fixed 200-row batches, company options before pagination, and lifecycle ordering."
    - "D-17: held-out equal-timestamp/equal-score pages append without duplicate IDs or prior-row reorder; cursor lifecycle/filter/order tampering rejects."
    - "D-18/D-19: one-row backfill and no-cursor caught-up results are explicit and tested."
    - "No schema push, function deployment, web release, or git push occurs in this plan."
  </acceptance_criteria>
  <done>The browser data layer exposes a stable, RLS-backed, filter-before-limit lifecycle queue with exact reversible mutations and truthful pagination/backfill state.</done>
</task>

</tasks>

## Artifacts this phase produces

- New exported constants: `UNITED_STATES_WORKDAY_FACET_ID`, `NASDAQ_WORKDAY_SOURCE_KEY`, `SP_GLOBAL_WORKDAY_SOURCE_KEY`, `MORNINGSTAR_WORKDAY_SOURCE_KEY`, `STATE_STREET_WORKDAY_SOURCE_KEY`.
- New registry contract: immutable `WorkdayCountryScope`/`WorkdayIdentity.countryScope` metadata and four frozen identity objects.
- New adapter mechanism: registered flat/nested country-facet discovery and scoped-count reconciliation.
- New migration/test: `supabase/migrations/0037_us_workday_dashboard_queue.sql` and `web/tests/migration-0037-us-workday-dashboard-queue.test.ts`.
- New database artifacts: `public.user_jobs.applied_at`, `user_jobs_lifecycle_mutual_exclusion`, lifecycle/cursor indexes, `public.dashboard_feed_page(...)`, and `public.dashboard_company_options(...)`.
- New frontend contracts/helpers: `LifecycleView`, `DashboardFeedQuery`, `DashboardFeedCursor`, `DashboardFeedPage`, `listFeedPage`, `listDashboardCompanyOptions`, `encodeDashboardFeedCursor`, `decodeDashboardFeedCursor`, `mergeDashboardFeedPages`, `backfillDashboardFeedRow`, `markJobApplied`, `undoJobApplied`, `dismissJob`, and `undismissJob`.
- New UI state: mutually exclusive Active/Applied/Dismissed view, paged query cache, 10-second applied Undo notice, backfill/retry/exhaustion state.
- New hosted verifier and exact-release inputs/outputs: `scripts/verify-phase-03-6-hosted.mjs`, `03.6-04-RELEASE-MANIFEST.json`, `03.6-04-HOSTED-VERIFICATION.json`, and `03.6-04-ROLLOUT-EVIDENCE.md`.
- New blocking UAT evidence: `03.6-UAT.md` and `03.6-UAT.json`.
- No feature flag, package, tracker row, application stage, note, or resume-tailoring artifact is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated browser → lifecycle RPC/table | User-controlled filters, cursors, IDs, and lifecycle mutations cross into RLS-protected data. |
| Per-user user_jobs → shared jobs | Lifecycle state must never mutate/delete the shared job record. |
| Cursor → ordered SQL window | Cursor position is untrusted and cannot grant authority or change query identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.6-02-01 | Elevation of Privilege | user_jobs lifecycle writes | high | mitigate | Own-row RLS, exact column grants, mutual-exclusion CHECK, authenticated-only execution, cross-user negative tests. |
| T-03.6-02-02 | Tampering | opaque cursor | medium | mitigate | Strict version/type/bounds/canonical encoding plus lifecycle/filter/order signature match; RLS remains independent authority. |
| T-03.6-02-03 | Denial of Service | dashboard RPC limits | medium | mitigate | Fixed maximum 200, bounded arrays/cursor size, indexed deterministic predicates/order, invalid inputs rejected. |
| T-03.6-02-04 | Tampering | shared jobs | high | mitigate | Lifecycle functions and grants touch only user_jobs; migration/source tests reject shared jobs UPDATE/DELETE. |
| T-03.6-02-05 | Information Disclosure | company options/pages | high | mitigate | Security-invoker RPCs with auth.uid/RLS and anonymous/public execute revocation. |
| T-03.6-02-SC | Tampering | dependency supply chain | low | accept | No package install; repository-pinned Supabase/Vitest tooling only. |
</threat_model>

<verification>
- `cd web && npm run test -- migration-0037-us-workday-dashboard-queue.test.ts feed.test.ts`
- `cd web && npm run build`
- Inspect migration contract for exact six-key allowlist, applied_at/RLS/grants, pre-limit predicates, bounded 200, unique cursor tie-breakers, and no shared-job mutation.
- Confirm migration is local-only until Plan 04 exact approval.
</verification>

<success_criteria>
- ROADMAP success criteria 1, 3, 4, 5, and 6 have a server-authoritative local implementation seam.
- D-01, D-03, D-04, D-06, D-07, D-09, D-10, and D-12 through D-19 are covered by executable SQL/data-layer tests.
- Migration 0037 remains unapplied and production stays at accepted source commit `edccac2dd1728a86a8ffa431f4d36bb1e10bff5c`.
</success_criteria>

<output>
Create `.planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-02-SUMMARY.md` when done.
</output>
