---
phase: 03-scoring-feed-notifications
plan: 11
type: execute
wave: 11
depends_on: [03-10]
files_modified:
  - .planning/phases/03-scoring-feed-notifications/03-11-PAID-PROOF.md
  - .planning/phases/03-scoring-feed-notifications/03-UAT.md
autonomous: false
gap_closure: true
requirements: [PREF-01, SCOR-01, SCOR-02, SCOR-03, SCOR-04, SCOR-05]
must_haves:
  truths:
    - "A new paid-run approval discloses the active maintenance latch, its strict maximum TTL/fail-safe, cron defense in depth, one process attempt, at most one owned paid score delta, and no retry."
    - "Exactly one verifier process runs against the exact Plan 03-10 release; its matching run UUID claims exactly two registered fixtures while normal/mismatched claim paths remain empty."
    - "The proof records one owned score-purpose usage delta, zero other paid deltas, a free filtered negative, a fresh positive, exact data/preferences/cron restoration, expired/ended latch, and zero residue."
    - "Human UAT reruns Equity Research across Adzuna, Greenhouse, and Ashby examples, rejects Coffee Distributor and Product Delivery Associate, accepts valid variants, and confirms each displayed row has a truthful nonblank company name in one unified ranked dashboard."
    - "Remaining focused-feed, score quality, detail, dismissal, and notification-absence checks apply to the same immutable release."
  artifacts:
    - path: ".planning/phases/03-scoring-feed-notifications/03-11-PAID-PROOF.md"
      provides: "one process/run identity, latch/TTL state, exact two-row claim set, paid delta, restoration, and release binding"
    - path: ".planning/phases/03-scoring-feed-notifications/03-UAT.md"
      provides: "exact-release rerun of failed relevance/company behavior and remaining Phase 03 user checks"
  key_links:
    - from: "scripts/verify-scoring-freshness.ts"
      to: ".planning/phases/03-scoring-feed-notifications/03-11-PAID-PROOF.md"
      via: "one captured process is transcribed without rerun, including run UUID, TTL, two claimed IDs, one owned delta, cleanup, and cron restore"
      pattern: "invocation_count: 1|maintenance_latch_ended: true|owned_score_usage_delta: 1|other_paid_deltas: 0"
    - from: ".planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md"
      to: ".planning/phases/03-scoring-feed-notifications/03-11-PAID-PROOF.md"
      via: "git, migration, Edge, Cloudflare, and immutable asset identities must match before the one run"
      pattern: "git_sha|cloudflare_deployment_id|asset_sha256"
    - from: ".planning/phases/03-scoring-feed-notifications/03-11-PAID-PROOF.md"
      to: ".planning/phases/03-scoring-feed-notifications/03-UAT.md"
      via: "passing exact-release automated proof unlocks one-at-a-time human UAT"
      pattern: "status: pass"
---

<objective>
Obtain separate approval for exactly one paid latch-backed production proof, run the verifier once without retry, record bounded evidence, and complete human UAT on the same release.

Purpose: the single paid operation must prove production isolation/restoration before the user judges cross-provider relevance, company identity, and focused-feed quality.
Output: one-run paid-proof evidence and completed or newly diagnosed Phase 03 UAT. Verifier source remains read-only.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-scoring-feed-notifications/03-UAT.md
@.planning/phases/03-scoring-feed-notifications/03-10-SUMMARY.md
@.planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md
@scripts/verify-scoring-freshness.ts
@scripts/verify-scoring-freshness.test.mjs
@scripts/verify-scoring-evidence.mjs

<interfaces>
- Exact permitted paid command: `node --env-file=scripts/.env --experimental-strip-types scripts/verify-scoring-freshness.ts`.
- Plan 03-10 evidence must bind migration 0025, score-tick, git SHA, Cloudflare deployment, immutable asset hash, maintenance-latch inventory/TTL, and zero prior verifier/manual-tick/OpenAI runs.
- The verifier pauses the score cron and proves quiescence as defense in depth, then activates the strict-TTL service-only latch with exactly two fixture user_job IDs before signaling. Poll/extract may remain active because the latch blocks no-ID seeding/claims.
- Any process attempt consumes the one authorization whether it passes, fails before OpenAI, or fails during cleanup. There is no automatic retry or second process.
- Separate source pages/tabs remain deferred; human testing covers one unified ranked dashboard and provider metadata never changes ranking/filter control flow.
</interfaces>
</context>

## Phase Goal

**As a** new grad seeking an entry-level job, **I want to** review relevant jobs scored against my preferences and resume in a web dashboard, **so that** I can quickly focus on the strongest opportunities that best fit me.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve exactly one paid latch-backed verifier process</name>
  <files></files>
  <read_first>
    - .planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md (exact live release, latch TTL, and zero prior paid/manual invocations)
    - .planning/phases/03-scoring-feed-notifications/03-10-SUMMARY.md (rollout completion and local safety-test status)
    - scripts/verify-scoring-freshness.ts (read-only one-shot state machine)
    - scripts/verify-scoring-freshness.test.mjs (no-network late-event, cleanup, and one-invocation coverage)
    - supabase/migrations/0025_scoring_freshness.sql (service-only latch and strict maximum TTL)
  </read_first>
  <action>Present the exact deployed git/migration/Edge/Cloudflare/asset identities, local safety-test result, exact command, and expected maximum paid effect of one owned OpenAI score call. Disclose that the verifier pauses/restores score cron as defense in depth, creates exactly two fixture user_jobs, starts a service-only maintenance latch with those IDs before signaling, and relies on its strict maximum TTL if cleanup cannot end it. Explain that while active, normal/no-ID and mismatched claims return zero and do not seed; only the matching run UUID may claim the two fixtures. State that poll/extract may remain active because the latch—not timing—enforces isolation. State that any process start consumes approval and no retry, loop, second tick, fallback, or legacy verifier is authorized. Stop after asking; perform no implementation, invocation, file edit, or outward action in this checkpoint.</action>
  <verify>
    <human-check>Approve or decline one execution of `node --env-file=scripts/.env --experimental-strip-types scripts/verify-scoring-freshness.ts`, with one process attempt, a strict-TTL two-fixture maintenance latch, at most one owned score call, zero other paid deltas, and no retry.</human-check>
  </verify>
  <acceptance_criteria>
    - Exact release, command, maximum paid effect, latch IDs/TTL/fail-safe, cron defense, cleanup, and one-attempt rule are visible.
    - Earlier rollout approval is explicitly insufficient.
    - Task 2 is blocked without the exact paid-run approval.
  </acceptance_criteria>
  <done>The user explicitly approves one paid verifier process attempt, or the plan stops without invocation.</done>
  <what-built>An exact deployed correction and locally safety-tested verifier that has never run against this release.</what-built>
  <how-to-verify>Review the release binding, latch/TTL isolation, exact command, maximum one owned call, and no-retry rule.</how-to-verify>
  <resume-signal>Reply `approve one paid verifier run` to authorize one process attempt, or decline.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run one verifier process and record bounded latch-backed proof</name>
  <files>.planning/phases/03-scoring-feed-notifications/03-11-PAID-PROOF.md</files>
  <read_first>
    - .planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md (release identities and zero-run baseline)
    - scripts/verify-scoring-freshness.ts (read-only executable; do not modify)
    - scripts/verify-scoring-freshness.test.mjs (one-invocation and cleanup expectations)
    - supabase/migrations/0025_scoring_freshness.sql (deployed latch/TTL/claim contract)
    - scripts/.env (variable names and availability only; never print values)
  </read_first>
  <action>Proceed only after Task 1 approval. Perform read-only identity checks and require origin SHA, migration head, deployed score-tick, Cloudflare deployment, immutable asset, and maintenance-latch definitions to equal Plan 03-10 evidence; mismatch blocks without consuming the run. Then start exactly one process with the approved command. Do not wrap it in retry, loop, watch, poller, fallback, or any construct capable of a second execution. Never run the legacy verifier or manually invoke score-tick.

Capture one process start/end, exit status, run UUID, configured/max TTL, two fixture user_job IDs, claimed/input revisions, cron snapshot/pause/quiescence/restore proof, preseed/snapshot/protection counts, late-event isolation results, no-ID/mismatched zero-work results, exact matching controlled claim set, ai_usage before/after IDs/counts, positive/negative outcomes, authenticated write denial, latch end/expiry status, restoration counts, and zero residue. Passing evidence requires exactly one new global purpose=score row owned by the target user and expected model, zero other paid deltas, the negative filtered free, positive freshly scored, exact pre-existing data/preferences restored, temporary seeds/fixtures deleted, latch absent or expired, and cron restored exactly. Write only bounded nonsecret evidence. If process or cleanup fails, record failed status, stop before UAT, and require new diagnosis plus new explicit approval for any future attempt.</action>
  <verify>
    <automated>node scripts/verify-scoring-evidence.mjs --paid .planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md .planning/phases/03-scoring-feed-notifications/03-11-PAID-PROOF.md</automated>
  </verify>
  <acceptance_criteria>
    - Exact release/latch identities match before the one run; mismatch does not consume it.
    - One process and one controlled score-tick invocation occur, with no retry/loop/manual tick/legacy verifier.
    - Active latch admits exactly the two registered fixtures despite late signals/jobs and makes no-ID/mismatched paths return zero.
    - Global ai_usage changes by one owned score row and zero others.
    - Latch end/expiry, row/preference restoration, temporary deletion, zero residue, and exact cron restoration pass without secret/raw-content disclosure.
    - Any failure blocks UAT and never triggers a second attempt.
  </acceptance_criteria>
  <done>One approved verifier process proves two-fixture isolation, one owned paid delta, zero others, exact restoration, and zero residue.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run one-at-a-time UAT on the exact proven unified dashboard</name>
  <files></files>
  <read_first>
    - .planning/phases/03-scoring-feed-notifications/03-11-PAID-PROOF.md (passing proof and exact release identity)
    - .planning/phases/03-scoring-feed-notifications/03-UAT.md (failed Test 2 wording and pending tests)
    - .planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md (production URL/deployment/asset)
    - .planning/phases/03-scoring-feed-notifications/03-08-SUMMARY.md (cross-provider filter and company-ingestion fixtures)
    - .planning/phases/03-scoring-feed-notifications/03-09-SUMMARY.md (unified feed mapping and cache behavior)
  </read_first>
  <action>Present the human checks only after Task 2 is status pass and guide them one at a time. Stop for the user's observation after each check. Perform no implementation, source edit, deployment, function invocation, or automatic pass in this checkpoint. The root UAT workflow may record exact user language in 03-UAT.md; any failure becomes a diagnosed gap and is not repaired here.</action>
  <verify>
    <human-check>
      1. Save Equity Research as the only target title. Immediately return to/refresh Dashboard: Coffee Distributor, Product Delivery Associate, Research Data Analyst, and other unrelated shared-token roles must not appear as current focused matches while refiltering.
      2. Inspect representative Adzuna, Greenhouse, and Ashby results in the one unified ranked dashboard. Valid Equity Research variants from each available source may pass; source metadata must not change matching/ranking, and every displayed row must show a truthful nonblank company name. No separate provider page/tab is expected.
      3. After bounded worker completion, saved chips persist and valid matches show current score, tier, routed resume, and plain-language reasons; no stale reasons return.
      4. Review 20 scored entry-level jobs; at least 16 have useful scores, tiers, and reasons consistent with current preferences/resume.
      5. Open one detail view and confirm JD snapshot, current score/reasons, advisory grouped gaps, covered keywords, routed resume, posted time, truthful company, and safe Apply link.
      6. Confirm Strong/Good defaults, score sorting, All jobs diagnostics, and dismissal/show-dismissed behavior in the unified feed.
      7. Confirm Settings contains no push/email/digest/quiet-hours/alert controls and the browser receives no job alerts.
    </human-check>
  </verify>
  <acceptance_criteria>
    - UAT is bound to the same git/migration/Edge/Cloudflare/asset identities as automated proof.
    - Cross-provider negatives filter free before AI and valid Equity Research variants remain eligible without source-specific control flow.
    - Every displayed Adzuna/Greenhouse/Ashby fixture/result has a truthful nonblank company name in one unified ranked dashboard; unknown names are not fabricated.
    - Separate provider pages/tabs are explicitly deferred.
    - Score quality, detail, focus, diagnostics, dismissal, and notification absence are each judged by the user.
    - Exact observations are recorded; failure routes to diagnosis rather than waiver or inline repair.
  </acceptance_criteria>
  <done>The user passes every applicable Phase 03 check, or supplies the first reproducible failure for a new diagnosed gap cycle.</done>
  <what-built>A release-bound, latch-verified, provider-agnostic unified scoring feed with truthful company identity.</what-built>
  <how-to-verify>Follow the seven checks one at a time on the exact production release and report pass or the first mismatch.</how-to-verify>
  <resume-signal>Reply `pass` for the current check, or describe the first mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| explicit approval -> paid verifier | one authorization permits one process and at most one owned score call |
| verifier -> production latch/data/cron | one-shot proof temporarily controls claimability and score cron |
| provider-normalized rows -> unified feed | untrusted source metadata must not bypass preference filtering or fabricate identity |
| exact release -> human UAT | observations must apply to the same immutable proven deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-3-35 | Repudiation | reused approval | critical | mitigate | Separate checkpoint discloses exact command, one attempt, paid bound, latch, TTL, and no retry. |
| T-3-36 | Denial of Service | verifier cleanup | critical | mitigate | Latch TTL fail-safe, finally end, complete data cleanup, and exact cron restore are passing evidence requirements. |
| T-3-37 | Tampering | organic work claimed/billed | critical | mitigate | Production latch blocks seed/no-ID/mismatch and admits only two registered IDs to exact run UUID. |
| T-3-38 | Spoofing | provider/company presentation | high | mitigate | UAT requires truthful normalized or source-provided nonblank company identity and forbids invented labels. |
| T-3-39 | Tampering | source-specific relevance bypass | high | mitigate | Cross-provider fixtures and UAT prove one post-dedup cheapFilter path before AI. |
| T-3-40 | Information Disclosure | proof record | high | mitigate | Evidence is limited to IDs, revisions, digests, counts, statuses, and release identities. |
</threat_model>

<verification>
Task 1 authorizes no action. Task 2 may run exactly one approved verifier process and only reads its evidence afterward. Task 3 is human-only, sequential, and begins only after automated proof passes.
</verification>

<success_criteria>
- A separate exact approval produces one verifier process attempt and no retry.
- Automated proof shows the latch confined claims to two fixtures, exactly one owned paid delta, zero others, exact restoration, and zero residue.
- The user confirms provider-agnostic relevance and truthful company identity across Adzuna, Greenhouse, and Ashby in one unified dashboard.
- Remaining feed/detail/notification checks pass on the same release, or the first failure becomes a new diagnosed gap.
</success_criteria>

<output>
Create `.planning/phases/03-scoring-feed-notifications/03-11-SUMMARY.md` only after paid proof and human UAT pass. On failure, retain evidence and route to diagnosis without a second verifier run.
</output>
