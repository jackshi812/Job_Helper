---
phase: 02.1-source-coverage-expansion
plan: "13"
type: execute
wave: 3
depends_on: ["02.1-12"]
files_modified: []
autonomous: false
requirements: [PREF-05, DISC-09]
gap_closure: true
must_haves:
  truths:
    - "A human confirms the exact deployed Watchlist is legible in light/dark and narrow layouts."
    - "Representative employer links reach the intended job-search destinations in safe new tabs."
    - "Removal failure is announced inside the modal, focus cannot reach or activate background content while it is open, retry/cancel work, and focus restores without background-refresh disruption."
  artifacts:
    - path: ".planning/phases/02.1-source-coverage-expansion/02.1-13-SUMMARY.md"
      provides: "Recorded Plan 08 D5 human UAT outcome tied to the exact repaired deployment"
  key_links:
    - from: "02.1-12 production SHA/asset"
      to: "human UAT result"
      via: "deployment ID and immutable asset hash recorded with observations"
      pattern: "deployment|sha256"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Complete the remaining Plan 08 D5 human visual, destination, keyboard, focus, and announcement verification on the repaired SHA-proven production release.

Purpose: These judgments cannot be honestly inferred from source strings or automated database checks.
Output: A pass or diagnosed issue record for the exact deployment.

Decision preservation: D-08 through D-14 are judged directly; D-01 through D-07 and D-15 remain covered by automated evidence.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-source-coverage-expansion/02.1-UI-SPEC.md
@.planning/phases/02.1-source-coverage-expansion/02.1-12-SUMMARY.md
@.planning/phases/02.1-source-coverage-expansion/02.1-08-SUMMARY.md
@.planning/phases/02.1-source-coverage-expansion/02.1-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Judge the repaired production Watchlist</name>
  <files></files>
  <read_first>
    - .planning/phases/02.1-source-coverage-expansion/02.1-12-SUMMARY.md
    - .planning/phases/02.1-source-coverage-expansion/02.1-UI-SPEC.md
  </read_first>
  <action>Before presenting the checkpoint, confirm the production deployment ID/SHA/asset hash still match Plan 12 and automated smoke remains green. Then guide the user through the exact checks below one by one. Record observed pass/fail notes without auto-passing visual or assistive behavior. If any check fails, capture the smallest reproducible symptom and route it to diagnosed UAT rather than changing production inside this checkpoint.</action>
  <verify>
    <automated>git rev-parse HEAD && git ls-remote origin refs/heads/main && cd web && npm test && npm run build</automated>
    <human-check>
      1. Open the SHA-proven production `/watchlist`; inspect normal and dark themes for text, borders, badges, links, and focus-ring contrast.
      2. At a narrow mobile-width viewport, focus the labeled Watchlist scroll region and horizontally scroll through all seven columns without lost content.
      3. Open Capital One plus at least two unsupported finance links; each must open the intended employer search page in a new tab with no opener takeover.
      4. Confirm Capital One shows Experimental, 3 of 3 checks passed, Scheduled polling off, and Health OK with a verification timestamp; unsupported rows show Disabled/Unsupported/Not monitored.
      5. Open Remove: initial focus is Keep company. Cause one bounded request failure using browser offline/request blocking, submit Remove, and confirm the failure is announced/visible inside the still-active modal; reconnect and verify retry or Keep company works.
      6. With the Remove modal open, press Tab and Shift+Tab repeatedly and confirm focus cycles only between enabled dialog controls. Try to focus or activate the background Add company, careers URL, table links, and Remove controls by keyboard and pointer: none may receive focus or activation through the full-screen overlay/semantic `aria-modal` boundary. Leave the modal open across a one-minute background refresh and confirm the dialog stays active, focus remains contained, and refreshed background content neither steals focus nor becomes operable.
      7. Cancel and confirm focus returns to the originating Remove button. Leave the page open across another one-minute refresh with no modal and confirm focus does not move and unchanged status is not repeatedly announced.
      8. Spot-check Resume deletion and Settings type-to-confirm so their distinct defaults/copy did not change.
    </human-check>
  </verify>
  <acceptance_criteria>
    - UAT is tied to the exact Plan 12 deployment/SHA/asset hash.
    - Light/dark, narrow scroll, destination quality, activation/health truth, modal failure, Tab/Shift+Tab containment, blocked background focus/activation, overlay/aria-modal boundary, modal-open refresh stability, retry/cancel, focus restoration, background refresh, Resume, and Settings are each explicitly recorded.
    - Failures become diagnosed UAT items; they are not silently waived or auto-fixed after deployment.
  </acceptance_criteria>
  <done>The user responds `pass` for every check, or describes a reproducible failure that is recorded for another gap cycle.</done>
  <what-built>Repaired truthful Experimental health, modal-contained removal errors, bounded provider execution, and non-destructive hosted verification on an exact SHA-proven release.</what-built>
  <how-to-verify>Follow the eight numbered checks in `<human-check>` on the production Watchlist.</how-to-verify>
  <resume-signal>Reply `pass` after all eight checks, or describe the first failing check.</resume-signal>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1; block on high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---:|---|---|
| T-02.1-13-01 | Tampering | mutable production URL -> UAT | high | mitigate | Reconfirm deployment SHA and immutable asset hash before judgment |
| T-02.1-13-02 | Information Disclosure | external employer link -> browser | medium | mitigate | Human destination check plus existing noopener/noreferrer contract |
| T-02.1-13-03 | Denial of Service | modal -> assistive user | medium | mitigate | Failure inside modal, retry/cancel, restoration, and refresh checks |
| T-02.1-13-04 | Repudiation | human evidence | low | mitigate | Record each numbered result against deployment identity |
</threat_model>

<verification>Automated deployment identity/build preflight passes, then all seven human checks are recorded individually.</verification>

<success_criteria>
- The exact repaired release passes D5 visual/interaction judgment.
- Any failure is reproducible and diagnosed rather than waived.
</success_criteria>

## Artifacts this phase produces

- Per-check D5 UAT result tied to production deployment ID, commit SHA, and asset SHA-256.
- Diagnosed UAT entry if any visual, destination, focus, or announcement check fails.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-13-SUMMARY.md` when done.</output>
