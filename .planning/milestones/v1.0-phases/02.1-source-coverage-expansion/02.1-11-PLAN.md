---
phase: 02.1-source-coverage-expansion
plan: "11"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/adapters/smartrecruiters.ts
  - web/tests/adapters.test.ts
autonomous: true
requirements: [DISC-07, DISC-09]
gap_closure: true
must_haves:
  truths:
    - "SmartRecruiters detail fan-out stops at the production defaults of 40 detail requests or 60,000 milliseconds total adapter duration, whichever comes first."
    - "Budget exhaustion returns validated partial jobs with `detail_budget_exceeded`, never closure credibility."
    - "Boards within the budget retain complete credible observations."
  artifacts:
    - path: "supabase/functions/_shared/adapters/smartrecruiters.ts"
      provides: "Deterministic total detail-count and deadline enforcement"
    - path: "web/tests/adapters.test.ts"
      provides: "Count-budget, time-budget, partial-result, and normal-path regressions"
  key_links:
    - from: "SmartRecruitersOptions"
      to: "detail batch loop/request timeout"
      via: "maxDetailRequests=40, totalDurationMs=60_000, concurrency two, and injectable clock"
      pattern: "detail_budget_exceeded"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Close WR-01 by bounding SmartRecruiters detail fan-out across the entire Edge invocation.

Purpose: A large valid board must degrade promptly and retain safe rows rather than being killed by the platform runtime.
Output: Deterministic adapter budget controls and regressions for both exhaustion dimensions.

Decision preservation: D-03, D-04, D-10, D-11, and D-13 govern this fix; all other D-01 through D-15 decisions remain unchanged.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
@.planning/phases/02.1-source-coverage-expansion/02.1-REVIEW.md
@.planning/phases/02.1-source-coverage-expansion/02.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify total SmartRecruiters detail budgets</name>
  <files>web/tests/adapters.test.ts, supabase/functions/_shared/adapters/smartrecruiters.ts</files>
  <read_first>
    - supabase/functions/_shared/adapters/smartrecruiters.ts
    - supabase/functions/_shared/adapters/types.ts
    - web/tests/adapters.test.ts
    - supabase/functions/_shared/adapters/recruitee.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
  </read_first>
  <behavior>
    - With no overrides, a board needing more than 40 detail requests stops before request 41 and returns partial validated jobs with warning `detail_budget_exceeded`.
    - With no overrides, an injected clock reaching the 60,000 ms adapter deadline stops before the next request/batch with the same stable warning.
    - No budget-exhausted result is `complete` or `credibleForClosure`.
    - A small board within both budgets remains complete and credible.
  </behavior>
  <action>Write failing tests for the exact production defaults, count exhaustion, elapsed-time exhaustion, zero extra fetches after exhaustion, retained safe list rows, and the unchanged happy path. Add named production constants `DEFAULT_MAX_DETAIL_REQUESTS = 40` and `DEFAULT_TOTAL_DURATION_MS = 60_000`; extend `SmartRecruitersOptions` with `maxDetailRequests`, `totalDurationMs`, and an injectable monotonic/current-time source so tests may use smaller deterministic overrides without changing production defaults. The 60-second deadline is deliberately below the repository's documented 150-second Supabase Edge wall-clock ceiling, leaving 90 seconds for `poll-tick` database/lifecycle work and concurrent company completion; the 40-request ceiling bounds logical detail fan-out even when responses are fast. Compute one deadline at adapter entry and apply it across list pagination and detail enrichment. Check count and remaining time before each two-wide detail batch and each detail request; cap every provider request's existing 15-second timeout by remaining adapter time and classify adapter-deadline exhaustion as `detail_budget_exceeded`. Preserve detail concurrency exactly two, page size 100, max pages 20, max jobs 1,000, byte limits, and normal retry behavior only while time remains. On exhaustion return `incomplete(jobs, 'detail_budget_exceeded', expectedCount, pageCount)` so closure remains false and validated rows survive. Do not start or retry provider work after the overall budget is exhausted and do not expand any existing cap.</action>
  <verify><automated>cd web && npx vitest run tests/adapters.test.ts</automated></verify>
  <acceptance_criteria>
    - Count and clock budget tests deterministically pass without real sleeping.
    - Tests assert the exact no-override defaults: 40 logical detail requests and 60,000 ms total duration; smaller values are accepted only through explicit test/options overrides.
    - Fetch-call counts prove no new detail batch starts after exhaustion.
    - Exhaustion returns partial/unknown, `credibleForClosure: false`, and exactly the stable warning.
    - Existing SmartRecruiters pagination/schema/detail tests and Recruitee tests pass.
  </acceptance_criteria>
  <done>SmartRecruiters always returns a bounded truthful observation before Edge runtime exhaustion.</done>
</task>

<task type="auto">
  <name>Task 2: Run connector and lifecycle regression gates</name>
  <files>supabase/functions/_shared/adapters/smartrecruiters.ts, web/tests/adapters.test.ts</files>
  <read_first>
    - supabase/functions/_shared/adapters/smartrecruiters.ts
    - web/tests/public-connectors.integration.test.ts
    - web/tests/source-observation.integration.test.ts
    - web/tests/lifecycle.test.ts
  </read_first>
  <action>Run adapter, public-connector, observation, and lifecycle regressions plus the full test/build/lint gate. Confirm the warning remains bounded to 64 characters through `incomplete`, source identity stays SmartRecruiters, safe partial jobs can ingest, and no exhausted observation can close omitted jobs. Do not activate or call the currently unsupported hosted SmartRecruiters tenant; release deployment and hosted smoke remain Plan 12.</action>
  <verify><automated>cd web && npx vitest run tests/adapters.test.ts tests/public-connectors.integration.test.ts tests/source-observation.integration.test.ts tests/lifecycle.test.ts && npm test && npm run build && npm run lint</automated></verify>
  <acceptance_criteria>
    - Focused and full quality gates pass.
    - Stable warning is bounded and no raw response/URL is exposed.
    - SmartRecruiters remains honest unsupported/unclaimable until real access evidence changes.
  </acceptance_criteria>
  <done>The bounded adapter composes with closure-safe lifecycle behavior without changing provider disposition.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1; block on high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---:|---|---|
| T-02.1-11-01 | Denial of Service | provider -> Edge runtime | high | mitigate | Count budget, total deadline, remaining-time request abort, concurrency two |
| T-02.1-11-02 | Tampering | partial provider data -> lifecycle | high | mitigate | Stable warning plus non-credible partial observation |
| T-02.1-11-03 | Information Disclosure | provider response -> logs/UI | medium | mitigate | Stable bounded code only; no response body or URL |
| T-02.1-11-SC | Tampering | dependencies | low | accept | Install nothing |
</threat_model>

<verification>Deterministic budget tests, connector/lifecycle integrations, full tests, build, and lint pass.</verification>

<success_criteria>
- Large/detail-heavy boards terminate within configured total bounds.
- Partial safe jobs survive, but closure never does.
- Normal boards preserve complete credible behavior.
</success_criteria>

## Artifacts this phase produces

- `SmartRecruitersOptions.maxDetailRequests` defaulting to 40, `totalDurationMs` defaulting to 60,000, and an injectable clock.
- Stable `detail_budget_exceeded` observation warning.
- Count/time exhaustion regressions in `web/tests/adapters.test.ts`.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-11-SUMMARY.md` when done.</output>
