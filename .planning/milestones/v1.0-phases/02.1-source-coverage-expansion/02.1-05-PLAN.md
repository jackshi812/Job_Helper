---
phase: 02.1-source-coverage-expansion
plan: "05"
type: execute
wave: 4
depends_on: ["02.1-04"]
files_modified: [web/tests/activation-policy.test.ts, supabase/migrations/0015_activation_windows.sql, supabase/functions/verify-board/index.ts]
autonomous: true
requirements: [DISC-07, DISC-09]
must_haves:
  truths:
    - "Three eligible observations must occur in three database-derived polling windows; replay or multiple calls in one window cannot advance progress."
    - "Only complete credible SmartRecruiters/Recruitee observations auto-promote; Workday is manual and remains Experimental."
    - "Local Vitest proves SQL structure and a pure TypeScript window-math mirror only; hosted PostgreSQL behavior is a blocking Plan 07 proof."
    - "Activation is intentionally real-user-driven: verify-board reuses Plan 02's in-function authenticated-user check, while server evidence and the service-role RPC alone decide progress/promotion."
    - "Each accepted ledger row atomically updates the companies progress fields consumed by Watchlist; ledger count, RPC response, company row, and UI value cannot diverge."
    - "Persisted progress is capped at three for every provider: a fourth distinct Workday window is rejected with no ledger/company/UI change while Workday remains Experimental."
  artifacts:
    - path: "supabase/migrations/0015_activation_windows.sql"
      provides: "Server-time activation evidence and record_connector_observation RPC"
    - path: "web/tests/activation-policy.test.ts"
      provides: "SQL-text assertions and pure-TypeScript window-math mirror; not hosted database behavior"
  key_links:
    - from: "supabase/functions/verify-board/index.ts"
      to: "public.record_connector_observation"
      via: "complete credible server result; no client timestamp"
      pattern: "record_connector_observation"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>Make staged activation server-authoritative and time-separated. Purpose: repeated calls in one instant are not separate polling opportunities. Output: migration/RPC, authenticated-user-driven function wiring, local structural/mirror tests, and an explicit hosted behavioral proof contract for Plan 07. Decisions: D-13, D-14.</objective>

<execution_context>@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/phases/02.1-source-coverage-expansion/02.1-CONTEXT.md
@.planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
@.planning/phases/02.1-source-coverage-expansion/02.1-04-SUMMARY.md
<interfaces>`verify-board` owns live verification and service-role persistence. SmartRecruiters/Recruitee are Experimental after Plan 04. Database time, not request data, owns eligibility.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Write failing temporal activation-policy tests</name>
  <files>web/tests/activation-policy.test.ts</files>
  <read_first>
    - supabase/functions/verify-board/index.ts
    - supabase/migrations/0012_connector_state.sql
    - .planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
  </read_first>
  <behavior>
    - Three calls in one window count once; replayed observation IDs count zero additional times.
    - Partial, unknown, non-credible, warning-bearing, count-inconsistent, disabled, and unsupported observations do not count.
    - Three separate eligible windows promote SmartRecruiters/Recruitee only; Workday never auto-promotes.
    - Accepted evidence atomically updates `companies` activation progress; rejected/replayed evidence changes neither ledger nor company progress.
    - Concurrent RPC/poll claims fail fast under a bounded lock timeout and can be retried; no network call occurs while the company row is locked.
    - Once persisted progress is three, a fourth or later distinct eligible window is rejected for SmartRecruiters, Recruitee, and Workday; ledger count, company progress, and activation state remain unchanged.
  </behavior>
  <action>Specify `record_connector_observation` and its persisted fields before implementation. Because this repository has no local PostgreSQL/Docker runtime, do not fake execution of `security definer` PL/pgSQL or `clock_timestamp()`. Build two honest local layers instead: SQL-text/AST-style assertions for signature, grants, constraints, clock use, locking, provider branches, and the provider-independent persisted-progress-at-three rejection before ledger insert; and a pure TypeScript contract mirror of 10-minute/30-minute window math plus fourth-window cap behavior. Include Workday explicitly: windows one through three may advance progress while leaving it Experimental, but a fourth distinct window changes no ledger/company/UI value. Confirm RED against the absent migration. Plan 07 is the sole behavioral proof of database rejection, locking, clock semantics, and the fourth Workday window.</action>
  <verify><automated>cd web && npx vitest run tests/activation-policy.test.ts</automated></verify>
  <acceptance_criteria>
    - Same-window, replay, partial, and cross-window cases are distinct.
    - Tests assert server-returned `window_start` and `next_eligible_at`.
    - Local structural/pure-contract cases assert progress cannot exceed three for any provider and Workday's fourth distinct window is a no-op rejection while activation remains Experimental.
    - Test names and comments explicitly label SQL checks/mirror math as local structural proof, not PostgreSQL execution.
  </acceptance_criteria>
  <done>The distinct-opportunity policy is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement server-time eligibility and atomic promotion</name>
  <files>web/tests/activation-policy.test.ts, supabase/migrations/0015_activation_windows.sql</files>
  <read_first>
    - web/tests/activation-policy.test.ts
    - supabase/migrations/0012_connector_state.sql
    - supabase/migrations/0008_claim_exclusive.sql
  </read_first>
  <action>Create ordered migration `0015_activation_windows.sql` after Plan 04's public-provider check expansion. Add `connector_observations` with `observation_id`, `company_id`, `provider`, database-generated `observed_at`, `eligibility_window_start`, completeness, credibility, job/expected counts, warning count, and evidence digest; unique observation ID and unique `(company_id, eligibility_window_start)`. Create service-role-only security-definer `record_connector_observation(p_company_id, p_observation_id, p_completeness, p_credible_for_closure, p_job_count, p_expected_count, p_warning_count, p_evidence_digest)` using `clock_timestamp()`: 10-minute windows for SmartRecruiters/Recruitee and 30-minute windows for Workday. Validate inputs before locking, use a short transaction-scoped lock timeout with a stable retryable contention result, lock only the one company row, and perform no network work in the transaction. After the lock, reject immediately when persisted company progress is already three, before inserting any ledger row, for every provider including Workday. Also reject replay/same-window, ineligible state/provider, incomplete/non-credible/warning-bearing/count-inconsistent evidence; count distinct windows; in the same transaction persist the count/last-verified/observation fields on `companies`, atomically promote only SmartRecruiters/Recruitee at three, and return progress/window/next eligibility from that persisted row. Revoke all browser access. Workday progress may reach exactly three but stays Experimental; fourth and later distinct windows change neither ledger nor company state. The ledger is bounded activation evidence, not a per-poll log: accepted rows are capped at three per company, so pruning is unnecessary for this phase. Do not push hosted schema. Addresses review concerns: UI progress persistence, provider-independent cap, Workday fourth-window rejection, lock contention, and ledger retention assumptions.</action>
  <verify><automated>cd web && npx vitest run tests/activation-policy.test.ts && npm run build</automated></verify>
  <acceptance_criteria>
    - No timestamp parameter exists in the RPC.
    - Local tests prove the migration declares unique replay/same-window constraints; Plan 07 must prove those constraints reject writes on hosted PostgreSQL.
    - Promotion requires three distinct eligible windows and is provider-restricted.
    - One transaction keeps ledger count, returned progress, and `companies` progress fields equal; contention fails promptly with no partial write.
    - SQL structure rejects persisted progress `>= 3` before ledger insertion for all providers; local contract coverage proves a fourth Workday window leaves count/progress/Experimental state unchanged.
  </acceptance_criteria>
  <done>Activation progress is atomic, replay-safe, and tied to server time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record only eligible live verification evidence</name>
  <files>web/tests/activation-policy.test.ts, supabase/functions/verify-board/index.ts</files>
  <read_first>
    - supabase/functions/verify-board/index.ts
    - supabase/migrations/0015_activation_windows.sql
    - supabase/functions/_shared/adapters/types.ts
  </read_first>
  <action>Only after Plan 02's bearer resolution returns a real user with role `authenticated`, call the RPC with server-derived complete credible reconciled evidence and a server-generated observation ID; never accept a browser timestamp, credibility flag, or activation target. Missing/malformed/anon/service tokens must exit before service-role client use or RPC invocation. The user initiates the observation, but Edge/service-role and the database clock exclusively decide evidence and promotion. Surface the persisted progress and `next_eligible_at` for resumable verification. Partial/ineligible results update truthful health but never invoke the RPC. Addresses review concern: activation must not reinterpret gateway JWT acceptance as a real authenticated user.</action>
  <verify><automated>cd web && npx vitest run tests/activation-policy.test.ts tests/public-connectors.integration.test.ts && npm test</automated></verify>
  <acceptance_criteria>
    - Function input cannot spoof time or promotion.
    - Tests state the real-user-authenticated request and service-role-only transition boundary and prove anon/service tokens invoke no RPC.
    - Same-window calls return no progress and a future server eligibility boundary.
    - Full regressions pass.
  </acceptance_criteria>
  <done>Only separate complete credible live opportunities can activate public connectors.</done>
</task>
</tasks>

## Review disposition

Migration 0013 is created by parallel Plan 03 while this plan depends on Plan 04 and creates 0015. This is intentionally retained: no hosted push occurs here, Plan 07 blocks until Plans 03/05/06 are complete, and the single linked push applies all migrations lexically as 0012→0016. Therefore no additional dependency on Plan 03 is required merely for file-creation timing.

The observation ledger is intentionally bounded to accepted Experimental activation windows (maximum three successful rows per company), not every scheduled poll, so a pruning job is rejected as unnecessary scope. Lock contention is incorporated through a short timeout/retry contract and receives hosted concurrency proof in Plan 07.

<threat_model>
**ASVS Level:** L1 — opportunistic coverage of primary trust boundaries and high-impact components.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Edge verification → activation RPC | Privileged evidence can enable scheduling. |
| Database clock/window → state | Eligibility and replay decisions are authoritative. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02.1-14 | Spoofing | Client observation time | high | mitigate | No timestamp input; derive windows from `clock_timestamp()`. |
| T-02.1-15 | Tampering / Denial of Service | Replay, over-cap, same-window, or contended calls | high | mitigate | Provider-independent three-row cap, unique IDs/windows, one-row short-timeout lock, retryable contention, eligibility validation, and atomic ledger/company count/promotion. |
| T-02.1-16 | Elevation of Privilege | Auto-promotion | high | mitigate | Service-role RPC and explicit SmartRecruiters/Recruitee allowlist; Workday excluded. |
| T-02.1-SC | Tampering | Supply chain | low | accept | No package change. |
</threat_model>
<verification>Run activation-policy/public-connector/full tests and inspect the RPC signature, unique constraints, grants, clock use, provider-specific promotion branch, and provider-independent progress-at-three guard before insertion. Treat these local results as structural/pure-contract proof only; Plan 07's hosted verifier must behaviorally prove replay rejection, same-window rejection, locking, `clock_timestamp()`, promotion, and fourth-window Workday no-op rejection.</verification>
<success_criteria>- Three distinct server windows are mandatory and accepted progress is capped at three. - Replay/partial/ineligible/over-cap observations never advance. - Workday cannot auto-promote, and its fourth distinct window changes no ledger/company/UI state.</success_criteria>

## Artifacts this phase produces
- `supabase/migrations/0015_activation_windows.sql` — server-time activation evidence/RPC.
- `web/tests/activation-policy.test.ts` — local SQL-structure assertions and pure TypeScript window-math mirror.
- `supabase/functions/verify-board/index.ts` — eligible-evidence recording path.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-05-SUMMARY.md` when done.</output>
