---
phase: 02.1-source-coverage-expansion
plan: "10"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/watchlist.ts
  - web/src/lib/watchlist.test.ts
  - web/src/components/ConfirmDialog.tsx
  - web/src/pages/Watchlist.tsx
  - web/src/components/ConfirmDialog.test.ts
autonomous: true
requirements: [PREF-05, DISC-09]
gap_closure: true
must_haves:
  truths:
    - "Workday Experimental at 3/3 with a successful verification timestamp and no error displays OK using that timestamp while scheduled polling stays off."
    - "Active connector health continues to use scheduled-poll success and is not made healthy by activation state alone."
    - "A failed Watchlist removal is announced inside the active modal and retry/cancel remain usable."
  artifacts:
    - path: "web/src/lib/watchlist.ts"
      provides: "Truthful experimental observation health projection"
    - path: "web/src/components/ConfirmDialog.tsx"
      provides: "Dialog-contained linked removal error alert"
  key_links:
    - from: "CompanyRecord.last_verified_at"
      to: "WatchlistRow.last_success_at/health_state"
      via: "experimental success fallback only when progress is positive and no error exists"
      pattern: "last_verified_at"
    - from: "Watchlist remove mutation error"
      to: "ConfirmDialog role=alert"
      via: "bounded optional error prop and aria description"
      pattern: "role=\"alert\""
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Close CR-02 and WR-02 with the smallest durable client-side health fix and an accessible in-modal removal failure path.

Purpose: Successful unscheduled verification must look healthy without implying scheduling, and destructive errors must be perceivable where focus is trapped.
Output: Tested mapping semantics and a dialog-contained bounded alert.

Decision preservation: D-08 through D-14 are enforced; D-01 through D-07 and D-15 remain unchanged. No migration is needed because `last_verified_at` is already server-owned persisted evidence.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-source-coverage-expansion/02.1-UI-SPEC.md
@.planning/phases/02.1-source-coverage-expansion/02.1-VERIFICATION.md
@.planning/phases/02.1-source-coverage-expansion/02.1-REVIEW.md
@.planning/phases/02.1-source-coverage-expansion/02.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make successful Experimental verification health truthful</name>
  <files>web/src/lib/watchlist.ts, web/src/lib/watchlist.test.ts</files>
  <read_first>
    - web/src/lib/watchlist.ts
    - web/src/lib/watchlist.test.ts
    - supabase/migrations/0015_activation_windows.sql
    - .planning/phases/02.1-source-coverage-expansion/02.1-UI-SPEC.md
  </read_first>
  <behavior>
    - Workday Experimental 3/3, `last_success_at: null`, `last_verified_at` set, `last_error_code: null`, and zero failures maps to health `ok` and displays the verification timestamp.
    - Experimental with an error or failure remains Degraded and retains the approved bounded error copy.
    - Active rows continue to derive freshness from `last_success_at`; Disabled rows remain Unsupported.
    - Activation stays Experimental with `3 of 3 checks passed` and `Scheduled polling off`.
  </behavior>
  <action>Write the failing Workday 3/3 regression first. In the pure mapper, compute a display/health success timestamp for Experimental rows from `last_verified_at` only when activation progress is positive, no bounded error code is present, and failure state is zero; otherwise preserve degradation. Do not change `activation_state`, `scheduled`, SQL claims, `deriveHealth` semantics for Active rows, or use catalog `verified_at` as operational health. Keep the mapped timestamp in the existing row field consumed by `HealthStatus` so its semantic `<time>` automatically shows the verification evidence. This is the smallest durable CR-02 fix and deliberately avoids a schema migration.</action>
  <verify><automated>cd web && npx vitest run src/lib/watchlist.test.ts</automated></verify>
  <acceptance_criteria>
    - The exact Workday 3/3 regression passes with `OK` and the `last_verified_at` value.
    - Error/failure Experimental cases remain Degraded.
    - Active/Disabled and activation-progress regressions remain unchanged.
    - No SQL migration or scheduling/claim behavior changes.
  </acceptance_criteria>
  <done>Successful unscheduled Workday verification is honestly healthy while activation and scheduling remain separate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Announce removal failure inside the active dialog</name>
  <files>web/src/components/ConfirmDialog.tsx, web/src/components/ConfirmDialog.test.ts, web/src/pages/Watchlist.tsx</files>
  <read_first>
    - web/src/components/ConfirmDialog.tsx
    - web/src/pages/Watchlist.tsx
    - web/src/pages/Resumes.tsx
    - web/src/pages/Settings.test.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-UI-SPEC.md
  </read_first>
  <behavior>
    - A rejected removal leaves the modal open and renders `Remove failed: {bounded message}` as `role=alert` inside it.
    - The alert has a stable ID linked through the dialog description; it is not solely rendered behind `aria-modal`.
    - After settlement, Keep company and Remove company are enabled so the user can cancel or retry.
    - Existing Resume confirmation behavior remains unchanged because the new prop is optional.
  </behavior>
  <action>Add a focused component/source contract regression before implementation. Extend `ConfirmDialog` with an optional bounded `errorMessage` prop and render it inside the dialog as a linked `role="alert"`; preserve the body description and append the alert ID to `aria-describedby` only while present. Pass `boundedErrorMessage(removeMutation.error)` from Watchlist while the target dialog is open. The page-level alert may exist only for an error after the modal closes; it must not be the sole channel during the active modal. Preserve initial Keep-company focus, trap, Escape behavior, pending labels, disabled-only-while-pending behavior, and focus restoration. Do not change Resume or Settings destructive defaults.</action>
  <verify><automated>cd web && npx vitest run src/components/ConfirmDialog.test.ts src/lib/watchlist.test.ts src/pages/Settings.test.ts src/lib/resumes.test.ts</automated></verify>
  <acceptance_criteria>
    - Removal failure alert is a descendant of the active dialog and linked semantically.
    - Retry and cancel are usable after a rejected promise settles.
    - Pending state still disables both actions and uses `Removing…` only while pending.
    - Resume and Settings confirmation regressions pass.
  </acceptance_criteria>
  <done>A keyboard or screen-reader user receives the removal failure without leaving the dialog and can recover.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1; block on high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---:|---|---|
| T-02.1-10-01 | Spoofing | database evidence -> UI | high | mitigate | Experimental-only fallback, positive progress, no error/failure; Active remains poll-derived |
| T-02.1-10-02 | Information Disclosure | mutation error -> modal | medium | mitigate | Pass only existing bounded safe message into alert |
| T-02.1-10-03 | Denial of Service | modal focus boundary | medium | mitigate | Alert inside modal; retry/cancel re-enabled after settlement |
| T-02.1-10-SC | Tampering | dependencies | low | accept | Install nothing |
</threat_model>

<verification>Focused health/modal regressions and the full web test/build/lint gate pass.</verification>

<success_criteria>
- Workday Experimental 3/3 displays OK with verification time and polling off.
- Active, degraded, and unsupported semantics do not regress.
- Removal failure is announced inside the active modal with usable recovery actions.
</success_criteria>

## Artifacts this phase produces

- Experimental observation-success timestamp projection in `mergeCoverageRows`.
- Optional `ConfirmDialogProps.errorMessage` and linked alert ID contract.
- `web/src/components/ConfirmDialog.test.ts` regressions for modal-contained errors.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-10-SUMMARY.md` when done.</output>
