---
phase: 02.1-source-coverage-expansion
plan: "07"
type: execute
wave: 6
depends_on: ["02.1-03", "02.1-05", "02.1-06"]
files_modified:
  - scripts/verify-source-coverage.ts
  - scripts/verify-watchlist.ts
  - scripts/verify-pipeline.ts
autonomous: true
requirements: [PREF-05, DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "Migrations 0012 through 0016 are pushed in order before any hosted source-coverage assertion runs."
    - "Every still-feasible public connector gets evidence from three distinct database-time windows; SmartRecruiters instead becomes catalog-only `unsupported_with_reason` if anonymous access has closed, while Workday remains Experimental."
    - "On SmartRecruiters authentication/prohibition fallback, the service-role verifier upserts bounded canonical/provider evidence into `source_coverage_catalog`, proves the row unclaimable and Not monitored, and restores prior state if verification aborts."
    - "Hosted controlled failed observations retain a sentinel job, skip closure, expose bounded detail, and cannot be repaired by Adzuna; fixture integration covers partial/implausibly-empty classes."
    - "The 60–90 minute soak is completed through repeated short `--resume` invocations that each exit after the current opportunity; no process sleeps through the soak."
    - "All twelve finance catalog entries exist with canonical links/evidence/disposition, and unsupported rows are never claimable or monitored."
    - "The Phase 2 watchlist and pipeline verifiers remain rerunnable after migration 0012: no authenticated company insert/restore remains, no real seed is deleted without a guaranteed restore, and both pass hosted."
    - "Hosted anon-key, service-role bearer, malformed bearer, and expired bearer calls to verify-board return 401/403 with deterministic zero-provider-call evidence and unchanged RPC/ledger/company baselines; a real authenticated user is the positive control."
    - "Capital One's Experimental company row is created through real-user verify-board, remains unclaimable, and produces no Workday job rows in this phase."
    - "A fourth distinct Workday window is rejected after progress reaches three; ledger count, company/UI progress, and Experimental activation remain unchanged."
  artifacts:
    - path: "scripts/verify-source-coverage.ts"
      provides: "Resumable non-destructive hosted connector, activation, catalog, authorization, and degradation proof"
      exports: ["run"]
    - path: "scripts/verify-watchlist.ts"
      provides: "Post-0012 shared-watchlist verifier using server-authoritative/service-role-safe fixtures"
    - path: "scripts/verify-pipeline.ts"
      provides: "Post-0012 existing-provider pipeline verifier without authenticated company inserts"
  key_links:
    - from: "scripts/verify-source-coverage.ts"
      to: "public.record_connector_observation"
      via: "verify-board results plus server-returned window_start/next_eligible_at; no client timestamps"
      pattern: "next_eligible_at"
    - from: "scripts/verify-source-coverage.ts"
      to: "hosted poll-tick"
      via: "invocation-scoped degradation probes with restoration in finally"
      pattern: "poll-tick"
    - from: "scripts/verify-watchlist.ts"
      to: "supabase/functions/verify-board/index.ts"
      via: "real-user seed/re-add flow; service role is limited to uniquely marked disposable fixture setup/cleanup"
      pattern: "verify-board"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Deploy and prove the backend source-coverage slice against the linked Supabase project before any production frontend acceptance.

Purpose: Fixtures cannot prove ordered migrations, hosted RLS, live provider contracts, server-time activation windows, or non-destructive degradation.
Output: Resumable hosted verifier, blocking schema push, affected function deployments, and hosted evidence across real polling opportunities.

Decisions: D-01 through D-15.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.1-source-coverage-expansion/02.1-CONTEXT.md
@.planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
@.planning/phases/02.1-source-coverage-expansion/02.1-03-SUMMARY.md
@.planning/phases/02.1-source-coverage-expansion/02.1-05-SUMMARY.md
@.planning/phases/02.1-source-coverage-expansion/02.1-06-SUMMARY.md

<interfaces>
`record_connector_observation` derives eligibility from the database clock and returns `window_start`, `next_eligible_at`, progress, and activation state. `scripts/verify-watchlist.ts` and `verify-pipeline.ts` provide required-env, isolated-client, PASS/FAIL, cleanup, and top-level-await conventions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a resumable non-destructive hosted verifier</name>
  <files>scripts/verify-source-coverage.ts, scripts/verify-watchlist.ts, scripts/verify-pipeline.ts</files>
  <read_first>
    - scripts/verify-watchlist.ts
    - scripts/verify-pipeline.ts
    - supabase/functions/verify-board/index.ts
    - supabase/migrations/0012_connector_state.sql
    - supabase/migrations/0013_source_coverage_catalog.sql
    - supabase/migrations/0014_public_connectors.sql
    - supabase/migrations/0015_activation_windows.sql
    - supabase/migrations/0016_workday_experimental.sql
  </read_first>
  <behavior>
    - `--dry-run` validates every probe and the exact twelve-company matrix without secrets, network, or database access.
    - `--resume` reads durable observation progress from hosted rows, records at most the current eligible server window, prints `PENDING next_eligible_at` or `COMPLETE`, and exits promptly.
    - The executor re-invokes `--resume` across the 60–90 minute wall-clock soak; the script never blocks or sleeps until the next 10/30-minute window.
    - Invocation-scoped failure probes restore every temporary mutation in `finally` and compare pre/post integrity baselines.
    - SmartRecruiters authentication/prohibition creates or updates a bounded catalog evidence row through service role; an aborted run restores the previous row/removes a newly staged row, while successful COMPLETE retains the honest unsupported evidence.
    - `verify-watchlist.ts` and `verify-pipeline.ts` contain no authenticated `companies` insert/update/restore path after 0012; real seeds use verify-board and uniquely marked disposable rows use the narrowly scoped service-role verifier client.
    - Startup removes only stale, exact-prefix Phase 02.1 probe rows older than the declared two-hour TTL; every invocation also cleans its own unique company/job markers in `finally`.
    - The controlled Recruitee failure assertion accepts either the verifier invocation or live cron as claimant and validates resulting state, then recovers/validates heartbeat without misreporting the controlled failure as a production incident.
    - Anon-key, service-role bearer, malformed bearer, and expired bearer each return 401/403 and reach zero provider fetches, RPCs, ledger inserts, or company writes; a real user reaches exactly one controlled provider verification as positive control.
    - In-function rejections report the instrumented handler stage/provider-fetch count; gateway-rejected malformed/expired calls show the gateway rejection signature and no handler-entry header, deterministically proving the provider wrapper was unreachable.
    - Concurrent activation calls complete or fail quickly with the retryable lock result, and accepted progress agrees across ledger, RPC response, company row, and Watchlist mapper; a fourth distinct Workday window changes none of those values and leaves Experimental unchanged.
  </behavior>
  <action>Create `scripts/verify-source-coverage.ts` with exported `run`, top-level await, PASS/FAIL accumulator, `--dry-run`, and idempotent `--resume`. Treat the bounded activation ledger as durable progress; never insert synthetic windows or pass timestamps. Each invocation records at most one eligible observation per connector, prints machine-readable PENDING/COMPLETE, and exits promptly. Before the linked push, migrate both Phase 2 verifiers: add a narrowly scoped service-role client to `verify-watchlist.ts` for exact-prefix disposable fixture setup/cleanup only, use real-user `verify-board` for seed creation/recreation, move the shared-delete probe to a disposable row, and prohibit deleting any pre-existing seed; update `verify-pipeline.ts` so `ensureSeeds` consumes verify-board's persisted row and no authenticated company insert/update remains. Both scripts must snapshot seed IDs/job counts, fail before destructive action if restoration cannot be guaranteed, and use `finally` cleanup. In the new verifier, run a raw hosted authorization matrix against the same safe existing-provider URL using unique correlation IDs: public anon-key JWT, service-role bearer, malformed bearer, and a deterministically expired signed user JWT. Snapshot company rows, activation fields, RPC-visible progress, and ledger counts first. Every invalid case must return 401/403; anon/service cases that enter the function must report the bounded auth-rejected stage and provider-fetch count zero from Plan 02's sole instrumented wrapper, while malformed/expired gateway rejections must have the gateway rejection signature and no handler-entry header. This distinction is the deterministic zero-provider-call proof, not an inference from database state. Assert all baselines unchanged and no correlation ID reaches a provider-call stage. Then send a fresh real authenticated-user token to the same controlled URL and require verified stage, exactly one provider-wrapper call, and the expected authoritative response as the positive control. Own SmartRecruiters fallback as already specified. If pinned UTURN Recruitee disappears, never substitute silently: persist bounded platform evidence with no schedulable row and stop at a blocking decision checkpoint for one explicitly evidenced replacement tenant or explicit D-04 unsupported acceptance. Create/reconcile Capital One only through real-user verify-board, collect exactly three accepted windows, prove it stays Experimental/unclaimable with zero Workday jobs, then at the fourth distinct server window call the RPC again and prove rejection leaves ledger count three, company/UI progress three, last-verified fields, and Experimental activation unchanged. For the failure probe, generate invocation-unique company/source/job markers and a two-hour TTL; on startup delete only stale exact-prefix probe fixtures left by crashed runs, then create the active reserved Recruitee row and sentinel. Reset `last_polled_at` immediately before poll-tick, but treat either the direct invocation or live cron as the claimant: observe bounded resulting row state and assert failure metadata advanced, last success unchanged, and sentinel open regardless of claimant. Acknowledge the controlled all-failed heartbeat/banner effect, delete the probe, invoke/observe a healthy no-work recovery tick, and assert heartbeat is not left stale; never overwrite a newer concurrent heartbeat. Clean current markers in `finally` and prove no orphan fixtures remain. Hosted activation probes must assert ledger count = RPC progress = `companies` progress = Watchlist mapper value, replay changes none, and concurrent calls either serialize quickly or return the stable retryable contention result within the lock budget. Compare the script's twelve-company matrix to actual catalog rows as a named PASS/FAIL. Record SuccessFactors as platform-level documented unsupported evidence in the SUMMARY (with research citations) and prove it is absent from executable unions/claims; do not add a synthetic company to the fixed D-05 matrix. Preserve exact provider pagination reconciliation and strict warning rejection. Never call unsupported finance hosts, bypass WAF/CAPTCHA/login, or log sensitive bodies/URLs. Addresses all hosted authorization, deterministic provider-call evidence, Workday cap, verifier-regression, race/crash, progress, Recruitee, Capital One, SuccessFactors, TTL, heartbeat, and contention review findings.</action>
  <verify><automated>node --experimental-strip-types --check scripts/verify-source-coverage.ts && node --experimental-strip-types scripts/verify-source-coverage.ts --dry-run</automated></verify>
  <acceptance_criteria>
    - Dry-run exits nonzero for any missing/invalid probe definition and makes no external call.
    - Resume behavior derives progress and eligibility only from hosted state/server responses and exits with PENDING/COMPLETE rather than waiting across windows.
    - All mutations are invocation-scoped and restored in `finally`.
    - Both legacy verifiers have explicit post-0012 write ownership, never delete a real seed for a permission probe, and contain no authenticated company insert/update/restore.
    - No invocation blocks across an eligibility window and no client timestamp reaches activation persistence.
    - The disposable failure row/sentinel strategy is deterministic, uniquely named, integrity-checked, and restored in `finally`.
    - Startup stale-probe cleanup is exact-prefix plus age bounded; cron races are claimant-agnostic; heartbeat recovers; no marked rows/jobs remain.
    - All four invalid bearer classes have explicit 401/403, zero-provider-call instrumentation/gateway evidence, zero RPC/ledger/company write, and unchanged-baseline assertions; the real-user case proves exactly one controlled provider call.
    - A fourth distinct Workday window is explicitly defined and must leave ledger count three, company/UI progress three, and Experimental activation unchanged.
    - SmartRecruiters fallback has an executable service-role catalog upsert, bounded fields, no pollable source key/company, unclaimable/Not monitored assertions, and abort-safe restoration.
  </acceptance_criteria>
  <done>The hosted proof can survive interruption and is safe to run repeatedly across real eligibility windows.</done>
</task>

<task type="auto">
  <name>Task 2: Push all migrations and deploy affected Edge Functions</name>
  <files>scripts/verify-source-coverage.ts</files>
  <read_first>
    - supabase/migrations/0012_connector_state.sql
    - supabase/migrations/0013_source_coverage_catalog.sql
    - supabase/migrations/0014_public_connectors.sql
    - supabase/migrations/0015_activation_windows.sql
    - supabase/migrations/0016_workday_experimental.sql
    - supabase/config.toml
    - scripts/verify-source-coverage.ts
  </read_first>
  <action>Confirm every implementation plan is committed, both legacy verifier rewrites from Task 1 are present, and no migration edit remains; run their syntax/dry local gates plus the full local test/build/lint gate, then run `cd web && npx supabase db push --linked` as the blocking schema gate. If authentication fails, create a dynamic authentication checkpoint and retry; never substitute manual SQL. Confirm the remote migration list includes 0012 connector state, 0013 catalog, 0014 public connector checks, 0015 activation windows, and 0016 Workday in order. Deploy `verify-board` with gateway JWT enforcement plus its mandatory in-function real-user check, and `poll-tick` with its existing cron-secret boundary. Do not run any hosted assertion before schema push and deployments succeed.</action>
  <verify><automated>cd web && npx supabase migration list --linked && npx supabase functions list</automated></verify>
  <acceptance_criteria>
    - The required `supabase db push` occurs after every migration edit.
    - Remote migration order matches local 0012 through 0016.
    - verify-board remains JWT-protected and poll-tick remains fail-closed on the cron secret.
    - The two Phase 2 verifier scripts are migrated before 0012 reaches hosted execution.
  </acceptance_criteria>
  <done>The linked backend contains the complete reviewed source-coverage implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Gather hosted evidence across distinct server windows</name>
  <files>scripts/verify-source-coverage.ts</files>
  <read_first>
    - scripts/verify-source-coverage.ts
    - supabase/migrations/0015_activation_windows.sql
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Over approximately 60–90 minutes, invoke the verifier repeatedly with gitignored credentials and `--resume`, once per short agent turn or command, and stop each invocation when it reports PENDING or COMPLETE; never wrap it in one blocking shell loop/process. Re-invoke at or after the database-reported `next_eligible_at`. Gather three eligible windows for Recruitee unless its pinned tenant triggers the Task 1 decision checkpoint, and only if anonymous access remains a safe public contract for SmartRecruiters before asserting Active; otherwise retain the honest unsupported evidence. Create/reconcile Capital One through real-user verify-board, gather exactly three accepted Workday windows, assert it remains Experimental/unclaimable with zero Workday jobs, then re-invoke in a fourth distinct Workday window and prove ledger/RPC/company/UI progress remains three and activation/last-verified state is unchanged. Prove hosted PostgreSQL replay/same-window rejection, bounded lock contention, server windows, and atomic equality across ledger/RPC/company/UI progress. Execute the full raw bearer matrix from Task 1: anon-key, service-role bearer, malformed bearer, and expired bearer each return 401/403 with unchanged baselines; assert the request-scoped provider-fetch counter is zero for in-handler rejections or the gateway signature plus absent handler-entry marker for gateway rejections, and assert the real authenticated positive control reaches exactly one controlled provider call. Run the claimant-agnostic, crash-cleanable Recruitee failure/sentinel/heartbeat recovery strategy and the explicit catalog-vs-script matrix check. Then rerun `verify-watchlist.ts` and `verify-pipeline.ts` hosted to prove Greenhouse/Lever/Ashby behavior and safe seed integrity after 0012. After COMPLETE, invoke once more to prove idempotence and no duplicate/replay progress. Keep the approved Adzuna cadence unchanged.</action>
  <verify><automated>node --env-file=scripts/.env --experimental-strip-types scripts/verify-source-coverage.ts --resume && node --env-file=scripts/.env --experimental-strip-types scripts/verify-watchlist.ts && node --env-file=scripts/.env --experimental-strip-types scripts/verify-pipeline.ts && cd web && npm test && npm run build && npm run lint</automated></verify>
  <acceptance_criteria>
    - The execution record shows repeated short PENDING invocations across 60–90 minutes, followed by COMPLETE and one short idempotent COMPLETE re-run; no single long-running soak command exists.
    - Three counted observations per connector have three distinct server window starts.
    - Recruitee is Active or the explicit no-substitution decision checkpoint records its D-04 disposition; SmartRecruiters is Active after three windows or has bounded catalog-only unsupported evidence; Workday remains Experimental/unclaimable with zero job rows.
    - Unsupported catalog rows are never labeled monitored or sent to dispatch.
    - Both Phase 2 hosted verifiers pass after 0012 and seed IDs/job links remain intact.
    - Anon/service/malformed/expired bearer probes each return 401/403, prove zero provider call through instrumentation or gateway non-entry evidence, and produce no RPC/ledger/company write; the real-user positive control reaches exactly one controlled provider call.
    - The fourth distinct Workday-window call leaves ledger count, RPC/company/UI progress, last-verified fields, and Experimental activation unchanged.
    - Contention terminates within budget and ledger/RPC/company/UI progress is equal.
  </acceptance_criteria>
  <done>The hosted backend satisfies all connector, degradation, activation, catalog, and authorization contracts.</done>
</task>

</tasks>

## Review disposition

Migration 0012's legacy-verifier regression is incorporated by modifying and rerunning both affected scripts before the only linked push. The failure fixture is TTL-marked, startup-cleanable, claimant-agnostic, heartbeat-aware, and finally-cleaned. Exact pagination and warning strictness remain fail-closed by design. SuccessFactors stays documentation-only because no real representative exists; its cited unsupported evidence is recorded in the SUMMARY and its absence from executable unions/claims is proven instead of inventing a catalog company. Observation-ledger pruning is rejected because Plan 05 bounds accepted rows to three per Experimental company. The existing nine-minute due threshold is accepted as the intended roughly ten-minute post-promotion cadence for documented public connectors; Workday remains unscheduled.

<threat_model>
**ASVS Level:** L1 — opportunistic coverage of primary trust boundaries and high-impact components.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI → hosted Supabase | Privileged schema/function deployment mutates hosted state. |
| Verifier → hosted services/providers | Credentials and controlled fixtures cross multiple boundaries. |
| Database time → activation | Server windows decide eligibility and scheduling promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-20 | Tampering | Schema deployment | high | mitigate | Blocking ordered db push, remote migration confirmation, and no manual SQL fallback. |
| T-02.1-21 | Spoofing / Tampering | Activation evidence | high | mitigate | Server time only, unique windows/IDs, provider-independent cap at three, fourth-Workday-window no-op proof, resumable hosted-ledger reads, and idempotence proof. |
| T-02.1-22 | Elevation of Privilege | Hosted state | high | mitigate | Independent browser/service-role probes and active-only claim verification. |
| T-02.1-29 | Elevation of Privilege | verify-board bearer handling | high | mitigate | Hosted anon/service/malformed/expired 401/403 matrix, instrumented zero-provider-call or gateway non-entry evidence, unchanged RPC/ledger/company baselines, and real-user exactly-one-call positive control. |
| T-02.1-30 | Tampering / Denial of Service | Disposable failure fixture and live cron | high | mitigate | Unique TTL markers, startup/finally cleanup, claimant-agnostic assertions, bounded retries, and heartbeat recovery without overwriting newer state. |
| T-02.1-23 | Information Disclosure | Verifier logs | medium | mitigate | Gitignored env, redaction, bounded output, and no bodies/query strings/applicant links. |
| T-02.1-24 | Denial of Service | Long soak | medium | mitigate | Bounded provider calls, prompt PENDING/COMPLETE exits, repeated short invocations over 60–90 minutes, and restart-safe hosted progress; never one blocking soak. |
| T-02.1-SC | Tampering | Deployment dependencies | low | accept | Install nothing; use pinned project CLI/tooling. |
</threat_model>

<verification>Local tests/build/lint pass; remote migrations/functions match; the execution log shows repeated short resumable invocations through COMPLETE plus one idempotent re-run; hosted rows prove distinct server windows, replay/same-window rejection, controlled failure retention, and provider-specific activation.</verification>

<success_criteria>
- The single blocking schema push precedes every hosted assertion.
- Hosted activation cannot be achieved through replay, same-window calls, or spoofed time.
- Hosted authorization proves all four invalid bearer classes reach zero provider/RPC/ledger/company work, with a real-user exactly-one-provider-call control.
- Workday accepts at most three windows; its fourth distinct window is a state-preserving rejection while Experimental.
- Finance catalog, failure retention, authorization, and existing providers pass non-destructive proof.
- Phase 2 verifier scripts remain rerunnable after 0012 with no authenticated company writes or seed-loss path.
</success_criteria>

## Artifacts this phase produces

- `scripts/verify-source-coverage.ts` — resumable/idempotent hosted proof driver.
- `scripts/verify-watchlist.ts` — post-0012 shared-watchlist verifier with safe disposable/service-role ownership.
- `scripts/verify-pipeline.ts` — post-0012 pipeline verifier using server-authoritative seed creation.
- Hosted migrations 0012–0016 and deployed `verify-board`/`poll-tick`.
- Three distinct server-window observations for each activated/experimental representative connector.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-07-SUMMARY.md` when done.</output>
