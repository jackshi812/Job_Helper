---
phase: 02.1-source-coverage-expansion
plan: "04"
subsystem: public-connectors
tags: [supabase, edge-functions, smartrecruiters, recruitee, vitest, postgres]

requires:
  - phase: 02.1-source-coverage-expansion
    provides: Closed connector registry and closure-safe PollObservation contract from Plans 01-02
provides:
  - Strict SmartRecruiters and Recruitee URL identities shared by verification and polling
  - Bounded public adapters with exact reconciliation and closure-ineligible partial evidence
  - Closed company/job source constraints and transactional write-path parity probes
  - Active-only exact dispatch with unknown and non-active connectors rejected before fetch
affects: [02.1-05-activation, 02.1-07-hosted-proof, public-source-coverage]

tech-stack:
  added: []
  patterns:
    - Provider responses become bounded PollObservations instead of trusted job arrays
    - Required detail fan-out runs at per-host concurrency no greater than two
    - Provider unions, registry keys, detection, and database checks expand together

key-files:
  created:
    - web/tests/public-connectors.integration.test.ts
    - supabase/functions/_shared/adapters/smartrecruiters.ts
    - supabase/functions/_shared/adapters/recruitee.ts
    - supabase/migrations/0014_public_connectors.sql
  modified:
    - web/tests/detect.test.ts
    - web/tests/source-observation.integration.test.ts
    - supabase/functions/_shared/adapters/types.ts
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/_shared/detect.ts

key-decisions:
  - "Accept only HTTPS exact SmartRecruiters board URLs and one conservative Recruitee tenant subdomain with no credentials, ports, path escapes, or extra segments."
  - "Treat page, count, record, content-type, payload, and required-detail failures as partial or unknown observations that retain safe rows but cannot authorize closure."
  - "Keep SmartRecruiters and Recruitee as the only new executable database values; unsupported targets and Workday remain outside migration 0014."

patterns-established:
  - "Bounded public fetch: fixed targets, redirect rejection, 15-second timeout, byte/job/page caps, stable warnings, and one Retry-After-aware retry."
  - "Migration parity probe: exercise insert, exact-ID reopen, and repost/source conversion inside the same transaction that expands closed checks."

requirements-completed: [DISC-07, DISC-09]

coverage:
  - id: D1
    description: Exact SmartRecruiters and Recruitee boards detect and active claimed rows dispatch to their exact registered adapters while unsafe URLs, unknown providers, and non-active rows fail before network access.
    requirement: DISC-07
    verification:
      - kind: integration
        ref: "web/tests/public-connectors.integration.test.ts#public connector detection journey and closed registry dispatch"
        status: pass
      - kind: unit
        ref: "web/tests/detect.test.ts#detectAts and buildEndpoint"
        status: pass
    human_judgment: false
  - id: D2
    description: Both public adapters normalize live-shaped fixtures and make pagination, count, schema, content-type, payload-cap, rate-limit, and required-detail failures closure-ineligible while retaining safe partial rows.
    requirement: DISC-09
    verification:
      - kind: integration
        ref: "web/tests/public-connectors.integration.test.ts#SmartRecruiters bounded observation and Recruitee bounded observation"
        status: pass
      - kind: other
        ref: "cd web && npm test"
        status: pass
    human_judgment: false
  - id: D3
    description: Normalized source types, registry providers, companies.ats_type, and jobs.source contain the same two new executable providers, with transactional insert/reopen/source-conversion parity and no unsupported target.
    requirement: DISC-07
    verification:
      - kind: integration
        ref: "web/tests/public-connectors.integration.test.ts#keeps normalized sources and database constraints in parity"
        status: pass
      - kind: other
        ref: "cd web && npm run build && npm run lint"
        status: pass
    human_judgment: false

duration: 8 min
completed: 2026-07-18
status: complete
---

# Phase 02.1 Plan 04: Public Connector Expansion Summary

**SmartRecruiters and Recruitee now use exact safe detection, bounded reconciled observations, and closed registry dispatch without weakening existing provider or lifecycle behavior.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-18T00:54:37Z
- **Completed:** 2026-07-18T01:02:05Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Added strict URL-to-registry journeys for SmartRecruiters and Recruitee, including HTTPS, credential, port, host-lookalike, encoded-separator, extra-segment, unknown-provider, and non-active-provider rejection before fetch.
- Added native-fetch adapters with fixed targets, redirect rejection, 15-second timeout, byte/job/page caps, bounded retry, exact count reconciliation, stable redacted warnings, safe partial-row retention, and SmartRecruiters detail concurrency capped at two.
- Added migration 0014 with closed company/job provider values and transactional normal-insert, exact-ID-reopen, and repost/source-conversion probes while leaving Workday and unsupported sources out.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write failing public connector journeys** - `655e5ad` (test, RED)
2. **Task 2: Implement bounded SmartRecruiters and Recruitee adapters** - `c4be52c` (feat, GREEN)
3. **Task 3: Wire exact detection and registry dispatch** - `db96502` (feat, GREEN)
4. **Corrective safety pass: Required detail degradation** - `35e0823` (fix)

## Files Created/Modified

- `web/tests/public-connectors.integration.test.ts` - Fixture-only vertical contracts for URL detection, bounded observations, source parity, and scheduled dispatch.
- `web/tests/detect.test.ts` - Expanded positive/negative URL and endpoint-construction matrices, including strict Ashby slugs.
- `web/tests/source-observation.integration.test.ts` - Updated the obsolete exact-three registry assertion while preserving all lifecycle safety tests.
- `supabase/functions/_shared/adapters/types.ts` - Added only SmartRecruiters and Recruitee to normalized executable sources.
- `supabase/functions/_shared/adapters/smartrecruiters.ts` - Offset/limit, totalFound, required-detail, cap, retry, and observation logic.
- `supabase/functions/_shared/adapters/recruitee.ts` - Documented offers-wrapper validation, normalization, cap, retry, and observation logic.
- `supabase/functions/_shared/connectors.ts` - Shared verification/poll registry entries and canonical identities for both providers.
- `supabase/functions/_shared/detect.ts` - Exact HTTPS public-board detection and fixed endpoint construction.
- `supabase/migrations/0014_public_connectors.sql` - Strict provider-check replacement and transactional ingestion-write parity assertions.

## Decisions Made

- Kept SmartRecruiters anonymous-access viability as a hosted Plan 07 disposition; local code attempts no API key, credential substitution, or bypass and returns stable closure-ineligible evidence on 401/403/prohibition.
- Kept `uturn.recruitee.com` as the pinned hosted representative while the reusable detector accepts only a single conservative Recruitee tenant label; Plan 07 may not silently substitute another tenant.
- Required SmartRecruiters detail retrieval only when list records lack the immutable description snapshot; failed detail calls retain safe list records as partial snapshots and deny closure credibility.
- Kept every network target server-constructed from a strict identity and rejected redirects instead of following provider-controlled origins.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Task-caused regression] Updated the existing exact-three registry assertion**
- **Found during:** Task 2 integrated verification
- **Issue:** `web/tests/source-observation.integration.test.ts` correctly hard-coded the three providers implemented by Plan 02, but Plan 04's file manifest omitted that test even though adding two registry keys necessarily made the assertion obsolete.
- **Fix:** With orchestrator authorization, expanded only that expectation to the planned five-provider registry and preserved every closure-safety assertion.
- **Files modified:** `web/tests/source-observation.integration.test.ts`
- **Verification:** Focused observation suite and full 210-test regression pass.
- **Committed in:** `db96502`

**2. [Rule 2 - Missing critical safety] Completed required-detail degradation semantics**
- **Found during:** Post-Task 3 plan acceptance review
- **Issue:** The first adapter pass reconciled pages and records but did not fetch a missing SmartRecruiters description, leaving the plan's required-detail failure contract unproven.
- **Fix:** Added fixed-origin detail calls in batches of at most two; detail failure now retains safe list rows as partial snapshots with bounded warnings and no closure credibility. Recruitee records without descriptions are likewise marked partial snapshots.
- **Files modified:** `supabase/functions/_shared/adapters/smartrecruiters.ts`, `supabase/functions/_shared/adapters/recruitee.ts`, `web/tests/public-connectors.integration.test.ts`
- **Verification:** Dedicated detail-failure fixture, focused 45-test adapter/observation suite, and full 210-test regression pass.
- **Committed in:** `35e0823`

---

**Total deviations:** 2 auto-fixed (1 Rule 1 regression, 1 Rule 2 missing critical safety contract).
**Impact on plan:** Both changes were required for the planned five-provider registry and safe partial-observation semantics; no provider, package, hosted, or product scope was added.

## Issues Encountered

- The production build retains the pre-existing chunk-size warning, and oxlint retains the pre-existing Fast Refresh warning in `AuthProvider.tsx`; both commands exit successfully and this plan did not modify those areas.
- The phase-wide schema gate still anticipates future local migrations. The user-approved local bypass was applied; migration 0014 was not pushed, and the real hosted schema/deployment approval remains Plan 07.

## Known Stubs

None.

## User Setup Required

None - no package, hosted schema, deployment, secret, schedule, or external source was changed.

## Next Phase Readiness

- Plan 05 can record separate activation observations for both Experimental public connectors without changing adapter or dispatch semantics.
- Plan 07 owns live anonymous-access/tenant evidence, honest unsupported fallback writes, the no-silent-Recruitee-substitution checkpoint, and the ordered migration/deployment approval.
- Migration 0014 remains local and follows migrations 0012-0013 in the future Plan 07 schema push.

## Automated Evidence

- RED proof: `655e5ad` failed on absent adapter modules, missing detections, lax Ashby slug acceptance, missing endpoint builders, and absent registry entries while all unaffected prior cases stayed green.
- Task 2 focused contract: 3 files, 45 tests passed after the required-detail correction.
- Combined public connector contract: 4 files, 88 tests passed.
- Full deterministic regression: 17 files, 210 tests passed.
- Production build: TypeScript and Vite passed; only the pre-existing chunk warning remains.
- Oxlint: passed with one pre-existing Fast Refresh warning.

## TDD Gate Compliance

- RED: `655e5ad` introduced the absent-provider fixture journeys and failed for the intended missing production behavior.
- GREEN: `c4be52c` added bounded adapters/schema parity and `db96502` completed exact detector/registry dispatch.
- Safety correction: `35e0823` closed the required-detail gap while retaining the full green suite.

## Self-Check: PASSED

- All nine implementation/test files exist, including the three created production artifacts and fixture-driven integration test.
- Commits `655e5ad`, `c4be52c`, `db96502`, and `35e0823` exist on `main` in RED → adapter GREEN → dispatch GREEN → safety-fix order.
- Focused 88-test verification, full 210-test regression, production build, lint, and structural source/registry/database parity checks pass.
- Migration 0014 contains only the two planned executable providers plus all prior values and was not pushed.
- No unsupported target, arbitrary HTML parser, provider credential, package, hosted mutation, deployment, schedule, or external-state change was introduced.
- Unrelated `.DS_Store` and agent-dashboard files remain untouched and uncommitted.

---
*Phase: 02.1-source-coverage-expansion*
*Completed: 2026-07-18*
