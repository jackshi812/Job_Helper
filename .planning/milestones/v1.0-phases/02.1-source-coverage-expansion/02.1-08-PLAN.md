---
phase: 02.1-source-coverage-expansion
plan: "08"
type: execute
wave: 7
depends_on: ["02.1-07"]
files_modified: []
autonomous: false
requirements: [PREF-05, DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "The user explicitly approves pushing the reviewed implementation commits to production origin/main."
    - "The matching Cloudflare Pages production deployment reports the exact pushed commit SHA before UI acceptance begins."
    - "Cloudflare credentials/account/project configuration are not assumed present; the executor expects and handles a dynamic auth/config checkpoint before deployment polling when ambient configuration is absent."
    - "Deployed assets contain the Phase 02.1 Watchlist Link/Activation/Health UI, eliminating stale-deployment false acceptance."
    - "Automated deployed checks pass before end-of-phase human UAT is recorded."
    - "Cloudflare polling has a 15-minute overall deadline with at-most-60-second intervals, and the post-deploy verifier is a mutation-free COMPLETE recheck that preserves all provider fallback evidence."
  artifacts:
    - path: ".planning/phases/02.1-source-coverage-expansion/02.1-08-SUMMARY.md"
      provides: "Approved push SHA, Cloudflare deployment identity, asset proof, automated deployed checks, and UAT handoff"
  key_links:
    - from: "origin/main"
      to: "Cloudflare Pages production deployment"
      via: "deployment metadata commit SHA equality"
      pattern: "commit.*sha"
    - from: "Cloudflare Pages deployed assets"
      to: "Phase 02.1 Watchlist UI"
      via: "asset markers plus browser checks after SHA match"
      pattern: "Open jobs|Scheduled polling off|Not monitored|Last-known jobs retained"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Obtain explicit production-mutation approval, push reviewed commits, wait for the exact Cloudflare build, prove deployed assets are current, and only then accept the deployed Watchlist.

Purpose: Phase 2 proved that browser acceptance against a stale Pages deployment can falsely pass while production lacks the reviewed UI.
Output: Approval checkpoint, agent-run origin/main push, SHA-matched Pages deployment proof, deployed asset checks, and UAT-ready visual evidence.

Decisions: D-01 through D-15.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.1-source-coverage-expansion/02.1-UI-SPEC.md
@.planning/phases/02.1-source-coverage-expansion/02.1-07-SUMMARY.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-VERIFICATION.md

<interfaces>
Cloudflare Pages is connected to production branch `main`, but the repository contains no Wrangler dependency/config proving local Cloudflare credentials, account ID, or project selection. This plan must expect a dynamic authentication/configuration checkpoint unless read-only preflight proves ambient CLI/API access and an unambiguous Pages project. After access is resolved, compare the pushed local HEAD SHA with Cloudflare deployment metadata and inspect the resulting production asset before any browser acceptance.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the production origin/main mutation</name>
  <files></files>
  <read_first>
    - .planning/phases/02.1-source-coverage-expansion/02.1-07-SUMMARY.md
    - .planning/phases/02.1-source-coverage-expansion/02.1-UI-SPEC.md
    - .planning/phases/02-watchlist-ingestion-monitoring/02-VERIFICATION.md
  </read_first>
  <action>Before asking, the agent runs read-only preflight: verify all Plans 01–07 are complete, the full local suite/build/lint and hosted verifier passed, current HEAD contains the reviewed implementation commits, the worktree has no uncommitted implementation change, the current production target is `origin/main`, and show the exact commit range/SHA that will be pushed. Also inspect Cloudflare CLI/API availability, authentication, account ID, Pages project, and production-branch metadata without assuming any repo-local Wrangler configuration. Record whether ambient access is ready or a dynamic auth/config checkpoint is expected after push approval. Ask only for approval of the external production mutation; do not ask the user to run any CLI or deployment step.</action>
  <verify><automated>git status --short && git log --oneline --decorate -12 && cd web && npm test && npm run build && npm run lint</automated></verify>
  <acceptance_criteria>
    - The approval prompt names `git push origin main`, the exact HEAD SHA, and the Cloudflare production consequence.
    - Preflight explicitly records that Cloudflare auth/account/project config is either proven ready or will require the expected dynamic checkpoint; absence is not treated as implementation failure.
    - No push or Cloudflare mutation occurs before explicit approval.
    - Declining stops the plan without altering production.
  </acceptance_criteria>
  <done>The user explicitly selects Approve; the executor records the approval and proceeds automatically.</done>
  <decision>Approve pushing the reviewed Phase 02.1 implementation commits to origin/main and triggering the connected Cloudflare Pages production deployment.</decision>
  <options>
    <option id="approve"><name>Approve production push</name><pros>Completes production deployment and acceptance.</pros><cons>Mutates origin/main and the live Pages site.</cons></option>
    <option id="decline"><name>Decline</name><pros>Leaves production unchanged.</pros><cons>Phase remains unverified in production.</cons></option>
  </options>
  <resume-signal>Select approve or decline.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push and prove the matching Cloudflare production deployment</name>
  <files></files>
  <read_first>
    - .planning/phases/02.1-source-coverage-expansion/02.1-07-SUMMARY.md
    - web/src/pages/Watchlist.tsx
    - web/src/lib/watchlist.ts
  </read_first>
  <action>After recorded approval, re-check that HEAD and the approved SHA are unchanged, then the agent runs `git push origin main`. Capture the pushed SHA. Do not assume Cloudflare credentials or project configuration exist: first attempt read-only CLI/API discovery. Unless it proves authenticated access plus an unambiguous account ID, Pages project, and `main` production branch, create the expected dynamic auth/config checkpoint for the missing login/account/project selection, then retry discovery; never invent identifiers or hand deployment/polling CLI labor to the user. Once access is proven, poll production deployments at intervals no longer than 60 seconds with a hard 15-minute overall deadline until the `main` deployment for that exact SHA reaches success. Fail cleanly on a newer/different SHA, failed build, overall timeout, or branch mismatch. Fetch the production HTML and referenced JavaScript assets and prove they contain the Phase 02.1 Watchlist markers `Open jobs`, `Scheduled polling off`, `Not monitored`, and `Last-known jobs retained`; also record the deployment ID, URL, SHA, and asset URL/hash. Do not begin deployed UI checks until both metadata SHA equality and asset proof pass. Addresses review concern: queue/build waits are bounded overall, not only per polling interval.</action>
  <verify><automated>git rev-parse HEAD && git ls-remote origin refs/heads/main</automated></verify>
  <acceptance_criteria>
    - `origin/main`, local approved HEAD, and Cloudflare production metadata all identify the same SHA.
    - Cloudflare auth/account/project/production-branch identity is discovered or resolved through a recorded dynamic checkpoint; no pre-existing config is assumed.
    - Cloudflare build status is successful and the production asset contains all four UI markers.
    - Poll/wait intervals never exceed 60 seconds.
    - Total deployment polling never exceeds 15 minutes and timeout exits with a bounded diagnostic.
    - Stale or mismatched deployment evidence blocks acceptance.
  </acceptance_criteria>
  <done>The exact reviewed commit is live and its deployed assets demonstrably contain the Phase 02.1 UI.</done>
</task>

<task type="auto">
  <name>Task 3: Run deployed checks and record the end-of-phase UI acceptance handoff</name>
  <files></files>
  <read_first>
    - .planning/phases/02.1-source-coverage-expansion/02.1-UI-SPEC.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02.1-source-coverage-expansion/02.1-07-SUMMARY.md
  </read_first>
  <action>Only after Task 2's SHA/asset proof, run automated production smoke checks for authenticated Watchlist load, safe HTTPS Link attributes, separate Activation/Health text, all twelve catalog rows, unsupported-not-monitored copy, active/public connector state, Experimental Workday with zero job rows, and a non-destructive degraded-retention fixture. Restore any temporary state. Rerun `verify-source-coverage.ts --resume` from its COMPLETE ledger and compare pre/post hashes/counts: it must add no observation/progress/company/job row, preserve SmartRecruiters/Recruitee unsupported fallback rows and SuccessFactors documentation disposition, and leave no probe markers. Run the Resumes and Settings regression tests, then use the signed-in deployed browser to capture light/dark and narrow-width evidence and record the exact UI-SPEC checks for the end-of-phase human UAT. Verify the Watchlist opts into Keep company/Removing/cancel-first focus while the Resume dialog keeps its existing prop defaults and Settings' separate type-to-confirm dialog remains unchanged; background refresh must not move focus or repeatedly announce unchanged state. Addresses review concerns: post-promotion idempotence and shared-dialog regression must be asserted, not assumed.</action>
  <verify>
    <automated>node --env-file=scripts/.env --experimental-strip-types scripts/verify-source-coverage.ts --resume && cd web && npx vitest run src/lib/resumes.test.ts src/pages/Settings.test.ts</automated>
    <human-check>On the SHA-proven production URL, confirm: every Link opens the intended HTTPS page in a new tab; Activation and Health are separate; Experimental shows check progress and Scheduled polling off; Disabled shows Unsupported and Not monitored; Degraded shows last success, bounded detail, and Last-known jobs retained; narrow width retains all columns through the labeled horizontal scroll; Watchlist remove initially focuses Keep company and restores focus; Resume deletion retains its existing labels/focus defaults; Settings type-to-confirm remains unchanged; light/dark contrast and focus rings match UI-SPEC.</human-check>
  </verify>
  <acceptance_criteria>
    - Automated deployed smoke and resumable hosted verifier exit zero after the production deployment.
    - The post-COMPLETE verifier changes no hosted counts/state, preserves fallback evidence rows, and leaves no marked probe fixtures.
    - `cd web && npx vitest run src/lib/resumes.test.ts src/pages/Settings.test.ts` passes before dialog UAT.
    - Evidence is tied to the deployment ID/SHA from Task 2, not an earlier URL state.
    - Human UAT checklist and captured evidence are recorded in the plan summary for the phase verification/UAT workflow.
  </acceptance_criteria>
  <done>The current production deployment is ready for final human UAT with automated and visual evidence tied to the reviewed SHA.</done>
</task>

</tasks>

## Review disposition

Cloudflare polling is bounded to 15 minutes overall. The post-deploy verifier is explicitly a read/idempotence contract and must preserve every truthful provider fallback row. Shared-dialog risk is resolved through prop-driven Watchlist behavior plus unchanged Resume defaults and Settings' separate dialog regression. No additional UI package or deployment mechanism is introduced.

<threat_model>
**ASVS Level:** L1 — opportunistic coverage of primary trust boundaries and high-impact components.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local git → origin/main | Approved external mutation changes production source. |
| origin/main → Cloudflare Pages | Git integration builds and publishes public assets. |
| Production browser → hosted data/external links | Deployed UI renders untrusted status/link data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-25 | Tampering | Production push | high | mitigate | Explicit approval, exact SHA preflight, unchanged-HEAD recheck, and direct origin/main comparison. |
| T-02.1-26 | Spoofing | Deployment identity/config | high | mitigate | Discover or dynamically checkpoint Cloudflare auth/account/project config, then match production branch and commit SHA; verify deployment success and asset markers before acceptance. |
| T-02.1-27 | Information Disclosure | Deployment/probe logs | medium | mitigate | Redact tokens, secrets, query strings, and provider bodies; record only IDs, SHA, URLs, and bounded results. |
| T-02.1-28 | Tampering | Deployed UI data | high | mitigate | Safe URL parsing/attributes, bounded status copy, catalog RLS, and non-destructive hosted smoke checks. |
| T-02.1-31 | Denial of Service | Cloudflare deployment wait | medium | mitigate | At-most-60-second polls, hard 15-minute overall deadline, and bounded failure output. |
| T-02.1-SC | Tampering | Build supply chain | low | accept | No package install; deploy reviewed lockfile/commits only. |
</threat_model>

<verification>Approval is recorded; origin/main and Cloudflare match the approved SHA; production asset markers pass before browser checks; automated deployed smoke/hosted verifier pass; UAT evidence is recorded.</verification>

<success_criteria>
- No production mutation occurs without explicit user approval.
- Acceptance cannot run against a stale Cloudflare build.
- The deployed Watchlist satisfies PREF-05 and DISC-07 through DISC-09 under D-15.
</success_criteria>

## Artifacts this phase produces

- Approved production push record and exact origin/main SHA.
- Cloudflare Pages production deployment ID/SHA/asset proof.
- Automated deployed Watchlist smoke results and final human-UAT checklist/evidence.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Honest representative monitoring states | 01-08 | COVERED | Contract, state, UI, connectors, hosted proof, production acceptance. |
| REQ | PREF-05 | Safe canonical Link column | 02,03,07,08 | COVERED | Schema, UI, hosted and deployed proof. |
| REQ | DISC-07 | Feasible platform representatives | 04-08 | COVERED | Recruitee active; SmartRecruiters active after windows or honestly unsupported if anonymous access closes; Workday Experimental; remainder unsupported. |
| REQ | DISC-08 | Fixed finance matrix under D-15 | 03,06-08 | COVERED | Safe direct monitor where possible; canonical evidence/reasons otherwise. |
| REQ | DISC-09 | Degraded retention and staged activation | 01,02,04-08 | COVERED | Closure safety through server-window and production proof. |
| RESEARCH | — | Registry, PollObservation, strict schema, bounded adapters | 01,02,04,06 | COVERED | No new packages. |
| RESEARCH | — | Three separate server-time opportunities | 05,07 | COVERED | Replay/same-window rejected; resumable hosted evidence. |
| RESEARCH | — | Finance evidence catalog and UI truthfulness | 03,07,08 | COVERED | Unsupported never monitored. |
| CONTEXT | D-01..D-15 | All locked decisions | 01-08 | COVERED | Deferred universal scraping/fallback/global completeness excluded. |

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-08-SUMMARY.md` when done.</output>
