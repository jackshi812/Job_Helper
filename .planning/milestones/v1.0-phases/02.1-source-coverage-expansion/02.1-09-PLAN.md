---
phase: 02.1-source-coverage-expansion
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-pipeline.ts
  - web/tests/pipeline-verifier-safety.test.ts
autonomous: true
requirements: [DISC-09]
gap_closure: true
must_haves:
  truths:
    - "Hosted reopen verification never selects or mutates a pre-existing production job."
    - "Every reopen probe owns an invocation-scoped active company and all jobs returned from the reserved PlanetScale Greenhouse board, then lets the deployed poll-tick path reopen its closed exact-ID fixture."
    - "A completed verifier run proves that no marked fixture row remains and that every pre-existing real job row is unchanged from the post-drain baseline."
  artifacts:
    - path: "scripts/verify-pipeline.ts"
      provides: "Collision-checked PlanetScale fixture lifecycle, claimant-agnostic poll-tick proof, scoped cleanup, and zero-residue assertion"
    - path: "web/tests/pipeline-verifier-safety.test.ts"
      provides: "Regression that forbids arbitrary seed-job mutation and requires fixture ownership"
  key_links:
    - from: "scripts/verify-pipeline.ts"
      to: "claim_due_companies -> poll-tick -> providerRegistry.greenhouse -> planCompanySync"
      via: "active greenhouse:global:planetscale fixture, exact returned IDs, and bounded observation loop"
      pattern: "phase-02.1-reopen-probe"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Close CR-01 by replacing legacy probe 15's arbitrary production-row mutation with a fully owned disposable company/job proof.

Purpose: Verification must not change real lifecycle state or race the live scheduler/provider.
Output: A fixture-owned hosted probe, cleanup assertions, and a regression that prevents the unsafe pattern returning.

Decision preservation: D-01 through D-15 remain unchanged; this plan specifically protects D-10 and D-11 lifecycle truthfulness.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-source-coverage-expansion/02.1-VERIFICATION.md
@.planning/phases/02.1-source-coverage-expansion/02.1-REVIEW.md
@.planning/phases/02.1-source-coverage-expansion/02.1-07-SUMMARY.md
@scripts/verify-source-coverage.ts

<interfaces>
`scripts/verify-source-coverage.ts` already demonstrates exact-prefix, TTL-bounded disposable company/job ownership. `claim_due_companies` accepts active Greenhouse rows, `pollConnector` dispatches them through `providerRegistry.greenhouse`, and `poll-tick` applies `planCompanySync` before its exact-ID reopen update. The reserved public fixture board is `https://job-boards.greenhouse.io/planetscale` / `greenhouse:global:planetscale`; its current low volume is a preflight condition, not an assumption. The live minute scheduler is concurrent and no cleanup may overwrite any pre-existing row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the verifier to disposable lifecycle ownership</name>
  <files>web/tests/pipeline-verifier-safety.test.ts, scripts/verify-pipeline.ts</files>
  <read_first>
    - scripts/verify-pipeline.ts
    - scripts/verify-source-coverage.ts
    - supabase/functions/_shared/lifecycle.ts
    - supabase/migrations/0006_jobs_pipeline.sql
    - supabase/migrations/0012_connector_state.sql
  </read_first>
  <behavior>
    - A source regression fails while probe 15 selects an open job from `seedIds` as its mutation target.
    - The accepted contract requires a unique invocation marker, the exact PlanetScale Greenhouse identity, collision preflight over every returned provider ID, fixture company/job IDs, a real `postTick` journey, `finally` deletion, and a post-cleanup zero-row assertion.
    - Cleanup may target only the company ID and provider job IDs proven absent before this invocation; it never restores or deletes a pre-existing real row.
  </behavior>
  <action>First add a Vitest source/contract regression that fails against the current arbitrary seed-job selection, direct lifecycle emulation, and unconditional restore. Refactor probe 15 around the reserved public board `https://job-boards.greenhouse.io/planetscale`: fetch its normal Greenhouse list endpoint, require a non-empty bounded board of at most 25 jobs, normalize the returned numeric IDs exactly as the adapter does, and abort before writes if a `greenhouse/planetscale` company, its deterministic source key, or any `greenhouse` job with any returned ID already exists. Do not substitute a seed or arbitrary board when preflight fails. Drain currently due active work with bounded claimant-agnostic `postTick` calls until `claimed === 0`, then snapshot every pre-existing job's ID, lifecycle fields, and first-sight fields; this post-drain snapshot is the real-row baseline. Create the uniquely named `phase-02.1-reopen-probe-{invocation}` company with `ats_type='greenhouse'`, `board_token='planetscale'`, `source_key='greenhouse:global:planetscale'`, `activation_state='active'`, and `last_polled_at=null`, plus one closed job whose `external_id` is selected from the collision-free returned set and whose complete immutable snapshot is recorded. Track the returned company/job IDs immediately after each successful insert. Then call the deployed `poll-tick` endpoint and observe the fixture by ID in a bounded loop: either this invocation or the live cron may claim it, so never infer ownership from response ordering or claimed counts. Require the fixture job to become open with `closed_at=null` and a newer `last_seen_at`, while source, external ID, title, location, URL, posted/first-seen timestamps, descriptions, partial flag, and fingerprint remain byte-identical. This must exercise `claim_due_companies -> processCompany -> pollConnector/providerRegistry.greenhouse -> planCompanySync -> reopen update`; do not import/call `planCompanySync` from the verifier and do not add a test-only route, header, branch, or bypass to production code. In `finally`, delete all jobs owned by the fixture company whose exact returned IDs were collision-free at preflight, then delete only the tracked marked company ID. After cleanup, require zero matching company marker/source-key rows and zero fixture-owned/returned-ID job rows, and compare every pre-existing real job baseline row byte-for-byte. Any concurrent baseline drift fails safely and is never repaired by forcing state. Addresses CR-01 and D-11.</action>
  <verify><automated>cd web && npx vitest run tests/pipeline-verifier-safety.test.ts</automated></verify>
  <acceptance_criteria>
    - The regression fails on the old seed-job mutation and passes only for invocation-owned IDs.
    - Probe 15 has no query that chooses an existing seed job, no direct `planCompanySync` emulation, and no unconditional real-row reopen restore.
    - The reserved PlanetScale board must be non-empty, at most 25 jobs, absent from companies, and collision-free across every returned `greenhouse` external ID before any fixture write.
    - The active fixture is processed through the deployed poll-tick endpoint without assuming which caller claimed it; its exact closed ID reopens and every immutable field is preserved.
    - Partial setup is tracked immediately, `finally` removes every invocation-owned job before its company, and post-cleanup exact marker/source-key/returned-ID assertions require zero residue.
    - Every pre-existing real job matches the post-drain baseline; detected concurrent drift fails without restoration.
    - Production `poll-tick` gains no test-only route, header, branch, or bypass.
  </acceptance_criteria>
  <done>Probe 15 proves lifecycle behavior using only rows it owns and cannot resurrect or timestamp-mutate a real job.</done>
</task>

<task type="auto">
  <name>Task 2: Prove local verifier safety and full regression compatibility</name>
  <files>scripts/verify-pipeline.ts, web/tests/pipeline-verifier-safety.test.ts</files>
  <read_first>
    - scripts/verify-pipeline.ts
    - web/tests/pipeline-verifier-safety.test.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-VERIFICATION.md
  </read_first>
  <action>Add a no-secrets/no-network contract mode if needed so CI can validate the exact reserved board identity, maximum board size, collision gate, active claim path, claimant-agnostic observation loop, partial-setup `finally` cleanup, real-row baseline diff, and absence of unsafe fallback/direct lifecycle emulation before hosted execution. Keep normal top-level-await behavior. Run the focused regression, syntax check, full test suite, build, lint, and source-coverage dry run. Do not run the hosted verifier in this plan; Plan 12 performs it only after all gap fixes and explicit production approval.</action>
  <verify><automated>node --experimental-strip-types --check scripts/verify-pipeline.ts && cd web && npm test && npm run build && npm run lint && cd .. && node --experimental-strip-types scripts/verify-source-coverage.ts --dry-run</automated></verify>
  <acceptance_criteria>
    - Syntax, focused safety regression, 268+ full tests, build, lint, and zero-network dry run pass.
    - Local verification requires no hosted secret and performs no external call.
    - Contract tests prove the production path remains `postTick` plus the active Greenhouse registry route rather than a pure lifecycle shortcut.
    - Hosted execution remains deferred to the approval-gated release plan.
  </acceptance_criteria>
  <done>The safe fixture contract is locally proven and ready for one approval-gated hosted rerun.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1; block on high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---:|---|---|
| T-02.1-09-01 | Tampering | verifier -> production database | high | mitigate | Invocation-owned IDs only, exact-prefix cleanup, zero-residue assertion, immutable seed baselines |
| T-02.1-09-02 | Tampering / Denial of Service | live scheduler -> fixture | high | mitigate | Active collision-free fixture, bounded claimant-agnostic observation, and no response-order ownership assumption |
| T-02.1-09-03 | Information Disclosure | verifier logs -> operator | medium | mitigate | Log bounded markers and assertions only; no bodies, tokens, or raw URLs |
| T-02.1-09-SC | Tampering | dependencies | low | accept | Install nothing; use pinned repository tooling |
</threat_model>

<verification>Focused ownership regression plus the full local quality gate pass; hosted proof is intentionally deferred to Plan 12.</verification>

<success_criteria>
- No pre-existing job is a verifier mutation target.
- Fixture cleanup runs on success and error and proves zero residue.
- Every post-drain pre-existing job row remains byte-identical; any drift fails without repair.
</success_criteria>

## Artifacts this phase produces

- `PIPELINE_REOPEN_PROBE_PREFIX` plus the exact PlanetScale Greenhouse fixture identity and 25-job safety ceiling.
- Disposable reopen fixture create/cleanup/assert helpers in `scripts/verify-pipeline.ts`.
- `web/tests/pipeline-verifier-safety.test.ts` — CR-01 regression.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-09-SUMMARY.md` when done.</output>
