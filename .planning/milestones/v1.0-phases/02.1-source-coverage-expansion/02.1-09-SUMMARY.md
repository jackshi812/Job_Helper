---
phase: 02.1-source-coverage-expansion
plan: "09"
subsystem: verification
tags: [supabase, greenhouse, lifecycle, hosted-verifier, fixture-safety]

requires:
  - phase: 02.1-source-coverage-expansion
    provides: Deployed poll-tick lifecycle path and the CR-01 unsafe-verifier finding
provides:
  - Collision-checked PlanetScale Greenhouse fixture ownership for hosted reopen proof
  - Bounded claimant-agnostic observation through deployed poll-tick
  - Exact fixture cleanup, zero-residue checks, and byte-for-byte real-job baseline protection
affects: [02.1-12-production-rerun, pipeline-verification, lifecycle-safety]

tech-stack:
  added: []
  patterns:
    - Preflight every provider identity before the first fixture write
    - Snapshot real rows only after draining currently due work, then fail safely on any later drift
    - Observe invocation-owned state by ID instead of inferring which scheduler caller claimed it

key-files:
  created:
    - web/tests/pipeline-verifier-safety.test.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-09-SUMMARY.md
  modified:
    - scripts/verify-pipeline.ts

key-decisions:
  - "Reserve greenhouse:global:planetscale exclusively for the hosted reopen fixture and abort before writes on any company or returned-ID collision."
  - "Treat concurrent real-job drift as verification failure; never restore or repair a pre-existing production row."

patterns-established:
  - "Hosted lifecycle proof: bounded provider preflight -> due-work drain -> real-row baseline -> owned fixture -> deployed observation -> exact cleanup -> zero residue -> baseline equality."

requirements-completed: [DISC-09]

coverage:
  - id: D1
    description: "Probe 15 can reopen only an invocation-owned closed PlanetScale job and cannot select, close, or force-restore a seed job."
    requirement: DISC-09
    verification:
      - kind: unit
        ref: "web/tests/pipeline-verifier-safety.test.ts#pipeline reopen verifier ownership contract"
        status: pass
      - kind: other
        ref: "node --experimental-strip-types --check scripts/verify-pipeline.ts"
        status: pass
    human_judgment: false
  - id: D2
    description: "The hosted fixture contract is collision-gated, bounded, routed through claim_due_companies and the Greenhouse registry, cleaned exactly, and guarded by a real-job baseline diff."
    requirement: DISC-09
    verification:
      - kind: unit
        ref: "web/tests/pipeline-verifier-safety.test.ts#orders every destructive action after preflight and bounds all hosted work"
        status: pass
      - kind: integration
        ref: "web/tests/pipeline-verifier-safety.test.ts#keeps production lifecycle execution behind the active connector registry"
        status: pass
      - kind: other
        ref: "node --experimental-strip-types scripts/verify-source-coverage.ts --dry-run"
        status: pass
    human_judgment: false

duration: 7 min
completed: 2026-07-18
status: complete
---

# Phase 02.1 Plan 09: Safe Hosted Reopen Fixture Summary

**Probe 15 now proves exact-ID reopen behavior with a collision-checked PlanetScale fixture while preserving every pre-existing production job byte-for-byte.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-07-18T17:01:46Z
- **Completed:** 2026-07-18T17:08:28Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Replaced arbitrary seed-job selection and unconditional restoration with a unique, invocation-owned active Greenhouse company and closed exact-ID job.
- Added non-empty/25-job board bounds plus company, source-key, and every-returned-ID collision preflight before any database write.
- Drains currently due active work, snapshots all real jobs, invokes deployed poll-tick, and observes the exact fixture by ID without assuming whether the verifier or live cron claimed it.
- Deletes only the tracked fixture company's collision-free returned IDs, proves zero marked residue, and fails without repair if any pre-existing row drifted.
- Added six no-secret/no-network source-contract tests covering ownership, ordering, bounded loops, registry routing, and absence of verifier-only production bypasses.

## Task Commits

1. **Task 1 RED: Lock the verifier to disposable lifecycle ownership** - `6a3ad1c`
2. **Task 1 GREEN: Replace the unsafe seed mutation with the owned fixture** - `2ef8f12`
3. **Task 2: Prove local verifier safety and full regression compatibility** - `a3633fd`

## Files Created/Modified

- `scripts/verify-pipeline.ts` - Bounded PlanetScale preflight, due-work drain, real-row snapshot, owned reopen observation, cleanup, and residue assertions.
- `web/tests/pipeline-verifier-safety.test.ts` - Static safety contract proving fixture ownership and the deployed production lifecycle chain without network or secrets.

## Decisions Made

- PlanetScale is a reserved verifier identity, not a fallback board. Any existing company or returned Greenhouse ID aborts the probe before writes.
- Baseline drift is evidence of concurrent production activity and causes failure; the verifier never overwrites the newer state.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The repository git index requires managed escalation for commits; approved commit operations completed normally with hooks.
- Existing lint and Vite chunk-size warnings remain unchanged and non-blocking.

## User Setup Required

None - local safety verification uses no hosted secrets and performs no external request.

## Next Phase Readiness

- Plan 12 may run this hosted verifier only after all gap fixes are deployed and the user approves the production mutation.
- Plan 09 itself performs no deployment and no hosted database mutation.

## Self-Check: PASSED

- Commits `6a3ad1c`, `2ef8f12`, and `a3633fd` exist.
- `scripts/verify-pipeline.ts` and `web/tests/pipeline-verifier-safety.test.ts` exist.
- Focused verifier safety suite passed 6/6; full suite passed 274/274.
- TypeScript syntax check, production build, lint, and zero-network source-coverage dry run passed.
- No production runtime route, header, branch, or bypass was added.

---
*Phase: 02.1-source-coverage-expansion*
*Completed: 2026-07-18*
