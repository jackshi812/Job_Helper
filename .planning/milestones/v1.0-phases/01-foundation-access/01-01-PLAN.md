---
phase: 01-foundation-access
plan: 1
type: execute
wave: 1
depends_on: []
# files_modified count (27) is scaffold-inflated: Vite scaffold output, one-line page stubs, and verbatim RESEARCH SQL templates account for it — ~12-14 genuinely authored files (checker scope_sanity: accepted as-is, no split)
files_modified:
  - .gitignore
  - web/package.json
  - web/vite.config.ts
  - web/index.html
  - web/src/main.tsx
  - web/src/index.css
  - web/src/lib/supabase.ts
  - web/src/auth/AuthProvider.tsx
  - web/src/auth/RequireAuth.tsx
  - web/src/components/Shell.tsx
  - web/src/pages/Login.tsx
  - web/src/pages/ResetPassword.tsx
  - web/src/pages/Dashboard.tsx
  - web/src/pages/Watchlist.tsx
  - web/src/pages/Resumes.tsx
  - web/src/pages/Tracker.tsx
  - web/src/pages/Settings.tsx
  - web/.env.example
  - supabase/config.toml
  - supabase/migrations/0001_profiles.sql
  - supabase/migrations/0002_resumes.sql
  - supabase/migrations/0003_storage.sql
  - supabase/migrations/0004_delete_my_data.sql
  - scripts/seed-users.ts
  - scripts/verify-auth.ts
  - scripts/.env.example
autonomous: false
requirements: [AUTH-01, AUTH-02]
user_setup:
  - service: supabase
    why: "Free-project creation, signup toggle, and API keys are dashboard-manual (no CLI covers them on free tier — RESEARCH Open Q3)"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API (goes in web/.env.local)"
      - name: VITE_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API keys (publishable key, goes in web/.env.local)"
      - name: SUPABASE_SECRET_KEY
        source: "Supabase Dashboard -> Project Settings -> API keys (secret key, goes in scripts/.env ONLY)"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase account settings -> Access Tokens (for non-interactive supabase link/db push)"
      - name: SUPABASE_DB_PASSWORD
        source: "Chosen at project creation (for supabase db push)"
    dashboard_config:
      - task: "Turn OFF 'Allow new users to sign up'"
        location: "Supabase Dashboard -> Authentication -> Sign In / Providers -> Email"
      - task: "Add user 2's email as organization member (reset-email delivery)"
        location: "Supabase Dashboard -> Organization -> Team"
must_haves:
  truths:
    - "Both invited users can sign in with email/password from the login page at localhost"
    - "A third party cannot create an account through any client call — client account creation is rejected"
    - "A signed-in user stays signed in after a browser refresh without re-entering credentials"
    - "The app shell shows 'Job Copilot' with nav entries Dashboard, Watchlist, Resumes, Tracker, Settings"
  artifacts:
    - path: "web/src/pages/Login.tsx"
      provides: "Minimal login page per D-08: app name, email, password, submit, forgot-password link"
      contains: "signInWithPassword"
    - path: "web/src/auth/AuthProvider.tsx"
      provides: "Session state from getSession + onAuthStateChange (AUTH-02)"
      contains: "onAuthStateChange"
    - path: "supabase/migrations/0002_resumes.sql"
      provides: "resumes table with RLS enabled and four per-operation policies"
      contains: "enable row level security"
    - path: "supabase/migrations/0003_storage.sql"
      provides: "Private 'resumes' bucket + per-user-folder storage.objects policies"
      contains: "storage.foldername"
    - path: "scripts/seed-users.ts"
      provides: "Invite-only account seeding via admin API (D-01)"
      contains: "createUser"
    - path: "scripts/verify-auth.ts"
      provides: "End-to-end skeleton test: both users sign in, third-party account creation rejected"
      min_lines: 25
  key_links:
    - from: "web/src/pages/Login.tsx"
      to: "web/src/lib/supabase.ts"
      via: "signInWithPassword on the shared client"
      pattern: "signInWithPassword"
    - from: "web/src/auth/AuthProvider.tsx"
      to: "web/src/lib/supabase.ts"
      via: "getSession + onAuthStateChange subscription"
      pattern: "getSession\\(\\)"
    - from: "scripts/seed-users.ts"
      to: "hosted Supabase Auth"
      via: "auth.admin.createUser with email_confirm true"
      pattern: "email_confirm"
---

<objective>
Walking Skeleton for Job Copilot: scaffold the Vite + React SPA, author the full Phase 1 schema (profiles, resumes, storage policies, delete_my_data RPC), provision the hosted Supabase free project (human step), push the schema, seed exactly two invite-only accounts (D-01, D-03, D-04), and prove login end-to-end with an executable script. No signup UI exists anywhere (D-01, D-02); no 2FA (D-07); sessions are long-lived by free-tier default (D-06, AUTH-02).

Purpose: Auth + RLS retrofits are painful — this plan locks the invite-only access model and the RLS schema template every later phase copies.
Output: Running local SPA (login → guarded shell), 4 applied migrations on the hosted project, 2 seeded users, passing `scripts/verify-auth.ts`.
</objective>

## Phase Goal

**As an** invited user, **I want to** securely log in to the deployed app, **so that** every row of my data stays isolated and under my own control.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-access/01-CONTEXT.md
@.planning/phases/01-foundation-access/01-RESEARCH.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 1 (this plan's share marked P1; plans 02/03 add the rest):

- **Files (P1):** `web/` Vite scaffold (`package.json`, `vite.config.ts`, `index.html`, `src/main.tsx`, `src/index.css`), `web/src/lib/supabase.ts`, `web/src/auth/AuthProvider.tsx`, `web/src/auth/RequireAuth.tsx`, `web/src/components/Shell.tsx`, `web/src/pages/{Login,ResetPassword,Dashboard,Watchlist,Resumes,Tracker,Settings}.tsx`, `web/.env.example`, `.gitignore`, `supabase/config.toml`, `supabase/migrations/0001_profiles.sql` … `0004_delete_my_data.sql`, `scripts/seed-users.ts`, `scripts/verify-auth.ts`, `scripts/.env.example`
- **Exported symbols (P1):** `supabase` (client, `web/src/lib/supabase.ts`), `AuthProvider` + `useSession` hook (`web/src/auth/AuthProvider.tsx`), `RequireAuth` (`web/src/auth/RequireAuth.tsx`), `Shell` (`web/src/components/Shell.tsx`), page components `Login`, `ResetPassword`, `Dashboard`, `Watchlist`, `Resumes`, `Tracker`, `Settings`
- **Database objects (P1):** tables `public.profiles`, `public.resumes`; function `public.delete_my_data()`; function `public.handle_new_user()` + trigger `on_auth_user_created`; storage bucket `resumes`; RLS policies `resumes_select_own`, `resumes_insert_own`, `resumes_update_own`, `resumes_delete_own`, `profiles_select_own`, `profiles_update_own`, `resumes_storage_select`, `resumes_storage_insert`, `resumes_storage_delete`
- **Env var names (P1):** `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_URL`, `SUPABASE_SECRET_KEY`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, `SUPABASE_DB_PASSWORD`, `USER1_EMAIL`, `USER2_EMAIL`, `SEED_PASSWORD_1`, `SEED_PASSWORD_2`
- **Plan 02 adds:** real `Resumes` page implementation, `ConfirmDialog`, `web/src/lib/resumes.ts` helpers, `scripts/verify-rls.ts`
- **Plan 03 adds:** real `Settings` implementation, `TypeToConfirmDialog`, `scripts/admin-reset-password.ts`, `scripts/verify-deletion.ts`

<tasks>

<task type="auto">
  <name>Task 1: Scaffold repo + SPA app shell with full auth wiring (frontend only)</name>
  <files>.gitignore, web/package.json, web/vite.config.ts, web/index.html, web/src/main.tsx, web/src/index.css, web/src/lib/supabase.ts, web/src/auth/AuthProvider.tsx, web/src/auth/RequireAuth.tsx, web/src/components/Shell.tsx, web/src/pages/Login.tsx, web/src/pages/ResetPassword.tsx, web/src/pages/Dashboard.tsx, web/src/pages/Watchlist.tsx, web/src/pages/Resumes.tsx, web/src/pages/Tracker.tsx, web/src/pages/Settings.tsx, web/.env.example</files>
  <read_first>
    - .planning/phases/01-foundation-access/01-RESEARCH.md (Standard Stack install commands, Recommended Project Structure, Code Example 6 auth wiring, State of the Art key naming)
    - .planning/phases/01-foundation-access/01-CONTEXT.md (D-08 login contents, D-12 nav entries, D-13 app name, D-14 theme, D-15 style)
  </read_first>
  <action>
    Run `git init -b main` at repo root if `.git` does not already exist (the planning commit may have initialized it). Scaffold the SPA with `npm create vite@latest web -- --template react-ts`, accepting scaffold-pinned versions (do not hand-upgrade TypeScript — RESEARCH A6). In `web/`, install `@supabase/supabase-js`, `react-router`, `@tanstack/react-query`, `tailwindcss`, `@tailwindcss/vite`, and dev-dep `supabase` (the pinned CLI lives in `web/node_modules/.bin/supabase` — every repo-root CLI invocation in Task 3 must call that exact binary, never bare `npx supabase`, which would resolve/download an unpinned version outside `web/`); if `react-router` v8 produces type errors, pin `react-router@^7` (RESEARCH A5, sanctioned fallback). Pin exact versions and commit `web/package-lock.json` (T-01-SC mitigation; the RESEARCH Package Legitimacy Audit approved all packages — SUS flags were solely `too-new` on official high-download packages, no human checkpoint required).

    Configure `web/vite.config.ts` with the `react()` and `tailwindcss()` plugins (no PostCSS config). `web/src/index.css` contains the Tailwind v4 CSS import directive; dark mode uses Tailwind's default media strategy so the theme follows the OS with zero toggle code (D-14). Styling everywhere: neutral palette, minimal, dense, function over flair (D-15).

    `web/src/lib/supabase.ts`: export a single `supabase` client from `createClient(import.meta.env.VITE_SUPABASE_URL, import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY)` with NO auth option overrides — the defaults `persistSession: true` + `autoRefreshToken: true` ARE the requirement (AUTH-02, D-06; RESEARCH Pattern 2 — do not write any custom session keep-alive code).

    `web/src/auth/AuthProvider.tsx`: React context exposing `session` and a `useSession()` hook. On mount call `supabase.auth.getSession()` once, then subscribe to `supabase.auth.onAuthStateChange`; inside the callback only set state — never await supabase calls in it (documented deadlock risk, RESEARCH Pattern 2 gotcha; add a code comment explaining why). On `PASSWORD_RECOVERY` event, navigate to `/reset-password`. Unsubscribe on unmount.

    `web/src/auth/RequireAuth.tsx`: route guard that redirects to `/login` when there is no session; render children otherwise. This guard is UX only — RLS is the enforcement boundary (RESEARCH State of the Art).

    `web/src/main.tsx`: providers in order QueryClientProvider → BrowserRouter → AuthProvider; routes: `/login` (public), `/reset-password` (public — recovery landing), and RequireAuth-guarded `/` (Dashboard), `/watchlist`, `/resumes`, `/tracker`, `/settings` rendered inside `Shell`.

    `web/src/components/Shell.tsx`: app name "Job Copilot" (D-13), nav entries exactly Dashboard, Watchlist, Resumes, Tracker, Settings (D-12), and a sign-out button calling `supabase.auth.signOut()` then redirecting to `/login`.

    Pages: `Dashboard.tsx`, `Watchlist.tsx`, `Tracker.tsx`, `Resumes.tsx`, `Settings.tsx` are "coming soon" empty states (one sentence of quiet copy each; Resumes and Settings get real implementations in plans 02/03). `Login.tsx` per D-08 exactly: app name, email field, password field, submit button calling `supabase.auth.signInWithPassword`, and a forgot-password link calling `supabase.auth.resetPasswordForEmail(email, { redirectTo: window.location.origin + '/reset-password' })` — nothing else on the page. Failed login shows a single generic error message with no hint whether the email exists (D-02, no-enumeration). `ResetPassword.tsx`: new-password form calling `supabase.auth.updateUser({ password })` then redirecting to `/` (D-05 landing page). There is NO sign-up route, link, or client sign-up call anywhere in `web/src` (D-01, D-02), and no admin-only UI or role concept exists (D-04).

    Write `web/.env.example` listing `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY` with placeholder values. Root `.gitignore`: `node_modules/`, `web/dist/`, `.env`, `.env.local`, `scripts/.env`, `.DS_Store`, `.planning/research/.cache/` — committed BEFORE any env file can exist (RESEARCH anti-pattern: secret key near frontend). The last two cover noise already present in the working tree; without them plan 03's clean-`git status --porcelain` gate can never pass.
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0 and emits `web/dist/index.html`
    - `grep -rn "signInWithPassword" web/src/pages/Login.tsx | wc -l` ≥ 1
    - `grep -rn "signUp" web/src/ | grep -v '//' | wc -l` equals 0 (no client sign-up call; comment lines filtered)
    - `grep -c "Job Copilot" web/src/components/Shell.tsx` ≥ 1 and Shell contains all five nav labels Dashboard, Watchlist, Resumes, Tracker, Settings
    - `grep -rn "persistSession" web/src/lib/supabase.ts | wc -l` equals 0 (defaults untouched — they are the D-06 requirement)
    - `.gitignore` contains the lines `scripts/.env`, `.env.local`, `.DS_Store`, and `.planning/research/.cache/`
    - `grep -c "VITE_SUPABASE_PUBLISHABLE_KEY" web/.env.example` ≥ 1 (new key naming, not legacy — RESEARCH Pitfall 6)
  </acceptance_criteria>
  <done>SPA builds; login page and guarded five-entry shell exist; zero sign-up surface; session handling is pure supabase-js defaults.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provision hosted Supabase project + populate env files (dashboard-manual)</name>
  <files>web/.env.local (user-created, gitignored), scripts/.env (user-created, gitignored)</files>
  <read_first>
    - .planning/phases/01-foundation-access/01-RESEARCH.md (Pitfall 1 reset-email delivery, Pitfall 6 key naming, Open Questions 1-3)
    - scripts/.env.example (written in Task 3 if executing out of order — otherwise use the variable list below)
  </read_first>
  <action>
    Everything below is dashboard-manual on the Supabase free tier (RESEARCH Open Q3) — Claude cannot do it via CLI/API:

    1. Create a free Supabase project (suggested name `job-copilot`) at supabase.com; record the project ref and the database password you choose.
    2. Authentication → Sign In / Providers → Email: turn **OFF** "Allow new users to sign up" (D-01 / AUTH-01 — client account creation must fail while the admin seed script still works) and set minimum password length to 8+ (ASVS V2).
    3. Authentication → URL Configuration: set Site URL to `http://localhost:5173` for now (plan 03 updates it to the deployed Pages URL — RESEARCH Pitfall 2) and add `http://localhost:5173/reset-password` to Redirect URLs.
    4. Project Settings → API keys: copy the project URL and the publishable key into `web/.env.local` as `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY`. Copy the SECRET key into `scripts/.env` as `SUPABASE_SECRET_KEY` — it must NEVER get a `VITE_` prefix or appear anywhere under `web/` (RESEARCH Pitfall 6).
    5. Account → Access Tokens: create a personal access token; put it in `scripts/.env` as `SUPABASE_ACCESS_TOKEN` (lets `supabase link` / `db push` run non-interactively). Also add `SUPABASE_URL` (project URL), `SUPABASE_PROJECT_REF`, and `SUPABASE_DB_PASSWORD` to `scripts/.env`.
    6. Add the two invited users to `scripts/.env`: `USER1_EMAIL=jk8839888@gmail.com`, `USER2_EMAIL=<second user's real email — you provide it now, D-03>`, plus strong `SEED_PASSWORD_1` and `SEED_PASSWORD_2` (delivered to each user out-of-band; both accounts have identical capability, D-04).
    7. Organization → Team: invite USER2's email as an organization member. This is what makes Supabase's default email sender deliver password-reset emails to user 2 — the default sender only delivers to team members at 2 emails/hour (RESEARCH Pitfall 1, path (a); assumption A2 gets live-verified in plan 03).
  </action>
  <verify>
    <automated>test -f web/.env.local && test -f scripts/.env && grep -q VITE_SUPABASE_URL web/.env.local && grep -q USER2_EMAIL scripts/.env</automated>
  </verify>
  <acceptance_criteria>
    - `web/.env.local` defines `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY`
    - `scripts/.env` defines `SUPABASE_URL`, `SUPABASE_SECRET_KEY`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, `SUPABASE_DB_PASSWORD`, `USER1_EMAIL`, `USER2_EMAIL`, `SEED_PASSWORD_1`, `SEED_PASSWORD_2`
    - `grep -rn "sb_secret" web/ | wc -l` equals 0 (secret key nowhere under web/, including .env.local)
    - User confirms: signup toggle OFF, user 2 invited to the organization
  </acceptance_criteria>
  <resume-signal>Type "done" once both env files are populated and the dashboard steps are complete (include USER2's email if not already in scripts/.env)</resume-signal>
  <done>Hosted project exists with signups disabled; all env values in place; user 2 is an org member.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Author migrations, push schema, seed the two accounts, prove login end-to-end</name>
  <files>supabase/config.toml, supabase/migrations/0001_profiles.sql, supabase/migrations/0002_resumes.sql, supabase/migrations/0003_storage.sql, supabase/migrations/0004_delete_my_data.sql, scripts/seed-users.ts, scripts/verify-auth.ts, scripts/.env.example</files>
  <read_first>
    - .planning/phases/01-foundation-access/01-RESEARCH.md (Code Examples 1-4 verbatim SQL/TS patterns, Pattern 3 RLS template, Pattern 4 storage policies, Environment Availability — no Docker, hosted workflow only)
    - scripts/.env (variable names only — confirm Task 2 populated everything)
  </read_first>
  <action>
    Run `web/node_modules/.bin/supabase init` in the repo root (creates `supabase/config.toml`; never run `supabase start` — Docker is unavailable, all work targets the hosted project per RESEARCH Environment Availability). All `supabase` CLI calls in this task use the pinned binary `web/node_modules/.bin/supabase` invoked from the repo root — never bare `npx supabase` (version drift).

    Author four migrations:
    - `0001_profiles.sql`: `public.profiles` (`id uuid primary key references auth.users (id) on delete cascade`, `email text not null`, `created_at timestamptz not null default now()`); enable RLS immediately after create; policies `profiles_select_own` and `profiles_update_own` for `authenticated` using `(select auth.uid()) = id`; `security definer` function `public.handle_new_user()` with `set search_path = ''` inserting the profile row, wired to trigger `on_auth_user_created` after insert on `auth.users`. No role column, no admin concept (D-04).
    - `0002_resumes.sql`: exactly the RESEARCH Code Example 2 template — `public.resumes` with `user_id uuid not null default auth.uid() references auth.users (id) on delete cascade`, `filename text not null`, `storage_path text not null unique`, `size_bytes bigint`, `created_at timestamptz not null default now()`; `enable row level security` immediately; btree index on `user_id`; four per-operation policies (`resumes_select_own`, `resumes_insert_own`, `resumes_update_own`, `resumes_delete_own`) `to authenticated` with `(select auth.uid()) = user_id`, and `with check` on INSERT and UPDATE so a row can never be re-parented to the other user (T-01-04). This file is the RLS template every later table copies (RESEARCH Pattern 3).
    - `0003_storage.sql`: exactly Code Example 3 — insert private bucket `resumes` (`public=false`, `file_size_limit` 5242880, `allowed_mime_types` DOCX + PDF only) with `on conflict (id) do nothing`; `storage.objects` policies `resumes_storage_select`, `resumes_storage_insert`, `resumes_storage_delete` for `authenticated`, each requiring `bucket_id = 'resumes' and (storage.foldername(name))[1] = (select auth.uid()::text)` (use the `auth.uid()::text` form everywhere, not the jwt-sub form — RESEARCH State of the Art).
    - `0004_delete_my_data.sql`: exactly Code Example 4 — `public.delete_my_data()` returns void, `language plpgsql`, `security invoker` (RLS still applies — convenience wrapper, not privilege escalation), `set search_path = ''`, deleting the caller's `public.resumes` rows; SQL comment noting Phases 2-4 append their user-owned tables here. Profiles row intentionally NOT deleted (account itself remains active).

    **[BLOCKING] Schema push (mandatory — phase cannot verify without it; build/type checks pass without the live schema, a false positive):** export `SUPABASE_ACCESS_TOKEN` and `SUPABASE_DB_PASSWORD` from `scripts/.env`, then run `web/node_modules/.bin/supabase link --project-ref "$SUPABASE_PROJECT_REF"` followed by `web/node_modules/.bin/supabase db push`. Both commands accept env-provided credentials and run non-TTY. If an interactive prompt still appears and cannot be suppressed with `--password "$SUPABASE_DB_PASSWORD"`, halt and surface the exact command for the user to run manually.

    `scripts/seed-users.ts` per Code Example 1: admin client from `SUPABASE_URL` + `SUPABASE_SECRET_KEY` with `autoRefreshToken:false, persistSession:false`; for USER1_EMAIL and USER2_EMAIL call `auth.admin.createUser({ email, password, email_confirm: true })` (D-01 — `email_confirm:true` sidesteps SMTP entirely for account creation); treat an "already registered" error as success so reruns are idempotent; print created user ids. Run it: `node --env-file=scripts/.env scripts/seed-users.ts`.

    `scripts/verify-auth.ts` — the walking skeleton's end-to-end test: using ONLY the publishable key (never secret), (1) `signInWithPassword` as USER1 → assert non-null session + access_token (AUTH-01); (2) same for USER2; (3) attempt client account creation for `intruder-probe@example.com` → assert it errors (signups disabled, D-01/D-02); print a PASS/FAIL line per check and exit non-zero on any failure. Run it with `node --env-file=scripts/.env scripts/verify-auth.ts`.

    Write `scripts/.env.example` documenting every variable name from Task 2 with placeholder values.
  </action>
  <verify>
    <automated>web/node_modules/.bin/supabase migration list --linked 2>/dev/null | grep -c "000" && node --env-file=scripts/.env scripts/verify-auth.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web/node_modules/.bin/supabase db push` exits 0; `web/node_modules/.bin/supabase migration list --linked` shows all four migrations applied remotely
    - `node --env-file=scripts/.env scripts/seed-users.ts` exits 0 and reports both user ids
    - `node --env-file=scripts/.env scripts/verify-auth.ts` exits 0: USER1 session non-null, USER2 session non-null, third-party client account creation rejected
    - `grep -c "with check" supabase/migrations/0002_resumes.sql` ≥ 2 (INSERT and UPDATE re-parent guard)
    - `grep -c "security invoker" supabase/migrations/0004_delete_my_data.sql` equals 1
    - `grep -c "storage.foldername" supabase/migrations/0003_storage.sql` ≥ 3 (select/insert/delete policies)
  </acceptance_criteria>
  <done>Schema live on hosted project, exactly two accounts exist, end-to-end login proven by script, client signups provably rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase Auth | Untrusted credentials cross here; GoTrue verifies |
| Developer machine → Supabase admin API | Secret key used only in local scripts/ |
| Repo → npm registry | Supply-chain surface at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing/Elevation | Login (signInWithPassword) | medium | mitigate | Supabase Auth built-in rate limits; strong seeded passwords; generic login error copy (no user enumeration, D-02); min password length 8+ in dashboard |
| T-01-02 | Information disclosure | Secret key vs client bundle | high | mitigate | Secret key only in `scripts/.env` (gitignored before creation); never `VITE_`-prefixed; grep gate over `web/` in Task 2 and again in plan 03 |
| T-01-04 | Tampering | resumes UPDATE policy | high | mitigate | `WITH CHECK ((select auth.uid()) = user_id)` on UPDATE in 0002 blocks re-parenting; probed live in plan 02 verify-rls |
| T-01-09 | Elevation | Public signup path | high | mitigate | Dashboard signup toggle OFF + zero sign-up code in client; verified by scripts/verify-auth.ts third-account probe |
| T-01-10 | Spoofing | No 2FA (D-07) | low | accept | Two known users, invite-only, strong passwords; user-locked decision D-07 |
| T-01-SC | Tampering | npm installs | high | mitigate | RESEARCH Package Legitimacy Audit ran: all packages Approved (SUS flags solely `too-new` on official 10M+/wk packages); mitigation = pin exact versions, commit lockfile, no postinstall scripts |
</threat_model>

<verification>
- `cd web && npm run build` green
- Four migrations applied to the linked hosted project
- `scripts/verify-auth.ts` exits 0 (both users in, intruder rejected)
- Manual (harvested at end of phase): open `npm run dev`, log in as user 1, refresh the tab — still signed in (AUTH-02)
</verification>

<success_criteria>
- AUTH-01 (local): exactly two accounts can authenticate; no client-side account-creation path exists or works
- AUTH-02 (local): session survives refresh via untouched supabase-js defaults
- Schema foundation for AUTH-03/AUTH-04 live: RLS-enabled resumes + profiles, storage policies, delete_my_data RPC
</success_criteria>

<output>
Create `.planning/phases/01-foundation-access/01-01-SUMMARY.md` when done
</output>
