---
phase: 03.4-replace-automatic-ai-scoring-with-deterministic-ranking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/score-tick/index.ts
  - supabase/functions/_shared/filters.ts
  - supabase/functions/_shared/deterministic-ranking.ts
  - web/tests/deterministic-ranking.test.ts
  - web/tests/deterministic-ranking-source.test.ts
  - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-01-CONTAINMENT-EVIDENCE.md
autonomous: false
requirements: [SCOR-01, SCOR-02, SCOR-03]
must_haves:
  truths:
    - "D-24: The deployed every-minute score-tick cannot call an AI provider, reserve paid score capacity, or write score-purpose usage, while POST-only and x-cron-secret authorization remain unchanged."
    - "D-01 through D-08 and D-10 through D-15: One pure evaluator reproducibly applies the complete owner-approved 100-point formula, hard filters, editable thresholds, and evidence contract."
    - "D-07: An explicitly stated minimum required experience strictly below the user's configured maximum earns 20 points; equal/above stays visible and earns 0; missing, ambiguous, preferred, or non-required experience earns 0."
    - "AI resume extraction remains intact, and D-25's future manual job-detail AI scoring is neither implemented nor made impossible by destructive cleanup."
    - "Containment reaches production and passes cost-ledger invariance before evaluator foundation work can delay the safety outcome."
  artifacts:
    - path: "supabase/functions/score-tick/index.ts"
      provides: "Schema-compatible, cron-authenticated containment endpoint with no automatic AI job-scoring path"
    - path: "supabase/functions/_shared/deterministic-ranking.ts"
      provides: "Pure rubric validator, US classifier, title evaluator, and deterministic score/evidence evaluator"
      exports: ["DEFAULT_RANKING_RUBRIC", "validateRankingRubric", "classifyUsLocation", "evaluateTitleMatch", "evaluateDeterministicRanking"]
    - path: "web/tests/deterministic-ranking.test.ts"
      provides: "Exact 100-point, hard-filter, threshold, evidence, and boundary fixtures"
    - path: "web/tests/deterministic-ranking-source.test.ts"
      provides: "Static no-automatic-AI and preserved-extraction source contract"
    - path: ".planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-01-CONTAINMENT-EVIDENCE.md"
      provides: "Approved worker identity and before/after paid-score invariance evidence"
  key_links:
    - from: "score-tick-every-minute"
      to: "supabase/functions/score-tick/index.ts"
      via: "unchanged endpoint and x-cron-secret request boundary"
      pattern: "x-cron-secret"
    - from: "supabase/functions/_shared/filters.ts"
      to: "supabase/functions/_shared/deterministic-ranking.ts"
      via: "shared normalized title concepts, exclusions, and restored explicit required-experience parser"
      pattern: "evaluateTitleMatch|experienceMinimumRequired"
    - from: "web/tests/deterministic-ranking-source.test.ts"
      to: "supabase/functions/score-tick/index.ts"
      via: "source contract rejects provider, paid reservation, and score-purpose accounting capabilities"
      pattern: "score-tick"
---

<objective>
Contain automatic paid job scoring in production first, then establish the pure deterministic evaluator against the locked rubric.

Purpose: Stop the imminent cost and mixed-semantics risk without breaking the existing scheduler/auth contract, while giving later schema and worker plans one tested scoring authority.
Output: A deployed no-AI score-tick containment release, immutable invariance evidence, and a complete pure deterministic evaluator.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-CONTEXT.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-RESEARCH.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-PATTERNS.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UI-SPEC.md
@supabase/functions/score-tick/index.ts
@supabase/functions/_shared/filters.ts
@supabase/functions/extract-resume/index.ts
@supabase/migrations/0019_user_jobs_scoring.sql
@supabase/migrations/0027_score_budget_after_free_work.sql

<interfaces>
- The containment build keeps `POST /functions/v1/score-tick`, `verify_jwt=false`, and the in-handler `x-cron-secret` comparison so the existing pg_cron row and secret remain compatible.
- `extract-resume` may continue to use OpenAI with `purpose='extract'`; the forbidden capability is automatic job scoring.
- The deterministic evaluator is pure TypeScript and installs no package.
- Default maximum arithmetic is `strict_title + location + recency + watchlist + experience + include_5_plus = 100`; weak title and intermediate keyword steps are alternate awards, not extra category maxima.
</interfaces>
</context>

<scope_fence>
Remove only the automatic/background job-scoring capability. Do not change ingestion, pg_cron cadence, cron secret, resume extraction, watchlist ownership, Dashboard queries, production schema, user data, AI secrets, or Phase 4 tailoring. Do not add the deferred watchlist-only filter or future manual AI-score actions.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the deployable no-AI containment worker</name>
  <read_first>
    - supabase/functions/score-tick/index.ts
    - supabase/functions/extract-resume/index.ts
    - supabase/migrations/0019_user_jobs_scoring.sql
    - supabase/migrations/0027_score_budget_after_free_work.sql
    - web/tests/scoring-input.test.ts
  </read_first>
  <files>supabase/functions/score-tick/index.ts, web/tests/deterministic-ranking-source.test.ts</files>
  <behavior>
    - A non-POST request returns 405 and a wrong or missing cron secret returns 401.
    - An authorized invocation returns a bounded successful containment response without claiming legacy scoring work or mutating user rows.
    - The automatic worker has no provider client/model/key access, paid score reservation, retry branch capable of paid scoring, or score-purpose usage write.
    - Resume extraction still has its separately bounded extraction provider and extraction-purpose accounting path.
  </behavior>
  <action>Write the failing source/handler contract first, then replace the current score-tick implementation with the smallest schema-compatible containment endpoint per D-24. Preserve its existing POST and `x-cron-secret` checks, non-persistent service-client setup only if needed for a bounded health response, endpoint name, deployment posture, and safe diagnostic responses. Remove every job-scoring provider import, model/key read, score request reservation, paid retry/fallback path, legacy AI field write, and score-purpose usage insert from this function rather than hiding them behind a flag. Do not call `claim_scoring_work`, `mark_recent_jobs_for_refilter`, or a new schema object before the additive migration exists. The source test must distinguish allowed resume extraction from forbidden job scoring and prove preferences, resume events, ingestion, and retries cannot reach a paid job-score capability through score-tick. Do not alter `extract-resume` or production in this task.</action>
  <acceptance_criteria>
    - Authorized containment preserves HTTP/cron compatibility and performs zero user-job, budget, usage, or provider mutation.
    - Static tests fail on restoration of any automatic job-scoring provider, budget, fallback, usage, or legacy AI publication capability in score-tick.
    - Extraction-only provider behavior remains source-visible and is explicitly allowed only for resume extraction.
    - No migration, scheduler, secret, preference, resume, feed, or production state changes occur.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm test -- --run tests/deterministic-ranking-source.test.ts tests/scoring-input.test.ts</automated>
  </verify>
  <done>The exact score-tick source is deployable against the current production schema and cannot spend or account for an automatic AI job score.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: [BLOCKING] Approve the containment-only production deployment</name>
  <read_first>
    - supabase/functions/score-tick/index.ts
    - web/tests/deterministic-ranking-source.test.ts
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-04-ROLLOUT-EVIDENCE.md
  </read_first>
  <files>None</files>
  <decision>Whether to deploy only the reviewed no-AI score-tick function before the next UTC score-budget reset.</decision>
  <context>Task 1 changes no schema or scheduler. Approval is limited to deploying the exact reviewed score-tick source with `--no-verify-jwt`, preserving the existing handler-level cron secret, invoking it once with the existing cron secret, and reading paid-score counters before and after. It does not authorize schema changes, preference saves, backfill, web deployment, scheduler edits, user-row writes, secret changes, extraction calls, or any AI request.</context>
  <action>Present the exact commit/source hash, focused test results, linked Supabase project, deployment command, current hosted function identity, `ai_usage` score-purpose count, and `score_request_budget` snapshot. Require explicit approval bound to those values. Stop on ambiguous assent, counter/read failure, unexpected dirty release files, or identity drift; perform no production mutation in this checkpoint.</action>
  <options>
    <option id="approve-containment">
      <name>Approve containment</name>
      <pros>Automatic paid job scoring stops while cron/auth/schema compatibility remains intact.</pros>
      <cons>The existing Dashboard remains on its last complete AI-era projection until deterministic backfill and web cutover.</cons>
    </option>
    <option id="decline-containment">
      <name>Decline deployment</name>
      <pros>Production remains unchanged.</pros>
      <cons>The every-minute worker can continue reserving or making paid score calls.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The owner sees the exact deploy target, immutable source identity, baseline cost counters, allowed invocation, and excluded mutations.
    - Approval is explicit and scope-bound; silence is not approval.
    - No outward mutation occurs inside the checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>git diff --check &amp;&amp; cd web &amp;&amp; npm test -- --run tests/deterministic-ranking-source.test.ts</automated>
    <human-check>Choose approve or decline after reviewing the containment-only deployment and zero-paid-effect proof boundary.</human-check>
  </verify>
  <resume-signal>Type `approve containment` or `decline`.</resume-signal>
  <done>The exact containment deployment is explicitly approved, or the plan stops with production unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deploy and prove containment, then implement the pure evaluator</name>
  <read_first>
    - supabase/functions/score-tick/index.ts
    - supabase/functions/_shared/filters.ts
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-CONTEXT.md
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-PATTERNS.md
    - web/tests/filters.test.ts
  </read_first>
  <files>supabase/functions/_shared/filters.ts, supabase/functions/_shared/deterministic-ranking.ts, web/tests/deterministic-ranking.test.ts, .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-01-CONTAINMENT-EVIDENCE.md</files>
  <behavior>
    - Strict normalized title phrase earns its configured strict award; otherwise all significant synonymous/inflected concepts earn weak, and neither match is ineligible.
    - Explicit foreign locations are ineligible; explicit US, blank, countryless Remote, ambiguous, and unknown locations remain eligible.
    - Required experience earns points only when an explicit required minimum is strictly below the configured maximum.
    - Unique description-only include matches award the editable nondecreasing 1/2/3/4/5+ ladder.
    - Watchlist `company_id`, preferred location phrase, and `posted_at` strictly within the preceding 24 hours earn their configured points.
    - Identical captured inputs and evaluation time produce byte-equivalent score, stored tier, filter code/detail, and six-row evidence breakdown.
  </behavior>
  <action>First abort unless Task 2 recorded `approve containment` for the unchanged source identity. Deploy only score-tick with its existing no-JWT gateway posture, invoke the cron-authenticated endpoint once, and record hosted identity plus unchanged score-purpose usage and reservation counters in `03.4-01-CONTAINMENT-EVIDENCE.md`; any nonzero delta, auth mismatch, or response drift blocks further work. Only after containment is proven, implement D-01 through D-15 as a pure evaluator. Export the hardened required-experience parser restored from the final Phase 03.2 history; retain D-12 title/general exclusions; remove preferred location from the hard-filter consumer per D-04/D-13; implement the conservative three-state US classifier for D-11; make strict/weak title evaluation share the existing synonym and inflection primitives for D-02/D-03/D-10; count unique include keywords in description only for D-08; use `posted_at` and an injected evaluation time for D-05; use non-null `company_id` for D-06. Validate exact rubric keys, integer bounds, weak-at-most-strict, 100-point maxima, nondecreasing keyword steps, and D-14/D-15 thresholds. Return six bounded text-evidence rows per D-18 without HTML or resume content. Do not deploy the evaluator or touch schema/UI.</action>
  <acceptance_criteria>
    - Production containment identity is recorded before evaluator work, and both paid-score counters are unchanged across the authorized invocation.
    - Exact default fixtures can earn 100 points and hit Strong/Good/Weak boundaries under stored thresholds.
    - Tests cover strict precedence, weak all-concepts matching, explicit-foreign versus unknown/Remote eligibility, strict experience `< max`, keyword ladder steps, watchlist bonus, and the 24-hour expiry boundary.
    - Invalid custom rubrics and thresholds are rejected with bounded errors; identical inputs are reproducible.
    - No automatic AI scoring, schema mutation, Dashboard cutover, watchlist redesign, or future manual-AI action is introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm test -- --run tests/deterministic-ranking.test.ts tests/deterministic-ranking-source.test.ts tests/filters.test.ts</automated>
  </verify>
  <done>Automatic production scoring is contained with cost invariance, and one tested pure evaluator fully implements D-01 through D-15 and D-18.</done>
</task>

</tasks>

<threat_model>
## ASVS Level

OWASP ASVS Level 1. Any HIGH or CRITICAL finding blocks deployment or completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| pg_cron → score-tick | Unauthenticated gateway traffic reaches a privileged function protected by a shared cron secret. |
| score-tick → AI/budget/data services | The legacy worker possessed paid-provider and service-role mutation capabilities that containment removes. |
| Job/preference text → evaluator | Untrusted bounded text is parsed into eligibility, score, and plain-text evidence. |
| Local release → hosted Edge function | A production function deployment must bind to reviewed source and approved scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.4-01 | Spoofing / Elevation of privilege | score-tick handler | high | mitigate | Preserve POST-only and exact `x-cron-secret` comparison, never log the secret, and reject unauthorized calls before privileged client creation; block release on handler tests. |
| T-03.4-02 | Denial of service / Financial abuse | automatic scoring capability | critical | mitigate | Remove provider, reservation, retry, fallback, and score-purpose accounting capabilities; deploy containment first and prove both ledgers invariant. |
| T-03.4-03 | Tampering | evaluator inputs and rubric | high | mitigate | Validate closed keys, integer/cardinality/byte bounds, exact 100-point maxima, monotonic steps, thresholds, and deterministic injected time before evaluation. |
| T-03.4-04 | Information disclosure | stored/logged evidence | medium | mitigate | Emit bounded factual tokens/timestamps only; never include resume text, prompts, secrets, provider responses, or raw errors. |
| T-03.4-05 | Repudiation | containment rollout | high | mitigate | Require explicit exact-source approval and record before/after identities, invocation result, and both cost-ledger snapshots without credentials. |
| T-03.4-SC | Tampering | package supply chain | high | mitigate | Install no packages; use only locked repository dependencies and the existing Supabase CLI. |
</threat_model>

## Artifacts this phase produces

- `supabase/functions/score-tick/index.ts`: no-AI, no-reservation, cron-compatible containment handler.
- `supabase/functions/_shared/filters.ts`: exported reusable title/exclusion concepts plus restored `experienceMinimumRequired`.
- `supabase/functions/_shared/deterministic-ranking.ts`: `RankingRubric`, `RankingThresholds`, `RankingBreakdownRow`, `DeterministicResult`, `DEFAULT_RANKING_RUBRIC`, `validateRankingRubric`, `classifyUsLocation`, `evaluateTitleMatch`, and `evaluateDeterministicRanking`.
- `web/tests/deterministic-ranking.test.ts`: exact evaluator and boundary fixture suite.
- `web/tests/deterministic-ranking-source.test.ts`: automatic-job-score capability and extraction-boundary source suite.
- `.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-01-CONTAINMENT-EVIDENCE.md`: approved hosted function and zero-paid-effect evidence.
- Hosted score-tick containment deployment identity; no new RPC, table, column, package, or scheduler is introduced by this plan.

<verification>
1. Focused source tests prove the deployed score-tick retains cron authorization but possesses no automatic paid job-scoring capability.
2. Read-only before/after production counters prove the approved containment invocation changes neither score-purpose usage nor score-request reservations.
3. Pure evaluator tests cover the exact 100-point formula, strict/weak title behavior, US classifier, strict experience boundary, description keyword ladder, watchlist bonus, recency expiry, editable validation, tiers, and deterministic evidence.
4. Diff review confirms ingestion, extraction, browser queries, schema, watchlist ownership, and Phase 4 code are untouched.
</verification>

<success_criteria>
- Production automatic AI job scoring is stopped before the next UTC reset without altering cron/auth/schema compatibility.
- Resume extraction remains operational and no deferred manual-AI UI/action is added.
- D-01 through D-15, D-18, D-24, and D-25 have executable source/tests or an explicit preserved/deferred boundary.
- All HIGH/CRITICAL threat mitigations and focused tests pass.
</success_criteria>

<output>
Create `.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-01-SUMMARY.md` when done.
</output>
