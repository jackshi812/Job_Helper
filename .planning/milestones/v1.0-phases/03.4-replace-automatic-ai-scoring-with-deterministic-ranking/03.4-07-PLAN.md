---
phase: 03.4-replace-automatic-ai-scoring-with-deterministic-ranking
plan: 07
type: execute
wave: 5
depends_on: [03.4-04]
files_modified:
  - scripts/verify-phase-03-4-release.mjs
  - scripts/verify-phase-03-4-release.test.mjs
autonomous: true
gap_closure: true
requirements: [SCOR-02, SCOR-04, SCOR-05]
must_haves:
  truths:
    - "Release verification hashes independently downloaded hosted function source rather than copying approved local hashes into hosted evidence."
    - "The exact hosted function ID/version is stable across metadata reads bracketing the download."
    - "Verification fails closed when remote source cannot be downloaded, parsed, or matched to the approved entry and transitive bundle inventory."
  artifacts:
    - path: "scripts/verify-phase-03-4-release.mjs"
      provides: "Independent hosted-source download, inventory, and hash comparison"
      contains: "functionProbe"
    - path: "scripts/verify-phase-03-4-release.test.mjs"
      provides: "Remote/local mismatch, version-race, and unavailable-source failures"
  key_links:
    - from: "scripts/verify-phase-03-4-release.mjs"
      to: "web/node_modules/.bin/supabase"
      via: "Pinned local CLI downloads hosted source with functions download --use-api"
      pattern: "functions.*download|--use-api"
    - from: "scripts/verify-phase-03-4-release.mjs"
      to: "supabase/functions/score-tick/index.ts"
      via: "Remote inventory is compared to independently computed approved local entry and transitive bundle hashes"
      pattern: "indexSha256|bundleManifestSha256"
---

<objective>
Make the deterministic release verifier independently prove the bytes currently hosted by Supabase.

Purpose: Close the release-integrity defect where hosted hash fields were populated from local inputs, while preserving D-16, D-17, D-24 and the exact-release boundary established by Plan 04.
Output: A fail-closed remote-source probe and adversarial standard-library verifier tests.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-VERIFICATION.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-REVIEW.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-SUMMARY.md
@scripts/verify-phase-03-4-release.mjs
@scripts/verify-phase-03-4-release.test.mjs

<interfaces>
- Supabase CLI contract: `supabase functions download [Function name] --project-ref &lt;ref&gt; --use-api`.
- Existing verifier inventory: entry SHA-256 plus deterministic transitive bundle-manifest SHA-256.
- Existing hosted metadata fields: stable function ID, version, status, and `verify_jwt`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace local hash substitution with an independent hosted-source probe</name>
  <files>scripts/verify-phase-03-4-release.mjs, scripts/verify-phase-03-4-release.test.mjs</files>
  <read_first>
    - scripts/verify-phase-03-4-release.mjs
    - scripts/verify-phase-03-4-release.test.mjs
    - supabase/functions/score-tick/index.ts
    - supabase/functions/extract-resume/index.ts
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-VERIFICATION.md
  </read_first>
  <behavior>
    - Hosted probe hashes originate only from files downloaded from the remote project into a fresh temporary directory.
    - Metadata ID/version/status/verify_jwt are read before and after download; any identity/version change fails the probe as a race.
    - Downloaded entry and every transitive local function dependency are canonicalized with the same manifest rules as approved local source.
    - Missing files, unexpected files in the approved transitive set, download failure, malformed output, and any entry/manifest mismatch fail closed.
    - Temporary source is removed after success or failure and secrets never enter output/evidence.
  </behavior>
  <action>
    Write failing tests around an injectable command runner and temporary directory. Change `functionProbe` so it cannot accept local hashes as hosted values. Use Node standard library `mkdtemp`, `spawn`/`execFile`, file walking, SHA-256, and cleanup. Invoke the repository-pinned Supabase CLI directly from `web/node_modules/.bin` with `functions download &lt;slug&gt; --project-ref &lt;ref&gt; --use-api --yes` in an isolated work directory; do not use `npx --yes`, install a package, or download into tracked `supabase/functions`. Compute remote entry and bundle-manifest hashes from downloaded bytes, bracket the operation with hosted metadata reads, then compare remote results to the separately approved local inventory. Redact tokens, project credentials, and downloaded source bodies from logs.
  </action>
  <verify>
    <automated>node --test scripts/verify-phase-03-4-release.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - No hosted hash field is assigned from a local inventory value.
    - Exact downloaded bytes pass; one-byte remote drift fails.
    - A changing hosted version, unavailable download, incomplete bundle, or command failure fails.
    - Tests use temporary fixtures and never contact or mutate production.
  </acceptance_criteria>
  <done>The verifier independently measures hosted source and cannot certify an unobserved deployment.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the complete fail-closed release-verifier contract offline</name>
  <files>scripts/verify-phase-03-4-release.mjs, scripts/verify-phase-03-4-release.test.mjs</files>
  <read_first>
    - scripts/verify-phase-03-4-release.mjs
    - scripts/verify-phase-03-4-release.test.mjs
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-ROLLOUT-EVIDENCE.md
  </read_first>
  <action>
    Extend existing preflight/post-release fixture coverage so remote hashes, remote version identity, local source hashes, exact release approval, migration parity, paid-ledger invariance, Git/Cloudflare identity, and immutable web asset remain independent inputs. Add explicit negative fixtures for swapped local/remote fields and stale cached downloads. Keep this task offline and read-only; the first real hosted download belongs to the separately approved Plan 09 preflight.
  </action>
  <verify>
    <automated>node --test scripts/verify-phase-03-4-release.test.mjs &amp;&amp; cd web &amp;&amp; npm test -- --run tests/deterministic-ranking-source.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All existing release safety failures still fail.
    - Remote-source proof is required in both preflight and post-release modes.
    - No fixture can pass by echoing approved local values into the hosted section.
  </acceptance_criteria>
  <done>The offline verifier contract proves independence before any hosted command is authorized.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- Independent downloaded-function hashing in the Phase 03.4 release verifier.
- Version-race, download-failure, incomplete-bundle, and byte-drift regressions.
- A remote/local provenance boundary that fails closed.
</artifacts_this_phase_produces>

<threat_model>
ASVS level: L1. Block execution completion on any unmitigated high or critical finding.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.4-07-01 | Tampering | Hosted release provenance | high | mitigate | Hash independently downloaded remote bytes and compare stable metadata before/after retrieval. |
| T-03.4-07-02 | Spoofing | CLI/project target | high | mitigate | Use explicit approved project ref, pinned local CLI, and exact function slug; reject identity/version drift. |
| T-03.4-07-03 | Information Disclosure | Download/logging | high | mitigate | Use private temporary paths, emit hashes/metadata only, redact credentials, and clean up in `finally`. |
| T-03.4-07-04 | Denial of Service | Remote download | medium | mitigate | Bound command duration/output and fail closed without retry storms. |
| T-03.4-07-SC | Tampering | Dependency supply chain | low | accept | No install; execute the lockfile-backed Supabase CLI already present in `web/node_modules`. |
</threat_model>

<verification>
- Node verifier suite passes without network.
- Focused no-AI source contract passes.
- Full suite/build/lint and `git diff --check` pass.
- No hosted command or production mutation occurs in this plan.
</verification>

<success_criteria>
- Verification gap 5 is closed in source and adversarial tests.
- Hosted and local hashes have independently traceable provenance.
- Real hosted proof remains gated by Plan 09 exact-release approval.
</success_criteria>

<output>
Create `.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-07-SUMMARY.md` when done.
</output>
