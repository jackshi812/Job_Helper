---
phase: 03.4-replace-automatic-ai-scoring-with-deterministic-ranking
plan: 06
type: execute
wave: 5
depends_on: [03.4-04]
files_modified:
  - web/src/lib/preferences.ts
  - web/src/lib/preferences.test.ts
  - web/src/pages/Preferences.tsx
  - web/src/pages/Preferences.test.tsx
  - web/src/lib/feed.ts
  - web/src/lib/feed.test.ts
  - web/src/lib/resumes.ts
  - web/src/lib/resumes.test.ts
autonomous: true
gap_closure: true
requirements: [PREF-01, SCOR-04, SCOR-05]
must_haves:
  truths:
    - "Every persisted preference text-array is checked against the same count, serialized-byte, trimmed-entry, and entry-length contract before the save RPC."
    - "The first invalid preference group exposes a field-specific accessible error and receives focus after submit."
    - "The database query excludes closed jobs before applying the 200-row Dashboard cap."
    - "A durable resume upload or deletion is reported as successful even if deterministic route-refresh work must be retried separately."
  artifacts:
    - path: "web/src/lib/preferences.ts"
      provides: "One shared ranking text-array validator"
      contains: "validate"
    - path: "web/src/pages/Preferences.tsx"
      provides: "Accessible group errors and first-invalid focus"
    - path: "web/src/lib/feed.ts"
      provides: "Open-job server query before limit"
      contains: "listFeed"
    - path: "web/src/lib/resumes.ts"
      provides: "Truthful primary mutation result without fallible post-commit RPC"
  key_links:
    - from: "web/src/pages/Preferences.tsx"
      to: "web/src/lib/preferences.ts"
      via: "Submit preflight uses the shared validator for all five arrays"
      pattern: "validate.*TextArray"
    - from: "web/src/lib/feed.ts"
      to: "public.jobs"
      via: "Inner embedded relation and open-status predicate precede ordering and limit"
      pattern: "jobs.*inner|jobs.status"
    - from: "web/src/lib/resumes.ts"
      to: "supabase/migrations/0032_deterministic_ranking.sql"
      via: "Plan 08 database trigger/outbox owns refresh signaling after metadata commit"
      pattern: "resumes"
---

<objective>
Close browser-side validation, feed completeness, and truthful resume-mutation gaps while preserving the approved UI and all unrelated interactions.

Purpose: Make PREF-01 and SCOR-04 observable at browser boundaries, and make the browser tell the truth about already committed resume operations while Plan 08 supplies transactional refresh signaling.
Output: Shared client validation, a pre-limit open-job query, and post-commit-safe resume helpers with focused tests.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-CONTEXT.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UI-SPEC.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-VERIFICATION.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-03-SUMMARY.md
@web/src/lib/preferences.ts
@web/src/pages/Preferences.tsx
@web/src/lib/feed.ts
@web/src/lib/resumes.ts

<interfaces>
- `savePreferences(input: SavePreferencesInput): Promise<SavePreferencesResult>`
- Existing server contract `is_valid_ranking_text_array(values, 50)`: no more than 50 entries, each trimmed and 1..200 characters, bounded serialized UTF-8 payload of 4,096 bytes.
- `listFeed(): Promise&lt;FeedRow[]&gt;`
- `uploadResume(file, displayName?): Promise&lt;ResumeRecord&gt;`
- `deleteResume(input): Promise&lt;void&gt;`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply one accessible text-array contract to every preference field</name>
  <files>web/src/lib/preferences.ts, web/src/lib/preferences.test.ts, web/src/pages/Preferences.tsx, web/src/pages/Preferences.test.tsx</files>
  <read_first>
    - web/src/lib/preferences.ts
    - web/src/lib/preferences.test.ts
    - web/src/pages/Preferences.tsx
    - web/src/pages/Preferences.test.tsx
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UI-SPEC.md
  </read_first>
  <behavior>
    - Titles, locations, include keywords, general excludes, and title excludes share one UTF-8-aware validator matching the server.
    - Invalid count, serialized byte size, blank/whitespace entry, or overlong entry blocks RPC invocation.
    - The affected semantic group exposes bounded field-specific copy with `aria-invalid`/`aria-describedby`.
    - Submit focuses the first invalid chip input in visual form order and retains all edits.
    - An authoritative `invalid_ranking_preferences`/generic RPC rejection revalidates the retained titles, locations, include keywords, general excludes, and title excludes against the exact current server bounds, exposes the offending group, and focuses its input.
    - Any server rejection retains every title/location/keyword chip and rubric edit; it never resets to the last saved record.
    - Valid keyword chips and the rubric validation flow remain unchanged.
  </behavior>
  <action>
    Generalize the current title-exclusion validator into a named shared validator that accepts a field identity and returns structured validation information suitable for both the service boundary and Preferences page. Apply it to all five arrays before `save_preferences_and_start_ranking`. In the page, validate in the UI-SPEC form order, attach an accessible error to the invalid group, focus its chip input on submit, retain draft chips, and keep the exact rubric/form-level save behavior. On an authoritative generic/`invalid_ranking_preferences` RPC rejection, preserve the submitted draft, re-run the exact shared validation over every array, map any detected violation to its group and focus target, and retain the bounded form-level message when the server supplies no safe field detail. Add the newly reported include-keyword save rejection as a regression at the exact 50-entry, 4,096 UTF-8-byte, and 200-character boundaries. Preserve NFKC comparison and first-entered spelling from Phase 03.3. Do not mutate or save while testing.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- --run src/lib/preferences.test.ts src/pages/Preferences.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five array fields has count, byte, whitespace, and entry-length boundary tests.
    - No invalid payload reaches the mocked RPC.
    - Accessible error association and first-invalid focus are asserted.
    - Generic server rejection preserves all edits and maps a reproducible keyword/array violation to the correct accessible group.
    - Valid saves still send the complete atomic payload once.
  </acceptance_criteria>
  <done>PREF-01 has equivalent server and browser bounds with actionable accessible feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter open jobs before the Dashboard row cap</name>
  <files>web/src/lib/feed.ts, web/src/lib/feed.test.ts</files>
  <read_first>
    - web/src/lib/feed.ts
    - web/src/lib/feed.test.ts
    - web/src/lib/dashboard.ts
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UI-SPEC.md
  </read_first>
  <behavior>
    - The PostgREST request uses an inner jobs relation and an open-status predicate before `.limit(200)`.
    - More than 200 newer closed rows cannot hide later eligible open rows.
    - Company identity, deterministic visibility, newest-first order, score sort inputs, and stored-tier authority remain unchanged.
  </behavior>
  <action>
    Change the list projection to an inner embedded jobs relation and apply the related-table open predicate before ordering and limiting. Keep `deterministicVisible` as a defense-in-depth shape/active-revision guard, not the primary closed-job filter. Extend the query-builder test to prove call arguments and operation order, and add a data fixture demonstrating that the intended 200 slots are open eligible jobs rather than a pre-filter slice. Plan 08 supplies the runtime-backed database proof; do not replace it with another standalone model here.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- --run src/lib/feed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The generated query constrains `jobs.status` to `open` before the cap.
    - Existing list/detail deterministic visibility tests pass.
    - The regression fixture covers more than 200 newer closed rows.
  </acceptance_criteria>
  <done>The Dashboard cap applies to eligible open jobs and cannot be consumed by closed rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Report durable resume mutations truthfully</name>
  <files>web/src/lib/resumes.ts, web/src/lib/resumes.test.ts</files>
  <read_first>
    - web/src/lib/resumes.ts
    - web/src/lib/resumes.test.ts
    - supabase/migrations/0032_deterministic_ranking.sql
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-VERIFICATION.md
  </read_first>
  <behavior>
    - Successful storage plus metadata insert resolves with the uploaded record and does not make a fallible post-commit refresh RPC.
    - Successful storage plus metadata deletion resolves successfully and does not make a fallible post-commit refresh RPC.
    - Storage-first rollback/error behavior remains unchanged for pre-commit failures.
    - Plan 08 database-backed tests prove metadata insert/delete transactionally records route-refresh demand.
  </behavior>
  <action>
    Remove the separate post-commit `request_deterministic_route_refresh` calls from browser upload/delete helpers. Keep storage-first deletion and failed-insert object cleanup exactly as established. Treat the primary metadata mutation response as the user-visible truth; route-refresh durability moves to the database trigger/outbox contract created in Plan 08. Update tests to prove RPC failure can no longer turn a committed upload/delete into a rejected helper result and to preserve every pre-commit error case.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- --run src/lib/resumes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No browser route-refresh RPC occurs after a committed upload or delete.
    - Upload cleanup and storage-first delete protections still pass.
    - Tests no longer describe a committed primary mutation as failed.
  </acceptance_criteria>
  <done>Resume helpers report durable operation truth while transactional database signaling owns downstream refresh.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- One shared client text-array validator used by all preference arrays.
- Accessible per-group validation and focus behavior matching the UI-SPEC.
- A server-filtered open-job Dashboard query.
- Resume helpers that cannot reject solely after their durable mutation committed.
</artifacts_this_phase_produces>

<threat_model>
ASVS level: L1. Block execution completion on any unmitigated high or critical finding.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.4-06-01 | Denial of Service | Preference arrays | high | mitigate | Mirror server count, byte, trim, and entry-length bounds before RPC; server validation remains authoritative. |
| T-03.4-06-02 | Tampering | Feed projection | high | mitigate | Apply open-state filtering in PostgREST before the cap and retain deterministic active-revision checks. |
| T-03.4-06-03 | Repudiation | Resume mutations | medium | mitigate | Return the durable primary mutation truth; database transaction records refresh demand independently. |
| T-03.4-06-04 | Information Disclosure | UI errors | low | accept | Show bounded field-specific messages only; never surface raw PostgREST payloads. |
| T-03.4-06-SC | Tampering | Dependency supply chain | low | accept | No package install; use existing React, Supabase JS, and Vitest versions. |
</threat_model>

<verification>
- Focused preference, feed, and resume suites pass.
- Full web suite, build, lint, and `git diff --check` pass.
- UI source preserves the Phase 03.4 UI-SPEC copy, semantics, and form order.
</verification>

<success_criteria>
- Verification gaps 4 (browser half), 6, and 7 (client half) are closed.
- Dashboard structure, sorting, resizing, company/tier controls, apply, dismiss, and detail navigation are preserved.
- No preference or production mutation occurs during plan verification.
</success_criteria>

<output>
Create `.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-06-SUMMARY.md` when done.
</output>
