---
phase: 02-watchlist-ingestion-monitoring
plan: 7
type: execute
wave: 2
depends_on: [02-04, 02-05, 02-06]
files_modified: []
autonomous: true
gap_closure: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-05, DISC-06]
must_haves:
  truths:
    - "The hosted database runs the locking claim_due_companies (migration 0008) and discovery health columns + accepted Chicago-local cadence (migration 0009)"
    - "Deployed poll-tick, discovery-sweep, and heartbeat functions carry the gap-closure behavior (reopen, no-work heartbeat, discovery health, 503 surfaces)"
    - "Hosted probes 1-16 pass, including concurrent-claim disjointness, no-work heartbeat advance, close-then-return reopen, and discovery health surfacing"
    - "The reworked watchlist verifier runs to completion against the hosted project and proves seed rows and job foreign keys unchanged"
  artifacts:
    - path: "supabase/migrations/0008_claim_exclusive.sql"
      provides: "Applied to the hosted database (remote migration list includes 0008)"
    - path: "supabase/migrations/0009_discovery_health_cadence.sql"
      provides: "Applied to the hosted database (remote migration list includes 0009)"
  key_links:
    - from: "supabase/migrations/0009_discovery_health_cadence.sql"
      to: "supabase/functions/discovery-sweep/index.ts"
      via: "discovery-sweep-schedule-gate invoking the redeployed function every 30 minutes; the function admits Chicago-local slots"
      pattern: "discovery-sweep"
    - from: "scripts/verify-pipeline.ts"
      to: "hosted project"
      via: "node --env-file=scripts/.env scripts/verify-pipeline.ts exercising probes 1-16"
      pattern: "probe 16"
---

<objective>
Ship the wave-1 gap fixes to the hosted `job-copilot` project and prove them there. Migrations 0008 (exclusive claims) and 0009 (discovery health + cadence) must be pushed — build and type checks pass without the push, which would leave verification falsely green — and the three edge functions redeployed, then the extended hosted probe suites run end to end.

Purpose: verification gaps only flip to passed when the hosted pipeline demonstrates the corrected behavior; this plan is the single deployment + proof gate for Plans 02-04/02-05/02-06.
Output: hosted schema at 0009, three redeployed functions, probes 1-16 + watchlist verifier passing, web suite and build green.
</objective>

## Phase Goal

**As a** job seeker, **I want to** receive watched-site postings exactly once within 5-15 minutes and aggregator discovery every 30 minutes from 6 AM-noon Chicago and every two hours otherwise, **so that** I can trust my job feed without manually checking each career site.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-VERIFICATION.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-USER-SETUP.md
</context>

## Artifacts this phase produces (Plan 07 portion)

- **Hosted state:** remote migration list includes `0008` and `0009`; cron job `discovery-sweep-schedule-gate` is live and prior discovery jobs are removed; `pipeline_heartbeat` has `last_discovery_at`, `last_discovery_success_at`, `discovery_status`
- **Deployed functions:** `poll-tick` (reopen + no-work heartbeat), `discovery-sweep` (health + 503 + dedupe), `heartbeat` (discovery-failed 503) — all with existing `verify_jwt = false` config from supabase/config.toml
- **Evidence:** probes 1-16 output from `scripts/verify-pipeline.ts`; probe 1-6 output from `scripts/verify-watchlist.ts` (non-destructive rerun proof)

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push migrations 0008 + 0009 and redeploy the three edge functions</name>
  <files>supabase/migrations/0008_claim_exclusive.sql, supabase/migrations/0009_discovery_health_cadence.sql</files>
  <read_first>
    - .planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md (hosted mutation conventions: user approval before hosted changes, gitignored credentials in scripts/.env, linked project fjcsvajkkztvlrpdplwx)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-01-SUMMARY.md (Deviations: direct DB hostname resolved to unreachable IPv6 — pushes went through the CLI-managed linked POOLER transport; reuse that workaround if push fails on the direct host)
    - supabase/migrations/0008_claim_exclusive.sql and supabase/migrations/0009_discovery_health_cadence.sql (exact SQL about to be applied)
  </read_first>
  <action>
    This task is BLOCKING for the phase: the schema push must complete before any verification runs — local builds pass without it, creating a false-positive state.
    1. Apply the user's checkpoint-approved goal override before hosted mutation: migration 0008 redefines claim_due_companies with locking; revised unpushed migration 0009 adds discovery health columns, removes prior discovery schedules, and invokes a DST-safe Chicago-local schedule gate every 30 minutes. The accepted cadence is 06:00-noon Chicago every 30 minutes and every two hours otherwise: 21 sweeps/day x 3 queries = about 63 requests/day. Record the user's exact acceptance verbatim in the SUMMARY. Then redeploy poll-tick, discovery-sweep, heartbeat.
    2. Run `supabase db push` with credentials from the established gitignored setup (SUPABASE_ACCESS_TOKEN available to the CLI, non-TTY safe). If the direct database host fails (IPv6/TLS), use the linked pooler transport exactly as Plan 02-01 did.
    3. Confirm application REMOTELY, not just locally: `supabase migration list` prints Local | Remote columns — a migration that exists only as a local file shows an empty Remote cell, which is exactly the falsely-green state this task exists to prevent. Assert 0008 and 0009 appear in BOTH the Local and Remote columns (the automated verify parses the Remote column with awk). If the CLI output is ambiguous, fall back to a hosted-DB check: `pg_get_functiondef('public.claim_due_companies(integer)'::regprocedure)` contains `skip locked`, and `pipeline_heartbeat.discovery_status` exists in information_schema.columns.
    4. Redeploy functions: `supabase functions deploy poll-tick discovery-sweep heartbeat` (config.toml already carries verify_jwt = false for all three; no config changes needed). Confirm each deploy reports the function active.
    5. No new secrets are required: CRON_SECRET, HEARTBEAT_SECRET, ADZUNA_APP_ID/KEY, and the Vault rows (project_url, cron_secret) all exist from Plans 02-02/02-03.
  </action>
  <verify>
    <automated>supabase migration list 2>&1 | awk -F'|' '$1 ~ /0008/ && $2 ~ /0008/ {a=1} $1 ~ /0009/ && $2 ~ /0009/ {b=1} END {exit !(a && b)}' && supabase functions list 2>&1 | grep -ci "poll-tick"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list` output shows 0008 and 0009 with the Remote column POPULATED for both (a local-only row with an empty Remote cell fails this criterion)
    - `supabase functions list` (or deploy output) shows poll-tick, discovery-sweep, and heartbeat active with new versions
    - No applied migration file (0001-0007) was modified; only 0008/0009 were added to the remote history
    - User approval for the hosted mutation set was obtained before any push or deploy; the accepted 30-minute morning/two-hour otherwise Chicago cadence and exact acceptance are recorded in the SUMMARY
  </acceptance_criteria>
  <done>Hosted schema and functions carry the gap-closure behavior; the cadence schedules and health columns are live.</done>
</task>

<task type="auto">
  <name>Task 2: Hosted proof — run probes 1-16, the non-destructive watchlist verifier, and the local regression suite</name>
  <files>scripts/verify-pipeline.ts, scripts/verify-watchlist.ts</files>
  <read_first>
    - scripts/verify-pipeline.ts (probes 13-16 semantics added by Plan 02-05 — what each asserts and their WARN-skip conditions)
    - scripts/verify-watchlist.ts (disposable probe + seed-integrity regression flow)
  </read_first>
  <action>
    1. Run `node --env-file=scripts/.env scripts/verify-pipeline.ts`. All probes must PASS: 1-12 (existing pipeline behavior still intact after the rewires), 13 (concurrent claim_due_companies calls return disjoint batches — gap 3 hosted proof), 14 (no-work tick advances last_success_at — gap 4), 15 (close-then-return job reopens with snapshot preserved — gap 2; a WARN-skip is acceptable only if no seed company has open jobs, record it verbatim), 16 (discovery response carries discoveryStatus and heartbeat row shows last_discovery_at — gap 5 surface).
    2. Run `node --env-file=scripts/.env scripts/verify-watchlist.ts`. All probes must PASS including "probe 6: seed companies and their job links are unchanged by verification" (gap 1 hosted proof). Run it TWICE back-to-back to demonstrate rerunnability — the second run must also fully pass.
    3. Run `cd web && npx vitest run && npm run build` for the full local regression (lifecycle, discovery-health, pipeline banner, plus the pre-existing 68 tests).
    4. Record in the SUMMARY: probe outputs, the double watchlist run, and cadence evidence. A manual probe proves discovery behavior; the scheduled gate and focused Chicago/DST tests prove the accepted cadence without waiting up to two hours for the next admitted slot.
  </action>
  <verify>
    <automated>node --env-file=scripts/.env scripts/verify-pipeline.ts && node --env-file=scripts/.env scripts/verify-watchlist.ts && node --env-file=scripts/.env scripts/verify-watchlist.ts && cd web && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - verify-pipeline.ts exits 0 with PASS lines for probes 13, 14, and 16; probe 15 is PASS or an explicitly recorded WARN-skip with reason
    - verify-watchlist.ts exits 0 twice consecutively, both runs printing the seed-integrity PASS line
    - `cd web && npx vitest run` passes (previous 68 tests plus lifecycle, discovery-health, and pipeline banner suites) and `npm run build` succeeds
    - Cadence evidence captured: either last_discovery_at advanced autonomously within 15 minutes during the active window, or the overnight condition is documented with probe 16 manual evidence
  </acceptance_criteria>
  <done>Every failed verification gap has hosted, rerunnable proof: exclusive claims, reopen-on-return, truthful no-work heartbeat, surfaced discovery failure, the explicitly accepted budget-safe cadence override, and a non-destructive verifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → hosted project | supabase db push / functions deploy with gitignored access token |
| verification scripts → hosted data | Probes mutate scheduling columns and one disposable row only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Tampering | hosted schema push | high | mitigate | Only additive migrations 0008/0009 pushed after user approval; applied migrations 0001-0007 untouched; remote migration list verified post-push |
| T-02-07-02 | Information Disclosure | credentials during deploy/verify | medium | mitigate | All secrets remain in gitignored scripts/.env and Supabase secret stores; no secret values echoed into SUMMARY or commits |
| T-02-07-03 | Denial of Service | probe manipulation of last_polled_at | low | accept | Probes only shift poll timing within the normal 9-minute due window; pipeline self-heals on the next cron tick |
| T-02-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; deploys use the existing pinned supabase CLI devDependency |
</threat_model>

<verification>
- Remote migration list includes 0008 and 0009; three functions redeployed active
- Probes 1-16 pass hosted; watchlist verifier passes twice consecutively
- Full web test suite and production build pass
</verification>

<success_criteria>
- All six failed VERIFICATION.md gaps have hosted proof and are ready for re-verification to flip to passed
- The four remaining behavior_unverified browser items (watchlist flow, badge visuals, banner render transition, plus any visual judgment) are explicitly left for /gsd-verify-work UAT — two of them (close-grace transition, banner decision logic) now also have automated logic coverage from Plans 02-04/02-06
</success_criteria>

<output>
Create `.planning/phases/02-watchlist-ingestion-monitoring/02-07-SUMMARY.md` when done.
</output>
