---
phase: 03.8-monitor-and-poll-the-branded-banking-companies-currently-on-
plan: 03
type: execute
wave: 3
depends_on: [03.8-02]
files_modified:
  - supabase/functions/_shared/connectors.ts
  - supabase/functions/_shared/detect.ts
  - supabase/functions/verify-board/index.ts
  - supabase/functions/observe-connectors/index.ts
  - supabase/functions/poll-tick/index.ts
  - supabase/config.toml
  - web/tests/branded-connectors.integration.test.ts
  - web/tests/observe-connectors.test.ts
autonomous: true
requirements: [DISC-01, DISC-07, DISC-09]
must_haves:
  truths:
    - "D-06 through D-10: recognition never grants authority; verification, Experimental observation, and Active polling re-resolve the exact server identity before fetch."
    - "D-16, D-18, and D-26: company work is bounded and independently settled; one provider failure cannot block or close another company."
    - "D-17 through D-20 and D-23: a cron-only Experimental lane records only positive complete evidence, writes no production jobs, and leaves SQL as the sole activation authority."
    - "D-21: exact primary URL re-add still uses authenticated verify-board while initial frozen-catalog admission remains migration-owned."
    - "DISC-01: Active polling uses the migration-owned ten-minute source cadence, one-minute claim cron, maximum four-minute initial stagger, and 120-second reserve."
  artifacts:
    - path: "supabase/functions/observe-connectors/index.ts"
      provides: "Cron-only bounded Experimental observation runner"
      exports: ["createObserveConnectorsHandler"]
    - path: "supabase/functions/_shared/connectors.ts"
      provides: "Closed provider registry and separate Active/Experimental dispatch"
      exports: ["observeConnector", "pollConnector", "verifyConnector"]
    - path: "supabase/functions/poll-tick/index.ts"
      provides: "Bounded independent Active polling"
  key_links:
    - from: "observe-connectors"
      to: "record_connector_observation"
      via: "server-derived positive complete evidence only"
      pattern: "record_connector_observation"
    - from: "poll-tick"
      to: "lifecycle reconciliation"
      via: "complete/credible observation and bounded per-company settlement"
      pattern: "planCompanySync|runBoundedPool"
---

<objective>
Wire the candidate adapters into exact authenticated verification, server-only Experimental observation, and bounded Active polling.

Purpose: Close the orchestration gap without weakening the Active-only dispatcher or creating a public observation/URL proxy.
Output: Closed registry wiring, exact detection/re-add behavior, an isolated observation function, and bounded polling integration tests.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-01-SUMMARY.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-02-SUMMARY.md

<interfaces>
`pollConnector(company, knownIds)` currently rejects every non-Active company and must remain Active-only. `createVerifyBoardHandler` authenticates with `auth.getUser` before service-role/provider work. `poll-tick` currently claims through `claim_due_companies`, batches writes, computes health, and calls `planCompanySync`; preserve these contracts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close registry, detection, and authenticated re-add wiring</name>
  <files>supabase/functions/_shared/connectors.ts, supabase/functions/_shared/detect.ts, supabase/functions/verify-board/index.ts, web/tests/branded-connectors.integration.test.ts</files>
  <read_first>
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/_shared/detect.ts
    - supabase/functions/verify-board/index.ts
    - web/tests/branded-connectors.integration.test.ts
    - web/tests/verify-board-auth.test.ts
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/workday-identities.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "Only exact reviewed primary URLs detect as the three branded providers; all lookalikes and seven Unsupported portals remain unsupported."
    - "Verify-board authenticates a real signed-in user before provider/service access and re-resolves every exact identity before fetch."
    - "Active polling stays Active-only; Experimental observation uses a distinct exact release-allowlisted dispatch."
  </behavior>
  <action>
    Extend `ProviderId`, `SupportedDetection`, and `providerRegistry` for `eightfold`, `oracle_recruiting`, and `goldman_higher`, then add a separate internal `observeConnector` that accepts Experimental state only for the three literal Plan 01 source keys. Keep `pollConnector`'s Active guard intact. Extend `detectAts`/`buildEndpoint` to recognize and reconstruct only exact primary public URLs through `resolveBrandedPublicUrl`; reject credentials, ports, fragments, queries or path/site drift before fetch. Extend verify-board only for authenticated D-21 re-add verification, deriving every provider field/evidence digest server-side. Do not expose caller-selected endpoint, tenant, facet, operation, source key, evidence, or activation target.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-connectors.integration.test.ts verify-board-auth.test.ts detect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Exact three primary URLs verify through frozen identities; hostile and seven Unsupported URLs produce zero network/service work."
    - "Rejected anonymous/invalid/forbidden requests cannot mutate company, observation, catalog, or job state."
    - "`pollConnector` still rejects Experimental/Disabled companies, while `observeConnector` rejects Active/Disabled/unknown identities."
    - "Capital One/Fidelity and all existing provider registry/detection tests remain green."
  </acceptance_criteria>
  <done>Recognition, verification, observation, and polling are separate exact-authority paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the cron-only Experimental observation lane</name>
  <files>supabase/functions/observe-connectors/index.ts, supabase/config.toml, web/tests/observe-connectors.test.ts</files>
  <read_first>
    - supabase/functions/observe-connectors/index.ts
    - supabase/config.toml
    - web/tests/observe-connectors.test.ts
    - supabase/functions/verify-board/index.ts
    - supabase/functions/poll-tick/index.ts
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/_shared/bounded-pool.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "Only POST with the exact `x-cron-secret` reaches service-role claim/adapter work."
    - "The runner claims due Experimental companies server-side, processes them independently, and records only complete, credible, warning-free, positive evidence."
    - "The runner never writes/updates/closes production jobs and never accepts a source key, URL, provider payload, observation time, or digest from the request."
  </behavior>
  <action>
    Create `observe-connectors` with `verify_jwt=false` in `supabase/config.toml` and the established POST plus `x-cron-secret` boundary. Create its admin client only after authorization. Call the migration-owned `claim_due_experimental_connectors` RPC with a fixed server batch size, process exact rows through `runBoundedPool`, re-resolve identities through `observeConnector`, derive a bounded digest from normalized source key/count/slice/category/country evidence, and call `record_connector_observation` only for non-zero complete/credible/warning-free results. Record bounded per-company Degraded health on failures. The endpoint must have no request-body network or activation authority and no production job persistence path.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- observe-connectors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Method/auth failures occur before environment/admin/provider access."
    - "Positive observations use server-derived ID/time/digest/evidence; zero/partial/drifted results do not call the observation RPC."
    - "Tests prove no job insert/update/close method is reachable from the runner."
    - "One provider error does not prevent a sibling observation or health update."
  </acceptance_criteria>
  <done>Experimental sources can earn independent server-timed checks without being scheduled or writing jobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bound Active polling and preserve safe lifecycle behavior</name>
  <files>supabase/functions/poll-tick/index.ts, web/tests/branded-connectors.integration.test.ts</files>
  <read_first>
    - supabase/functions/poll-tick/index.ts
    - web/tests/branded-connectors.integration.test.ts
    - supabase/functions/_shared/bounded-pool.ts
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/_shared/lifecycle.ts
    - supabase/functions/_shared/adapters/types.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "At most two companies are processed concurrently and the invocation reserve stops new scheduling."
    - "Each company retains independent persistence, health, last-success, error, close/reopen, and heartbeat outcomes."
    - "Incomplete, failed, empty, timed-out, or drifted observations retain last-known jobs and close none."
  </behavior>
  <action>
    Replace the unbounded top-level company `Promise.allSettled` fan-out with `runBoundedPool` while retaining per-company settled results, database batch size, orphan reattachment, exact return/reopen semantics, health updates, and heartbeat behavior. Use the server-owned company concurrency/deadline configuration from Plan 01, including the 120-second stop-scheduling reserve, while SQL owns the ten-minute due cadence and four-minute initial stagger. Persist each eligible Phase 03.8 job's bounded `scopeEvidence` atomically with the job row; reject persistence when exact source/category/matched-term/US/external-ID digest provenance is missing or invalid. Treat deadline/cap/schema/category/country/pagination failures as Degraded and closure-ineligible. Ensure one failed company cannot suppress successful sibling persistence/health and that a complete scoped observation is the only path to missing-job closure.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-connectors.integration.test.ts lifecycle.test.ts poll-tick.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Concurrency instrumentation proves the company cap and no new scheduling after reserve."
    - "Failure-isolation tests prove one provider timeout/schema error neither blocks a sibling nor closes either company's last-known jobs."
    - "Complete observations still batch insert/refresh/reopen/close through the existing provider-neutral lifecycle."
    - "Phase 03.8 job writes atomically retain constrained source/category/matched-term/US/digest provenance for hosted and signed-in evidence."
    - "Capital One/Fidelity and existing-provider poll regressions pass unchanged."
  </acceptance_criteria>
  <done>Active polling remains provider-neutral, bounded, independently healthy, and safe against incomplete closures.</done>
</task>

</tasks>

## Artifacts this phase produces

- Three closed provider registry entries and exact branded detection/re-add branches.
- `observeConnector` as a separate Experimental-only internal dispatch.
- `supabase/functions/observe-connectors/index.ts` and its `verify_jwt=false` configuration.
- Bounded `poll-tick` company execution with preserved batching/lifecycle/heartbeat semantics.
- Authentication, zero-fetch SSRF, no-job-write observation, concurrency, failure-isolation, and safe-closure integration tests.
- No public generic provider endpoint, browser-supplied observation evidence, or UI change.

<threat_model>
ASVS level 1 applies; every high-severity threat below is blocking until its mitigation is implemented and verified.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser URL → verify-board | Signed-in input is untrusted and recognition must not grant network authority. |
| Cron caller → observe/poll functions | Shared-secret callers cross into service-role database and provider access. |
| Persisted company → provider adapter | Catalog/operational identity drift must fail before fetch. |
| Provider observation → jobs/health/closure | Partial results must never become destructive lifecycle authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.8-03-01 | Spoofing / SSRF | detect/build/registry | high | mitigate | Exact primary URL recognition followed by frozen identity re-resolution at every dispatch. |
| T-03.8-03-02 | Elevation of Privilege | verify-board | high | mitigate | `auth.getUser` authenticated role before service/provider work; server-derived identity/evidence only. |
| T-03.8-03-03 | Spoofing | observe/poll cron boundary | high | mitigate | POST and constant secret check before environment/admin initialization; functions configured for the existing shared-secret boundary. |
| T-03.8-03-04 | Information Disclosure | cron/provider errors | high | mitigate | Bounded codes/counters only; never return or log secrets, URLs with credentials, query bodies, payloads, or evidence digests. |
| T-03.8-03-05 | Elevation of Privilege | cross-company activation | high | mitigate | Exact claimed company/source tuple, Experimental-only dispatch, server evidence RPC, and SQL-owned activation. |
| T-03.8-03-06 | Tampering | incomplete closure | high | mitigate | Lifecycle consumes explicit credibility/closure flags; every partial/failed/empty observation closes zero jobs. |
| T-03.8-03-07 | Denial of Service | company execution | high | mitigate | Fixed claim size, bounded pool/deadline, adapter limits, batched writes, and per-company settlement. |
| T-03.8-03-08 | Tampering | catalog/operational identity drift | high | mitigate | Re-resolve persisted fields to one frozen identity before every provider request. |
| T-03.8-03-SC | Tampering | dependency supply chain | low | accept | No install; reuse existing Supabase and runtime dependencies. |
</threat_model>

<verification>
- Run the focused registry/auth/observation/poll integration suites plus existing detection, lifecycle, and poll tests.
- Run `cd web && npm run build && npm run lint`.
- Confirm the observation runner contains no jobs table mutation and all function auth paths precede admin/provider access.
</verification>

<success_criteria>
- Exact identity authorization applies at recognition, verification, Experimental observation, and Active polling.
- Experimental evidence is positive, server-owned, isolated, and non-persistent until SQL activation.
- Active polling preserves last-known jobs, close/reopen safety, heartbeat, and failure isolation under bounded concurrency.
</success_criteria>

<output>
Create `.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-03-SUMMARY.md` when done.
</output>
