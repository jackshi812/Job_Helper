---
phase: 03.8-monitor-and-poll-the-branded-banking-companies-currently-on-
plan: 02
type: execute
wave: 2
depends_on: [03.8-01]
files_modified:
  - supabase/functions/_shared/adapters/eightfold.ts
  - supabase/functions/_shared/adapters/oracle-recruiting.ts
  - supabase/functions/_shared/adapters/goldman-higher.ts
  - web/tests/branded-adapters.test.ts
autonomous: true
requirements: [DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "D-04 through D-10: each evidence-backed primary portal has one exact structured anonymous adapter; the seven Unsupported targets receive no adapter."
    - "D-11 through D-16: all provider observations prove full scoped pagination, stable IDs, provider category, detail-country, usable detail content, and bounded execution."
    - "D-23: zero eligible jobs, missing category evidence, or missing U.S. detail can never produce activation-eligible evidence."
    - "Goldman Sachs stays non-admissible when its live trustworthy category field cannot yield a positive allowed-category proof."
  artifacts:
    - path: "supabase/functions/_shared/adapters/eightfold.ts"
      provides: "Exact Morgan Stanley Eightfold list/detail adapter"
      exports: ["pollMorganStanleyEightfold"]
    - path: "supabase/functions/_shared/adapters/oracle-recruiting.ts"
      provides: "Exact JPMorgan Oracle Recruiting sliced adapter"
      exports: ["pollJpmorganOracleRecruiting"]
    - path: "supabase/functions/_shared/adapters/goldman-higher.ts"
      provides: "Exact Goldman Higher GraphQL list/detail adapter"
      exports: ["pollGoldmanHigher"]
  key_links:
    - from: "three provider adapters"
      to: "supabase/functions/_shared/branded-identities.ts"
      via: "frozen identity inputs only"
      pattern: "BrandedIdentity"
    - from: "provider responses"
      to: "PollObservation"
      via: "shared U.S./category predicates and per-slice completeness"
      pattern: "credibleForClosure|allowMissingClosure"
---

<objective>
Implement the three evidence-backed candidate transports as strict normalized adapters.

Purpose: Prove structured portal contracts without widening network authority or treating transport success as completeness.
Output: Eightfold, Oracle Recruiting, and Goldman Higher adapters with exhaustive deterministic fail-closed tests.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-RESEARCH.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-01-SUMMARY.md

<interfaces>
Consume only `BrandedIdentity` objects from Plan 01. Emit the existing normalized `PollObservation` surface and the new bounded source/category/country evidence. Reuse SmartRecruiters' injected `FetchLike`, redirect denial, timeout, JSON/content-length/actual-byte caps, required-shape validation, stable-ID checks, and bounded warning conventions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Morgan Stanley Eightfold completeness</name>
  <files>supabase/functions/_shared/adapters/eightfold.ts, web/tests/branded-adapters.test.ts</files>
  <read_first>
    - supabase/functions/_shared/adapters/eightfold.ts
    - web/tests/branded-adapters.test.ts
    - supabase/functions/_shared/adapters/smartrecruiters.ts
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/adapters/scope.ts
    - supabase/functions/_shared/adapters/types.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-RESEARCH.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "Every Eightfold search page reconciles the stable total and every retained job has a stable ID, allowed business-area/category, usable detail, and United States detail location."
    - "Count drift, repeat IDs/pages, page/job/detail/deadline caps, malformed schema, HTML content, redirect, missing category/country/detail, and zero eligible results are closure- and activation-ineligible."
  </behavior>
  <action>
    Per D-04, D-08, D-09, and D-11 through D-16, implement only the frozen Morgan Stanley `/api/pcsx/search` and `/api/pcsx/position_details` contract. Construct requests from the immutable Eightfold identity, reject redirects and non-JSON, enforce declared and actual byte caps, paginate through the complete reviewed U.S. scope, validate stable totals/IDs, and fetch details through the shared bounded pool. Use only trustworthy Eightfold business-area/category labels and exact detail-country evidence; normalize eligible jobs into `source: "eightfold"`. Any anomaly returns bounded Degraded evidence with no closure authorization. Capture fixtures from the exact primary contract; do not call live endpoints in CI.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-adapters.test.ts -t Eightfold</automated>
  </verify>
  <acceptance_criteria>
    - "A multi-page positive fixture produces a non-zero complete credible observation with stable normalized jobs and immutable scope provenance."
    - "Every enumerated transport, schema, pagination, category, detail, country, cap, and deadline failure is tested as closure-ineligible."
    - "Unknown identity fields and redirect targets cause zero downstream fetches."
  </acceptance_criteria>
  <done>Morgan Stanley has a reusable but exact-authorized Eightfold adapter whose completeness is independently provable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement JPMorgan Oracle slice reconciliation</name>
  <files>supabase/functions/_shared/adapters/oracle-recruiting.ts, web/tests/branded-adapters.test.ts</files>
  <read_first>
    - supabase/functions/_shared/adapters/oracle-recruiting.ts
    - web/tests/branded-adapters.test.ts
    - supabase/functions/_shared/adapters/smartrecruiters.ts
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/adapters/scope.ts
    - supabase/functions/_shared/adapters/types.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-RESEARCH.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "Each frozen JPMorgan category facet is paginated and reconciled separately with its expected label and fixed U.S. location facet."
    - "Overlapping slices deduplicate only after every slice is complete; provider totals are never summed as the unique union count."
    - "Detail country/category mismatch, facet-label drift, or one incomplete slice makes the whole observation closure-ineligible."
  </behavior>
  <action>
    Per D-06 and D-11 through D-16, implement only the exact `jpmc.fa.oraclecloud.com` Candidate Experience site `CX_1001`. Build the Oracle finder from server-owned site, country facet, page size, offset, and fixed allowed category-facet IDs; callers supply none of them. Validate each facet's expected normalized label, first/last totals, stable IDs, offsets/pages, and completion independently, then deduplicate the stable-ID union. Fetch exact detail resources within the shared bounds and require a usable description, allowed provider job-family/category, and at least one United States detail location before normalization as `source: "oracle_recruiting"`. Any slice drift or partial result retains safe rows only as closure-ineligible evidence.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-adapters.test.ts -t Oracle</automated>
  </verify>
  <acceptance_criteria>
    - "Positive overlapping-slice fixtures reconcile each slice and emit one job per stable ID."
    - "Tests prove slice count/label/offset drift, duplicate/repeated pages, missing detail country/category, and caps cannot authorize closure or activation."
    - "No caller-controlled site, host, facet, or finder fragment appears in the adapter API."
  </acceptance_criteria>
  <done>JPMorgan Chase has an exact Oracle adapter with truthful per-slice completeness and deduplicated U.S. scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Goldman Higher with a positive-category admission fence</name>
  <files>supabase/functions/_shared/adapters/goldman-higher.ts, web/tests/branded-adapters.test.ts</files>
  <read_first>
    - supabase/functions/_shared/adapters/goldman-higher.ts
    - web/tests/branded-adapters.test.ts
    - supabase/functions/_shared/adapters/smartrecruiters.ts
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/adapters/scope.ts
    - supabase/functions/_shared/adapters/types.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-RESEARCH.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "Only the frozen `api-higher.gs.com` GraphQL path and reviewed operations/variables execute."
    - "GraphQL errors, total/page drift, missing IDs, untrusted or empty jobFunction/division, missing detail country/content, and zero eligible jobs are admission- and closure-ineligible."
    - "A positive fixture activates the adapter contract only when a trustworthy provider category matches the locked allowlist."
  </behavior>
  <action>
    Per D-09, D-12 through D-15, and D-23, implement the branded Higher list/detail contract with hard-coded reviewed GraphQL documents and identity-derived variables. Deny redirects, validate JSON/content bounds and GraphQL errors, reconcile `totalCount` across every page, deduplicate stable `roleId`/source IDs, and validate detail description/apply/country. Accept category only from the reviewed provider `jobFunction`/`division` provenance; do not infer from title or description. Normalize as `source: "goldman_higher"` only for a non-zero allowed-category U.S. proof. Preserve a first-class bounded `category_evidence_missing`/contract outcome so catalog admission can truthfully keep Goldman Unsupported when live proof fails.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-adapters.test.ts -t Goldman</automated>
  </verify>
  <acceptance_criteria>
    - "Positive fixtures prove complete GraphQL pagination, stable IDs, trusted category provenance, U.S. detail, usable content, and non-zero normalization."
    - "GraphQL errors and every missing/drifted field yield bounded non-credible evidence with no payload or credential leakage."
    - "The adapter exposes no caller-selected operation, variables affecting identity, host, path, credentials, or fallback category."
  </acceptance_criteria>
  <done>Goldman Higher is implementable only behind a live positive-category admission fence; otherwise its truthful outcome remains Unsupported.</done>
</task>

</tasks>

## Artifacts this phase produces

- `pollMorganStanleyEightfold`, `pollJpmorganOracleRecruiting`, and `pollGoldmanHigher`.
- Captured deterministic list/detail fixtures for each evidence-backed primary portal.
- Per-slice Oracle reconciliation and deduplicated-union evidence.
- Goldman positive-category admission outcome and bounded category-evidence failure.
- Complete transport/schema/pagination/content/category/country/deadline/cap regression coverage.
- No adapter for Bank of America, Citi, BlackRock, Wells Fargo, UBS, Barclays, or Charles Schwab.

<threat_model>
ASVS level 1 applies; every high-severity threat below is blocking until its mitigation is implemented and verified.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Frozen identity → external provider | Each adapter may contact only one exact reviewed machine contract. |
| External response → normalized observation | Provider data is untrusted and may drift, truncate, inject HTML, or conceal incompleteness. |
| Scoped observation → lifecycle | Only complete positive U.S./category evidence may authorize activation or closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.8-02-01 | Spoofing / SSRF | all adapter request builders | high | mitigate | Accept frozen identity objects only, deny redirects, and test altered host/path/site/facet/operation with zero fetch. |
| T-03.8-02-02 | Information Disclosure | provider errors/logs | high | mitigate | Emit bounded codes only; never log credentials, query bodies, cookies, anti-forgery values, or provider payloads. |
| T-03.8-02-03 | Tampering | provider pagination/schema | high | mitigate | Required-shape validation, stable totals/IDs, repeated-page checks, per-slice reconciliation, GraphQL error handling. |
| T-03.8-02-04 | Elevation of Privilege | category/country scope | high | mitigate | Shared provider-label and detail-country gates; no title/description inference. |
| T-03.8-02-05 | Tampering | incomplete observations authorizing closure | high | mitigate | Any drift/cap/timeout/missing detail sets non-credible and `allowMissingClosure=false`. |
| T-03.8-02-06 | Denial of Service | page/detail fan-out | high | mitigate | Shared bounded pool, byte/page/job/detail caps, per-request abort, and invocation reserve. |
| T-03.8-02-07 | Elevation of Privilege | HTML/WAF/session workarounds | high | mitigate | JSON-only required content, redirect denial, and no code paths for HTML parsing, challenge handling, authentication, cookies, or bootstrap tokens. |
| T-03.8-02-SC | Tampering | dependency supply chain | low | accept | No install; built-in fetch/URL/AbortController and existing test runtime only. |
</threat_model>

<verification>
- Run each provider-focused test slice and then the full `branded-adapters.test.ts`.
- Run existing adapter/lifecycle regressions to prove the normalized surface remains compatible.
- Confirm deterministic tests use injected fetch/time and do not require live third-party availability.
</verification>

<success_criteria>
- The three candidate adapters are exact, bounded, and fail closed under every researched contract failure.
- No zero-result, category-less, non-U.S., partial, or drifted observation can authorize activation or closure.
- Unsupported-provider workarounds remain absent.
</success_criteria>

<output>
Create `.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-02-SUMMARY.md` when done.
</output>
