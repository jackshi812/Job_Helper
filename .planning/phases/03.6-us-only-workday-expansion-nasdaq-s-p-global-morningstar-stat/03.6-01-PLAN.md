---
phase: 03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/workday-identities.ts
  - supabase/functions/_shared/adapters/workday.ts
  - web/tests/workday-connector.integration.test.ts
autonomous: true
requirements:
  - "Source-coverage extension with a bounded precursor to Phase 4 application tracking; no formal requirement IDs are remapped"
must_haves:
  truths:
    - "D-01: only the exact Nasdaq, S&P Global, Morningstar, and State Street Workday tuples resolve; lookalike, malformed, wrong-region, wrong-site, and unknown tuples fail before provider access."
    - "D-02: every new identity proves and applies United States of America facet ID bc33aa3152ec42d4995f4791a106ed09; Morningstar alone traverses locationMainGroup to locationCountry."
    - "D-03/D-04: complete scoped-count reconciliation is required before closure credibility, and each identity's evidence, degradation, activation progress, and jobs remain independent."
    - "D-05: Capital One, Fidelity, all other connectors, deterministic ranking, and source-health semantics remain regression-green."
  artifacts:
    - path: "supabase/functions/_shared/workday-identities.ts"
      provides: "Six-entry frozen exact Workday registry with explicit U.S. country-scope metadata for the four new identities"
    - path: "supabase/functions/_shared/adapters/workday.ts"
      provides: "Flat/nested country-facet discovery, scoped pagination, and fail-closed reconciliation"
    - path: "web/tests/workday-connector.integration.test.ts"
      provides: "Exact tuple, held-out negative, facet-shape, closure, staging, polling, and regression coverage"
  key_links:
    - from: "resolveWorkdayIdentity"
      to: "pollWorkdayRecent/verifyWorkdayListing"
      via: "immutable WorkdayIdentity.countryScope metadata, never caller-provided origins or facet paths"
      pattern: "resolveWorkdayIdentity|countryScope"
    - from: "country facet discovery"
      to: "PollObservation.credibleForClosure"
      via: "exact ID/descriptor/route validation plus scoped total and raw pagination reconciliation"
      pattern: "bc33aa3152ec42d4995f4791a106ed09|credibleForClosure"
---

<objective>
Extend the frozen Workday admission and adapter spine with four exact, independently scoped U.S.-only sources per D-01 through D-05.

Purpose: Make Nasdaq, S&P Global, Morningstar, and State Street safe to verify and poll without widening the trusted identity boundary or allowing facet drift to close jobs.
Output: Exact registry entries, reusable flat/nested U.S.-facet handling, and held-out integration regressions.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-CONTEXT.md
@.planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-PATTERNS.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-VERIFICATION.md

<interfaces>
`WorkdayIdentity` in `supabase/functions/_shared/workday-identities.ts` is the sole server-owned origin/tenant/site/region/host-form authority. `resolveWorkdayIdentity(tenant, region, site, hostForm)` must remain an exact four-field resolver. `pollWorkdayRecent(identity, fetch, options)` and `verifyWorkdayListing(fetch, options, identity)` receive that resolved identity. `PollObservation` closure authority is represented by `completeness`, `credibleForClosure`, `allowMissingClosure`, `expectedCount`, and `warnings`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact identity and U.S.-facet RED coverage</name>
  <files>web/tests/workday-connector.integration.test.ts</files>
  <read_first>
    - .planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-CONTEXT.md
    - .planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-PATTERNS.md
    - supabase/functions/_shared/workday-identities.ts
    - supabase/functions/_shared/adapters/workday.ts
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/verify-board/index.ts
    - supabase/functions/poll-tick/index.ts
    - web/tests/workday-connector.integration.test.ts
  </read_first>
  <behavior>
    - "Exact positives: Nasdaq nasdaq/wd1/Global_External_Site/jobs, S&P Global spgi/wd5/SPGI_Careers/jobs, Morningstar morningstar/wd5/morningstar/jobs, and State Street statestreet/wd1/Global/jobs resolve to their canonical URLs and source keys."
    - "Held-out negatives alter one tuple field at a time, use lookalike hosts/paths, wrong host forms, wrong case, null-like values, or unknown tenants; every case is rejected with zero provider fetches at verify and poll."
    - "Nasdaq, S&P Global, and State Street accept only a flat locationCountry facet carrying bc33aa3152ec42d4995f4791a106ed09 and descriptor United States of America."
    - "Morningstar accepts only locationMainGroup containing locationCountry with the same exact ID/descriptor."
    - "Missing, duplicate, negative-count, wrong-descriptor, wrong-ID, wrong-route, ignored-filter, scoped-total mismatch, later-page drift, contradictory totals, cap, and non-U.S. fixtures return closure-ineligible observations."
    - "One source's failed facet fixture does not alter a sibling source's successful observation or staged activation call."
  </behavior>
  <action>
    Add the full RED matrix under the exact test group name `Phase 03.6 exact Workday identity registry and U.S. scope` before production changes, including one case named `resolves all four exact Phase 03.6 identities`. Use volatile counts only as fixture values; never encode the observed 48/93/65/518 totals as production expectations. Assert request bodies carry the registered facet route and ID on every scoped page, verification reports the reconciled U.S. count, and non-U.S. rows never materialize. Exercise authenticated verify-board staging as `experimental`, one observation RPC for the exact company, scheduled dispatch tuple rechecks, `Promise.allSettled` failure isolation, and unchanged Capital One/Fidelity cases per D-01 through D-05. The new tests must fail on missing identity/scope behavior, not by weakening existing assertions.
  </action>
  <verify>
    <automated>cd web &amp;&amp; ! npm run test -- workday-connector.integration.test.ts --testNamePattern "Phase 03.6 exact Workday identity registry and U.S. scope"</automated>
  </verify>
  <acceptance_criteria>
    - "D-01: tests spell all four canonical URLs and exact source keys and include held-out one-field tuple mutations."
    - "D-02: tests spell bc33aa3152ec42d4995f4791a106ed09 and distinguish flat locationCountry from Morningstar locationMainGroup → locationCountry."
    - "D-03/D-04: every malformed/drifted/ignored/count-mismatch fixture asserts credibleForClosure=false and no sibling-state coupling."
    - "D-05: Capital One, Fidelity, non-Workday connector dispatch, ranking, and health regression assertions remain present."
    - "The focused command reaches an expected RED failure caused by the missing new registry/scope contract."
  </acceptance_criteria>
  <done>The held-out identity, facet, closure, independence, and regression matrix exists and fails for the intended missing production behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the four immutable U.S.-only Workday identities</name>
  <files>supabase/functions/_shared/workday-identities.ts, web/tests/workday-connector.integration.test.ts</files>
  <read_first>
    - supabase/functions/_shared/workday-identities.ts
    - web/tests/workday-connector.integration.test.ts
    - .planning/phases/03.5-generic-workday-connector-fidelity/03.5-01-SUMMARY.md
    - .planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-CONTEXT.md
  </read_first>
  <action>
    Extend `WorkdayIdentity` with an optional immutable `countryScope` whose descriptor, ID, facet parameter, and route are server-owned. Export `UNITED_STATES_WORKDAY_FACET_ID` with exact value `bc33aa3152ec42d4995f4791a106ed09` plus source-key constants for `workday:wd1:nasdaq:Global_External_Site`, `workday:wd5:spgi:SPGI_Careers`, `workday:wd5:morningstar:morningstar`, and `workday:wd1:statestreet:Global`. Add four separately frozen Form-A identities with the exact D-01 public boards `https://nasdaq.wd1.myworkdayjobs.com/Global_External_Site`, `https://spgi.wd5.myworkdayjobs.com/SPGI_Careers`, `https://morningstar.wd5.myworkdayjobs.com/morningstar`, and `https://statestreet.wd1.myworkdayjobs.com/Global`, their matching `/wday/cxs/{tenant}/{site}` roots, and truthful company names. Encode flat `locationCountry` for Nasdaq/S&P Global/State Street and nested `locationMainGroup` then `locationCountry` for Morningstar per D-02. Preserve Capital One and Fidelity fields and behavior unchanged; do not infer origins or scope from region/company names, and do not add a second registry.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run test -- workday-connector.integration.test.ts --testNamePattern "resolves all four exact Phase 03.6 identities"</automated>
  </verify>
  <acceptance_criteria>
    - "D-01: `WORKDAY_IDENTITIES` contains exactly Capital One, Fidelity, Nasdaq, S&P Global, Morningstar, and State Street entries; unknown tuples still return null."
    - "D-02: all four new entries share the exact U.S. ID/descriptor while Morningstar alone has the nested route."
    - "D-04: every entry owns an immutable scope object; no mutable/global discovered facet state is introduced."
    - "D-05: byte-behavior regression tests for the two existing Workday identities pass."
  </acceptance_criteria>
  <done>The sole frozen Workday registry admits exactly six identities and carries explicit, identity-local U.S. scope metadata for the four additions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement flat/nested country discovery and fail-closed scoped polling</name>
  <files>supabase/functions/_shared/adapters/workday.ts, web/tests/workday-connector.integration.test.ts</files>
  <read_first>
    - supabase/functions/_shared/adapters/workday.ts
    - supabase/functions/_shared/workday-identities.ts
    - supabase/functions/_shared/lifecycle.ts
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/verify-board/index.ts
    - supabase/functions/poll-tick/index.ts
    - web/tests/workday-connector.integration.test.ts
  </read_first>
  <action>
    Generalize the existing Fidelity facet discovery discipline into a bounded country-scope helper driven only by `identity.countryScope`. Validate arrays/objects, unique route segments, exact facet parameter, exact ID, exact descriptor, non-negative integer count, and nesting shape. Discover the live U.S. count, apply the registered `appliedFacets.locationCountry` ID on every page, and require the first scoped total plus raw pagination/tombstone/duplicate reconciliation to equal that discovered count. Preserve safe detail-path and request-size/deadline/cap protections. Any missing, duplicate, malformed, ignored, contradictory, drifted, empty, or capped evidence must use the existing `incomplete` path with `credibleForClosure=false` and `allowMissingClosure=false`; never close from a selective/recent observation lacking complete scoped accounting. Keep facet state request-local so the four verify/poll paths activate and degrade independently per D-03/D-04. Do not change connector health semantics, deterministic filtering/ranking, or Capital One/Fidelity paths per D-05.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run test -- workday-connector.integration.test.ts adapters.test.ts detect.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - "D-02: flat and Morningstar nested success fixtures return only U.S.-scoped jobs and reconciled expectedCount values."
    - "D-03: all held-out closure negatives retain prior jobs and never authorize missing-job closure."
    - "D-04: a failed identity leaves the other three observations, health updates, and activation calls independently successful."
    - "D-05: Capital One, Fidelity, detector, connector, poll-tick, ranking, and health regressions pass with no source-specific frontend exception."
    - "No production push, function deployment, web release, or git push occurs in this plan."
  </acceptance_criteria>
  <done>The adapter proves exact flat/nested U.S. facet evidence, reconciles complete scoped populations, and fails closed per identity without regressions.</done>
</task>

</tasks>

## Artifacts this phase produces

- New exported constants: `UNITED_STATES_WORKDAY_FACET_ID`, `NASDAQ_WORKDAY_SOURCE_KEY`, `SP_GLOBAL_WORKDAY_SOURCE_KEY`, `MORNINGSTAR_WORKDAY_SOURCE_KEY`, `STATE_STREET_WORKDAY_SOURCE_KEY`.
- New registry contract: immutable `WorkdayCountryScope`/`WorkdayIdentity.countryScope` metadata and four frozen identity objects.
- New adapter mechanism: registered flat/nested country-facet discovery and scoped-count reconciliation.
- New migration: `supabase/migrations/0037_us_workday_dashboard_queue.sql`.
- New migration test: `web/tests/migration-0037-us-workday-dashboard-queue.test.ts`.
- New database field/constraint/indexes: `public.user_jobs.applied_at`, `user_jobs_lifecycle_mutual_exclusion`, and lifecycle/cursor indexes named by migration 0037.
- New security-invoker RPCs: `public.dashboard_feed_page(...)` and `public.dashboard_company_options(...)`.
- New frontend contracts: `LifecycleView`, `DashboardFeedQuery`, `DashboardFeedCursor`, `DashboardFeedPage`, and lifecycle-specific mutation helpers in `web/src/lib/feed.ts`.
- New UI state: mutually exclusive Active/Applied/Dismissed view, paged query cache, 10-second applied Undo notice, backfill/retry/exhaustion state.
- New release evidence: `03.6-04-PREFLIGHT.md`, `03.6-04-ROLLOUT-EVIDENCE.md`, and `03.6-UAT.md`.
- No feature flag, package, tracker row, application stage, note, or resume-tailoring artifact is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted URL/tuple → exact registry | Browser/provider input must never construct a pollable Workday origin. |
| Workday CXS → closure authority | External facet/list/detail payloads are untrusted evidence. |
| One source → sibling sources | Provider drift must remain isolated to the exact source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.6-01-01 | Spoofing / Elevation | `resolveWorkdayIdentity` | high | mitigate | Exact frozen four-field tuple admission, canonical origins/CXS roots, and zero-fetch held-out negatives. |
| T-03.6-01-02 | Tampering | country facet discovery | high | mitigate | Exact route/ID/descriptor/count validation and first-scoped-total reconciliation before credibility. |
| T-03.6-01-03 | Information Disclosure / SSRF | detail and list fetches | high | mitigate | Preserve exact-origin, path, credential, port, redirect, size, and traversal guards. |
| T-03.6-01-04 | Denial of Service | provider pagination/detail calls | medium | mitigate | Existing page/detail/byte/deadline caps; cap exhaustion is closure-ineligible. |
| T-03.6-01-05 | Tampering | cross-source evidence | high | mitigate | Request-local scope state, exact source key in verification/dispatch, and independent settled-result tests. |
| T-03.6-01-SC | Tampering | dependency supply chain | low | accept | No dependency install occurs; repository-pinned TypeScript/Vitest tooling only. |
</threat_model>

<verification>
- `cd web && npm run test -- workday-connector.integration.test.ts adapters.test.ts detect.test.ts`
- `cd web && npm run build`
- Confirm exact four URLs, four source keys, U.S. facet ID, and Morningstar nested route appear in registry/tests.
- Confirm malformed/ignored/drifted/cap fixtures are closure-ineligible and Capital One/Fidelity regressions remain green.
</verification>

<success_criteria>
- ROADMAP success criteria 1-3 are implemented locally and test-proven.
- D-01 through D-05 are cited and satisfied without authorizing any deferred Workday employer.
- All four new sources have exact, independent, fail-closed U.S.-scoped verification and polling paths.
- Production remains at accepted source commit `edccac2dd1728a86a8ffa431f4d36bb1e10bff5c`; planning/implementation commits are not pushed or deployed here.
</success_criteria>

<output>
Create `.planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-01-SUMMARY.md` when done.
</output>
