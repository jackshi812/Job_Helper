---
phase: 02-watchlist-ingestion-monitoring
plan: 3
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - supabase/functions/_shared/adapters/adzuna.ts
  - web/tests/adzuna.test.ts
  - supabase/migrations/0007_discovery.sql
  - supabase/functions/discovery-sweep/index.ts
  - supabase/functions/heartbeat/index.ts
  - supabase/config.toml
  - web/src/lib/pipeline.ts
  - web/src/components/Shell.tsx
  - scripts/verify-pipeline.ts
autonomous: false
requirements: [DISC-02, DISC-06]
user_setup:
  - service: adzuna
    why: "Aggregator discovery (DISC-02) needs a free developer account; app_id/app_key only exist after human signup at developer.adzuna.com"
    env_vars:
      - name: ADZUNA_APP_ID
        source: "developer.adzuna.com → register → application dashboard; then Claude runs: npx supabase secrets set ADZUNA_APP_ID=… ADZUNA_APP_KEY=…"
      - name: ADZUNA_APP_KEY
        source: "same dashboard"
  - service: cron-job.org
    why: "D-07 external dead-man's switch — must live OUTSIDE Supabase because a paused project cannot alert about itself; account creation is human-only"
    dashboard_config:
      - task: "Create a cronjob hitting the deployed heartbeat URL (with ?k=<HEARTBEAT_SECRET>) every 5–10 minutes; enable failure AND recovery email notifications"
        location: "console.cron-job.org → Create cronjob → Notifications tab"

must_haves:
  truths:
    - "An hourly Adzuna sweep on the D-08 seed queries inserts jobs not already present, and never duplicates an open ATS-sourced job (DISC-02, DISC-03 cross-source)"
    - "The sweep stops before exceeding the ~250 req/day Adzuna budget (request counter with 240 cutoff)"
    - "A banner appears on every page when the pipeline's last successful poll is more than 30 minutes old, and disappears on recovery (D-07)"
    - "An external monitor (cron-job.org) emails when the heartbeat endpoint reports stale or stops responding — dead cron detectable within one poll cycle (DISC-06, D-07)"
  artifacts:
    - path: "supabase/functions/heartbeat/index.ts"
      provides: "Public secret-gated liveness endpoint: 200 fresh / 503 stale / 401 bad secret"
    - path: "supabase/functions/discovery-sweep/index.ts"
      provides: "Adzuna sweep with fingerprint dedup and daily budget guard"
    - path: "supabase/migrations/0007_discovery.sql"
      provides: "seed_queries table + seed rows (D-08), budget columns on pipeline_heartbeat, hourly sweep cron"
      contains: "create table public.seed_queries"
    - path: "web/src/components/Shell.tsx"
      provides: "Global stale-pipeline banner between header and main"
    - path: "web/src/lib/pipeline.ts"
      provides: "Heartbeat read for the banner"
      exports: ["fetchHeartbeat"]
  key_links:
    - from: "web/src/components/Shell.tsx"
      to: "web/src/lib/pipeline.ts"
      via: "useQuery ['pipeline_heartbeat'] with refetchInterval 60_000 driving the banner"
      pattern: "from '../lib/pipeline'"
    - from: "supabase/functions/discovery-sweep/index.ts"
      to: "supabase/functions/_shared/dedup.ts"
      via: "fingerprint() lookup against open jobs before insert (ATS wins)"
      pattern: "fingerprint\\("
    - from: "supabase/functions/heartbeat/index.ts"
      to: "supabase/migrations/0006_jobs_pipeline.sql"
      via: "reads pipeline_heartbeat.last_success_at written by poll-tick"
      pattern: "pipeline_heartbeat"
---

<objective>
Close the phase: aggregator discovery beyond the watchlist (Adzuna on D-08 seed queries, budget-guarded, fingerprint-deduped against ATS rows) plus full pipeline liveness — the in-app stale banner and the mandatory external dead-man's switch (D-07). Ends with the phase-wide hosted verification and the two human account setups (Adzuna, cron-job.org).

Purpose: DISC-02 gives breadth beyond the 100-company watchlist; DISC-06/D-07 make silent pipeline death — the project's named default failure mode — impossible to miss.
Output: adzuna adapter + tests, migration 0007 (pushed), `discovery-sweep` + `heartbeat` deployed, Shell banner, extended verify-pipeline script, live external monitor.
</objective>

## Phase Goal

**As an** invited job seeker, **I want to** have new postings from 100+ watched career sites plus an aggregator land in the system exactly once within 5–15 minutes, **so that** I can trust per-company health and pipeline liveness at a glance and never miss a fresh posting.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-CONTEXT.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-PATTERNS.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-UI-SPEC.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 03 portion)

- **Tables:** `public.seed_queries` (`id`, `owner_label`, `what`, `where_loc`, `enabled`, `created_at`) with 4–6 seed rows (D-08); new columns on `pipeline_heartbeat`: `adzuna_requests_today integer`, `adzuna_budget_date date`
- **Cron jobs:** `discovery-sweep-hourly` (`0 * * * *` — RESEARCH Pattern 7 budget-safe choice)
- **Migration:** `supabase/migrations/0007_discovery.sql`
- **Functions (TS):** `mapAdzunaResult(raw): NormalizedJob`, `buildAdzunaUrl(country, what, whereLoc, appId, appKey): string`, `fetchHeartbeat(): Promise<HeartbeatRow | null>`
- **Types:** `HeartbeatRow` (in web/src/lib/pipeline.ts)
- **Edge functions:** `discovery-sweep` (POST, x-cron-secret gated), `heartbeat` (GET, public + `?k=` secret, 200/503/401)
- **Config keys:** `[functions.discovery-sweep]` and `[functions.heartbeat]` with `verify_jwt = false` in supabase/config.toml
- **Env/secrets:** `ADZUNA_APP_ID`, `ADZUNA_APP_KEY`, `HEARTBEAT_SECRET` (edge secrets)
- **Components:** stale-pipeline banner block inside `Shell` (no new component file)
- **Scripts:** extended `scripts/verify-pipeline.ts` (heartbeat + sweep probes)

<tasks>

<task type="auto">
  <name>Task 1: Heartbeat endpoint + global stale-pipeline banner</name>
  <files>supabase/functions/heartbeat/index.ts, supabase/config.toml, web/src/lib/pipeline.ts, web/src/components/Shell.tsx</files>
  <read_first>
    - .planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md (Pattern 6 + the complete "Heartbeat endpoint" code example; A5 verify_jwt assumption)
    - web/src/components/Shell.tsx (current header/error-strip structure — banner slots between header and main; Shell is inside QueryClientProvider per PATTERNS.md)
    - web/src/lib/resumes.ts (lib module conventions for pipeline.ts)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-UI-SPEC.md (Component 3: exact banner classes, role="status", copy)
    - supabase/config.toml (section style; poll-tick section added in Plan 02)
  </read_first>
  <action>
    `supabase/functions/heartbeat/index.ts` per the RESEARCH code example: GET handler; 401 unless query param `k` equals `Deno.env.get('HEARTBEAT_SECRET')`; service-role read of `pipeline_heartbeat.last_success_at`; respond 200 body 'ok' when fresher than 30 minutes, else 503 body 'stale' (null/never-succeeded counts as stale). Read-only, leaks nothing beyond fresh/stale (D-07). Generate HEARTBEAT_SECRET (`openssl rand -hex 16`), `npx supabase secrets set HEARTBEAT_SECRET=<value>` from web/, append to scripts/.env. Config.toml: add `[functions.heartbeat]` with `verify_jwt = false` (public endpoint — the secret param is the gate; A5 fallback: deploy with --no-verify-jwt). Deploy `npx supabase functions deploy heartbeat`.

    `web/src/lib/pipeline.ts`: `HeartbeatRow` interface (`last_tick_at`, `last_success_at`, both string | null) + `fetchHeartbeat()` selecting the singleton row via the shared browser client (authenticated read under the Plan 02 select policy), `{ data, error }` throw convention, `.maybeSingle()` so a missing row returns null instead of throwing.

    `web/src/components/Shell.tsx`: add the stale-pipeline banner per 02-UI-SPEC Component 3, between the closing of `<header>` and `<main>`: `useQuery({ queryKey: ['pipeline_heartbeat'], queryFn: fetchHeartbeat, refetchInterval: 60_000 })`; render when last_success_at is null or older than 30 minutes (D-07): full-width strip `border-b border-amber-200 bg-amber-50 text-amber-800 dark:border-amber-900 dark:bg-amber-950 dark:text-amber-300`, inner `mx-auto max-w-6xl px-4 py-2 text-sm sm:px-6`, `role="status"`, copy "Job monitoring hasn't run since {time} — new postings may be missed." with {time} formatted via Intl (UI-SPEC allows "since 10:42 AM"); when last_success_at is null use "Job monitoring hasn't run yet — new postings may be missed." No dismiss button — it reflects live state and disappears on recovery. Do not disturb the existing error strip.
  </action>
  <verify>
    <automated>cd web && npm run build && HB_URL="$(grep SUPABASE_URL ../scripts/.env | cut -d= -f2)/functions/v1/heartbeat" && curl -s -o /dev/null -w "%{http_code}\n" "$HB_URL" && curl -s -o /dev/null -w "%{http_code}\n" "$HB_URL?k=$(grep HEARTBEAT_SECRET ../scripts/.env | cut -d= -f2)"</automated>
  </verify>
  <acceptance_criteria>
    - Deployed heartbeat URL without `k` returns 401; with correct `k` returns 200 (pipeline running from Plan 02) — 503 acceptable only if the tick cron is not yet live
    - `cd web && npm run build` succeeds
    - `grep -c "role=\"status\"" web/src/components/Shell.tsx` returns 1
    - `grep -c "refetchInterval: 60_000" web/src/components/Shell.tsx` returns 1
    - supabase/config.toml contains `[functions.heartbeat]` with verify_jwt = false
    - `grep -c "maybeSingle" web/src/lib/pipeline.ts` returns 1
  </acceptance_criteria>
  <done>Liveness is visible in-app (banner within one poll cycle of silence, D-07) and probeable externally (200/503/401 endpoint) — the two halves DISC-06 requires.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [BLOCKING] Adzuna adapter + migration 0007 (seed queries, budget, hourly cron) + db push + discovery-sweep deploy</name>
  <files>supabase/functions/_shared/adapters/adzuna.ts, web/tests/adzuna.test.ts, supabase/migrations/0007_discovery.sql, supabase/functions/discovery-sweep/index.ts, supabase/config.toml</files>
  <read_first>
    - .planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md (Pattern 1 Adzuna section: endpoint, params, response fields, snippet warning; Pitfall 7 budget math; Assumptions A1/A2; Pattern 4 aggregator-side dedup rule)
    - supabase/functions/_shared/adapters/types.ts + greenhouse.ts (NormalizedJob contract and adapter conventions from Plan 02)
    - supabase/functions/poll-tick/index.ts (x-cron-secret gate + service-role client pattern to reuse)
    - supabase/migrations/0006_jobs_pipeline.sql (cron.schedule style, pipeline_heartbeat shape)
  </read_first>
  <behavior>
    - Test: `mapAdzunaResult` maps a fixture result (id, title, description snippet, created ISO, redirect_url, company.display_name, location.display_name) → NormalizedJob with source 'adzuna', `snapshotPartial: true` (description is explicitly a snippet — never a full JD), absoluteUrl = redirect_url, companyName = company.display_name
    - Test: `buildAdzunaUrl('us', 'software engineer', 'san francisco', id, key)` produces `https://api.adzuna.com/v1/api/jobs/us/search/1` with query params app_id, app_key, what, where, sort_by=date, max_days_old=1, results_per_page=50, content-type=application/json
    - Test: mapper is pure; missing optional fields (location, company) yield nulls, not throws
  </behavior>
  <action>
    Adapter first (RED→GREEN with web/tests/adzuna.test.ts): `supabase/functions/_shared/adapters/adzuna.ts` exporting pure `mapAdzunaResult` and `buildAdzunaUrl` (no npm: specifiers — Vitest-importable).

    Migration `supabase/migrations/0007_discovery.sql`: (1) `public.seed_queries`: `id uuid primary key default gen_random_uuid()`, `owner_label text not null` (which user the query serves — informational only, no FK per D-08's "config rows" nature), `what text not null`, `where_loc text not null`, `enabled boolean not null default true`, `created_at timestamptz not null default now()`; RLS enabled, `grant select to authenticated`, single `seed_queries_select_shared` policy `using (true)` — rows are edited via SQL only per D-08 (comment this); seed 4 rows (2 per user) with placeholder role/location values plus a comment telling the users to edit them in the SQL editor (Phase 3's preferences UI replaces this seed as the query source per D-08). (2) `alter table public.pipeline_heartbeat add column adzuna_requests_today integer not null default 0, add column adzuna_budget_date date` (Pitfall 7 budget ledger). (3) `cron.schedule('discovery-sweep-hourly', '0 * * * *', …)` mirroring the 0006 net.http_post block but targeting `/functions/v1/discovery-sweep` — hourly is the RESEARCH budget-safe choice (~144 req/day worst case for 6 queries, inside 250/day with headroom; DISC-02 explicitly does not guarantee aggregator latency).

    [BLOCKING] `npx supabase db push` from web/ after the migration file is complete and before deploying the sweep; confirm 0007 via `npx supabase migration list --linked`.

    `supabase/functions/discovery-sweep/index.ts`: 401 unless x-cron-secret matches (same gate as poll-tick); config.toml gets `[functions.discovery-sweep]` verify_jwt = false. If ADZUNA_APP_ID/ADZUNA_APP_KEY env are absent, respond 200 `{ skipped: 'missing adzuna credentials' }` without error (graceful pre-signup no-op so the hourly cron doesn't spam failures before Task 3's checkpoint). Budget guard FIRST: read pipeline_heartbeat; if adzuna_budget_date differs from today (UTC) reset counter to 0 and set date; if adzuna_requests_today >= 240 respond 200 `{ skipped: 'budget' }` (Pitfall 7). For each enabled seed_queries row: fetch buildAdzunaUrl(...), increment the counter per request (write back even on partial failure), map results through mapAdzunaResult. Dedup per Pattern 4 aggregator rule: compute fingerprint(companyName, title, location); if ANY open job shares the fingerprint (any source) → skip (ATS/self wins); else upsert with `on conflict (source, external_id) do update set last_seen_at = now()`. Adzuna rows: company_id null, snapshot_partial true — they age out via first_seen_at (30-day rule) rather than stale-close; the actual age-out delete/close statement runs at the end of the sweep: close open adzuna-source jobs with first_seen_at older than 30 days (also serves the CLAUDE.md DB-pruning guidance). Deploy `npx supabase functions deploy discovery-sweep`.
  </action>
  <verify>
    <automated>cd web && npm test && npx supabase migration list --linked</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` green including adzuna.test.ts (snapshotPartial true asserted)
    - `npx supabase migration list --linked` (from web/) shows 0007 applied
    - `grep -c "snapshotPartial: true" supabase/functions/_shared/adapters/adzuna.ts` returns 1
    - `grep -c ">= 240" supabase/functions/discovery-sweep/index.ts` returns >= 1 (budget cutoff)
    - `grep -c "discovery-sweep-hourly" supabase/migrations/0007_discovery.sql` returns 1 and the schedule expression is `0 * * * *`
    - Deployed discovery-sweep invoked with x-cron-secret but WITHOUT Adzuna secrets returns 200 with a skipped body (graceful no-op)
  </acceptance_criteria>
  <done>Sweep infrastructure is live, budget-capped, fingerprint-deduped, and safely dormant until Adzuna credentials arrive.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Human setup (Adzuna keys, cron-job.org monitor) + full-phase hosted verification</name>
  <files>scripts/verify-pipeline.ts</files>
  <read_first>
    - scripts/verify-pipeline.ts (Plan 02 version being extended)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md (Open Questions 1–2; Assumptions A1/A2 to confirm at first keyed call)
  </read_first>
  <action>
    Two 5-minute account setups Claude cannot perform (no API for account creation):

    1. **Adzuna** — register at developer.adzuna.com (free), create an application, copy app_id and app_key. Paste them here; Claude will run `npx supabase secrets set ADZUNA_APP_ID=… ADZUNA_APP_KEY=…` and re-verify.
    2. **cron-job.org** — create a free account at console.cron-job.org; create a cronjob whose URL is the deployed heartbeat endpoint INCLUDING the secret: `https://<project-ref>.supabase.co/functions/v1/heartbeat?k=<HEARTBEAT_SECRET from scripts/.env>`; schedule every 5–10 minutes; in the Notifications settings enable BOTH failure and recovery emails to your address. This external monitor is mandatory (D-07): when the Supabase project pauses, the endpoint dies and cron-job.org still emails — no in-platform watcher can do that. Also complete the Plan 02 Vault user_setup now if not yet done (project_url + cron_secret rows), or the 1-minute tick cron is still inert.
  </action>
  <instructions>
    After the user supplies keys and confirms the monitor, Claude finishes autonomously: set the Adzuna secrets; extend scripts/verify-pipeline.ts with phase-final probes: (9) heartbeat endpoint 401 without k / 200-or-503 with k, matching pipeline_heartbeat freshness; (10) invoke discovery-sweep with x-cron-secret → 200 non-skipped, adzuna_requests_today incremented, at least one query returned results (confirms A1/A2 params + quota at first keyed call — if max_days_old or sort_by is rejected, adjust buildAdzunaUrl and note the deviation in the SUMMARY); (11) no open adzuna-source job shares a fingerprint with an open non-adzuna job (aggregator dedup, DISC-03); (12) pipeline_heartbeat.last_tick_at advanced within the last ~3 minutes WITHOUT any manual poll-tick invocation during the probe window (proves the pg_cron path end-to-end, DISC-06). Then run the full script.
  </instructions>
  <verify>
    <automated>node --env-file=scripts/.env scripts/verify-pipeline.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase secrets list` (from web/) shows ADZUNA_APP_ID, ADZUNA_APP_KEY, HEARTBEAT_SECRET, CRON_SECRET
    - `node --env-file=scripts/.env scripts/verify-pipeline.ts` prints PASS for probes 1–12 and exits 0
    - jobs table contains at least one source='adzuna' row with snapshot_partial=true, OR probe 10 documents that the seed queries legitimately returned zero fresh results (acceptable — latency not guaranteed for the aggregator path)
    - User confirms receipt of a cron-job.org test/failure notification (pause the tick by unscheduling or wait for a natural 503 — one intentional stale test per RESEARCH Open Q2)
  </acceptance_criteria>
  <done>Adzuna + heartbeat secrets set; probes 1–12 PASS; external monitor live with failure+recovery notifications — phase success criterion 4 proven end-to-end.</done>
  <resume-signal>Paste the Adzuna app_id/app_key and type "monitor configured" once the cron-job.org job with failure+recovery notifications is live</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → heartbeat | Public endpoint reachable by anyone with the URL |
| pg_cron → discovery-sweep | Scheduled call with shared secret |
| discovery-sweep → api.adzuna.com | Keyed third-party API; snippet content enters jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Information disclosure | Adzuna app_id/app_key | high | mitigate | Keys live only in supabase edge secrets (set via CLI), never in SPA, repo, or migrations; verified by grep absence in web/ build output being impossible (keys never touch web/) |
| T-02-11 | Elevation | discovery-sweep invocation | high | mitigate | Same x-cron-secret 401 gate as poll-tick (verify_jwt=false compensated in-function) |
| T-02-12 | Information disclosure / Spoofing | public heartbeat endpoint | medium | mitigate | Shared-secret `k` param (401 otherwise); read-only; response is exactly 'ok'/'stale' — no data leak surface |
| T-02-13 | Denial of service | Adzuna quota exhaustion / 429 blocking | medium | mitigate | Hourly cron + 240/day counter cutoff with UTC-day reset (Pitfall 7) |
| T-02-14 | Tampering (stored XSS) | adzuna description snippets in jobs | low | mitigate | Same store-only posture as T-02-06; Phase 3 sanitize-at-render contract covers all sources |
| T-02-SC | Tampering | npm/pip/cargo installs | low | mitigate | No package-manager installs; only pinned Deno specifiers already audited in RESEARCH (`he@1.2.0` OK, `@supabase/supabase-js@2.110.7` pinned) |
</threat_model>

<verification>
- `cd web && npm test && npm run build` — adzuna suite + banner build clean
- `cd web && npx supabase migration list --linked` — 0007 applied
- `node --env-file=scripts/.env scripts/verify-pipeline.ts` — probes 1–12 PASS (full phase pipeline: ATS + aggregator + dedup + heartbeat + cron liveness)
- Human-observed: cron-job.org failure email arrives when the heartbeat goes stale (roadmap success criterion 4, external half)
</verification>

<success_criteria>
- Roadmap success criterion 2 fully closed: aggregator overlap never yields a second row (fingerprint dedup, ATS wins)
- Roadmap success criterion 4 fully closed: per-company health visible on the watchlist (Plans 01–02) AND a dead or silently failing cron surfaces within one poll cycle via the in-app banner and the external monitor (D-07)
- DISC-02 satisfied on D-08 seed queries within the 250/day free budget; seed rows editable via SQL until Phase 3's preferences UI replaces them
</success_criteria>

<output>
Create `.planning/phases/02-watchlist-ingestion-monitoring/02-03-SUMMARY.md` when done.
</output>
