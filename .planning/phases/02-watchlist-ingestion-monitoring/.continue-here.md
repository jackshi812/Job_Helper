---
context: phase
phase: 02-watchlist-ingestion-monitoring
task: 2
total_tasks: 3
status: blocked
last_updated: 2026-07-17T13:55:11Z
---

<current_state>
Phase 02 execution is paused in the final plan, 02-03, before Task 2 of 3. Plans 02-01 and 02-02 are complete and live. Task 1 is complete, deployed, verified, and committed as `88be22e`. Task 2 has not started and requires a fresh authority checkpoint before its hosted migration, schedule, deployment, or probe.
</current_state>

<completed_work>

- Plan 02-01 complete: watchlist detection/verification/health UI, migration `0005`, and `verify-board`.
- Plan 02-02 complete: migration `0006`, watched-board polling, ATS adapters, deduplication, snapshots, health, heartbeat, Vault-backed minute cron, and hosted verification. Metadata commit: `a7b6cce`.
- Plan 02-03 Task 1 complete in `88be22e`:
  - `supabase/functions/heartbeat/index.ts`
  - `web/src/lib/pipeline.ts`
  - global stale banner in `web/src/components/Shell.tsx`
  - `[functions.heartbeat] verify_jwt = false` in `supabase/config.toml`
- `HEARTBEAT_SECRET` generated and stored only in gitignored `scripts/.env`, then set on project `fjcsvajkkztvlrpdplwx`.
- Hosted `heartbeat` version 1 is ACTIVE with `verify_jwt=false`; probes returned `401` without `k` and `200` with the secret.
- Local verification passes: 65/65 tests, production build, grep criteria, and `git diff --check`.
</completed_work>

<remaining_work>

- Task 2: after explicit approval, implement the Adzuna adapter, migration `0007`, discovery-sweep, budget guard, seed queries, and function config; apply migration `0007` to the hosted project; create the hourly `discovery-sweep-hourly` schedule; deploy `discovery-sweep` with JWT verification disabled; and invoke it with the existing cron secret, expecting the graceful missing-Adzuna-credentials skip. None of Task 2 is started.
- Task 3: user creates Adzuna app credentials and configures cron-job.org every 5–10 minutes with failure and recovery email notifications; then run full-phase hosted verification.
- Phase-level code review, regression tests, verification, UAT routing, and completion remain after Plan 02-03.
</remaining_work>

<decisions_made>

- The heartbeat endpoint is read-only, reveals only `ok`/`stale`, uses the same 30-minute threshold as the UI, and is protected by a dedicated query secret.
- The stale-pipeline banner is global so liveness failure is visible throughout the authenticated app.
- Every new hosted mutation retains a fresh approval boundary; Task 1 approval does not carry into Task 2.
</decisions_made>

<blockers>

- Blocking human approval: apply migration `0007`, create the hourly discovery schedule, deploy `discovery-sweep` with JWT verification disabled, and run the secret-gated graceful-skip probe. The user has not approved these Task 2 hosted actions.
- Later human setup: Adzuna app_id/app_key and cron-job.org external monitoring with failure and recovery notifications.
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-watchlist-ingestion-monitoring/.continue-here.md` — this checkpoint and exact pending authority.
2. `.planning/phases/02-watchlist-ingestion-monitoring/02-03-PLAN.md` — authoritative Task 2–3 actions and human setup.
3. `.planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md` — live ingestion/heartbeat contracts this plan extends.
4. `.planning/STATE.md` — current phase position and accumulated decisions.
5. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)

- None discovered. Stopping before a new hosted mutation without explicit approval was the intended safety gate.

## Infrastructure State

- Supabase project: `job-copilot` (`fjcsvajkkztvlrpdplwx`).
- Live: migrations `0001`–`0006`, `verify-board`, `poll-tick`, `heartbeat`, `CRON_SECRET`, `HEARTBEAT_SECRET`, Vault `project_url`/`cron_secret`, and automatic minute heartbeat advancement.
- Not live: migration `0007`, discovery-sweep, Adzuna secrets/calls, hourly discovery cron, and cron-job.org monitor.
- No background server, watcher, subagent, or async external job is running.
- No uncommitted Plan 02-03 implementation files remain.
- Unrelated user change: `.DS_Store`; preserve it and never stage, edit, discard, or commit it.

<context>
Resume with a fresh Plan 02-03 continuation executor. Verify commit `88be22e` and do not redo Task 1. Do not create migration `0007`, Adzuna adapter/test files, `discovery-sweep`, or any hosted Task 2 change until the user explicitly approves the applicable mutation set.
</context>

<next_action>
Obtain explicit approval for Task 2: local implementation plus hosted migration `0007`, the hourly discovery schedule, `discovery-sweep` deployment with JWT verification disabled, and the secret-gated graceful-skip probe. Do not request Adzuna credentials or call Adzuna until Task 3.
</next_action>
