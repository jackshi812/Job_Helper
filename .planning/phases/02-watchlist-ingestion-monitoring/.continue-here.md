---
context: phase
phase: 02-watchlist-ingestion-monitoring
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-07-17T03:58:11.837Z
---

<current_state>
Phase 02 execution is paused in the final plan, 02-03, at Task 1 of 3. Plans 02-01 and 02-02 are complete and live. Task 1's heartbeat endpoint and stale-pipeline banner are complete locally, but the first Plan 02-03 hosted actions remain unapproved and unattempted.
</current_state>

<completed_work>

- Plan 02-01 complete: watchlist detection/verification/health UI, migration `0005`, and `verify-board`.
- Plan 02-02 complete: migration `0006`, watched-board polling, ATS adapters, deduplication, snapshots, health, heartbeat, Vault-backed minute cron, and hosted verification. Metadata commit: `a7b6cce`.
- Plan 02-03 Task 1 local work prepared:
  - `supabase/functions/heartbeat/index.ts`
  - `web/src/lib/pipeline.ts`
  - global stale banner in `web/src/components/Shell.tsx`
  - `[functions.heartbeat] verify_jwt = false` in `supabase/config.toml`
- Local verification passes: 65/65 tests, production build, grep criteria, and `git diff --check`.
</completed_work>

<remaining_work>

- Task 1: obtain explicit approval, generate `HEARTBEAT_SECRET`, store it only in gitignored `scripts/.env`, set it on Supabase project `job-copilot` (`fjcsvajkkztvlrpdplwx`), deploy `heartbeat`, verify `401` without `k` and `200` or `503` with the secret, then commit Task 1.
- Task 2: implement the Adzuna adapter, migration `0007`, discovery-sweep, budget guard, seed queries, hourly cron, hosted database push, and function deployment. None of these actions is currently approved or started.
- Task 3: user creates Adzuna app credentials and configures cron-job.org every 5–10 minutes with failure and recovery email notifications; then run full-phase hosted verification.
- Phase-level code review, regression tests, verification, UAT routing, and completion remain after Plan 02-03.
</remaining_work>

<decisions_made>

- The heartbeat endpoint is read-only, reveals only `ok`/`stale`, uses the same 30-minute threshold as the UI, and is protected by a dedicated query secret.
- The stale-pipeline banner is global so liveness failure is visible throughout the authenticated app.
- Every new hosted mutation retains a fresh approval boundary; prior Plan 02-01/02-02 approvals do not carry into Plan 02-03.
</decisions_made>

<blockers>

- Blocking human approval: generate/store/set `HEARTBEAT_SECRET`, deploy `heartbeat`, and run the two hosted probes. The user has not approved these actions.
- Later human setup: Adzuna app_id/app_key and cron-job.org external monitoring with failure and recovery notifications.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable checkpoint and exact pending authority.
2. `.planning/phases/02-watchlist-ingestion-monitoring/02-03-PLAN.md` — authoritative Task 1–3 actions and human setup.
3. `.planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md` — live ingestion/heartbeat contracts this plan extends.
4. `.planning/STATE.md` — current phase position and accumulated decisions.
5. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)

- None discovered. Stopping before a new hosted mutation without explicit approval was the intended safety gate.

## Infrastructure State

- Supabase project: `job-copilot` (`fjcsvajkkztvlrpdplwx`).
- Live: migrations `0001`–`0006`, `verify-board`, `poll-tick`, `CRON_SECRET`, Vault `project_url`/`cron_secret`, and automatic minute heartbeat advancement.
- Not live: `HEARTBEAT_SECRET`, heartbeat function, migration `0007`, discovery-sweep, Adzuna secrets/calls, hourly discovery cron, and cron-job.org monitor.
- No background server, watcher, subagent, or async external job is running.
- Uncommitted Task 1 files: `supabase/functions/heartbeat/index.ts`, `web/src/lib/pipeline.ts`, `web/src/components/Shell.tsx`, `supabase/config.toml`.
- Unrelated user change: `.DS_Store`; preserve it and never stage, edit, discard, or commit it.

<context>
Resume with a fresh Plan 02-03 continuation executor. Preserve the prepared Task 1 files and do not redo local work. The executor must not generate a heartbeat secret, deploy, probe the hosted endpoint, or begin Task 2 until the user explicitly approves the applicable mutation set.
</context>

<next_action>
Start with: run `$gsd-resume-work`, then obtain explicit approval for the five Task 1 heartbeat actions and continue Plan 02-03 from Task 1.
</next_action>
