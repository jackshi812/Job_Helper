---
context: phase
phase: 02-watchlist-ingestion-monitoring
task: 3
total_tasks: 3
status: blocked
last_updated: 2026-07-17T14:08:36Z
---

<current_state>
Phase 02 execution is paused in the final plan, 02-03, at the human-action checkpoint before Task 3 of 3. Plans 02-01 and 02-02 are complete and live. Tasks 1 and 2 are complete, deployed, verified, and committed. No Task 3 external setup has been performed.
</current_state>

<completed_work>

- Plan 02-01 complete: watchlist detection/verification/health UI, migration `0005`, and `verify-board`.
- Plan 02-02 complete: migration `0006`, watched-board polling, ATS adapters, deduplication, snapshots, health, heartbeat, Vault-backed minute cron, and hosted verification. Metadata commit: `a7b6cce`.
- Plan 02-03 Task 1 complete in `88be22e`:
  - `supabase/functions/heartbeat/index.ts`
  - `web/src/lib/pipeline.ts`
  - global stale banner in `web/src/components/Shell.tsx`
  - `[functions.heartbeat] verify_jwt = false` in `supabase/config.toml`
- `HEARTBEAT_SECRET` generated and stored only in gitignored `scripts/.env`, then set on project `fjcsvajkkztvlrpdplwx`.
- Hosted `heartbeat` version 1 is ACTIVE with `verify_jwt=false`; probes returned `401` without `k` and `200` with the secret.
- Plan 02-03 Task 2 completed with the required TDD sequence:
  - RED test commit `9ae6bc3`
  - GREEN adapter commit `a8f76f9`
  - Migration, scheduler, and deployed sweep commit `9c7b174`
- Migration `0007_discovery.sql` is live with four shared seed queries, Adzuna daily-budget fields, RLS, and `discovery-sweep-hourly` at `0 * * * *`.
- Hosted `discovery-sweep` is ACTIVE with `verify_jwt=false` and in-function `x-cron-secret` authorization.
- Hosted probes returned `401` without the cron secret and `200 {"skipped":"missing adzuna credentials"}` with it, proving the dormant path is specifically credential-gated.
- Local verification passes: 68/68 tests, production build, task grep criteria, and `git diff --check`.
</completed_work>

<remaining_work>

- Task 3: user creates Adzuna app credentials and configures cron-job.org every 5–10 minutes with failure and recovery email notifications; then run full-phase hosted verification.
- Phase-level code review, regression tests, verification, UAT routing, and completion remain after Plan 02-03.
</remaining_work>

<decisions_made>

- The heartbeat endpoint is read-only, reveals only `ok`/`stale`, uses the same 30-minute threshold as the UI, and is protected by a dedicated query secret.
- The stale-pipeline banner is global so liveness failure is visible throughout the authenticated app.
- Every new hosted mutation retains a fresh approval boundary; Task 1 approval does not carry into Task 2.
- Adzuna description data is always stored as a partial text snapshot; exact open Adzuna rows refresh only `last_seen_at`, preserving the first snapshot.
- Missing Adzuna credentials are checked only after cron-secret authorization and return an explicit safe no-op before any database or third-party request.
</decisions_made>

<blockers>

- Blocking human setup: create an Adzuna application and place its app ID/key in gitignored `scripts/.env`; configure cron-job.org external monitoring with failure and recovery notifications.
- No credentialed Adzuna API call or cron-job.org configuration has occurred.
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-watchlist-ingestion-monitoring/.continue-here.md` — this checkpoint and exact pending human setup.
2. `.planning/phases/02-watchlist-ingestion-monitoring/02-03-PLAN.md` — authoritative Task 3 actions and human setup.
3. `.planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md` — live ingestion/heartbeat contracts this plan extends.
4. `.planning/STATE.md` — current phase position and accumulated decisions.
5. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)

- None discovered. Stopping before a new hosted mutation without explicit approval was the intended safety gate.

## Infrastructure State

- Supabase project: `job-copilot` (`fjcsvajkkztvlrpdplwx`).
- Live: migrations `0001`–`0007`, `verify-board`, `poll-tick`, `heartbeat`, `discovery-sweep`, `CRON_SECRET`, `HEARTBEAT_SECRET`, Vault `project_url`/`cron_secret`, automatic minute heartbeat advancement, and the hourly discovery schedule.
- Not live/configured: Adzuna secrets/calls and the cron-job.org monitor.
- No background server, watcher, subagent, or async external job is running.
- No uncommitted Plan 02-03 implementation files remain.
- Unrelated user change: `.DS_Store`; preserve it and never stage, edit, discard, or commit it.

<context>
Resume with a fresh Plan 02-03 continuation executor. Verify commits `88be22e`, `9ae6bc3`, `a8f76f9`, and `9c7b174`; do not redo Tasks 1 or 2. Continue only after the user completes both Task 3 account/configuration steps.
</context>

<next_action>
Ask the user to complete Task 3: create Adzuna credentials and configure the external heartbeat monitor. After confirmation, set the Adzuna hosted secrets, extend `scripts/verify-pipeline.ts`, perform the first credentialed sweep, and run probes 1–12. Never print or commit secret values.
</next_action>
