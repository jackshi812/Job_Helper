---
phase: 02-watchlist-ingestion-monitoring
plan: 2
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - supabase/functions/_shared/adapters/types.ts
  - supabase/functions/_shared/adapters/greenhouse.ts
  - supabase/functions/_shared/adapters/lever.ts
  - supabase/functions/_shared/adapters/ashby.ts
  - supabase/functions/_shared/dedup.ts
  - web/tests/adapters.test.ts
  - web/tests/dedup.test.ts
  - supabase/migrations/0006_jobs_pipeline.sql
  - supabase/functions/poll-tick/index.ts
  - supabase/config.toml
  - scripts/verify-pipeline.ts
autonomous: true
requirements: [DISC-01, DISC-03, DISC-04, DISC-05, DISC-06, PREF-04]
user_setup:
  - service: supabase
    why: "pg_cron → edge-function auth needs two Vault secrets; Vault rows can only be created in the Dashboard SQL editor (no CLI/API path on free tier)"
    env_vars:
      - name: CRON_SECRET
        source: "Generate once locally (openssl rand -hex 16); the SAME value goes to (a) supabase secrets set CRON_SECRET=…, (b) Vault secret named cron_secret, (c) scripts/.env for verification"
    dashboard_config:
      - task: "Create Vault secrets: run in Dashboard → SQL editor: select vault.create_secret('https://<project-ref>.supabase.co', 'project_url'); select vault.create_secret('<generated value>', 'cron_secret');"
        location: "Supabase Dashboard → SQL Editor (Vault UI also works: Integrations → Vault)"

must_haves:
  truths:
    - "A job newly posted on a watched board is inserted into jobs within 15 minutes and exactly once across repeated polls (DISC-01, DISC-03)"
    - "Every ATS-sourced job row carries a full JD snapshot (description_html + description_text, snapshot_partial=false) captured at first sight (DISC-04)"
    - "Jobs absent from ~3 consecutive successful polls of their company are marked closed; a failed poll never closes anything (DISC-05)"
    - "companies.last_success_at / consecutive_failures / last_error update on every poll, feeding the Plan 01 badges (PREF-04, D-06)"
    - "pipeline_heartbeat.last_tick_at advances on every invocation and last_success_at when at least one company polled successfully (DISC-06)"
  artifacts:
    - path: "supabase/migrations/0006_jobs_pipeline.sql"
      provides: "jobs table with unique (source, external_id) + fingerprint, pipeline_heartbeat singleton, claim_due_companies() RPC, pg_cron per-minute schedule"
      contains: "create table public.jobs"
    - path: "supabase/functions/poll-tick/index.ts"
      provides: "Per-minute pipeline: due-queue claim, adapter fetch, two-layer dedup, snapshots, stale-close, health + heartbeat writes"
    - path: "supabase/functions/_shared/dedup.ts"
      provides: "Pure fingerprint normalization"
      exports: ["fingerprint"]
    - path: "supabase/functions/_shared/adapters/types.ts"
      provides: "NormalizedJob contract shared by all adapters and poll-tick"
      contains: "NormalizedJob"
  key_links:
    - from: "supabase/functions/poll-tick/index.ts"
      to: "supabase/functions/_shared/adapters/greenhouse.ts"
      via: "imports pollGreenhouse/pollLever/pollAshby returning NormalizedJob[]"
      pattern: "from ['\"]\\.\\./_shared/adapters"
    - from: "supabase/functions/poll-tick/index.ts"
      to: "supabase/migrations/0006_jobs_pipeline.sql"
      via: "rpc('claim_due_companies') due-queue claim + upsert on (source, external_id)"
      pattern: "claim_due_companies"
    - from: "supabase/migrations/0006_jobs_pipeline.sql"
      to: "supabase/functions/poll-tick/index.ts"
      via: "cron.schedule net.http_post to /functions/v1/poll-tick with x-cron-secret from Vault"
      pattern: "poll-tick"
---

<objective>
Ingestion pipeline vertical slice: pg_cron fires `poll-tick` every minute; it claims the ~10 most-overdue watched companies, polls their boards through per-ATS adapters, inserts new postings exactly once (two-layer dedup), snapshots JDs at first sight, advances last_seen_at watermarks, closes stale jobs only after successful polls, and writes per-company health plus the pipeline heartbeat row. After this plan, the Plan 01 health badges show live truth and new postings from the three seeded boards land automatically.

Purpose: this is the core-value engine — roadmap success criteria 2 and 3 plus the data half of 4.
Output: adapters + dedup modules (Vitest-covered), migration 0006 (pushed), `poll-tick` deployed and scheduled, hosted verification script.
</objective>

## Phase Goal

**As an** invited job seeker, **I want to** have new postings from 100+ watched career sites plus an aggregator land in the system exactly once within 5–15 minutes, **so that** I can trust per-company health and pipeline liveness at a glance and never miss a fresh posting.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-CONTEXT.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-PATTERNS.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 02 portion)

- **Tables:** `public.jobs` (columns: `id`, `company_id`, `source`, `external_id`, `title`, `location`, `absolute_url`, `posted_at`, `description_html`, `description_text`, `snapshot_partial`, `fingerprint`, `status`, `first_seen_at`, `last_seen_at`, `closed_at`; unique `(source, external_id)`), `public.pipeline_heartbeat` (singleton: `id`, `last_tick_at`, `last_success_at`)
- **SQL functions:** `public.claim_due_companies(batch_size int)` (due-queue claim, service-role only)
- **Cron jobs:** `poll-tick-every-minute` (`* * * * *` via pg_cron + pg_net + Vault)
- **Migration:** `supabase/migrations/0006_jobs_pipeline.sql`
- **Types:** `NormalizedJob` interface (fields per RESEARCH: `source`, `externalId`, `title`, `location`, `absoluteUrl`, `postedAt`, `descriptionHtml`, `descriptionText`, `snapshotPartial`, `companyName`)
- **Functions (TS):** `fingerprint(company, title, location)`, `mapGreenhouseJob()`, `pollGreenhouse(token, knownIds)`, `mapLeverPosting()`, `pollLever(site, region)`, `mapAshbyJob()`, `pollAshby(name)`
- **Edge function:** `poll-tick` (POST, gated by `x-cron-secret` header = env `CRON_SECRET`)
- **Config keys:** `[functions.poll-tick] verify_jwt = false` in supabase/config.toml
- **Env/secrets:** `CRON_SECRET` (edge secret + Vault `cron_secret` + Vault `project_url`)
- **Scripts:** `scripts/verify-pipeline.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ATS adapters + dedup fingerprint (pure modules, fixture-tested)</name>
  <files>supabase/functions/_shared/adapters/types.ts, supabase/functions/_shared/adapters/greenhouse.ts, supabase/functions/_shared/adapters/lever.ts, supabase/functions/_shared/adapters/ashby.ts, supabase/functions/_shared/dedup.ts, web/tests/adapters.test.ts, web/tests/dedup.test.ts</files>
  <read_first>
    - .planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md (Pattern 1: live-verified field tables for all three ATSs — the fixture source of truth; Code Examples: NormalizedJob, greenhouse poll shape, fingerprint normalization; Pitfalls 1–4)
    - supabase/functions/_shared/detect.ts (pure-module conventions established in Plan 01)
    - web/src/lib/resumes.ts (interface + error-throwing style)
    - web/tests/detect.test.ts (cross-directory test import pattern from Plan 01)
  </read_first>
  <behavior>
    - Test (dedup): `fingerprint('Stripe', 'Software Engineer (Remote)', 'San Francisco, CA')` → `stripe|software engineer|san francisco` — lowercase, parentheticals dropped, punctuation stripped, whitespace collapsed, city = first comma segment
    - Test (dedup): null location → empty city segment; identical titles differing only in punctuation/case produce equal fingerprints
    - Test (greenhouse): mapping a fixture list item (fields per RESEARCH: id int 7954688, title, first_published ISO-with-offset, absolute_url, location.name) + separately-fetched entity-escaped content → NormalizedJob with externalId '7954688' (stringified), postedAt ISO, descriptionHtml entity-DECODED (input containing the escaped form of an h2 tag comes out as a real h2 tag — Pitfall 1)
    - Test (lever): fixture posting (id UUID, text as the TITLE field, createdAt epoch-milliseconds 1711403416463, hostedUrl, categories.location, description + lists[] + additional) → postedAt equals new Date(ms).toISOString() (Pitfall 2 — never seconds), descriptionHtml concatenates description + rendered lists + additional
    - Test (ashby): fixture job (id UUID, title with leading whitespace, publishedAt ISO, jobUrl, location, isListed, descriptionHtml) → title trimmed, unlisted jobs (isListed false) filtered out by the mapper's list-level helper, snapshotPartial false
    - Test (all): mapping functions are pure — no fetch mocking required
  </behavior>
  <action>
    Write failing fixture tests first (fixtures hand-built from the RESEARCH Pattern 1 field tables — they were captured from live calls on 2026-07-16), then implement. `types.ts`: export the `NormalizedJob` interface exactly as in RESEARCH Code Examples. `dedup.ts`: export `fingerprint(company, title, location)` implementing the RESEARCH normalization verbatim; pure, no imports. Adapters: separate pure mapping (`mapGreenhouseJob`, `mapLeverPosting`, `mapAshbyJob` + list-level helpers) from thin fetch wrappers (`pollGreenhouse(token, knownIds)`, `pollLever(site, region)`, `pollAshby(name)`) so Vitest covers mapping without network. Greenhouse: lean list URL (no content param), per-NEW-job detail fetch for content (12x cheaper — Pitfall 4 / 2s CPU cap), entity-decode via `npm:he@1.2.0` — keep the decode call isolated in the fetch wrapper OR inject a `decode` function parameter into the mapper so the pure mapper stays npm-specifier-free and Vitest-importable (PATTERNS.md requires npm: imports stay out of Vitest-reachable code paths; the test passes the real `he` decode from web/'s node_modules if installed, else a minimal stub — prefer the injected-parameter design). Lever: `mode=json`, region 'eu' switches host to api.eu.lever.co; title comes from the `text` field; createdAt is epoch MILLISECONDS. Ashby: `includeCompensation=true`, filter `isListed === true`, trim titles, descriptionHtml already unescaped. All fetch wrappers: check `res.ok` before parsing (Ashby 404 is plain text — Pitfall 3), throw `Error` with company context in the message (resumes.ts style). Every adapter returns `NormalizedJob[]` with source-correct `snapshotPartial: false`. Code style: no semicolons, single quotes, named exports.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` green including adapters.test.ts and dedup.test.ts
    - `grep -c "export function fingerprint" supabase/functions/_shared/dedup.ts` returns 1
    - `grep -c "toISOString" supabase/functions/_shared/adapters/lever.ts` returns >= 1 (epoch-ms normalization)
    - `grep -c "isListed" supabase/functions/_shared/adapters/ashby.ts` returns >= 1
    - `grep -rn "npm:" supabase/functions/_shared/dedup.ts supabase/functions/_shared/adapters/types.ts` returns no matches (Vitest-importable modules stay specifier-free)
  </acceptance_criteria>
  <done>Three adapters normalize verified ATS shapes to NormalizedJob with all four timestamp/escaping/filter pitfalls covered by tests; fingerprint matches the RESEARCH normalization exactly.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migration 0006 (jobs + heartbeat + claim RPC + pg_cron schedule) + supabase db push</name>
  <files>supabase/migrations/0006_jobs_pipeline.sql, supabase/config.toml</files>
  <read_first>
    - supabase/migrations/0005_watchlist.sql (Plan 01 conventions + the companies columns the RPC reads)
    - supabase/migrations/0004_delete_my_data.sql (function security posture: security invoker, set search_path = '', revoke-then-grant)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md (Pattern 3 claim SQL, Pattern 7 scheduling SQL, Pitfall 8 RLS shape)
    - supabase/config.toml (existing table-per-section style; [edge_runtime] area for placement reference)
  </read_first>
  <action>
    Migration `supabase/migrations/0006_jobs_pipeline.sql` in the 0002/0005 ordering (create → RLS → index → grant → policies):

    (1) `public.jobs`: `id uuid primary key default gen_random_uuid()`, `company_id uuid references public.companies (id) on delete set null` (nullable — aggregator rows have no company), `source text not null` constrained to ('greenhouse','lever','ashby','adzuna'), `external_id text not null`, `title text not null`, `location text`, `absolute_url text not null`, `posted_at timestamptz`, `description_html text`, `description_text text`, `snapshot_partial boolean not null default false`, `fingerprint text not null`, `status text not null default 'open'` constrained to ('open','closed'), `first_seen_at timestamptz not null default now()`, `last_seen_at timestamptz not null default now()`, `closed_at timestamptz`; `unique (source, external_id)` (dedup layer 1, DISC-03 backstop for concurrent ticks); partial index `jobs_open_fingerprint_idx on public.jobs (fingerprint) where status = 'open'` (layer-2 lookups match open jobs only per RESEARCH Pattern 4); index on `company_id`. RLS enabled; `grant select to authenticated` ONLY — all writes come from the service-role poller which bypasses RLS (Pitfall 8); one `jobs_select_shared` policy `using (true)`; SQL comment noting the shared-read D-01 rationale and that Phase 3 MUST sanitize description_html at render time (stored third-party HTML).

    (2) `public.pipeline_heartbeat` singleton: `id boolean primary key default true` with `check (id)`, `last_tick_at timestamptz`, `last_success_at timestamptz`; insert the single seed row; RLS enabled, `grant select to authenticated`, select-only policy `using (true)`.

    (3) `public.claim_due_companies(batch_size int default 10) returns setof public.companies`: implements the RESEARCH Pattern 3 claim exactly — update companies set last_polled_at = now() where id in (select id from companies where last_polled_at is null or last_polled_at < now() - interval '9 minutes' order by last_polled_at asc nulls first limit batch_size) returning * — `language sql`, `security invoker`, `set search_path = ''` (0004 posture; fully qualify public.companies inside). Revoke execute from public, anon, AND authenticated: only the service-role poller may claim (claim mutates polling state).

    (4) Scheduling: `create extension if not exists pg_cron; create extension if not exists pg_net;` then `cron.schedule('poll-tick-every-minute', '* * * * *', …)` with body per RESEARCH Pattern 7: `net.http_post` to `(vault project_url) || '/functions/v1/poll-tick'`, headers jsonb with Content-Type application/json and `x-cron-secret` set from the Vault secret named `cron_secret`, empty jsonb body, timeout_milliseconds 120000. NOTE the auth deviation from RESEARCH Pattern 7 and record it in a SQL comment: Phase 1 adopted the new publishable/secret key system whose keys are NOT JWTs, so Authorization-header JWT verification is not usable for cron calls; instead poll-tick deploys with verify_jwt disabled and enforces the shared-secret header in-function (same trust level: secret lives only in Vault + edge env). The cron.schedule call stores SQL text — it succeeds at migration time even before Vault secrets exist; ticks no-op with an error in cron.job_run_details until user_setup completes.

    `supabase/config.toml`: append `[functions.poll-tick]` with `verify_jwt = false` (RESEARCH A5 — if the key name differs at deploy, fall back to `supabase functions deploy poll-tick --no-verify-jwt`).

    [BLOCKING] `npx supabase db push` from web/ AFTER the migration file is complete; confirm via `npx supabase migration list --linked` showing 0006. Set SUPABASE_ACCESS_TOKEN if a non-TTY prompt blocks. Then generate the CRON_SECRET value (`openssl rand -hex 16`), run `npx supabase secrets set CRON_SECRET=<value>` from web/, append `CRON_SECRET=<value>` to scripts/.env (gitignored), and surface the user_setup Vault instructions (project_url + cron_secret) — the pipeline cannot tick until the user creates those two Vault rows.
  </action>
  <verify>
    <automated>cd web && npx supabase migration list --linked && grep -c "poll-tick" ../supabase/config.toml</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase migration list --linked` (from web/) shows 0006 applied remotely
    - `grep -c "unique (source, external_id)" supabase/migrations/0006_jobs_pipeline.sql` returns 1
    - `grep -c "where status = 'open'" supabase/migrations/0006_jobs_pipeline.sql` returns >= 1 (partial fingerprint index)
    - `grep -c "cron.schedule" supabase/migrations/0006_jobs_pipeline.sql` returns 1
    - `grep -c "grant select on table public.jobs to authenticated" supabase/migrations/0006_jobs_pipeline.sql` returns 1 and no insert/update/delete grant on jobs exists for authenticated (write path is service-role only)
    - supabase/config.toml contains a `[functions.poll-tick]` section with verify_jwt = false
    - `npx supabase secrets list` (from web/) shows CRON_SECRET
  </acceptance_criteria>
  <done>jobs/pipeline_heartbeat live with correct shared-read RLS, the claim RPC is service-role-only, the per-minute cron is scheduled, and CRON_SECRET is set everywhere Claude can set it.</done>
</task>

<task type="auto">
  <name>Task 3: poll-tick edge function + deploy + hosted pipeline verification</name>
  <files>supabase/functions/poll-tick/index.ts, scripts/verify-pipeline.ts</files>
  <read_first>
    - .planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md (Patterns 3–6 — this function IS those four patterns composed; Pitfalls 4, 5)
    - supabase/functions/_shared/adapters/greenhouse.ts, lever.ts, ashby.ts, supabase/functions/_shared/dedup.ts (Task 1 import surface)
    - supabase/functions/verify-board/index.ts (Deno.serve + response conventions from Plan 01)
    - scripts/verify-rls.ts (script skeleton: env validation, probe clients, PASS/FAIL, finally-cleanup, main-module guard)
    - web/src/lib/supabase.ts (isolated-client options pattern: persistSession false, autoRefreshToken false)
  </read_first>
  <action>
    `supabase/functions/poll-tick/index.ts` (Deno.serve): FIRST gate — return 401 unless request header `x-cron-secret` equals `Deno.env.get('CRON_SECRET')` (compensates for verify_jwt=false). Create a service-role client per invocation via `npm:@supabase/supabase-js@2.110.7` (EXACT pin — package-legitimacy audit forbids bumping) using SUPABASE_URL + the auto-injected service key env, with persistSession/autoRefreshToken false. Sequence per RESEARCH:

    (a) Heartbeat-first: upsert `pipeline_heartbeat.last_tick_at = now()` BEFORE any work (proves invocation even when polling fails — Pattern 6 / Pitfall 6).
    (b) Claim batch: `rpc('claim_due_companies', { batch_size: 10 })` (Pattern 3 — 100 companies / 10 per tick ≈ every company each ~10 min, inside the 5–15 window, DISC-01).
    (c) Poll with `Promise.allSettled`, one entry per company routed by ats_type to pollGreenhouse/pollLever/pollAshby; for greenhouse pass knownIds = external_ids of that company's open jobs (fetched in one select) so only NEW jobs trigger content fetches. Per-company failure isolation: a rejected promise records that company's failure and never skips the rest.
    (d) Success path per company: two-layer dedup + upsert (Pattern 4). For each NormalizedJob compute `fingerprint(companyName-or-watchlist-name, title, location)`. Upsert with `on conflict (source, external_id) do update set last_seen_at = now()`. Before inserting a genuinely-new ATS row: if an OPEN adzuna-source row shares the fingerprint → UPDATE that row in place (set company_id, source, external_id, full snapshot fields, snapshot_partial=false) instead of inserting; if an OPEN row of the SAME company shares the fingerprint with a different external_id (repost) → update that row's external_id + last_seen_at instead of inserting. New inserts carry the JD snapshot at first sight (DISC-04): description_html/description_text from the adapter, immutable afterwards (conflict-update touches only last_seen_at). Then health: set last_success_at = now(), consecutive_failures = 0, last_error = null. Then stale-close (DISC-05, Pattern 5): only in this success path, and only when the poll returned a non-empty job list for a company that previously had open jobs (implausible-empty guard, Pitfall 5) — close open jobs of this company with last_seen_at older than 35 minutes (status='closed', closed_at=now()).
    (e) Failure path per company: increment consecutive_failures, set last_error to the error message (feeds D-06 badges). Never close jobs here.
    (f) Finish: if ≥1 company succeeded, update pipeline_heartbeat.last_success_at = now(). Respond 200 with a JSON summary { claimed, succeeded, failed, inserted, closed } for log legibility. Keep per-tick compute lean (Pitfall 4: 2s CPU cap) — no re-stringifying large bodies, upsert per company as you go.

    Deploy: `npx supabase functions deploy poll-tick` from web/ (add --no-verify-jwt if config.toml key is not honored).

    `scripts/verify-pipeline.ts` (verify-rls.ts skeleton; env: SUPABASE_URL, publishable + secret keys per scripts/.env.example naming, CRON_SECRET; run `node --env-file=scripts/.env scripts/verify-pipeline.ts`): (1) assert the three seeded companies exist (stripe/palantir/ramp from Plan 01 — insert via the verify-then-insert flow if missing); (2) POST the deployed poll-tick URL with x-cron-secret up to 10 times or until all three companies have last_polled_at set (each call claims the due batch); (3) assert jobs rows exist for all three sources, each with non-null description_html, description_text, fingerprint, snapshot_partial=false, status='open'; (4) record total jobs count, POST poll-tick again (companies won't be due — also directly re-run once with a forced claim by clearing last_polled_at via secret client on ONE company), assert the total count did not grow from re-seeing the same postings (DISC-03 exactly-once); (5) assert pipeline_heartbeat.last_tick_at and last_success_at are both within the last few minutes; (6) assert companies.consecutive_failures = 0 and last_success_at set for all three; (7) wrong/missing x-cron-secret → 401; (8) authenticated (publishable-key session) insert into jobs is rejected by RLS. Print PASS/FAIL per probe, exit non-zero on any failure.
  </action>
  <verify>
    <automated>node --env-file=scripts/.env scripts/verify-pipeline.ts && cd web && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node --env-file=scripts/.env scripts/verify-pipeline.ts` prints PASS for all 8 probes and exits 0
    - jobs table contains rows with source greenhouse AND lever AND ashby, every ATS row having non-null description_html and snapshot_partial=false (DISC-04)
    - Invoking poll-tick twice for the same board does not increase the jobs count (DISC-03)
    - poll-tick returns 401 without the correct x-cron-secret header
    - `grep -c "allSettled" supabase/functions/poll-tick/index.ts` returns >= 1 (per-company isolation)
    - `grep -c "npm:@supabase/supabase-js@2.110.7" supabase/functions/poll-tick/index.ts` returns 1 (exact pin)
    - Stale-close statements appear only inside the per-company success branch (code review assertion: no close/update-to-closed call reachable from the catch/failure path)
  </acceptance_criteria>
  <done>New postings from the three live seeded boards land exactly once with full snapshots; health columns and heartbeat advance; the per-minute cron (once Vault user_setup is done) keeps every watched company inside the 5–15 minute window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron/pg_net → poll-tick | Scheduled HTTP call carrying the shared secret across the network |
| poll-tick → external ATS APIs | Third-party JSON (and JD HTML) enters the system |
| Postgres jobs table → future Phase 3 renderer | Stored third-party HTML crosses to the browser later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Elevation | poll-tick invocation | high | mitigate | verify_jwt=false is compensated by a mandatory x-cron-secret header check returning 401 on mismatch; secret exists only in Vault + edge env + gitignored scripts/.env; asserted by verify-pipeline probe 7 |
| T-02-06 | Tampering (stored XSS) | jobs.description_html | medium | mitigate | Phase 2 stores and never renders; migration + code comments establish the binding sanitize-at-render contract for Phase 3 (DOMPurify or equivalent) |
| T-02-07 | Information disclosure | service-role key | high | mitigate | Key read from edge env only, client created per-invocation, never in SPA or repo; scripts keep secret key in gitignored scripts/.env (Phase 1 boundary) |
| T-02-08 | Tampering (integrity) | concurrent tick dedup race | low | mitigate | DB unique (source, external_id) + on-conflict upsert is the backstop; claim-then-poll RPC makes overlapping ticks safe |
| T-02-09 | Denial of service | oversized ATS responses vs 2s CPU cap | medium | mitigate | Lean Greenhouse lists, batch of 10, per-company upserts, no blob re-stringify (Pitfall 4) |
| T-02-SC | Tampering | npm/pip/cargo installs | low | mitigate | No package-manager installs: Deno npm: specifiers pinned to audited versions (`he@1.2.0` OK verdict; `@supabase/supabase-js@2.110.7` pinned per audit — bump forbidden) |
</threat_model>

<verification>
- `cd web && npm test` — adapter + dedup fixture suites green
- `cd web && npx supabase migration list --linked` — 0006 applied
- `node --env-file=scripts/.env scripts/verify-pipeline.ts` — all 8 hosted probes PASS
- After user_setup (Vault secrets): cron.job_run_details shows poll-tick firing each minute and pipeline_heartbeat.last_tick_at advancing without manual invocation (observable in Dashboard; also confirmed by Plan 03's banner staying absent)
</verification>

<success_criteria>
- Roadmap success criterion 2 (ATS half): a newly posted job on a watched board appears within 15 minutes, exactly once across repeated polls and reposts
- Roadmap success criterion 3: every captured job has a first-sight JD snapshot; disappeared jobs close only after successful polls
- Plan 01 badges now show live OK/Failing/Stale states from real health columns (D-05/D-06 data path complete)
</success_criteria>

<output>
Create `.planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md` when done.
</output>
