---
phase: 02-watchlist-ingestion-monitoring
plan: 4
type: execute
wave: 1
depends_on: [02-02]
files_modified:
  - supabase/functions/_shared/lifecycle.ts
  - web/tests/lifecycle.test.ts
  - supabase/functions/poll-tick/index.ts
autonomous: true
gap_closure: true
requirements: [DISC-03, DISC-05, DISC-06]
must_haves:
  truths:
    - "A currently live ATS posting is open and current even after it was previously stale-closed (gap: closed jobs cannot reopen)"
    - "The heartbeat reports a healthy scheduler when an authorized tick has no companies to poll (gap: false-stale heartbeat on no-work ticks)"
    - "A job absent for the 35-minute successful-poll grace period transitions from open to closed, while a failed or empty poll closes nothing (behavior-unverified item now automated)"
    - "Reopening a returned closed job preserves its immutable first-sight snapshot (description_html, description_text, first_seen_at, posted_at unchanged)"
  artifacts:
    - path: "supabase/functions/_shared/lifecycle.ts"
      provides: "Pure job-lifecycle classification: exact-ID resolution across open AND closed rows, reopen set, close-candidate set, no-work heartbeat decision"
      exports: ["planCompanySync", "shouldAdvanceSuccessHeartbeat"]
    - path: "web/tests/lifecycle.test.ts"
      provides: "Close-then-return reopen regression, disappearance-grace close transition, failed/empty-poll no-close, and heartbeat-advance transition tests"
    - path: "supabase/functions/poll-tick/index.ts"
      provides: "Poll tick that loads open and closed jobs, reopens returned closed exact-ID matches, closes via pure close-candidate ids, and advances last_success_at on successful no-work ticks"
  key_links:
    - from: "supabase/functions/poll-tick/index.ts"
      to: "supabase/functions/_shared/lifecycle.ts"
      via: "imports planCompanySync and shouldAdvanceSuccessHeartbeat with explicit .ts extension"
      pattern: "from '\\.\\./_shared/lifecycle\\.ts'"
    - from: "web/tests/lifecycle.test.ts"
      to: "supabase/functions/_shared/lifecycle.ts"
      via: "cross-directory Vitest import (same pattern as web/tests/detect.test.ts)"
      pattern: "supabase/functions/_shared/lifecycle"
---

<objective>
Close verification gaps 2 and 4: poll-tick currently loads only `status = 'open'` jobs, so a stale-closed posting that returns to the board hits the `ignoreDuplicates` upsert's unique `(source, external_id)` conflict and stays closed forever; and `last_success_at` advances only when `succeeded > 0`, so a legitimately empty or fully not-due watchlist becomes falsely stale after 30 minutes. Extract the lifecycle decisions into a pure, Vitest-covered module (the established pure-core/thin-Deno-wrapper pattern from Plans 02-01/02-02), then rewire poll-tick. Also converts the previously behavior-unverified disappearance-grace close transition into automated coverage.

Purpose: restores the "exactly once AND currently open when actually open" trust contract (DISC-03, DISC-05) and truthful scheduler health (DISC-06).
Output: `_shared/lifecycle.ts` pure module, `web/tests/lifecycle.test.ts`, rewired `poll-tick/index.ts`. Deployment of the function happens in Plan 02-07.
</objective>

## Phase Goal

**As a** job seeker, **I want to** receive new job postings from watched career sites and an aggregator exactly once within 5-15 minutes, **so that** I can trust my job feed is current without manually checking each career site.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-VERIFICATION.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md
@supabase/functions/poll-tick/index.ts
@supabase/functions/_shared/adapters/types.ts
</context>

## Artifacts this phase produces (Plan 04 portion)

- **Module:** `supabase/functions/_shared/lifecycle.ts`
- **Types:** `ExistingJobRow` interface (`id`, `source`, `external_id`, `fingerprint`, `status: 'open' | 'closed'`, `last_seen_at`), `CompanySyncPlan` interface (`seenOpenIds: string[]`, `reopenIds: string[]`, `newJobs: NormalizedJob[]`, `closeIds: string[]`)
- **Functions (TS):** `planCompanySync(existing: ExistingJobRow[], returned: NormalizedJob[], nowIso: string, graceMs?: number): CompanySyncPlan` (default `graceMs` 35 * 60_000), `shouldAdvanceSuccessHeartbeat(claimed: number, succeeded: number): boolean`
- **Test file:** `web/tests/lifecycle.test.ts`
- **Behavior change:** `poll-tick` response JSON gains a `reopened` count field alongside `claimed/succeeded/failed/inserted/closed`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure lifecycle module — reopen, close-grace, and heartbeat decisions (RED then GREEN)</name>
  <files>supabase/functions/_shared/lifecycle.ts, web/tests/lifecycle.test.ts</files>
  <read_first>
    - supabase/functions/poll-tick/index.ts (current processCompany logic: exactIds map at lines ~191-193, seen/new classification at lines ~200-214, close SQL at lines ~236-248, heartbeat write at lines ~318-324 — the behavior being extracted)
    - supabase/functions/_shared/adapters/types.ts (NormalizedJob contract: source, externalId fields used for exact matching)
    - supabase/functions/_shared/dedup.ts (pure-module style: no I/O, no Deno globals, plain exports)
    - web/tests/detect.test.ts (cross-directory Vitest import pattern for supabase/functions/_shared modules)
  </read_first>
  <behavior>
    - Test (reopen regression, close-then-return): existing rows contain one CLOSED row with source 'greenhouse', external_id '123', last_seen_at 40 minutes before nowIso; returned contains a NormalizedJob with source 'greenhouse', externalId '123' → planCompanySync puts that row id in reopenIds, NOT in newJobs, NOT in closeIds
    - Test (open exact match): an OPEN existing row whose source/external_id matches a returned job lands in seenOpenIds only
    - Test (genuinely new): a returned job matching no existing source/external_id lands in newJobs untouched
    - Test (disappearance grace closes): an OPEN existing row not present in returned, last_seen_at 36 minutes before nowIso, with returned non-empty → id in closeIds
    - Test (inside grace stays open): same but last_seen_at 10 minutes before nowIso → NOT in closeIds
    - Test (empty poll closes nothing): returned is an empty array → closeIds is empty even when open rows are 36+ minutes unseen (failed/empty polls never close — Phase 02 locked decision)
    - Test (closed rows never in closeIds): a CLOSED existing row absent from returned appears in no output set
    - Test (heartbeat no-work success): shouldAdvanceSuccessHeartbeat(0, 0) === true
    - Test (heartbeat all-claimed-failed stays stale): shouldAdvanceSuccessHeartbeat(3, 0) === false
    - Test (heartbeat partial success): shouldAdvanceSuccessHeartbeat(3, 1) === true
  </behavior>
  <action>
    RED: create web/tests/lifecycle.test.ts with the behaviors above importing planCompanySync and shouldAdvanceSuccessHeartbeat from '../../supabase/functions/_shared/lifecycle.ts' (mirror the relative-import style detect.test.ts uses for _shared/detect). Run and confirm failure (module absent). Commit test.
    GREEN: create supabase/functions/_shared/lifecycle.ts as a pure module (no Deno/network/Supabase imports; only `import { type NormalizedJob } from './adapters/types.ts'`). Implement:
    - planCompanySync(existing, returned, nowIso, graceMs = 35 * 60_000): build a Map keyed `${source}|${external_id}` from existing rows (BOTH statuses — this is the gap fix; the old code only mapped open rows). For each returned job: exact match with status 'open' → seenOpenIds; exact match with status 'closed' → reopenIds; no match → newJobs. closeIds = existing rows where status === 'open', id not in seenOpenIds/reopenIds, and Date.parse(last_seen_at) < Date.parse(nowIso) - graceMs — but ONLY when returned.length > 0; when returned is empty return closeIds: [].
    - shouldAdvanceSuccessHeartbeat(claimed, succeeded): return claimed === 0 || succeeded > 0.
    Run tests green, commit.
  </action>
  <verify>
    <automated>cd web && npx vitest run tests/lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vitest run tests/lifecycle.test.ts` passes with at least 10 tests including one whose name contains "reopen" and one whose name contains "close"
    - `grep -c "export function planCompanySync" supabase/functions/_shared/lifecycle.ts` outputs 1; `grep -c "export function shouldAdvanceSuccessHeartbeat" supabase/functions/_shared/lifecycle.ts` outputs 1
    - `grep -cE "Deno\.|fetch\(|createClient" supabase/functions/_shared/lifecycle.ts | grep -qx 0` (module stays pure)
    - Two commits exist: a `test(02-04)` RED commit and a `feat(02-04)` GREEN commit
  </acceptance_criteria>
  <done>Pure lifecycle module classifies open exact matches, closed exact matches (reopen), new jobs, and grace-expired close candidates, with the no-work heartbeat rule, all Vitest-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire poll-tick to reopen returned closed jobs and report no-work success</name>
  <files>supabase/functions/poll-tick/index.ts</files>
  <read_first>
    - supabase/functions/poll-tick/index.ts (entire file — every edit below is scoped against current line structure)
    - supabase/functions/_shared/lifecycle.ts (the contract just built in Task 1)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-VERIFICATION.md (gaps 2 and 4 exact reasons)
  </read_first>
  <action>
    Modify supabase/functions/poll-tick/index.ts (deployed later by Plan 02-07):
    1. Import `planCompanySync`, `shouldAdvanceSuccessHeartbeat`, and `type ExistingJobRow` from '../_shared/lifecycle.ts'.
    2. In processCompany, change the jobs query to load BOTH statuses: select 'id, source, external_id, fingerprint, status, last_seen_at' for the company with `.in('status', ['open', 'closed'])` (replacing the open-only filter). Type rows as ExistingJobRow[].
    3. Build knownIds (passed to pollGreenhouse to skip detail refetches) from BOTH open and closed rows whose source matches company.ats_type — a returning closed job must not trigger a wasteful detail fetch since its first-sight snapshot is preserved.
    4. Keep the implausible-empty guard keyed on OPEN rows only: throw when openRows.length > 0 && normalizedJobs.length === 0 (openRows = existing.filter(status === 'open')).
    5. Replace the manual exactIds/seenIds/newJobs classification and the cutoff-based close UPDATE with the plan from `planCompanySync(existing, normalizedJobs, seenAt)`:
       - seenOpenIds → existing updateSeenJobs(admin, plan.seenOpenIds, seenAt) unchanged
       - reopenIds → `admin.from('jobs').update({ status: 'open', closed_at: null, last_seen_at: seenAt }).in('id', batch)` batched via the existing batches() helper. Update ONLY those three columns — description_html, description_text, first_seen_at, posted_at, title, fingerprint must not appear in this update payload (first-sight snapshot preservation, per the "Immutable first sight" pattern from 02-02-SUMMARY).
       - plan.newJobs → compute fingerprints exactly as today (fingerprint(normalized.companyName ?? company.name, normalized.title, normalized.location)) and feed ingestNewJobs unchanged. The repost fingerprint map (companyFingerprintIds) must be built from OPEN rows only, preserving current repost-merge semantics.
       - plan.closeIds → replace the `lt('last_seen_at', cutoff)` close UPDATE with `admin.from('jobs').update({ status: 'closed', closed_at: seenAt }).eq('status', 'open').in('id', batch)` batched. Drop the surrounding `existing.length > 0 && normalizedJobs.length > 0` condition — planCompanySync already returns [] for empty polls; keep a code comment stating closure is reachable only after a successful non-empty poll.
    6. CompanyResult gains `reopened: number`; processCompany returns it; the serve handler sums it and includes `reopened` in the response JSON next to inserted/closed.
    7. Heartbeat (gap 4): replace the `if (succeeded > 0)` guard around the last_success_at update with `if (shouldAdvanceSuccessHeartbeat(companies.length, succeeded))` — a tick that claims zero companies is a healthy no-work tick and advances last_success_at; a tick where companies were claimed and every one failed does NOT advance it.
  </action>
  <verify>
    <automated>cd web && npx vitest run && npm run build && grep -c "from '../_shared/lifecycle.ts'" ../supabase/functions/poll-tick/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shouldAdvanceSuccessHeartbeat(companies.length, succeeded)" supabase/functions/poll-tick/index.ts` outputs 1
    - `grep -c "planCompanySync" supabase/functions/poll-tick/index.ts` outputs at least 2 (import + call)
    - `grep -cF ".in('status', ['open', 'closed'])" supabase/functions/poll-tick/index.ts` outputs 1
    - `grep -cF "closed_at: null" supabase/functions/poll-tick/index.ts` outputs 1 (reopen payload) and that update's payload contains no description_html key: `grep -n "closed_at: null" -A 2 supabase/functions/poll-tick/index.ts` shows only status/closed_at/last_seen_at
    - `grep -c "reopened" supabase/functions/poll-tick/index.ts` outputs at least 3 (CompanyResult field, accumulation, response)
    - Full web suite (`cd web && npx vitest run`) passes and `npm run build` succeeds
  </acceptance_criteria>
  <done>poll-tick resolves exact source/external-ID matches across open and closed rows, reopens returned closed matches preserving the first-sight snapshot, closes only pure-computed grace-expired candidates after successful non-empty polls, and advances last_success_at on successful no-work ticks while all-claimed-failed ticks stay stale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → poll-tick | x-cron-secret gated scheduler entry (unchanged by this plan) |
| ATS responses → jobs table | Third-party data crosses into service-role writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Tampering | reopen update in poll-tick | high | mitigate | Reopen payload restricted to status/closed_at/last_seen_at columns; acceptance grep proves no snapshot columns in the payload; lifecycle tests prove closed rows never classify as new inserts |
| T-02-04-02 | Denial of Service | all-status jobs load per company | low | accept | 2-user scale with 30-day prune policy (CLAUDE.md); closed-row volume stays small; revisit if company job counts grow past thousands |
| T-02-04-03 | Repudiation | false heartbeat state | medium | mitigate | shouldAdvanceSuccessHeartbeat unit tests pin all three transitions (no-work true, all-failed false, partial true) so monitoring never lies in either direction |
| T-02-04-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed by this plan; imports are existing local modules only |
</threat_model>

<verification>
- `cd web && npx vitest run` — full suite green including new tests/lifecycle.test.ts
- `cd web && npm run build` — type-check and production build pass
- Acceptance greps in both tasks confirm the wiring (lifecycle import, all-status load, restricted reopen payload, heartbeat guard)
</verification>

<success_criteria>
- Gap 2 closed at source: a returned closed exact-ID match is reopened, snapshot untouched, with a named close-then-return regression test
- Gap 4 closed at source: successful no-work ticks advance last_success_at; claimed-and-all-failed ticks do not; both transitions tested
- Behavior-unverified item "disappearance grace closes / failed poll never closes" now has automated coverage
- Hosted behavior lands via Plan 02-07 deployment and probes 14-15
</success_criteria>

<output>
Create `.planning/phases/02-watchlist-ingestion-monitoring/02-04-SUMMARY.md` when done.
</output>
