---
phase: 02-watchlist-ingestion-monitoring
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_watchlist.sql
  - supabase/functions/_shared/detect.ts
  - web/tests/detect.test.ts
  - supabase/functions/verify-board/index.ts
  - web/src/lib/watchlist.ts
  - web/src/lib/watchlist.test.ts
  - web/src/pages/Watchlist.tsx
  - scripts/verify-watchlist.ts
autonomous: true
requirements: [PREF-02, PREF-03, PREF-04]

must_haves:
  truths:
    - "User can paste a Greenhouse/Lever/Ashby careers URL and the company appears in the watchlist table with its detected ATS shown (D-03)"
    - "Unsupported or unverifiable URLs are rejected with teaching guidance and no row is saved (D-04)"
    - "Either user can see and remove any watched company, including ones the other added (D-01, D-02)"
    - "Each watchlist row shows an OK/Failing/Stale health badge with last-success time on hover (D-05, D-06)"
  artifacts:
    - path: "supabase/migrations/0005_watchlist.sql"
      provides: "companies table with health columns, shared-table RLS (using (true)), unique (ats_type, board_token)"
      contains: "create table public.companies"
    - path: "supabase/functions/_shared/detect.ts"
      provides: "Pure ATS URL detection + endpoint construction (SSRF-audited single constructor)"
      exports: ["detectAts", "buildEndpoint"]
    - path: "supabase/functions/verify-board/index.ts"
      provides: "Live board verification edge function (D-03)"
    - path: "web/src/lib/watchlist.ts"
      provides: "Watchlist data-access layer"
      exports: ["listCompanies", "addCompany", "removeCompany", "deriveHealth"]
    - path: "web/src/pages/Watchlist.tsx"
      provides: "Watchlist page: add form, dense table, health badges, remove confirm"
      min_lines: 100
  key_links:
    - from: "web/src/pages/Watchlist.tsx"
      to: "web/src/lib/watchlist.ts"
      via: "imports listCompanies/addCompany/removeCompany, TanStack Query key ['watchlist']"
      pattern: "from '../lib/watchlist'"
    - from: "web/src/lib/watchlist.ts"
      to: "supabase/functions/verify-board/index.ts"
      via: "supabase.functions.invoke('verify-board') before insert (D-03 verify-then-save)"
      pattern: "functions\\.invoke\\('verify-board'"
    - from: "supabase/functions/verify-board/index.ts"
      to: "supabase/functions/_shared/detect.ts"
      via: "imports detectAts/buildEndpoint so endpoint construction stays in one audited module"
      pattern: "from ['\"]\\.\\./_shared/detect"
---

<objective>
Watchlist management vertical slice: a user pastes a careers URL, the app auto-detects the ATS (Greenhouse/Lever/Ashby), verifies the board with one live API call, saves it to the shared global watchlist, and shows it in a dense table with per-company health badges and removal. Implements D-01, D-02, D-03, D-04, D-05 (badge UI), D-06 (badge derivation).

Purpose: PREF-02/PREF-03 are the entry point of the whole discovery engine — without watched companies, nothing downstream exists. PREF-04's badge surface lands here so Plan 02's health data has a visible home.
Output: migration 0005 (pushed), `verify-board` edge function (deployed), detect module + tests, watchlist lib + page, hosted verification script.
</objective>

## Phase Goal

**As an** invited job seeker, **I want to** have new postings from 100+ watched career sites plus an aggregator land in the system exactly once within 5–15 minutes, **so that** I can trust per-company health and pipeline liveness at a glance and never miss a fresh posting.

(Derived directly from the ROADMAP Phase 2 Goal line, same transformation the Phase 1 plans used.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-CONTEXT.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-PATTERNS.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 01 portion)

- **Tables:** `public.companies` (columns: `id`, `name`, `ats_type`, `board_token`, `region`, `last_polled_at`, `last_success_at`, `consecutive_failures`, `last_error`, `created_at`; unique `(ats_type, board_token)`)
- **Migration:** `supabase/migrations/0005_watchlist.sql`
- **Types:** `DetectResult` (discriminated union), `CompanyRecord`, `HealthStatus` (`'ok' | 'failing' | 'stale'`), `VerifyBoardResponse`
- **Functions (TS):** `detectAts(url: string): DetectResult`, `buildEndpoint(d: DetectResult): string`, `listCompanies()`, `addCompany(url: string)`, `removeCompany(id: string)`, `deriveHealth(company: CompanyRecord, now?: Date): HealthStatus`
- **Edge function:** `verify-board` (POST `{ url }` → `{ ok: true, ats, board_token, region, company_name, job_count }` or `{ ok: false, reason: 'unsupported' | 'not_found' | 'error', message }`)
- **Components:** rewritten `Watchlist` page component; inline `HealthBadge` render helper (local to Watchlist.tsx)
- **Scripts:** `scripts/verify-watchlist.ts`
- **Constants:** `COMPANY_COLUMNS` column-list string in `web/src/lib/watchlist.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ATS URL detection module (pure, Vitest-covered)</name>
  <files>supabase/functions/_shared/detect.ts, web/tests/detect.test.ts</files>
  <read_first>
    - web/src/auth/recovery.ts (discriminated-union + URL-classification pattern to mirror — PATTERNS.md names it the analog)
    - web/tests/verify-rls.test.ts (cross-directory import pattern from web/tests/)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md (Pattern 2: detection table, slug rules, SSRF constraints)
  </read_first>
  <behavior>
    - Test: `detectAts('https://boards.greenhouse.io/stripe')` → `{ ats: 'greenhouse', slug: 'stripe' }`
    - Test: `detectAts('https://job-boards.greenhouse.io/acme')` → greenhouse (new host variant, RESEARCH "State of the Art")
    - Test: `detectAts('https://greenhouse.io/embed/job_board?for=acme')` and www-prefixed hosts → greenhouse with slug 'acme'
    - Test: `detectAts('https://jobs.lever.co/palantir')` → `{ ats: 'lever', slug: 'palantir' }` (no region)
    - Test: `detectAts('https://jobs.eu.lever.co/foo')` → `{ ats: 'lever', slug: 'foo', region: 'eu' }`
    - Test: `detectAts('https://jobs.ashbyhq.com/ramp')` → `{ ats: 'ashby', slug: 'ramp' }`; an Ashby path segment containing spaces is accepted as one segment and buildEndpoint URL-encodes it
    - Test: Workday URL, a company's own careers domain, and a deep Greenhouse job URL with extra path segments beyond the board token resolve to `{ ats: 'unsupported' }` (fall-through default)
    - Test: Greenhouse/Lever slugs violating `^[A-Za-z0-9_-]+$` → `{ ats: 'unsupported' }`
    - Test: `buildEndpoint` returns exactly `https://boards-api.greenhouse.io/v1/boards/{slug}/jobs`, `https://api.lever.co/v0/postings/{slug}?mode=json`, `https://api.eu.lever.co/v0/postings/{slug}?mode=json`, `https://api.ashbyhq.com/posting-api/job-board/{encoded}` per detected ATS
    - Test: module performs no I/O — invalid input never throws, always returns the unsupported variant
  </behavior>
  <action>
    Write failing tests first in web/tests/detect.test.ts importing from '../../supabase/functions/_shared/detect' (RED), then implement supabase/functions/_shared/detect.ts (GREEN). Export `type DetectResult = { ats: 'greenhouse' | 'lever' | 'ashby'; slug: string; region?: 'eu' } | { ats: 'unsupported' }` plus `detectAts(url: string): DetectResult` and `buildEndpoint(d: DetectResult): string` (throws on 'unsupported'). Parse with `new URL()` (never regex the raw href); use a module-level host-pattern table; validate slugs with `^[A-Za-z0-9_-]+$` for greenhouse/lever, single-path-segment rule for ashby. This module is the single audited endpoint constructor (SSRF control per RESEARCH Security Domain V5): the pasted URL is only ever parsed, never fetched. Pure TS, zero Deno APIs and zero npm: specifiers so Vitest in web/ can import it. Code style: no semicolons, single quotes, named exports, 2-space indent (project convention).
  </action>
  <verify>
    <automated>cd web && npx vitest run ../web/tests/detect.test.ts 2>/dev/null || npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` passes with all detect.test.ts cases green
    - `grep -c "export function detectAts" supabase/functions/_shared/detect.ts` returns 1
    - `grep -c "new URL(" supabase/functions/_shared/detect.ts` returns >= 1
    - `grep -rn "fetch(" supabase/functions/_shared/detect.ts` returns no matches (pure module, no I/O)
  </acceptance_criteria>
  <done>detectAts/buildEndpoint cover all six host patterns from RESEARCH Pattern 2, reject everything else, and are fully unit-tested from web/tests/.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migration 0005 (shared companies table) + supabase db push + verify-board edge function deploy</name>
  <files>supabase/migrations/0005_watchlist.sql, supabase/functions/verify-board/index.ts</files>
  <read_first>
    - supabase/migrations/0002_resumes.sql (create → enable RLS → index → grant → named per-operation policies ordering; naming conventions)
    - supabase/migrations/0004_delete_my_data.sql (function security posture reference; confirm Phase 2 tables are NOT added to it — shared, not user-owned)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-RESEARCH.md (Pitfall 8 shared-RLS shape; Pattern 2 verification-call behavior; Pitfall 3 error shapes)
    - supabase/functions/_shared/detect.ts (import surface from Task 1)
  </read_first>
  <action>
    Migration `supabase/migrations/0005_watchlist.sql`: create `public.companies` with columns `id uuid primary key default gen_random_uuid()`, `name text not null`, `ats_type text not null` constrained to ('greenhouse','lever','ashby'), `board_token text not null`, `region text` (null or 'eu'), `last_polled_at timestamptz`, `last_success_at timestamptz`, `consecutive_failures integer not null default 0`, `last_error text`, `created_at timestamptz not null default now()`, plus `unique (ats_type, board_token)` (duplicate-add guard) and index `companies_last_polled_at_idx` (due-queue support for Plan 02). Enable RLS; `grant select, insert, update, delete to authenticated`; per-operation policies named `companies_{op}_shared` with `using (true)` / `with check (true)` — this is the deliberate D-01/D-02 shared-watchlist exception to AUTH-03's per-user isolation; say so in a SQL comment so the security reviewer has the rationale. Zero grants to anon. Per PATTERNS.md note on 0004: `delete_my_data()` intentionally unchanged — companies is shared, not user-owned (state this in the migration comment too, no `added_by` column per D-02).

    [BLOCKING] Push the schema before anything consumes it: run `npx supabase db push` from web/ (the Phase 1 invocation that succeeded — supabase CLI is a web devDependency and the project is already linked). If the command prompts interactively and cannot be suppressed, set SUPABASE_ACCESS_TOKEN and retry; confirm with `npx supabase migration list --linked` showing 0005 applied. Do not proceed to the edge function until the push succeeds.

    Edge function `supabase/functions/verify-board/index.ts` (first Deno function in repo — no analog; use RESEARCH Pattern 2): `Deno.serve` handler accepting POST `{ url: string }` with the caller's user JWT (keep default verify_jwt so only signed-in users can invoke — RESEARCH threat "unauthenticated function invocation"). Flow per D-03: `detectAts(url)`; if unsupported → respond 200 `{ ok: false, reason: 'unsupported', message }` where message is the exact D-04 copy from 02-UI-SPEC.md ("This URL isn't a supported job board. Job Copilot works with Greenhouse, Lever, and Ashby. …"). Otherwise fetch `buildEndpoint(result)` — the constructed allowlisted URL only, never the pasted URL. Check `res.ok` BEFORE parsing (Pitfall 3: Ashby 404 is plain text, do not call res.json() first): 404 → `{ ok: false, reason: 'not_found', message }` with the UI-SPEC verification-failed copy; other failures → `{ ok: false, reason: 'error', message }`. On 200, parse per ATS (greenhouse `{jobs: [...]}`; lever bare array; ashby `{jobs: [...]}` filtered to `isListed === true`) and respond `{ ok: true, ats, board_token, region, company_name, job_count }` — company_name from greenhouse `jobs[0].company_name` when present, else the slug. Import detect via relative `../_shared/detect.ts`. No npm: imports needed here.

    Deploy: `npx supabase functions deploy verify-board` from web/ (bundles remotely; Docker not available locally per RESEARCH Environment Availability).
  </action>
  <verify>
    <automated>cd web && npx supabase migration list --linked && curl -s -o /dev/null -w "%{http_code}" -X POST "$(grep SUPABASE_URL ../scripts/.env | cut -d= -f2)/functions/v1/verify-board" -H "Content-Type: application/json" -d '{"url":"https://example.com"}'</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase migration list --linked` (from web/) lists 0005 as applied remotely
    - `grep -c "using (true)" supabase/migrations/0005_watchlist.sql` returns >= 4 (one per operation policy)
    - `grep -c "to authenticated" supabase/migrations/0005_watchlist.sql` returns >= 5 (grant + four policies scoped to authenticated only; anon access is asserted behaviorally by Task 3's probe 5)
    - Unauthenticated curl POST to the deployed verify-board URL returns 401 (JWT required)
    - `grep -c "res.ok" supabase/functions/verify-board/index.ts` returns >= 1 (status checked before body parse)
  </acceptance_criteria>
  <done>companies table live on the hosted project with shared RLS; verify-board deployed and rejecting anonymous calls; endpoint construction flows only through detect.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Watchlist lib + page (add / table / badges / remove) + hosted verification script</name>
  <files>web/src/lib/watchlist.ts, web/src/lib/watchlist.test.ts, web/src/pages/Watchlist.tsx, scripts/verify-watchlist.ts</files>
  <read_first>
    - web/src/lib/resumes.ts (data-access layer to copy exactly: column-list constant, Row interface, validation-before-network, {data,error} throw pattern)
    - web/src/lib/resumes.test.ts (vi.mock at module scope, "rejects before network" assertions, fixture rows)
    - web/src/pages/Resumes.tsx (D-15 page skeleton: form, dense table, alert paragraph, mutation/invalidation, ConfirmDialog wiring)
    - web/src/components/ConfirmDialog.tsx (props contract — reuse as-is)
    - web/src/pages/Watchlist.tsx (current 8-line stub being replaced)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-UI-SPEC.md (exact copy strings, columns, badge spec, status palette — the UI contract; follow verbatim)
    - scripts/verify-rls.ts (env validation, probe-client, PASS/FAIL accumulation, main-module guard patterns; reuse its env var names for the two user accounts)
  </read_first>
  <action>
    `web/src/lib/watchlist.ts` in the resumes.ts shape: `COMPANY_COLUMNS` constant listing all companies columns; `CompanyRecord` interface (snake_case mirroring DB); `listCompanies()` ordered by created_at desc; `addCompany(url: string)` — client-side `detect`-style precheck is NOT duplicated here; instead call `supabase.functions.invoke('verify-board', { body: { url } })` (carries the user JWT), throw `Error` with the returned message when `ok` is false (D-04 guidance surfaces through the standard errorMessage channel), then `.insert({ name, ats_type, board_token, region }).select(COMPANY_COLUMNS).single()` on success (D-03 verify-then-save); map the Postgres unique-violation (code 23505) to the UI-SPEC duplicate message "{Company} is already on the watchlist."; `removeCompany(id: string)` delete by id; `type HealthStatus = 'ok' | 'failing' | 'stale'` and pure `deriveHealth(company, now = new Date())` per D-06/RESEARCH Pattern 6: 'failing' when `consecutive_failures >= 3`; 'stale' when last_success_at is null-or-older-than-30-minutes without 3 failures; else 'ok'. Unit tests in watchlist.test.ts (resumes.test.ts style): deriveHealth boundary cases (2 vs 3 failures per D-06; 29 vs 31 minutes; never-succeeded), addCompany throws the verify-board message without inserting when ok=false.

    `web/src/pages/Watchlist.tsx` replaces the stub, mirroring Resumes.tsx structure and following 02-UI-SPEC.md verbatim: page title "Watchlist", intro "Companies polled every few minutes for new postings. Shared between both users."; add form with `type="url"` input (label "Careers page URL", placeholder "https://boards.greenhouse.io/company") and primary "Add company" button (pending: "Verifying…", `disabled:cursor-wait disabled:opacity-60`); inline `role="alert"` rejection under the form, input keeps its value on rejection; `mt-8` table card with loading ("Loading watchlist…"), error ("Unable to load the watchlist: {message}") and empty state ("No companies watched yet" heading + body copy from spec). Columns: Company (font-medium, truncate max-w-sm), Source (capitalized ATS name, muted), Health (badge), Added (Intl.DateTimeFormat dateStyle 'medium'), Actions (right-aligned bordered destructive "Remove"). Health badge (D-05): inline pill `inline-flex items-center gap-1.5 rounded-full border px-2 py-0.5 text-xs font-medium` with 6px solid dot + text label, status palette classes exactly from UI-SPEC Color section (emerald/red/amber pairs with dark: variants), `title` attribute "Last successful poll: {relative time}" or "No successful poll yet". Removal uses ConfirmDialog with title "Remove {company}?", message "Polling stops immediately. Jobs already captured stay in the system.", confirm label "Remove company". TanStack Query: queryKey ['watchlist'], `refetchInterval: 60_000` so badges track reality (UI-SPEC Interaction States); mutations invalidate on success, `mutation.reset()` before re-trigger.

    `scripts/verify-watchlist.ts` in the verify-rls.ts shape (env validation, isolated probe clients with persistSession:false, PASS/FAIL accumulation, cleanup in finally, main-module guard; run via `node --env-file=scripts/.env scripts/verify-watchlist.ts`): (1) as user A, invoke verify-board with an unsupported URL → expect ok:false reason 'unsupported'; (2) with a misspelled board (e.g. boards.greenhouse.io/definitely-not-a-real-board-xyz) → ok:false 'not_found'; (3) with https://boards.greenhouse.io/stripe → ok:true ats greenhouse; (4) insert the verified company as A, sign in as user B, assert B lists it (D-01) and B can delete it (D-02); (5) anon/publishable client without session selects companies → expect zero rows or error (no anon access). Finally, seed the watchlist with the three RESEARCH-verified boards through the same verify-then-insert flow so Plan 02's poller has real data on day one: Stripe (greenhouse), Palantir (lever), Ramp (ashby) — idempotent (skip when the unique key already exists).
  </action>
  <verify>
    <automated>cd web && npm test && npm run build && node --env-file=../scripts/.env ../scripts/verify-watchlist.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm test` green including watchlist.test.ts deriveHealth boundaries (2 vs 3 failures, D-06)
    - `cd web && npm run build` succeeds (tsc -b + vite)
    - `node --env-file=scripts/.env scripts/verify-watchlist.ts` prints PASS for all 5 probes and exits 0
    - After the script, `companies` contains rows for stripe (greenhouse), palantir (lever), ramp (ashby)
    - `grep -c "functions.invoke('verify-board'" web/src/lib/watchlist.ts` returns 1
    - Watchlist.tsx contains the exact D-04 empty-state and rejection copy from 02-UI-SPEC.md (grep for "Works with Greenhouse, Lever, and Ashby")
  </acceptance_criteria>
  <done>A user can paste a careers URL, get it verified live, see the company in the dense table with a health badge, and remove it with confirmation; both accounts share the list; three real boards are seeded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → verify-board | User-supplied URL string crosses into server-side code |
| verify-board → external ATS APIs | Outbound fetch constructed from user input |
| Browser → Postgres (companies) | Direct table writes under RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering (SSRF) | verify-board / detect.ts | high | mitigate | Never fetch the pasted URL; parse with `new URL()`, allowlist exact API hosts (boards-api.greenhouse.io, api.lever.co, api.eu.lever.co, api.ashbyhq.com), slug regex `^[A-Za-z0-9_-]+$`, single audited constructor `buildEndpoint()` |
| T-02-02 | Elevation | verify-board invocation | medium | mitigate | Keep default verify_jwt: only authenticated users can invoke; unauthenticated curl must return 401 (asserted in Task 2) |
| T-02-03 | Information disclosure | companies shared table | low | accept | Cross-user visibility is the deliberate D-01/D-02 design; documented in migration comment; anon has zero grants |
| T-02-04 | Denial of service | verify-board outbound fetch | low | accept | 2 invited users; one fetch per add attempt; ATS APIs are public unauthenticated endpoints |
| T-02-SC | Tampering | npm/pip/cargo installs | low | mitigate | No package-manager installs in this plan (edge code uses zero external imports; web/ deps unchanged). RESEARCH Package Legitimacy Audit: `he` OK (used in Plan 02), `@supabase/supabase-js` pinned at already-vetted 2.110.7 — no version bump permitted |
</threat_model>

<verification>
- `cd web && npm test` — detect + watchlist unit suites green
- `cd web && npm run build` — type-check + bundle clean
- `cd web && npx supabase migration list --linked` — 0005 applied
- `node --env-file=scripts/.env scripts/verify-watchlist.ts` — hosted add/reject/shared-visibility/remove probes PASS; three real boards seeded
</verification>

<success_criteria>
- Roadmap success criterion 1 fully satisfied: add/edit/remove companies by pasting career-site URLs with ATS auto-detection and stored polling identity (ats_type + board_token + region, per RESEARCH Pattern 2 — endpoint constructed, not stored raw)
- D-04 rejections teach (exact UI-SPEC copy), and nothing is saved on rejection
- Health badges render for all three states with hover last-success (data arrives in Plan 02; until then rows show the never-succeeded state)
</success_criteria>

<output>
Create `.planning/phases/02-watchlist-ingestion-monitoring/02-01-SUMMARY.md` when done.
</output>
