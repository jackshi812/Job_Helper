---
phase: 02-watchlist-ingestion-monitoring
plan: 5
type: execute
wave: 1
depends_on: [02-02]
files_modified:
  - supabase/migrations/0008_claim_exclusive.sql
  - scripts/verify-watchlist.ts
  - scripts/verify-pipeline.ts
autonomous: true
gap_closure: true
requirements: [DISC-01, DISC-03, PREF-02]
must_haves:
  truths:
    - "Concurrent poll ticks claim disjoint due-company batches and preserve the 5-15 minute cadence (gap: non-exclusive claim)"
    - "Hosted verification can be rerun without corrupting watched-company identity or exactly-once job state (gap: destructive hosted verifier)"
    - "Production seed companies (Stripe, Palantir, Ramp) and their job foreign keys are provably unchanged after a verifier run"
  artifacts:
    - path: "supabase/migrations/0008_claim_exclusive.sql"
      provides: "claim_due_companies replaced with a locking CTE using FOR UPDATE SKIP LOCKED; service-role-only grants re-issued"
      contains: "for update skip locked"
    - path: "scripts/verify-watchlist.ts"
      provides: "Cross-user RLS probes against a dedicated disposable probe row inserted by the current invocation, plus a seed-integrity regression check"
    - path: "scripts/verify-pipeline.ts"
      provides: "Probes 13-16: concurrent-claim disjointness, no-work heartbeat advance, close-then-return reopen, discovery health surface"
  key_links:
    - from: "scripts/verify-pipeline.ts"
      to: "supabase/migrations/0008_claim_exclusive.sql"
      via: "admin.rpc('claim_due_companies') invoked concurrently, asserting disjoint returned IDs"
      pattern: "claim_due_companies"
    - from: "scripts/verify-watchlist.ts"
      to: "supabase/migrations/0005_watchlist.sql"
      via: "disposable probe row honoring unique (ats_type, board_token) with an invocation-unique board_token"
      pattern: "probe-"
---

<objective>
Close verification gaps 3 and 1. Gap 3: `claim_due_companies` (migration 0006, lines 55-71) updates rows selected by an uncorrelated IN subquery with no `FOR UPDATE SKIP LOCKED`, so two overlapping pg_cron ticks can claim and poll the same companies — a new migration 0008 replaces it with a locking CTE (0006 is already applied to the hosted DB and must not be edited). Gap 1: `scripts/verify-watchlist.ts` (lines 124-168) selects the pre-existing Stripe row as its cross-user deletion target, deleting a production seed and orphaning its jobs (`company_id` nulls via `on delete set null`) — it must operate only on a disposable probe row it inserted itself and prove seeds are untouched. This plan also extends `scripts/verify-pipeline.ts` with hosted probes for the fixes shipping in Plans 02-04 and 02-06 (probes run in Plan 02-07 after deployment).

Purpose: exclusive claims protect the 5-15 minute cadence at 100+ companies (DISC-01); non-destructive verification makes the exactly-once trust contract (DISC-03) and shared-watchlist proof (PREF-02) rerunnable.
Output: migration 0008 (pushed by Plan 02-07), reworked verify-watchlist.ts, verify-pipeline.ts probes 13-16.
</objective>

## Phase Goal

**As a** job seeker, **I want to** receive new job postings from watched career sites and an aggregator exactly once within 5-15 minutes, **so that** I can trust my job feed is current without manually checking each career site.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-VERIFICATION.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-01-SUMMARY.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md
@supabase/migrations/0006_jobs_pipeline.sql
@scripts/verify-watchlist.ts
@scripts/verify-pipeline.ts
</context>

## Artifacts this phase produces (Plan 05 portion)

- **Migration:** `supabase/migrations/0008_claim_exclusive.sql` (NEW file — never edits applied migration 0006)
- **SQL functions:** `public.claim_due_companies(batch_size integer default 10)` redefined via `create or replace` with a `with due as (... for update skip locked)` CTE; `revoke execute ... from public, anon, authenticated` and `grant execute ... to service_role` re-issued
- **Scripts:** `scripts/verify-watchlist.ts` (disposable-probe rework + seed-integrity regression), `scripts/verify-pipeline.ts` (probes 13-16)
- **Probe contract:** probe 13 concurrent-claim disjointness; probe 14 no-work heartbeat advance; probe 15 close-then-return reopen; probe 16 discovery health field surface

<tasks>

<task type="auto">
  <name>Task 1: Migration 0008 — exclusive due-company claims with FOR UPDATE SKIP LOCKED</name>
  <files>supabase/migrations/0008_claim_exclusive.sql</files>
  <read_first>
    - supabase/migrations/0006_jobs_pipeline.sql (current claim_due_companies definition lines 55-75: signature, security invoker, search_path, grants — all must be preserved verbatim except the body)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-VERIFICATION.md (gap 3 reason)
  </read_first>
  <action>
    Create supabase/migrations/0008_claim_exclusive.sql (do NOT touch 0006 — it is applied to the hosted DB). Redefine the function with identical signature and attributes (`create or replace function public.claim_due_companies(batch_size integer default 10) returns setof public.companies language sql security invoker set search_path = ''`) but replace the uncorrelated `where id in (select ...)` body with a locking CTE:
    - CTE `due`: select id from public.companies where last_polled_at is null or last_polled_at < now() - interval '9 minutes', order by last_polled_at asc nulls first, limit batch_size, `for update skip locked`
    - Main statement: `update public.companies c set last_polled_at = now() from due where c.id = due.id returning c.*`
    Re-issue the exact grant posture from 0006 after the function definition: revoke execute from public, anon, authenticated; grant execute to service_role. Add a header comment explaining why: concurrent pg_cron ticks previously selected overlapping batches because plain subquery selection takes no row locks; SKIP LOCKED makes overlapping invocations claim disjoint sets without blocking, preserving the 9-minute due interval and per-minute cadence.
  </action>
  <verify>
    <automated>grep -ci "for update skip locked" supabase/migrations/0008_claim_exclusive.sql && grep -c "create or replace function public.claim_due_companies" supabase/migrations/0008_claim_exclusive.sql && grep -c "grant execute on function public.claim_due_companies(integer) to service_role" supabase/migrations/0008_claim_exclusive.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "for update skip locked" supabase/migrations/0008_claim_exclusive.sql` outputs at least 1
    - `grep -c "security invoker" supabase/migrations/0008_claim_exclusive.sql` outputs 1 and `grep -c "set search_path = ''" supabase/migrations/0008_claim_exclusive.sql` outputs 1
    - Both revoke and grant statements for claim_due_companies(integer) are present
    - `git diff --name-only` shows supabase/migrations/0006_jobs_pipeline.sql is NOT modified
  </acceptance_criteria>
  <done>A new migration redefines claim_due_companies so overlapping invocations lock and skip each other's rows, returning disjoint company batches, with the service-role-only grant posture preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Non-destructive verify-watchlist — disposable probe row + seed-integrity regression</name>
  <files>scripts/verify-watchlist.ts</files>
  <read_first>
    - scripts/verify-watchlist.ts (entire file — the lines ~124-168 destructive block being replaced, the finally-cleanup at ~175-180, and the idempotent seeding loop at ~187-212 which stays unchanged)
    - supabase/migrations/0005_watchlist.sql (companies has unique (ats_type, board_token) — the probe row needs an invocation-unique board_token; last_polled_at column exists and is claimable when null)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-VERIFICATION.md (gap 1 missing items)
  </read_first>
  <action>
    Rework scripts/verify-watchlist.ts so the cross-user probes never touch pre-existing rows:
    1. BEFORE the probes, capture a seed-integrity baseline with clientA: select id, ats_type, board_token from companies for the three seed identities (greenhouse/stripe, lever/palantir, ashby/ramp), and count jobs rows... jobs are not readable? jobs grants select to authenticated (migration 0006), so clientA CAN read them. Record `{ count } = await clientA.from('jobs').select('*', { count: 'exact', head: true }).in('company_id', seedCompanyIds)` as jobsBefore. If a seed identity is missing at baseline, skip it in the baseline (first-ever run) rather than failing.
    2. Replace the reuse-Stripe block (old lines 124-168): ALWAYS insert a fresh disposable probe row via clientA: `{ name: 'RLS Probe (disposable)', ats_type: 'greenhouse', board_token: `probe-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`, region: null, last_polled_at: new Date().toISOString() }` — the unique board_token avoids the (ats_type, board_token) constraint, and setting last_polled_at to now keeps the row out of the poll-tick due queue for the 9-minute window so cron never polls the fake board while it exists. Track the returned id in a variable set ONLY from this insert (never from a select of pre-existing rows). Probe 3 (Stripe verifies via verify-board) remains read-only — keep the verifyBoard call but remove any dependency of the probe row on it.
    3. Probes 4a (User B reads it) and 4b (User B deletes it) target ONLY the tracked disposable id. The finally-block cleanup likewise deletes only that id when still set.
    4. AFTER the probes (before the seeding loop), add the regression probe: re-select the three seed identities and assert (a) every seed id captured in the baseline still exists with the same id, and (b) the jobs count for those company ids is >= jobsBefore (poll-tick may legitimately insert during the run; it must never shrink or orphan). Label it "probe 6: seed companies and their job links are unchanged by verification". Fail the run when violated.
    5. Keep the idempotent seeding loop and final summary; update the closing summary line count accordingly.
  </action>
  <verify>
    <automated>node --check scripts/verify-watchlist.ts && grep -c "probe 6" scripts/verify-watchlist.ts
  </verify>
  <acceptance_criteria>
    - `grep -c "probe-" scripts/verify-watchlist.ts` outputs at least 1 (invocation-unique disposable board_token)
    - `grep -c "seed companies and their job links are unchanged" scripts/verify-watchlist.ts` outputs 1
    - `grep -cF ".eq('board_token', stripe.board_token)" scripts/verify-watchlist.ts | grep -qx 0` — no probe-path lookup selects the Stripe identity as a mutation target (the seeding loop's maybeSingle lookup by verified identity is read-then-skip and may remain; the deletion path must reference only the tracked disposable id)
    - The deletion probe and finally-cleanup reference a variable assigned exclusively from the disposable insert's returned id
    - Script is not executed in this task (hosted run happens in Plan 02-07); a syntax check (node --check or tsc --noEmit) passes
  </acceptance_criteria>
  <done>verify-watchlist.ts creates and deletes only a dedicated invocation-tracked probe row, proves seed rows and their job foreign keys survived, and remains safely rerunnable against the hosted project.</done>
</task>

<task type="auto">
  <name>Task 3: verify-pipeline probes 13-16 — concurrent claims, no-work heartbeat, reopen, discovery health</name>
  <files>scripts/verify-pipeline.ts</files>
  <read_first>
    - scripts/verify-pipeline.ts (entire file — client construction, assert helper, postTick/postDiscoverySweep helpers, probe numbering through 12, finally-cleanup)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-04-PLAN.md (poll-tick contract this probes: reopened response field, no-work last_success_at advance)
    - .planning/phases/02-watchlist-ingestion-monitoring/02-06-PLAN.md (discovery-sweep contract: discoveryStatus response field, pipeline_heartbeat.last_discovery_at / discovery_status columns)
  </read_first>
  <action>
    Append four probes to runPipelineVerification() after probe 12 (these run hosted in Plan 02-07, after migrations 0008/0009 are pushed and functions redeployed):
    - Probe 13 (gap 3, concurrent-claim disjointness): set last_polled_at = null on all three seed company ids via admin, then fire four concurrent `admin.rpc('claim_due_companies', { batch_size: 2 })` calls with Promise.all. Collect returned company ids per call; assert no id appears in more than one call's result set and the union size equals the total returned row count. Label: "probe 13: concurrent claim_due_companies calls return disjoint company batches". (The secret-key client executes as service_role, which holds the execute grant.)
    - Probe 14 (gap 4, no-work heartbeat): set last_polled_at = new Date().toISOString() on ALL companies rows via admin (nothing due), read pipeline_heartbeat.last_success_at as baseline, call postTick with the cron secret, assert response.ok, response JSON claimed === 0, and re-read last_success_at strictly greater than baseline (treat a null baseline as advanced when the new value is non-null). Label: "probe 14: a successful no-work tick advances last_success_at".
    - Probe 15 (gap 2, close-then-return reopen): pick one open job row belonging to a seed company via admin (id, description_html, first_seen_at); update it to status 'closed', closed_at now, last_seen_at 40 minutes ago; set its company's last_polled_at = null; call postTick with the cron secret; re-read the row and assert status === 'open', closed_at is null, and description_html plus first_seen_at are byte-identical to the captured values. Skip with an explicit console WARN (not a failure) if no seed company currently has open jobs. Label: "probe 15: a returned closed exact-ID job reopens with its first-sight snapshot preserved".
    - Probe 16 (gap 5 surface): call postDiscoverySweep with the cron secret; parse JSON; assert the body contains a discoveryStatus field whose value is 'ok', 'degraded', or 'failed' (or the body is a budget/credentials skip), and read pipeline_heartbeat columns last_discovery_at + discovery_status via admin asserting last_discovery_at is non-null after a non-skip sweep. Label: "probe 16: discovery sweep surfaces health state in its response and heartbeat row".
    After probe 14/15 manipulation, restore normal scheduling by leaving last_polled_at as the probes set it (poll-tick re-polls within 9 minutes naturally). Do not renumber existing probes.
  </action>
  <verify>
    <automated>node --check scripts/verify-pipeline.ts && grep -c "probe 1[3-6]" scripts/verify-pipeline.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "probe 13" scripts/verify-pipeline.ts`, `grep -c "probe 14" scripts/verify-pipeline.ts`, `grep -c "probe 15" scripts/verify-pipeline.ts`, `grep -c "probe 16" scripts/verify-pipeline.ts` each output at least 1
    - `grep -c "claim_due_companies" scripts/verify-pipeline.ts` outputs at least 1 (direct RPC concurrency probe)
    - Probe 13 asserts disjointness across call results (a Set-size or per-id membership comparison is present)
    - Probe 15 captures description_html and first_seen_at before closing and compares equality after the tick
    - Script is not executed in this task; syntax check passes
  </acceptance_criteria>
  <done>verify-pipeline.ts carries executable hosted regressions for exclusive claims, no-work heartbeat success, close-then-return reopen, and discovery health surfacing, ready for Plan 02-07 to run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification scripts → hosted project | Scripts authenticate with gitignored credentials and mutate hosted rows |
| service_role RPC | claim_due_companies executes with elevated table access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Tampering | verify-watchlist against production seeds | high | mitigate | Probes mutate only an invocation-inserted disposable row; probe 6 regression asserts seed ids and job counts survive every run |
| T-02-05-02 | Elevation of Privilege | claim_due_companies redefinition | high | mitigate | Migration 0008 re-issues revoke from public/anon/authenticated and grant to service_role only; security invoker + empty search_path preserved |
| T-02-05-03 | Information Disclosure | script credentials | medium | mitigate | Established convention: secrets only in gitignored scripts/.env via node --env-file; no secret values in code or planning artifacts |
| T-02-05-04 | Denial of Service | probe 13 locking claims | low | accept | SKIP LOCKED never blocks; four concurrent batch_size=2 calls against 3 seed rows finish in milliseconds |
| T-02-05-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed by this plan |
</threat_model>

<verification>
- Task acceptance greps prove locking CTE, grant posture, disposable-probe tracking, seed regression, and probes 13-16 exist
- `node --check` / tsc syntax pass on both scripts
- Hosted execution of both scripts is deferred to Plan 02-07 (after `supabase db push` and function redeploys)
</verification>

<success_criteria>
- Gap 3 closed: claim_due_companies uses a locking CTE with FOR UPDATE SKIP LOCKED in a NEW migration; concurrent-claim disjointness has a hosted integration probe
- Gap 1 closed: the watchlist verifier is non-destructive, uses a dedicated tracked probe row, and proves production seed rows and job foreign keys are unchanged
- Probes 14-16 exist to prove Plans 02-04/02-06 behavior on the hosted project in Plan 02-07
</success_criteria>

<output>
Create `.planning/phases/02-watchlist-ingestion-monitoring/02-05-SUMMARY.md` when done.
</output>
