---
phase: 03.7-watchlist-first-jobs-dashboard
verified: 2026-07-28T20:54:28Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 3/5
  gaps_closed:
    - "Phase 04 applied-history rows now carry normalized watched-company membership, and Watchlist Jobs filters them fail closed while All Jobs retains the combined set."
    - "Owner UAT on exact deployed release 3901b90 passed navigation, ranking completion, both source scopes, applied-history scoping, and preserved Dashboard interactions."
  gaps_remaining: []
  regressions: []
gaps: []
behavior_unverified_items: []
---

# Phase 03.7: Watchlist-First Jobs Dashboard Verification Report

**Phase Goal:** Prioritize postings from explicitly watched companies by making a watchlist-only jobs dashboard the default destination while preserving the existing combined watchlist-plus-external Dashboard as a secondary All Jobs view.
**Verified:** 2026-07-28T20:54:28Z
**Current source:** `3901b90dc7221458e7a5449e61274b91d72e3b1f`
**Status:** passed
**Re-verification:** Yes — the prior release-bound pass was refreshed against current post-Phase-04 source.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | The default jobs destination shows only current preference-eligible postings linked to companies explicitly present in the Watchlist. | ✓ VERIFIED | `main.tsx:63` mounts `Dashboard scope="watchlist"` at the index route. `feed.ts:458-475` and the paged RPC contract supply complete eligible rows; `Dashboard.tsx:511-518` passes loaded rows through `dashboardSourceRows`; the focused rendered test excludes an external row. |
| 2 | The existing combined watchlist-plus-external Dashboard remains available as a clearly labeled secondary All Jobs view rather than being duplicated or removed. | ✓ VERIFIED | `main.tsx:64`, `Shell.tsx:8-10`, and `Dashboard.tsx:727` wire `/all-jobs`, its navigation label, and shared component title. The rendered `scope="all"` test includes watched and external rows. |
| 3 | The watchlist-only view retains the existing company filter, deterministic score and tier controls, sorting, lifecycle actions, job details, safe apply links, active queue behavior, accessibility, responsive layout, and table interactions. | ✓ VERIFIED | Current-release owner UAT passed the preserved Dashboard interactions on deployed commit `3901b90`; focused tests and the production build also pass. |
| 4 | The watchlist-only restriction is derived from the existing normalized job-to-company relationship and fails closed: externally discovered jobs without a watched-company relationship never appear there. | ✓ VERIFIED | Migration 0058 derives `has_watched_company` only through `applications.source_job_id → jobs.company_id → companies.id`; `dashboardAppliedSourceRows` requires that flag for Watchlist Jobs and retains all rows for All Jobs. Pure, parser, migration-contract, and rendered regressions cover watched and external applied rows. |
| 5 | The implementation reuses the current feed and Dashboard components and introduces no database migration, ingestion/provider change, company directory, external-only page, or new persistence contract. | ✓ VERIFIED | Both routes mount the same `Dashboard`; the original Phase 03.7 implementation commit `0403362` changed only seven web UI/test files and no backend, migration, package, ingestion, provider, or persistence file. Later Phase 04 migrations are not Phase 03.7 changes. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `web/src/lib/dashboard.ts` | Pure fail-closed watched-job source classification | ✓ VERIFIED | Exists, substantive, imported by Dashboard, and unit-tested with watched and external rows. Membership uses only normalized relationship presence. |
| `web/src/pages/Dashboard.tsx` | One reusable Dashboard with watchlist and all scopes | ✓ VERIFIED | Active/dismissed rows and company options are scoped. Applied history is also filtered through normalized watched-company membership before rendering. |
| `web/src/main.tsx` | Default watchlist route and secondary All Jobs route | ✓ VERIFIED | Both routes exist and pass explicit scope props to the shared Dashboard. |
| `web/src/components/Shell.tsx` | Watchlist Jobs and All Jobs navigation | ✓ VERIFIED | Both labels/routes are present; both Dashboard routes retain full-width layout. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `web/src/main.tsx` | `web/src/pages/Dashboard.tsx` | Index and `/all-jobs` route elements | ✓ WIRED | Exact `watchlist` and `all` scope props are passed. |
| `web/src/components/Shell.tsx` | Dashboard routes | Primary navigation | ✓ WIRED | `Watchlist Jobs` points to `/`; `All Jobs` points to `/all-jobs`. |
| `web/src/pages/Dashboard.tsx` | `web/src/lib/dashboard.ts` | `dashboardSourceRows(allRows, scope)` | ✓ WIRED | Active and dismissed feed rows are filtered before count and table rendering. |
| `web/src/pages/Dashboard.tsx` | `web/src/lib/feed.ts` | `listFeedPage` → infinite pages → merged rows → table | ✓ WIRED | Real owner-scoped paged feed data reaches the rendered active/dismissed table. |
| `web/src/pages/Dashboard.tsx` | Applied-history source membership | `listDashboardAppliedApplications` → `dashboardAppliedSourceRows` | ✓ WIRED | The RPC returns strict `hasWatchedCompany` membership; Watchlist Jobs filters fail closed and All Jobs retains all owned system applications. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `Dashboard.tsx` active/dismissed table | `rows` | `listFeedPage` → `feedQuery.data.pages` → `mergedInfinitePage` → `dashboardSourceRows` | Yes | ✓ FLOWING |
| `Dashboard.tsx` watchlist company options | `companyOptions` | Unfiltered bounded `listFeedPage` request → `dashboardWatchlistCompanyOptions` | Yes | ✓ FLOWING |
| `Dashboard.tsx` applied table | `appliedApplications` | `dashboard_applied_applications_v2()` → strict parser → `dashboardAppliedSourceRows` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Source separation, routes, labels, Dashboard rendering, tracker parsing, applied scope, and migration contract | `cd web && npm test -- --run src/lib/dashboard.test.ts src/pages/Dashboard.test.tsx src/components/Shell.test.tsx src/lib/tracker.test.ts tests/application-tracker-happy-path.test.tsx tests/migration-0058-dashboard-applied-watchlist-scope.test.ts` | 6 files, 96 tests passed | ✓ PASS |
| Current TypeScript/Vite production build | `cd web && npm run build` | Build passed; existing large-chunk advisory only | ✓ PASS |
| Current lint | `cd web && npm run lint` | Exit 0; two unrelated existing warnings | ✓ PASS |
| Watchlist scope on applied history | Static data-flow trace across `Dashboard.tsx`, `tracker.ts`, and migration 0058 | Normalized membership is projected, strictly parsed, and filtered fail closed | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED — Phase 03.7 declares no probe and no conventional `scripts/*/tests/probe-*.sh` exists.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SCOR-04 | `03.7-01-PLAN.md` | User can view one atomically complete deterministic Dashboard feed with stored score/tier, posted time, company controls, sorting, and a direct HTTPS employer apply link. | ✓ SATISFIED | The current active feed retains the server-paged deterministic result, score/tier rendering, posted time, company controls, sorting, and `safeApplyUrl`-guarded direct links; current-release UAT passed both source scopes. |

No additional requirement is mapped specifically to Phase 03.7 in `REQUIREMENTS.md`; SCOR-04 is also mapped to its originating scoring phases.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `web/src/pages/Dashboard.test.tsx` | 198-217, 326-396 | Raw-source assertions stand in for interactive behavior | ⚠️ Warning | The suite confirms symbols and intended wiring but does not execute scope changes, lifecycle mutations, rollback/refill, or applied-history source separation. |
| Phase-modified files | — | `TBD` / `FIXME` / `XXX`, placeholder output, empty handlers, console-only implementation | None | No blocker debt marker or substantive stub found. |

### Human Verification

`03.7-UAT.md` records 6/6 passing checks against exact deployed release
`3901b90dc7221458e7a5449e61274b91d72e3b1f`: production navigation, ranking
completion, Watchlist page availability, Watchlist Jobs versus All Jobs,
applied-job source scope, and preserved Dashboard interactions.

### Gaps Summary

No gap remains. Migration 0058 and deployed commit `3901b90` close the Phase 04
applied-history regression with a versioned owner-scoped RPC, strict TypeScript
parsing, fail-closed source filtering, rendered coverage, and owner UAT.

---

_Verified: 2026-07-28T20:54:28Z_
_Verifier: the agent (gsd-verifier)_
