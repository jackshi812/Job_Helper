---
phase: 03.7-watchlist-first-jobs-dashboard
status: human_needed
score: 5/5
verified: 2026-07-25
commit: 0403362e4fde7a100181e869567c435d9c5b456d
---

# Phase 03.7 Verification

## Automated Result

All five roadmap truths are implemented locally:

1. `Watchlist Jobs` is the default route.
2. Watched-source membership fails closed on the existing normalized company relationship.
3. `All Jobs` preserves the combined watched-plus-external feed.
4. Existing Dashboard behavior is reused rather than duplicated.
5. No database, ingestion, provider, package, persistence, or production-data contract changed.

## Automated Checks

- `npm test`: 52 files passed, 1,127 tests passed.
- `npm run build`: TypeScript and Vite production build passed.
- `npm run lint`: passed with two pre-existing warnings.
- `git diff --check`: passed.

## Human Verification Required

1. On the deployed exact commit, `/` opens `Watchlist Jobs` and shows only postings collected through watched company relationships.
2. `All Jobs` opens `/all-jobs` and includes the existing combined watched-plus-external feed.
3. Watchlist Jobs retains company/tier filters, sorting, job details, apply links, applied/dismissed actions, horizontal scrolling, and responsive navigation.

The local browser-control surface had no available browser, so these signed-in checks were not falsely marked passed.
