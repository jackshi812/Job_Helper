---
phase: "03.2-dashboard-precision-company-visibility"
verified: "2026-07-22T22:26:29Z"
status: human_needed
score: "8/10 must-haves verified"
behavior_unverified: 2
overrides_applied: 0
behavior_unverified_items:
  - truth: "The signed-in Dashboard's staged company controls reset on remount, apply only on Show results, and keep newly refreshed companies visible by default."
    test: "Open the company panel, search and change the draft, apply or cancel it, navigate away/back or reload, and observe a newly refreshed company."
    expected: "Search only narrows the checklist; draft changes wait for Show results; Escape cancels; reload/navigation restores all companies; a new feed company is visible."
    why_human: "Pure transition tests and source wiring pass, but the repository has no browser interaction test environment and no authenticated browser was available."
  - truth: "Pointer and keyboard resizing behaves correctly in a real browser, including cancellation, persistence, sort isolation, and narrow-screen scrolling."
    test: "Resize columns by pointer and keyboard, cancel a drag, reload, sort Score, and repeat at a narrow viewport."
    expected: "Widths clamp and persist only on completion; cancel restores the starting width and page styles; resizing never sorts; all columns remain horizontally reachable."
    why_human: "Reducers, storage guards, ARIA markup, and event wiring are tested, but physical pointer capture, focus feel, and responsive rendering require a browser."
human_verification:
  - test: "Fresh defaults and session reset"
    expected: "All loaded companies and Strong, Good, and Weak start selected; hidden companies return after reload/navigation; newly refreshed companies are visible."
    why_human: "Requires an authenticated live feed and component remounts."
  - test: "Staged company search and AND filtering"
    expected: "Search only changes checklist options; checkbox edits wait for Show results; Reset changes the draft; Escape cancels; company and tier filters combine with AND semantics and announce the count."
    why_human: "Requires real focus, keyboard, and row updates."
  - test: "Maximum required-experience preference"
    expected: "Blank, 0, 3, and 20 save; decimal, negative, and 21 are blocked; cap 3 retains explicit minimum 3, filters required minimums above 3, and does not reject optional years or senior title words."
    why_human: "The parser and save/refilter chain are automated, but the deployed form and authenticated convergence need owner observation."
  - test: "Layout, Location, and detail reasons"
    expected: "Only Dashboard uses full width; Location shows real data or an em dash; Match reason is absent from the list and Match reasons remain on job detail; other routes stay centered."
    why_human: "Visual route geometry and production navigation require a browser."
  - test: "Column resize and persistence"
    expected: "Pointer drag, cancel, Arrow/Shift+Arrow/Home/End, bounds, Score-sort isolation, and reload persistence all work; company Reset does not reset widths."
    why_human: "Pointer capture, focus, and localStorage persistence need real browser interaction."
  - test: "Narrow and coarse-pointer use"
    expected: "The complete table remains available through labeled horizontal scrolling and resize hit areas remain usable without hiding columns."
    why_human: "Responsive and touch-target quality is judgment-dependent."
---

# Phase 03.2: Dashboard Precision & Company Visibility Verification Report

**Phase Goal:** Users can tune the current dashboard feed by company, score tier, and explicit required-experience cap while retaining truthful future ingestion and gaining a full-width, accessible, resizable results table.
**Verified:** 2026-07-22T22:26:29Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Current-feed company controls are truthful, staged, session-only, and do not hide future refreshed companies by default. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `dashboardCompanyOptions` derives labels only through `companyName`; hidden-key state, draft copy/reset/toggle, and filtering are pure in `web/src/lib/dashboard.ts:24-95`. Dashboard owns these sets only in component state at `web/src/pages/Dashboard.tsx:153-157`, and source gates find no preference/storage write for company or tier state. Pure tests cover truthfulness, staging, reset, and new-company default, but no authenticated browser exercised the full UI transition. |
| 2 | Strong, Good, and Weak start selected; Weak remains reachable; company and tier filters compose with AND semantics. | ✓ VERIFIED | `ALL_SCORE_TIERS` and the final combined predicate are implemented in `web/src/lib/dashboard.ts:10,86-95`; `defaultVisible` no longer removes Weak rows in `web/src/lib/feed.ts:124-130`. Unit/static-render tests cover all-selected defaults, Weak visibility, zero-tier state, and AND behavior. |
| 3 | A nullable integer 0–20 experience cap persists per user and reuses the existing refilter path. | ✓ VERIFIED | Migration `0030_dashboard_precision.sql` adds the nullable bounded column; `web/src/lib/preferences.ts:3-21,38-67` loads/upserts it then calls `mark_recent_jobs_for_refilter`; `Preferences.tsx:110-153,176-180` hydrates, validates, and submits the field. Save-order and cache/refilter tests pass. |
| 4 | Cap semantics are conservative: exact threshold passes, only explicit required minima above it fail, ranges use the minimum, and title/optional/ambiguous text remains eligible. | ✓ VERIFIED | `experienceMinimumRequired` normalizes and extracts mandatory lower bounds at `supabase/functions/_shared/filters.ts:29-124`; strict `minimum > cap` is at lines 260-263 and never inspects title seniority. Table-driven tests cover 38 parser cases plus 21 candidate-local filter cases, including reversed required/preferred headings, ranges, possessives, titles, and unrelated durations. |
| 5 | The experience gate is provider-neutral, precedes routing/budget/AI, participates in scoring freshness, and has a database-safe bounded reason. | ✓ VERIFIED | `score-tick` maps `max_required_experience` at lines 158-165 and 475-480, calls `cheapFilter` before routing/hash/reservation/AI at 243-310, and uses `filter-v3`. `scoring-input.ts` hashes the nullable cap; migration 0030 and `feed.ts` agree on `experience_above_max`. Isolation tests assert there is no source/provider bypass. |
| 6 | Dashboard alone uses the remaining browser width and owns the inline company/tier controls. | ✓ VERIFIED | `Shell.tsx:16-20,97-99` branches only pathname `/`; header/banner retain `max-w-6xl`. Dashboard renders the expandable labeled panel and three tier toggles at `Dashboard.tsx:325-424`. Shell route tests cover Dashboard, job detail, preferences, and shared chrome. |
| 7 | The list shows Location and no Match reason column, while job detail keeps complete Match reasons. | ✓ VERIFIED | Feed list/detail projections include `jobs.location` and retain `reasons` in `web/src/lib/feed.ts:17-29`; Dashboard renders Location at `Dashboard.tsx:593-598` with em-dash fallback; `JobDetail.tsx:228-241` renders Match reasons. Static render tests assert the list/detail split. |
| 8 | Column widths use a versioned, bounded, same-origin storage model and invalid data falls back safely. | ✓ VERIFIED | `web/src/lib/dashboardColumns.ts:1-175` defines all nine exact bounds, validates version/known finite in-range values, catches read/write denial, persists only the width map, and computes the table minimum width. Unit tests cover corrupt JSON, wrong version, unknown/non-finite/out-of-range values, denial, clamping, and key reducers. |
| 9 | Resizing is accessible and isolated from Score sorting across pointer, keyboard, cancellation, and responsive scroll behavior. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `ColumnResizeHandle.tsx:35-141` wires focusable separator semantics, numeric ARIA values, shared pointer ownership, capture, cancellation rollback, document-style restoration, and keyboard reduction. Dashboard renders eight sibling handles, a separate Score button, a fixed colgroup, and a labeled focusable overflow region. Unit/static tests pass, but no real browser exercised pointer capture, focus, or responsive feel. |
| 10 | The reviewed source is deployed as an exact identified production build. | ✓ VERIFIED | Corrected source is on `origin/main` at `6a255ab76753ae09284d5d5721d7b62d4efaabec`. Cloudflare deployment `9e2af450-838f-4908-97a7-e301d1f8ff0a` and both immutable/mutable aliases serve `assets/index-za8j5L8B.js`; local/immutable/alias SHA-256 is `e8d65e7782512c20303abede466ae85a3fffb38d9e83a5b9711031593bf0174d`. Migration 0030 and corrected `score-tick` are recorded deployed to Supabase project `fjcsvajkkztvlrpdplwx`. This proves release identity, not authenticated interaction behavior. |

**Score:** 8/10 truths verified; 2 present and wired but awaiting browser behavior verification.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `supabase/migrations/0030_dashboard_precision.sql` | Nullable 0–20 cap and fourth filter reason | ✓ VERIFIED | Exists, substantive schema change, deployed, and projections consume both additions. |
| `supabase/functions/_shared/filters.ts` | Conservative provider-neutral experience parser/gate | ✓ VERIFIED | Exported parser and gate are called by the worker; all 81 focused filter tests pass. |
| `supabase/functions/_shared/scoring-input.ts` | Semantic cap invalidation | ✓ VERIFIED | Cap is canonicalized into the scoring hash and worker supplies `filter-v3`. |
| `web/src/lib/preferences.ts`, `web/src/pages/Preferences.tsx` | Load/save/validate/explain the cap | ✓ VERIFIED | Select, type, payload, UI, RPC, cache invalidation, and error paths are wired. |
| `web/src/lib/dashboard.ts` | Pure company/tier filter model | ✓ VERIFIED | Imported by Dashboard and fed real `FeedRow[]`; no storage/network calls. |
| `web/src/pages/Dashboard.tsx` | Full Dashboard control/table composition | ✓ VERIFIED | Real query data flows through helpers into status, table, links, and actions. |
| `web/src/lib/dashboardColumns.ts` | Typed width/persistence model | ✓ VERIFIED | Imported by Dashboard and resize handle; validates untrusted storage. |
| `web/src/components/ColumnResizeHandle.tsx` | Accessible pointer/keyboard separator | ✓ VERIFIED | Rendered for each non-final header and connected to Dashboard state/persistence. |
| `web/src/components/Shell.tsx` | Dashboard-only width branch | ✓ VERIFIED | Current pathname controls only `<main>`; shared chrome remains centered. |
| `web/src/pages/JobDetail.tsx` | Detail-only reasons | ✓ VERIFIED | Real detail query renders stored reasons. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Preferences form | recent jobs | `savePreferences` → preferences upsert → `mark_recent_jobs_for_refilter` | ✓ WIRED | Upsert must succeed before the RPC; tests pin ordering and failure behavior. |
| Database preference | pre-AI filter | worker select/map → `cheapFilter` | ✓ WIRED | The worker selects the cap for claimed users and filters before routing, reservation, or AI. |
| Experience cap | score freshness | `scoringInputHash` + `filter-v3` | ✓ WIRED | Semantic changes cannot silently reuse the previous input hash. |
| Authorized feed | Dashboard controls/table | `listFeed` → `companyName`/filter pipeline → rows | ✓ WIRED | Options and rows derive from the same RLS-scoped, truthful feed projection. |
| Column storage | rendered table | hydrate → Dashboard state → `<colgroup>`/min-width → commit persist | ✓ WIRED | Width changes update the table and completion writes the validated full map. |
| Resize separator | Score header | sibling targets with propagation guards | ✓ WIRED | Sort remains a separate button; shared coordination rejects concurrent gestures. |
| Job row | detail reasons | `/jobs/:id` → `getFeedJob` → `row.reasons` | ✓ WIRED | Removing the list column did not remove detail reasons. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Dashboard | `feedQuery.data` | RLS-scoped Supabase `user_jobs` joined to jobs/companies | Yes; query returns up to 200 eligible live rows | ✓ FLOWING |
| Company panel | `companyOptions` | `companyName(row)` over loaded feed | Yes; no hardcoded or user-created company options | ✓ FLOWING |
| Results table | `rows` | base eligibility → company → tier → sort | Yes; table maps the filtered query result | ✓ FLOWING |
| Preferences | `maxRequiredExperience` | own-row preferences select/upsert | Yes; nullable server field hydrates and saves | ✓ FLOWING |
| Width layout | `columnWidths` | validated localStorage or exact defaults | Yes; state drives all nine `<col>` widths and table min-width | ✓ FLOWING |
| Job detail | `row.reasons` | detail-only `user_jobs` projection | Yes; real stored reasons are rendered | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Experience parser/filter semantics | `cd web && npm test -- --run tests/filters.test.ts ...` | Included in 132/132 focused tests | ✓ PASS |
| Dashboard company/tier/column reducers and markup | Same focused Vitest command | Included in 132/132 focused tests | ✓ PASS |
| Preference projection/refilter and worker ordering | Same focused Vitest command | Included in 132/132 focused tests | ✓ PASS |
| Full regression suite | `cd web && npm test` (execution gate evidence) | 38 files, 570/570 tests passed | ✓ PASS |
| Production build/lint | `npm run build`; `npm run lint` | Build passed; lint exits successfully with only the pre-existing `AuthProvider.tsx` Fast Refresh warning | ✓ PASS |

### Probe Execution

No phase probe script was declared or found. Hosted verification used the approved migration/function/web deployment path and exact source/asset identity evidence instead.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| PREF-01 | 03.2-01 | Persist maximum required experience with user preferences | ✓ SATISFIED | Database bound, browser projection/form, own-row upsert, and refilter RPC are wired. |
| SCOR-01 | 03.2-01 | Cheap filters discard irrelevant postings before AI | ✓ SATISFIED | Provider-neutral experience gate runs before route/hash/budget/AI and records a bounded outcome. |
| SCOR-03 | 03.2-01, 03.2-02 | Plain-language match reasons remain available | ✓ SATISFIED | New filter reason has a label; full scored reasons remain on job detail after list-column removal. |
| SCOR-04 | 03.2-02, 03.2-03 | Dashboard feed precision and accessible table | ⚠️ NEEDS HUMAN | All code, unit/static tests, build, and deployment identity pass; signed-in interaction and responsive visual behavior remain. |

No additional Phase 03.2 requirement is mapped in `REQUIREMENTS.md`; there are no orphaned requirement IDs.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| Phase-modified source set | — | No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers found | None | No blocker or incomplete implementation marker. |
| `web/src/pages/Dashboard.test.tsx` | interaction assertions | Static-render/source assertions cannot simulate real pointer/focus behavior | ℹ️ Info | Explicitly routed to human verification rather than treated as proof. |

The final standard-depth code review is clean with zero Critical, Warning, or Info findings after all original parser, resize-transaction, and locale-normalization fixes. Focused review acceptance passed 89/89 tests.

### Human Verification Required

1. **Fresh defaults and session reset**
   - Sign in, load Dashboard, hide a company, reload/navigate away and back, and observe a later feed refresh.
   - Expected: all companies and all tiers start selected; hidden state resets; new companies are visible.

2. **Staged company search and AND filtering**
   - Search, change checkboxes, cancel with Escape, apply with Show results, Reset the draft, and toggle tiers.
   - Expected: rows change only on Show results; Escape cancels; company and tier filters use AND; result count/empty copy is accurate.

3. **Experience preference boundary**
   - Try blank, 0, 3, 20, decimal, negative, and 21; save 3 and observe representative jobs.
   - Expected: only valid values save; explicit minimum 3 remains; required values above 3 filter; optional years and senior title words remain eligible.

4. **Layout and detail contract**
   - Compare Dashboard, Preferences, and a job detail page.
   - Expected: Dashboard alone is full width; Location is truthful or `—`; list omits Match reason; detail retains Match reasons.

5. **Resize transaction and persistence**
   - Drag, cancel, use Arrow/Shift+Arrow/Home/End, click Score sort, reload, and press company Reset.
   - Expected: cancellation restores width/styles; bounds hold; sort is isolated; widths persist; company Reset does not affect widths.

6. **Narrow/coarse-pointer use**
   - Use a narrow viewport and, if available, touch/coarse pointer input.
   - Expected: every column remains horizontally reachable and resize targets remain usable.

### Gaps Summary

No implementation gap was found. The phase goal is present, wired, regression-tested, reviewed, and deployed. The status remains `human_needed` because two interaction-dependent truths and the authenticated production experience could not be exercised without an available browser session.

---

_Verified: 2026-07-22T22:26:29Z_
_Verifier: the agent (gsd-verifier)_
