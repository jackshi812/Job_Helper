---
phase: 03.2-dashboard-precision-company-visibility
plan: 03
type: execute
wave: 3
depends_on: [03.2-02]
files_modified:
  - web/src/lib/dashboardColumns.ts
  - web/src/components/ColumnResizeHandle.tsx
  - web/src/pages/Dashboard.tsx
  - web/src/lib/dashboard.test.ts
  - web/src/pages/Dashboard.test.tsx
autonomous: false
requirements: [SCOR-04]
must_haves:
  truths:
    - "D-14: Shared header and non-Dashboard routes retain the existing centered shell."
    - "D-15: Resizing does not remove detail Match reasons."
    - "D-16: Resizing preserves the Location column."
    - "D-17: Column widths persist per browser in localStorage with no width-reset action, independent of company Reset."
    - "Every resizable column except the last has a keyboard-focusable separator with vertical separator semantics and numeric min/max/current values."
    - "Pointer and keyboard resizing clamps to declared bounds, supports Arrow/Shift+Arrow/Home/End, and cannot trigger Score sorting."
    - "Only validated versioned same-origin localStorage column widths persist; denied, malformed, unknown, non-finite, and out-of-range values fall back silently to defaults."
    - "Narrow screens keep all columns in an accessible horizontally scrollable table and coarse-pointer resize hit areas remain usable."
  artifacts:
    - "web/src/lib/dashboardColumns.ts exports the typed width model, clamp/hydrate/persist helpers, and keyboard reducer."
    - "web/src/components/ColumnResizeHandle.tsx exports the accessible pointer/keyboard separator."
  key_links:
    - "dashboardColumns width model -> colgroup/table min width -> resize handle -> guarded localStorage"
---

<objective>Make the native Dashboard table resizable, accessible, responsive, and persistent per browser.</objective>

## Decision coverage
- D-17: widths persist per browser in guarded localStorage with no reset-width action, independently of company Reset.
- D-14: Dashboard remains the only full-width route; shared shell/header geometry stays unchanged.
- D-15: resize work does not remove detail match reasons.
- D-16: resize work preserves the Location column.

<tasks>
<task type="auto">
  <name>Task 1: Add typed width model and guarded persistence</name>
  <files>web/src/lib/dashboardColumns.ts, web/src/lib/dashboard.test.ts</files>
  <read_first>web/src/pages/Dashboard.tsx, web/src/lib/dashboard.ts, .planning/phases/03.2-dashboard-precision-company-visibility/03.2-UI-SPEC.md</read_first>
  <action>Create dashboardColumns.ts with the nine column IDs, labels, defaults/minimums/maximums from UI-SPEC, clamping, finite-number validation, versioned key job-copilot.dashboard.column-widths.v1, safe read/write wrappers catching storage denial, and Arrow/Shift+Arrow/Home/End reducer behavior. Unknown columns and invalid JSON must be ignored without throwing.</action>
  <verify>Run dashboard.test.ts for defaults, corrupt/denied storage, unknown keys, bounds, and all keyboard increments; run TypeScript checks.</verify>
  <acceptance_criteria>Hydration happens once per Dashboard initialization; only column widths are written; no reset-width UI or company/tier persistence exists.</acceptance_criteria>
  <done>Width reducer and storage tests pass for valid, invalid, denied, unknown, and bounded values.</done>
</task>
<task type="auto">
  <name>Task 2: Render pointer and keyboard resize separators</name>
  <files>web/src/components/ColumnResizeHandle.tsx, web/src/pages/Dashboard.tsx, web/src/pages/Dashboard.test.tsx</files>
  <read_first>web/src/pages/Dashboard.tsx, web/src/lib/dashboardColumns.ts, .planning/phases/03.2-dashboard-precision-company-visibility/03.2-UI-SPEC.md</read_first>
  <action>Create ColumnResizeHandle.tsx with role separator, aria-orientation vertical, label Resize {Column} column, current/min/max values, pointer capture, pointerup/pointercancel cleanup, selection/cursor restoration, and keyboard controls. Render one handle at each resizable header edge and drive a colgroup from the width model; keep Score sorting in a separate button target.</action>
  <verify>Run static Dashboard tests for separator ARIA, colgroup widths, last-column omission, independent Score sort target, responsive scroll region, and no-match-reason/location headers; manually exercise pointer cancel and keyboard resize.</verify>
  <acceptance_criteria>Drag previews continuously, final width persists on pointer-up, cancellation leaves normal cursor/selection, and every width remains within declared bounds.</acceptance_criteria>
  <done>Separator ARIA, colgroup, responsive scroll, keyboard, pointer-cancel, and independent sort-target assertions pass.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve final web rollout and UAT</name>
  <files>web/src/pages/Dashboard.tsx, web/src/lib/dashboardColumns.ts, web/src/components/ColumnResizeHandle.tsx</files>
  <read_first>.planning/phases/03.2-dashboard-precision-company-visibility/03.2-UI-SPEC.md, .planning/phases/03.2-dashboard-precision-company-visibility/03.2-RESEARCH.md, web/src/pages/Dashboard.tsx</read_first>
  <action>After plans 01 and 02 deployment prerequisites pass, obtain owner approval for the final web build/deploy. Perform UAT for all-three-tier/companies defaults, staged company search, exact required-years threshold, full-width-only Dashboard, Location/detail Match reasons, keyboard/pointer resize, localStorage persistence, refresh reset, and narrow horizontal scrolling.</action>
  <verify>Record UAT evidence and run the full existing 64-test baseline plus the new focused tests before publishing the web revision.</verify>
  <acceptance_criteria>Owner approves the exact release after backend compatibility is confirmed; no production mutation occurs without that approval; all D-01–D-17 manual truths are evidenced.</acceptance_criteria>
  <done>UAT evidence and measured test/build/lint results are recorded before web release, or release is not performed.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser storage → table layout | Untrusted localStorage values influence only clamped CSS widths. |
| Pointer/keyboard input → resize state | User events update bounded client state and never execute code or issue network requests. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.2-06 | Tampering | localStorage widths | medium | mitigate | Parse finite numbers, ignore unknown keys, clamp bounds, catch SecurityError, and fall back defaults. |
| T-03.2-07 | Denial of service | pointer lifecycle | medium | mitigate | Pointer capture plus pointerup/pointercancel cleanup restores selection/cursor state. |
| T-03.2-SC | Tampering | web deployment | high | mitigate | Require blocking owner approval and preserve no-package/no-secret constraint. |
</threat_model>

## Artifacts this phase produces
- `web/src/lib/dashboardColumns.ts` width model, persistence, and keyboard reducer
- `web/src/components/ColumnResizeHandle.tsx` accessible resize separator
- Dashboard colgroup/responsive resize integration and regression/UAT evidence

<verification>Run all focused and full web tests, TypeScript/build/lint checks, then execute the blocking UAT/release checkpoint.</verification>
<success_criteria>Column widths are accessible, bounded, responsive, and browser-persistent while company/tier state remains session-only and the complete dashboard contract is verified.</success_criteria>
