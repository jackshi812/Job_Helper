---
phase: 03.2-dashboard-precision-company-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0030_dashboard_precision.sql
  - supabase/functions/_shared/filters.ts
  - supabase/functions/_shared/scoring-input.ts
  - supabase/functions/score-tick/index.ts
  - web/src/lib/preferences.ts
  - web/src/pages/Preferences.tsx
  - web/src/lib/feed.ts
  - scripts/verify-scoring.ts
  - web/tests/filters.test.ts
  - web/tests/preferences.test.ts
  - web/tests/scoring-input.test.ts
  - web/tests/preference-refilter-feed.integration.test.ts
autonomous: false
requirements: [PREF-01, SCOR-01, SCOR-03]
must_haves:
  truths:
    - "D-02: Company visibility remains session-only and this persisted preference does not affect ingestion."
    - "D-07: A cap of 3 keeps explicit required minimum 3 and excludes only greater values."
    - "D-08: Title seniority words are never parsed as a rejection signal."
    - "D-09: Missing, ambiguous, and unparseable required-years text remains eligible."
    - "D-10: Ranges use the minimum and preferred/desired/bonus years never exclude."
    - "D-11: Preference changes reuse the provider-neutral refilter/rescore path."
    - "A blank or 0–20 maximum required-experience preference is stored per user, and a value of 3 keeps explicit required minimums of exactly 3 while excluding only values above 3."
    - "Title seniority, preferred/desired/bonus years, and missing or ambiguous years never cause the new filter to reject a job."
    - "The experience gate remains provider-neutral and runs before routing, scoring-input hashing, budget reservation, and AI calls."
    - "Existing filter reasons remain valid and the new experience_above_max reason is accepted by the database, worker, feed types, and verifier."
  artifacts:
    - "supabase/migrations/0030_dashboard_precision.sql adds nullable max_required_experience and the fourth filter-reason value."
    - "web/src/lib/preferences.ts and Preferences.tsx project, validate, save, and explain the new preference."
  key_links:
    - "savePreferences -> mark_recent_jobs_for_refilter -> score-tick preference projection -> cheapFilter"
    - "max_required_experience -> scoringInputHash and SCORING_FILTER_REVISION"
---

<objective>
Add the persisted maximum required-experience preference and its provider-neutral pre-AI filtering contract.

Purpose: make entry-level precision truthful without title-based seniority inference or stale score reuse.
Output: migration 0030, browser/worker wiring, parser/filter tests, and a deploy-ready schema checkpoint.
</objective>

## Decision coverage
- D-07: persist a configurable maximum required-experience cap; exact threshold remains eligible and only explicit required minimums above it reject.
- D-08: never infer seniority from title words.
- D-09: missing, ambiguous, or unparseable years remain eligible.
- D-10: ranges use the minimum and optional/preferred years never exclude.
- D-11: reuse the existing refilter/rescore path and provider-neutral filter.
- D-02: this persisted preference is distinct from session-only Dashboard visibility and does not alter ingestion.

<tasks>
<task type="auto">
  <name>Task 1: Add preference schema and end-to-end projection</name>
  <files>supabase/migrations/0030_dashboard_precision.sql, web/src/lib/preferences.ts, web/src/pages/Preferences.tsx, web/src/lib/preferences.test.ts, web/src/pages/Preferences.test.tsx, supabase/functions/score-tick/index.ts, web/src/lib/feed.ts, scripts/verify-scoring.ts</files>
  <read_first>supabase/migrations/0017_preferences.sql, supabase/migrations/0019_user_jobs_scoring.sql, supabase/migrations/0027_score_budget_after_free_work.sql, web/src/lib/preferences.ts, supabase/functions/score-tick/index.ts, web/src/lib/feed.ts</read_first>
  <action>Create supabase/migrations/0030_dashboard_precision.sql with nullable preferences.max_required_experience, a CHECK allowing NULL or integer 0 through 20, and a recreated user_jobs filter_reason CHECK adding exactly experience_above_max while retaining existing values. Add the column to the browser preference select/record/upsert and implement Preferences.tsx blank-capable whole-number input, helper/error copy, and submit prevention. Extend src/lib/preferences.test.ts and src/pages/Preferences.test.tsx for null, 0, 20, decimal, negative, and above-20 cases. Also update worker select/map, FeedRow FilterReason union/label, and verify-scoring accepted reason set. Preserve existing auth.uid() RLS and refilter RPC behavior.</action>
  <verify>From `web/`, run `npm test -- --run src/lib/preferences.test.ts src/pages/Preferences.test.tsx src/lib/scoring-input.test.ts`; run the migration/schema structural test command documented in the repository; assert the migration text contains the 0–20 check and all four reason values.</verify>
  <acceptance_criteria>Existing users remain uncapped when the column is NULL; browser, worker, feed, and verifier compile against the same nullable field; no new table, RLS path, provider branch, or dependency is introduced.</acceptance_criteria>
  <done>Migration and all projections agree on the nullable field and four filter reasons; focused schema/projection tests pass.</done>
</task>
<task type="auto">
  <name>Task 2: Implement conservative required-years filtering and invalidation</name>
  <files>supabase/functions/_shared/filters.ts, supabase/functions/_shared/scoring-input.ts, supabase/functions/score-tick/index.ts, web/tests/filters.test.ts, web/tests/scoring-input.test.ts, web/tests/preference-refilter-feed.integration.test.ts</files>
  <read_first>supabase/functions/_shared/filters.ts, supabase/functions/_shared/scoring-input.ts, supabase/functions/score-tick/index.ts, supabase/functions/_shared/adapters/workday.ts, web/tests/filters.test.ts</read_first>
  <action>Extend FilterPreferences with nullable maxRequiredExperience and implement a normalized description parser that recognizes explicit required lower bounds (including 3 years, at least/minimum 3, 3+, and ranges) while ignoring preferred/desired/bonus/optional clauses and uncertain text. Use strict `minimum > cap` rejection, never inspect job.title for seniority, preserve existing reason precedence, return experience_above_max before routing/hash/AI, include the cap in the canonical scoring input, and bump the filter revision to filter-v3.</action>
  <verify>Run table-driven filter, scoring-input, and preference-refilter integration tests; include exact-threshold, above-threshold, optional-only, missing, ambiguous, title-only, multiple-minimum, Unicode dash/space, and provider-neutral cases.</verify>
  <acceptance_criteria>All required-years semantic cases are deterministic and pure; a cap of 3 rejects 4 but accepts 3; optional years and title words do not reject; changing the cap changes the semantic hash/revision and causes existing refilterable rows to be reclaimed.</acceptance_criteria>
  <done>Required-years tests cover exact, above, optional, missing, ambiguous, title-only, range, and provider-neutral cases with strict greater-than semantics.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve hosted schema and worker rollout</name>
  <files>supabase/migrations/0030_dashboard_precision.sql, supabase/functions/score-tick/index.ts</files>
  <read_first>.planning/phases/03.2-dashboard-precision-company-visibility/03.2-RESEARCH.md, supabase/migrations/0030_dashboard_precision.sql, supabase/functions/score-tick/index.ts</read_first>
  <action>After local tests pass, obtain explicit owner approval before running the hosted Supabase migration push. Execute deployment in order: migration 0030, updated score-tick Edge function, then web deployment. Do not add secrets or change polling/provider configuration.</action>
  <verify>Record migration success, confirm score-tick selects max_required_experience and accepts experience_above_max, and verify one uncapped and one capped preference row through the existing authenticated verification path.</verify>
  <acceptance_criteria>Hosted schema is compatible before worker deployment, worker is compatible before web save/select code is exposed, and the approval evidence is recorded in the execution summary.</acceptance_criteria>
  <done>Owner approval and ordered hosted verification evidence are recorded, or execution stops without production mutation.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser → preferences | Authenticated user input crosses into the own-row preferences upsert. |
| Worker → database | Service-role score-tick writes bounded filter outcomes to user_jobs. |
| Job description → parser | Provider-supplied text is parsed without HTML execution or provider-specific branching. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.2-01 | Tampering | preferences upsert | high | mitigate | Enforce integer 0–20 in browser and PostgreSQL; preserve auth.uid() RLS. |
| T-03.2-02 | Elevation | score-tick filter path | high | mitigate | Keep one provider-neutral cheapFilter before routing, budget, and AI; test no source branch. |
| T-03.2-03 | Denial of service | description parser | medium | mitigate | Use bounded normalized text and pure regex parsing; retain existing worker row/budget limits. |
| T-03.2-SC | Tampering | deployment/package surface | high | mitigate | No package installs; require blocking owner approval before hosted migration/deploy. |
</threat_model>

## Artifacts this phase produces
- `supabase/migrations/0030_dashboard_precision.sql`
- `experienceMinimumRequired`, `maxRequiredExperience`, and `experience_above_max` filter contract symbols
- Updated preference/filter/scoring-input/feed/verifier projections and their tests

<verification>Run the focused tests above, the existing full web test suite, TypeScript/build checks, and the hosted checkpoint only after approval.</verification>
<success_criteria>Preference save, refilter invalidation, conservative required-years semantics, schema constraint, scoring hash, and worker ordering are all verified without provider-specific behavior.</success_criteria>
