---
phase: 03.10-goldman-sachs-selective-higher-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/branded-identities.ts
  - supabase/functions/_shared/adapters/types.ts
  - supabase/functions/_shared/adapters/scope.ts
  - supabase/functions/_shared/adapters/goldman-higher.ts
  - supabase/functions/_shared/connectors.ts
  - web/tests/branded-identities.test.ts
  - web/tests/branded-scope.test.ts
  - web/tests/branded-adapters.test.ts
autonomous: true
requirements: [DISC-01, DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "D-01, D-02, D-03, and D-04: only the frozen goldman_higher:roles object can call the anonymous Higher GetRoles/GetRoleById contract; /results is canonical, /roles is legacy-only, and only the exact source-ID-bound Goldman Oracle Apply URL persists."
    - "D-05, D-06, and D-07: list/detail startDate, not lastPostedDate, is independently validated against one rolling 168-hour poll wall clock; unknown freshness makes the observation incomplete, and age never changes provider lifecycle."
    - "D-08, D-09, D-10, and D-11: exactly Data, Technology, Finance, Investment, Research, Risk, and Capital Markets match as normalized whole words/phrases from jobFunction or division, with exact selected-field/label/term list-detail agreement."
    - "D-12, D-13, D-14, and D-15: EARLY_CAREER and PROFESSIONAL are independently reconciled, only GS_EARLY_CAREER/GS_MID_CAREER details pass, and no Goldman-specific title, seniority, internship, temporary-program, or experience relevance heuristic bypasses existing preferences."
    - "D-16 and D-17: every page and selected detail reconciles under finite bounds, zero/partial/drifted observations cannot activate, and every Goldman result has allowMissingClosure=false."
  artifacts:
    - path: "supabase/functions/_shared/branded-identities.ts"
      provides: "Frozen canonical Higher identity, literal aliases, and exact Oracle Apply coordinates"
    - path: "supabase/functions/_shared/adapters/types.ts"
      provides: "Goldman-specific durable job and aggregate observation evidence contracts"
    - path: "supabase/functions/_shared/adapters/goldman-higher.ts"
      provides: "Two-population exact-168-hour Higher list/detail observer"
    - path: "web/tests/branded-adapters.test.ts"
      provides: "Deterministic complete, boundary, drift, and bounded-failure coverage"
  key_links:
    - from: "goldman_higher:roles"
      to: "https://api-higher.gs.com/gateway/api/v1/graphql"
      via: "reference-equal frozen GoldmanHigherBrandedIdentity"
      pattern: "GOLDMAN_HIGHER_SOURCE_KEY|GetRoles|GetRoleById"
    - from: "Higher list/detail evidence"
      to: "GoldmanHigherJobScopeEvidence"
      via: "startDate, recruitingType, selected category field/label/term, US, source ID, and Apply URL equality"
      pattern: "startDate|recruitingType|providerCategoryField|providerSourceId"
    - from: "complete selective observation"
      to: "shared connector acceptance"
      via: "complete positive evidence with allowMissingClosure=false"
      pattern: "requireCompleteBrandedObservation|allowMissingClosure"
---

<objective>
Implement the exact local Goldman Higher observation contract.

Purpose: Convert the existing broad Goldman candidate into a frozen, two-population, exact-168-hour selective observer with trustworthy provider evidence and no false closure path.

Output: Tested identity aliases, Goldman evidence types/digests, strict Apply validation, complete Early Career plus Professional reconciliation, and connector-level selective acceptance.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-CONTEXT.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-RESEARCH.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-PATTERNS.md
@.planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-SUMMARY.md

<interfaces>
The source key remains the literal `goldman_higher:roles`. Network authority is
`https://api-higher.gs.com/gateway/api/v1/graphql`, operations `GetRoles` and
`GetRoleById`. Navigation storage is `https://higher.gs.com/results`; exact
`/results`, `/results/`, `/roles`, and `/roles/` are the complete alias set.

The direct Apply host is `hdpc.fa.us2.oraclecloud.com`; the only accepted path
is `/hcmUI/CandidateExperience/en/sites/LateralHiring/job/{numericSourceId}/apply/email`
with HTTPS, no credentials/port/query/fragment, and an ID equal to the Higher
list/detail source ID.

`PollObservation` remains the shared output. A clean Goldman observation is
complete, positive, warning-free, internally credible, and always has
`allowMissingClosure=false`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze Goldman identity and durable evidence contracts</name>
  <files>supabase/functions/_shared/branded-identities.ts, supabase/functions/_shared/adapters/types.ts, supabase/functions/_shared/adapters/scope.ts, web/tests/branded-identities.test.ts, web/tests/branded-scope.test.ts</files>
  <read_first>
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-CONTEXT.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-RESEARCH.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-PATTERNS.md
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/adapters/types.ts
    - supabase/functions/_shared/adapters/scope.ts
    - web/tests/branded-identities.test.ts
    - web/tests/branded-scope.test.ts
  </read_first>
  <behavior>
    - "Exact /results and /results/ resolve to the frozen Goldman object; exact legacy /roles and /roles/ resolve to that same object."
    - "Query, fragment, credentials, port, encoded/subpath, /jobs, lookalike host, API origin, and caller-built identities fail before fetch."
    - "Capital Markets Technology selects Capital Markets; Database Administration does not select Data."
    - "Changing source ID, category field/label/term, country, posted instant, recruiting type, or external ID changes the Goldman evidence digest."
  </behavior>
  <action>
    Implement D-01 through D-04 and D-08 through D-11. Extend
    `GoldmanHigherBrandedIdentity` with an immutable literal `publicUrlAliases`
    collection and exact server-owned Oracle Apply host/site/path coordinates.
    Change only Goldman's canonical `publicUrl` to
    `https://higher.gs.com/results`; preserve the literal source key and
    GraphQL origin/path/operations. `resolveBrandedPublicUrl` must accept only
    the four reviewed literal navigation strings and return the registry
    object, never a caller-derived copy.

    Add discriminated `GoldmanHigherJobScopeEvidence` and
    `GoldmanHigherObservationScopeEvidence` contracts. Bind the job evidence
    to `sourceKey`, literal selection mode
    `recent_exact_us_provider_category`, integer `recentHours: 168`,
    `providerSourceId`, `providerCategoryField` (`jobFunction` or `division`),
    normalized `providerCategoryLabel`, one existing seven-term
    `matchedTerm`, `detailCountryCode: 'US'`, canonical `postedAt`,
    `recruitingType` (`GS_EARLY_CAREER` or `GS_MID_CAREER`), and
    `externalIdDigest`. Add `CreateGoldmanHigherScopeEvidenceInput` and
    `createGoldmanHigherScopeEvidence`; use Web Crypto SHA-256 over one fixed
    JSON tuple so Plan 02 can reproduce the bytes in SQL and poll persistence.

    Keep `ALLOWED_BRANDED_CATEGORY_TERMS` exactly Data, Technology, Finance,
    Investment, Research, Risk, and Capital Markets. Reuse
    `findAllowedBrandedCategoryTerm` unchanged for NFKC, case-insensitive,
    longest whole-token matching; do not add title/description evidence or
    widen any Morgan Stanley/JPMorgan behavior. Write the failing identity and
    evidence tests first, then implement.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-identities.test.ts branded-scope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "`resolveBrandedIdentity('goldman_higher:roles')` returns one frozen object with canonical `/results`, the exact four navigation literals, and unchanged GraphQL coordinates."
    - "Every unreviewed navigation/network variation returns null or throws before any fetch spy is called."
    - "`GoldmanHigherJobScopeEvidence` contains all and only the source, selection, recency, source-ID, category, country, posting, population, and digest facts named above."
    - "The seven existing terms and whole-token behavior remain exact; no title/description or substring fallback exists."
    - "Focused tests exit 0."
  </acceptance_criteria>
  <done>The local authorization and durable evidence contracts are frozen without widening any sibling source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement independent two-population selective reconciliation</name>
  <files>supabase/functions/_shared/adapters/goldman-higher.ts, supabase/functions/_shared/connectors.ts, web/tests/branded-adapters.test.ts</files>
  <read_first>
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-CONTEXT.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-RESEARCH.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-PATTERNS.md
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/adapters/types.ts
    - supabase/functions/_shared/adapters/scope.ts
    - supabase/functions/_shared/adapters/goldman-higher.ts
    - supabase/functions/_shared/adapters/oracle-recruiting.ts
    - supabase/functions/_shared/bounded-pool.ts
    - supabase/functions/_shared/connectors.ts
    - web/tests/branded-adapters.test.ts
  </read_first>
  <behavior>
    - "Exactly two listing streams issue experiences ['EARLY_CAREER'] and ['PROFESSIONAL']; each independently proves stable totalCount/page metadata and complete raw count before union deduplication."
    - "A startDate exactly 168 hours old passes; one millisecond older, future, missing, malformed, or list/detail-mismatched startDate excludes the role and makes the observation incomplete."
    - "Either jobFunction or division may prove one exact allowed category, but detail must repeat the selected field, normalized label, and term."
    - "Only an exact source-ID-bound LateralHiring Oracle Apply URL persists; same-host path/query/ID variations fail closed."
    - "Every success, partial, timeout, and error has allowMissingClosure=false."
  </behavior>
  <action>
    Implement D-03 through D-17 inside `pollGoldmanHigher`. Extend
    `GoldmanHigherAdapterOptions` with an injected epoch `wallClockNow`
    separate from the monotonic deadline `now`; capture one finite poll wall
    clock and compute an inclusive cutoff of exactly 168 hours. Query
    `startDate` in list and detail, require canonical instant equality, and do
    not use `lastPostedDate`, `createdDate`, or a provider recency filter as
    freshness authority.

    Replace the combined experience request with two exact streams:
    `experiences: ['EARLY_CAREER']` and `experiences: ['PROFESSIONAL']`, both
    with `filters: []` and `searchTerm: ''`. For each stream independently
    require echoed page number/page size, stable `totalCount`, coherent
    `hasNext`, monotonic pagination, page-size bounds, no repeated page or
    duplicate role/source ID, and final fetched count equal to reported count.
    Deduplicate only the proven union; any cross-slice identity drift is
    incomplete. Preserve the reviewed finite bounds of 100 rows/page, 100
    pages, 5,000 raw roles, 5,000 details, detail concurrency four,
    1 MiB/response, 15 seconds/request, and 120 seconds total unless the
    checked-in identity already exposes stricter equal-or-safer values.

    Replace `trustedCategory` with a field-preserving Goldman selector that
    checks `jobFunction` before `division`, permits one positive field when the
    other is empty, and returns exact `{field,label,term}`. Detail hydration
    must repeat role ID, numeric provider source ID, title, selected primary
    U.S. location, selected category field/normalized label/term, `startDate`,
    `status='POSTED'`, `externalJobStatus='POSTED'`,
    `externalSourceType='ORACLE'`, `applyActive=true`,
    `applyInExternalSource=true`, and recruiting type
    `GS_EARLY_CAREER` or `GS_MID_CAREER`. A role outside those two requested
    populations makes the observation incomplete; internship/temporary text
    receives no special rejection or admission.

    Add private `safeGoldmanApplyUrl` using `URL` and exact host/path/source-ID
    equality; persist the returned Oracle URL as `absoluteUrl`. Require a
    bounded nonempty full description. Build per-job and aggregate Goldman
    evidence with exactly two population slice digests plus sorted job,
    country, category, freshness, and application digests. Reserve
    `PollObservation.expectedCount` for the final deduplicated selected job
    count. Zero selected jobs, raw/page/count/detail caps, schema drift, count
    drift, missing detail, timeout, or any failed equality cannot activate.

    Set `allowMissingClosure=false` in the healthy return as well as every
    incomplete branch. Extend `requireCompleteBrandedObservation` so Goldman
    requires this selective flag and the exact Goldman aggregate discriminator
    before verify/observe/poll can accept it. Add no preference, title,
    seniority, analyst/associate, manager, internship, or experience heuristic.
    Write the complete boundary/failure fixture matrix before implementation.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-adapters.test.ts -t "Goldman" &amp;&amp; npm test -- branded-identities.test.ts branded-scope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Test call inspection proves exactly two server-owned experience streams and independent page/count reconciliation before union deduplication."
    - "Healthy fixtures populate postedAt from list/detail startDate, direct Oracle absoluteUrl, full description, U.S./category/population evidence, exactly two slice digests, and allowMissingClosure=false."
    - "The exact 168-hour boundary is inclusive; every untrusted freshness/category/population/identity/Apply/completeness case is incomplete, bounded, and closure-disabled."
    - "No lastPostedDate freshness use or Goldman-specific downstream relevance heuristic exists."
    - "Focused tests exit 0."
  </acceptance_criteria>
  <done>The connector produces only complete, recent, evidence-backed Goldman jobs and never treats selective absence as provider closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL/source input → frozen Goldman identity | Untrusted navigation and caller objects must not choose network coordinates |
| Anonymous Higher GraphQL → adapter | Provider JSON, counts, timestamps, categories, populations, and URLs are untrusted |
| List slices → detail union | Independent populations must reconcile before deduplication and hydration |
| Adapter evidence → shared connector | Only exact complete closure-disabled Goldman evidence may pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.10-01-01 | Spoofing / SSRF | frozen identity | high | mitigate | Task 1 literal registry object, four URL literals, fixed GraphQL coordinates, and zero-fetch hostile tests |
| T-03.10-01-02 | Tampering | Oracle Apply URL | high | mitigate | Task 2 exact parsed HTTPS host/path/source-ID checks before persistence |
| T-03.10-01-03 | Tampering | provider timestamp | high | mitigate | Task 2 list/detail startDate equality and exact injected 168-hour wall-clock tests |
| T-03.10-01-04 | Elevation of Privilege | category/population evidence | high | mitigate | Tasks 1–2 exact seven terms, field/label/term binding, two requested slices, and recruiting-type allowlist |
| T-03.10-01-05 | Denial of Service | anonymous page/detail fan-out | high | mitigate | Task 2 page/job/detail/byte/concurrency/request/total bounds and bounded errors |
| T-03.10-01-06 | Tampering | selective lifecycle | high | mitigate | Task 2 hard Goldman allowMissingClosure=false on every outcome and connector guard |
| T-03.10-01-07 | Information Disclosure | provider diagnostics | medium | mitigate | Task 2 bounded reason codes; no raw payload, secret, or unbounded response persistence |
| T-03.10-01-SC | Tampering | dependency supply chain | low | accept | No package installation; use repository runtime, URL, Web Crypto, and existing bounded pool |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- Modified `GoldmanHigherBrandedIdentity` with canonical `/results`, literal `publicUrlAliases`, and frozen Oracle Apply coordinates
- New `GoldmanHigherJobScopeEvidence` interface
- New `GoldmanHigherObservationScopeEvidence` interface
- New `CreateGoldmanHigherScopeEvidenceInput` interface
- New `createGoldmanHigherScopeEvidence` function
- New private field-preserving Goldman category selector in `goldman-higher.ts`
- New private `safeGoldmanApplyUrl` validator in `goldman-higher.ts`
- Updated `GoldmanHigherAdapterOptions.wallClockNow`
- Updated `pollGoldmanHigher` two-slice exact-168-hour implementation
- Updated `requireCompleteBrandedObservation` Goldman selective guard
- Expanded Goldman identity, scope, and adapter regression tests
</artifacts_this_phase_produces>

<verification>
- Focused identity/evidence tests prove exact authorization, aliases, whole-token terms, and digest sensitivity.
- Focused adapter tests prove two independent population slices, every page/detail, exact freshness/category/population/Apply evidence, and bounded failures.
- Every observed Goldman outcome is closure-disabled.
</verification>

<success_criteria>
- Local Goldman polling returns a positive complete observation only for the locked D-01–D-17 scope.
- Drift, untrusted freshness, incomplete population/page/detail evidence, or unsafe Apply data cannot produce activation-grade evidence.
- Sibling provider behavior and existing downstream preference filtering remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-01-SUMMARY.md` when done.
</output>
