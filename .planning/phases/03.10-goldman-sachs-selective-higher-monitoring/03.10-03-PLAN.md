---
phase: 03.10-goldman-sachs-selective-higher-monitoring
plan: 03
type: execute
wave: 3
depends_on: [03.10-02]
files_modified:
  - scripts/run-phase-03-10-rollout.mjs
  - scripts/run-phase-03-10-rollout.test.mjs
autonomous: true
requirements: [DISC-01, DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "D-18 and D-19: the rollout controller validates one finite ordered positive-or-Unsupported release/proof inventory, including no more than three accepted observation windows, no fourth invocation, one later scheduler-owned natural poll, and qualifying persisted-role evidence."
    - "D-20: exact approval text and tokens are derived only from a caller-supplied final manifest; non-exact approval, source drift, hash drift, target drift, baseline drift, or action drift executes zero commands."
    - "D-21: manifest validation requires the complete protected sibling/user snapshot scope, closed scheduler-field exclusion list, exact cleanup namespace, and recursive credential-redaction contract."
  artifacts:
    - path: "scripts/run-phase-03-10-rollout.mjs"
      provides: "Manifest-aware read-only dry run and approval-gated finite migration/function deployment controller"
    - path: "scripts/run-phase-03-10-rollout.test.mjs"
      provides: "Injected-runner proof of zero-command rejection and exact finite release execution from synthetic manifest fixtures"
  key_links:
    - from: "Plan 05 final release manifest"
      to: "project-local Supabase CLI actions"
      via: "validateManifest and exactApproval equality before executeRelease"
      pattern: "validateManifest|exactApproval|executeRelease"
    - from: "manifest executable and test inventory"
      to: "source commit and path hashes"
      via: "recomputed Git object identity and SHA-256 equality before any command"
      pattern: "source_commit|privileged_executables|safety_tests"
---

<objective>
Build and test the manifest-aware Phase 03.10 rollout controller before release identity is frozen.

Purpose: Establish the fail-closed deployment and approval semantics that Plan 05 will apply to the completed rollout, activation, hosted-verification, and UAT tooling.

Output: Tested rollout controller and safety tests. This plan creates no immutable release manifest, approval token, production mutation, or live activation.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-CONTEXT.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-RESEARCH.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-PATTERNS.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-01-SUMMARY.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-02-SUMMARY.md
@.planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-SUMMARY.md
@.planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-02-SUMMARY.md

<interfaces>
The controller consumes, but does not create or finalize, a release manifest.
Plan 05 will supply the immutable manifest only after Plan 04 has completed all
activation, hosted-verification, UAT-helper, and safety-test files.

The final inventory must bind the source commit containing every executable
byte, the Supabase project ref, expected hosted migration baseline through
0047, migration 0048 path/hash, exact entry and transitive bundle hashes for
every changed function, privileged executable hashes, safety-test hashes,
finite action inventory, protected snapshots, cleanup, redaction, and UAT.

The rollout script uses `web/node_modules/.bin/supabase`. Its only schema
operation is `db push --linked --yes`, executed with `SUPABASE_ACCESS_TOKEN`
in the environment after exact approval. Direct CLI mutation outside the
manifest/hash protocol is forbidden.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build and test the manifest-aware rollout controller</name>
  <files>scripts/run-phase-03-10-rollout.mjs, scripts/run-phase-03-10-rollout.test.mjs</files>
  <read_first>
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-CONTEXT.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-RESEARCH.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-PATTERNS.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-02-SUMMARY.md
    - scripts/run-phase-03-9-rollout.mjs
    - scripts/run-phase-03-9-rollout.test.mjs
    - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-RELEASE-MANIFEST.json
    - supabase/migrations/0048_phase_03_10_goldman_higher.sql
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/adapters/types.ts
    - supabase/functions/_shared/adapters/scope.ts
    - supabase/functions/_shared/adapters/goldman-higher.ts
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/verify-board/index.ts
    - supabase/functions/observe-connectors/index.ts
    - supabase/functions/poll-tick/index.ts
  </read_first>
  <behavior>
    - "A dry run validates a supplied manifest, committed source objects, exact target/baseline/action inventory, emits the approval derived from those final bytes, and executes zero Supabase commands."
    - "Any one-byte manifest, migration, function entry/bundle, privileged executable, safety test, target, baseline, source, action, protected-snapshot, cleanup, redaction, or UAT-contract drift rejects execution."
    - "A non-exact approval executes zero commands; an exact approval permits only one db push and only manifest-listed changed function deployments."
    - "Synthetic and temporary fixture manifests exercise validation without creating a Phase 03.10 release manifest or reusable production approval."
  </behavior>
  <action>
    Implement D-18 through D-21 by forking the Phase 03.9 release controller
    into Phase 03.10-specific `validateManifest`, `exactApproval`,
    `dryRunPlan`, and `executeRelease` exports. Keep manifest construction out
    of this plan. Accept the final manifest as explicit input and validate its
    schema without mutating it.

    Require the final manifest schema to contain one immutable release ID; the
    final executable source commit and exact Git object identities; project
    ref; source key `goldman_higher:roles`; canonical
    `https://higher.gs.com/results`; hosted migration baseline through 0047;
    exact migration 0048 path/hash; exact entry/transitive inventories, hashes,
    and JWT settings for changed `verify-board`, `observe-connectors`, and
    `poll-tick`; hashes for `run-phase-03-10-rollout.mjs`,
    `run-phase-03-10-activation.ts`, `verify-phase-03-10-hosted.mjs`, every
    executable UAT helper, and their rollout/activation/hosted safety tests;
    unchanged web deployment identity; current Goldman baseline; protected
    sibling and real-data fingerprints; exact selective-source facts; cleanup,
    ACL, redaction, and owner-browser contracts.

    Validate an exact finite ordered action inventory: `db_push_0048`, changed
    function deploys, zero-mutation live probe, exactly one positive or
    Unsupported terminal outcome, at most three accepted observations with no
    fourth invocation, one later scheduler-owned natural poll, persisted-job/
    feed-aging/isolation checks, cleanup on every exit, and owner-browser UAT.
    `--dry-run` is read-only. `executeRelease` requires the exact freshly
    generated signal and revalidates the source commit's manifest-listed Git
    objects plus every recorded SHA-256, target, hosted baseline, and action
    immediately before invoking the injected command runner. The only schema
    command is the project-local Supabase CLI `db push --linked --yes`;
    function deploys are restricted to the manifest list with exact project
    ref/JWT settings.

    Define `exactApproval` from a canonical final-manifest approval payload and
    its SHA-256; never accept a free-form or prior-phase token. Add
    injected-runner tests using non-production fixture manifests for
    zero-command dry run/rejection, exact finite command list, source-object
    drift, baseline drift, unexpected file/action/function rejection, missing
    executable/test coverage, and approval invalidation after any byte change.
    The test suite must not write the final preflight or release-manifest
    artifacts and must not emit a reusable production approval token.
  </action>
  <verify>
    <automated>node --test scripts/run-phase-03-10-rollout.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - "The controller requires exact source, target, baseline, migration, function bundles, every privileged executable and safety test, finite actions, protected snapshots, cleanup, credential redaction, and UAT fields."
    - "Fixture dry runs execute zero mutation commands and cannot be mistaken for the final Phase 03.10 approval."
    - "Only an exact approval derived from a supplied validated manifest can run the single manifest-bound schema push and manifest-listed function deployments."
    - "Any byte/state/action drift fails closed before the first command."
    - "Rollout tests exit 0 without creating a final release manifest."
  </acceptance_criteria>
  <done>The rollout controller is fully behavior-tested and ready to consume only the immutable manifest created after all privileged tooling exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Security baseline: OWASP ASVS Level 1.

| Boundary | Description |
|---|---|
| Completed source bytes → future release manifest | Every executable, test, target, protected scope, and finite action must be hash-bound after Plan 04 |
| Approval text → Supabase CLI | Only one exact final-manifest-derived signal may authorize the finite deployment inventory |
| Fixture manifests → tests | Synthetic test data must never become a reusable production approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.10-03-01 | Spoofing / Repudiation | release approval | critical | mitigate | Task 1 canonical final-manifest payload, exact token equality, and drift invalidation |
| T-03.10-03-02 | Tampering | executable/test inventory | high | mitigate | Task 1 requires Git object and SHA-256 coverage for every privileged executable and safety test |
| T-03.10-03-03 | Elevation of Privilege | finite action inventory | high | mitigate | Task 1 exact rollout/terminal/window/natural-poll/cleanup action validation and rejection of extras/reordering |
| T-03.10-03-04 | Information Disclosure | credential contract | high | mitigate | Task 1 environment-only credential and output/artifact redaction assertions |
| T-03.10-03-SC | Tampering | dependency supply chain | low | accept | No package installation; exact repository CLI/runtime and committed source only |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- New `scripts/run-phase-03-10-rollout.mjs`
- New rollout exports `validateManifest`, `exactApproval`, `dryRunPlan`, and `executeRelease`
- New `scripts/run-phase-03-10-rollout.test.mjs`
- No release manifest, preflight, production approval, or hosted claim
</artifacts_this_phase_produces>

<verification>
- Rollout tests prove exact final-manifest approval semantics, zero-command rejection, complete executable/test coverage, and finite command behavior using fixtures.
- No Phase 03.10 immutable manifest or reusable production approval exists before Plans 04 and 05.
</verification>

<success_criteria>
- The rollout controller can consume a later immutable manifest without changing or widening it.
- No production mutation can occur without an exact signal derived from that final manifest.
- Plan 03 owns only rollout executable/test source; release identity is finalized after all privileged tooling in Plan 05.
</success_criteria>

<output>
Create `.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-03-SUMMARY.md` when done.
</output>
