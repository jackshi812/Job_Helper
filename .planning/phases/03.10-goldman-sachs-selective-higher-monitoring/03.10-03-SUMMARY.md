---
phase: 03.10-goldman-sachs-selective-higher-monitoring
plan: 03
subsystem: release-tooling
tags: [goldman-higher, release-manifest, git-objects, sha256, approval-gate]
requires:
  - phase: 03.10-01
    provides: exact Goldman identity, adapter, and selective evidence contracts
  - phase: 03.10-02
    provides: migration 0048 and changed verify/observe/poll persistence boundaries
  - phase: 03.9
    provides: manifest-bound selective-source rollout precedent
provides:
  - Fail-closed Phase 03.10 final-manifest validator with Git-object and SHA-256 coverage
  - Canonical manifest-derived approval tied to exact target, baseline, actions, protection, cleanup, redaction, and UAT contracts
  - Finite release executor restricted to migration 0048 and three manifest-listed functions
affects: [phase-03.10-activation, phase-03.10-hosted-verification, phase-03.10-release]
tech-stack:
  added: []
  patterns:
    - revalidate immutable source objects and current bytes immediately before mutation
    - keep mutable evidence templates object-bound but outside immutable current-byte checks
    - derive approval from canonical manifest and approval-payload hashes
key-files:
  created:
    - scripts/run-phase-03-10-rollout.mjs
    - scripts/run-phase-03-10-rollout.test.mjs
  modified: []
key-decisions:
  - "Immutable executables, safety tests, migration, and function bundles must match both their source-commit Git objects and current SHA-256 bytes before any command."
  - "Mutable rollout, hosted, evidence, and UAT templates retain source-commit object and initial-hash identity while later writes follow an exact writer/state-transition contract."
  - "The only release commands are one linked migration push and exact verify-board, observe-connectors, and poll-tick deployments with frozen JWT settings."
patterns-established:
  - "Approval invalidation: release text includes both the caller-supplied manifest-file SHA-256 and a canonical safety-payload SHA-256."
  - "Synthetic rollout tests use temporary Git repositories and an explicitly non-production release profile."
requirements-completed: [DISC-01, DISC-07, DISC-08, DISC-09]
coverage:
  - id: D1
    description: Complete immutable and mutable release inventory validation fails closed on source, object, byte, target, baseline, action, protection, cleanup, ACL, redaction, or UAT drift
    requirement: DISC-07
    verification:
      - kind: unit
        ref: "scripts/run-phase-03-10-rollout.test.mjs#source, byte, target, baseline, action, and function drift fail closed"
        status: pass
      - kind: unit
        ref: "scripts/run-phase-03-10-rollout.test.mjs#missing executable/test and protected, cleanup, ACL, redaction, or UAT drift reject"
        status: pass
    human_judgment: false
  - id: D2
    description: Only an exact final-manifest-derived approval can run one migration push and the three approved function deployments
    requirement: DISC-08
    verification:
      - kind: unit
        ref: "scripts/run-phase-03-10-rollout.test.mjs#non-exact approval rejects before the first command"
        status: pass
      - kind: unit
        ref: "scripts/run-phase-03-10-rollout.test.mjs#exact approval runs one schema push and only the three listed functions"
        status: pass
    human_judgment: false
  - id: D3
    description: Dry-run and rejection fixtures execute no production commands and create no reusable Phase 03.10 release artifacts
    requirement: DISC-09
    verification:
      - kind: unit
        ref: "scripts/run-phase-03-10-rollout.test.mjs#dry run validates final bytes, emits derived approval, and runs zero commands"
        status: pass
      - kind: unit
        ref: "scripts/run-phase-03-10-rollout.test.mjs#tests leave no final Phase 03.10 manifest or reusable approval artifact"
        status: pass
    human_judgment: false
duration: 9min
completed: 2026-07-27
status: complete
---

# Phase 03.10 Plan 03: Manifest-Aware Goldman Rollout Controller Summary

**Git-object- and SHA-bound Goldman rollout validation with canonical exact approval, zero-command dry runs, and a four-command deployment ceiling**

## Performance

- **Duration:** 9 min
- **Started:** 2026-07-27T15:33:13Z
- **Completed:** 2026-07-27T15:42:05Z
- **Tasks:** 1 TDD task
- **Files modified:** 2

## Accomplishments

- Added a strict final-manifest schema covering source commit objects, migration 0048, exact function entry/transitive inventories, privileged executables, safety tests, mutable evidence templates, unchanged web identity, protected snapshots, finite activation semantics, cleanup, ACL, recursive redaction, and owner-only UAT.
- Added canonical release approval derivation and immediate pre-command revalidation so approval, source, byte, target, baseline, or state drift invokes zero commands.
- Restricted execution to one `db push --linked --yes` and the manifest-listed `verify-board`, `observe-connectors`, and `poll-tick` deployments with exact project ref and JWT settings.
- Added temporary-repository fixture tests that exercise exact execution, dry-run non-mutation, post-approval drift, complete inventory rejection, and absence of final release/preflight artifacts.

## Task Commits

Each TDD gate was committed atomically:

1. **Task 1 RED: Rollout safety behavior tests** — `564e30a`
2. **Task 1 GREEN: Manifest-bound rollout controller** — `ec1ac51`

## Files Created/Modified

- `scripts/run-phase-03-10-rollout.mjs` — validates the future immutable manifest, derives exact approval, emits read-only dry-run plans, and executes only the finite deployment prefix.
- `scripts/run-phase-03-10-rollout.test.mjs` — proves zero-command rejection and exact finite execution with synthetic temporary Git repositories.

## Decisions Made

- Kept final manifest construction outside this plan; the controller exports the contract that Plan 05 must populate after all Plan 04 tooling exists.
- Required immutable current bytes to equal source-commit objects, while PENDING evidence templates are checked against their committed initial objects so later approved evidence transitions do not masquerade as immutable source.
- Required `SUPABASE_ACCESS_TOKEN` only from the execution environment after approval and full revalidation; it is never embedded in the manifest, plan, command arguments, or output.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The controller and tests performed no Supabase command, deployment, live probe, activation, release-manifest creation, preflight creation, or production mutation.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 04 can build the injected activation, hosted-verification, cleanup, and UAT tooling against the exported `validateManifest`, `exactApproval`, `dryRunPlan`, and `executeRelease` contract.
- Plan 05 remains responsible for creating the final source-commit-bound manifest only after every privileged executable and safety test exists.

## Self-Check: PASSED

- Both created script files exist.
- RED/GREEN commits `564e30a` and `ec1ac51` exist in order.
- `node --test scripts/run-phase-03-10-rollout.test.mjs` passes 10/10 tests.
- `node --check scripts/run-phase-03-10-rollout.mjs` and `git diff --check` pass.
- The Phase 03.10 final release manifest and preflight do not exist.
- Stub scan found no goal-blocking placeholder implementation; nullable fixture/UAT fields are intentional contract values.
- No unplanned network endpoint, authentication path, file-access boundary, or schema change was introduced.

---
*Phase: 03.10-goldman-sachs-selective-higher-monitoring*
*Completed: 2026-07-27*
