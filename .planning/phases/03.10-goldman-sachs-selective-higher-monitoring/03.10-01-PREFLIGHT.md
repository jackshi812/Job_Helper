# Phase 03.10 Plan 05 Preflight

Status: **PASS — truthful Active owner-UAT instructions frozen; final verification pending**

## Release Identity

- Release manifest ID: `c378b665-d6fc-49a0-bf71-a5d15f3cabd5`
- Target project ref: `fjcsvajkkztvlrpdplwx`
- Exact source: `goldman_higher:roles`
- Canonical public URL: `https://higher.gs.com/results`
- Executable source commit: `b47376e730bb87f1d916a6d1c58c132b650bd616`
- Source commit object: `b47376e730bb87f1d916a6d1c58c132b650bd616`
- Manifest file SHA-256: `d7ac436331ff609fcfe08a897bdcf37d46105066798230113ec8f3ef183b97a8`
- Release approval payload SHA-256: `5a8a5811258d6f03192c4036f84ea358c60ae88d155180a84f47df9bbd727eb6`

The source commit predates this metadata-only manifest/preflight commit. Every
immutable path is bound to both its Git blob object at that source commit and
its current SHA-256 bytes. The manifest and preflight are not executable source.

## Local Safety Gate

- Activation controller tests: **20/20 PASS**
- Rollout, hosted/UAT, retention, and permanent-dismissal tests:
  **59/59 PASS**
- Focused Goldman web and migration suite: **270/270 PASS**
- Full web suite: **1,429/1,429 PASS**
- Production web build: **PASS**
- Activation TypeScript syntax check: **PASS**
- Git whitespace/error check: **PASS**
- Package additions: **none**

## Immutable Inventory

The manifest contains a sorted 41-path `immutable_source` inventory:

- Privileged executables/UAT helper: 3
  - `scripts/run-phase-03-10-rollout.mjs`
  - `scripts/run-phase-03-10-activation.ts`
  - `scripts/verify-phase-03-10-hosted.mjs`
- Safety tests: 12
  - rollout, activation, hosted/UAT verifier
  - Goldman adapter, connector, identity, scope, observer, poller, lifecycle,
    migration, seven-day retention, and user-only permanent-dismissal contracts
- Forward migration: exact
  `0050_phase_03_10_goldman_provider_id_regex.sql`
- Function entrypoints: exact `verify-board`, `observe-connectors`, and
  `poll-tick`
- Function transitive inputs: every recursive repository-local import reachable
  from those three entrypoints

Every function has a deterministic bundle hash over its sorted
`[path, SHA-256]` pairs. JWT settings are frozen as:

- `verify-board`: `verify_jwt=true`
- `observe-connectors`: `verify_jwt=false`
- `poll-tick`: `verify_jwt=false`

## Mutable Evidence Contract

The five initial PENDING templates are Git-object- and initial-SHA-bound but
are deliberately outside immutable current-byte checks:

| Artifact | Initial state | Sole writer | Allowed states |
|---|---|---|---|
| `03.10-01-ROLLOUT-VERIFICATION.json` | `PENDING` | activation controller | `PENDING`, `PASS`, `UNSUPPORTED` |
| `03.10-01-ROLLOUT-EVIDENCE.md` | `PENDING` | activation controller | `PENDING`, `PASS`, `UNSUPPORTED` |
| `03.10-01-HOSTED-VERIFICATION.json` | `PENDING` | hosted verifier | `PENDING`, `PASS`, `UNSUPPORTED` |
| `03.10-UAT.json` | `PENDING_OWNER_BROWSER` | hosted verifier | `PENDING_OWNER_BROWSER`, `PASS`, `UNSUPPORTED` |
| `03.10-UAT.md` | `PENDING_OWNER_BROWSER` | hosted verifier | `PENDING_OWNER_BROWSER`, `PASS`, `UNSUPPORTED` |

Their exact initial bytes define the initial schema. After an approved writer
transition, hosted verification must bind the resulting evidence hashes; an
updated evidence artifact is never treated as unchanged executable source.

## Hosted and Web Baselines

- Hosted migration baseline: exact `0001` through `0050` (50 migrations)
- Goldman catalog baseline fingerprint:
  `cc2333b38d1805eb9428ac866678f11b8da7fd820683c9185bcab7943d80a73b`
- Goldman company baseline fingerprint:
  `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`
- Goldman terminal baseline fingerprint:
  `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`
- Web deployment remains unchanged:
  - source commit `baf81fcaa72443995f227b118d10eec90da25679`
  - deployment `b8b33556-1b6c-4a42-bada-e54e4a2f6855`
  - asset SHA-256
    `8e28857fb560b5997865201c75226069e71a973a5fe66451726d52a7d7a41f1f`

Migration 0050 is applied. The live adapter completed a zero-mutation probe
with 27 fully verified jobs across both populations and 13 reconciled pages,
Goldman remains Active 3/3, and the next natural poll persisted all 27 jobs.
This verifier-only release requires exact `0001…0050` hosted parity and
resumes monitoring without another schema/function mutation or replaying
admission.

## Frozen Scope and Terminal

- Country: exact `US`
- Freshness: rolling `720` hours (30 days)
- Populations: exact `EARLY_CAREER` and `PROFESSIONAL`
- Provider category terms, in contract order: `Data`, `Technology`, `Finance`,
  `Investment`, `Research`, `Risk`, `Capital Markets`
- Missing-job closure: always disabled
- Terminal: exactly one of positive or precise Unsupported
- Positive authority: requires a qualifying persisted role
- Accepted activation observations: at most three distinct scheduler windows
- Fourth invocation: forbidden
- Completion: requires a strictly later scheduler-owned natural poll

## Protection, Cleanup, ACL, and Redaction

- Thirteen exact sibling source identities are frozen in the manifest.
- Catalog, company, job, all-real-user, cron, and function surfaces each have a
  separate protected fingerprint.
- The scheduler-field exclusion list is closed to the nine exact
  company/heartbeat timestamps and health fields in the manifest; no job,
  identity, authority, ownership, or evidence field is excluded.
- Cleanup namespace: `phase-03-10-goldman-verifier`
- Exact cleanup UUIDs:
  - `275466ec-de5a-42ae-8c76-309674aca7ab`
  - `9ddf4e0d-a83d-4d8a-a3a8-4c277f8d9188`
  - `85d01f18-d77f-4f65-b0a3-f055676027a8`
- Cleanup is mandatory on success, Unsupported, error, timeout, assertion
  failure, and artifact-write failure; residue must equal zero.
- The four rollout RPCs are service-role-only and denied to `public`, `anon`,
  and `authenticated`.
- `CRON_SECRET`, `SUPABASE_ACCESS_TOKEN`, and
  `SUPABASE_SERVICE_ROLE_KEY` are environment-only.
- Errors, logs, JSON, Markdown, and nested causes require recursive redaction.

## Exact Ordered Production Actions

Plan 06 may perform only these manifest-listed actions, in this order:

1. `verify_migration_0050`
2. `zero_mutation_live_probe`
3. `positive_or_unsupported_terminal`
4. `observe_up_to_three_windows_no_fourth`
5. `natural_scheduler_poll`
6. `persisted_job_feed_aging_isolation_checks`
7. `cleanup_every_exit`
8. `owner_browser_uat`

The rollout executable performs one read-only exact `0001…0050` history check.
It performs no schema push or function deployment. The resume controller
requires the already-Active 3/3 state and never invokes the terminal admission
RPC.

## Exact Release Approval

The validated dry run emitted:

```text
approve Phase 03.10 Goldman rollout c378b665-d6fc-49a0-bf71-a5d15f3cabd5 d7ac436331ff609fcfe08a897bdcf37d46105066798230113ec8f3ef183b97a8 5a8a5811258d6f03192c4036f84ea358c60ae88d155180a84f47df9bbd727eb6
```

This is the only release approval accepted for these manifest bytes. Any
manifest, source commit, Git object, path hash, target, baseline, action,
protected snapshot, cleanup, ACL, redaction, web identity, or UAT-contract
change invalidates it before the first production command.

Prior approvals are consumed and invalid. The first stopped on Free Plan
read-only mode; the second applied migration 0048 and stopped on the obsolete
`--verify-jwt` CLI flag; the third verified migration 0048 and stopped before
any deployment POST when local Docker bundling failed. The fourth successfully
deployed all three functions through server-side bundling, then its read-only
probe stopped before network or mutation because Node 26 strip-only mode
rejected one TypeScript parameter property. This refreeze preserves the applied
migration boundary and deploy transport while repairing that runtime syntax.
That repaired run recorded the precise `posting_date_ineligible` Unsupported
terminal with zero authority and residue. The evidence audit then found that
the frozen writers could not certify Unsupported hosted parity or prepare the
required Not-monitored owner UAT. This refreeze adds only observed hosted
collection, normalized Unsupported rollout evidence, and Unsupported-aware UAT.
The first collection attempt then fail-closed on the production manifest's
`web_deployment.source_commit` field because the evidence helper expected the
older test-only `commit_sha` spelling. This refreeze accepts the production
field while retaining backward-compatible tests and exact web identity.
After the 30-day release admitted Goldman Active 3/3, the first natural poll
exposed the invalid PostgreSQL repetition upper bound. This final refreeze adds
only the forward 0050 constraint repair, an Active-only resume path that cannot
replay admission, and live Active hosted evidence collection.
Migration 0050 then allowed a natural poll to persist all 27 jobs. PostgreSQL
and PostgREST serialize that `timestamptz` as `+00:00`, while the constraint-
bound JSON evidence uses the equivalent `.000Z`. This verifier-only refreeze
compares the two parsed instants instead of requiring byte-identical timestamp
strings.
Hosted download then proved every executable file byte-identical and erased
only `_shared/adapters/types.ts`, the exact declaration-only module. This
refreeze accepts that single empty hosted representation only when the
manifest-bound local module contains no runtime syntax; all other drift still
fails closed.
The final verifier-only repair renders the pending owner-UAT instructions from
the observed terminal kind: Active evidence now requires checking Active 3/3,
a current Goldman job, complete detail, and the employer Apply link, while the
precise Unsupported branch retains its Not-monitored checks.

## Owner-UAT Approval Template

Owner UAT remains `PENDING_OWNER_BROWSER`. Codex browser use is forbidden and
`owner_attestation` remains null.

The future UAT record must bind this release plus the final hosted and rollout
evidence hashes through this canonical payload:

```text
{
  schema_version,
  phase,
  release_manifest_id,
  manifest_file_sha256,
  hosted_verification_sha256,
  rollout_verification_sha256,
  source_key,
  migration,
  functions,
  web,
  observed,
  expected_watchlist,
  expected_job,
  cleanup,
  owner_browser_required,
  codex_browser_used,
  manifest_source_commit
}
```

Only after those future evidence slots are populated may the verifier derive:

```text
approve Phase 03.10 Goldman Sachs owner-browser UAT c378b665-d6fc-49a0-bf71-a5d15f3cabd5 <sha256(canonical UAT payload)>
```

The last token is intentionally not fabricated now. The exact future token
changes with any hosted evidence, rollout evidence, runtime, job, cleanup,
browser-use, or release drift.

## Dry-Run Result

- Status: `PENDING_EXPLICIT_APPROVAL`
- Production fixture: `true`
- Production commands executed: **0**
- Source/object/hash coverage: **complete**
- Generated release approval: **matches the exact text above**
- Owner UAT: **not performed and not claimed**
