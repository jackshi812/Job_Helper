---
phase: 03.10-goldman-sachs-selective-higher-monitoring
plan: 02
type: execute
wave: 2
depends_on: [03.10-01]
files_modified:
  - supabase/functions/observe-connectors/index.ts
  - supabase/functions/poll-tick/index.ts
  - supabase/migrations/0048_phase_03_10_goldman_higher.sql
  - web/tests/observe-connectors.test.ts
  - web/tests/poll-tick.test.ts
  - web/tests/lifecycle.test.ts
  - web/tests/phase-03-10-migration.test.ts
autonomous: true
requirements: [DISC-01, DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "D-07 and D-17: an aged or selectively absent Goldman role remains provider-open, produces no close action, and is hidden only from the Active feed after 168 hours."
    - "D-16: observer, poller, and SQL independently require exact positive two-slice/count/page/detail/freshness/category/population/application evidence before observation or persistence."
    - "D-18: only goldman_higher:roles can enter the existing service-role, replay-safe, three-distinct-server-window Experimental-to-Active contract and exact ten-minute Active claim path."
    - "D-19: the schema can record exact positive terminal admission or a precise Unsupported outcome; Unsupported grants no company, observation, or scheduled claim authority."
    - "D-21: the forward migration preserves JPMorgan, Morgan Stanley, Charles Schwab, every active Workday source, public ATS sources, RLS, ACLs, and existing data constraints byte-semantically except for the exact Goldman branches."
  artifacts:
    - path: "supabase/functions/observe-connectors/index.ts"
      provides: "Activation-grade Goldman aggregate validation and service-role observation handoff"
    - path: "supabase/functions/poll-tick/index.ts"
      provides: "Per-job Goldman evidence recomputation before lifecycle/persistence"
    - path: "supabase/migrations/0048_phase_03_10_goldman_higher.sql"
      provides: "Forward-only Goldman identity/evidence/terminal/activation/claim/feed authority"
    - path: "web/tests/phase-03-10-migration.test.ts"
      provides: "Exact SQL authority, ACL, feed-aging, and sibling-preservation contract"
  key_links:
    - from: "GoldmanHigherObservationScopeEvidence"
      to: "record_connector_observation"
      via: "positive aggregate validation and digest-only service-role RPC"
      pattern: "isPositiveObservation|record_connector_observation"
    - from: "GoldmanHigherJobScopeEvidence"
      to: "jobs.scope_evidence"
      via: "poll-tick TypeScript recomputation plus jobs_scope_evidence_check SQL recomputation"
      pattern: "createGoldmanHigherScopeEvidence|jobs_scope_evidence_check"
    - from: "dashboard_feed_page"
      to: "current Active Goldman visibility"
      via: "database-time 168-hour predicate without jobs.status mutation"
      pattern: "clock_timestamp\\(\\).*168 hours|goldman_higher"
---

<objective>
Integrate the selective Goldman contract into persistence, activation, scheduling, and feed visibility.

Purpose: Make the exact adapter evidence independently enforceable at the Edge and database boundaries while preserving provider lifecycle truth and every sibling source.

Output: Tested observer/poller validation and forward migration 0048 with Goldman-only terminal, three-window activation, Active claims, durable evidence, and Active-feed aging.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-CONTEXT.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-RESEARCH.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-PATTERNS.md
@.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-01-SUMMARY.md
@.planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-SUMMARY.md

<interfaces>
Plan 01 provides `GoldmanHigherJobScopeEvidence`,
`GoldmanHigherObservationScopeEvidence`, and
`createGoldmanHigherScopeEvidence`, all discriminated by
`sourceKey='goldman_higher:roles'`. A valid aggregate has exactly two
population slice digests and `recentHours=168`; a valid job binds its row
source/external ID/posted_at to provider source/category/population/country
facts and one SHA-256 digest.

Migration 0048 is provisional until execution confirms 0047 is still the
latest migration. It must be a new transaction; migrations 0040–0047 are
deployed history and are never edited.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce selective evidence at observation, persistence, and lifecycle boundaries</name>
  <files>supabase/functions/observe-connectors/index.ts, supabase/functions/poll-tick/index.ts, web/tests/observe-connectors.test.ts, web/tests/poll-tick.test.ts, web/tests/lifecycle.test.ts</files>
  <read_first>
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-CONTEXT.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-PATTERNS.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-01-SUMMARY.md
    - supabase/functions/_shared/adapters/types.ts
    - supabase/functions/_shared/adapters/scope.ts
    - supabase/functions/_shared/adapters/goldman-higher.ts
    - supabase/functions/_shared/lifecycle.ts
    - supabase/functions/observe-connectors/index.ts
    - supabase/functions/poll-tick/index.ts
    - web/tests/observe-connectors.test.ts
    - web/tests/poll-tick.test.ts
    - web/tests/lifecycle.test.ts
  </read_first>
  <behavior>
    - "A complete positive Goldman observation with exactly two population slices and allowMissingClosure=false records one evidence digest; the same payload with true, missing, malformed, or mismatched evidence records none and degrades."
    - "poll-tick recomputes every Goldman evidence field/digest and requires job.postedAt equal evidence.postedAt before any job write."
    - "An existing Goldman job absent from a complete selective observation yields no closeIds and keeps status open."
    - "Direct Oracle absoluteUrl, full description, postedAt, and exact scope evidence land in one persistence snapshot."
  </behavior>
  <action>
    Implement D-07, D-16, D-17, and the runtime portion of D-18/D-19.
    Extend `isPositiveObservation` in `observe-connectors/index.ts` with an
    exact Goldman branch requiring complete/credible/positive/warning-free
    evidence, `expectedCount === jobs.length`, `allowMissingClosure === false`,
    the Goldman aggregate discriminator, literal source key, integer 168-hour
    scope, exactly the two independently reconciled population slice digests,
    and valid aggregate freshness/category/country/application digests. Send
    only bounded counts and the aggregate evidence digest to
    `record_connector_observation`; never persist raw provider payloads.

    Extend `validateBrandedPersistenceEvidence` in `poll-tick/index.ts` so
    Goldman also hard-requires `allowMissingClosure=false`. For every Goldman
    job call `createGoldmanHigherScopeEvidence` from the job row and supplied
    evidence, then compare every field and digest, including exact
    `job.postedAt === evidence.postedAt`, numeric provider source ID, category
    field/normalized label/term, U.S., recruiting type, selection mode, and
    recent hours. Preserve the existing normalized snapshot fields so the
    exact Oracle Apply URL, posting instant, nonpartial full description, and
    evidence persist atomically. Do not add a provider-specific close update;
    continue through `planCompanySync`.

    Add observer/poller negative tests for closure-enabled evidence, wrong
    slice count/enums, stale or changed postedAt, wrong Apply evidence,
    category-field drift, population drift, and digest drift. Add a named
    Goldman lifecycle test proving a healthy complete selective observation
    refreshes health but leaves an absent aged open row out of `closeIds`.
    Write failures before production changes.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- observe-connectors.test.ts poll-tick.test.ts lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Only exact complete closure-disabled Goldman aggregate evidence can reach record_connector_observation."
    - "Every persisted Goldman job's source, external ID, postedAt, U.S., category, population, Apply, full-detail, and digest facts are revalidated before write."
    - "Aged/absent Goldman IDs produce zero closeIds and do not change jobs.status or closed_at."
    - "Existing JPMorgan, Morgan Stanley, Workday, and public ATS test behavior remains unchanged."
    - "Focused tests exit 0."
  </acceptance_criteria>
  <done>Runtime persistence accepts only the exact selective Goldman evidence and cannot manufacture closure from age or absence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add forward-only Goldman activation and Active-feed authority</name>
  <files>supabase/migrations/0048_phase_03_10_goldman_higher.sql, web/tests/phase-03-10-migration.test.ts</files>
  <read_first>
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-CONTEXT.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-RESEARCH.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-PATTERNS.md
    - .planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-01-SUMMARY.md
    - supabase/migrations/0038_dashboard_feed_page_repair.sql
    - supabase/migrations/0040_phase_03_8_branded_connectors.sql
    - supabase/migrations/0043_phase_03_8_workday_candidates.sql
    - supabase/migrations/0044_phase_03_8_selective_workday_scope_evidence.sql
    - supabase/migrations/0045_phase_03_9_jpmorgan_oracle.sql
    - supabase/migrations/0046_phase_03_9_jpmorgan_catalog_repair.sql
    - supabase/migrations/0047_phase_03_9_jpmorgan_company_identity.sql
    - web/tests/phase-03-8-activation.test.ts
    - web/tests/phase-03-9-migration.test.ts
  </read_first>
  <behavior>
    - "Exact positive terminal proof admits only goldman_higher:roles as Experimental 0/3; precise Unsupported leaves it absent/unclaimable."
    - "Replay and same-server-window attempts do not advance; exactly three accepted distinct server windows produce Active 3/3."
    - "Only the exact Active Goldman source joins the ten-minute scheduled claim allowlist."
    - "An open Goldman job older than database clock minus 168 hours is absent only from p_lifecycle='active'; Applied/Dismissed history and provider status remain unchanged."
    - "Every protected sibling source key, evidence branch, function ACL, and RLS boundary remains present."
  </behavior>
  <action>
    Implement D-07 and D-16 through D-21 in new forward-only migration
    `0048_phase_03_10_goldman_higher.sql`; first confirm no migration newer
    than 0047 exists, and if one does, renumber this new migration and every
    phase reference without changing prior files. Use `begin`, local five
    second lock timeout, local sixty second statement timeout, and `commit`.
    Never edit migrations 0040–0047 or mutate hosted data manually.

    Repair only the Goldman catalog/company identity to canonical
    `https://higher.gs.com/results` while retaining board token/source key
    `goldman_higher:roles`. Replace `jobs_scope_evidence_check` by preserving
    every current branch and adding one exact-key Goldman JSON branch. Require
    the complete Plan 01 field set, literal 168 hours/two-population selection,
    allowed enums/terms, row `posted_at` equality, and PostgreSQL
    `extensions.digest(convert_to(...),'sha256')` byte parity with
    `createGoldmanHigherScopeEvidence`.

    Add service-role-only `public.finalize_goldman_higher_candidate` with
    `security definer`, `search_path=''`, advisory/row locking, replay/state
    checks, one exact positive admission path, and bounded precise Unsupported
    reasons for navigation, Higher contract, timestamp, population, category,
    country, Apply, page/count/detail, cap, timeout, or zero-result failures.
    Revoke it from `public`, `anon`, and `authenticated`; do not re-grant the
    historical shared branded terminal.

    Replace `public.record_connector_observation`,
    `public.claim_due_experimental_connectors`, and
    `public.claim_due_companies` from the exact latest definitions. Preserve
    all current JPMorgan/Workday/public ATS branches and add only literal
    `ats_type='goldman_higher'` plus
    `source_key='goldman_higher:roles'`: database server time, UUID replay
    rejection, one-minute same-window rejection, hard three-row cap,
    Experimental progress 0/3→3/3, deterministic zero-to-four-minute
    activation stagger, and exact ten-minute Active cadence. Retain
    service-role-only ACLs.

    Replace `public.dashboard_feed_page` from migration 0038's current
    signature/body and add a pre-pagination predicate: only when
    `p_lifecycle='active'` and `job.source='goldman_higher'`, require non-null
    `job.posted_at >= clock_timestamp() - interval '168 hours'`. Applied and
    Dismissed results remain accessible. Do not update `jobs.status`,
    `closed_at`, ranking, or user lifecycle data because age is visibility,
    not provider closure.

    Create source-text migration tests asserting exact catalog/company
    identity, JSON keys/digest, terminal ACL, server-window activation, claim
    key/cadence, Active-only feed aging, no lifecycle mutation, and literal
    preservation of JPMorgan, Morgan Stanley, Charles Schwab, every active
    Workday key, public ATS authority, function grants, and RLS-facing
    `auth.uid()` behavior.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- phase-03-10-migration.test.ts phase-03-9-migration.test.ts phase-03-8-activation.test.ts &amp;&amp; npm test -- observe-connectors.test.ts poll-tick.test.ts lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Migration 0048 is the only new schema file and prior deployed migrations are byte-unchanged."
    - "`finalize_goldman_higher_candidate` and shared observation/claim RPCs grant only service_role; only goldman_higher:roles gains new authority."
    - "Exactly three distinct accepted server windows activate Goldman; replay, same-window, fourth-row, wrong-source, wrong-state, anon, and authenticated paths fail."
    - "`dashboard_feed_page` hides aged open Goldman rows only from Active without changing provider or user lifecycle."
    - "All protected sibling source keys, evidence branches, ACLs, and RLS behavior remain asserted."
    - "Focused tests exit 0."
  </acceptance_criteria>
  <done>The forward schema safely prepares exact Goldman activation, scheduling, durable evidence, and truthful current-feed aging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Edge observation → service-role RPC | External provider evidence may advance activation only after exact validation |
| Poll result → jobs table | Durable evidence, content, timestamp, and Apply URL must be recomputed before write |
| Service role → activation/claim SQL | Privileged calls must be source-specific, replay-safe, locked, and capped |
| Database clock → Active feed | Time-based visibility must not corrupt provider or user lifecycle |
| New migration → shared source authority | Whole-object SQL replacement must preserve every sibling branch and ACL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.10-02-01 | Tampering | aggregate/job evidence | high | mitigate | Task 1 exact TypeScript recomputation before observation and persistence |
| T-03.10-02-02 | Elevation of Privilege | terminal/observation RPCs | high | mitigate | Task 2 exact source/state, service-role ACL, locks, replay/same-window/cap guards |
| T-03.10-02-03 | Tampering | feed aging | high | mitigate | Task 2 Active-only database-time predicate plus no-status-mutation tests |
| T-03.10-02-04 | Elevation of Privilege | sibling source allowlists | high | mitigate | Task 2 literal full-current-branch assertions and no shared-terminal regrant |
| T-03.10-02-05 | Spoofing | Unsupported/positive terminal | high | mitigate | Task 2 exact evidence digest/state transition and bounded terminal reason set |
| T-03.10-02-06 | Information Disclosure | user feed/RLS | high | mitigate | Task 2 preserves auth.uid/RLS-owned dashboard function behavior |
| T-03.10-02-07 | Repudiation | forward migration | medium | mitigate | New numbered transaction, immutable historical files, focused source-contract tests |
| T-03.10-02-SC | Tampering | dependency supply chain | low | accept | No dependency installation; migration and current repository tests only |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- Updated `isPositiveObservation` Goldman aggregate branch
- Updated `validateBrandedPersistenceEvidence` Goldman job-evidence branch
- New Goldman selective lifecycle regression cases
- New `supabase/migrations/0048_phase_03_10_goldman_higher.sql`
- New SQL function `public.finalize_goldman_higher_candidate`
- Updated SQL function `public.record_connector_observation`
- Updated SQL function `public.claim_due_experimental_connectors`
- Updated SQL function `public.claim_due_companies`
- Updated SQL function `public.dashboard_feed_page`
- Updated `public.jobs` constraint `jobs_scope_evidence_check`
- New `web/tests/phase-03-10-migration.test.ts`
</artifacts_this_phase_produces>

<verification>
- Observer, poller, and lifecycle tests prove evidence parity, closure disablement, and atomic persistence.
- Migration tests prove Goldman-only terminal/activation/claim/feed behavior and full sibling preservation.
- No production schema or function mutation occurs in this plan.
</verification>

<success_criteria>
- Local runtime and SQL independently enforce the same exact Goldman facts.
- Goldman can activate only through three real server windows and cannot close jobs from selective absence.
- Active feed recency is correct without altering provider or user history.
- All existing source authority and data boundaries remain intact.
</success_criteria>

<output>
Create `.planning/phases/03.10-goldman-sachs-selective-higher-monitoring/03.10-02-SUMMARY.md` when done.
</output>
