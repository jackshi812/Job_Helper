# Phase 03.1 Live Evidence

Captured: 2026-07-22
Project: `fjcsvajkkztvlrpdplwx`
Approved scope: migration `0029`, `verify-board`, `poll-tick`, and the resumable Paylocity verifier only.

## Deployment identity

- Migration `0028` was already remote; migration `0029` applied exactly once after the owner-approved `db push --linked`.
- Migration 0029 local SHA-256: `0bbbc1d8e6ddec3fb4a193e93fd2df0520d496cc8a0e104a60b9f345b22b57f2`.
- `verify-board`: ACTIVE version 15, `verify_jwt=true`, deployed asset SHA-256 `3f01d92035a94b7f79007428165dc74b3f3e10f475f4542af11ace02e185c4d6`.
- `poll-tick`: ACTIVE version 18, `verify_jwt=false`, deployed asset SHA-256 `18954b2171abc710aafbe8107b72e4b8cb6b62d0a879eda28826f87c8de1a1e4`.
- `score-tick`: unchanged ACTIVE version 6, `verify_jwt=false`.

## Activation and polling

- Exact company identity: source key `paylocity:global:d6628b21-949b-4400-a3d0-c9082bbf3eb1`.
- Company state: `active`; activation successes: `3`.
- Connector observations: `3`; clean complete positive reconciled observations: `3`.
- Paylocity jobs: `15`; distinct immutable external IDs: `15`; duplicates: `0`.
- The verifier’s controlled incomplete-observation journey completed without closing existing jobs or losing the prior successful health boundary.
- Final score cron state: active and restored.

## Scoring and feed

- Verifier sentinel: `COMPLETE`.
- Score-tick invocations: `1`.
- Paid scoring usage increased by exactly one verifier-owned request (entry `670`, final `671`).
- One actual Paylocity job traversed the existing user-job, preference, scoring, truthful-company, All/Focused feed contract.
- Disposable auth/profile/preferences/resume/extract/user_jobs/latch fixtures were deleted; residue assertion passed.
- No real-user rows were modified; no secrets, tokens, resume content, provider bodies, or raw user data are included here.

## Provider isolation and cleanup

- Capital One source key `workday:wd12:capitalone:Capital_One` remains active with three observations; no additional Workday identity was introduced.
- SuccessFactors company count remains `0` and remains unsupported/evidence-only.
- Cron pause/restore used exact precondition comparisons and active-only `cron.alter_job` mutation compatible with the hosted pg_cron permissions; final active state matched the entry state.
- Final pipeline heartbeat advanced through the legitimate poll.

## Local and hosted verification

- Local full gate: 35 test files / 480 tests passed; build and lint passed with only pre-existing warnings.
- Verifier dry-run: `network_calls=0 database_calls=0 auth_calls=0 paid_calls=0`.
- Verifier safety tests: 9/9 passed.
- Focused live-schema and identity regressions: 65/65 passed.

## Rollout corrections

- Corrected `verify-board` failure-path identity to use the reviewed Paylocity UUID source key.
- Mapped the live Paylocity feed schema (`title`, `displayUrl`, `jobLocation.name`) while retaining strict identity, URL, count, and content validation.
- Adapted verifier cron CAS to hosted pg_cron permissions and seeded all non-null disposable `user_jobs` fields explicitly.

## Blocking arrays

- restoration conflicts: `0`
- immutable drift: `0`
- unexplained diffs: `0`
- disposable residue: `0`
- paid requests over ceiling: `0`

Status: COMPLETE
