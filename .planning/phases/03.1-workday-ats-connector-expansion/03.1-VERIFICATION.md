---
phase: 03.1-workday-ats-connector-expansion
verified: 2026-07-28T20:19:17Z
status: human_needed
score: 8/10 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 8/10
  gaps_closed:
    - "The final local migration frontier now restores exact Paylocity admission and its ten-minute staged-activation window without changing the providers preserved from migration 0048."
    - "A final-frontier regression test now fails if a later migration supersedes the Paylocity repair or if the repair widens or changes pre-existing provider admission."
  gaps_remaining: []
  regressions: []
behavior_unverified_items:
  - truth: "A new or re-created exact Paylocity company earns three clean server-timed windows, becomes Active, and only then reaches scheduled polling."
    test: "Apply migrations through 0057 in an approved disposable or hosted database, submit three eligible observations in distinct server-timed Paylocity windows, and then claim the company for polling."
    expected: "Progress advances 1, 2, 3; the third observation changes activation_state to active and schedules next_poll_at; replayed, same-window, malformed, empty, or identity-drifted evidence does not advance progress."
    why_human: "The new tests inspect the final PL/pgSQL definition but do not execute the state transition. This re-verification was prohibited from schema pushes, hosted calls, and production mutation, and no release-bound evidence shows migration 0057 applied."
human_verification:
  - test: "Exercise exact Paylocity staged activation against a database whose applied migration frontier includes 0057."
    expected: "Three distinct clean server-timed windows produce progress 1/2/3, Active state on the third, and eligibility for the Active-only scheduler; rejected evidence leaves state unchanged."
    why_human: "Static source assertions cannot prove PL/pgSQL state-transition behavior, and database or hosted mutation was outside this verification's authority."
  - test: "In the current signed-in Dashboard, filter to The Only Facial and inspect a current Paylocity job after the deterministic-ranking replacement."
    expected: "The row shows truthful company identity, stored deterministic score/tier, a safe employer apply link, and ordinary provider-neutral Dashboard behavior."
    why_human: "The available Paylocity browser proof predates the deterministic-ranking replacement, and hosted/browser access was outside this verification's authority."
---

# Phase 03.1: SuccessFactors & Paylocity Connector Expansion Verification Report

**Phase Goal:** Add Paylocity Recruiting through its documented public feed and establish a bounded, allowlisted SAP SuccessFactors employer contract that either proves safe recurring ingestion or records an honest unsupported disposition, with staged activation, deduplication, non-destructive failures, scheduled polling, scoring, and dashboard delivery.

**Verified:** 2026-07-28T20:19:17Z  
**Status:** human_needed  
**Re-verification:** Yes — after commit `652ed45`

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Only the reviewed The Only Facial Paylocity board is recognized, and its public board UUID cannot substitute for the separate feed key. | ✓ VERIFIED | `provider-identities.ts` freezes one UUID-to-feed-key map; `detect.ts` performs exact recognition and server-side endpoint construction. Hostile URL fixtures passed. |
| 2 | Complete Paylocity Job Feed V2 snapshots normalize immutable jobs; malformed, drifted, oversized, duplicate, failed, or empty observations are closure-ineligible. | ✓ VERIFIED | `paylocity.ts` bounds transport, schema, identity, and counts. The focused adapter suite passed. |
| 3 | W. L. Gore and Statsforvalteren remain honest exact-employer `unsupported_with_reason` SuccessFactors dispositions with no executable provider surface. | ✓ VERIFIED | `03.1-SUCCESSFACTORS-EVIDENCE.md` records both exact proofs and `public_json_detail_unavailable`; registry/type tests passed. |
| 4 | Authenticated verification derives the exact Paylocity identity and evidence on the server, while active dispatch independently rejects identity drift before fetch. | ✓ VERIFIED | `verify-board` authenticates before provider work and derives RPC arguments; `connectors.ts` independently re-resolves exact dispatch identity. Focused forgery and zero-fetch tests passed. |
| 5 | A new or re-created exact Paylocity company earns three clean server-timed windows, becomes Active, and only then reaches scheduled polling. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Migration 0057 is now the final local `record_connector_observation` definition and restores exact Paylocity admission, the ten-minute window, replay/same-window rejection, third-window promotion, and service-role ACL. The new frontier tests pass, but they inspect SQL text rather than execute the transition; 0057 has no release-bound application evidence. |
| 6 | Failed, partial, malformed, drifted, or implausibly empty observations retain prior jobs and the prior success boundary and cannot authorize closure. | ✓ VERIFIED | Shared lifecycle and poll-tick paths only advance on complete credible evidence. Focused lifecycle tests passed, and historical live evidence records retained jobs and health after controlled incomplete evidence. |
| 7 | An already-Active exact Paylocity row remains scheduled, independently fail-closed, and deduplicated by immutable provider ID. | ✓ VERIFIED | The final stable claim includes Active Paylocity; dispatch rechecks exact identity; poll-tick upserts on `(source, external_id)`. Historical proof records 15 distinct jobs and zero duplicates. |
| 8 | Paylocity jobs use the common persistence, deterministic ranking, and feed contracts without provider-specific scoring or dashboard branches. | ✓ VERIFIED | Poll-tick writes normalized generic jobs; deterministic ranking and feed consume provider-neutral records. No Paylocity-specific dashboard path exists. |
| 9 | The exact Paylocity Edge bytes and prior hosted monitoring proof are release-bound rather than summary-only claims. | ✓ VERIFIED | Later release evidence binds deployed function hashes and migration history through 0050. Those function bytes still match locally; this historical proof does not claim that the new migration 0057 is deployed. |
| 10 | A current Paylocity job is visibly present in the signed-in Dashboard after the deterministic-ranking replacement. | ? UNCERTAIN | The current common code path is wired and earlier proof established feed eligibility, but no current release-bound Paylocity browser observation after ranking replacement was found. |

**Score:** 8/10 truths verified (1 present but behavior-unverified; 1 requires current visual confirmation)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `supabase/functions/_shared/provider-identities.ts` | Exact two-token Paylocity identity | ✓ VERIFIED | Frozen and imported by detection, dispatch, and verification. |
| `supabase/functions/_shared/detect.ts` | Exact public URL SSRF gate and mapped endpoint | ✓ VERIFIED | Exact host/path/UUID/slug recognition and separate server-owned feed key are wired and tested. |
| `supabase/functions/_shared/adapters/paylocity.ts` | Bounded normalized observation | ✓ VERIFIED | Substantive adapter; current SHA matches later deployed release evidence. |
| `supabase/functions/_shared/connectors.ts` | Closed registry verification and exact dispatch | ✓ VERIFIED | Verification and polling are both exact-identity gated. |
| `supabase/functions/verify-board/index.ts` | Authenticated staged activation caller | ✓ VERIFIED | Paylocity remains staged and invokes `record_connector_observation` with server-derived evidence. |
| `supabase/migrations/0057_restore_paylocity_staged_activation.sql` | Final exact Paylocity activation frontier | ⚠️ PRESENT, RUNTIME UNVERIFIED | Exists, is substantive, final in lexical migration order, restores only Paylocity relative to 0048, and preserves ACLs. No database execution or deployment evidence was authorized. |
| `web/tests/paylocity-activation-frontier.test.ts` | Final-frontier regression guard | ✓ VERIFIED | Enumerates the migration chain, pins 0057 as final, proves exact Paylocity identity/window/ordering/ACL text, and byte-compares the non-Paylocity definition with 0048. |
| `scripts/verify-paylocity.ts` | Safe dry-run/resumable hosted proof | ⚠️ PARTIAL | Safety and cleanup logic are substantive and dry-run passes, but its schema assertion still reads migration 0029 rather than the final frontier. The new frontier test now guards the repair independently. |
| `03.1-SUCCESSFACTORS-EVIDENCE.md` | Exact unsupported dispositions | ✓ VERIFIED | Both employers, proof limits, and explicit non-authorization are present. |
| `03.1-LIVE-EVIDENCE.md` | Earlier deployment and monitoring evidence | ✓ HISTORICAL EVIDENCE | Supports the prior rollout but cannot prove migration 0057 application or current ranking UI. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `detect.ts` | `provider-identities.ts` | Exact board lookup before endpoint construction | ✓ WIRED | Public board UUID resolves to one server-owned feed key. |
| `connectors.ts` | Paylocity adapter | Closed registry verification and polling | ✓ WIRED | Positive verification and exact active dispatch call the bounded adapter. |
| `verify-board/index.ts` | migration 0057 `record_connector_observation` | Server-derived staged evidence | ✓ STRUCTURALLY WIRED | The previous identity mismatch is repaired at the local migration frontier; transition execution remains unverified. |
| `claim_due_companies` | poll-tick / `pollConnector` | Active scheduled claim and common dispatch | ✓ WIRED | Active Paylocity remains in the stable claim set and exact dispatch gate. |
| poll-tick | `jobs` → deterministic ranking → `user_jobs` → feed | Provider-neutral persistence/ranking/feed | ✓ WIRED | No Paylocity-specific bypass exists. |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Paylocity adapter | `PollObservation.jobs` | Exact Job Feed V2 response | Historical live proof: 15 distinct jobs | ✓ FLOWING |
| Staged activation | activation observation | verify-board → final local RPC | SQL path is connected; transition not executed in this audit | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED |
| Scheduled ingestion | normalized jobs | Active claim → poll-tick → jobs | Historical proof plus current dispatch bytes | ✓ FLOWING FOR EXISTING ACTIVE ROW |
| Ranking/feed | deterministic `user_jobs` | generic open jobs → worker → Dashboard RPC | Provider-neutral code and earlier Paylocity feed proof | ✓ FLOWING; current visual row needs human confirmation |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Connector, lifecycle, activation, verifier, and final-frontier regression tests | `cd web && npm test -- tests/paylocity-activation-frontier.test.ts tests/activation-policy.test.ts tests/detect.test.ts tests/adapters.test.ts tests/public-connectors.integration.test.ts tests/source-observation.integration.test.ts tests/paylocity-verifier-safety.test.ts` | 7 files, 232 tests passed | ✓ PASS for local assertions |
| Capital One exact active dispatch regression | `cd web && npm test -- tests/workday-connector.integration.test.ts -t "dispatches only the exact active Capital One identity"` | 1 passed, 122 skipped during initial verification | ✓ PASS; no repair-file overlap |
| Paylocity verifier no-I/O gate | `node --experimental-strip-types scripts/verify-paylocity.ts --dry-run` | COMPLETE; network/database/auth/paid calls all 0 | ⚠️ PASS; schema oracle remains pinned to 0029 |
| Three-window PL/pgSQL state transition | Database execution through migration 0057 | Not run: no schema push, hosted action, or mutation authorized | ? SKIP → HUMAN |

### Probe Execution

No `probe-*.sh` file is declared. The phase's TypeScript verifier was run in `--dry-run` mode and performed zero external calls.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| DISC-07 | 01, 02, 03, 05 | Validate feasible additional platforms while preserving existing support | ⚠️ NEEDS HUMAN | The code frontier restores exact Paylocity activation and the differential test protects all pre-existing providers, but the transition and applied frontier remain unproven. |
| DISC-08 | 01, 02, 03, 05 | Monitor stable contracts; label all others honestly unsupported | ✓ SATISFIED | Exact Paylocity monitoring evidence exists; both SuccessFactors employers are explicitly unsupported and non-executable. |
| DISC-09 | 01, 02, 03, 04, 05 | Non-destructive degradation and staging before schedule | ⚠️ NEEDS HUMAN | Degradation safety is verified and the final SQL expresses the required transition, but no behavioral database check executes it. |

No orphaned Phase 03.1 requirement IDs were found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `web/tests/activation-policy.test.ts` | 4 | Historical migration 0029 remains its oracle | ⚠️ Warning | This test alone cannot detect final-frontier drift; the new enumerating frontier test now covers that gap. |
| `scripts/verify-paylocity.ts` | 941 | Dry-run validates migration 0029 | ⚠️ Warning | Dry-run can still report schema safety without inspecting the final function definition. |

No unreferenced `TBD`, `FIXME`, or `XXX` markers or stub patterns were found in the 0057 repair and its frontier test. `git diff --check 652ed45^ 652ed45` passed.

### Human Verification Required

#### 1. Paylocity Activation Transition and Applied Frontier

**Test:** In an approved disposable or hosted database whose applied frontier includes migration 0057, create/re-create the exact Experimental Paylocity row, submit three clean observations in distinct server-timed windows, and then invoke the scheduler claim. Also submit malformed, replayed, and same-window evidence.  
**Expected:** Clean observations advance 1/2/3; the third activates and schedules the row; the Active-only scheduler can claim it. Rejected evidence does not advance state.  
**Why human:** The local frontier tests are structural and do not execute PL/pgSQL. No schema, hosted, or production actions were authorized here.

#### 2. Current Paylocity Dashboard Row

**Test:** In the current signed-in Dashboard, filter to The Only Facial and inspect a current Paylocity job after deterministic ranking.  
**Expected:** Truthful company identity, stored deterministic score/tier, safe employer apply link, and provider-neutral Dashboard behavior.  
**Why human:** Available browser evidence predates the ranking replacement, and hosted/browser verification was outside this audit.

### Gaps Summary

The code blocker is closed. Commit `652ed45` adds a forward-only final migration that restores the exact Paylocity admission branch and ten-minute activation window while leaving the migration-0048 providers byte-identical. The final-frontier test closes the prior blind spot and all 232 focused assertions pass. No code gap or regression remains.

The phase cannot be marked `passed` yet because the repaired state transition has only structural evidence and migration 0057 has no release-bound application proof. A current post-ranking Dashboard observation also remains unavailable. Under the verification decision tree, those two human checks make the result `human_needed`, not `gaps_found`.

---

_Verified: 2026-07-28T20:19:17Z_  
_Verifier: the agent (gsd-verifier)_
