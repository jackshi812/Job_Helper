---
phase: 03.1-workday-ats-connector-expansion
plan: 11
type: execute
wave: 7
depends_on: ["03.1-10"]
files_modified: []
autonomous: false
requirements: [DISC-07, DISC-08, DISC-09]

must_haves:
  truths:
    - "Migrations 0028 through 0031 are applied to the hosted project only after explicit owner approval"
    - "Edge functions are deployed only after explicit owner approval, and only the functions this phase changed"
    - "Verification of this phase cannot pass on build and type checks alone, because those pass without the schema push"
    - "A rollback path is stated before any mutation is attempted"
  artifacts: []
  key_links:
    - "authored migrations -> applied hosted schema -> honest verification"
---

<objective>
Apply this phase's four authored migrations and deploy the changed edge functions to the hosted project, under explicit owner approval.

Purpose: **[BLOCKING] Schema push required.** This phase modifies Supabase migration files. Build and type checks pass without the push because the TypeScript types come from source, not from the live database, which creates a false-positive verification state. The phase cannot pass verification without this step.

**This plan is not autonomous.** Phase 02.1 established that hosted mutation and deployment are separately owner-approved checkpoints, following the Plan 07 and Plan 12 precedent. No task here may run without the owner's explicit approval at the checkpoint below.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-source-coverage-expansion/02.1-12-SUMMARY.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-08-SUMMARY.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-09-SUMMARY.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-10-SUMMARY.md
</context>

## Artifacts this phase produces (this plan)

No new symbols. This plan applies previously authored artifacts:

| Artifact | Applied by |
|---|---|
| `supabase/migrations/0028_paylocity_connector.sql` | `supabase db push` |
| `supabase/migrations/0029_workday_tenant_allowlist.sql` | `supabase db push` |
| `supabase/migrations/0030_closure_reason.sql` | `supabase db push` |
| `supabase/migrations/0031_workday_pilot_catalog.sql` | `supabase db push` |
| `verify-board`, `poll-tick` edge functions | `supabase functions deploy` |

<tasks>

<task type="auto">
  <name>Task 1: Assemble and present the pre-push evidence pack</name>
  <files></files>
  <read_first>
    - all four authored migrations in supabase/migrations/ numbered 0028 through 0031
    - .planning/phases/02.1-source-coverage-expansion/02.1-12-SUMMARY.md (the hosted-evidence discipline this plan follows)
    - the summaries for plans 04, 08, 09, and 10
  </read_first>
  <action>
    Do not mutate anything in this task. Produce the evidence the owner needs to decide.

    Confirm the local migration state is coherent: list the applied and pending migrations as the tooling reports them, and confirm the four new files are the only pending ones. If any earlier migration is unexpectedly pending, stop and report it rather than pushing — Phase 3 recorded a case where a pending migration had to be handled from an isolated checksum-matched project, and that history means an unexpected pending migration is a decision for the owner, not an obstacle to work around.

    Confirm the full local suite, the build, and the lint all pass on the exact commit that will be pushed, and record that commit hash. Verification must be bound to one immutable commit, following the Phase 3 release-binding precedent.

    Enumerate the destructive surface of each migration honestly. Name every constraint dropped and re-added, every function replaced, every column added, and every row inserted. Call out specifically that migration 0029 removes the clause that currently makes this provider permanently unschedulable, and that its replacement gates are the per-tenant claim proof in the same migration and the dispatch gate already shipped in plan 07.

    State the rollback path before any mutation. For each migration, describe what a reverting migration would have to do, and note which parts are not cleanly reversible — a dropped-and-re-added constraint is reversible, an inserted catalog row is reversible by delete, and a column addition is reversible by drop provided no rows carry a value. Record whether a database backup or point-in-time restore is available on the current plan.

    Confirm the migration preconditions can be met: migration 0029 refuses to apply if any job row exists for this provider. Query that count read-only and report it. If it is not zero, stop and report — that is a decision for the owner about job identity, not something to force.
  </action>
  <verify>
    <automated>npm --prefix web run test &amp;&amp; npm --prefix web run build &amp;&amp; npm --prefix web run lint</automated>
  </verify>
  <acceptance_criteria>
    - The pending-migration list, the passing suite, the commit hash, the destructive surface, the rollback path, and the read-only precondition count are all recorded.
    - No write, push, deploy, or hosted mutation of any kind was performed in this task.
  </acceptance_criteria>
  <done>The owner has everything needed to approve or refuse the push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Owner approval for hosted schema push and edge deployment</name>
  <action>Present the Task 1 evidence pack to the owner and halt. Perform no hosted mutation of any kind until the owner replies with an explicit approval. This checkpoint is never auto-approvable; an absent or ambiguous reply is a refusal.</action>
  <what-built>
    Four authored migrations and two changed edge functions, with a full green local suite on one recorded commit:
    - 0028 adds Paylocity to the closed source unions and the activation-evidence ledger
    - 0029 replaces the fixed Workday identity constraint with the eight reviewed tuples and makes the scheduled claim query a per-tenant proof check
    - 0030 adds the distinct closure-reason column
    - 0031 records the eight pilot companies in the coverage catalog, seven as unsupported pending live evidence
  </what-built>
  <how-to-verify>
    1. Read the evidence pack from Task 1: pending migrations, commit hash, destructive surface, rollback path, and the job-row precondition count.
    2. Confirm the precondition count for this provider's job rows is zero. If it is not, refuse — the external-identifier convention change would split job identity.
    3. Confirm you accept that migration 0029 removes the clause that currently makes this provider permanently unschedulable, and that its replacements are the per-tenant claim proof in the same migration plus the dispatch gate from plan 07. No tenant becomes schedulable by this push alone; each still needs its own three accepted windows.
    4. Confirm you accept the rollback posture as stated, including anything recorded as not cleanly reversible.
    5. Decide whether to approve the schema push, the edge deployment, both, or neither.
  </how-to-verify>
  <resume-signal>Type "approved: push and deploy", "approved: push only", or describe what to change. Anything else is a refusal and the plan stops here.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the approved push and deployment</name>
  <files></files>
  <read_first>
    - the owner's recorded approval from Task 2
    - the evidence pack from Task 1
  </read_first>
  <action>
    Perform only what the owner approved. If approval was refused or partial, do only the approved portion and record the rest as not performed.

    Apply the pending migrations with the Supabase CLI push command against the hosted project, with the access token supplied through the environment rather than an interactive prompt. Capture the full output verbatim, including every assertion the migrations raise, because those assertions are the evidence that the constraint parity, the write-shape probes, the reason-value probe, and the catalog count all held on real schema.

    If any migration aborts, stop immediately. Do not retry, do not amend a migration to get past an assertion, and do not push a subset. An aborted assertion is a genuine finding and belongs in the summary and back to the owner.

    Deploy only the edge functions this phase changed. Preserve each function's existing JSON web token verification setting exactly — the scheduled callers in this project use a dedicated shared-secret boundary rather than a user token, and changing that setting during a deployment would silently move the trust boundary.

    After the push, re-read the applied migration list and confirm all four are recorded as applied. Read back, read-only, the three things the migrations assert: that the Workday identity constraint contains eight tuples, that the claim function references the evidence ledger, and that the catalog holds eight rows for this provider.

    Do not perform any other hosted mutation. No company row is created, no activation state is changed, no live provider request is made. Gathering per-tenant evidence is plan 12's separately approved work.
  </action>
  <verify>
    <automated>echo "Manual-gated deployment task — evidence is the captured CLI output recorded in the summary"</automated>
  </verify>
  <acceptance_criteria>
    - Verbatim CLI output for every command run is recorded in the summary.
    - All four migrations are confirmed applied by a read-back, or the exact abort point is recorded.
    - The read-only post-push confirmations of the tuple count, the claim predicate, and the catalog count are recorded.
    - No company row, activation state, or live provider request was touched.
    - Anything the owner did not approve is recorded as not performed.
  </acceptance_criteria>
  <done>The approved schema push and deployment are complete with verbatim evidence, or the refusal and its scope are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository -> hosted database schema | Irreversible-in-part DDL against production data |
| local repository -> hosted edge runtime | Deployment that changes what the scheduled caller executes |
| deployment settings -> function authorization | Where a token-verification setting change would silently move a trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-11-01 | Tampering | unreviewed schema mutation against production | critical | mitigate | The plan is non-autonomous; a blocking human checkpoint with an evidence pack, a stated destructive surface, and a stated rollback path precedes any mutation |
| T-03.1-11-02 | Repudiation | false-positive verification without the push | high | mitigate | The push is a blocking prerequisite of phase verification, recorded as such, because build and type checks pass without it |
| T-03.1-11-03 | Tampering | job identity splitting on the identifier change | critical | mitigate | Migration 0029 refuses to apply if any job row exists for this provider; the count is also reported read-only to the owner before approval |
| T-03.1-11-04 | Elevation of privilege | deployment changing function authorization | high | mitigate | Existing per-function token-verification settings are preserved exactly; the scheduled callers keep their dedicated shared-secret boundary |
| T-03.1-11-05 | Elevation of privilege | a tenant becoming schedulable by the push alone | critical | mitigate | The push grants no scheduling: each tenant still needs its own three accepted windows at the claim boundary and must pass the independent dispatch gate; the checkpoint states this explicitly |
| T-03.1-11-06 | Tampering | working around a failed migration assertion | high | mitigate | An aborted assertion halts the task; retrying, amending the migration, or pushing a subset are all forbidden and the finding returns to the owner |
| T-03.1-11-07 | Information disclosure | credentials in captured output | medium | mitigate | The access token is supplied through the environment, never on a command line, and captured output is reviewed before it is written to the summary |
| T-03.1-11-08 | Denial of service | migration lock against live traffic | low | accept | Two-user private deployment, brief constraint operations, supervised window |
</threat_model>

<verification>
- Every hosted action in this plan was explicitly approved by the owner at the blocking checkpoint.
- Verbatim CLI evidence exists for each action performed.
- No live provider request was made and no company activation state was changed.
- The two `scripts/agent-dashboard.*` files remain untracked, and `.DS_Store` (which was already tracked and already modified before this phase began) is left exactly as this phase found it. This phase changes none of the three.
</verification>

<success_criteria>
The four migrations are applied and the changed edge functions deployed under explicit owner approval, with verbatim evidence and read-back confirmation — or the refusal is recorded honestly and nothing was mutated.
</success_criteria>

<output>
Create `.planning/phases/03.1-workday-ats-connector-expansion/03.1-11-SUMMARY.md` when done.
</output>
