---
phase: 03.1-workday-ats-connector-expansion
plan: 08
type: execute
wave: 5
depends_on: ["03.1-04", "03.1-07"]
files_modified:
  - supabase/migrations/0029_workday_tenant_allowlist.sql
  - web/tests/activation-policy.test.ts
  - web/tests/workday-connector.integration.test.ts
autonomous: true
requirements: [DISC-07, DISC-08, DISC-09]

must_haves:
  truths:
    - "The companies table admits a Workday row only for one of the eight reviewed identity tuples, with its source key asserted as a derivation of its own tokens (D-04, D-05)"
    - "The scheduled claim query admits a Workday row only when that specific company carries three accepted rows in the per-company evidence ledger (D-20, D-21)"
    - "The per-tenant SQL proof is re-derived from the ledger, not read from the denormalised counter, so it fails independently of the application dispatch gate"
    - "Auto-promotion for Workday is per tenant and never provider-wide"
    - "The migration refuses to apply if any Workday job row already exists, because the external-identifier convention changes in this phase"
  artifacts:
    - supabase/migrations/0029_workday_tenant_allowlist.sql
  key_links:
    - "companies_workday_identity_check -> reviewed allowlist tuples"
    - "claim_due_companies -> connector_observations count per company -> scheduling eligibility"
---

<objective>
Replace the fixed Capital One Workday constraint and the provider-wide claim exclusion with per-tenant identity constraints and a per-tenant proof check.

Purpose: this is the SQL half of D-21's defense in depth. It must become a per-tenant proof check and must never become a provider-wide allow.
Output: migration `0029_workday_tenant_allowlist.sql`, authored but not applied.

Applying this migration to the hosted project is plan 11's owner-approved blocking checkpoint. No autonomous task in this phase performs a hosted schema push.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-CONTEXT.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-PATTERNS.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-06-SUMMARY.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-07-SUMMARY.md
@supabase/migrations/0016_workday_experimental.sql
@supabase/migrations/0015_activation_windows.sql
@supabase/migrations/0013_source_coverage_catalog.sql
</context>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind |
|---|---|
| `supabase/migrations/0029_workday_tenant_allowlist.sql` | new migration |
| `companies_region_check` (redefined) | constraint |
| `companies_workday_identity_check` (redefined) | constraint |
| `public.claim_due_companies` (replaced) | function |
| `public.record_connector_observation` (replaced) | function |

<tasks>

<task type="auto">
  <name>Task 1: Replace the fixed-identity constraints with the reviewed tuple allowlist</name>
  <files>supabase/migrations/0029_workday_tenant_allowlist.sql</files>
  <read_first>
    - supabase/migrations/0016_workday_experimental.sql (whole file — the constraints being replaced, the claim function, the parity block, and the constraint comments)
    - supabase/functions/_shared/adapters/workday-allowlist.ts (the eight reviewed entries this migration must mirror exactly)
    - .planning/phases/03.1-workday-ats-connector-expansion/03.1-PATTERNS.md section "supabase/migrations/00XX_workday_tenant_allowlist.sql"
  </read_first>
  <action>
    Create `supabase/migrations/0029_workday_tenant_allowlist.sql` wrapped in an explicit transaction.

    Open the migration with a guard block that raises an exception if any job row already exists for this provider. The external-identifier convention for this provider changes in this phase to the list-level canonical path, and that is safe only because no such row exists. Making the assumption a hard precondition means a false assumption fails the migration rather than silently splitting job identity across two conventions. Record that reasoning in a comment above the guard.

    Drop and re-add the region constraint, widening it so a Workday row may carry any of the five pilot regions while preserving the existing regional value for the other provider that uses one, and preserving the null-allowed case.

    Drop and re-add the Workday identity constraint. The new form asserts two things together. First, that the board token, region, and site token tuple is one of the eight reviewed tuples, expressed as a values list so the set is readable in one place and mirrors the application allowlist. Second, that the source key equals the four-part key composed from that row's own region, board token, and site token — a derivation assertion rather than a literal, so no row can carry a key belonging to a different tenant (D-05).

    Deliberately remove the clause that forced the activation state to a fixed value inside this constraint. That clause is what made the provider permanently unschedulable, and D-19 through D-21 replace it with a per-tenant proof gate at two independent boundaries. Removing it here without adding both of those gates would be a provider-wide allow, so Task 2 must land in the same migration.

    Update the constraint comment to describe the reviewed tenant allowlist and to state that membership grants eligibility to gather evidence only, never scheduling. Do not describe the pilot as a ten-company round — eight is the honest current scope (D-03) and two further companies are outstanding from the owner.

    Retain the existing constraint-parity assertion block and the catalog-identity parity assertion unchanged in spirit, updating only what plan 04 already changed. This migration adds no new provider to the source unions.
  </action>
  <verify>
    <automated>npm --prefix web run test -- tests/workday-connector.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The migration begins with a transaction statement and ends with a commit.
    - The zero-row precondition exists and is blocking. A count-based grep is insufficient evidence here, because the parity assertions alone satisfy it. Assert it structurally instead: a test in `workday-connector.integration.test.ts` locates the guard block in the migration text and asserts all three of its properties — that it counts job rows for this provider, that a nonzero count raises rather than warns, logs, skips, or continues, and that the guard appears before the first statement that alters a constraint, so a violating database aborts before any DDL is applied. The guard's abort is the sole justification for changing the external-identifier convention in this phase; if it is absent or non-blocking, the identity change is unsafe and this task is not done.
    - The identity constraint lists eight tuples: a test in `workday-connector.integration.test.ts` parses the migration text and asserts the tuple count is exactly 8 and that each tuple matches an entry in the application allowlist module, so the two allowlists cannot drift.
    - The source key is asserted as a derivation, not a literal: the identity constraint contains a concatenation of the row's own columns rather than a quoted four-part key.
    - `npm --prefix web run test` passes in full.
  </acceptance_criteria>
  <done>Only the eight reviewed identity tuples can exist as Workday rows, each with a key derived from its own tokens, and the forced-state clause is gone with both replacement gates landing in the same migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the scheduled claim query a per-tenant proof check</name>
  <files>supabase/migrations/0029_workday_tenant_allowlist.sql, web/tests/activation-policy.test.ts, web/tests/workday-connector.integration.test.ts</files>
  <read_first>
    - supabase/migrations/0016_workday_experimental.sql lines 33-62 (the claim function and its comment)
    - supabase/migrations/0015_activation_windows.sql lines 130-215 (the three-row cap and the auto-promotion branch)
    - web/tests/activation-policy.test.ts (the negative Workday auto-promotion assertion and the exact provider triple)
    - .planning/phases/03.1-workday-ats-connector-expansion/03.1-CONTEXT.md decisions D-19 through D-23
  </read_first>
  <behavior>
    Assertions against the migration text and the policy model:
    - The claim function admits the stable providers unconditionally — the flat membership list as it stands after migration 0028, which is to say the stable set plus Paylocity. Carry that list forward; do not restore the pre-0028 list from the older migration text.
    - The claim function admits a Workday row only through an additional per-company predicate that counts accepted rows in the evidence ledger for that company identifier and requires at least three.
    - The claim predicate counts the ledger; it does not read the denormalised success counter on the company row.
    - The claim function retains its ordering by staleness, its row limit, and its skip-locked behavior, so one slow or failing tenant cannot delay or block another (D-22, D-23).
    - The auto-promotion branch admits Workday per tenant on that tenant's own third accepted window, and no branch promotes by provider alone.
    - The three-row cap, the replay guard, the same-window guard, the row lock, and the unique-violation handler are all unchanged.
    - The longer observation window for this provider is unchanged.
  </behavior>
  <action>
    Update the assertions in `activation-policy.test.ts` first and commit them red. The negative assertion that this provider is absent from the auto-promotion branch becomes a positive per-tenant assertion — this is a deliberate change, not a loosening, and the summary must say so. Add an assertion that the claim function's Workday predicate references the evidence ledger table and does not reference the company success counter, because reading the counter in both gates would collapse D-21's two independent gates into one.

    Then replace `claim_due_companies` in full in the migration. This function is replaced twice in this phase — plan 04's migration 0028 widened its flat list to include Paylocity, and this migration replaces it again. Read the 0028 version, not the pre-phase version, as the base; silently dropping Paylocity here would make it unclaimable again. Keep the existing common table expression shape, the staleness ordering, the row limit, and the skip-locked clause. Change the provider predicate from a flat membership list to a disjunction: membership in that flat list as of 0028, or this provider together with an existence or count subquery over the evidence ledger requiring at least three accepted rows for that same company identifier.

    Write the subquery against the ledger table directly. Re-deriving the proof here rather than trusting the counter is the entire point of the second gate: if the counter drifts, or if a future code path writes it, the claim boundary still refuses.

    Re-state the security context, the empty search path, and the revoke-and-grant pair exactly as the current definition does. Replace the function comment with one describing the per-tenant proof requirement and stating explicitly that this must never become a provider-wide allow.

    Then replace `record_connector_observation` in full, changing exactly one thing relative to plan 04's version: the auto-promotion branch admits this provider alongside the others, still conditioned on that company's own third accepted window. Every other line, including the cap, the guards, the lock, and the exception handler, is carried across unchanged.

    Add a comment above the auto-promotion branch recording that promotion to an active state is necessary but not sufficient for scheduling: the claim boundary independently re-derives the proof, and the application dispatch gate independently checks allowlist membership and identity consistency.

    Do not shard tenants across ticks in this migration. The claim function's staleness ordering, row limit, and skip-locked behavior already give per-tenant independence; the tick-level sharding decision (D-23) belongs with the scheduler in plan 09, where the claim batch size is read.
  </action>
  <verify>
    <automated>npm --prefix web run test -- tests/activation-policy.test.ts tests/workday-connector.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior assertion passes.
    - The claim predicate reads the ledger: `grep -c "connector_observations" supabase/migrations/0029_workday_tenant_allowlist.sql` returns at least 1.
    - The claim predicate does not read the denormalised counter. Assert this against the claim function body specifically, not the whole file: a test in `activation-policy.test.ts` extracts the text of the replaced claim function from the migration and asserts the counter column name is absent from it. A whole-file count is the wrong instrument here — the observation function this migration also restates legitimately names that column twice, once where it is selected into the returned row and once where it is assigned in the promotion branch, so a faithful restatement of the file contains two occurrences and any file-wide equality check would pressure the executor into deleting a needed line.
    - The claim function carries forward the flat provider list as widened by migration 0028: a test asserts Paylocity is present in the claim function body of this migration, so the second replacement cannot silently revert plan 04's widening.
    - The three-row cap survives: `grep -c "connector observation cap violated" supabase/migrations/0029_workday_tenant_allowlist.sql` returns 1.
    - The claim function retains its concurrency behavior: `grep -c "skip locked" supabase/migrations/0029_workday_tenant_allowlist.sql` returns 1.
    - Privileges are restated: the migration contains one revoke and one grant for each replaced function.
    - `npm --prefix web run test` passes in full.
  </acceptance_criteria>
  <done>A Workday row becomes claimable only when its own company identifier carries three accepted ledger rows, re-derived at the claim boundary independently of the application gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| companies table -> `claim_due_companies` | The SQL boundary that decides what the scheduler is allowed to poll |
| evidence ledger -> proof derivation | The authoritative record of per-tenant activation evidence |
| migration -> live schema | DDL that removes a constraint which currently makes this provider unschedulable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-08-01 | Elevation of privilege | removing the forced-state clause | critical | mitigate | The clause is removed only in the same migration that adds the per-tenant claim proof and the per-tenant promotion condition, and only after plan 07 landed the independent application gate; tests assert no provider-wide allow exists |
| T-03.1-08-02 | Elevation of privilege | unreviewed tenant becoming schedulable | critical | mitigate | The identity constraint admits only the eight reviewed tuples, a test asserts the SQL tuple set equals the application allowlist so the two cannot drift, and scheduling additionally requires that tenant's own ledger proof |
| T-03.1-08-03 | Spoofing | a row carrying another tenant's source key | high | mitigate | The identity constraint asserts the source key as a concatenation of the row's own region, board token, and site token rather than accepting a literal |
| T-03.1-08-04 | Elevation of privilege | both gates sharing one value | critical | mitigate | The claim boundary counts the ledger while the application gate reads the denormalised counter; a test asserts the claim predicate does not reference the counter |
| T-03.1-08-05 | Repudiation | job identity silently splitting | high | mitigate | The migration refuses to apply if any job row for this provider already exists, making the external-identifier change provably non-destructive |
| T-03.1-08-06 | Denial of service | one tenant delaying or blocking another | high | mitigate | The claim query retains staleness ordering, a row limit, and skip-locked selection, and the ledger and its unique constraint are already scoped per company |
| T-03.1-08-07 | Elevation of privilege | function privileges after replacement | high | mitigate | Both replaced functions restate their security context, empty search path, revoke from public, anonymous, and authenticated, and grant to the service role only |
| T-03.1-08-08 | Tampering | replay or same-window evidence | high | mitigate | The observation identifier replay guard, the company-and-window unique constraint, the row lock, and the three-row cap are carried across unchanged |
</threat_model>

<verification>
- `npm --prefix web run test` passes in full.
- The migration is authored only; no `supabase db push`, `supabase db reset`, or other hosted mutation is executed.
- The SQL tuple allowlist and the application allowlist module are proven equal by test.
- No artifact produced by this plan describes the pilot as a ten-company round.
- The two `scripts/agent-dashboard.*` files remain untracked, and `.DS_Store` (which was already tracked and already modified before this phase began) is left exactly as this phase found it. This phase changes none of the three.
</verification>

<success_criteria>
The database admits only the eight reviewed Workday identities with self-derived source keys, and grants scheduling only to a tenant whose own company identifier carries three accepted ledger rows — re-derived at the claim boundary, independently of the application dispatch gate.
</success_criteria>

<output>
Create `.planning/phases/03.1-workday-ats-connector-expansion/03.1-08-SUMMARY.md` when done.
</output>
