---
phase: 03.1-workday-ats-connector-expansion
plan: "05"
type: execute
wave: 5
depends_on: ["03.1-04"]
files_modified:
  - .planning/phases/03.1-workday-ats-connector-expansion/03.1-LIVE-EVIDENCE.md
autonomous: true
requirements: [DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "Hosted migration 0029 and deployed Edge bundles exactly match the owner-approved local artifacts before any live assertion runs."
    - "The exact The Only Facial board earns three distinct clean windows, becomes Active, is claimed on the existing cadence, and repeatedly ingests stable IDs without duplicates."
    - "A controlled incomplete observation retains prior Paylocity jobs and success health and cannot close jobs or advance activation."
    - "One actual Paylocity job traverses persistence, preferences, scoring eligibility, truthful company identity, and the existing dashboard feed contract without frontend special-casing."
    - "SuccessFactors remains unsupported-only and Capital One remains the sole exact Workday identity with its scheduled polling intact."
  artifacts:
    - path: ".planning/phases/03.1-workday-ats-connector-expansion/03.1-LIVE-EVIDENCE.md"
      provides: "Exact deployment, activation, polling, degradation, deduplication, scoring/feed, cleanup, and regression evidence"
  key_links:
    - from: "supabase/migrations/0029_paylocity_connector.sql"
      to: "hosted public.claim_due_companies"
      via: "approved linked schema push and remote migration parity"
      pattern: "0029"
    - from: "deployed verify-board"
      to: "connector_observations"
      via: "three real-user, database-timed warning-free windows"
      pattern: "activation_successes"
    - from: "deployed poll-tick"
      to: "public.jobs"
      via: "exact active Paylocity claim and common persistence"
      pattern: "paylocity"
    - from: "public.jobs"
      to: "existing dashboard feed"
      via: "preferences, score-tick, user_jobs, and common feed projection"
      pattern: "preferenceVisible|defaultVisible"
---

<objective>
Apply the explicitly approved production rollout and produce closure-safe, exact-identity evidence that Paylocity polls regularly and reaches the existing scored dashboard feed.

Purpose: Complete DISC-07 through DISC-09 for the Phase 03.1 extension while preserving SuccessFactors honesty, Capital One Workday, and real-user state.
Output: Hosted schema/function parity plus a redacted live evidence artifact with exact cleanup and zero unexplained residue.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-04-SUMMARY.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-SUCCESSFACTORS-EVIDENCE.md
@supabase/migrations/0029_paylocity_connector.sql
@scripts/verify-paylocity.ts
@.planning/phases/03-scoring-feed-notifications/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply only the approved schema and Edge rollout</name>
  <files>.planning/phases/03.1-workday-ats-connector-expansion/03.1-LIVE-EVIDENCE.md</files>
  <read_first>Explicitly approved 03.1-04-SUMMARY.md; Plan 03 summary; migration 0029 checksum; current linked migration/function inventory; supabase/config.toml JWT settings.</read_first>
  <action>Abort unless Plan 04 records the exact approval signal. Re-run full local tests/build/lint and verifier dry-run, confirm the worktree/commit set excludes unrelated files, then use the established non-TTY linked workflow: from `web`, run `npx supabase db push --linked`; require the remote migration list to show 0029 exactly once after 0028. Deploy only `verify-board` with default gateway JWT enforcement and `poll-tick` with `--no-verify-jwt` so its existing `x-cron-secret` boundary remains authoritative. Record local migration SHA-256, remote migration parity, function versions/deployment identifiers, JWT posture, and source-manifest hashes before any hosted verifier call. If auth fails, stop for a dynamic authentication checkpoint; never substitute dashboard SQL, manual migration application, or a broader deploy. Per D-21 and D-22, no frontend/Cloudflare deployment, new secret, schedule, SuccessFactors component, or Workday component is authorized.</action>
  <verify>
    <automated>cd web &amp;&amp; npx supabase migration list --linked &amp;&amp; npx supabase functions list</automated>
  </verify>
  <acceptance_criteria>Remote migration order and both function identities match the approved local artifacts, with verify-board JWT and poll-tick cron-secret posture preserved.</acceptance_criteria>
  <done>Hosted schema and exactly two Edge functions are current and identity-bound before live proof.</done>
</task>

<task type="auto">
  <name>Task 2: Earn activation and prove recurring closure-safe ingestion</name>
  <files>.planning/phases/03.1-workday-ats-connector-expansion/03.1-LIVE-EVIDENCE.md</files>
  <read_first>Verifier help/dry-run output; Plan 04 approved mutation envelope; exact hosted company/observation/job baselines; SuccessFactors evidence artifact.</read_first>
  <action>Run `node --env-file=scripts/.env --experimental-strip-types scripts/verify-paylocity.ts --resume` once per eligible server window. Honor PENDING and `next_eligible_at`; do not spoof time, insert ledger rows directly, or loop provider requests. Per D-16, require exactly three distinct warning-free, complete, positive, reconciled observations before Active. Per D-17 through D-19, prove the exact row is claimable through the common SQL function and independently accepted by dispatch, then invoke the existing poll-tick boundary and reconcile two real snapshots by immutable provider IDs with no duplicates. Run the verifier's controlled incomplete-observation journey and prove prior jobs remain open, last successful health is retained, and only the exact Paylocity company degrades. Query the hosted claim predicate and executable unions to confirm SuccessFactors remains absent; compare the Capital One source tuple, active state, evidence count, recent polling health, and claim eligibility to the entry baseline, allowing only legitimate concurrent scheduler advances.</action>
  <verify>
    <automated>node --env-file=scripts/.env --experimental-strip-types scripts/verify-paylocity.ts --resume</automated>
  </verify>
  <acceptance_criteria>The verifier reaches its activation/polling sentinel with three immutable windows, Active state, recurring exact-ID ingestion, zero duplicates, safe degradation, and unchanged provider isolation.</acceptance_criteria>
  <done>The exact Paylocity board is regularly pollable and non-destructive in production; no SuccessFactors or additional Workday source is executable.</done>
</task>

<task type="auto">
  <name>Task 3: Prove common scoring/feed delivery and seal the evidence envelope</name>
  <files>.planning/phases/03.1-workday-ats-connector-expansion/03.1-LIVE-EVIDENCE.md</files>
  <read_first>Verifier output/mutation envelope; Phase 3 verification; existing `cheapFilter`, `claim_scoring_work`, `score-tick`, `listFeed`, `preferenceVisible`, and `defaultVisible` contracts.</read_first>
  <action>Let the approved verifier continue only after an actual Paylocity job exists. Per D-20, use the invocation-owned disposable account/preferences/resume extract and the unchanged score-tick worker to route that provider job; permit at most one physical paid request, then query the same user_jobs/jobs/companies fields and predicates consumed by the existing dashboard. Prove truthful company name, preference pass, score/reasons, open status, safe apply URL, and All/Focused eligibility consistent with the resulting score—without a provider-specific frontend query or synthetic score. In `finally`, CAS-restore the score cron, delete every disposable user/profile/preference/resume/extract/user_jobs/usage fixture that is verifier-owned, and assert zero residue. Preserve durable Paylocity company/evidence/jobs/health and legitimate scheduler heartbeats. Write the redacted evidence artifact with deployment identities, three windows, job ID/count reconciliation, controlled failure, scoring request count/cost metadata, feed projection, cleanup, immutable drift, restoration conflicts, unexplained diffs, SuccessFactors disposition, and Capital One regression results; any nonempty blocking array fails the phase.</action>
  <verify>
    <automated>node --env-file=scripts/.env --experimental-strip-types scripts/verify-paylocity.ts --resume &amp;&amp; rg -n "COMPLETE|three distinct|duplicates: 0|closure retained|dashboard feed|paid requests: [01]|residue: 0|SuccessFactors|Capital One" .planning/phases/03.1-workday-ats-connector-expansion/03.1-LIVE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>One actual Paylocity job is visible through the existing normalized scoring/feed contract; cleanup is exact, cron is restored, paid calls do not exceed one, and all evidence blockers are empty.</acceptance_criteria>
  <done>Production proof is exact-release-bound, dashboard-complete, redacted, restoration-safe, and residue-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local approved artifacts → hosted Supabase | Deployment identity and migration order determine production authorization. |
| public Paylocity feed → hosted jobs | Provider drift or malicious content can affect durable lifecycle and user-visible scoring. |
| service-role verifier → auth/data/cron/OpenAI | Proof has privileged mutation and bounded paid-call capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-21 | Tampering | deployment identity | high | mitigate | Checksum migration 0029, verify linked order, record function versions/JWT posture/source manifests before assertions. |
| T-03.1-22 | Spoofing / Tampering | live provider identity | high | mitigate | Exact board/feed/root/row/source reconciliation on every window and poll; no generalized host or token input. |
| T-03.1-23 | Tampering | job closure and health | high | mitigate | Controlled incomplete proof must retain jobs and last success; complete positive reconciliation alone can close. |
| T-03.1-24 | Elevation of privilege | SQL claims and dispatch | high | mitigate | Hosted assertions prove both gates require exact Active Paylocity and retain the literal Capital One-only Workday branch. |
| T-03.1-25 | Information disclosure | evidence/logs | high | mitigate | Redact tokens, keys, resumes, provider bodies, user data, and raw errors; record only bounded codes, IDs, hashes, counts, and costs. |
| T-03.1-26 | Tampering / Repudiation | privileged verifier cleanup | high | mitigate | Entry/final pagination, owned IDs, CAS restoration, immutable-drift/unexplained-diff gates, and zero-residue audit. |
| T-03.1-27 | Denial of service / financial abuse | scoring proof | high | mitigate | One actual job, one disposable user, one physical request maximum, pause/drain, atomic reservation, and guaranteed cron restoration. |
</threat_model>

## Artifacts this phase produces

| Artifact | New symbol/content |
|---|---|
| `03.1-LIVE-EVIDENCE.md` | migration/function identities, three activation windows, recurring polling, dedupe, degradation, scoring/feed, cleanup, SuccessFactors and Capital One proof |
| Hosted schema | migration 0029 constraints, Paylocity activation eligibility, exact claim branch |
| Hosted Edge | updated verify-board and poll-tick bundles only |
| Hosted durable data | exact The Only Facial company, activation observations, legitimate Paylocity jobs and health |

<verification>
Require the verifier COMPLETE sentinel, empty restoration/conflict/drift/residue arrays, remote migration/function parity, exact provider/source unions, full local tests/build/lint after proof, and a final read-only query showing the exact Paylocity row remains Active and claimable while Capital One remains the only Workday identity.
</verification>

<success_criteria>
- Paylocity has repeated clean activation evidence, scheduled polling, stable deduplicated jobs, and safe failure recovery in production.
- An actual provider job traverses the unchanged preference/scoring/feed pipeline and qualifies for the existing dashboard contract.
- SuccessFactors remains exact unsupported evidence only.
- Capital One remains active under its exact identity and no Workday scope is widened or rolled back.
- The verifier leaves no disposable residue, restores cron state, leaks no secrets, and uses at most one paid call.
</success_criteria>

<output>
Create `.planning/phases/03.1-workday-ats-connector-expansion/03.1-05-SUMMARY.md` when done.
</output>
