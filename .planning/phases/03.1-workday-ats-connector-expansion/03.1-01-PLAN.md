---
phase: 03.1-workday-ats-connector-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/provider-identities.ts
  - supabase/functions/_shared/detect.ts
  - supabase/functions/_shared/adapters/paylocity.ts
  - supabase/functions/_shared/adapters/types.ts
  - web/tests/detect.test.ts
  - web/tests/adapters.test.ts
  - .planning/phases/03.1-workday-ats-connector-expansion/03.1-SUCCESSFACTORS-EVIDENCE.md
autonomous: true
requirements: [DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "Only The Only Facial's reviewed Paylocity board URL resolves to a provider request, and the public board UUID is never accepted as the feed key."
    - "A complete positive Paylocity snapshot normalizes immutable jobs, while every malformed, drifted, oversized, slow, duplicate, or empty observation is closure-ineligible."
    - "W. L. Gore and Statsforvalteren end with exact unsupported_with_reason evidence and create no executable SuccessFactors surface."
    - "Capital One Workday detection and adapter behavior remain unchanged."
  artifacts:
    - path: "supabase/functions/_shared/provider-identities.ts"
      provides: "Server-owned Paylocity board UUID to feed-key identity mapping"
      exports: ["PAYLOCITY_BOARD_UUID", "PAYLOCITY_SOURCE_KEY", "resolvePaylocityIdentity"]
    - path: "supabase/functions/_shared/adapters/paylocity.ts"
      provides: "Bounded Paylocity Job Feed V2 observation and normalization"
      exports: ["mapPaylocityJob", "pollPaylocity"]
    - path: ".planning/phases/03.1-workday-ats-connector-expansion/03.1-SUCCESSFACTORS-EVIDENCE.md"
      provides: "Exact non-executable SuccessFactors proof and dispositions"
  key_links:
    - from: "supabase/functions/_shared/detect.ts"
      to: "supabase/functions/_shared/provider-identities.ts"
      via: "exact public board identity lookup before endpoint construction"
      pattern: "resolvePaylocityIdentity"
    - from: "supabase/functions/_shared/adapters/paylocity.ts"
      to: "supabase/functions/_shared/adapters/types.ts"
      via: "normalized jobs and PollObservation"
      pattern: "PollObservation|NormalizedJob"
---

<objective>
Establish the exact Paylocity identity and bounded observation contract, and close the SuccessFactors proof honestly without creating executable provider code.

Purpose: D-01 and D-03 make Paylocity the only executable addition; D-09 through D-13 require the two named SuccessFactors employers to stop at evidence because the anonymous JSON-detail gate failed.
Output: Strict two-token Paylocity identity, safe URL detection, whole-snapshot adapter, fixtures, and the SuccessFactors unsupported evidence artifact.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-CONTEXT.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-RESEARCH.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-PATTERNS.md
@supabase/functions/_shared/detect.ts
@supabase/functions/_shared/adapters/recruitee.ts
@supabase/functions/_shared/adapters/types.ts

<interfaces>
Existing `detectAts(href): DetectResult` is the authoritative SSRF gate and `buildEndpoint(detected): string` constructs provider endpoints. Existing adapters return `PollObservation { jobs, completeness, credibleForClosure, pageCount, expectedCount?, warnings }`, and every `NormalizedJob` supplies immutable provider identity and snapshot fields. Preserve the current literal Capital One `workday` result branch exactly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the two-token Paylocity identity before any fetch</name>
  <files>supabase/functions/_shared/provider-identities.ts, supabase/functions/_shared/detect.ts, web/tests/detect.test.ts</files>
  <read_first>03.1-RESEARCH.md sections “Critical Planning Correction” and “Pattern 1”; current detect.ts and detect.test.ts; the Capital One identity assertions in workday-connector.integration.test.ts.</read_first>
  <behavior>
    - Exact canonical board URL with board UUID d6628b21-949b-4400-a3d0-c9082bbf3eb1 resolves to paylocity and source key paylocity:global:d6628b21-949b-4400-a3d0-c9082bbf3eb1.
    - Endpoint construction uses only the mapped feed key f3f28b00-201d-4fba-a7dd-532a9e558191.
    - Unknown UUIDs, the feed key in the board slot, slug drift, query strings, extra or encoded path segments, HTTP, credentials, ports, fragments, case ambiguity, and lookalike hosts reject before network access.
    - Every existing Capital One positive and hostile URL fixture remains byte-for-byte equivalent.
  </behavior>
  <action>Write failing fixtures first, then add an immutable server-owned identity module and an exact `/recruiting/jobs/All/{boardUuid}/{reviewedSlug}` parser for `recruiting.paylocity.com`. Per D-04 and D-05, detection stores the public board UUID while `buildEndpoint` resolves the separate mapped feed key; never accept a caller-supplied endpoint or feed key. Per D-02, do not refactor, generalize, or edit the exact Workday branch. Update bounded unsupported guidance to name the allowlisted Paylocity board without claiming generic Paylocity coverage.</action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- tests/detect.test.ts tests/workday-connector.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>The exact approved board produces the separately mapped feed URL; every non-approved identity returns unsupported; all Workday identity tests still pass.</acceptance_criteria>
  <done>Paylocity recognition and fetch authorization are distinct, server-owned, exhaustively fixture-tested, and incapable of widening Workday.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the bounded whole-snapshot Paylocity observation</name>
  <files>supabase/functions/_shared/adapters/paylocity.ts, supabase/functions/_shared/adapters/types.ts, web/tests/adapters.test.ts</files>
  <read_first>03.1-RESEARCH.md “Pattern 2” and Paylocity evidence table; adapters/recruitee.ts; adapters/types.ts; current adapter mapping and budget tests.</read_first>
  <behavior>
    - One fixed-origin request sends `Accept: application/json`, rejects redirects, times out after 15 seconds, and enforces declared and actual two-megabyte caps plus a 1,000-job cap.
    - Root displayName, every present companyName, every listUrl board UUID, safe Paylocity HTTPS URL, nonblank title, bounded field, parseable date, and unique jobId must reconcile.
    - jobId becomes the immutable decimal-string external ID; description plus requirements form the first-sight HTML/text snapshot; publishedDate precedes createdUtc.
    - Any bad sibling preserves safe rows as partial but denies closure; duplicate IDs, drift, malformed JSON/schema, content-type mismatch, cap breach, network failure, and zero jobs are non-credible with one bounded reason code.
  </behavior>
  <action>Write RED fixture cases before production code, then add `paylocity` only to `NormalizedJob['source']` and implement `mapPaylocityJob` plus `pollPaylocity`. Per D-06, a complete result requires positive whole-root reconciliation and safe siblings may survive only in a closure-ineligible partial observation. Per D-07, exact root and per-job company identity disagreement is drift. Per D-08, immutable provider IDs own repoll identity. Treat provider HTML/JSON as untrusted input, convert HTML to bounded plain text with the established adapter style, never log payloads, and keep all URLs on exact provider HTTPS hosts. Do not add `successfactors` to any executable union.</action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- tests/adapters.test.ts tests/source-observation.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>All happy-path mappings and every transport/schema/identity/cap/empty/partial fixture return the specified observation truth without exposing provider bodies.</acceptance_criteria>
  <done>The exact feed yields normalized Paylocity jobs, while every ambiguous observation retains jobs safely and cannot authorize closure.</done>
</task>

<task type="auto">
  <name>Task 3: Record the bounded SuccessFactors unsupported evidence</name>
  <files>.planning/phases/03.1-workday-ats-connector-expansion/03.1-SUCCESSFACTORS-EVIDENCE.md</files>
  <read_first>03.1-RESEARCH.md representative evidence and official API boundary; CONTEXT D-09 through D-13.</read_first>
  <action>Create an evidence matrix for W. L. Gore and Statsforvalteren i Troms og Finnmark with exact host, locale, company/site/category identity, HTML/session/CSRF bootstrap, JSON list request, observed pagination/count behavior, detail response type, missing JSON description, W. L. Gore duplicate-page ID, and Statsforvalteren locale-sensitive zero result. Per D-09, treat the employers as distinct contracts. Per D-10, record exact server-owned identities and reject tenant/host/locale generalization. Per D-11 and D-12, give both `unsupported_with_reason: public_json_detail_unavailable` and explicitly state that no DOM parser, browser automation, provider enum, schema union, company row, claim branch, dispatch entry, or scheduler hook is authorized. Per D-13, explain that a future proof would authorize only the exact employer identity proven.</action>
  <verify>
    <automated>test -f .planning/phases/03.1-workday-ats-connector-expansion/03.1-SUCCESSFACTORS-EVIDENCE.md &amp;&amp; rg -n "W\. L\. Gore|Statsforvalteren|unsupported_with_reason: public_json_detail_unavailable|JSON detail|HTML" .planning/phases/03.1-workday-ats-connector-expansion/03.1-SUCCESSFACTORS-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>Both exact employers have reproducible evidence and the artifact makes the non-executable disposition impossible to mistake for monitoring support.</acceptance_criteria>
  <done>SuccessFactors ends as truthful exact-employer evidence with no executable surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| pasted careers URL → detector | Untrusted user input approaches a privileged provider fetch path. |
| detector identity → Paylocity feed | Public board UUID and separate feed API key can be confused or substituted. |
| Paylocity response → normalized jobs | Untrusted JSON and HTML enter durable snapshot storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-01 | Spoofing / Information disclosure | detect.ts and provider-identities.ts | high | mitigate | Exact HTTPS host/path/UUID/slug allowlist, fixed-origin endpoint construction, redirect rejection, and zero-fetch hostile fixtures prevent SSRF. |
| T-03.1-02 | Spoofing | two-token Paylocity identity | high | mitigate | Store the board UUID as source identity and resolve the distinct feed key only from the immutable server map; reconcile root and row identity. |
| T-03.1-03 | Tampering | paylocity.ts response parsing | high | mitigate | Validate root and every row, unique IDs, bounded URLs/strings/dates, and deny closure for any sibling or identity drift. |
| T-03.1-04 | Denial of service | provider response | high | mitigate | One request, 15-second timeout, redirect denial, declared/actual byte limits, and job cap. |
| T-03.1-05 | Information disclosure | error/log boundary | medium | mitigate | Emit bounded stable codes only and never log response bodies, feed payloads, credentials, or user content. |
</threat_model>

## Artifacts this phase produces

| Artifact | New symbol/content |
|---|---|
| `supabase/functions/_shared/provider-identities.ts` | `PAYLOCITY_BOARD_UUID`, `PAYLOCITY_SOURCE_KEY`, `resolvePaylocityIdentity` and the exact frozen mapping |
| `supabase/functions/_shared/adapters/paylocity.ts` | `mapPaylocityJob`, `pollPaylocity`, bounded provider-error handling |
| `supabase/functions/_shared/adapters/types.ts` | executable `paylocity` normalized source member only |
| `supabase/functions/_shared/detect.ts` | exact Paylocity `DetectResult` branch and endpoint construction |
| `03.1-SUCCESSFACTORS-EVIDENCE.md` | two exact `unsupported_with_reason` dispositions |

<verification>
Run the focused tests above, then `cd web &amp;&amp; npm test -- tests/detect.test.ts tests/adapters.test.ts tests/workday-connector.integration.test.ts`. Confirm no package, database, Edge, scheduler, or production mutation occurred.
</verification>

<success_criteria>
- The one approved Paylocity board has a server-owned two-token identity and no unsafe variant can reach the network.
- Complete Paylocity snapshots normalize immutable jobs; every incomplete or implausible observation is closure-ineligible.
- Both SuccessFactors employers are documented unsupported and no executable SuccessFactors code or schema value exists.
- Existing Capital One behavior and assertions remain green.
</success_criteria>

<output>
Create `.planning/phases/03.1-workday-ats-connector-expansion/03.1-01-SUMMARY.md` when done.
</output>
