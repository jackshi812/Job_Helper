---
phase: 03.1-workday-ats-connector-expansion
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - supabase/functions/_shared/connectors.ts
  - supabase/functions/verify-board/index.ts
  - web/tests/public-connectors.integration.test.ts
  - web/tests/source-observation.integration.test.ts
  - web/tests/workday-connector.integration.test.ts
autonomous: true
requirements: [DISC-07, DISC-09]

must_haves:
  truths:
    - "Paylocity appears in the closed provider registry and is exhaustively type-checked against ProviderId"
    - "verify-board resolves a Paylocity board through the poll adapter and refuses anything that is not a complete observation"
    - "A newly verified Paylocity board is created by the server as staged, never as active, with the server owning activation state"
    - "The canonical careers URL and the deterministic source key for a Paylocity board are server-derived from the validated GUID, never from client free text"
  artifacts:
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/verify-board/index.ts
  key_links:
    - "detectAts -> verifyConnector -> providerRegistry.paylocity.verify -> pollPaylocity"
    - "verify-board staged activation -> record_connector_observation"
---

<objective>
Wire the Paylocity adapter into the closed provider registry, the verification path, and the staged-activation write path in `verify-board`.

Purpose: make a Paylocity board addable to the watchlist under the same server-owned identity and staged-activation discipline as every other provider.
Output: a `paylocity` registry entry, `verifyPaylocity`, and the updated registry-size assertions across three integration test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-CONTEXT.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-PATTERNS.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-01-SUMMARY.md
@supabase/functions/_shared/connectors.ts
@supabase/functions/verify-board/index.ts
</context>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind |
|---|---|
| `'paylocity'` member of `ProviderId` | type union member |
| `verifyPaylocity(detected, fetchImpl)` | function in `connectors.ts` |
| `providerRegistry.paylocity` | registry entry |
| `paylocity` member of `stagedActivationProviders` | set member in `verify-board/index.ts` |

<tasks>

<task type="auto">
  <name>Task 1: Add Paylocity to the closed provider registry and verification path</name>
  <files>supabase/functions/_shared/connectors.ts</files>
  <read_first>
    - supabase/functions/_shared/connectors.ts (whole file — `ProviderId`, `canonicalCareersUrl`, `deterministicSourceKey`, `verifySmartRecruiters`, `providerRegistry`, `pollConnector`)
    - supabase/functions/_shared/adapters/paylocity.ts (created in plan 01)
    - .planning/phases/03.1-workday-ats-connector-expansion/03.1-PATTERNS.md section "supabase/functions/_shared/connectors.ts (MODIFY — registry/router)"
  </read_first>
  <action>
    Add a `paylocity` member to `ProviderId`. Because `providerRegistry` is declared with `satisfies Record<ProviderId, ProviderConnector>`, the compiler will now require the registry entry — let that error guide the rest of the task.

    Add a `canonicalCareersUrl` branch returning the documented public careers URL for the validated GUID. The company-name segment of that URL is display-only and unknown at detection time; build the canonical URL from the fixed prefix and the GUID alone so one board can never produce two canonical URLs. `deterministicSourceKey` needs no branch — the existing generic form already produces a stable key from the provider and validated slug for a provider with no region.

    Add `verifyPaylocity` following the `verifySmartRecruiters` idiom exactly: call the poll adapter with the injected fetch, throw the first warning code when completeness is not complete, and derive the company name from the observation rather than from the pasted URL. Because the Paylocity adapter sets every job's `companyName` from the reviewed root display name (D-27), reading it from the first job is the correct server-owned source; fall back to the slug when the observation carries no jobs, matching the sibling verifiers.

    Add the `paylocity` registry entry: `verify` is the new function, `poll` calls the adapter with `company.board_token`. Follow the `recruitee` entry shape — Paylocity takes no `knownIds` because D-26 gives it descriptions inline and no detail fan-out.

    Leave `pollConnector` untouched in this plan. Paylocity needs no provider-specific dispatch gate; the existing activation check and registry-membership check already fail closed for it. The Workday branch of `pollConnector` is plan 07's work.
  </action>
  <verify>
    <automated>npm --prefix web run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web run build` type-checks, proving the `satisfies Record<ProviderId, ProviderConnector>` exhaustiveness constraint is met.
    - `grep -v '^\s*//' supabase/functions/_shared/connectors.ts | grep -c "verifyPaylocity"` returns at least 2 (declaration and registry reference).
    - Derive the phase base commit once, and use it for every diff-based criterion in this plan:
      `PHASE_BASE=$(git log --format=%H --reverse -- .planning/phases/03.1-workday-ats-connector-expansion/03.1-01-PLAN.md | head -1)`
      This resolves to `b4d7cc9`, the commit that created this plan set. Anchor on the first plan file, not on the phase directory: the directory also contains a `.continue-here.md` and a `.gitkeep` committed earlier in `501bca1`, so a directory-wide derivation resolves one commit too early. Either commit happens to be safe here, since no source file changes between them, but the plan-file anchor is the one that matches the stated hash and stays correct if that ever ceases to hold. It is the correct base because every commit between it and the start of execution touches planning documents only; no source file under `supabase/` or `web/` changes until this phase's tasks run. Diffing source paths against it therefore yields exactly this phase's source changes, and the value is stable regardless of how many commits land before or during the plan.
    - `pollConnector` is byte-identical to its pre-plan state: `git diff $PHASE_BASE -- supabase/functions/_shared/connectors.ts | grep -c "^[-+].*inactive_connector"` returns 0. Anchor to the phase base rather than `HEAD~1`: this plan has three tasks committing in sequence and wave-2 sibling plan 05 commits in parallel, so `HEAD~1` is not reliably this task's own commit and the criterion would compare against an arbitrary neighbour.
  </acceptance_criteria>
  <done>Paylocity is a first-class member of the closed registry with a verification path that demands a complete observation.</done>
</task>

<task type="auto">
  <name>Task 2: Stage Paylocity activation in verify-board</name>
  <files>supabase/functions/verify-board/index.ts</files>
  <read_first>
    - supabase/functions/verify-board/index.ts lines 60-130 (dependencies, `stagedActivationProviders`, `sourceKeyForDetection`, `boundedVerificationCode`)
    - supabase/functions/verify-board/index.ts lines 220-300 (verify-failure recording and the staged-activation write path)
    - .planning/phases/02.1-source-coverage-expansion/02.1-CONTEXT.md (the inherited staged-activation rule: the server, never the client, owns activation state)
  </read_first>
  <action>
    Add Paylocity to `stagedActivationProviders`.

    Record the reasoning in the plan summary: D-26 makes Paylocity the one documented public contract in this phase, which argues for immediate activation like Greenhouse. Stage it anyway. The documentation states no pagination model, no rate limits, and no terms, so there is no independent total to reconcile against and completeness rests entirely on locally checkable properties. Three independently server-timed clean windows are cheap insurance against a contract that is documented but unwitnessed by this project, and staging costs one polling interval. This is the conservative reading of the inherited Phase 02.1 rule and it is deliberately chosen over the faster path.

    Confirm the window interval this choice implies. The activation window function currently branches Workday to a longer interval and everything else to the shorter one; Paylocity inherits the shorter branch. Plan 04 sets that value in SQL — do not change it here, but note in the summary which branch Paylocity lands in so plan 04's author does not have to re-derive it.

    Leave `sourceKeyForDetection` alone in this plan. It currently duplicates `deterministicSourceKey` and pins the Workday key; collapsing that duplication is plan 07's work because it must land together with the per-tenant Workday derivation. Paylocity is already correct under the existing generic branch.

    Make no other change to the write path. The existing insert already sets the activation state from the staged-provider set and initialises the success counter at zero, then records the observation and re-reads the persisted row — that path is provider-generic and Paylocity inherits it unchanged.
  </action>
  <verify>
    <automated>npm --prefix web run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' supabase/functions/verify-board/index.ts | grep -c "stagedActivationProviders"` returns at least 2 and the set literal contains the new provider.
    - The change to this file is limited to the provider set literal: with `PHASE_BASE` derived as in Task 1, `git diff $PHASE_BASE -- supabase/functions/verify-board/index.ts | grep -E '^\+' | grep -v '^\+\+\+' | wc -l` returns 1. The base anchor is required, not cosmetic: a bare `git diff` reads only unstaged changes, so under this phase's commit-then-verify discipline it returns 0 the moment the task commits and the criterion would fail on correct work.
    - `npm --prefix web run build` and `npm --prefix web run lint` pass.
  </acceptance_criteria>
  <done>A newly verified Paylocity board is created staged, with the server owning activation state.</done>
</task>

<task type="auto">
  <name>Task 3: Update the registry-shape assertions across the three integration suites</name>
  <files>web/tests/public-connectors.integration.test.ts, web/tests/source-observation.integration.test.ts, web/tests/workday-connector.integration.test.ts</files>
  <read_first>
    - web/tests/public-connectors.integration.test.ts lines 195-230 (the registry array assertion and the migration-scope assertion)
    - web/tests/source-observation.integration.test.ts lines 90-110 (the registry array assertion)
    - web/tests/workday-connector.integration.test.ts lines 100-125 (the registry array assertion and the dispatch-rejection assertion)
    - .planning/phases/03.1-workday-ats-connector-expansion/03.1-PATTERNS.md table "Exact assertions that WILL break"
  </read_first>
  <action>
    Update the three exact `Object.keys(providerRegistry).sort()` array assertions to the new seven-element sorted array. Update them by adding the new member to the expected literal — do not relax any of them to a length check, a subset check, or a snapshot. The pattern map records that Phase 02.1 hit this exact regression class; the exactness is the protection.

    Leave the negative migration-scope assertion in `public-connectors.integration.test.ts` untouched. It asserts that the earlier public-connectors migration predates Workday and is scoped to that one migration file, so it stays true and must not be edited.

    Leave the Workday dispatch-rejection assertion in `workday-connector.integration.test.ts` untouched in this plan. It is still correct — Workday is still provider-wide banned at dispatch until plan 07 replaces that ban with a per-tenant proof check, and plan 07 owns the corresponding test change.

    Add one assertion in `source-observation.integration.test.ts` proving a degraded Paylocity observation produces no closure: build an observation with a warning and assert the sync plan yields an empty close list. This is the DISC-09 guarantee for the new provider and it belongs with the other per-provider degradation assertions in that file.
  </action>
  <verify>
    <automated>npm --prefix web run test</automated>
  </verify>
  <acceptance_criteria>
    - The full suite passes.
    - Each of the three registry assertions still compares against an explicit sorted array literal of exactly seven members — no length-only or subset assertion was introduced.
    - The Workday dispatch-rejection test and the migration-scope negative assertion are unchanged in this plan's diff.
  </acceptance_criteria>
  <done>All registry-shape assertions are deliberately updated and the new provider has a degradation-never-closes assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user -> `verify-board` | A signed-in user submits a URL that selects a server-owned identity |
| `verify-board` -> companies table | Server-owned write of identity and activation state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-03-01 | Elevation of privilege | activation state on insert | high | mitigate | The new provider is added to the staged set, so the server writes a staged state with a zero success counter; the client cannot supply an activation state and cannot reach an active state without three server-timed accepted observations |
| T-03.1-03-02 | Spoofing | company name on the created row | medium | mitigate | The company name is read from the verified observation, which the adapter derived from the reviewed root display name and cross-checked per job (D-27); the pasted URL's display segment is never used |
| T-03.1-03-03 | Tampering | source key and canonical careers URL | high | mitigate | Both are derived server-side from the validated GUID through the shared deterministic derivation; client free text never reaches either |
| T-03.1-03-04 | Information disclosure | verification error surface | medium | mitigate | The existing bounded-code filter collapses any provider error that is not a short lowercase code, so provider internals cannot leak into the response |
| T-03.1-03-05 | Elevation of privilege | registry exhaustiveness | medium | mitigate | `satisfies Record<ProviderId, ProviderConnector>` plus the three exact registry-array assertions make an unreviewed registry addition a compile or test failure |
</threat_model>

<verification>
- `npm --prefix web run test`, `build`, and `lint` all pass.
- No Workday source file, migration, or Workday-specific assertion is modified by this plan.
- The two `scripts/agent-dashboard.*` files remain untracked, and `.DS_Store` (which was already tracked and already modified before this phase began) is left exactly as this phase found it. This phase changes none of the three.
</verification>

<success_criteria>
Paylocity verifies through the poll adapter, is created staged by the server, and is exhaustively represented in the closed registry with exact assertions updated rather than loosened.
</success_criteria>

<output>
Create `.planning/phases/03.1-workday-ats-connector-expansion/03.1-03-SUMMARY.md` when done.
</output>
