---
phase: 03.1-workday-ats-connector-expansion
plan: "03"
type: execute
wave: 3
depends_on: ["03.1-02"]
files_modified:
  - supabase/migrations/0029_paylocity_connector.sql
  - web/tests/activation-policy.test.ts
  - web/tests/workday-connector.integration.test.ts
  - scripts/verify-paylocity.ts
  - web/tests/paylocity-verifier-safety.test.ts
autonomous: true
requirements: [DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "Migration 0029 admits only the exact Paylocity identity while preserving all shipped provider unions and the literal Capital One Workday claim branch."
    - "Three distinct server-timed clean Paylocity windows promote its company; replay, same-window, partial, empty, drifted, or forged evidence cannot."
    - "SQL claim eligibility and application dispatch both fail closed, and SuccessFactors remains absent from every executable database surface."
    - "A resumable verifier can prove real provider ingestion through one bounded disposable-user scoring/feed journey with exact cleanup and no unowned-row repair."
  artifacts:
    - path: "supabase/migrations/0029_paylocity_connector.sql"
      provides: "Paylocity constraints, activation eligibility, promotion, and exact scheduled claim authorization"
      contains: "claim_due_companies"
    - path: "scripts/verify-paylocity.ts"
      provides: "Dry-run and resumable hosted Paylocity activation, polling, scoring, and feed proof"
    - path: "web/tests/paylocity-verifier-safety.test.ts"
      provides: "Static and fixture safety proof for hosted mutation accounting and cleanup"
  key_links:
    - from: "supabase/migrations/0029_paylocity_connector.sql"
      to: "supabase/functions/_shared/connectors.ts"
      via: "same exact active Paylocity identity at SQL claim and application dispatch"
      pattern: "paylocity:global:d6628b21-949b-4400-a3d0-c9082bbf3eb1"
    - from: "scripts/verify-paylocity.ts"
      to: "supabase/functions/verify-board/index.ts"
      via: "real-user verification across three database-timed windows"
      pattern: "verify-board"
    - from: "scripts/verify-paylocity.ts"
      to: "supabase/functions/score-tick/index.ts"
      via: "one actual persisted Paylocity job through preferences, scoring eligibility, and common feed projection"
      pattern: "score-tick"
---

<objective>
Add the database authorization frontier and a mutation-accounted hosted verifier, while structurally proving that Paylocity cannot widen or roll back Capital One Workday behavior.

Purpose: D-16 through D-22 require database-time activation, exact dual scheduling gates, common persistence-to-dashboard delivery, migration 0029, and explicit separation between local proof and owner-approved production mutation.
Output: Migration 0029, SQL/Workday regressions, and a dry-run/resumable hosted verification command ready for the approval checkpoint.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-CONTEXT.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-RESEARCH.md
@.planning/phases/03.1-workday-ats-connector-expansion/03.1-02-SUMMARY.md
@supabase/migrations/0014_public_connectors.sql
@supabase/migrations/0015_activation_windows.sql
@supabase/migrations/0016_workday_experimental.sql
@supabase/migrations/0028_capital_one_recent_active.sql
@scripts/verify-source-coverage.ts
@scripts/verify-scoring-freshness.ts

<interfaces>
Migration 0028 is the authoritative `claim_due_companies(integer)` frontier: active stable providers are one branch and the literal Capital One tuple is a separate Workday branch, with nine-minute staleness and `FOR UPDATE SKIP LOCKED`. `record_connector_observation` is service-role-only, database-timed, locks one company after input validation, caps progress at three, and returns accepted/reason/progress/window boundaries/state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend migration 0029 without widening the Workday claim</name>
  <files>supabase/migrations/0029_paylocity_connector.sql, web/tests/activation-policy.test.ts, web/tests/workday-connector.integration.test.ts</files>
  <read_first>Migrations 0014, 0015, 0016, and especially final frontier 0028; activation-policy SQL/mirror tests; final Workday migration guard; RESEARCH Pattern 3.</read_first>
  <behavior>
    - Final company, job, and connector-observation constraints add paylocity and retain every shipped value; SuccessFactors remains non-executable.
    - Only board UUID/source key d6628b21-949b-4400-a3d0-c9082bbf3eb1, null region/site, and experimental/active state satisfy the Paylocity identity constraint.
    - Three distinct ten-minute clean windows promote Paylocity; replay, same-window, cap, malformed counts, warnings, and non-credible evidence preserve state.
    - claim_due_companies includes Paylocity in the stable branch while the literal Capital One tuple, nine-minute cadence, ordering, lock, grant, and separate Workday branch remain intact.
  </behavior>
  <action>Write the final-frontier structural tests first, importing migration 0029 rather than asserting an older migration. Per D-21, create only `0029_paylocity_connector.sql`. Rebuild the affected check constraints and `record_connector_observation` from their latest definitions, adding `paylocity` only where executable stable providers belong and adding an exact Paylocity identity constraint. Per D-16, retain database time, ten-minute windows, replay/same-window rejection, three-row cap, lock ordering, and service-role-only grants. Per D-17, extend the final 0028 claim function with Paylocity in the stable list but preserve the entire Capital One disjunct as a separately asserted literal. Per D-02, do not drop, recreate loosely, or generalize Capital One's identity constraint, promotion trigger, adapter filter, active state, cadence, or claim tuple. Add transactional parity assertions so a migration cannot silently widen source/provider sets.</action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- tests/activation-policy.test.ts tests/workday-connector.integration.test.ts tests/public-connectors.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>Migration 0029 is locally parseable and structurally proves exact Paylocity admission, service-role activation, stable claiming, and byte-specific Capital One preservation.</acceptance_criteria>
  <done>The local schema frontier authorizes one Paylocity connector and no SuccessFactors or additional Workday identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build a resumable, mutation-accounted hosted Paylocity verifier</name>
  <files>scripts/verify-paylocity.ts, web/tests/paylocity-verifier-safety.test.ts</files>
  <read_first>verify-source-coverage.ts dry-run/resume/window and mutation-baseline patterns; verify-scoring-freshness.ts disposable-user, cron pause/restore, one-call guard, snapshot pagination, cleanup and residue gates; Phase 3 verification feed truth.</read_first>
  <behavior>
    - `--dry-run` validates environment names, exact identities, mutation registry, command mode, cleanup ownership, and performs zero network/database/paid calls.
    - `--resume` uses a real-user verify-board call, exits PENDING before the next server window when progress is below three, and is idempotently resumable until exact active state.
    - Once active, one claimed poll ingests actual Paylocity rows without duplicates; an injected incomplete observation cannot close a sentinel or advance success health.
    - A disposable verifier account routes one actual Paylocity job through temporary preferences/resume extraction, at most one paid score-tick request, and the same user_jobs/feed predicates used by the dashboard; cleanup restores cron state and removes every disposable row/account.
  </behavior>
  <action>Create an import-safe script with mutually exclusive `--dry-run` and `--resume` modes. Per D-18 and D-19, snapshot the exact Paylocity company, observations, provider jobs, heartbeats, relevant cron rows, scoring usage, and owned verifier rows before mutation; classify durable provider jobs/company health/evidence separately from temporary claims and disposable scoring state. Per D-20, select one actual persisted Paylocity job and prove it passes through the existing preference filter, scoring worker, user_jobs record, truthful company identity, and feed query contract—never insert a synthetic score or special-case the frontend. Cap the paid path to one physical request, pause/drain and CAS-restore the existing scoring cron, use an invocation-unique disposable account, paginate all snapshots, delete only owned fixtures, and fail on immutable job drift, restoration conflicts, unexplained mutations, secret/log leakage, or residue. Per D-22, this task runs dry-run and local tests only; do not call the provider, linked database, Edge functions, auth admin, cron mutation, or OpenAI.</action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-paylocity.ts --dry-run &amp;&amp; cd web &amp;&amp; npm test -- tests/paylocity-verifier-safety.test.ts</automated>
  </verify>
  <acceptance_criteria>Dry-run proves zero I/O and the safety test confirms one-call maximum, exact ownership, resumability, pagination, CAS restoration, immutable provider-job handling, and exhaustive cleanup gates.</acceptance_criteria>
  <done>A reviewed verifier can later prove activation, polling, non-destructive failure, scoring eligibility, and existing dashboard delivery without risking real-user state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| migration SQL → hosted scheduler authority | A widened claim function can authorize unintended providers or tenants. |
| service-role verifier → production state | The verifier can mutate companies, jobs, activation evidence, auth users, and cron state. |
| provider job → scoring/feed | Untrusted content enters a paid worker and user-visible dashboard path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.1-11 | Elevation of privilege | 0029 claim and activation SQL | high | mitigate | Closed constraints, exact Paylocity tuple, service-role-only RPC/grants, active-state conjunct, and structural parity tests. |
| T-03.1-12 | Tampering | Capital One Workday migration state | high | mitigate | Base claims on 0028 and assert the literal source tuple, separate branch, cadence, locking, constraint, and promotion evidence remain intact. |
| T-03.1-13 | Elevation of privilege | SuccessFactors schema surface | high | mitigate | Tests assert no executable SuccessFactors provider/source/claim/promotion/dispatch value. |
| T-03.1-14 | Tampering / Repudiation | hosted verifier | high | mitigate | Entry snapshots, exact mutation registry, invocation ownership, pagination, immutable drift checks, CAS restoration, zero-residue and unexplained-diff gates. |
| T-03.1-15 | Information disclosure | scripts and evidence logs | high | mitigate | Redact tokens, provider bodies, resume text, secrets, and personal data; emit bounded identifiers/counts and hashed evidence only. |
| T-03.1-16 | Denial of service / financial abuse | score-tick proof | high | mitigate | One actual provider job, one disposable account, pause/drain, one physical request maximum, existing atomic reservation, and guaranteed cron restoration. |
</threat_model>

## Artifacts this phase produces

| Artifact | New symbol/content |
|---|---|
| `supabase/migrations/0029_paylocity_connector.sql` | exact Paylocity checks, Paylocity-aware `record_connector_observation`, final `claim_due_companies`, transactional parity assertions |
| `scripts/verify-paylocity.ts` | import-safe `dryRun`/`resume` hosted proof, PENDING/COMPLETE sentinels, mutation envelope |
| `web/tests/paylocity-verifier-safety.test.ts` | no-I/O dry-run, one-paid-call, ownership, restoration, redaction, and residue contract |
| `web/tests/workday-connector.integration.test.ts` | 0029 final-frontier Capital One no-widening regression |

<verification>
Run the focused commands above, then `cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint`. Confirm linked migration status is read-only and that no schema push, function deployment, provider request, auth mutation, scheduler mutation, or paid call occurred.
</verification>

<success_criteria>
- Migration 0029 is the sole new schema file and admits only exact Paylocity.
- Capital One remains the sole Workday tuple and keeps its shipped polling behavior.
- Local tests cover activation forgery, SQL widening, cross-employer lifecycle safety, and verifier cleanup.
- The hosted verifier is dry-run proven and ready for an explicit owner checkpoint.
</success_criteria>

<output>
Create `.planning/phases/03.1-workday-ats-connector-expansion/03.1-03-SUMMARY.md` when done.
</output>
