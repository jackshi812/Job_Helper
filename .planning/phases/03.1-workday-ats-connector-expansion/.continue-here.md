---
context: phase
phase: 03.1-workday-ats-connector-expansion
task: 4
total_tasks: 11
status: in_progress
last_updated: 2026-07-21T02:43:02.013Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints came from direct provider failures and must remain structurally enforced.

- [ ] CONSTRAINT: Paylocity has two non-interchangeable UUIDs — the public board UUID returned deceptive HTTP 200 empty placeholders when used as the Job Feed key; only exact server-owned board UUID → reviewed feed-key mapping may authorize a fetch.
- [ ] CONSTRAINT: SuccessFactors failed the executable contract — both tested employers require HTML/session bootstrap and HTML details for scoring-quality descriptions; keep the provider evidence-only and absent from every executable enum, schema union, claim, registry, and dispatch surface.

**Do not proceed until both constraints are explained in the resume understanding check.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Board UUID used as feed key | Direct Paylocity controls returned HTTP 200 placeholder roots with no jobs, which can masquerade as a healthy empty board. | blocking | `resolvePaylocityIdentity` exact allowlist maps the reviewed board UUID to a separate feed key; empty remains closure-ineligible. |
| SuccessFactors support by HTML fallback | Live boards exposed JSON lists without descriptions; full details and CSRF bootstrap require HTML parsing prohibited by D-12. | blocking | Evidence artifact records `unsupported_with_reason`; tests and plans forbid adding `successfactors` to executable provider/schema surfaces. |
| Accidental Workday widening | New provider migrations can copy an older claim predicate or add Workday to a broad union, stopping or widening the shipped Capital One contract. | advisory | Plan 03 starts from migration 0028 and structurally asserts the literal Capital One tuple remains the sole Workday authorization branch. |

<current_state>

Phase 03.1 was rescoped, researched, replanned, and independently plan-checked. Five replacement plans exist. Plan 01 is complete and committed; Plans 02–05 are incomplete. The user paused immediately after Plan 01 returned and before the orchestrator ran its independent Wave 1 post-merge build/test gate.

</current_state>

<completed_work>

- Rescoped Phase 03.1 to Paylocity plus bounded SuccessFactors evidence; removed all new Workday expansion. Planning commit: `5e6f77b`.
- Plan 01 implemented exact two-token Paylocity identity and URL authorization (`fe3d2ef`, `68fa536`).
- Plan 01 implemented the bounded whole-snapshot Paylocity adapter (`e9b5667`, `05a3c08`).
- Recorded W. L. Gore and Statsforvalteren as `unsupported_with_reason`, with no executable SuccessFactors surface (`654caf2`).
- Created the Plan 01 summary and marked roadmap progress 1/5 (`4269edd`).
- Executor verification passed: full suite 451/451, focused suite 140/140, production build, and lint with one pre-existing warning.
- Capital One Workday implementation remained unchanged; unrelated workspace files remained untouched.

</completed_work>

<remaining_work>

1. Run the orchestrator's independent Wave 1 post-merge gate and spot-check Plan 01 summary/files/commits.
2. Execute Plan 02: closed registry, authenticated staged verification, lifecycle, and exact dispatch integration.
3. Execute Plan 03: migration `0029_paylocity_connector.sql`, Capital One regression structure, and resumable hosted verifier with dry-run safety tests.
4. Stop at Plan 04 and present the exact production mutation inventory for explicit owner approval or decline.
5. Only after approval, execute Plan 05: schema/function rollout, three server-timed activation windows, recurring polling/dedup/failure evidence, one bounded scoring/feed proof, and cleanup.

</remaining_work>

<decisions_made>

- Paylocity executable support is limited to The Only Facial's exact reviewed board UUID mapped server-side to its separately published Job Feed V2 key.
- One exact feed-enabled employer is sufficient for this phase; other Paylocity boards require their own reviewed public feed key later.
- SuccessFactors proof stays in the phase evidence artifact and does not create Watchlist catalog rows or executable provider values.
- Migration numbering begins at 0029 because `0028_capital_one_recent_active.sql` is already shipped.
- Dashboard delivery uses the existing normalized persistence, preference, scoring, and feed pipeline; no frontend/provider special case is authorized.
- No production mutation or paid call occurs before Plan 04's explicit decision checkpoint.

</decisions_made>

<blockers>

- None for local execution of Plans 02 and 03.
- Later human checkpoint: Plan 04 requires explicit approval before schema push, Edge deployment, activation, scheduler effects, or the one-paid-call proof.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current position and exact next action.
2. `.planning/phases/03.1-workday-ats-connector-expansion/03.1-01-SUMMARY.md` — completed Wave 1 implementation and verification.
3. `.planning/phases/03.1-workday-ats-connector-expansion/03.1-02-PLAN.md` — next executable plan.
4. `.planning/phases/03.1-workday-ats-connector-expansion/03.1-CONTEXT.md` — locked decisions D-01 through D-22.
5. `.planning/phases/03.1-workday-ats-connector-expansion/03.1-RESEARCH.md` — verified two-token Paylocity and unsupported SuccessFactors findings.
6. `.planning/phases/03.1-workday-ats-connector-expansion/03.1-PATTERNS.md` — exact code and migration analogs.
7. `supabase/migrations/0028_capital_one_recent_active.sql` and `web/tests/workday-connector.integration.test.ts` — shipped Capital One boundary to preserve.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat URL recognition, a board UUID, or HTTP 200 as Paylocity fetch authorization; require the exact server-owned mapping and positive identity evidence.
- Do not add `successfactors` to `ProviderId`, normalized job sources, SQL unions, claims, dispatch, or scheduler paths.
- Do not copy claim SQL from migration 0016; migration 0029 must preserve migration 0028's exact Capital One disjunct.
- Do not skip the independent Wave 1 post-merge gate just because the executor already ran its own full suite.
- Do not stage or commit `.DS_Store`, `scripts/agent-dashboard.mjs`, or `scripts/agent-dashboard.test.mjs`.

## Infrastructure State

- No servers, watchers, async external jobs, or local processes are active from this workflow.
- No Phase 03.1 production schema, Edge function, scheduler, activation, provider, auth, job, or paid-scoring mutation has occurred.
- Existing production Capital One remains on its previously shipped exact fixed Workday contract and regular polling schedule.
- Working tree contains only the pre-existing unrelated `.DS_Store` modification and two untracked agent-dashboard scripts outside this handoff.

<context>

Resume sequentially on the main worktree. Plan 01 is trustworthy but its orchestrator-level post-wave check was interrupted; run that first. If it passes, proceed with Plan 02, then Plan 03. The phase deliberately separates local work from production: Plan 04 is the hard owner checkpoint and Plan 05 must not start from implied or stale approval.

</context>

<next_action>

Start with: run the independent post-wave build/test gate for completed Plan 01, verify `03.1-01-SUMMARY.md` and the three created files/commits, then resume `$gsd-execute-phase 03.1` at Plan 02.

</next_action>
