---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 08
type: execute
wave: 5
depends_on:
  - 05.1-05
  - 05.1-06
files_modified:
  - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-21 and D-22: release approval depends only on verified Plan 05/06 implementation, exact release source/build inputs, current target/build-setting preflight, and a fresh owner receipt; Plans 04/13 are not prerequisites."
    - "D-23: approval covers only migration 0064, the score-tick closure, and one Git-triggered Dashboard release; 0062/0065, retention, deletion, cleanup, history, maintenance, scheduler/secrets, and manual calls are excluded."
    - "The rejected f3cac981 payload and its response remain immutable audit history and are not reusable for this release."
    - "Generated local web assets are build-environment outputs, not pre-deployment release authority; only Plan 09 seals the actual deployed asset hashes."
    - "Migration 0064 remains local and non-authorizing until the exact new digest-bearing owner response is sealed."
  artifacts:
    - ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md contains one new canonical payload and one exact fresh owner receipt."
  key_links:
    - "Plan 05/06 summaries + exact release commit/tree + 0064 + score-tick closure + tracked web build-input closure -> new payload digest"
    - "Supabase/Git/Cloudflare targets + Cloudflare build-setting identity -> approved target envelope"
    - "Exact new owner response -> receipt digest -> Plan 09 one-time authority"
---

<objective>
Create and obtain explicit owner approval for one exact release containing only migration 0064, score-tick, and the Dashboard tracked source/build-input closure (D-21, D-22, D-23).

Purpose: Bind production authority to exact source, local proofs, provider build settings, targets, commands, scope, and a fresh non-reusable owner response without treating environment-dependent local output as the deployed artifact.
Output: One new immutable approval packet/receipt; the rejected packet remains unchanged and no deployment, Git push, provider write, worker invocation, or production mutation occurs.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-05-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-06-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-RELEASE-APPROVAL.md
@supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
@supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql
@supabase/functions/_shared/deterministic-worker.ts
@supabase/functions/score-tick/index.ts
@web/src/pages/Dashboard.tsx
@web/src/lib/supabase.ts
@web/.env.example
@web/package.json
@web/package-lock.json
@web/vite.config.ts
@web/tsconfig.json
@web/tsconfig.app.json
@web/tsconfig.node.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a fresh source-and-target-bound release payload</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md</files>
  <read_first>
    - `05.1-05-SUMMARY.md` and `05.1-06-SUMMARY.md`: local test receipts, source blobs, no-deploy assertions, and exclusions.
    - `05.1-CONTEXT.md`: D-01-D-23, especially D-21/D-22/D-23 supersession and deferral.
    - `05.1-03-SUMMARY.md`: current 0063 deployment identity and excluded 0062 boundary.
    - `05.1-08-RELEASE-APPROVAL.md`: rejected packet at SHA-256 `d414358d4857afa3f2ba713b0324bcc4b64d70813c99d5dadc60e0bd6af6baae`; preserve it byte-for-byte and reuse no payload or response bytes.
    - `web/src/lib/supabase.ts`, `.env.example`, package/lock, Vite, and TypeScript configs: public build-variable key contract and tracked web build-input boundary.
  </read_first>
  <action>
    Verify the rejected approval artifact still has SHA-256 `d414358d4857afa3f2ba713b0324bcc4b64d70813c99d5dadc60e0bd6af6baae` and commits `c85f182`, `20c7a89`, and `4376770` still exist. Never edit, copy forward, reseal, or summarize that rejected packet or its response. Create a distinct fresh artifact. Fail closed unless Plans 05 and 06 have passing summaries, their declared tests/build can be rerun, migration 0064 remains undeployed, and all release files resolve from one clean reachable `release_source_commit`. Bind the exact commit, tree, parent, normalized origin URL, origin/main preflight SHA and fast-forward relation, Plan 03 plan blob `5b3e7908ed7f26ca5785bf798b0f0d7e8f6ea621`, unchanged migration 0063, and exact 0064 blob/SHA-256 with 0062 excluded.

    Build canonical source inventories from that exact commit for migration 0064; the complete recursive repository-local `score-tick` import closure including `supabase/functions/_shared/deterministic-worker.ts`; relevant tests/verifiers; and the complete tracked web build-input closure. The web closure contains every tracked file under `web/src/**` and `web/public/**`, plus `web/index.html`, `web/.env.example`, `web/package.json`, `web/package-lock.json`, `web/vite.config.ts`, `web/tsconfig.json`, `web/tsconfig.app.json`, and `web/tsconfig.node.json`; bind each path/blob/SHA-256, the aggregate closure hash, and the exact local command `npm --prefix web run build`. Require the public build-variable key set to be exactly `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY` as consumed by `web/src/lib/supabase.ts`. Exclude `web/dist/**`, `web/node_modules/**`, ignored `.env*` values, process-environment values, and every generated filename/hash from approval authority.

    Before target access, rerun the Plan 05 cached-image local verifier, focused Plan 06 tests, and the exact production web build without pull/download/install. Record sanitized command/result/log digests and compiler/Vite identities. Record only PASS and the expected public variable key names for the local build; local build success proves the tracked inputs compile and is not an immutable deployed-artifact claim.

    Perform bounded sanitized read-only preflight of Supabase project `fjcsvajkkztvlrpdplwx`, the current score-tick deployment/JWT/cron-secret posture, origin/main, and the established Cloudflare account/project `job-helper`/source repository/production branch `main`/domain `job-helper-qs9.pages.dev`. Require hosted migration history through 0063 with 0062/0064 absent. Query and canonically bind Cloudflare's production build-setting identity: root directory, build command, destination directory, source repository/branch linkage, production compatibility settings that affect the build, and the exact public build-variable key set. If the provider safely exposes either public value, store only a keyed SHA-256 and never its value; inability to read a value is not an approval blocker when the exact key set and build settings are proven. Do not compare provider values with ignored local `.env.local`, reproduce the provider environment, or create another environment-provenance/authentication loop. Any target/build-setting drift blocks packet creation.

    Create a fresh isolated migration view containing exact 0001-0061, 0063, and 0064 only. From repository-root cwd bind literal/resolved `web/node_modules/.bin/supabase`, SHA/version, `SUPABASE_TELEMETRY_DISABLED=1`, and a linked dry run that proposes exactly 0064 once. Bind Plan 09's exact sole schema push, exact sole `score-tick` deployment preserving `--no-verify-jwt` and the existing secret boundary, and exact sole non-force fast-forward push of `release_source_commit` to `origin/main`. Bind one read-only Cloudflare Git-build observation window of 900 seconds with a 15-second poll interval, attempt limits of one, forward-only stop/recovery rules, and zero manual function/queue/worker invocations.

    Write one new canonical payload region with exact source/test/target/build-setting/command hashes and exactly three allowed effects: apply `0064_delta_deterministic_ranking_and_batch_staging.sql` once; deploy the exact recursive `score-tick` closure once with `--no-verify-jwt` and the existing secret boundary; and fast-forward the approved commit once so the established Cloudflare Git integration produces one web release. Explicitly exclude 0062, 0065, every other migration/function/web diff, retention/session/deletion/cleanup/history/maintenance/vacuum, scheduler or secret changes, paid AI, manual function/queue/worker calls, retries, rollback, force push, and manual Cloudflare deployment (D-23). Set `predeployment_generated_asset_authority: NONE`, `approval_status: AWAITING_OWNER_APPROVAL`, compute a new `approval_payload_sha256` over canonical LF bytes, and print only `APPROVE 05.1-08 RELEASE &lt;approval_payload_sha256&gt;` as the accepting signal.
  </action>
  <acceptance_criteria>
    - Dependencies are exactly Plan 05 and Plan 06; no Plan 04/13/retention dependency exists.
    - Payload binds exact release commit/tree, 0064, score-tick closure, tracked web source/build inputs, local PASS proofs, current targets/Cloudflare build settings, exact commands, one-attempt limits, and forward-only recovery.
    - No local generated asset path, filename, size, or hash is approval authority or a pre-deployment equality gate.
    - 0062/0065 and all retention/cleanup/history/deletion scope are explicitly excluded.
    - The rejected artifact/commits remain byte-for-byte audit history and no prior payload/response is reused.
    - This task performs read-only target preflight only and no production/Git/provider mutation.
  </acceptance_criteria>
  <verify>
    <automated>test "$(shasum -a 256 .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-RELEASE-APPROVAL.md | awk '{print $1}')" = d414358d4857afa3f2ba713b0324bcc4b64d70813c99d5dadc60e0bd6af6baae &amp;&amp; git cat-file -e c85f182^{commit} &amp;&amp; git cat-file -e 20c7a89^{commit} &amp;&amp; git cat-file -e 4376770^{commit} &amp;&amp; SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-delta-local.mjs &amp;&amp; npm --prefix web test -- Dashboard.test.tsx feed.test.ts resume-routing-source.test.ts deterministic-ranking-worker-runtime.test.ts deterministic-ranking-source.test.ts migration-0064-delta-ranking-and-batch-staging.test.ts &amp;&amp; npm --prefix web run build &amp;&amp; test -s .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md &amp;&amp; rg -F 'approval_status: AWAITING_OWNER_APPROVAL' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md &amp;&amp; rg -F 'allowed_migrations: [0064]' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md &amp;&amp; rg -F 'predeployment_generated_asset_authority: NONE' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md &amp;&amp; git diff --check -- .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md</automated>
  </verify>
  <done>A new canonical source/build/target-bound payload is ready for a fresh owner decision and authorizes nothing by itself.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Review and decide the fresh release payload</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md</files>
  <what-built>A new immutable payload for exactly migration 0064, score-tick, and the Dashboard tracked source/build inputs plus Cloudflare build settings, with generated local assets and all retention/cleanup/history work outside approval authority.</what-built>
  <action>Present the unchanged new canonical payload to the repository owner and accept only the exact digest-bearing response produced at this checkpoint. Do not infer approval from silence, generic assent, the rejected response, or any earlier receipt.</action>
  <how-to-verify>
    1. Review `05.1-08-FRESH-RELEASE-APPROVAL.md`, especially release source/tree, 0064, score-tick and web build-input closures, tests/build PASS, Supabase/Git/Cloudflare targets/build settings, exact commands, allowed effects, exclusions, and forward-only stop conditions.
    2. Confirm generated local asset filenames/hashes are not approval authority; Plan 09 will seal the actual provider-built deployment/index/asset manifest after the one approved push.
    3. Confirm 0062/0065, retention, deletion, cleanup/history, scheduler/secret changes, manual function/queue/worker calls, retries, rollback, force push, and manual Cloudflare deployment are not authorized.
    4. If every byte/scope/target is acceptable, respond with the exact new signal printed in the artifact: `APPROVE 05.1-08 RELEASE &lt;approval_payload_sha256&gt;`. The rejected response, any prior receipt, generic approval, altered digest, silence, or response from another actor is rejection.
  </how-to-verify>
  <resume-signal>Paste the exact new digest-bearing approval signal, or say `defer`.</resume-signal>
  <verify>
    <automated>rg -F 'approval_status: AWAITING_OWNER_APPROVAL' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md</automated>
    <human-check>The owner reviews the exact new payload and returns its new digest-bearing signal or defers.</human-check>
  </verify>
  <done>A fresh exact owner decision is available for Task 3 without any deployment effect.</done>
</task>

<task type="auto">
  <name>Task 3: Seal only the fresh owner receipt without deploying</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md</files>
  <read_first>
    - `05.1-08-FRESH-RELEASE-APPROVAL.md`: unchanged new payload bytes/digest and awaiting status.
    - The raw current checkpoint response: exact actor-provided bytes only.
  </read_first>
  <action>
    Recompute the unchanged new payload digest and reject any drift. Accept only the exact raw response produced at this checkpoint by the repository owner containing the new payload SHA-256. Append a canonical receipt region with actor, UTC timestamp, raw-response SHA-256, payload SHA-256, exact allowed/excluded effects, and `approval_status: APPROVED`; compute a receipt SHA-256. Do not read the rejected response as checkpoint input, copy it, reinterpret it, or use any earlier containment/provenance/cleanup approval. If deferred or mismatched, record `approval_status: NOT_APPROVED` in the fresh artifact and stop. Perform no deployment, push, provider write, function/worker call, or credential persistence.
  </action>
  <acceptance_criteria>
    - Receipt is fresh, actor-bound, new-payload-bound, non-reusable after any drift, and exact-scope.
    - Plan 09 can independently recompute every authority field, including Cloudflare build settings and the absence of pre-deployment asset authority.
    - Rejected artifact bytes and response lineage remain unchanged and non-authorizing.
    - No external mutation occurs.
  </acceptance_criteria>
  <verify>
    <automated>rg -F 'approval_status: APPROVED' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md &amp;&amp; rg -F 'allowed_migrations: [0064]' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md &amp;&amp; rg -F 'retention_calls_authorized: 0' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md &amp;&amp; test "$(shasum -a 256 .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-RELEASE-APPROVAL.md | awk '{print $1}')" = d414358d4857afa3f2ba713b0324bcc4b64d70813c99d5dadc60e0bd6af6baae &amp;&amp; git diff --check -- .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md</automated>
  </verify>
  <done>The exact new owner response is sealed as Plan 09's sole release authority, or the release remains unapproved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local source/tests -> release payload | Mutable source/build inputs must be frozen without elevating generated local output to authority. |
| Hosted target/build settings -> approval | Read-only provider state may drift or expose public configuration values. |
| Owner response -> deployment authority | Replayed, rejected, or generic approval must not authorize changed scope. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05.1-08-01 | Spoofing | owner approval | critical | mitigate | Require the exact current actor response bound to the new canonical payload SHA-256; reject every prior response. |
| T-05.1-08-02 | Tampering | release payload | high | mitigate | Bind commit/tree/blobs, 0064, score-tick and tracked web input closures, tests, targets, build settings, commands, dry run, and exclusions. |
| T-05.1-08-03 | Elevation of Privilege | release scope | high | mitigate | Exact three-effect allowlist and zero authority for 0062/0065/retention/cleanup/history/scheduler/secrets/manual calls/retries/rollback. |
| T-05.1-08-04 | Information Disclosure | provider preflight | high | mitigate | Process-only credentials; persist key names and optional public-value hashes only, never values or secret-bearing output. |
| T-05.1-08-05 | Tampering | web artifact authority | high | mitigate | Exclude local generated assets from approval authority; Plan 09 hashes the unique provider-built deployment and verifies the production domain against it. |
| T-05.1-08-SC | Tampering | tool/dependency supply chain | low | accept | No install/update; verify repository CLI, lockfile, exact cached images, source commit, and tool hashes. |
</threat_model>

<verification>
The fresh approval artifact must independently validate payload/receipt hashes, exact 0064/function/tracked-web-build-input scope, Cloudflare build-setting and target identities, one-attempt commands, no pre-deployment generated-asset authority, and all D-23 exclusions.
</verification>

<success_criteria>
- A fresh owner receipt approves only exact 0064, score-tick, tracked web source/build inputs, and one Git-triggered Cloudflare release.
- Migration 0064 remains undeployed until Plan 09.
- The rejected artifact remains unchanged; no prior payload/response is reused and no retention/cleanup authority exists.
</success_criteria>

<output>
Create `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-SUMMARY.md` only after a valid new receipt is sealed. Record the fresh artifact path, payload/receipt hashes, exact release source/build-input/target/build-setting/command/exclusion identities, the rejected artifact SHA as non-reusable history, and `production_mutations: 0`; never copy credentials, generated local asset hashes, the rejected response, or imply deployment.
</output>
