---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
  - web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts
  - supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
  - scripts/verify-phase-05-1-containment-local.mjs
  - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json
autonomous: true
requirements: []
requirement_note: "None — operational stabilization"

must_haves:
  truths:
    - "Per D-01 and D-19, an idle ranking owner whose only missing open jobs have permanent-dismissal tombstones creates zero new_job runs and zero ranking items on every repeated enqueue call."
    - "Per D-01 and D-19, one genuinely new open job without a current-revision user_job or matching tombstone still qualifies its affected owner for exactly one new_job run; after that run publishes, a second enqueue is a no-op."
    - "Per D-01 and D-05, containment preserves the deployed full-snapshot run construction and the migration-0052 complete deterministic publication contract; it does not introduce delta ranking or restore paid/background AI scoring."
    - "Per D-04, the forward migration replaces only enqueue_deterministic_new_jobs and performs no ranking-history deletion, queue repair, storage maintenance, or production data cleanup."
    - "Before any hosted approval or deployment, a fresh ephemeral local Supabase/Postgres applies exact migrations 0001-0061 plus 0063, excludes 0062, and a pgTAP integration invokes the real enqueue and finalizer functions with transaction-rolled-back synthetic fixtures."
    - "The tested migration, Vitest source test, SQL integration test, isolated runner, source commit/tree, exact passing commands/output digests, resolved CLI identity, and all three preserved Phase 5 stash identities are recorded in a machine-checkable containment evidence artifact for the later owner-approval gate."
    - "The evidence isolates 0063 as the sole containment release artifact and records pre-existing local migration 0062 as excluded because diagnosis found hosted schema only through 0061; later approval/deployment must fail closed rather than bundle 0062."
    - "No production deployment, linked migration application, worker invocation, queue mutation, hosted write, or dashboard change occurs in this plan."
  artifacts:
    - path: "supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"
      provides: "Forward-only tombstone-aware replacement of enqueue_deterministic_new_jobs"
    - path: "web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts"
      provides: "Static migration structure, privilege, scope, and publication-preservation regressions"
    - path: "supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql"
      provides: "Real-schema pgTAP coverage of enqueue/finalize behavior, isolation, privileges, and publication preservation"
    - path: "scripts/verify-phase-05-1-containment-local.mjs"
      provides: "Fail-closed isolated local Supabase setup, exact migration-manifest enforcement, test execution, and teardown"
    - path: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json"
      provides: "Hash- and commit-bound tested containment candidate for Plan 05.1-02 approval"
  key_links:
    - from: "supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"
      to: "public.user_job_dismissals"
      via: "owner plus provider source/external_id anti-join in both qualification and projection seeding"
      pattern: "user_job_dismissals|source|external_id"
    - from: "supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"
      to: "public.enqueue_deterministic_new_jobs(integer)"
      via: "same signature, SECURITY DEFINER boundary, bounded owner lock, existing run/item construction, and service-role-only grant"
      pattern: "create or replace function public.enqueue_deterministic_new_jobs"
    - from: "web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts"
      to: "supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"
      via: "raw migration contract and forbidden-scope assertions"
      pattern: "0063_dismissal_tombstone_ranking_containment.sql"
    - from: "supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql"
      to: "public.enqueue_deterministic_new_jobs(integer) and public.finalize_deterministic_ranking_run(uuid)"
      via: "real ephemeral Postgres calls over synthetic owner/job/user_job/tombstone/run fixtures after exact 0001-0061-plus-0063 migration application"
      pattern: "enqueue_deterministic_new_jobs|finalize_deterministic_ranking_run"
    - from: "scripts/verify-phase-05-1-containment-local.mjs"
      to: "supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql"
      via: "repository-root invocation of the pinned CLI with --workdir on every isolated start/reset/test/stop command"
      pattern: "web/node_modules/.bin/supabase|--workdir|test db"
    - from: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json"
      to: "supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"
      via: "canonical path, SHA-256, Task-1 commit/tree identity, and passing test command"
      pattern: "migration_sha256|source_commit|source_tree"
---

<objective>
Implement and test the containment-only forward migration so permanent-dismissal tombstones cannot qualify as new ranking work, repeated ticks create no phantom work, and one genuine arrival still enqueues exactly once (D-01, D-04, D-05, D-19).

Purpose: Stop the confirmed tombstone feedback loop without bundling the later ranking redesign, worker batching, retention, dashboard, dismissal, deletion, or production rollout work.
Output: One forward migration, one static regression test, one real-SQL integration test with an isolated local runner, and one immutable-identity containment evidence record for the next owner-approval plan.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@.planning/debug/job-copilot-performance.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-PLAN-OUTLINE.md
@supabase/migrations/0032_deterministic_ranking.sql
@supabase/migrations/0048_phase_03_10_goldman_higher.sql
@supabase/migrations/0052_decouple_resume_routing.sql
@supabase/migrations/0062_phase_03_11_asset_manager_payments_workday.sql
@supabase/config.toml
@web/tests/deterministic-ranking-migration.test.ts
@web/tests/deterministic-ranking-snapshot.integration.test.ts
@web/tests/resume-routing-migration.test.ts

<interfaces>
- The effective enqueue interface remains `public.enqueue_deterministic_new_jobs(batch_size integer default 25) returns table (initialized_count integer, seeded_count integer)` and remains callable only by `service_role`.
- Migration 0048 stores permanent intent as `public.user_job_dismissals(user_id, source, external_id)` and its insert trigger suppresses recreation of a matching `user_jobs` projection. Containment must use that durable provider identity, not a transient `job_id`.
- An arrival is genuine for containment eligibility only when `jobs.status = 'open'`, no owner/current-active-revision `user_jobs` projection exists, and no owner/source/external_id dismissal tombstone exists.
- The qualifying owner scan remains idle/current-revision-only, ordered, bounded by 1..25, and locked with `FOR UPDATE SKIP LOCKED`.
- Once a genuine arrival qualifies an owner, existing full-open-universe run and item construction remains unchanged in this release. Migration 0052's `finalize_deterministic_ranking_run(uuid)` remains the publication authority and is not redefined.
- Plan 05.1-02 consumes `05.1-01-CONTAINMENT-EVIDENCE.json` and `05.1-01-SUMMARY.md`; it must re-resolve every recorded hash and Git identity before requesting approval.
- The diagnosis observed hosted migration parity only through 0061 while local `0062_phase_03_11_asset_manager_payments_workday.sql` remained pending. The evidence must distinguish that excluded pre-existing migration from the sole 0063 containment candidate; this plan does not choose or execute the later isolated deployment mechanism.
- Local containment proof runs from the canonical repository-root cwd through literal CLI path `web/node_modules/.bin/supabase`; every CLI subprocess, including version, start, reset, test, and stop, receives exact environment `SUPABASE_TELEMETRY_DISABLED=1`. The isolated runner records that environment contract, resolved executable path, SHA-256, version, canonical command templates, exact 0001-0061-plus-0063 manifest, output digest, and teardown result. This local proof is mandatory before production can be the target of any SQL execution.
</interfaces>
</context>

<prohibitions>
- Do not edit any deployed migration, including 0032, 0048, or 0052; add only the forward 0063 migration.
- Do not implement incremental/delta ranking, set-based staging, worker batching, retention, history deletion, dashboard behavior, dismissal behavior, or storage maintenance in this plan (D-01, D-04).
- Do not change `finalize_deterministic_ranking_run`, ranking publication/state switching, recency/route/preference/retry behavior, feed RPCs, worker source, score-tick source, or the paid/background AI-scoring boundary (D-05).
- Do not deploy or apply the migration to a linked/hosted project, run hosted SQL, call a Supabase Management API, push schema, deploy a function, or perform any production write. Exact 0001-0061-plus-0063 migration application is allowed only in the runner-created ephemeral local Supabase/Postgres.
- Do not treat a command that would also apply pending migration 0062 as a containment release path. Plan 01 authorizes no apply command; Plans 05.1-02/03 must fail closed unless the exact approved 0063 artifact can remain isolated.
- Do not invoke score-tick, deterministic workers, claim/stage/finalize/enqueue RPCs, cron, queue repair, run cancellation, or manual retry against a linked, hosted, shared, or nonisolated database. The SQL integration must invoke the real enqueue and finalizer only inside its disposable local transaction.
- Do not delete or update ranking runs/items/state, run cleanup, purge history, vacuum, analyze, reindex, or perform storage maintenance outside the SQL test's synthetic local transaction and rollback (D-04).
- Do not apply, pop, drop, clear, rewrite, or otherwise alter any Git stash. Preserve these Phase 5 stash objects exactly: `913be95897066e53e6e1be0bd6e9483ab52a8437`, `2e6738fcf39a8a764be9e3664ee4023b01f051c9`, and `db09c3a2ff51dfa6652b36b0b809c1f9654f0117`.
- Do not reset, clean, restore, stage, or overwrite unrelated owner work. Path-scope every Git check to this plan's five artifacts.
- Do not install a package or access the network; use the checked-in Vitest toolchain, repository-pinned Supabase CLI, already available Docker/Postgres images, and synthetic non-production fixtures only. If the pinned CLI, Docker daemon, local Postgres stack, or pgTAP test cannot run, fail the plan; never substitute a linked/hosted database.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove and implement tombstone-aware enqueue containment</name>
  <read_first>
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
    - .planning/debug/job-copilot-performance.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-PLAN-OUTLINE.md
    - supabase/migrations/0032_deterministic_ranking.sql
    - supabase/migrations/0048_phase_03_10_goldman_higher.sql
    - supabase/migrations/0052_decouple_resume_routing.sql
    - supabase/migrations/0060_ranking_item_owner_delete_index.sql
    - supabase/migrations/0061_tracker_chicago_dates.sql
    - supabase/migrations/0062_phase_03_11_asset_manager_payments_workday.sql
    - web/tests/deterministic-ranking-migration.test.ts
    - web/tests/deterministic-ranking-gap-closure-migration.test.ts
    - web/tests/deterministic-ranking-snapshot.integration.test.ts
    - web/tests/resume-routing-migration.test.ts
    - web/tests/phase-03-10-migration.test.ts
    - web/package.json
  </read_first>
  <files>supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql, web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts, supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql, scripts/verify-phase-05-1-containment-local.mjs</files>
  <behavior>
    - "RED/GREEN — real tombstone-only repeat: on an ephemeral schema through 0061 the real enqueue remains tombstone-blind; after applying 0063, two real service-role-equivalent calls return zero initialized/seeded work and leave run/item/state counts unchanged."
    - "GREEN — real genuine arrival: adding one open job with neither a current-revision user_job nor a matching tombstone makes only the affected owner qualify and creates exactly one new_job run with the existing full-open-user_job snapshot seed behavior."
    - "GREEN — real publication/idempotence: complete the synthetic run's items, invoke the real migration-0052 finalizer, assert the full snapshot becomes active atomically, then call enqueue again and receive initialized_count 0/seeded_count 0 with no extra run or item."
    - "GREEN — real identity isolation: a different owner's tombstone and same-owner tombstones with a different source or external_id do not suppress the genuine arrival; the matching owner/source/external_id tombstone does."
    - "GREEN — real privilege/catalog contract: pg_proc, pg_roles, and information-schema/ACL assertions prove the enqueue remains SECURITY DEFINER, postgres-owned, empty-search-path, and executable only by service_role; anon/authenticated/public lack EXECUTE."
    - "GREEN — source/publication preservation: static SQL assertions prove 0063 replaces only enqueue, preserves bounded SKIP LOCKED/full-snapshot construction, contains no cleanup/maintenance, and leaves migration 0052's finalizer plus dashboard/feed surfaces untouched."
  </behavior>
  <action>
    Write `migration-0063-dismissal-tombstone-ranking-containment.test.ts` first, following the raw-SQL Vitest conventions in the existing migration tests. Use it for deterministic source-contract assertions: the new function body, both tombstone anti-joins, unchanged full-snapshot/finalizer boundary, privileges, bounded transaction, and forbidden cleanup/maintenance surface. Do not use an in-memory state model as behavioral proof.

    Create `supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql` as a pgTAP test wrapped in `BEGIN`/`ROLLBACK` with a declared assertion count and `finish()`. Insert only unmistakably synthetic auth/profile/preference/ranking-state/job/user_job/dismissal fixtures under dedicated UUIDs/provider identities. Call the real `public.enqueue_deterministic_new_jobs(integer)` in the local database under the same effective privilege boundary used by `service_role`, assert exact returned counters and database row counts for two tombstone-only calls, then test genuine-arrival and cross-owner/source/external_id isolation. For the genuine run, stage/complete its synthetic items according to the existing schema, invoke the real `public.finalize_deterministic_ranking_run(uuid)`, prove the complete full snapshot is published atomically, and prove the next enqueue is a database no-op. Query the live catalogs for signature, owner, SECURITY DEFINER, `proconfig` search path, and EXECUTE grants. Every fixture write must be transaction-local and rolled back; assert no fixture survives.

    Create `scripts/verify-phase-05-1-containment-local.mjs` as the sole local integration entry point. It must require canonical cwd `git rev-parse --show-toplevel`, literal CLI path `web/node_modules/.bin/supabase`, and resolve/hash/version that executable. It creates a validated `mktemp -d` root, copies a temporary config with a unique project id/collision-free loopback ports, copies exactly the ordered migration filenames 0001 through 0061 plus current 0063 and the one SQL test, and rejects 0062 or any other migration. Build one frozen child environment with `SUPABASE_TELEMETRY_DISABLED=1` and inject it into every pinned-CLI subprocess, including `--version`, `start`, `db reset --local --no-seed`, `test db {fresh-local-root}/supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql --local`, and `stop --no-backup`; every stateful command also carries explicit `--workdir {fresh-local-root}` and runs from repository-root cwd. Reject any CLI subprocess missing or overriding that environment. A `finally` path must attempt stop with the same environment and remove only the validated temporary root. Fail nonzero if CLI/Docker/Postgres/pgTAP is unavailable, reset does not report the exact manifest, the SQL test fails, teardown fails, a hosted credential/linked mode is requested, or 0062 appears. Emit sanitized canonical JSON containing cwd, `cli_environment: {"SUPABASE_TELEMETRY_DISABLED":"1"}`, literal/resolved CLI identity, CLI SHA/version, command templates/command-array SHA-256, migration manifest/digest, SQL-test SHA-256, test-output SHA-256, `result: passed`, and `teardown: passed`; never emit ports, credentials, connection strings, or row identifiers.

    Run the isolated runner before 0063 exists against a RED view containing exact 0001-0061 and confirm the real tombstone-only assertion fails for the expected reason, not for setup. Production must not be the first database execution of this SQL. Preserve the RED output digest without fixtures/secrets, then add 0063 and rerun against exact 0001-0061 plus 0063 for GREEN.

    Add `0063_dismissal_tombstone_ranking_containment.sql` as one bounded `BEGIN`/`COMMIT` forward transaction with the established five-second lock timeout and sixty-second statement timeout. Replace only `public.enqueue_deterministic_new_jobs(integer)`, preserving its signature, 1..25 validation, `SECURITY DEFINER`, empty search path, idle/current-revision owner scan, deterministic ordering, `FOR UPDATE SKIP LOCKED`, captured-run fields, existing full-open-user_job expected count/item seed, state transition, zero-item finalization path, and returned counters.

    Per D-01 and D-19, add a correlated `NOT EXISTS` against `public.user_job_dismissals` to the owner-qualification candidate so an open missing job is genuine only when no tombstone matches `state.user_id`, `job.source`, and `job.external_id`. Apply the same provider-identity exclusion to the `insert into public.user_jobs ... select` seed so the function neither qualifies nor attempts to recreate dismissed projections. Do not narrow the later run/item snapshot to only the triggering jobs; that proportional redesign belongs after the clean containment gate. Revoke execution from public/anon/authenticated, grant only to service_role, and reassert postgres ownership. Add no other function, table/index/trigger, DML cleanup, or maintenance statement.

    Run the new static test together with the existing deterministic migration, atomic snapshot publication, and resume-routing migration regressions, then run `SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-containment-local.mjs` to completion. Commit the migration, Vitest, SQL integration, and local runner together as the tested containment source before Task 2 creates evidence.
  </action>
  <acceptance_criteria>
    - The pgTAP test invokes the real enqueue twice for tombstone-only zero work, exercises one genuine arrival, invokes the real finalizer, and proves the post-publication enqueue no-op using exact initialized/seeded/run/item/state assertions.
    - Both qualification and user_job seeding use the exact owner/source/external_id dismissal identity; cross-owner and different-identity cases remain genuine.
    - The migration replaces only enqueue_deterministic_new_jobs in a bounded forward transaction and preserves its service-role-only security contract.
    - A genuine arrival still starts the existing full-snapshot new_job run; no delta algorithm, worker change, retention, dashboard change, deletion, or cleanup is introduced.
    - Migration 0052 publication remains untouched, and existing deterministic snapshot/finalizer regressions pass unchanged.
    - The isolated runner applies exactly 0001-0061 plus 0063 with 0062 absent, passes the SQL test locally, records CLI/command/manifest/output hashes, tears down, and fails rather than falling back to production when local prerequisites are unavailable.
    - The three named Phase 5 stash object IDs still appear in `git stash list` after the task.
  </acceptance_criteria>
  <verify>
    <automated>test "$(pwd -P)" = "$(git rev-parse --show-toplevel)" &amp;&amp; npm --prefix web test -- migration-0063-dismissal-tombstone-ranking-containment.test.ts deterministic-ranking-migration.test.ts deterministic-ranking-snapshot.integration.test.ts resume-routing-migration.test.ts &amp;&amp; SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-containment-local.mjs &amp;&amp; git diff --check -- supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql scripts/verify-phase-05-1-containment-local.mjs</automated>
  </verify>
  <done>The containment function is tombstone-aware, the real local enqueue/finalize/idempotence/isolation/privilege cases pass on exact 0001-0061-plus-0063 with teardown complete, and all four tested source artifacts are committed without touching production or Phase 5 stashes.</done>
</task>

<task type="auto">
  <name>Task 2: Bind the tested containment candidate for owner approval</name>
  <read_first>
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-PLAN-OUTLINE.md
    - supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
    - web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts
    - supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
    - scripts/verify-phase-05-1-containment-local.mjs
    - web/tests/deterministic-ranking-snapshot.integration.test.ts
    - web/tests/resume-routing-migration.test.ts
    - web/package.json
  </read_first>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json</files>
  <action>
    Require the Task-1 source commit to exist and require the migration, Vitest, SQL integration, and runner paths to be clean in the worktree and index; do not require global cleanliness and do not modify unrelated paths. Re-run the exact focused Vitest command and `SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-containment-local.mjs` from canonical repository-root cwd. Fail closed if either command fails, the runner does not prove exact 0001-0061-plus-0063 with 0062 absent, local teardown is incomplete, production/linked mode appears, or any preserved stash object is absent.

    Write canonical JSON at `05.1-01-CONTAINMENT-EVIDENCE.json` with `schema_version: 1`, `plan_id: "05.1-01"`, UTC generation time, the Task-1 source commit/tree, and an `artifacts` object containing exact migration, Vitest, SQL integration, and runner paths plus SHA-256 and Git blob OID for each. Record both exact passing top-level commands and their canonical command-array SHA-256. Add `local_database_verification` containing `result: "passed"`, `teardown: "passed"`, `canonical_cwd`, exact `cli_environment: {"SUPABASE_TELEMETRY_DISABLED":"1"}`, literal/resolved CLI path, executable SHA-256/version, internal command templates and digest, exact ordered 0001-0061-plus-0063 manifest/digest, excluded 0062, SQL-test hash, sanitized output hash, real enqueue/finalizer/catalog assertion groups, synthetic-fixture rollback status, and `hosted_or_linked_writes: 0`. Record all three preserved stash object IDs and subjects. Add `release_boundary` with diagnosis-observed `hosted_schema_through: "0061"`, `preexisting_pending_migrations: ["supabase/migrations/0062_phase_03_11_asset_manager_payments_workday.sql"]`, and `containment_release_migrations: ["supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"]`; mark hosted observation for later read-only revalidation. Add a `scope` object whose booleans are all false for `production_deployed`, `production_write_performed`, `linked_migration_applied`, `worker_invoked`, `queue_mutated`, `queue_repaired`, `ranking_history_deleted`, `storage_maintenance_performed`, `dashboard_changed`, `retention_changed`, `ranking_redesigned`, and `paid_ai_scoring_restored`; local transaction-rolled-back synthetic calls are recorded separately and are not production effects.

    Record that the candidate is `approval_status: "not_requested"` and `deployment_status: "not_deployed"`; Plan 05.1-02, not this plan, owns the approval request. Recompute all four artifact hashes and blob OIDs after writing, resolve the recorded commit/tree, confirm that commit contains all tested source paths, validate the JSON with `jq`, and commit only the evidence artifact. State that real SQL has passed only in a disposable local database; later approval/deployment plans still own linked dry-run, exact production target proof, and the separately approved one push.
  </action>
  <acceptance_criteria>
    - The evidence JSON resolves to the committed Task-1 migration/Vitest/SQL/runner bytes and their exact SHA-256/blob identities.
    - The recorded source commit exists, its recorded tree matches Git, and all four tested source paths exist in that commit.
    - The exact Vitest and isolated local-SQL commands plus command/output hashes are recorded as passed; exact 0001-0061-plus-0063 application, 0062 exclusion, real function/catalog assertions, fixture rollback, and teardown are machine-checkable while approval remains not requested and deployment remains not deployed.
    - The release boundary lists only 0063 as containment and explicitly lists pending 0062 as excluded/revalidation-required, so no later approval can silently bind a multi-migration apply.
    - Every forbidden production side-effect flag is false; the only database writes were synthetic and rolled back in the isolated local stack, and no hosted dry-run or applicability claim appears.
    - All three Phase 5 stash hashes and subjects are present and unchanged.
    - Only the evidence JSON changes in Task 2; unrelated work remains byte-for-byte untouched.
  </acceptance_criteria>
  <verify>
    <automated>test "$(pwd -P)" = "$(git rev-parse --show-toplevel)" &amp;&amp; npm --prefix web test -- migration-0063-dismissal-tombstone-ranking-containment.test.ts deterministic-ranking-migration.test.ts deterministic-ranking-snapshot.integration.test.ts resume-routing-migration.test.ts &amp;&amp; SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-containment-local.mjs &amp;&amp; jq -e '.schema_version == 1 and .plan_id == "05.1-01" and .verification.result == "passed" and .local_database_verification.result == "passed" and .local_database_verification.teardown == "passed" and .local_database_verification.cli_environment.SUPABASE_TELEMETRY_DISABLED == "1" and .local_database_verification.excluded_migrations == ["0062"] and .local_database_verification.hosted_or_linked_writes == 0 and .approval_status == "not_requested" and .deployment_status == "not_deployed" and .release_boundary.hosted_schema_through == "0061" and .release_boundary.preexisting_pending_migrations == ["supabase/migrations/0062_phase_03_11_asset_manager_payments_workday.sql"] and .release_boundary.containment_release_migrations == ["supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"] and ([.scope[]] | all(. == false)) and (.preserved_stashes | length == 3)' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json &amp;&amp; test "$(shasum -a 256 supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql | awk '{print $1}')" = "$(jq -r '.artifacts.migration.sha256' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json)" &amp;&amp; test "$(shasum -a 256 web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts | awk '{print $1}')" = "$(jq -r '.artifacts.vitest.sha256' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json)" &amp;&amp; test "$(shasum -a 256 supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql | awk '{print $1}')" = "$(jq -r '.artifacts.sql_integration.sha256' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json)" &amp;&amp; test "$(shasum -a 256 scripts/verify-phase-05-1-containment-local.mjs | awk '{print $1}')" = "$(jq -r '.artifacts.local_runner.sha256' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json)" &amp;&amp; test "$(git rev-parse "$(jq -r '.source.commit' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json)^{tree}")" = "$(jq -r '.source.tree' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json)" &amp;&amp; git stash list --format='%H' | grep -Fx '913be95897066e53e6e1be0bd6e9483ab52a8437' &amp;&amp; git stash list --format='%H' | grep -Fx '2e6738fcf39a8a764be9e3664ee4023b01f051c9' &amp;&amp; git stash list --format='%H' | grep -Fx 'db09c3a2ff51dfa6652b36b0b809c1f9654f0117'</automated>
  </verify>
  <done>The next approval plan receives a machine-checkable, source-committed candidate whose static and real local SQL tests passed on the exact isolated chain with hashes/teardown bound, explicit zero production effect, and preserved Phase 5 stashes.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

- `supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql` is a forward-only, bounded replacement of only `enqueue_deterministic_new_jobs(integer)`. It excludes matching dismissal provider identities from qualification and user_job seeding while preserving the existing full-snapshot run and complete-publication protocol.
- `web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts` proves tombstone-only zero work across repeated calls, a single genuine arrival trigger, post-publication idempotence, owner/provider identity isolation, service-role security, forward-only scope, and unchanged publication behavior.
- `supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql` invokes the real local enqueue/finalizer against synthetic transaction-scoped fixtures and proves repeat no-work, genuine arrival, publication/no-op, identity isolation, and catalog privileges.
- `scripts/verify-phase-05-1-containment-local.mjs` enforces repository-root execution, exact 0001-0061-plus-0063 isolation, 0062 exclusion, local setup/test/teardown, and sanitized command/manifest/output identities.
- `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json` binds all four tested source artifacts to SHA-256/blob IDs and Task-1 commit/tree identity; records the passing Vitest/local-SQL commands, CLI/command/manifest/output hashes and teardown; preserves all three Phase 5 stash identities; encodes the 0061-hosted/0062-excluded/0063-only release boundary; and proves zero production effect for Plan 05.1-02.
- `05.1-01-SUMMARY.md` records implementation results only. No approval receipt, deployment record, hosted observation, cleanup result, queue repair, worker result, dashboard artifact, retention artifact, or deletion artifact is produced.
</artifacts>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Status | Plan mapping |
|---|---|---|---|---|
| GOAL | — | Stop phantom full-catalog ranking before later proportional ranking/dashboard/retention work | COVERED | 05.1-01 removes tombstone-only qualification and proves repeated zero work without bundling later changes. |
| REQ | — | No roadmap requirement IDs; operational stabilization | N/A | `requirements: []` is intentional and matches ROADMAP/outline. |
| RESEARCH | — | No phase RESEARCH.md exists | N/A | Diagnosis artifact supplies the confirmed producer-loop evidence; no new dependency or discovery is required. |
| CONTEXT | D-01 | First release is containment only | COVERED | Only enqueue tombstone awareness, tests, and approval evidence are created. |
| CONTEXT | D-04 | No history deletion or storage maintenance | COVERED | Migration contains no cleanup/maintenance and scope evidence records zero effect. |
| CONTEXT | D-05 | Deterministic ranking remains publication authority; paid AI stays removed | COVERED | Finalizer/publication and worker boundaries are untouched; existing publication tests run. |
| CONTEXT | D-19 | Every new_job run corresponds to a genuine arrival; no minute-cadence phantom growth | COVERED FOR CONTAINMENT | Tombstone-only calls create zero runs/items and one genuine arrival triggers once; proportional item redesign remains assigned to 05.1-05 by D-01 and the outline. |
| CONTEXT | D-02, D-03, D-18 | Natural queue settling and clean 24-hour observation | OUTLINE-SCOPED | Plans 05.1-03 and 05.1-04 own deployment/observation after approval. |
| CONTEXT | D-06, D-07, D-16 | Proportional ranking, complete publication, freshness targets | OUTLINE-SCOPED | Plan 05.1-05 owns post-gate redesign and acceptance; this plan preserves its prerequisite publication contract. |
| CONTEXT | D-08–D-11, D-17 | Dismissal UI/critical-path behavior | OUTLINE-SCOPED | Plan 05.1-06; no dashboard source is touched here. |
| CONTEXT | D-12–D-15 | Retention and separately approved deletion | OUTLINE-SCOPED | Plans 05.1-07 and 05.1-10 through 05.1-12; no retention/deletion is touched here. |
| CONTEXT | Deferred ideas | Filter tuning, connector expansion, outreach work | EXCLUDED | Outside Phase 05.1 containment and absent from all tasks. |

No GOAL, REQ, RESEARCH, or plan-scoped CONTEXT item is omitted. Decisions assigned by the accepted outline to later plans are not pulled into this containment release.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| scheduler/service role → enqueue SECURITY DEFINER RPC | A recurring privileged call may create durable runs/items and must remain bounded and least-privileged. |
| jobs + user_jobs + dismissal tombstones → owner qualification | Shared job truth and private durable dismissal intent determine whether work is genuine. |
| enqueue run construction → migration-0052 finalizer | Containment may admit work but must not weaken complete deterministic publication. |
| tested repository bytes → owner approval | Later approval must bind the exact migration/test commit, hashes, and zero-effect scope. |

## STRIDE Threat Register — OWASP ASVS Level 1 (block on high)

Every critical/high threat below is blocking: Plan 05.1-01 cannot complete unless its mitigation is present and the focused automated verification passes.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05.1-01-01 | Denial of Service | Tombstone-blind owner qualification | high | mitigate | Correlated owner/source/external_id dismissal anti-join plus repeated-tick zero-run/zero-item regression. |
| T-05.1-01-02 | Tampering | Projection seed versus qualification mismatch | high | mitigate | Apply the same durable provider-identity exclusion to qualification and user_job seeding; test cross-owner and different-identity behavior. |
| T-05.1-01-03 | Elevation of Privilege | SECURITY DEFINER enqueue RPC | high | mitigate | Preserve empty search path, bounded input/locking, postgres ownership, revoke public/anon/authenticated, and grant only service_role. |
| T-05.1-01-04 | Tampering | Atomic deterministic publication | high | mitigate | Redefine no finalizer/feed function, preserve full-snapshot construction, and run existing migration-0052/snapshot publication regressions. |
| T-05.1-01-05 | Repudiation | Candidate selected for approval | medium | mitigate | Evidence JSON records canonical paths, SHA-256, source commit/tree, exact passing command, approval/deployment status, and zero-effect scope. |
| T-05.1-01-06 | Tampering | Pending migration release set | high | mitigate | Evidence records hosted-through-0061 observation, excludes pending 0062, identifies 0063 as the sole containment artifact, and requires later read-only revalidation/fail-closed isolation. |
| T-05.1-01-07 | Information Disclosure | Tests/evidence | low | accept | Fixtures are synthetic; evidence contains only repository paths, hashes, Git identities, aggregate status, and stash subjects already local to the repository owner. |
| T-05.1-01-SC | Tampering | Dependency supply chain | low | accept | No package install or network access; use only the checked-in lockfile/toolchain and existing Node/Vitest dependencies. |
</threat_model>

<verification>
1. RED proves the missing migration leaves every requested containment behavior unsatisfied; GREEN adds only migration 0063.
2. The focused new test proves tombstone-only zero work, one genuine arrival, the second enqueue no-op, identity isolation, forward-only scope, and least privilege.
3. Existing deterministic migration, snapshot-publication, and resume-routing migration tests prove complete publication and decoupled routing remain unchanged.
4. The evidence gate re-runs the same focused suite, recomputes both SHA-256 values, resolves source commit/tree, confirms all three preserved stash hashes, and encodes the 0061-hosted/0062-excluded/0063-only release boundary.
5. Verification is local and source/model-only. It performs no queue call, database write, linked migration application, worker invocation, cleanup, deployment, or hosted observation.
</verification>

<success_criteria>
- Tombstone-only owners create zero new_job runs and zero items across repeated scheduler-equivalent calls.
- One genuine open arrival qualifies its affected owner exactly once; after publication, the next enqueue is a no-op.
- The migration changes only enqueue eligibility/seeding and preserves existing full-snapshot construction, migration-0052 atomic publication, and deterministic ranking authority.
- Focused and existing publication regression tests pass with no package install or network access.
- The evidence JSON is hash/commit/tree-valid, says approval not requested and deployment not deployed, and records every forbidden side effect as false.
- The evidence prevents a later release from silently bundling locally pending migration 0062 with the sole containment migration 0063.
- All three Phase 5 stash objects remain present and unchanged; unrelated owner work is untouched.
- No production deployment, production write, queue mutation, worker call, cleanup, retention, dashboard, deletion, or storage-maintenance action occurs.
</success_criteria>

<output>
Create `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-SUMMARY.md` after execution. Its key files are the 0063 migration, its focused test, and `05.1-01-CONTAINMENT-EVIDENCE.json`. The summary must not claim owner approval, deployment, linked/hosted application, queue settling, 24-hour observation, production cleanup, or any later-plan behavior.
</output>
