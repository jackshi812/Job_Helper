---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 09
type: execute
wave: 7
depends_on:
  - 05.1-14
files_modified:
  - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The failed first schema process recorded at fd14f63 remains immutable one-attempt/zero-effect history; it is never reset, relabeled, retried, or removed."
    - "Only the unchanged new Plan 14 payload/receipt can authorize one NEW_FORWARD_REPAIR application of corrected migration 0064, followed after schema PASS by the unchanged score-tick closure and one non-force fast-forward push of the exact release_source_candidate whose sole parent is the approval-bound origin/main pre-state and whose tree equals the repaired release tree."
    - "The prior Plan 08 payload, receipt, owner signal, and launch authorization are non-reusable because the approved 0064 bytes changed and their sole schema process was consumed."
    - "New payload/receipt/source/target/build-setting/command drift, candidate non-ancestry from origin/main, parent mismatch, or repaired-tree mismatch fails closed before any new production process."
    - "0062/0065, retention/deletion/cleanup/history/maintenance, scheduler/secrets, extra functions/web commits, manual calls, retries, rollback, force push, and manual Cloudflare deploy remain at zero (D-23)."
    - "After the one push, PASS requires proof that origin/main transitioned from the approved pre-state to the release commit and exactly one new Git-triggered Cloudflare production deployment appeared after the pre-push baseline for that commit."
    - "D-21: the new 24-hour, freshness, phantom-work, and dismissal acceptance belongs only to Plan 10 and consumes this plan's actual deployment/asset record."
  artifacts:
    - ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md preserves the failed first process and appends new Plan 14 authority validation, one forward-repair process, cumulative counters, exact after-state identities, actual deployed index/asset hashes, domain parity, and exclusions."
  key_links:
    - "Immutable fd14f63 failed-attempt commit/artifact digests + new Plan 14 payload/receipt -> one exact corrected-0064 forward-repair schema process, without requiring candidate ancestry from fd14f63"
    - "Approved recursive score-tick closure -> one function deployment preserving no-verify-jwt and the existing secret boundary"
    - "Approved origin/main pre-state + pre-push Cloudflare deployment-ID baseline -> one fast-forward transition -> exactly one new Git-triggered deployment ID -> actual deployed index/asset manifest -> production-domain byte parity"
---

<objective>
Resume the stopped release under new Plan 14 authority: apply corrected 0064 through one separately approved forward-repair process, then release the unchanged score-tick and Dashboard source candidate only after schema success.

Purpose: Release the two verified fixes without widening authority or assuming that a local build reproduces the provider build environment.
Output: Preserved first-attempt history plus exact corrected schema/function/Git-triggered web release and a sanitized actual-deployment receipt, or a truthful stopped state requiring another separately scoped decision.
</objective>

## Artifacts this phase produces

- An append-only continuation of `05.1-09-RELEASE-DEPLOYMENT.md` that retains the complete `fd14f63` failed-attempt record and adds new Plan 14 authority, cumulative process/effect counters, corrected hosted schema parity, and downstream release evidence.
- On PASS only, the active approved score-tick closure and one provider-built Cloudflare production release for the new source candidate, with immutable asset/domain parity.
- No new migration, cleanup, maintenance, monitoring, manual worker/queue invocation, retry, or rollback artifact.

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-SUMMARY.md
@supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql
@supabase/functions/score-tick/index.ts
@supabase/functions/_shared/deterministic-worker.ts
@web/src/pages/Dashboard.tsx
@web/src/lib/supabase.ts
@web/package.json
@web/package-lock.json
@web/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate new forward-repair authority and release corrected 0064 plus score-tick</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md</files>
  <read_first>
    - `05.1-09-RELEASE-DEPLOYMENT.md` and immutable commit `fd14f63`: the first schema process, SQLSTATE 57014 failure, independent zero-effect after-state, cumulative counters, and unchanged downstream state.
    - `05.1-14-FORWARD-REPAIR-APPROVAL.md` and `05.1-14-SUMMARY.md`: new canonical payload/receipt, corrected source candidate, two independent local verifier receipts, exact targets/commands/limits, Plan 08 non-reuse, and exclusions.
    - `05.1-08-FRESH-RELEASE-APPROVAL.md`: historical payload/receipt only; verify its bytes and explicitly reject it as current authority.
    - `supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql`: corrected schema bytes and the one targeted `NOT VALID` constraint.
    - `supabase/functions/score-tick/index.ts` and `supabase/functions/_shared/deterministic-worker.ts`: approved recursive closure and cron/JWT boundary.
  </read_first>
  <action>
    Resume the existing deployment artifact; do not recreate, truncate, normalize, or rewrite its failed-attempt sections. Before mutation-capable credentials or any new process, verify `git show fd14f63:.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md` resolves to blob `0ffcfa53b9de0c79b9875252c35902def808a86d` and SHA-256 `021379bc5d44a498ee55efd682ac24525d2b8af1455439cbcd55f8e495210a06`. Append a `NEW_FORWARD_REPAIR_PRECHECK` region that carries the historical one schema process/zero schema effects and zero downstream attempts/effects unchanged.

    Independently recompute the Plan 14 payload, receipt, exact current owner signal, `repair_evidence_commit`, `repaired_release_tree`, `origin_main_pre_state`, `release_source_candidate`, candidate tree/sole-parent/count/ancestor/distance fields, corrected 0064 and proof hashes, unchanged recursive score-tick closure, unchanged tracked web build-input closure, twice-replayed local verifier receipts, focused suite/build receipts, exact targets/build settings, commands, wait bounds, limits, and exclusions. Require `git merge-base --is-ancestor &lt;origin_main_pre_state&gt; &lt;release_source_candidate&gt;` to succeed, the candidate's complete parent list to contain exactly `origin_main_pre_state`, the candidate tree to equal `repaired_release_tree` and the Task 1 repair-evidence commit tree, and the fast-forward distance to equal one. Validate `fd14f63` and the failed deployment artifact only through their exact Plan 14 commit/blob/SHA-256 fields; do not require or infer candidate ancestry from the failed-attempt branch. Append these results as `release_candidate_preflight: PASS` only after every object-level gate passes.

    Prove the Plan 08 payload/receipt/launch signal are historical and non-authorizing. Require the Plan 14 receipt to authorize exactly one `NEW_FORWARD_REPAIR` schema process with function/Git effects conditional on schema PASS and zero retries. Revalidate process-only credentials, exact linked Supabase project `fjcsvajkkztvlrpdplwx`, remote migration history through 0063 with 0062/0064/0065 absent, every catalog object listed in the failed after-state still absent, unchanged existing function/security hashes, score-tick v16, scheduler/secret metadata, origin/main equal to `origin_main_pre_state`, and Cloudflare source/build settings. Any drift stops with `new_forward_repair_schema_processes: 0` and leaves historical counters unchanged.

    Reconstruct the Plan 14-bound isolated root with exact 0001–0061, 0063, and corrected 0064 only. Recompute its ordered manifest and repeat the exact linked dry run from repository-root cwd with the bound repository CLI and `SUPABASE_TELEMETRY_DISABLED=1`; it must propose only 0064 once. Durably increment `new_forward_repair_schema_processes` from zero to one before starting the exact approved `supabase db push`. Keep `historical_schema_push_processes: 1`, `cumulative_schema_push_processes: 2`, and `retries: 0` distinct; this is the separately approved forward repair, not a retry. Never start a second new process.

    On zero exit, independently require migration history equals the pre-state plus 0064 exactly once; 0062/0065 remain absent; all approved 0064 columns/table/RLS/revokes/grants/trigger/index/functions match the corrected candidate; `deterministic_ranking_items_job_input_revision_valid` exists with its exact predicate and `convalidated = false`; and `jobs_deterministic_input_revision_valid` exists with `convalidated = true`. Do not validate either constraint or write a production probe row. On failure or ambiguity, perform one bounded read-only after-state inventory, append the truthful disposition, and stop without function/Git/Cloudflare effects, retry, rollback command, backfill, validation, maintenance, or repair.

    Only after corrected schema PASS, recompute the unchanged Plan 14 authority and revalidate the approved recursive score-tick closure, project, CLI, exact `--no-verify-jwt` command, handler secret enforcement, scheduler/secret non-mutation contract, and one-attempt limit. Increment the new score-tick deployment counter to one and deploy exactly `score-tick` once; do not invoke it. Independently require one ACTIVE deployment whose downloaded/normalized closure equals approval, verify_jwt remains false, the handler secret boundary remains present, and scheduler/secret identities are unchanged. Any failure or mismatch stops without retry, rollback, Git push, or web release. Keep credentials process-only and evidence sanitized.
  </action>
  <acceptance_criteria>
    - The `fd14f63` failed-attempt record and its one-process/zero-effect counters remain exact and append-only.
    - Every new Plan 14 authority, source, target, build-setting, and command check passes before the forward-repair process, including exact candidate origin/main ancestry, sole-parent, repair-tree, and one-commit distance gates.
    - Plan 08 authority is rejected as non-reusable before mutation.
    - Remote migration history gains exactly 0064 once; 0062/0065 remain absent.
    - The repaired ranking-item check is present with `convalidated = false`, still locally proven to enforce new writes, while the jobs check is validated.
    - The active score-tick closure exactly matches approval with preserved JWT/secret/scheduler posture.
    - Historical schema processes equal one, new forward-repair schema processes equal one, cumulative schema processes equal two, retries equal zero, and the new function attempt equals one on success.
    - No retention/history/cleanup/maintenance or monitoring action occurs.
  </acceptance_criteria>
  <verify>
    <automated>rg -F 'forward_repair_approval_validation: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'release_candidate_preflight: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'release_source_candidate_origin_main_ancestor: true' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'release_source_candidate_first_parent_equals_origin_main_pre_state: true' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'release_source_candidate_tree_equals_repaired_release_tree: true' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'failed_attempt_candidate_ancestry_required: false' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'historical_schema_push_processes: 1' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'new_forward_repair_schema_processes: 1' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'cumulative_schema_push_processes: 2' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'schema_status: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'deterministic_ranking_items_job_input_revision_valid_convalidated: false' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'jobs_deterministic_input_revision_valid_convalidated: true' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'score_tick_status: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'retries: 0' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'manual_worker_invocations: 0' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md</automated>
  </verify>
  <done>The failed process remains immutable, corrected 0064 and score-tick are released through their distinct new limits with independent after-state parity, or execution stops without another unapproved action.</done>
</task>

<task type="auto">
  <name>Task 2: Push once, identify the unique Cloudflare release, and seal actual deployed assets</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md</files>
  <read_first>
    - `05.1-09-RELEASE-DEPLOYMENT.md`: immutable failed-attempt history plus Task 1 corrected schema/function PASS state, zeroed Git/Cloudflare counters, and unchanged Plan 14 authority.
    - `05.1-14-FORWARD-REPAIR-APPROVAL.md`: approved new source candidate, origin/main pre-state, Cloudflare target/build settings, tracked web input closure, wait bounds, and exact push command.
    - `web/index.html`, `web/public/**`, and the tracked web build-input manifest: paths needed to recognize public outputs without trusting local generated assets.
  </read_first>
  <action>
    Proceed only when Task 1 has independently sealed corrected schema/function PASS. Immediately before the Git write, recompute the unchanged Plan 14 payload/receipt, `repair_evidence_commit`, `repaired_release_tree`, `origin_main_pre_state`, `release_source_candidate`, candidate tree/sole-parent/count/ancestor/distance fields, tracked web build-input closure, exact push command, Cloudflare account/project/source repository/production branch/domain/build settings/public variable key set, wait bounds, exclusions, and Plan 08 non-reuse. Require the Git object gates again: `origin_main_pre_state` is an ancestor, the candidate has exactly that one parent, its tree equals the repaired release tree and repair-evidence commit tree, and its fast-forward distance is one. Read origin/main and require it equals that exact approval-bound pre-state, then record `origin_main_pre_push` and `git_release_candidate_contract: PASS`. The immutable `fd14f63` failed-attempt fields remain evidence only and are not part of this ancestry proof. Query the Cloudflare deployments endpoint and record the sorted IDs plus canonical digest for every existing deployment that already matches the approved project/repository, established Git integration, production branch/environment, and `release_source_candidate`; this is the immutable pre-push matching-ID baseline. Do not build or hash local generated assets as an authority check. Any drift or unreadable baseline stops with `git_push_attempts: 0` and no web effect.

    Record `git_push_started_at_utc`, increment `git_push_attempts` to one, and execute only the Plan 14-bound non-force fast-forward push of the exact `release_source_candidate` to `origin/main`; never substitute the planning-branch repair commit, push another ref/commit, add a force option, or retry. Require the push to report an actual accepted ref update, then immediately read origin/main and record `origin_main_post_push`. PASS requires an exact transition from the recorded approval-bound pre-state to `release_source_candidate`; an up-to-date/no-update result, pre-push mismatch, post-push mismatch, or concurrent remote drift fails closed. Record the exact transition tuple/digest and `origin_main_transition: PASS` only after both endpoints and the push result agree.

    Starting from `git_push_started_at_utc`, poll only the read-only Cloudflare deployments endpoint at the approved 15-second interval for at most 900 seconds. On each poll, apply the full causal predicate: project `job-helper`; deployment ID absent from the pre-push matching-ID baseline; established Git integration trigger rather than a manual/API deployment; approved source repository; production branch `main`; production environment; exact `deployment_trigger.metadata.commit_hash == release_source_candidate`; and provider created/triggered timestamp within the bounded push-attempt window. Historical same-commit deployment IDs in the baseline are ignored for new-match cardinality and can neither satisfy PASS nor cause duplicate failure. Continue while zero new causal matches are nonterminal and within the window; fail immediately on more than one new causal match, a terminal failed/canceled new match, causal metadata drift, or provider ambiguity. PASS requires exactly one new baseline-absent deployment ID whose terminal status is success. Record baseline IDs/digest, new ID, trigger/repository/branch/environment/commit identities, causal timestamps, and `cloudflare_deployment_causality: PASS`. On timeout or failure, record observed baseline/new sets and digests and stop without another push, manual Cloudflare deploy, rollback, force push, or unrelated mutation.

    From the unique successful deployment record, bind the deployment ID, immutable deployment URL, source commit, branch, environment, build-setting identity, created/completed UTCs, and canonical metadata hash. Fetch the immutable deployment's live `index.html`, then retrieve and hash the actual same-origin asset closure referenced by its script/link/module graph plus the tracked `web/public/**` output paths. Store only canonical path, byte length, content type, and SHA-256 entries; never store public-variable values or response bodies. Compute `deployed_index_sha256` and `deployed_asset_manifest_sha256` from these actual provider-built bytes. Do not compare their filenames or hashes to any local `web/dist` output.

    Fetch the production domain's `index.html` and the same canonical asset paths within the remaining approved observation window. Require its index bytes and complete asset manifest to equal the unique immutable deployment, while Cloudflare metadata binds that deployment to the approved source commit. Record `production_domain_deployment_parity: PASS`, the production-domain manifest digest, and observation UTC only on exact equality; otherwise stop truthfully without retry or mutation.

    Run final read-only parity for remote migrations (through 0063 plus corrected 0064 only), 0064 definitions/grants/constraint validation flags, active score-tick closure/JWT/secret/scheduler posture, origin/main, unique Cloudflare source candidate/deployment, actual index/asset manifest, production-domain parity, and Plan 14 approval identities. Reconcile integer counters: historical failed schema processes 1 and effects 0; new forward-repair schema processes 1 and effects 1 on PASS; cumulative schema processes 2; new score-tick deploy 1; Git push 1; Cloudflare manual deploy 0; manual function/worker/queue calls 0; 0062/0065 effects 0; constraint validations/backfills 0; retention/deletion/cleanup/history/maintenance/vacuum 0; paid AI 0; scheduler/secret/JWT edits 0; retries/rollbacks/force pushes 0. Canonicalize and hash the sanitized append-only evidence region. Mark `deployment_status: PASS` only when every identity/effect check agrees. Do not perform Plan 10 monitoring or make any 24-hour/freshness/dismissal acceptance claim (D-21).
  </action>
  <acceptance_criteria>
    - Origin/main receives only the exact Plan 14 candidate through one non-force fast-forward push; the planning-branch repair commit is never pushed.
    - Immediately before the push, candidate ancestry, sole-parent, repaired-tree, and one-commit distance are re-proven from Git objects and `origin/main` equals the bound pre-state.
    - Origin/main demonstrably transitions from the exact approved pre-state to the release commit; up-to-date/no-transition or concurrent drift cannot pass.
    - Exactly one successful, baseline-absent Cloudflare deployment is created by the established Git integration for the approved repository/production branch/environment/source commit within the bounded push-attempt window.
    - Historical same-commit deployments remain recorded in the baseline but cannot satisfy or duplicate the new deployment match.
    - The deployment record seals actual provider-built index/asset hashes, and the production domain serves exactly those bytes.
    - No pre-deployment local asset equality is asserted or required.
    - Final evidence is Plan-14-receipt/source/target/build-setting-bound, preserves the first failed process, carries cumulative counters, and contains no credentials.
    - Plan 10 is the first and only post-release monitoring/acceptance step and consumes this actual deployment/asset record.
  </acceptance_criteria>
  <verify>
    <automated>rg -F 'deployment_status: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'git_release_candidate_contract: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'git_push_attempts: 1' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'origin_main_transition: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'cloudflare_pre_push_matching_deployment_ids_sha256:' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'cloudflare_new_matching_production_deployments: 1' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'cloudflare_new_deployment_absent_from_baseline: true' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'cloudflare_deployment_causality: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'web_status: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'production_domain_deployment_parity: PASS' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'deployed_asset_manifest_sha256:' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'excluded_migrations_applied: []' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'retention_calls: 0' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; rg -F 'automatic_retries_or_rollbacks: 0' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; git diff --check -- .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md</automated>
  </verify>
  <done>The exact approved schema/function/source release is deployed once and its actual provider-built assets/domain parity are sealed, or a truthful stopped state remains with no automatic repair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Failed attempt + new owner receipt -> production authority | Immutable history and new authority must both match before any forward-repair process. |
| Isolated migration/function closure -> Supabase | Only 0064 and the approved score-tick closure may cross the boundary once. |
| Git push -> Cloudflare Git integration | One approved commit may trigger one provider-built production release. |
| Cloudflare deployment -> production domain | Actual immutable deployment bytes must match the mutable production alias before PASS. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05.1-09-01 | Spoofing | Plan 14 authority | critical | mitigate | Recompute current actor/payload/receipt/source/target/build-setting/command hashes, reject Plan 08 replay, and repeat the gate before Git push. |
| T-05.1-09-02 | Tampering | failed history and migration/function/Git release | critical | mitigate | Exact fd14f63 commit/artifact evidence independent of candidate lineage; exact origin/main-rooted candidate ancestor/sole-parent/repaired-tree gates; append-only cumulative counters; corrected 0064-only migration view; repeated dry run; one new process/deploy/push each; and independent hosted parity with no retry. |
| T-05.1-09-03 | Tampering | Cloudflare deployment causality | high | mitigate | Snapshot matching IDs before push, prove the exact origin transition, and accept exactly one new baseline-absent Git-triggered production deployment within the bounded attempt. |
| T-05.1-09-04 | Spoofing | production domain | high | mitigate | Hash actual immutable deployment index/assets and require the production domain to serve the identical manifest. |
| T-05.1-09-05 | Elevation of Privilege | excluded operations | high | mitigate | Exact zero counters for 0062/0065/retention/cleanup/history/manual deploy/calls/scheduler/secrets/retry/rollback/force push. |
| T-05.1-09-06 | Information Disclosure | deployment evidence | high | mitigate | Process-only credentials; persist only sanitized metadata, public asset paths/hashes, and no environment values or bodies. |
| T-05.1-09-SC | Tampering | tool/dependency supply chain | low | accept | No install/update; every CLI/source/lock/closure identity must equal the Plan 14 approval. |
</threat_model>

<verification>
Independent pre-mutation checks must preserve the fd14f63 first-attempt record as separate immutable commit/artifact evidence, reject Plan 08 authority replay, prove the exact Plan 14 candidate descends directly from the approval-bound origin/main pre-state with the repaired release tree, fail closed on authority/source/target/build-setting/command drift, and snapshot the origin/deployment baseline. Independent after-state reads must prove corrected 0064 with the targeted constraint unvalidated, the exact origin transition, one new causal Git-triggered deployment absent from that baseline, cumulative counters, and actual index/asset parity with production-domain bytes.
</verification>

<success_criteria>
- The first failed schema process remains immutable; corrected migration 0064 is applied through exactly one new forward-repair process under the Plan 14 receipt.
- Score-tick and the approved source candidate are released exactly once only after corrected schema PASS.
- Exactly one new baseline-absent, Git-triggered Cloudflare production deployment causally follows the proven origin/main transition to that commit, and its actual provider-built index/asset manifest is sealed and served by the production domain.
- 0062/0065 and all retention/cleanup/history/extra/manual/retry/rollback effects remain absent.
- Post-release monitoring is untouched and Plan 10 can consume the actual deployment/asset record.
</success_criteria>

<output>
Create `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-SUMMARY.md` when done. Record the immutable fd14f63 failed-attempt commit/artifact anchor separately from candidate lineage, Plan 08 non-reuse, new Plan 14 approval/evidence hashes, repair-evidence commit/tree, origin/main pre-state, release candidate commit/tree/sole-parent plus ancestor/tree/distance gates, corrected migration/function/Git/Cloudflare identities, both constraint validation flags, origin pre/post transition tuple, pre-push matching deployment-ID baseline digest, the one new causal deployment ID/trigger/repository/timestamps, actual deployed index/asset manifest digest, production-domain parity, historical/new/cumulative process and effect counters, stop disposition, and exclusions. Do not claim Plan 10 acceptance.
</output>
