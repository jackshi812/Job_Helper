---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 02
type: execute
wave: 2
depends_on: [05.1-01]
files_modified:
  - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md
autonomous: false
requirements: []
requirement_note: "None — operational stabilization"

must_haves:
  truths:
    - "Per D-01, the owner can approve only the exact tested 0063 containment migration; the approval cannot authorize pending migration 0062, delta ranking, worker batching, retention, dashboard work, or any later release."
    - "Per D-02, the approval explicitly requires natural claimed/building completion or lease expiry and authorizes no manual worker call, run cancellation, retry, lease manipulation, or queue repair."
    - "Per D-04, the approval authorizes no ranking-history deletion and no vacuum, analyze, reindex, file-shrink, or other storage maintenance."
    - "Per D-20, the owner approval payload binds only immutable source/evidence/migration/function-security/target-policy/isolated-command/exclusion/rollback/stash/scope identities; volatile production counts, ranking work state, timestamps, and fixed-window log totals are outside approval-digest equality."
    - "The version-2 hashed payload is a closed, ordered flat-key schema: every payload key is allowlisted and validated, so unknown, duplicate, missing, out-of-order, or malformed keys fail before owner presentation."
    - "Per D-20, Plan 05.1-03 must remeasure aggregate operational state immediately before the one allowed push and return NO_DEPLOY for natural drain and read-only recheck when any building run/state or pending/claimed active item exists."
    - "All three Phase 5 stash commits and trees remain present and unchanged before request, at owner response, and when Plan 05.1-03 consumes the receipt."
    - "Only a fresh exact owner signal containing the corrected immutable-only payload SHA-256 can seal a canonical namespaced receipt whose payload reference is unique and whose digest is stored outside the hashed receipt bytes."
    - "This plan performs zero production mutations and produces only an immutable approval receipt for the separately executed deployment plan."
  artifacts:
    - path: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md"
      provides: "Canonical containment preflight payload plus exact owner approval receipt, both SHA-256 bound"
  key_links:
    - from: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json"
      to: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md"
      via: "Independent Git/tree/four-artifact/static-test/real-local-SQL revalidation before any linked dry run or payload generation"
      pattern: "source_commit|source_tree|migration_sha256|vitest_sha256|sql_integration_sha256|local_runner_sha256|local_database_verification"
    - from: "isolated 0001-0061 plus 0063 migration view"
      to: "owner approval payload"
      via: "Content inventory digest and linked dry run proving 0063 is the sole proposed migration"
      pattern: "0063_dismissal_tombstone_ranking_containment|sole_pending"
    - from: "owner checkpoint response"
      to: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md"
      via: "Exact payload-digest-bearing signal plus canonical namespaced receipt fields and an external receipt digest"
      pattern: "receipt_approval_signal|receipt_approved_payload_sha256|approval_receipt_sha256"
    - from: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md"
      to: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-CONTAINMENT-DEPLOYMENT.md"
      via: "Plan 05.1-03 recomputes every immutable payload/receipt/source/target/function/security/stash identity, then independently remeasures volatile aggregate safety immediately before the one approved push"
      pattern: "OWNER_APPROVED|approval_payload_sha256|approval_receipt_sha256|immutable_gate_status|operational_gate_status|NO_DEPLOY"
---

<objective>
Obtain the owner's fresh exact containment deployment approval, bound to immutable tested-release identities while volatile production state remains an immediate fail-closed deployment gate (D-01, D-02, D-04, D-20).

Purpose: Make the production boundary cryptographically narrow enough that approval for tombstone containment cannot be reused for migration 0062, later ranking/dashboard/retention work, manual queue intervention, deletion, or maintenance, without making normal scheduler-driven count drift an impossible approval-equality condition.
Output: One immutable `05.1-02-CONTAINMENT-APPROVAL.md` receipt. No migration, worker, function, web, queue, history, storage, or stash mutation occurs.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@.planning/debug/job-copilot-performance.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-PLAN-OUTLINE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json
@supabase/migrations/0062_phase_03_11_asset_manager_payments_workday.sql
@supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
@web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts
@supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
@scripts/verify-phase-05-1-containment-local.mjs
@.planning/milestones/v1.0-phases/03-scoring-feed-notifications/03-10-SUMMARY.md

<interfaces>
- Plan 05.1-01 evidence is authoritative only after independent revalidation: `.source.commit`, `.source.tree`; migration/Vitest/SQL-integration/local-runner paths, SHA-256 values, and Git blob OIDs; exact passing static and real-local-SQL commands; local runner output/command/manifest/CLI/teardown identities; `hosted_schema_through: "0061"`; excluded pending 0062; sole containment 0063; three preserved stash identities; and every zero-production-effect scope flag.
- The exact containment artifact is `supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql`; `0062_phase_03_11_asset_manager_payments_workday.sql` is pre-existing, unrelated, pending, and excluded from this authorization.
- The envelope uses `approval_contract_version: 2`; the immutable owner-bound payload uses `payload_contract_version: 2`, `payload_digest_policy: immutable_authorization_only`, and `payload_serialization: flat_key_value_lf_v2`. It binds the expected pre-push migration policy (ordered 0001 through 0061 present; 0062 and 0063 absent), not a timestamped observation by byte equality. A fresh isolated release view contains exact committed migrations 0001 through 0061 plus exact 0063, never 0062, and its linked dry run must propose only 0063.
- The approval artifact uses exact version-2 marker lines. The envelope is between `<!-- containment-approval-v2-envelope-begin -->` and `<!-- containment-approval-v2-envelope-end -->`. The owner-hashed payload is between `<!-- containment-approval-v2-payload-begin -->` and `<!-- containment-approval-v2-payload-end -->`. `approval_payload_sha256` is the SHA-256 of only the exact LF-terminated payload lines between those markers; marker bytes are excluded.
- The payload serialization is `flat_key_value_lf_v2`: one unindented `key: value` record per line, no blank/comment/continuation lines, no duplicate keys, and the exact allowlisted order in `<immutable_payload_key_allowlist>`. Composite values are compact canonical JSON on that same line. The ordered key list's SHA-256 is `e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d` and is stored as `payload_immutable_key_order_sha256`.
- The receipt is between `<!-- containment-owner-receipt-v2-begin -->` and `<!-- containment-owner-receipt-v2-end -->` with the exact ordered namespaced fields in `<canonical_receipt_contract>`. `approval_receipt_sha256` is not inside that region: it is the sole key between `<!-- containment-owner-receipt-v2-digest-begin -->` and `<!-- containment-owner-receipt-v2-digest-end -->`. The digest covers only exact LF-terminated receipt lines between the receipt markers, excluding both markers.
- Operational observations live after the payload end marker in a separately labeled non-authorizing region. Its ranking-state counts, active run/item counts, catalog estimates, observation timestamps, and fixed-window score-tick totals may carry query/output digests for provenance, but neither their values nor the informational-region digest is an input to `approval_payload_sha256`. Plan 05.1-03 remeasures the safety aggregates rather than comparing them with these observations.
- The command contract is byte-stable across Plans 02 and 03: canonical cwd is the repository root; literal executable is `web/node_modules/.bin/supabase`; its resolved path, SHA-256, and version are bound separately; linked dry-run argv is `db push --dry-run --linked --yes --workdir {fresh-isolated-root}`; deployment argv is `db push --linked --yes --workdir {fresh-isolated-root}`. Every linked CLI preflight, dry run, and push is invoked from repository-root cwd with `--workdir {fresh-isolated-root}`; the executable is never resolved relative to the isolated root.
- The only accepting signal is a newly returned `APPROVE 05.1-02 CONTAINMENT <approval_payload_sha256>` for the version-2 corrected payload. Plan 05.1-03 may act only when the payload hash, exact signal, response hash, receipt hash, immutable source/tree/artifact/target/policy/function/security/command/exclusion/rollback/stash identities all still match and its separate live operational gate passes immediately before push.
- Git commits `30955dc52da35097c744872a62de156ba3c23164` and `a22a88b25d663a4622cbbbb98057ff6c12108c6e`, prior payload digest `0d997347ba6840ad6f7e1dbf89fcfe530d2c8e7c943dcce4c6f0ce08e5c0f1d1`, and pending digest `050492bd188ead83d18ec83ee2121104d585a95a6b7752ea33bdfe3ce9dfa3bf` are audit-only invalid/unsealed/never-deployed history. Preserve them in Git, never reuse their signals, and do not rewrite, revert, amend, or erase them.
- The three protected stash objects are commit `913be95897066e53e6e1be0bd6e9483ab52a8437` / tree `482a7a5239b4801026406e321a8e3ec4213acbfb`, commit `2e6738fcf39a8a764be9e3664ee4023b01f051c9` / tree `1460e2564ce791e8abc5a4ce39c361fdfebdc552`, and commit `db09c3a2ff51dfa6652b36b0b809c1f9654f0117` / tree `1ec53bbf9099d84ce8068572b734515d994c70ca`.
</interfaces>

<immutable_payload_key_allowlist>
The owner-hashed payload is a closed schema. Its records MUST appear exactly once and in this exact order; the LF-joined key names, including a trailing LF, hash to `e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d`:

1. `payload_contract_version`
2. `payload_digest_policy`
3. `payload_serialization`
4. `payload_plan_id`
5. `payload_immutable_key_order_sha256`
6. `plan_01_evidence_path`
7. `plan_01_evidence_sha256`
8. `plan_01_evidence_blob_oid`
9. `plan_01_summary_path`
10. `plan_01_summary_sha256`
11. `plan_01_summary_blob_oid`
12. `source_commit`
13. `source_tree`
14. `migration_path`
15. `migration_sha256`
16. `migration_blob_oid`
17. `vitest_path`
18. `vitest_sha256`
19. `vitest_blob_oid`
20. `sql_integration_path`
21. `sql_integration_sha256`
22. `sql_integration_blob_oid`
23. `local_runner_path`
24. `local_runner_sha256`
25. `local_runner_blob_oid`
26. `plan_01_static_test_command_sha256`
27. `plan_01_static_test_output_sha256`
28. `plan_01_local_runner_command_sha256`
29. `plan_01_local_runner_output_sha256`
30. `plan_01_local_migration_manifest_sha256`
31. `plan_01_local_sql_result`
32. `plan_01_local_fixture_rollback_result`
33. `plan_01_local_teardown_result`
34. `canonical_cwd`
35. `cli_environment`
36. `cli_path`
37. `cli_resolved_path`
38. `cli_sha256`
39. `cli_version`
40. `target_supabase_project_ref`
41. `expected_remote_migration_policy`
42. `expected_remote_migration_policy_sha256`
43. `expected_pre_0063_function_signature`
44. `expected_pre_0063_function_definition_sha256`
45. `expected_pre_0063_function_security_sha256`
46. `expected_pre_0063_function_security_contract`
47. `expected_post_0063_function_signature`
48. `expected_post_0063_function_definition_sha256`
49. `expected_post_0063_function_security_sha256`
50. `expected_post_0063_function_security_contract`
51. `isolated_release_manifest`
52. `isolated_release_manifest_sha256`
53. `isolated_release_contains_0062`
54. `isolated_release_contains_later_migrations`
55. `linked_inventory_command_argv`
56. `dry_run_command_argv`
57. `dry_run_expected_pending_migrations`
58. `deployment_command_argv`
59. `deployment_max_attempts`
60. `excluded_pending_migration_path`
61. `excluded_pending_migration_sha256`
62. `explicit_exclusions`
63. `rollback_triggers`
64. `rollback_definition`
65. `authorization_scope`
66. `allowed_postconditions`
67. `protected_stash_1_commit`
68. `protected_stash_1_tree`
69. `protected_stash_1_subject`
70. `protected_stash_2_commit`
71. `protected_stash_2_tree`
72. `protected_stash_2_subject`
73. `protected_stash_3_commit`
74. `protected_stash_3_tree`
75. `protected_stash_3_subject`
76. `protected_stash_mutation_authorized`
77. `migration_0062_authorized`
78. `only_migration_0063_authorized`
79. `manual_worker_or_queue_intervention_authorized`
80. `ranking_history_deletion_authorized`
81. `storage_maintenance_authorized`
82. `scheduler_mutation_authorized`
83. `function_or_web_deployment_authorized`
84. `operational_drift_requires_new_owner_approval`
85. `immutable_drift_requires_new_owner_approval`
86. `volatile_observations_excluded_from_payload_digest`
87. `fresh_owner_approval_required`

Each payload line is exactly `key: value` with no indentation or control characters. SHA-256 values are 64 lowercase hexadecimal characters; Git commit/tree/blob identities are 40 lowercase hexadecimal characters; booleans are literal `true` or `false`; counts are unsigned base-10 integers. Composite arrays/objects use compact canonical JSON with sorted object keys and no insignificant whitespace. Unknown, duplicate, missing, out-of-order, blank, comment, continuation, or malformed records fail closed before the checkpoint and again before deployment.

The allowlist intentionally permits the deterministic Plan 01 test/runner output digests because those evidence bytes are source-anchored. It categorically excludes current production counts, ranking/run/item/log state, observation timestamps/windows, catalog estimates, hosted query/output hashes, dry-run output hashes, unrelated dirty-path counts/digests, and generic provenance. Put immutable stale-history audit facts in `<!-- containment-approval-v2-audit-begin -->` / `<!-- containment-approval-v2-audit-end -->` and volatile observations in `<!-- containment-approval-v2-observations-begin -->` / `<!-- containment-approval-v2-observations-end -->`; neither region participates in owner authorization.
</immutable_payload_key_allowlist>

<canonical_receipt_contract>
The envelope region contains exactly these records in order: `approval_contract_version`, `approval_status`, `approval_payload_sha256`, `required_owner_signal`. The payload hash field and required signal each occur exactly once in the entire file.

After approval, the receipt region contains exactly these records in order; the LF-joined key names, including a trailing LF, hash to `4323074504d17e4ee1b001e2edd303174ab083a6362d13f6c018655b3374d46b`:

1. `receipt_contract_version`
2. `receipt_serialization`
3. `receipt_key_order_sha256`
4. `receipt_plan_id`
5. `receipt_checkpoint_id`
6. `receipt_approval_actor`
7. `receipt_approval_signal`
8. `receipt_approval_signal_sha256`
9. `receipt_approved_payload_sha256`
10. `receipt_approved_at_utc`
11. `receipt_authorization_scope_sha256`
12. `receipt_operational_drift_requires_new_owner_approval`
13. `receipt_immutable_drift_requires_new_owner_approval`

The same flat-line grammar and fail-closed schema rules apply to the receipt. `receipt_approved_payload_sha256` is the sole receipt reference to the envelope's payload digest; the ambiguous envelope key name never appears inside the receipt. `receipt_approval_signal` must byte-equal `required_owner_signal`, and `receipt_approval_signal_sha256` hashes those exact signal bytes without a trailing newline. `receipt_authorization_scope_sha256` hashes the exact payload `authorization_scope` value without a trailing newline. `approval_receipt_sha256` occurs exactly once, solely inside the digest-marker region and outside the receipt-marker region. It hashes the exact LF-terminated receipt records between the receipt markers, excluding both markers and the external digest record.
</canonical_receipt_contract>
</context>

<prohibitions>
- Do not deploy, push, apply, repair, clean up, or otherwise mutate production in this plan. Only linked dry-run and sanitized read-only inventory/baseline operations are allowed.
- Do not authorize or apply migration 0062 or any migration other than the exact tested 0063 SHA-256. A repo-root dry run that proposes 0062 plus 0063 is invalid and cannot be approved.
- Do not authorize incremental/delta ranking, set-based staging, worker batching, retry redesign, feed/dashboard code, dismissal UI behavior, retention, or any source outside the exact 0063 migration (D-01).
- Do not call score-tick, invoke `enqueue_deterministic_new_jobs`, claim/stage/finalize work, trigger cron, retry a run, cancel a run, expire or rewrite a lease, change ranking state, or repair queue rows against production. Re-executing Plan 01's disposable local SQL runner is mandatory and is the only synthetic queue/function activity allowed; natural production work must finish or expire without intervention (D-02).
- Do not delete, truncate, update, archive, or compact ranking runs/items/history. Do not run vacuum, analyze, reindex, cluster, file shrink, or any storage-maintenance command, including `VACUUM FULL` (D-04).
- Do not deploy an Edge Function or web build, change cron/schedules/secrets/JWT settings, push Git commits to a remote, restore paid/background AI scoring, or perform later-phase verification under this approval.
- Do not apply, pop, drop, clear, create, rewrite, or otherwise alter Git stashes. Read-only `git stash list`/object inspection is allowed solely to prove the three exact protected commits and trees remain present.
- Do not require global worktree cleanliness and do not stash, clean, reset, restore, stage, commit, or overwrite unrelated owner work. Require cleanliness only for the exact Plan 01 release-source paths and preserve a digest of unrelated dirty-path inventory without its contents.
- Do not print or persist access tokens, database passwords, service-role keys, connection strings, owner IDs, job IDs, resume text, or row-level production data. Evidence stores only safe project identity, aggregate counts, hashes, versions, statuses, and timestamps.
- Do not install or update packages. Use the repository-pinned Supabase CLI and existing Node/Vitest/jq tooling.
- Do not treat silence, `yes`, generic approval, a normalized/retyped response, either stale payload commit/digest, an expired prior response, or approval for another plan as authorization. Immutable source, evidence, target-policy, migration, function/security, isolated-command, rollback, stash, exclusion, or scope drift requires a newly generated payload and owner checkpoint. Normal operational count/state/log/timestamp drift does not; it is handled only by Plan 05.1-03's live fail-closed gate per D-20.
</prohibitions>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the immutable read-only containment approval payload</name>
  <read_first>
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
    - .planning/debug/job-copilot-performance.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-PLAN-OUTLINE.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-SUMMARY.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json
    - supabase/migrations/0062_phase_03_11_asset_manager_payments_workday.sql
    - supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
    - web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts
    - supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
    - scripts/verify-phase-05-1-containment-local.mjs
    - supabase/config.toml
    - web/package.json
    - web/package-lock.json
    - .planning/milestones/v1.0-phases/03-scoring-feed-notifications/03-10-SUMMARY.md
  </read_first>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md</files>
  <action>
    Fail closed unless Plan 05.1-01 is complete and its evidence validates exactly: schema/plan identity, static and real-local-SQL results passed, local teardown passed, approval not requested, deployment not deployed, every production-effect flag false, source commit/tree resolvable, all four source artifacts present in that commit with exact SHA-256/blob IDs, and the recorded commands pass again. Use the evidence's source commit as the immutable release source; never substitute current HEAD. Require the migration, Vitest, SQL integration, local runner, and evidence paths clean in both worktree and index, while hashing only the names/statuses of unrelated dirty paths and leaving them untouched.

    Before loading hosted credentials or running any linked command, from canonical repository-root cwd rerun the exact Vitest command and `SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-containment-local.mjs`. Require the runner to inject that exact environment into every CLI subprocess and recompute/compare its environment, canonical output, command-array, exact 0001-0061-plus-0063 manifest, excluded-0062, literal/resolved CLI, CLI SHA/version, SQL-test, fixture-rollback, and teardown identities with Plan 01 evidence. Production must not be the first SQL execution. Missing Docker/local Postgres/pgTAP or any local mismatch blocks approval; never fall back to a linked database.

    Reconfirm each protected Phase 5 stash commit is still present in `git stash list`, resolves to its exact tree above, and retains its recorded subject. Record commit, tree, subject, and presence for all three; stash indices are informational and never identity. Recompute and record the SHA-256 of excluded migration 0062 so the payload can reject its inclusion rather than ignore it.

    With existing credentials loaded privately, perform bounded, sanitized, read-only hosted inspection. Require the exact linked Supabase project ref expected by Plan 01 and remote migration parity through 0061 with neither 0062 nor 0063 applied; any extra, missing, repaired, or reordered migration blocks payload creation. Bind the target and the expected ordered pre-migration policy, not timestamped inventory output bytes. Capture and bind the expected pre-0063 `enqueue_deterministic_new_jobs(integer)` definition plus owner/security-definer/search-path/execute-grant identity, and derive/bind the expected post-0063 definition/security identity from the approved migration and passing local SQL evidence.

    Separately capture aggregate-only informational observations with a bounded statement timeout: ranking state by status, building runs by reason/status, pending/claimed items belonging to building runs by status/attempt, catalog estimates for ranking-item live/dead rows and bytes rather than a whole-table count, and the preceding 24-hour score-tick HTTP status totals. Record query/output provenance and observation times without identifiers or credentials. Keep this complete informational region outside the canonical approval payload markers and label it non-authorizing, excluded from `approval_payload_sha256`, and expected to drift. These observations help the owner understand current conditions but are never compared byte-for-byte when sealing or consuming approval; Plan 05.1-03 must remeasure its conservative safe-state predicate immediately before push per D-20.

    Reconstruct a fresh temporary Supabase release root from the recorded source commit, containing exact migrations 0001 through 0061 and exact 0063 plus the minimum linked configuration, with 0062 and every later migration absent. Canonically hash the ordered path/SHA-256 inventory. From canonical repository-root cwd run literal executable `web/node_modules/.bin/supabase` with exact argv `db push --dry-run --linked --yes --workdir {fresh-isolated-root}`; any linked CLI inventory/preflight command must use the same cwd/executable and include `--workdir {fresh-isolated-root}`. The dry run must propose exactly `0063_dismissal_tombstone_ranking_containment.sql` and no repair or other migration. Bind the semantic sole-0063 expectation plus `canonical_cwd`, literal `cli_path`, `cli_resolved_path`, resolved executable SHA-256/version, exact `dry_run_command_argv`, and exact deployment command template with argv `db push --linked --yes --workdir {fresh-isolated-root}`, invocation count one. Keep any sanitized dry-run output digest as provenance outside approval equality because the output is a new observation; Plan 05.1-03 reruns the semantic gate. Remove only the validated temporary root after recording evidence.

    Replace the pending approval artifact with a newly generated request using the exact version-2 envelope/payload/audit/observation markers declared in `<interfaces>`. The envelope contains only `approval_contract_version: 2`, `approval_status: AWAITING_OWNER_APPROVAL`, the computed `approval_payload_sha256`, and the exact `required_owner_signal`, in that order. Retain exact audit fields `stale_payload_commits`, `stale_payload_digests`, `stale_payload_history_status: INVALID_UNSEALED_NEVER_DEPLOYED`, and `stale_payload_history_reusable: false` only in the external audit region. Populate the two commit fields with `30955dc52da35097c744872a62de156ba3c23164` and `a22a88b25d663a4622cbbbb98057ff6c12108c6e`, and the two digest fields with `0d997347ba6840ad6f7e1dbf89fcfe530d2c8e7c943dcce4c6f0ce08e5c0f1d1` and `050492bd188ead83d18ec83ee2121104d585a95a6b7752ea33bdfe3ce9dfa3bf`. Mark all associated signals invalid and non-reusable. Preserve Git history; do not amend, revert, or delete those commits.

    Serialize the canonical payload as the exact 87-record closed allowlist in `<immutable_payload_key_allowlist>`, including `payload_immutable_key_order_sha256: e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d`. Bind only the exact Plan 01 evidence/source and four artifact identities; target project; `expected_remote_migration_policy: ordered_0001_through_0061;0062_absent;0063_absent`; expected pre- and post-0063 function definition/security identities; canonical isolated-release manifest; sole-0063 dry-run semantic and exact command identities; `canonical_cwd`; literal/resolved CLI identity; allowed one-push action and bounded postconditions; all three stash identities; exhaustive exclusions; and forward-only rollback rules. Record `linked_inventory_command_argv` exactly as `web/node_modules/.bin/supabase migration list --linked --workdir {fresh-isolated-root}`, `dry_run_command_argv` exactly as `web/node_modules/.bin/supabase db push --dry-run --linked --yes --workdir {fresh-isolated-root}`, `dry_run_expected_pending_migrations` as canonical JSON `["0063_dismissal_tombstone_ranking_containment.sql"]`, and `deployment_command_argv` exactly as `web/node_modules/.bin/supabase db push --linked --yes --workdir {fresh-isolated-root}`, both mutation commands from repository-root cwd and with the pinned environment. Canonicalize every allowlisted composite value as compact sorted-key JSON. Hash each paired raw contract value exactly as named, including the migration policy and isolated manifest. Set every authorization boolean explicitly, including `volatile_observations_excluded_from_payload_digest: true` and `fresh_owner_approval_required: true`.

    Before presenting the artifact, parse every line between the exact payload markers and every line between the exact envelope markers. Reject the artifact on unknown, duplicate, missing, out-of-order, blank, comment, continuation, malformed, noncanonical JSON, invalid SHA/OID, extra marker, or missing marker input. This allowlist validation—not a volatile-field blacklist—proves that production counts, ranking owner/run/item/log state, observation times/windows, catalog estimates, hosted query/output hashes, dry-run output hashes, unrelated dirty-path metrics, and generic provenance are outside approval equality. Require the external audit and observation markers exactly once each; require no receipt or receipt-digest markers/fields before owner response. Implement no-match checks as successful numeric zero counts while preserving nonzero parser/file/command failures; do not suppress a failed search or pipeline.

    Encode rollback and the live gate precisely. Immutable before-push drift means `NO_DEPLOY_NEW_APPROVAL_REQUIRED`. Operational unsafe state means `NO_DEPLOY_NATURAL_DRAIN`: allow scheduler-owned completion/lease expiry and bounded aggregate-only rechecks, with no worker, repair, run/item/lease/state/config/scheduler mutation and no new owner approval unless an immutable identity or scope changes. A nonzero or transaction-failed push requires read-only proof of whether 0063 was recorded and the prior function/migration identity remains; then stop without retry. Any post-commit migration, function-definition, ownership, grant, target, or scope mismatch means `FORWARD_RESTORE_APPROVAL_REQUIRED`: stop all further mutation, preserve evidence, and require a separately tested and separately approved forward migration restoring the approved contract. Never edit/delete deployed migration history, run a down migration, invoke workers, or repair queues. Hash the exact immutable payload-region bytes and print the fresh expected owner signal containing that digest. Production migration/function identities and all protected stashes must remain unchanged after this task.
  </action>
  <acceptance_criteria>
    - Plan 01 source commit/tree, all four source/evidence/summary paths and identities, static command, real-local-SQL runner output/manifest/CLI/fixture rollback/teardown are independently revalidated before linked access; no moving HEAD or uncommitted byte is approved.
    - Hosted target and migration history are read-only proven as exact 0001-0061, with 0062 and 0063 absent before approval.
    - The isolated migration inventory contains only 0001-0061 plus 0063, and its linked dry run proposes only exact 0063; 0062's path/hash is explicitly excluded.
    - The version-2 payload binds the pre- and expected post-0063 function/security identities, repository-root cwd, literal and resolved CLI identity, exact `--workdir {fresh-isolated-root}` dry-run/push argv, one-invocation contract, postconditions, and forward-only rollback definition.
    - Aggregate production counts, owner/run/item state, observation timestamps, catalog estimates, fixed-window log totals, and their provenance appear only in a labeled non-authorizing region outside the payload bytes; ordinary drift cannot invalidate immutable approval.
    - All three protected stash commit/tree/subject identities are present and payload-bound; unrelated work is inventory-hashed but untouched.
    - Allowed scope is exact 0063 plus read-only checks. Every later ranking, worker, dashboard, retention, deletion, maintenance, queue, scheduler, function, web, and Git-remote action is explicitly non-authorized.
    - The two stale payload commits and both prior pending digests remain audit-visible as invalid, unsealed, never deployed, and non-reusable without rewriting history.
    - `approval_payload_sha256` equals the exact corrected immutable-only payload-region bytes, and a newly requested owner signal embeds that digest.
    - Remote migrations/function, queue/history/storage, stashes, source, and external deployments remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>
      set -euo pipefail
      containment_approval=.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md
      payload_begin='&lt;!-- containment-approval-v2-payload-begin --&gt;'
      payload_end='&lt;!-- containment-approval-v2-payload-end --&gt;'
      envelope_begin='&lt;!-- containment-approval-v2-envelope-begin --&gt;'
      envelope_end='&lt;!-- containment-approval-v2-envelope-end --&gt;'
      audit_begin='&lt;!-- containment-approval-v2-audit-begin --&gt;'
      audit_end='&lt;!-- containment-approval-v2-audit-end --&gt;'
      observations_begin='&lt;!-- containment-approval-v2-observations-begin --&gt;'
      observations_end='&lt;!-- containment-approval-v2-observations-end --&gt;'
      receipt_begin='&lt;!-- containment-owner-receipt-v2-begin --&gt;'
      receipt_end='&lt;!-- containment-owner-receipt-v2-end --&gt;'
      receipt_digest_begin='&lt;!-- containment-owner-receipt-v2-digest-begin --&gt;'
      receipt_digest_end='&lt;!-- containment-owner-receipt-v2-digest-end --&gt;'
      region_keys() {
        awk -v begin="$1" -v end="$2" '
          $0 == begin { begin_count++; if (begin_count != 1 || inside || end_count) exit 41; inside=1; next }
          $0 == end { end_count++; if (!inside || end_count != 1) exit 42; inside=0; next }
          inside {
            if ($0 ~ /^[[:space:]]*$/ || $0 ~ /^[[:space:]]*#/) exit 43
            if ($0 !~ /^[a-z][a-z0-9_]*: [^[:cntrl:]]+$/) exit 44
            key=$0; sub(/:.*/, "", key)
            value=substr($0, length(key) + 3)
            if (key ~ /_sha256$/ &amp;&amp; (length(value) != 64 || value !~ /^[0-9a-f]+$/)) exit 45
            if (key ~ /(_blob_oid|_commit|_tree)$/ &amp;&amp; (length(value) != 40 || value !~ /^[0-9a-f]+$/)) exit 46
            print key
          }
          END { if (begin_count != 1 || end_count != 1 || inside) exit 47 }
        ' "$3"
      }
      region_value() {
        awk -v begin="$1" -v end="$2" -v name="$3" '
          $0 == begin { inside=1; next }
          $0 == end { inside=0; next }
          inside {
            sep=index($0, ": "); key=substr($0, 1, sep - 1)
            if (key == name) { count++; value=substr($0, sep + 2) }
          }
          END { if (count != 1) exit 51; print value }
        ' "$4"
      }
      region_sha256() {
        awk -v begin="$1" -v end="$2" '
          $0 == begin { inside=1; next }
          $0 == end { inside=0; next }
          inside { print }
        ' "$3" | shasum -a 256 | awk '{print $1}'
      }
      marker_count() {
        awk -v marker="$1" '$0 == marker { count++ } END { print count + 0 }' "$2"
      }
      key_count() {
        awk -v name="$1" '
          {
            line=$0; sub(/^[[:space:]]*/, "", line); sep=index(line, ": ")
            if (sep &gt; 0 &amp;&amp; substr(line, 1, sep - 1) == name) count++
          }
          END { print count + 0 }
        ' "$2"
      }
      require_canonical_json() {
        value="$(region_value "$payload_begin" "$payload_end" "$1" "$containment_approval")"
        normalized="$(printf '%s' "$value" | jq -ceS .)"
        test "$normalized" = "$value"
      }
      payload_keys_csv='payload_contract_version,payload_digest_policy,payload_serialization,payload_plan_id,payload_immutable_key_order_sha256,plan_01_evidence_path,plan_01_evidence_sha256,plan_01_evidence_blob_oid,plan_01_summary_path,plan_01_summary_sha256,plan_01_summary_blob_oid,source_commit,source_tree,migration_path,migration_sha256,migration_blob_oid,vitest_path,vitest_sha256,vitest_blob_oid,sql_integration_path,sql_integration_sha256,sql_integration_blob_oid,local_runner_path,local_runner_sha256,local_runner_blob_oid,plan_01_static_test_command_sha256,plan_01_static_test_output_sha256,plan_01_local_runner_command_sha256,plan_01_local_runner_output_sha256,plan_01_local_migration_manifest_sha256,plan_01_local_sql_result,plan_01_local_fixture_rollback_result,plan_01_local_teardown_result,canonical_cwd,cli_environment,cli_path,cli_resolved_path,cli_sha256,cli_version,target_supabase_project_ref,expected_remote_migration_policy,expected_remote_migration_policy_sha256,expected_pre_0063_function_signature,expected_pre_0063_function_definition_sha256,expected_pre_0063_function_security_sha256,expected_pre_0063_function_security_contract,expected_post_0063_function_signature,expected_post_0063_function_definition_sha256,expected_post_0063_function_security_sha256,expected_post_0063_function_security_contract,isolated_release_manifest,isolated_release_manifest_sha256,isolated_release_contains_0062,isolated_release_contains_later_migrations,linked_inventory_command_argv,dry_run_command_argv,dry_run_expected_pending_migrations,deployment_command_argv,deployment_max_attempts,excluded_pending_migration_path,excluded_pending_migration_sha256,explicit_exclusions,rollback_triggers,rollback_definition,authorization_scope,allowed_postconditions,protected_stash_1_commit,protected_stash_1_tree,protected_stash_1_subject,protected_stash_2_commit,protected_stash_2_tree,protected_stash_2_subject,protected_stash_3_commit,protected_stash_3_tree,protected_stash_3_subject,protected_stash_mutation_authorized,migration_0062_authorized,only_migration_0063_authorized,manual_worker_or_queue_intervention_authorized,ranking_history_deletion_authorized,storage_maintenance_authorized,scheduler_mutation_authorized,function_or_web_deployment_authorized,operational_drift_requires_new_owner_approval,immutable_drift_requires_new_owner_approval,volatile_observations_excluded_from_payload_digest,fresh_owner_approval_required'
      expected_payload_keys="$(printf '%s' "$payload_keys_csv" | tr ',' '\n')"
      test "$(pwd -P)" = "$(git rev-parse --show-toplevel)"
      npm --prefix web test -- migration-0063-dismissal-tombstone-ranking-containment.test.ts deterministic-ranking-migration.test.ts deterministic-ranking-snapshot.integration.test.ts resume-routing-migration.test.ts
      SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-containment-local.mjs
      jq -e '.schema_version == 1 and .plan_id == "05.1-01" and .verification.result == "passed" and .local_database_verification.result == "passed" and .local_database_verification.teardown == "passed" and .local_database_verification.cli_environment.SUPABASE_TELEMETRY_DISABLED == "1" and .local_database_verification.hosted_or_linked_writes == 0 and .approval_status == "not_requested" and .deployment_status == "not_deployed" and .release_boundary.hosted_schema_through == "0061" and .release_boundary.preexisting_pending_migrations == ["supabase/migrations/0062_phase_03_11_asset_manager_payments_workday.sql"] and .release_boundary.containment_release_migrations == ["supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"] and ([.scope[]] | all(. == false)) and (.preserved_stashes | length == 3)' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json
      test -s "$containment_approval"
      actual_payload_keys="$(region_keys "$payload_begin" "$payload_end" "$containment_approval")"
      test "$actual_payload_keys" = "$expected_payload_keys"
      test "$(printf '%s\n' "$actual_payload_keys" | shasum -a 256 | awk '{print $1}')" = 'e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d'
      test "$(region_value "$payload_begin" "$payload_end" payload_immutable_key_order_sha256 "$containment_approval")" = 'e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d'
      test "$(region_keys "$envelope_begin" "$envelope_end" "$containment_approval")" = "$(printf '%s\n' approval_contract_version approval_status approval_payload_sha256 required_owner_signal)"
      for marker in "$payload_begin" "$payload_end" "$envelope_begin" "$envelope_end" "$audit_begin" "$audit_end" "$observations_begin" "$observations_end"; do test "$(marker_count "$marker" "$containment_approval")" -eq 1; done
      for marker in "$receipt_begin" "$receipt_end" "$receipt_digest_begin" "$receipt_digest_end"; do test "$(marker_count "$marker" "$containment_approval")" -eq 0; done
      test "$(key_count approval_payload_sha256 "$containment_approval")" -eq 1
      test "$(key_count required_owner_signal "$containment_approval")" -eq 1
      test "$(key_count receipt_approved_payload_sha256 "$containment_approval")" -eq 0
      test "$(key_count approval_receipt_sha256 "$containment_approval")" -eq 0
      test "$(region_value "$envelope_begin" "$envelope_end" approval_contract_version "$containment_approval")" = '2'
      test "$(region_value "$envelope_begin" "$envelope_end" approval_status "$containment_approval")" = 'AWAITING_OWNER_APPROVAL'
      test "$(region_value "$payload_begin" "$payload_end" payload_contract_version "$containment_approval")" = '2'
      test "$(region_value "$payload_begin" "$payload_end" payload_digest_policy "$containment_approval")" = 'immutable_authorization_only'
      test "$(region_value "$payload_begin" "$payload_end" payload_serialization "$containment_approval")" = 'flat_key_value_lf_v2'
      test "$(region_value "$payload_begin" "$payload_end" payload_plan_id "$containment_approval")" = '05.1-02'
      test "$(region_value "$payload_begin" "$payload_end" canonical_cwd "$containment_approval")" = 'repository-root'
      test "$(region_value "$payload_begin" "$payload_end" cli_environment "$containment_approval")" = 'SUPABASE_TELEMETRY_DISABLED=1'
      test "$(region_value "$payload_begin" "$payload_end" expected_remote_migration_policy "$containment_approval")" = 'ordered_0001_through_0061;0062_absent;0063_absent'
      test "$(region_value "$payload_begin" "$payload_end" linked_inventory_command_argv "$containment_approval")" = 'web/node_modules/.bin/supabase migration list --linked --workdir {fresh-isolated-root}'
      test "$(region_value "$payload_begin" "$payload_end" dry_run_command_argv "$containment_approval")" = 'web/node_modules/.bin/supabase db push --dry-run --linked --yes --workdir {fresh-isolated-root}'
      test "$(region_value "$payload_begin" "$payload_end" dry_run_expected_pending_migrations "$containment_approval")" = '["0063_dismissal_tombstone_ranking_containment.sql"]'
      test "$(region_value "$payload_begin" "$payload_end" deployment_command_argv "$containment_approval")" = 'web/node_modules/.bin/supabase db push --linked --yes --workdir {fresh-isolated-root}'
      test "$(region_value "$payload_begin" "$payload_end" deployment_max_attempts "$containment_approval")" = '1'
      test "$(region_value "$payload_begin" "$payload_end" excluded_pending_migration_path "$containment_approval")" = 'supabase/migrations/0062_phase_03_11_asset_manager_payments_workday.sql'
      test "$(region_value "$payload_begin" "$payload_end" isolated_release_contains_0062 "$containment_approval")" = 'false'
      test "$(region_value "$payload_begin" "$payload_end" isolated_release_contains_later_migrations "$containment_approval")" = 'false'
      for key in protected_stash_mutation_authorized migration_0062_authorized manual_worker_or_queue_intervention_authorized ranking_history_deletion_authorized storage_maintenance_authorized scheduler_mutation_authorized function_or_web_deployment_authorized operational_drift_requires_new_owner_approval; do test "$(region_value "$payload_begin" "$payload_end" "$key" "$containment_approval")" = 'false'; done
      for key in only_migration_0063_authorized immutable_drift_requires_new_owner_approval volatile_observations_excluded_from_payload_digest fresh_owner_approval_required; do test "$(region_value "$payload_begin" "$payload_end" "$key" "$containment_approval")" = 'true'; done
      for key in expected_pre_0063_function_security_contract expected_post_0063_function_security_contract isolated_release_manifest dry_run_expected_pending_migrations explicit_exclusions rollback_triggers rollback_definition authorization_scope allowed_postconditions; do require_canonical_json "$key"; done
      containment_payload_expected="$(region_value "$envelope_begin" "$envelope_end" approval_payload_sha256 "$containment_approval")"
      containment_payload_actual="$(region_sha256 "$payload_begin" "$payload_end" "$containment_approval")"
      test "$containment_payload_actual" = "$containment_payload_expected"
      test "$containment_payload_expected" != '0d997347ba6840ad6f7e1dbf89fcfe530d2c8e7c943dcce4c6f0ce08e5c0f1d1'
      test "$containment_payload_expected" != '050492bd188ead83d18ec83ee2121104d585a95a6b7752ea33bdfe3ce9dfa3bf'
      test "$(region_value "$envelope_begin" "$envelope_end" required_owner_signal "$containment_approval")" = "APPROVE 05.1-02 CONTAINMENT $containment_payload_expected"
      rg -F 'stale_payload_commits: [30955dc52da35097c744872a62de156ba3c23164,a22a88b25d663a4622cbbbb98057ff6c12108c6e]' "$containment_approval"
      rg -F 'stale_payload_digests: [0d997347ba6840ad6f7e1dbf89fcfe530d2c8e7c943dcce4c6f0ce08e5c0f1d1,050492bd188ead83d18ec83ee2121104d585a95a6b7752ea33bdfe3ce9dfa3bf]' "$containment_approval"
      rg -F 'stale_payload_history_status: INVALID_UNSEALED_NEVER_DEPLOYED' "$containment_approval"
      rg -F 'stale_payload_history_reusable: false' "$containment_approval"
      git stash list --format='%H' | grep -Fx '913be95897066e53e6e1be0bd6e9483ab52a8437'
      git stash list --format='%H' | grep -Fx '2e6738fcf39a8a764be9e3664ee4023b01f051c9'
      git stash list --format='%H' | grep -Fx 'db09c3a2ff51dfa6652b36b0b809c1f9654f0117'
      git diff --check -- "$containment_approval"
    </automated>
  </verify>
  <done>A corrected immutable-only source/target-policy/function-security/rollback/command/exclusion/stash payload plus separately labeled informational observations is ready for a fresh owner signal, with production unchanged.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve or defer only the exact 0063 containment deployment</name>
  <read_first>
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-SUMMARY.md
    - supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
    - supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
    - scripts/verify-phase-05-1-containment-local.mjs
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
  </read_first>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md</files>
  <decision>Authorize or defer only the corrected immutable-payload-hash-bound containment migration 0063, its mandatory live operational gate, and its read-only post-deployment identity checks.</decision>
  <context>
    Display the exact version-2 payload SHA-256; Plan 01 source commit/tree and evidence/artifact identities; Supabase target; expected ordered 0001-0061 policy with 0062/0063 absent; isolated 0001-0061-plus-0063 manifest and sole-0063 command contract; pre- and expected post-0063 function/security identities; canonical repository-root cwd and CLI identity; stash identities; authorization scope; rollback rules; and every exclusion. Show aggregate production observations in a separate informational section labeled excluded from approval equality. Per D-20, these counts may drift and Plan 05.1-03 must remeasure safe state immediately before push; unsafe state yields NO_DEPLOY and natural drain only. Per D-02/D-04, no worker/queue intervention, history deletion, or maintenance is authorized. Only immutable identity or scope drift invalidates this checkpoint.
  </context>
  <action>
    Immediately before presentation, from repository-root cwd rerun Plan 01's real local runner first, then every immutable hosted read-only/linked preflight using literal `web/node_modules/.bin/supabase` and `--workdir {fresh-isolated-root}`, then the exact dry-run, payload-hash, function/security, target-policy, and stash gates. Rerun Task 1's closed-schema parser against the exact version-2 envelope and payload markers: all 87 allowlisted payload keys and all four envelope keys must be present once in canonical order, the key-order hash and payload hash must match, each marker and top-level approval field must have the prescribed occurrence count, and no receipt field may exist yet. A zero occurrence is a valid negative result only when the parser/search command itself succeeds; any file/read/parser/pipeline failure blocks the checkpoint. If an immutable approved field drifts, generate a new pending payload and digest and present only its fresh exact signal. Refresh informational production aggregates outside the payload without changing its digest; normal count/state/log/timestamp drift never recycles this checkpoint. State plainly that approval permits Plan 05.1-03 to reconstruct the approved isolated root, run a fresh fail-closed immutable plus operational gate immediately before the command, and execute the byte-identical one-push command only when all safe-state counts are zero. Unsafe operation permits natural drain/read-only recheck only. Preserve all exclusions and perform no production or stash mutation inside this checkpoint.
  </action>
  <options>
    <option id="approve-exact-containment">
      <name>Approve exact containment</name>
      <pros>Authorizes only the displayed tombstone-containment migration and read-only identity verification.</pros>
      <cons>Mutates the displayed production schema once; later stabilization work remains separately gated.</cons>
    </option>
    <option id="defer-containment">
      <name>Defer containment</name>
      <pros>Keeps production unchanged.</pros>
      <cons>The confirmed phantom ranking producer remains unfixed in production.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The owner sees one corrected immutable-only payload digest and every bound source/evidence/migration/target-policy/function-security/command/rollback/stash/scope identity before deciding; volatile observations are visibly separate and non-authorizing.
    - Only a newly returned `APPROVE 05.1-02 CONTAINMENT <approval_payload_sha256>` for `approval_contract_version: 2` is acceptable; silence, generic assent, commit 30955dc/a22a88b signals, and both prior pending digests are rejected.
    - Approval is non-transitive and becomes void on immutable identity or scope drift. Normal production count/state/log/timestamp drift does not void it; Plan 05.1-03 fails closed until its live gate passes.
    - No production, queue, history, storage, source, remote Git, deployment, or stash mutation occurs at the checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>
      set -euo pipefail
      containment_approval=.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md
      payload_begin='&lt;!-- containment-approval-v2-payload-begin --&gt;'
      payload_end='&lt;!-- containment-approval-v2-payload-end --&gt;'
      envelope_begin='&lt;!-- containment-approval-v2-envelope-begin --&gt;'
      envelope_end='&lt;!-- containment-approval-v2-envelope-end --&gt;'
      receipt_begin='&lt;!-- containment-owner-receipt-v2-begin --&gt;'
      receipt_end='&lt;!-- containment-owner-receipt-v2-end --&gt;'
      receipt_digest_begin='&lt;!-- containment-owner-receipt-v2-digest-begin --&gt;'
      receipt_digest_end='&lt;!-- containment-owner-receipt-v2-digest-end --&gt;'
      region_keys() {
        awk -v begin="$1" -v end="$2" '
          $0 == begin { begin_count++; if (begin_count != 1 || inside || end_count) exit 41; inside=1; next }
          $0 == end { end_count++; if (!inside || end_count != 1) exit 42; inside=0; next }
          inside {
            if ($0 ~ /^[[:space:]]*$/ || $0 ~ /^[[:space:]]*#/) exit 43
            if ($0 !~ /^[a-z][a-z0-9_]*: [^[:cntrl:]]+$/) exit 44
            key=$0; sub(/:.*/, "", key); value=substr($0, length(key) + 3)
            if (key ~ /_sha256$/ &amp;&amp; (length(value) != 64 || value !~ /^[0-9a-f]+$/)) exit 45
            if (key ~ /(_blob_oid|_commit|_tree)$/ &amp;&amp; (length(value) != 40 || value !~ /^[0-9a-f]+$/)) exit 46
            print key
          }
          END { if (begin_count != 1 || end_count != 1 || inside) exit 47 }
        ' "$3"
      }
      region_value() {
        awk -v begin="$1" -v end="$2" -v name="$3" '
          $0 == begin { inside=1; next } $0 == end { inside=0; next }
          inside { sep=index($0, ": "); key=substr($0, 1, sep - 1); if (key == name) { count++; value=substr($0, sep + 2) } }
          END { if (count != 1) exit 51; print value }
        ' "$4"
      }
      region_sha256() {
        awk -v begin="$1" -v end="$2" '$0 == begin { inside=1; next } $0 == end { inside=0; next } inside { print }' "$3" | shasum -a 256 | awk '{print $1}'
      }
      marker_count() { awk -v marker="$1" '$0 == marker { count++ } END { print count + 0 }' "$2"; }
      key_count() {
        awk -v name="$1" '{ line=$0; sub(/^[[:space:]]*/, "", line); sep=index(line, ": "); if (sep &gt; 0 &amp;&amp; substr(line, 1, sep - 1) == name) count++ } END { print count + 0 }' "$2"
      }
      test -s "$containment_approval"
      payload_keys="$(region_keys "$payload_begin" "$payload_end" "$containment_approval")"
      test "$(printf '%s\n' "$payload_keys" | awk 'END { print NR }')" -eq 87
      test "$(printf '%s\n' "$payload_keys" | shasum -a 256 | awk '{print $1}')" = 'e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d'
      test "$(region_value "$payload_begin" "$payload_end" payload_immutable_key_order_sha256 "$containment_approval")" = 'e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d'
      test "$(region_keys "$envelope_begin" "$envelope_end" "$containment_approval")" = "$(printf '%s\n' approval_contract_version approval_status approval_payload_sha256 required_owner_signal)"
      for marker in "$payload_begin" "$payload_end" "$envelope_begin" "$envelope_end"; do test "$(marker_count "$marker" "$containment_approval")" -eq 1; done
      for marker in "$receipt_begin" "$receipt_end" "$receipt_digest_begin" "$receipt_digest_end"; do test "$(marker_count "$marker" "$containment_approval")" -eq 0; done
      test "$(key_count approval_payload_sha256 "$containment_approval")" -eq 1
      test "$(key_count required_owner_signal "$containment_approval")" -eq 1
      test "$(key_count receipt_approved_payload_sha256 "$containment_approval")" -eq 0
      test "$(key_count approval_receipt_sha256 "$containment_approval")" -eq 0
      test "$(region_value "$envelope_begin" "$envelope_end" approval_contract_version "$containment_approval")" = '2'
      test "$(region_value "$envelope_begin" "$envelope_end" approval_status "$containment_approval")" = 'AWAITING_OWNER_APPROVAL'
      test "$(region_value "$payload_begin" "$payload_end" payload_contract_version "$containment_approval")" = '2'
      test "$(region_value "$payload_begin" "$payload_end" payload_digest_policy "$containment_approval")" = 'immutable_authorization_only'
      test "$(region_value "$payload_begin" "$payload_end" payload_serialization "$containment_approval")" = 'flat_key_value_lf_v2'
      containment_payload_expected="$(region_value "$envelope_begin" "$envelope_end" approval_payload_sha256 "$containment_approval")"
      test "$(region_sha256 "$payload_begin" "$payload_end" "$containment_approval")" = "$containment_payload_expected"
      test "$containment_payload_expected" != '0d997347ba6840ad6f7e1dbf89fcfe530d2c8e7c943dcce4c6f0ce08e5c0f1d1'
      test "$containment_payload_expected" != '050492bd188ead83d18ec83ee2121104d585a95a6b7752ea33bdfe3ce9dfa3bf'
      test "$(region_value "$envelope_begin" "$envelope_end" required_owner_signal "$containment_approval")" = "APPROVE 05.1-02 CONTAINMENT $containment_payload_expected"
      git diff --check -- "$containment_approval"
    </automated>
    <human-check>Approve only the corrected immutable 0063 authorization by returning its full fresh digest-bearing signal, or defer. Confirm that volatile observations are informational, Plan 03 must pass its immediate zero-active-work gate, and 0062, manual workers, queue repair, scheduler pause, history deletion, storage maintenance, later ranking/UI/retention work, and stash changes are not authorized.</human-check>
  </verify>
  <resume-signal>Reply with the exact newly generated `APPROVE 05.1-02 CONTAINMENT &lt;approval_payload_sha256&gt;` signal for contract version 2, or reply `DEFER 05.1-02 CONTAINMENT &lt;approval_payload_sha256&gt;`. No prior signal is reusable.</resume-signal>
  <done>The owner explicitly approves the corrected immutable-only containment payload with a fresh exact signal, or the plan stops with production unchanged and Plan 05.1-03 blocked.</done>
</task>

<task type="auto">
  <name>Task 3: Seal the exact owner approval receipt without deploying</name>
  <read_first>
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-SUMMARY.md
    - supabase/migrations/0062_phase_03_11_asset_manager_payments_workday.sql
    - supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
    - supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
    - scripts/verify-phase-05-1-containment-local.mjs
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
  </read_first>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md</files>
  <action>
    Run this task only after the raw checkpoint response byte-for-byte equals the newly required approval signal for the currently revalidated version-2 payload and differs from every stale signal. From repository-root cwd recompute Plan 01's source/evidence/artifact identities and real-local-SQL proof first; then recompute the target, expected 0001-0061-with-0062/0063-absent policy, pre- and expected post-0063 function/security identities, isolated manifest, sole-0063 command semantics, exact root-cwd CLI/argv contract, exclusions, rollback, authorization scope, and all stash identities. Rerun the closed-schema payload/envelope parser before accepting the response; unknown, duplicate, missing, out-of-order, malformed, or marker-invalid input fails closed. Immutable drift invalidates the response and returns to a newly generated Task 2 checkpoint. Refreshing or observing ordinary operational aggregates does not alter the payload or invalidate the response.

    Change only the envelope status to `OWNER_APPROVED`, then append one receipt between the exact version-2 receipt markers. Serialize exactly the 13 ordered namespaced fields in `<canonical_receipt_contract>`: `receipt_contract_version: 2`, `receipt_serialization: flat_key_value_lf_v2`, `receipt_key_order_sha256: 4323074504d17e4ee1b001e2edd303174ab083a6362d13f6c018655b3374d46b`, `receipt_plan_id: 05.1-02`, `receipt_checkpoint_id: 05.1-02/task-2/blocking-human`, `receipt_approval_actor: jackshi812`, the exact raw `receipt_approval_signal`, its no-newline `receipt_approval_signal_sha256`, the unique namespaced `receipt_approved_payload_sha256`, whole-second canonical UTC `receipt_approved_at_utc`, the no-newline hash of the payload's exact `authorization_scope` value as `receipt_authorization_scope_sha256`, `receipt_operational_drift_requires_new_owner_approval: false`, and `receipt_immutable_drift_requires_new_owner_approval: true`. Never repeat the envelope key `approval_payload_sha256` inside the receipt and never use unnamespaced actor/signal fields.

    Hash only the exact LF-terminated receipt lines between the receipt markers. Store the result as the sole `approval_receipt_sha256` record between the exact receipt-digest markers after the receipt end marker, so the digest record cannot hash itself. Reopen the file and independently rerun strict parsers for the envelope, payload, receipt, and digest regions; require exact ordered key sets and marker/key occurrence counts, payload and receipt hash equality, exact signal equality, raw-signal hash equality, payload-reference equality, authorization-scope hash equality, and the two drift booleans. State that the response approves only immutable payload identities and that Plan 05.1-03 must independently repeat these validations plus run a fresh aggregate operational safe-state gate immediately before mutation. The approved file becomes immutable input to Plan 05.1-03; later edits or appended permissions invalidate it.

    If the owner chose the exact defer signal, do not execute the approval-sealing path and do not create an `OWNER_APPROVED` receipt; leave production unchanged, record the deferral truthfully only if the executor's checkpoint protocol requires it, and stop before Plan 05.1-03. Under either response, perform no schema push, worker call, queue mutation, history deletion, maintenance, deployment, Git remote operation, or stash mutation.
  </action>
  <acceptance_criteria>
    - The sealed receipt contains exactly the 13 ordered namespaced version-2 fields, including the unique `receipt_approved_payload_sha256` reference, exact actor/signal plus raw-signal hash, UTC receipt time, checkpoint identity, authorization-scope hash, and drift policy.
    - Payload and receipt hashes recompute from their canonical byte regions after reopening the file; the sole `approval_receipt_sha256` record is outside the receipt region in its exact digest-marker region.
    - The exact approved signal names the current payload digest; generic, stale, altered, deferred, or cross-plan responses cannot yield `OWNER_APPROVED`.
    - Source/tree/evidence/artifact hashes, expected hosted 0001-0061 policy, pre/post function-security identities, isolated sole-0063 command, rollback definition, exclusions, and all three stash commit/tree identities remain unchanged at sealing.
    - Operational observations remain outside payload equality; their normal drift does not require another owner signal, but an unsafe Plan 03 live gate still causes NO_DEPLOY and natural drain only.
    - The receipt explicitly authorizes only one exact 0063 push after both Plan 03 gates plus read-only checks and remains incapable of authorizing 0062, later ranking/UI/retention, manual workers, queue repair, scheduler mutation, history deletion, or storage maintenance.
    - Production and every stash remain unchanged; only the approval artifact is modified.
  </acceptance_criteria>
  <verify>
    <automated>
      set -euo pipefail
      containment_approval=.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md
      payload_begin='&lt;!-- containment-approval-v2-payload-begin --&gt;'
      payload_end='&lt;!-- containment-approval-v2-payload-end --&gt;'
      envelope_begin='&lt;!-- containment-approval-v2-envelope-begin --&gt;'
      envelope_end='&lt;!-- containment-approval-v2-envelope-end --&gt;'
      audit_begin='&lt;!-- containment-approval-v2-audit-begin --&gt;'
      audit_end='&lt;!-- containment-approval-v2-audit-end --&gt;'
      observations_begin='&lt;!-- containment-approval-v2-observations-begin --&gt;'
      observations_end='&lt;!-- containment-approval-v2-observations-end --&gt;'
      receipt_begin='&lt;!-- containment-owner-receipt-v2-begin --&gt;'
      receipt_end='&lt;!-- containment-owner-receipt-v2-end --&gt;'
      receipt_digest_begin='&lt;!-- containment-owner-receipt-v2-digest-begin --&gt;'
      receipt_digest_end='&lt;!-- containment-owner-receipt-v2-digest-end --&gt;'
      region_keys() {
        awk -v begin="$1" -v end="$2" '
          $0 == begin { begin_count++; if (begin_count != 1 || inside || end_count) exit 41; inside=1; next }
          $0 == end { end_count++; if (!inside || end_count != 1) exit 42; inside=0; next }
          inside {
            if ($0 ~ /^[[:space:]]*$/ || $0 ~ /^[[:space:]]*#/) exit 43
            if ($0 !~ /^[a-z][a-z0-9_]*: [^[:cntrl:]]+$/) exit 44
            key=$0; sub(/:.*/, "", key); value=substr($0, length(key) + 3)
            if (key ~ /_sha256$/ &amp;&amp; (length(value) != 64 || value !~ /^[0-9a-f]+$/)) exit 45
            if (key ~ /(_blob_oid|_commit|_tree)$/ &amp;&amp; (length(value) != 40 || value !~ /^[0-9a-f]+$/)) exit 46
            print key
          }
          END { if (begin_count != 1 || end_count != 1 || inside) exit 47 }
        ' "$3"
      }
      region_value() {
        awk -v begin="$1" -v end="$2" -v name="$3" '
          $0 == begin { inside=1; next } $0 == end { inside=0; next }
          inside { sep=index($0, ": "); key=substr($0, 1, sep - 1); if (key == name) { count++; value=substr($0, sep + 2) } }
          END { if (count != 1) exit 51; print value }
        ' "$4"
      }
      region_sha256() {
        awk -v begin="$1" -v end="$2" '$0 == begin { inside=1; next } $0 == end { inside=0; next } inside { print }' "$3" | shasum -a 256 | awk '{print $1}'
      }
      marker_count() { awk -v marker="$1" '$0 == marker { count++ } END { print count + 0 }' "$2"; }
      key_count() {
        awk -v name="$1" '{ line=$0; sub(/^[[:space:]]*/, "", line); sep=index(line, ": "); if (sep &gt; 0 &amp;&amp; substr(line, 1, sep - 1) == name) count++ } END { print count + 0 }' "$2"
      }
      test -s "$containment_approval"
      payload_keys="$(region_keys "$payload_begin" "$payload_end" "$containment_approval")"
      test "$(printf '%s\n' "$payload_keys" | awk 'END { print NR }')" -eq 87
      test "$(printf '%s\n' "$payload_keys" | shasum -a 256 | awk '{print $1}')" = 'e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d'
      test "$(region_value "$payload_begin" "$payload_end" payload_immutable_key_order_sha256 "$containment_approval")" = 'e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d'
      test "$(region_keys "$envelope_begin" "$envelope_end" "$containment_approval")" = "$(printf '%s\n' approval_contract_version approval_status approval_payload_sha256 required_owner_signal)"
      receipt_keys="$(region_keys "$receipt_begin" "$receipt_end" "$containment_approval")"
      test "$receipt_keys" = "$(printf '%s\n' receipt_contract_version receipt_serialization receipt_key_order_sha256 receipt_plan_id receipt_checkpoint_id receipt_approval_actor receipt_approval_signal receipt_approval_signal_sha256 receipt_approved_payload_sha256 receipt_approved_at_utc receipt_authorization_scope_sha256 receipt_operational_drift_requires_new_owner_approval receipt_immutable_drift_requires_new_owner_approval)"
      test "$(printf '%s\n' "$receipt_keys" | shasum -a 256 | awk '{print $1}')" = '4323074504d17e4ee1b001e2edd303174ab083a6362d13f6c018655b3374d46b'
      test "$(region_keys "$receipt_digest_begin" "$receipt_digest_end" "$containment_approval")" = 'approval_receipt_sha256'
      for marker in "$payload_begin" "$payload_end" "$envelope_begin" "$envelope_end" "$audit_begin" "$audit_end" "$observations_begin" "$observations_end" "$receipt_begin" "$receipt_end" "$receipt_digest_begin" "$receipt_digest_end"; do test "$(marker_count "$marker" "$containment_approval")" -eq 1; done
      test "$(key_count approval_payload_sha256 "$containment_approval")" -eq 1
      test "$(key_count required_owner_signal "$containment_approval")" -eq 1
      test "$(key_count receipt_approved_payload_sha256 "$containment_approval")" -eq 1
      test "$(key_count approval_receipt_sha256 "$containment_approval")" -eq 1
      test "$(key_count receipt_approval_signal "$containment_approval")" -eq 1
      test "$(key_count receipt_approval_actor "$containment_approval")" -eq 1
      test "$(key_count approval_signal "$containment_approval")" -eq 0
      test "$(key_count approval_actor "$containment_approval")" -eq 0
      test "$(region_value "$envelope_begin" "$envelope_end" approval_contract_version "$containment_approval")" = '2'
      test "$(region_value "$envelope_begin" "$envelope_end" approval_status "$containment_approval")" = 'OWNER_APPROVED'
      test "$(region_value "$receipt_begin" "$receipt_end" receipt_contract_version "$containment_approval")" = '2'
      test "$(region_value "$receipt_begin" "$receipt_end" receipt_serialization "$containment_approval")" = 'flat_key_value_lf_v2'
      test "$(region_value "$receipt_begin" "$receipt_end" receipt_key_order_sha256 "$containment_approval")" = '4323074504d17e4ee1b001e2edd303174ab083a6362d13f6c018655b3374d46b'
      test "$(region_value "$receipt_begin" "$receipt_end" receipt_plan_id "$containment_approval")" = '05.1-02'
      test "$(region_value "$receipt_begin" "$receipt_end" receipt_checkpoint_id "$containment_approval")" = '05.1-02/task-2/blocking-human'
      test "$(region_value "$receipt_begin" "$receipt_end" receipt_approval_actor "$containment_approval")" = 'jackshi812'
      containment_payload_expected="$(region_value "$envelope_begin" "$envelope_end" approval_payload_sha256 "$containment_approval")"
      containment_required_signal="$(region_value "$envelope_begin" "$envelope_end" required_owner_signal "$containment_approval")"
      containment_receipt_signal="$(region_value "$receipt_begin" "$receipt_end" receipt_approval_signal "$containment_approval")"
      test "$(region_sha256 "$payload_begin" "$payload_end" "$containment_approval")" = "$containment_payload_expected"
      test "$containment_payload_expected" != '0d997347ba6840ad6f7e1dbf89fcfe530d2c8e7c943dcce4c6f0ce08e5c0f1d1'
      test "$containment_payload_expected" != '050492bd188ead83d18ec83ee2121104d585a95a6b7752ea33bdfe3ce9dfa3bf'
      test "$containment_required_signal" = "APPROVE 05.1-02 CONTAINMENT $containment_payload_expected"
      test "$containment_receipt_signal" = "$containment_required_signal"
      test "$(printf '%s' "$containment_receipt_signal" | shasum -a 256 | awk '{print $1}')" = "$(region_value "$receipt_begin" "$receipt_end" receipt_approval_signal_sha256 "$containment_approval")"
      test "$(region_value "$receipt_begin" "$receipt_end" receipt_approved_payload_sha256 "$containment_approval")" = "$containment_payload_expected"
      authorization_scope="$(region_value "$payload_begin" "$payload_end" authorization_scope "$containment_approval")"
      test "$(printf '%s' "$authorization_scope" | shasum -a 256 | awk '{print $1}')" = "$(region_value "$receipt_begin" "$receipt_end" receipt_authorization_scope_sha256 "$containment_approval")"
      test "$(region_value "$receipt_begin" "$receipt_end" receipt_operational_drift_requires_new_owner_approval "$containment_approval")" = 'false'
      test "$(region_value "$receipt_begin" "$receipt_end" receipt_immutable_drift_requires_new_owner_approval "$containment_approval")" = 'true'
      receipt_time="$(region_value "$receipt_begin" "$receipt_end" receipt_approved_at_utc "$containment_approval")"
      printf '%s\n' "$receipt_time" | grep -Eq '^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$'
      containment_receipt_expected="$(region_value "$receipt_digest_begin" "$receipt_digest_end" approval_receipt_sha256 "$containment_approval")"
      test "$(region_sha256 "$receipt_begin" "$receipt_end" "$containment_approval")" = "$containment_receipt_expected"
      git stash list --format='%H' | grep -Fx '913be95897066e53e6e1be0bd6e9483ab52a8437'
      git stash list --format='%H' | grep -Fx '2e6738fcf39a8a764be9e3664ee4023b01f051c9'
      git stash list --format='%H' | grep -Fx 'db09c3a2ff51dfa6652b36b0b809c1f9654f0117'
      git diff --check -- "$containment_approval"
    </automated>
  </verify>
  <done>The fresh exact version-2 immutable owner approval receipt is sealed for Plan 05.1-03's separate immediate operational gate, and no deployment or other outward mutation has occurred.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

- `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md` contains the corrected immutable-only preapproval payload, separately labeled non-authorizing operational observations, stale-history audit ledger, and fresh exact owner receipt. It binds Plan 01 source/tree/migration/test/evidence hashes, target and expected migration policy, pre/post function-security identities, excluded 0062, isolated sole-0063 command, rollback definition, three protected stash commits/trees, exact allowed action, and every exclusion.
- `05.1-02-SUMMARY.md` records the receipt digest and checkpoint result only. It must not reproduce credentials or row-level data and must not claim deployment, queue settling, worker execution, 24-hour observation, history cleanup, maintenance, ranking redesign, dashboard work, or retention.
- No source, migration, test, function, web artifact, deployment artifact, queue artifact, cleanup artifact, retention artifact, or production data is created or modified by this plan.
</artifacts>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Status | Plan mapping |
|---|---|---|---|---|
| GOAL | — | Restore timely deterministic publication and responsive lifecycle actions | COVERED AS GATE | 05.1-02 supplies the exact authorization boundary for the first containment deployment; behavior delivery remains in the accepted 01-12 outline. |
| REQ | — | No roadmap requirement IDs; operational stabilization | N/A | `requirements: []` intentionally matches ROADMAP and the outline. |
| RESEARCH | — | No phase RESEARCH.md exists | N/A | The read-only diagnosis supplies the confirmed producer loop, hosted-through-0061 baseline, 0062 drift, and no-mutation constraints used here. |
| CONTEXT | D-01 | First release is containment only | COVERED | Approval binds only exact 0063 and excludes 0062 plus every redesign/UI/retention action. |
| CONTEXT | D-02 | Let claimed/building work finish or expire; no manual worker/cancel/repair | COVERED | Payload, checkpoint, prohibitions, rollback definition, and receipt all deny those actions. |
| CONTEXT | D-04 | No ranking-history deletion or storage maintenance | COVERED | Payload and receipt explicitly deny deletion and all maintenance. |
| CONTEXT | D-20 | Immutable owner payload plus immediate volatile operational gate | COVERED | Approval digest excludes volatile counts/state/log windows; Plan 03 must remeasure aggregate safe state immediately before push and permits natural drain only when unsafe. |
| CONTEXT | D-03, D-18, D-19 | Clean 24-hour observation and phantom-work acceptance | OUTLINE-SCOPED | Plan 05.1-04 owns the observation after Plan 03's exact approved deployment. |
| CONTEXT | D-05-D-07, D-16, D-19 | Deterministic proportional ranking and freshness | OUTLINE-SCOPED | Plan 05.1-05 owns post-gate implementation; this approval explicitly cannot authorize it. |
| CONTEXT | D-08-D-11, D-17 | Dismissal UI and critical-path behavior | OUTLINE-SCOPED | Plan 05.1-06; no dashboard authorization is present here. |
| CONTEXT | D-12-D-15 | Retention and separately approved deletion | OUTLINE-SCOPED | Plans 05.1-07 and 05.1-10 through 05.1-12; this receipt denies retention deletion/maintenance authority. |
| CONTEXT | Deferred ideas | Filter tuning, connector expansion, outreach work | EXCLUDED | Absent from the payload and from this plan's tasks. |

No plan-scoped source item is missing. Later decisions remain assigned to their accepted outline plans and are made explicit exclusions so the containment approval cannot absorb them.
</source_coverage_audit>

<threat_model>
## OWASP ASVS Level 1 — block on high

Every high or critical threat below is blocking. The owner checkpoint cannot be presented or sealed until its mitigation and automated gate pass.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 01 source/evidence -> approval payload | Repository bytes and prior evidence are untrusted until Git/tree/hash/test validation succeeds. |
| Local/isolated release view -> linked production Supabase | An unrelated pending migration can silently broaden a normal repo-root push. |
| Hosted read-only state -> owner decision | Immutable target/migration/function-security policy must be exact; operational observations must be bounded, sanitized, visibly non-authorizing, and excluded from payload equality. |
| Owner response -> immutable receipt | A human signal must bind one exact payload and cannot be replayed after drift. |
| Approval receipt -> Plan 05.1-03 | The next plan receives authority for one migration push only; every other action remains denied. |
| Git release preparation -> Phase 5 stashes/unrelated work | Release handling must preserve three owner stash objects and every unrelated worktree byte. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05.1-02-01 | Tampering / Repudiation | Plan 01 candidate identity | high | mitigate | Resolve recorded commit/tree, recompute migration/test/evidence hashes, rerun tests, require scoped cleanliness, and bind canonical payload bytes. |
| T-05.1-02-02 | Tampering | Pending migration release set | critical | mitigate | Require hosted 0001-0061, reconstruct content-hashed 0001-0061-plus-0063 isolated view, and accept only a sole-0063 linked dry run; record 0062 path/hash as excluded. |
| T-05.1-02-03 | Spoofing / Repudiation | Owner approval | critical | mitigate | Blocking-human checkpoint, version-2 immutable-only payload digest, fresh exact signal, actor/time/response hash, immutable receipt hash, and rejection of commits 30955dc/a22a88b plus all stale/generic/cross-plan responses. |
| T-05.1-02-04 | Elevation of Privilege | Approval scope | high | mitigate | Closed allowed-action inventory, explicit non-transitivity, one-push bound, and exhaustive exclusions for later ranking/UI/retention, functions/web, Git remote, queue, deletion, and maintenance. |
| T-05.1-02-05 | Denial of Service / Tampering | Worker and queue | high | mitigate | D-02/D-20 forbid manual calls, cancellation, retry, lease/state/config/scheduler edits, and repair; Plan 03 requires immediate zero-building/pending/claimed aggregate state and otherwise returns NO_DEPLOY for natural drain/recheck without invalidating immutable approval. |
| T-05.1-02-09 | Tampering / Denial of Service | Approval digest boundary | high | mitigate | Canonical payload contains immutable identities only; a mechanically checked separate region holds volatile counts, timestamps, catalog estimates, and fixed-window logs so ordinary drift cannot create an impossible equality gate. |
| T-05.1-02-06 | Tampering | Rollback path | high | mitigate | Define no-deploy and atomic-failure checks; post-commit drift stops and requires a separately tested/approved forward restoration rather than an improvised down/repair. |
| T-05.1-02-07 | Tampering / Denial of Service | Phase 5 stashes and unrelated work | high | mitigate | Pin three commit/tree identities, recheck at all boundaries, use no stash mutation, require only scoped cleanliness, and preserve unrelated path inventory. |
| T-05.1-02-08 | Information Disclosure | Linked credentials and hosted evidence | high | mitigate | Load credentials privately, never print/persist secrets or row IDs, use bounded aggregate queries, and store only safe target/version/hash/count/timestamp evidence. |
| T-05.1-02-SC | Tampering | Dependency supply chain | low | accept | No installs or updates; use repository-pinned CLI and existing lockfile/tooling, whose identity is payload-bound. |
</threat_model>

<verification>
1. Plan 01 evidence, source commit/tree, migration/test bytes, and focused tests revalidate independently before any hosted access or approval request.
2. Sanitized read-only checks prove the exact Supabase target, expected hosted 0001-0061 policy, pre/post-0063 function/security identities, and zero Plan 02 production mutation; volatile operational observations are outside approval-digest equality.
3. A content-hashed isolated view excludes 0062 and every later migration; its linked dry run proposes exact 0063 as the sole change.
4. The version-2 immutable payload hash recomputes before presentation; commits 30955dc/a22a88b and prior digests remain invalid/unsealed/never deployed; the owner returns a fresh exact digest-bearing signal.
5. The final owner receipt hash recomputes and binds actor, exact response, payload digest, timestamp, allowed one-push-after-live-gate action, rollback definition, and all exclusions.
6. Each protected stash commit/tree remains present, unrelated work remains untouched, and no production/queue/history/storage/function/web/Git-remote mutation occurs.
</verification>

<success_criteria>
- The fresh exact owner-approved version-2 receipt exists with valid payload and receipt SHA-256 values and exact blocking-human provenance; no stale signal is reusable.
- Approval is bound to tested 0063/source/evidence identities, safe target and expected hosted-through-0061 policy, pre/post function-security identities, isolated sole-0063 command, pinned CLI/argv, exclusions, stashes, authorization scope, and rollback terms.
- Volatile production counts, ranking state, timestamps, catalog estimates, and fixed-window logs are excluded from payload equality; Plan 05.1-03 must independently pass its immediate aggregate safe-state gate.
- Migration 0062 and every later ranking, worker, dashboard, retention, deletion, maintenance, queue, scheduler, function, web, and Git-remote action remain explicitly unauthorized.
- D-02 natural-settlement, D-04 zero-history/zero-maintenance, and D-20 immutable-versus-volatile gate rules are present in the payload, checkpoint, receipt, and Plan 05.1-03 handoff contract.
- All three Phase 5 stash commits/trees remain exact and present; unrelated owner work is untouched.
- All ASVS L1 high/critical mitigations and automated gates pass.
- Production remains unchanged; Plan 05.1-03 is the first plan permitted to perform the one exact approved schema push.
</success_criteria>

<output>
Create `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-SUMMARY.md` after execution. Record contract version 2, immutable payload/receipt digests, fresh exact owner checkpoint result, stale-history invalidation, source/target-policy/function-security/migration/stash identities, the volatile-observation exclusion, Plan 03 live-gate handoff, and zero-production-effect boundary. Do not reproduce secrets or claim deployment, queue intervention, 24-hour observation, cleanup, deletion, maintenance, ranking redesign, dashboard work, or retention.
</output>
