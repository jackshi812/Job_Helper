---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 14
type: execute
wave: 6
depends_on:
  - 05.1-08
files_modified:
  - supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql
  - supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql
  - web/tests/migration-0064-delta-ranking-and-batch-staging.test.ts
  - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-05 through D-07, D-19, and D-22: migration 0064 retains the already-proven bounded changed-job ranking, atomic batch staging, and complete-publication behavior; only its historically expensive ranking-item check is changed."
    - "The deterministic_ranking_items_job_input_revision_valid constraint is installed NOT VALID so PostgreSQL does not scan historical ranking items, while new or updated invalid rows are still rejected."
    - "The jobs_deterministic_input_revision_valid constraint remains immediately validated because its original statement completed within the 60-second timeout before the transaction rolled back and there is no evidence requiring a broader change."
    - "D-01, D-02, D-20, and D-23: migration 0063 and every containment/provenance artifact remain immutable; no 0065, backfill, validation, retention, deletion, cleanup, maintenance, paid AI, manual queue/worker invocation, retry, or rollback is introduced."
    - "The failed Plan 09 attempt remains immutable history with one consumed schema process and zero schema effects; fd14f63 and the failed deployment artifact are bound by exact commit/blob/SHA-256 fields, never by requiring the new candidate to descend from that failed-attempt branch."
    - "The release candidate is a distinct one-parent commit whose sole parent is the freshly revalidated approval-bound origin/main pre-state and whose tree exactly equals Task 1's repaired release tree, so Plan 09 can perform the required non-force fast-forward."
    - "D-21: a new source candidate and exact owner receipt authorize at most one new forward-repair deployment attempt plus the same bounded score-tick and Git-triggered Cloudflare effects after schema success; Plan 14 performs no production action."
  artifacts:
    - path: "supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql"
      provides: "One targeted NOT VALID repair on deterministic_ranking_items_job_input_revision_valid"
      contains: "deterministic_ranking_items_job_input_revision_valid"
    - path: "supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql"
      provides: "Disposable PostgreSQL proof of convalidated=false plus new-write enforcement"
      contains: "convalidated"
    - path: "web/tests/migration-0064-delta-ranking-and-batch-staging.test.ts"
      provides: "Static exact-source proof for the targeted constraint repair"
      contains: "not valid"
    - path: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md"
      provides: "New candidate-bound forward-repair payload and owner receipt"
  key_links:
    - "0064 constraint source -> static Vitest assertion -> disposable pgTAP catalog/enforcement proof"
    - "Task 1 repair-evidence commit/tree -> distinct candidate with sole parent equal to approval-bound origin/main -> exact tree/ancestor/fast-forward gates -> new canonical payload digest"
    - "fd14f63 commit + deployment-artifact blob/SHA-256 -> immutable failed-attempt evidence fields independent of candidate ancestry"
    - "Exact current owner response -> Plan 14 receipt digest -> revised Plan 09 one-new-attempt authority"
---

<objective>
Remove the one proven migration-time scan blocker, prove the corrected 0064 locally, and obtain a new non-reusable owner authorization for a single forward-repair release attempt (D-05–D-07, D-19, D-21–D-23).

Purpose: Ship the already-built dashboard and delta-ranking fixes without reopening their design or weakening production mutation containment.
Output: One targeted 0064 source change, focused static and real-PostgreSQL proof, a new release source candidate, and a fresh digest-bound approval receipt. No production schema push or hosted mutation occurs in this plan.
</objective>

## Artifacts this phase produces

- Corrected migration 0064 bytes with only `deterministic_ranking_items_job_input_revision_valid` declared `NOT VALID`.
- Static and pgTAP proofs that the constraint begins with `convalidated = false` and still rejects new invalid writes.
- A new local release source candidate plus `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md`, containing a new canonical payload and current owner receipt.
- No deployment record, production change, backfill, later validation, cleanup, or monitoring artifact.

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-05-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-08-FRESH-RELEASE-APPROVAL.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md
@supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
@supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql
@supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql
@web/tests/migration-0064-delta-ranking-and-batch-staging.test.ts
@scripts/verify-phase-05-1-delta-local.mjs

<interfaces>
- Failed-attempt immutable anchor: commit `fd14f638ba8ffec32cbe91be024798116f43878a`, Git blob `0ffcfa53b9de0c79b9875252c35902def808a86d`, whole-file SHA-256 `021379bc5d44a498ee55efd682ac24525d2b8af1455439cbcd55f8e495210a06` for `05.1-09-RELEASE-DEPLOYMENT.md`.
- Original migration 0064 SHA-256: `0b1ccb22f348b0af94cc9aea0607941e955f847d9ab1162bea3dbabc67df2005`.
- Original failed statement: statement 4 added `deterministic_ranking_items.job_input_revision` and its check in one `ALTER TABLE`; SQLSTATE `57014` canceled the historical validation scan under the migration-local 60-second statement timeout.
- Confirmed hosted after-state: migration history ends at 0063; every 0064 column, constraint, table, RLS/grant, trigger, index, and new/replaced function effect is absent; score-tick v16, scheduler/secret metadata, origin/main, and Cloudflare remain unchanged.
- Local verifier contract: exact cached images, repository Supabase CLI 2.109.1, exact 0001–0061 + 0063 + 0064 manifest, 0062 excluded, two pgTAP invocations per verifier run, no linked/hosted access.
- PostgreSQL contract: a `NOT VALID` check is not applied to pre-existing rows during `ADD CONSTRAINT`, is recorded with `pg_constraint.convalidated = false`, and is enforced for later inserts and updates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement the targeted NOT VALID repair</name>
  <files>supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql, supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql, web/tests/migration-0064-delta-ranking-and-batch-staging.test.ts</files>
  <read_first>
    - `05.1-09-RELEASE-DEPLOYMENT.md`: SQLSTATE 57014 statement ordinal, exact zero-effect after-state, consumed attempt counter, and forward-repair-only disposition.
    - `supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql`: the two initial `ALTER TABLE` statements and every already-proven delta-ranking object that must remain unchanged.
    - `supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql`: existing real-PostgreSQL fixture lifecycle and assertion style.
    - `web/tests/migration-0064-delta-ranking-and-batch-staging.test.ts`: current static transaction, queue, trigger, batching, publication, and ACL assertions.
  </read_first>
  <behavior>
    - Static source proof fails until exactly `deterministic_ranking_items_job_input_revision_valid` is declared `NOT VALID`.
    - Static source proof confirms `jobs_deterministic_input_revision_valid` is not broadened to the deferred-validation form.
    - Disposable PostgreSQL reports the repaired ranking-item constraint with `convalidated = false` immediately after migration installation.
    - Disposable PostgreSQL reports the jobs revision constraint as validated.
    - A new or updated row with a negative `job_input_revision` is rejected by `deterministic_ranking_items_job_input_revision_valid` even though historical validation is deferred.
    - Every existing queue, batch, retry, tombstone, atomic publication, RLS, grant, trigger, index, and function assertion still passes.
  </behavior>
  <action>
    Write the static and pgTAP assertions first and run the focused static test to prove RED. Then change only the `deterministic_ranking_items_job_input_revision_valid` declaration in migration 0064 by adding PostgreSQL's deferred historical-validation modifier after its existing predicate. Do not change the predicate, column definition, transaction, lock timeout, statement timeout, `jobs_deterministic_input_revision_valid`, or any later migration statement.

    In pgTAP, inspect `pg_catalog.pg_constraint` by exact constraint/table identity and assert the ranking-item constraint is present with `convalidated = false`; separately assert the jobs constraint remains present and validated. Add a transaction-safe invalid-write assertion against a newly created or existing synthetic ranking item and require the named check violation. Keep the fixture rollback-safe and preserve the verifier's repeatability. In Vitest, parse the two initial constraint declarations narrowly enough to prove the targeted modifier appears once and is not applied to the jobs constraint.

    After all Task 1 tests pass, finish the task with the executor's atomic planning-branch commit containing only these three listed repair/proof files. Task 2 must treat that commit as `repair_evidence_commit` and its tree as `repaired_release_tree`; it must not construct or approve the release candidate before this commit exists. Preserve the unrelated `.DS_Store` modification and `.planning/HANDOFF.json` deletion byte-for-byte and porcelain-for-porcelain outside the commit.

    Do not add `VALIDATE CONSTRAINT`, a backfill, a data scan, a later migration, retention, deletion, cleanup, vacuum/analyze, maintenance, timeout inflation, or changes to migration 0063. Preserve all D-05–D-07/D-19 ranking semantics and D-01/D-02/D-20 containment boundaries.
  </action>
  <acceptance_criteria>
    - The production migration diff is one targeted constraint-modifier change; tests are the only other source changes.
    - The ranking-item constraint is initially unvalidated but enforces its predicate on new/updated rows.
    - The jobs constraint remains immediately validated and every unrelated 0064 statement is byte-identical.
    - Migration 0063, failed-attempt evidence, and all containment/provenance artifacts are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix web test -- migration-0064-delta-ranking-and-batch-staging.test.ts &amp;&amp; git diff --check -- supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql web/tests/migration-0064-delta-ranking-and-batch-staging.test.ts</automated>
  </verify>
  <done>The sole historical-scan blocker has the targeted deferred-validation form, both catalog state and new-write enforcement are specified, no broader migration behavior changed, and the three repaired evidence files are sealed in one planning-branch commit before candidate construction.</done>
</task>

<task type="auto">
  <name>Task 2: Replay local proof twice and prepare the new source-bound payload</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md</files>
  <read_first>
    - Task 1's committed migration and test bytes: the only release-source delta from the failed candidate.
    - `scripts/verify-phase-05-1-delta-local.mjs`: exact cached-image, manifest, double-pgTAP, no-network, and teardown contract; do not modify it.
    - `05.1-08-FRESH-RELEASE-APPROVAL.md`: prior payload/receipt format and exact downstream function/Git/Cloudflare scope; treat all old authority as non-reusable.
    - `05.1-09-RELEASE-DEPLOYMENT.md` at `fd14f63`: immutable consumed-attempt and zero-effect history.
  </read_first>
  <action>
    Invoke `SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-delta-local.mjs` twice as two independent verifier runs. Require each run's internal first and rollback-replay pgTAP executions to pass, exact cached image and CLI identities to match, 0062 to remain excluded, teardown to pass, and hosted/linked reads and writes to remain zero. Then run the focused migration/worker/Dashboard suites and `npm --prefix web run build`. Record exact commands, sanitized output digests, assertion counts, tool identities, and PASS results. Do not install, pull, use credentials, call a hosted service, or mutate production.

    Treat Task 1's atomic planning-branch commit only as `repair_evidence_commit`, and bind its exact tree as `repaired_release_tree`. Freshly revalidate the normalized origin and current `origin/main`; require that remote-tracking pre-state still equals the Plan 08 approval-bound and failed-attempt after-state commit `7ca62938c2e46d0603656e156bc1bd9ee3dcc758`, then bind it as `origin_main_pre_state`. Do not use the Task 1 commit itself as the release candidate.

    Without checking out a different tree, moving a branch, rebasing, merging, cherry-picking, or touching the index/worktree, construct a distinct commit object from `repaired_release_tree` with exactly one explicit parent, `origin_main_pre_state`, using the established Plan 08 one-parent candidate pattern (`git commit-tree &lt;repaired_release_tree&gt; -p &lt;origin_main_pre_state&gt;`). Bind that object as `release_source_candidate`. Before the payload can reach `AWAITING_OWNER_APPROVAL`, require all of these automated gates: the candidate object exists; `git merge-base --is-ancestor &lt;origin_main_pre_state&gt; &lt;release_source_candidate&gt;` succeeds; the candidate has exactly one parent and it equals `origin_main_pre_state`; the candidate tree equals both `repaired_release_tree` and the Task 1 commit tree; and the fast-forward distance from the pre-state is exactly one. The candidate must not use `fd14f63` as a parent, and no approval condition may require candidate ancestry from the failed-attempt branch.

    Preserve the unrelated `.DS_Store` modification plus `.planning/HANDOFF.json` deletion byte/porcelain-exact: neither working-tree delta may enter the Task 1 commit or the candidate-tree delta, so the candidate retains the Task 1 committed tree rather than the unrelated worktree state. Bind the exact fields `repair_evidence_commit`, `repaired_release_tree`, `origin_main_pre_state`, `release_source_candidate`, `release_source_candidate_tree`, `release_source_candidate_first_parent`, `release_source_candidate_parent_count`, `release_source_candidate_origin_main_ancestor`, `release_source_candidate_first_parent_equals_origin_main_pre_state`, `release_source_candidate_tree_equals_repaired_release_tree`, and `release_source_candidate_fast_forward_distance`. Also bind candidate-independent failed-attempt fields `failed_attempt_commit: fd14f638ba8ffec32cbe91be024798116f43878a`, `failed_attempt_deployment_artifact_blob_sha1: 0ffcfa53b9de0c79b9875252c35902def808a86d`, `failed_attempt_deployment_artifact_sha256: 021379bc5d44a498ee55efd682ac24525d2b8af1455439cbcd55f8e495210a06`, and `failed_attempt_candidate_ancestry_required: false`. Bind normalized origin, the repaired 0064 blob/SHA-256, the unchanged 0063 hash, unchanged recursive score-tick closure, unchanged tracked web build-input closure, the three proof files, the two independent verifier receipts, focused suite/build receipts, and the exact one-line semantic delta from the failed 0064 bytes.

    Create `05.1-14-FORWARD-REPAIR-APPROVAL.md` with a canonical LF-delimited payload and `approval_status: AWAITING_OWNER_APPROVAL`. Bind the candidate construction/gate fields above and the immutable failed-attempt commit/blob/SHA-256 as separate evidence fields, historical counters (`schema_push_process_starts: 1`, `schema_push_effects: 0`, all downstream attempts/effects zero), and the rule that Plan 08's payload/receipt/launch signal authorize nothing after the migration byte change and consumed attempt. Bind exact Supabase project, expected remote-through-0063 zero-0064 pre-state for Plan 09 to revalidate, exact process-only credential policy, new isolated 0064-only push command, and one `NEW_FORWARD_REPAIR` schema process limit.

    The payload may authorize only: one new forward-repair application of corrected 0064; after schema PASS, one unchanged `score-tick` deployment with `--no-verify-jwt` and the existing handler-secret boundary; after function PASS, one non-force fast-forward of the new candidate to origin/main so the established Cloudflare Git integration may create one bounded production deployment. Preserve the existing 900-second/15-second causality and provider-asset parity contract. Explicitly authorize zero retries and classify the new schema process as separately approved forward repair, not replay of the consumed attempt. Exclude 0062/0065/every other migration, constraint validation, backfill, retention, deletion, cleanup, history mutation/reconstruction, maintenance, vacuum/analyze, paid AI, scheduler/secret/JWT changes, manual function/queue/worker calls, retry, rollback, force push, manual Cloudflare deployment, and Plan 10 monitoring.

    Set production/hosted/Git mutation counters for Plan 14 to zero, compute a new payload SHA-256 over the exact payload bytes, and print only `APPROVE 05.1-14 FORWARD REPAIR &lt;approval_payload_sha256&gt;` as the accepting response. Do not reuse any prior owner-response bytes or digest.
  </action>
  <acceptance_criteria>
    - The local verifier passes twice independently; the focused web suite and production build pass.
    - Task 1's planning-branch commit is evidence only; a distinct one-parent release candidate is rooted directly at the freshly revalidated approval-bound origin/main pre-state and has the exact repaired release tree.
    - The ancestor, sole-parent, exact-tree, and one-commit fast-forward gates pass before the payload can be presented for approval.
    - Candidate authority changes only where required by the repaired 0064 and its proof, while score-tick and web behavior remain the already-approved closures.
    - Payload preserves the first failed attempt as immutable one-process/zero-effect history through exact commit/blob/SHA-256 fields, without candidate-ancestry coupling, and grants at most one separately approved forward-repair process.
    - Plan 08 authority is explicitly non-reusable; payload digest and accepting signal are new.
    - Plan 14 records zero hosted reads, hosted writes, schema processes, function deploys, Git pushes, Cloudflare writes, manual calls, retries, and rollbacks.
  </acceptance_criteria>
  <verify>
    <automated>SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-delta-local.mjs &amp;&amp; SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-delta-local.mjs &amp;&amp; npm --prefix web test -- migration-0064-delta-ranking-and-batch-staging.test.ts deterministic-ranking-worker-runtime.test.ts deterministic-ranking-source.test.ts Dashboard.test.tsx feed.test.ts resume-routing-source.test.ts &amp;&amp; npm --prefix web run build &amp;&amp; APPROVAL=.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md &amp;&amp; test -s "$APPROVAL" &amp;&amp; REPAIR_COMMIT=$(awk -F': ' '$1=="repair_evidence_commit"{print $2}' "$APPROVAL") &amp;&amp; REPAIRED_TREE=$(awk -F': ' '$1=="repaired_release_tree"{print $2}' "$APPROVAL") &amp;&amp; ORIGIN_PRE=$(awk -F': ' '$1=="origin_main_pre_state"{print $2}' "$APPROVAL") &amp;&amp; CANDIDATE=$(awk -F': ' '$1=="release_source_candidate"{print $2}' "$APPROVAL") &amp;&amp; CANDIDATE_TREE=$(awk -F': ' '$1=="release_source_candidate_tree"{print $2}' "$APPROVAL") &amp;&amp; CANDIDATE_PARENT=$(awk -F': ' '$1=="release_source_candidate_first_parent"{print $2}' "$APPROVAL") &amp;&amp; CANDIDATE_PARENT_COUNT=$(awk -F': ' '$1=="release_source_candidate_parent_count"{print $2}' "$APPROVAL") &amp;&amp; CANDIDATE_DISTANCE=$(awk -F': ' '$1=="release_source_candidate_fast_forward_distance"{print $2}' "$APPROVAL") &amp;&amp; test -n "$REPAIR_COMMIT" &amp;&amp; test -n "$REPAIRED_TREE" &amp;&amp; test -n "$ORIGIN_PRE" &amp;&amp; test -n "$CANDIDATE" &amp;&amp; test "$CANDIDATE" != "$REPAIR_COMMIT" &amp;&amp; git cat-file -e "$CANDIDATE^{commit}" &amp;&amp; git merge-base --is-ancestor "$ORIGIN_PRE" "$CANDIDATE" &amp;&amp; test "$CANDIDATE_PARENT" = "$ORIGIN_PRE" &amp;&amp; test "$CANDIDATE_PARENT_COUNT" = 1 &amp;&amp; test "$CANDIDATE_DISTANCE" = 1 &amp;&amp; test "$(git show -s --format=%P "$CANDIDATE")" = "$ORIGIN_PRE" &amp;&amp; test "$(git show -s --format=%T "$REPAIR_COMMIT")" = "$REPAIRED_TREE" &amp;&amp; test "$CANDIDATE_TREE" = "$REPAIRED_TREE" &amp;&amp; test "$(git show -s --format=%T "$CANDIDATE")" = "$REPAIRED_TREE" &amp;&amp; test "$(git rev-list --count "$ORIGIN_PRE..$CANDIDATE")" = 1 &amp;&amp; rg -F 'release_source_candidate_origin_main_ancestor: true' "$APPROVAL" &amp;&amp; rg -F 'release_source_candidate_first_parent_equals_origin_main_pre_state: true' "$APPROVAL" &amp;&amp; rg -F 'release_source_candidate_tree_equals_repaired_release_tree: true' "$APPROVAL" &amp;&amp; rg -F 'failed_attempt_commit: fd14f638ba8ffec32cbe91be024798116f43878a' "$APPROVAL" &amp;&amp; rg -F 'failed_attempt_candidate_ancestry_required: false' "$APPROVAL" &amp;&amp; rg -F 'approval_status: AWAITING_OWNER_APPROVAL' "$APPROVAL" &amp;&amp; rg -F 'prior_plan_08_authority_reusable: false' "$APPROVAL" &amp;&amp; rg -F 'new_forward_repair_schema_process_limit: 1' "$APPROVAL" &amp;&amp; git diff --check -- "$APPROVAL"</automated>
  </verify>
  <done>The repair evidence is committed locally, a distinct origin/main-rooted exact-tree candidate passes all fast-forward gates, and the corrected release has two independent local database replays, focused web/build proof, and a new non-authorizing payload ready for the owner.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Authorize and seal exactly one new forward-repair release attempt</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md</files>
  <read_first>
    - `05.1-14-FORWARD-REPAIR-APPROVAL.md`: unchanged candidate-bound payload, local proof receipts, failed-attempt lineage, allowed effects, exclusions, and new digest.
    - The raw current checkpoint response: exact actor-provided bytes only; prior Plan 08 responses are not inputs.
  </read_first>
  <what-built>A corrected 0064 candidate whose ranking-item check avoids the historical validation scan, with two independent local verifier passes, focused web/build proof, immutable first-attempt evidence, and a new zero-production-effect payload.</what-built>
  <action>
    Pause before every production-capable action. Present the complete unchanged Plan 14 payload to the repository owner. Accept only the exact current response `APPROVE 05.1-14 FORWARD REPAIR &lt;approval_payload_sha256&gt;`; silence, generic approval, altered bytes, the Plan 08 signal, or any prior receipt is rejection.

    After the exact current signal is returned, recompute the unchanged payload digest and repeat the candidate object, origin/main ancestor, sole-parent, exact-tree, and one-commit fast-forward gates before sealing. Append a canonical receipt region to the same artifact with actor `jackshi812`, repository-owner role, checkpoint identity, UTC, exact raw-response SHA-256, payload SHA-256, authorization-scope SHA-256, the exact repair/candidate/origin fields, the separate failed-attempt commit/blob/SHA-256 fields, the historical consumed-attempt counters, one new forward-repair process limit, downstream effects conditioned on schema PASS, all zero forbidden-effect limits, `prior_plan_08_authority_reusable: false`, `production_mutations: 0`, and `approval_status: APPROVED`. Compute the receipt digest from the exact LF-terminated receipt bytes. If the response is deferred or mismatched, record `NOT_APPROVED` and stop. Do not load a production credential, execute `supabase db push`, deploy/invoke a function, push Git, call Cloudflare, or touch Plan 09 counters in this task.
  </action>
  <how-to-verify>
    1. Confirm the migration delta is limited to the named ranking-item constraint's deferred historical validation and does not change the jobs constraint, timeout, or any other 0064 behavior.
    2. Confirm both independent local verifier runs, the focused web suite, and the build passed with zero hosted access.
    3. Confirm the release candidate is distinct from the planning-branch repair commit, has the freshly revalidated origin/main pre-state as its sole parent, has exactly the repaired release tree, and passes the non-force fast-forward ancestor gate.
    4. Confirm the failed first schema process remains recorded separately by exact fd14f63 commit and deployment-artifact digests as one consumed attempt with zero effects, and that neither candidate ancestry nor Plan 08 authority is reused.
    5. Confirm the new receipt would authorize exactly one new forward-repair schema process, then the unchanged score-tick/Git-triggered Cloudflare effects only after preceding success, with every excluded operation still zero.
    6. Return the exact new digest-bearing signal or `defer`.
  </how-to-verify>
  <resume-signal>Paste `APPROVE 05.1-14 FORWARD REPAIR &lt;approval_payload_sha256&gt;` exactly, or say `defer`.</resume-signal>
  <acceptance_criteria>
    - Owner response is current-checkpoint-only, actor-bound, payload-bound, scope-bound, and non-reusable after any byte or target drift.
    - The sealed receipt authorizes one new forward-repair process, not a retry or erasure of the failed process.
    - Score-tick and Git-triggered Cloudflare effects remain conditional on successful corrected schema application.
    - Plan 14 ends with zero production, provider, and Git-remote mutations.
  </acceptance_criteria>
  <verify>
    <automated>rg -F 'approval_status: APPROVED' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md &amp;&amp; rg -F 'prior_plan_08_authority_reusable: false' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md &amp;&amp; rg -F 'new_forward_repair_schema_process_limit: 1' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md &amp;&amp; rg -F 'production_mutations: 0' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md &amp;&amp; git diff --check -- .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-FORWARD-REPAIR-APPROVAL.md</automated>
    <human-check>The repository owner reviews the exact candidate, prior failed-attempt counters, one-new-attempt scope, downstream conditions, and exclusions before returning the new digest-bearing signal.</human-check>
  </verify>
  <done>A new exact Plan 14 receipt is sealed for revised Plan 09, or deployment remains unauthorized; Plan 14 itself performs no production action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Failed Plan 09 evidence -> repair authority | Immutable one-attempt/zero-effect history must not be rewritten or reset. |
| Corrected local source -> owner payload | One targeted migration byte change and its proof must be bound without importing unrelated worktree state; the release candidate must be rebuilt as a direct child of the approval-bound origin/main pre-state. |
| Owner response -> revised Plan 09 | Replayed Plan 08 authority or generic assent must never authorize changed bytes or another process. |
| Local proof -> production boundary | Plan 14 must remain credential-free and local-only; all hosted mutation remains behind the owner checkpoint. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05.1-14-01 | Spoofing | owner authority | critical | mitigate | New payload and receipt digests, exact current repository-owner signal, explicit Plan 08 non-reuse, and fail-closed actor/scope validation. |
| T-05.1-14-02 | Tampering | failed-attempt history and release lineage | high | mitigate | Bind commit `fd14f63` and exact deployment-artifact blob/SHA-256 separately from candidate lineage; construct a distinct exact-tree candidate whose sole parent is the approval-bound origin/main pre-state and verify ancestor/parent/tree/distance mechanically. |
| T-05.1-14-03 | Elevation of Privilege | release scope | critical | mitigate | One new forward-repair schema process only; only the exact non-force-fast-forward candidate may be pushed after preceding PASS; exact exclusions and zero retry/rollback/manual-call authority. |
| T-05.1-14-04 | Denial of Service | 0064 historical constraint scan | high | mitigate | Targeted `NOT VALID` declaration plus disposable catalog/new-write proof; no backfill or validation scan in this phase. |
| T-05.1-14-05 | Information Disclosure | local proof and approval artifact | medium | mitigate | No credentials or hosted calls; persist sanitized hashes/counts and public repository identities only. |
| T-05.1-14-SC | Tampering | tool/dependency supply chain | low | accept | No install/update; reuse the exact repository CLI and cached image identities already proven by Plan 05. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Restore bounded timely ranking and responsive Dashboard release while preserving 0063 | 05, 06, 14, 09, 10 | COVERED | Plan 14 closes the only failed schema blocker; Plan 09 deploys; Plan 10 observes.
REQ | — | No Phase 05.1 roadmap requirement IDs | — | COVERED | Operational insertion intentionally has no OUTR requirement mapping.
RESEARCH | — | Preserve immutable evidence and use forward-only recovery | 13, 14, 09 | COVERED | D-21 supersedes historical reconstruction; failed attempt remains immutable and the repair is additive.
RESEARCH | — | No package installation or new dependency | 14 | COVERED | Existing Node/Vitest/Supabase tooling only.
CONTEXT | D-01–D-04, D-20 | Preserve 0063 containment, natural drain, no history/storage maintenance | 01–03, 14 | COVERED | Completed containment is untouched.
CONTEXT | D-05–D-07, D-19, D-22 | Deterministic delta ranking, bounded work, atomic publication, no paid AI | 05, 14 | COVERED | Existing implementation is unchanged except the targeted constraint installation mode.
CONTEXT | D-08–D-11, D-17 | Immediate row-scoped Dismiss with exact rollback and detached refill | 06, 09, 10 | COVERED | No Plan 14 UI change.
CONTEXT | D-12–D-15, D-23 | Retention/deletion/cleanup/vacuum and approval machinery deferred | — | COVERED | Explicit exclusion, not an active-phase gap.
CONTEXT | D-16, D-18, D-21 | Freshness and 24-hour health are post-release; exact current release checkpoint required | 14, 09, 10 | COVERED | Plan 14 supplies the new release receipt; Plan 10 remains postrelease only.
</source_audit>

<verification>
Plan 14 passes only when the static test identifies the one targeted source modifier, two independent invocations of the existing disposable verifier pass, pgTAP proves `convalidated = false` plus invalid-new-write rejection, the focused web suite/build pass, the old failed-attempt artifact remains exact, and a new owner receipt is sealed with all Plan 14 production counters at zero.
</verification>

<success_criteria>
- The corrected migration avoids the proven historical validation scan without changing data, increasing timeouts, or weakening enforcement for new/updated rows.
- All existing 0064 semantics and exact local proof pass twice; 0063 and historical evidence remain unchanged.
- A new source candidate with the approval-bound origin/main pre-state as its sole parent and Task 1's repaired tree as its exact tree, plus a Plan 14 receipt, replace rather than reuse Plan 08 authority.
- Revised Plan 09 alone may perform one new forward-repair deployment process; Plan 14 performs none.
</success_criteria>

<output>
Create `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-14-SUMMARY.md` when done. Record repaired migration old/new blobs and SHA-256 values, exact one-line semantic delta, static/pgTAP proof, both verifier-run receipts, focused web/build results, `repair_evidence_commit`, `repaired_release_tree`, `origin_main_pre_state`, release candidate commit/tree/sole-parent plus ancestor/tree/distance PASS gates, payload/receipt digests, the separate immutable `fd14f63` failed-attempt commit/deployment-artifact blob/SHA-256/counters, Plan 08 non-reuse, `hosted_reads: 0`, `production_mutations: 0`, and every exclusion. Do not claim deployment or Plan 10 acceptance.
</output>
