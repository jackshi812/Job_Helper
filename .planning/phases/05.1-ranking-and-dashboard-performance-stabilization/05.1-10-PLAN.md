---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 10
type: execute
wave: 8
depends_on:
  - 05.1-09
files_modified:
  - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json
  - scripts/verify-phase-05-1-postdeploy-acceptance.mjs
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-18 and D-21: the 24-hour window starts after the exact Plan 09 release and is post-release acceptance only, never an implementation, approval, or deployment prerequisite."
    - "D-16: ranking publication-to-dashboard P95 is at most 15 minutes and database-insert-to-visible P95 is at most 2 minutes over the source-bound window."
    - "D-17: observed Dismiss removal is under 100 ms, unrelated cards stay usable, and dismiss RPC P95 is at most 1.5 seconds."
    - "D-08 through D-11: deployed success remains removed despite refill failure; a failed RPC restores only its card with visible error; pending state is per-card."
    - "D-19: score-tick success is at least 99%, HTTP 546 count is zero, phantom `new_job` runs are zero, and routine work matches genuine inserted/changed jobs × affected owners without minute-cadence catalog growth."
  artifacts:
    - ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json contains source-bound aggregate metrics, live dismissal observations, exclusions, and a canonical digest."
    - "scripts/verify-phase-05-1-postdeploy-acceptance.mjs independently recomputes release binding, observation completeness, proportional work, complete publication, dismissal RPC P95, and the canonical evidence digest."
  key_links:
    - "Plan 09 release identities → 24-hour observation start/end → aggregate score-tick/delta/freshness metrics"
    - "Approved production web asset → owner-intended Dismiss action → DOM/network/refill observations"
    - "Plan 06 automated rollback receipt → deployed asset identity → failure-branch acceptance without destructive synthetic production data"
    - "Plan 10 evidence + exact Plan 09 deployment file → local validator → recomputed PASS/INSUFFICIENT_EVIDENCE verdict"
---

<objective>
Observe the exact deployed release for 24 hours and validate score-tick, phantom-work, freshness, and dismissal outcomes without becoming an implementation prerequisite.

Purpose: Confirm the two shipped fixes under current production behavior, using bounded aggregate/read-only monitoring plus one owner-intended normal product action.
Output: One sanitized source-bound postdeploy acceptance artifact; no retention, cleanup, history reconstruction, repair, or deployment.
</objective>

## Artifacts this phase produces

- `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json` with exact successful Plan 09 release binding, 24-hour aggregate metrics, dismissal observations, verdict, and evidence digest.
- `scripts/verify-phase-05-1-postdeploy-acceptance.mjs` with offline self-tests and independent acceptance recomputation.
- No deployment, retention, cleanup, history reconstruction, queue/worker invocation, or repair artifact.

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-06-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect a source-bound 24-hour postrelease observation</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json</files>
  <read_first>
    - `05.1-09-RELEASE-DEPLOYMENT.md` and `05.1-09-SUMMARY.md`: exact deployment completion UTC, 0064/function/web identities, and evidence digest.
    - `05.1-CONTEXT.md`: D-05–D-07, D-16–D-19, D-21, and D-23 thresholds/scope.
    - `05.1-04-CONTAINMENT-OBSERVATION.json`: schema shape ideas only; its historical window and provenance are not reused.
  </read_first>
  <action>
    Revalidate exact Plan 09 schema/function/web identities and set `window_start` no earlier than the successful complete release. Arm a new contiguous window of at least 86,400 seconds. Use recurring bounded read-only checks and aggregate provider/database observations only; never invoke score-tick, a queue/RPC repair, or a synthetic job mutation. Keep credentials process-only and persist no owner/job/run/item IDs, row payloads, captured preferences, raw logs, or secret-bearing endpoints.

    Record per-minute tick counts/status/latency, HTTP 546 count, changed-job queue depth/age, `new_job` run/item counts, database-insert/change-to-visible latency, publication-to-dashboard latency, active/building state, and automatic AI flag. Add `observation` completeness fields for interval size, expected/observed/missing interval counts, source digests, and covered UTC bounds. Add aggregate `ranking.work_buckets` aligned to each queue-drain interval; every bucket contains distinct genuine inserted/changed jobs, affected owners, tombstone-excluded owner/job pairs, recomputable expected owner/job pairs, queued pairs, run items, completed items, and published pairs. This is the validator's source for proportional-work and complete-publication equality rather than a trusted boolean.

    Bind observations to the exact Plan 09 deployment artifact with its whole-file SHA-256 plus the release source commit, migration 0064 SHA-256, active score-tick closure/version, and web commit/asset SHA-256 copied from that artifact. Collect the same-window Dismiss RPC latency evidence as an identifier-free numeric `dismissal.rpc_samples_ms` vector with `rpc_sample_count`, `rpc_p95_ms`, percentile method `nearest-rank-ceil-0.95n`, covered UTC bounds, source digests, and missing-log interval count. Require at least 20 complete source-bound RPC samples for a supportable P95; a smaller or gapped sample cannot yield PASS.

    Derive aggregate P50/P95/max and integers. Pass ranking acceptance only when: duration ≥86,400 seconds; every expected observation interval is present; score-tick success ≥0.99; HTTP 546 = 0; automatic paid/background AI calls = 0; phantom new_job runs/items = 0; every work bucket's recomputed expected owner/job pairs equal queued pairs, run items, completed items, and published pairs; empty ticks create zero work; no minute-cadence catalog growth; every genuine eligible change publishes; publication-to-dashboard P95 ≤900 seconds; database-change-to-visible P95 ≤120 seconds.

    Bind all observations to the exact 0064 definition hash, score-tick deployment closure/version, web asset/commit, Plan 09 evidence hash, UTC start/end, query/log source identifiers/digests, sample counts, missing intervals, and redaction counters. If the release identity changes, data is incomplete, or a threshold fails, record `ranking_acceptance: FAIL` or `INSUFFICIENT_EVIDENCE` truthfully; do not repair, reconstruct earlier history, extend the window backward, or mutate production.
  </action>
  <acceptance_criteria>
    - Window is entirely postrelease and at least 24 hours.
    - Metrics explicitly cover score-tick success/546, phantom and proportional work, freshness, and no paid AI.
    - Observation intervals and dismissal log coverage are source-bound with explicit expected/observed/missing counts; missing coverage cannot become PASS.
    - Evidence is aggregate, sanitized, source-bound, and complete enough to recompute the verdict.
    - Monitoring occurs after release and is not referenced by Plans 05/06/08/09 as a prerequisite.
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.schema_version == 1 and .window.duration_seconds &gt;= 86400 and (.ranking.metrics.score_tick_success_rate &gt;= 0.99) and .ranking.metrics.http_546_count == 0 and .ranking.metrics.phantom_new_job_runs == 0 and .ranking.metrics.empty_tick_work_items == 0 and .ranking.metrics.minute_cadence_catalog_growth == false and (.ranking.metrics.publication_to_dashboard_p95_seconds &lt;= 900) and (.ranking.metrics.database_change_to_visible_p95_seconds &lt;= 120) and .ranking.metrics.automatic_ai_calls == 0' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json</automated>
  </verify>
  <done>A new exact-release 24-hour window truthfully records ranking health, proportional work, phantom-work absence, and freshness thresholds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Observe one owner-intended deployed dismissal</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json</files>
  <what-built>The exact Plan 09 release has completed a source-bound 24-hour ranking observation; the deployed Dashboard dismissal path is ready for one normal owner-intended acceptance action.</what-built>
  <action>Ask the owner to perform only the normal permanent Dismiss action on a card they genuinely intend to dismiss, then report the observable timing and interaction results below. Do not induce a production RPC failure or synthetic mutation.</action>
  <how-to-verify>
    1. Open the approved production Dashboard and confirm its served asset hash/commit matches Plan 09.
    2. Choose a real card you genuinely intend to dismiss permanently. Do not use a card you may want restored.
    3. Click Dismiss while another card's Apply/Mark Applied/Dismiss controls remain in view. Confirm the target disappears immediately (under 100 ms in the recorded browser timing), unrelated controls remain usable, and no page-wide pending lock appears.
    4. Confirm the dismiss RPC settles within 1.5 seconds for this observation and the permanent success announcement appears before any refill finishes.
    5. If refill fails naturally, confirm the card stays dismissed and a working Retry warning appears. Do not induce a destructive or ambiguous production network failure; the exact-RPC-failure rollback branch is accepted from Plan 06's automated receipt plus deployed asset identity.
  </how-to-verify>
  <resume-signal>Type `approved` with the observed optimistic and RPC timings, or describe the exact failure.</resume-signal>
  <verify>
    <automated>jq -e '.ranking.acceptance == "PASS"' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json</automated>
    <human-check>The owner confirms the selected card was genuinely intended for permanent dismissal and reports the observed timings/interaction state.</human-check>
  </verify>
  <done>One safe owner-intended production dismissal observation is available for the combined acceptance seal.</done>
</task>

<task type="auto">
  <name>Task 3: Seal combined ranking and dismissal acceptance</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json, scripts/verify-phase-05-1-postdeploy-acceptance.mjs</files>
  <read_first>
    - `05.1-10-POSTDEPLOY-ACCEPTANCE.json`: Task 1 source-bound metrics and preliminary verdict.
    - `05.1-06-SUMMARY.md`: exact success/failure/concurrency/refill automated test receipt and source blobs.
    - `05.1-09-RELEASE-DEPLOYMENT.md`: exact release identities and file bytes the local validator must bind.
    - Current human checkpoint response and Plan 09 deployed asset identity.
  </read_first>
  <action>
    Add the owner-observed optimistic/RPC timing as `owner_observed_rpc_ms`, unrelated-control result, success-before-refill result, optional refill-warning result, actor/UTC, production asset identity, and Plan 06 rollback/concurrency test receipt hashes. Include the checkpoint's genuine successful dismissal in `rpc_samples_ms` only when it is inside the bound window and has the same source/timing provenance. Set `rpc_sample_count` to the numeric vector length and compute `rpc_p95_ms` by sorting ascending and selecting nearest-rank index `ceil(0.95 × sample_count) - 1`. PASS requires at least 20 complete source-bound samples, zero missing dismissal-log intervals, and computed `rpc_p95_ms ≤ 1500`; otherwise set `dismissal_acceptance` and overall `status` to `INSUFFICIENT_EVIDENCE` rather than PASS. Never substitute the single owner observation for P95.

    Create one dependency-free Node validator `scripts/verify-phase-05-1-postdeploy-acceptance.mjs`. Its `--self-test` mode uses in-memory fixtures to prove PASS, tampered digest/release identity, proportional-work mismatch, incomplete publication, missing observation interval, fewer than 20 RPC samples, and recomputed P95 over 1.5 seconds. Its normal mode accepts only the Plan 10 JSON and exact Plan 09 deployment Markdown paths and independently recomputes: whole-file Plan 09 SHA-256 and exact copied release identities; 24-hour UTC/window and expected=observed with zero missing intervals/source digests; every work bucket's `expected_owner_job_pairs = genuine_changed_jobs × affected_owners - tombstone_excluded_pairs`; equality of expected, queued, run, completed, and published pairs; zero unfinished work at seal; `rpc_sample_count === rpc_samples_ms.length`, minimum 20, nearest-rank P95 equality and threshold; and the canonical evidence SHA-256. Define the canonical digest as UTF-8 JSON with root `evidence_sha256` omitted, object keys recursively lexicographically sorted, array order preserved, compact encoding, and one terminal LF. Use only Node built-ins and local files; the validator performs no network/provider/database/browser call.

    Set `dismissal_acceptance: PASS` only when optimistic removal &lt;100 ms, the validator-recomputed source-bound P95 is ≤1.5 seconds, unrelated cards remained usable, success preceded refill, the served asset equals Plan 09, and Plan 06 exact-failure rollback/refill tests still match the deployed source. Set `ranking_acceptance: PASS` only from validator-recomputed work/publication/completeness/release/digest checks, never by trusting the JSON's preliminary field. Set overall `status: PASS` only when the validator derives both PASS results. Structural/tamper/threshold failures are `FAIL`; missing intervals, missing source binding, or unsupported sample counts are `INSUFFICIENT_EVIDENCE`. Do not repair, redeploy, retry a production mutation, or alter earlier evidence.

    Add D-01–D-23 decision disposition references, including D-03/D-18 superseded historical prerequisites, D-12–D-15 and D-23 retention/cleanup deferral, and zero counts for migration/function/web/worker/retention/cleanup/maintenance effects in this plan. Canonicalize the JSON, recursively reject secrets and row-level identifiers, compute `evidence_sha256` over the defined evidence projection, reopen, and recompute.
  </action>
  <acceptance_criteria>
    - Overall PASS requires both the 24-hour ranking thresholds and deployed dismissal behavior.
    - `rpc_sample_count` is source-bound, equals the timing vector length, is at least 20, and `rpc_p95_ms` exactly equals the validator's nearest-rank computation and is ≤1500 ms.
    - Proportional work, complete publication, observation completeness, exact Plan 09 binding, and canonical digest are recomputed locally rather than trusted from stored PASS fields.
    - Failure rollback is grounded in exact deployed source/test hashes without inducing an unsafe production failure.
    - Artifact includes sample counts, limitations, all decision dispositions, and zero forbidden effects.
    - No retention, cleanup, repair, deployment, historical reconstruction, or approval reuse occurs.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-phase-05-1-postdeploy-acceptance.mjs --self-test &amp;&amp; node scripts/verify-phase-05-1-postdeploy-acceptance.mjs .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-RELEASE-DEPLOYMENT.md &amp;&amp; git diff --check -- .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POSTDEPLOY-ACCEPTANCE.json scripts/verify-phase-05-1-postdeploy-acceptance.mjs</automated>
  </verify>
  <done>The exact release has one truthful combined postdeploy verdict with recomputable ranking/dismissal evidence and no expanded scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider/database observations → acceptance | Aggregate data must be complete, source-bound, and non-secret. |
| Production UI/RPC → owner-intended dismissal | The action is permanent and must be chosen by the owner. |
| Local test receipt → deployed failure-branch claim | Source/asset hashes must match exactly. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05.1-10-01 | Tampering | 24-hour metrics | high | mitigate | Exact release/window/source binding, aggregate reconciliation, sample counts, gap accounting, and canonical evidence hash. |
| T-05.1-10-02 | Repudiation | dismissal observation | medium | mitigate | Record owner checkpoint, UTC, timings, asset identity, and Plan 06 test receipt hashes. |
| T-05.1-10-03 | Information Disclosure | logs/evidence | high | mitigate | Aggregate-only data, recursive secret/identifier rejection, process-only credentials. |
| T-05.1-10-04 | Denial of Service | monitoring | low | accept | Bounded recurring read-only queries; monitoring never invokes or repairs the worker. |
| T-05.1-10-SC | Tampering | Tool/dependency supply chain | low | accept | No install/update; observations bind existing approved source and provider identities. |
</threat_model>

<verification>
The dependency-free local validator must self-test, bind the exact Plan 09 file/identities, recompute observation completeness, proportional work, complete publication, at least 20 source-bound dismissal samples and nearest-rank RPC P95, and the canonical JSON digest. It may emit PASS only when every recomputed gate passes; insufficient samples or coverage must remain `INSUFFICIENT_EVIDENCE`.
</verification>

<success_criteria>
- Score ticks, phantom/proportional work, and freshness meet D-16/D-18/D-19 over 24 hours.
- Deployed Dismiss meets D-08–D-11/D-17 without unsafe synthetic failure, with source-bound `rpc_sample_count ≥ 20` and recomputed `rpc_p95_ms ≤ 1500`.
- Monitoring is post-release only and retention/cleanup stays deferred.
</success_criteria>

<output>
Create `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-SUMMARY.md` when done. Record the exact release/window/evidence hash, validator blob/self-test result, recomputed work/publication/completeness checks, `rpc_sample_count`, percentile method, recomputed `rpc_p95_ms`, dismissal checkpoint/test lineage, limitations, and zero forbidden effects. Do not claim retention or cleanup completion.
</output>
