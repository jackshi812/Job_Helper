---
context: phase
phase: 05.1-ranking-and-dashboard-performance-stabilization
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-07-31T21:15:03.915Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Immutable approval only — Two owner payloads were invalidated by normal ranking activity because volatile production counts were hashed into approval equality. Use the corrected version-2 87-key immutable allowlist and keep volatile observations outside the owner digest; enforce operational safety immediately before deployment instead.
- [ ] CONSTRAINT: Canonical receipt serialization — Independent review proved a blacklist could miss renamed volatile fields and a flat receipt could duplicate or self-reference its digest. Use the corrected exact payload/receipt markers, ordered keys, namespaced receipt fields, occurrence checks, and external receipt digest from commit `1f36ef7`.
- [ ] CONSTRAINT: Invalid approval history — Commits `30955dc` and `a22a88b`, old digest `0d997347ba6840ad6f7e1dbf89fcfe530d2c8e7c943dcce4c6f0ce08e5c0f1d1`, and every response bound to them are invalid, unsealed, never deployed, and non-reusable. Generate a fresh version-2 payload and owner signal.
- [ ] CONSTRAINT: Cached-image preflight — During Plan 01 development, `supabase test db` automatically pulled `public.ecr.aws/supabase/pg_prove:3.36`, violating the intended no-network run. Every future local Supabase command must first verify the exact required images are already cached and fail before the CLI can pull/install anything.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Volatile approval coupling | Live owner/run/item/log state changed between payload construction and receipt sealing, invalidating two otherwise identical approval attempts. | blocking | Corrected Plan 02 hashes only its closed 87-key immutable schema; corrected Plan 03 performs immediate read-only operational checks and returns `NO_DEPLOY_NATURAL_DRAIN` for active work. |
| Blacklist and ambiguous receipt | A blacklist could accept renamed volatile keys, and duplicated `approval_payload_sha256` fields could make receipt extraction ambiguous or self-referential. | blocking | Enforce the ordered immutable-key allowlist and canonical 13-key namespaced receipt; store `approval_receipt_sha256` outside its hashed region and assert exact occurrence counts. |
| Reusing invalid authorization | The first exact owner response matched its payload but that payload was invalidated before sealing; a later regenerated payload also drifted before checkpoint. | blocking | Retain invalid commits only as audit history; require a newly generated v2 digest and byte-identical owner signal before any receipt or deployment. |
| Implicit Docker image download | The Supabase CLI pulled a missing pgTAP image during a run intended to be network-free. | blocking | Preflight exact local Docker image identities before the first Supabase CLI subprocess; fail closed if any are missing. |
| Unbounded passive observation | A bounded read-only observer exceeded its ten-minute return window and had to be interrupted. | advisory | Use explicit deadlines, return immediately at the boundary, and never extend monitoring or intervene in queues/workers without new authority. |

<current_state>
Phase 05.1 is paused during Plan 05.1-02 Task 1. Plan 05.1-01 is complete. The approval-gate redesign is committed and independently verified, but the fresh version-2 approval payload was not generated before the user requested a usage-limit pause. The v2 executor was interrupted before editing or committing anything. Plan 05.1-03 has not started, and production has not been deployed or mutated.
</current_state>

<completed_work>

Completed:
- Plan 05.1-01 implemented migration `0063`, static tests, 44-assertion pgTAP proof, cached-image-preflighted local runner, source-bound evidence, summary, and tracking (`c70152b`, `e5324a7`, `e565c90`, `4ef94da`).
- Local build passed and 22 targeted web tests passed after Plan 01.
- Initial Plan 02 read-only production parity proved remote migrations exactly `0001..0061`, with `0062` and `0063` absent; no deployment occurred.
- Two stale approval payload attempts were safely invalidated before sealing/deployment (`30955dc`, `a22a88b`).
- Owner decision D-20 separated immutable owner approval from volatile immediate operational checks (`a7ab672`).
- Strict payload and receipt serialization was hardened (`1f36ef7`): 87 ordered immutable payload keys and 13 ordered namespaced receipt fields, fail-closed parsing, no self-reference.
- Independent `gsd-plan-checker` revalidated corrected Plans 02/03 and returned `VERIFICATION PASSED` with zero blockers.
</completed_work>

<remaining_work>

- Plan 05.1-02 Task 1: Run corrected read-only/local gates and supersede the invalid approval artifact with a fresh version-2 immutable payload and digest.
- Plan 05.1-02 Task 2: Present the exact fresh `APPROVE`/`DEFER` signal to the owner. Do not infer approval.
- Plan 05.1-02 Task 3: After byte-identical fresh approval, create the canonical receipt, commit it, create SUMMARY/tracking, and mark Plan 02 complete without deploying.
- Stop after Plan 02. The user explicitly requested a pause before Plan 03; do not begin production deployment automatically.
- Plans 05.1-03 through 05.1-12 remain unstarted.
</remaining_work>

<decisions_made>

- Owner selected the narrow gate correction: immutable migration/source/evidence/function-security/manifest/command/exclusion/rollback/stash/scope identities remain approval-bound; volatile state is informational and checked immediately before deployment.
- Active claimed/building work at Plan 03's immediate gate must return `NO_DEPLOY_NATURAL_DRAIN`; only read-only rechecks after natural drain are permitted. No worker, queue, lease, run, item, scheduler, or config intervention.
- Immutable drift requires a new owner approval. Operational drift alone does not recycle an otherwise exact immutable approval.
- Plan 03 remains one attempt, exact migration `0063` only, with `0062` excluded and forward-only recovery.
- Pause now due usage limit; complete Plan 02 in a fresh session and stop before Plan 03.
</decisions_made>

<blockers>
- No valid current Plan 02 owner payload/signal exists. The checked-in approval artifact is invalid audit history and must be superseded.
- A fresh exact owner response is required after v2 payload generation.
- Current session usage limit prompted the pause.
</blockers>

## Required Reading (in order)

1. `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/.continue-here.md` — acknowledge every blocking constraint before work.
2. `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-PLAN.md` — corrected v2 payload, checkpoint, and receipt contract.
3. `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-PLAN.md` — corrected immediate immutable/operational deployment gate; read for handoff compatibility only.
4. `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md` — D-20 owner decision and locked stabilization constraints.
5. `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json` — exact tested migration/source identities.
6. `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-SUMMARY.md` — completed containment proof and disclosed image-pull deviation.
7. `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md` — invalid audit history only; never treat it as current approval.
8. `.planning/METHODOLOGY.md` if present — apply project analytical lenses before assumptions.

## Critical Anti-Patterns (do NOT repeat these)

- Do not hash volatile operational snapshots into owner approval equality; use the v2 immutable allowlist plus Plan 03's immediate operational gate.
- Do not validate approval payloads with a blacklist or ambiguous global field extraction; use exact marker-scoped parsers and occurrence counts.
- Do not reuse either invalid payload/digest or the prior owner response.
- Do not let Supabase CLI auto-pull images; preflight cached identities before its first subprocess.
- Do not wait past a declared observation deadline or manually intervene in natural work.

## Infrastructure State

- Git branch: `main`; HEAD: `1f36ef788dc96409064ca5e0dd9ea2c06f570771`.
- Dirty files intentionally preserved: `.DS_Store`, `.planning/STATE.md`.
- Protected Phase 5 stashes remain exact: `913be95897066e53e6e1be0bd6e9483ab52a8437`, `2e6738fcf39a8a764be9e3664ee4023b01f051c9`, `db09c3a2ff51dfa6652b36b0b809c1f9654f0117`.
- No subagent, async external job, Colima VM, or local Supabase stack is running.
- Production was never mutated or deployed in this phase and was last verified with migrations `0001..0061`; `0062`/`0063` absent.
- Current Phase 05.1 placeholder scan found no false completion; the match in Plan 01 summary is explanatory prose.

## Pre-Execution Critique Required

- Design artifacts: corrected `05.1-02-PLAN.md` and `05.1-03-PLAN.md`.
- Critique result: independent checker passed commit `1f36ef7` with zero blockers after verifying the 87-key payload, 13-key receipt, immediate operational gate, invalid-history rules, and one-push scope.
- Gate: Resume only Plan 05.1-02. Do not begin Plan 05.1-03 until Plan 02 is complete and the owner explicitly chooses to resume beyond the requested pause.

<context>
The containment code is ready and tested; the remaining work is authorization packaging, not migration implementation. The original approval design failed because it coupled owner authorization to live ranking snapshots. That is fixed. Resume from the corrected v2 Plan 02 contract, generate one fresh immutable payload, obtain a new exact owner response, seal the canonical receipt, and pause. Keep all production interactions read-only until a later separately resumed Plan 03.
</context>

<next_action>
Start with: run `$gsd-resume-work`, pass the blocking-constraint understanding check, verify HEAD/dirty files/stash identities, then dispatch corrected Plan 05.1-02 Task 1 to generate and commit the fresh version-2 approval payload. Stop at its exact owner checkpoint.
</next_action>
