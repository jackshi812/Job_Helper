---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 03
type: execute
wave: 3
depends_on: [05.1-02]
files_modified:
  - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-CONTAINMENT-DEPLOYMENT.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "D-01/D-04: production applies only the approved containment forward migration; unrelated pending migration 0062, later ranking redesign, retention, dashboard, history-deletion, and storage-maintenance work remain undeployed."
    - "D-02: claimed/building work is neither invoked nor edited by this release; the existing scheduler may let it finish or expire naturally while this plan performs only bounded read-only observation."
    - "D-20: the fresh Plan 02 version-2 owner receipt binds immutable identities only; volatile counts, ranking work state, timestamps, and fixed-window log totals are never compared with approval by equality."
    - "D-20: after reconstructing the isolated root and immediately before the one push, this plan reruns immutable migration/function/security drift checks and aggregate operational checks; any building state/run or pending/claimed item causes NO_DEPLOY, natural drain, and read-only recheck only."
    - "Immutable drift requires a new exact owner approval; ordinary operational drift does not. No checkpoint, scheduler pause, repair, worker call, or other mutation may occur between the passing final gate and the exact push command."
    - "Stale commits 30955dc/a22a88b and payload digests 0d997347.../050492bd... remain invalid, unsealed, never deployed, and never reusable."
    - "Post-deployment evidence binds migration 0063 and its affected database definition to the approved source identity while proving migration 0062 remains unapplied remotely and function/web deployment identities remain unchanged."
    - "A sanitized mutation ledger records zero manual worker calls, queue/run edits, ranking-history deletes, scheduler changes, function/web deployments, VACUUM/ANALYZE, or other maintenance, plus explicit forward-repair rollback triggers."
  artifacts:
    - path: "supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"
      provides: "Plan 01's approved containment-only forward migration"
    - path: "supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql"
      provides: "Plan 01's real local enqueue/finalizer/catalog integration contract"
    - path: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md"
      provides: "Plan 02's hash-, target-, action-, and actor-bound owner approval receipt"
    - path: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-CONTAINMENT-DEPLOYMENT.md"
      provides: "Pre-push identity proof, one-migration deployment identity, read-only postconditions, zero-effect ledger, and rollback trigger"
  key_links:
    - from: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json"
      to: "supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql"
      via: "Exact four-artifact SHA-256/blob IDs, real-local runner identities, source commit/tree, and stash comparison"
      pattern: "0063_dismissal_tombstone_ranking_containment|sql_integration_sha256|local_runner_output_sha256|source_commit|source_tree"
    - from: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md"
      to: "isolated Supabase migration view"
      via: "Fresh version-2 receipt-bound target/policy/function-security/migration manifest/command/approval/exclusions, with volatile observations explicitly outside digest equality"
      pattern: "approval_contract_version|immutable_authorization_only|0063|isolated|db push --linked --yes --workdir \{fresh-isolated-root\}|approval"
    - from: "isolated Supabase migration view"
      to: "linked production database"
      via: "Repository-root immutable drift plus aggregate zero-active-work gate immediately followed by literal one-push command and read-only remote parity proof"
      pattern: "immutable_gate_status|operational_gate_status|NO_DEPLOY|db push --dry-run --linked --yes --workdir \{fresh-isolated-root\}|db push --linked --yes --workdir \{fresh-isolated-root\}"
---

<objective>
Deploy only the freshly approved containment migration after an immediate fail-closed immutable and operational gate, then leave any later/reappearing work to finish or expire through its natural scheduler path (D-01, D-02, D-04, D-20).

Purpose: Stop the tombstone-driven phantom-work producer at the narrowest production boundary without bundling unrelated schema, runtime, dashboard, cleanup, or operational mutations.
Output: A production-checkpointed, source-bound containment deployment record with read-only postconditions, a zero-effect mutation ledger, and fail-closed forward-repair triggers.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/job-copilot-performance.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-PLAN-OUTLINE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md
@supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
@web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts
@supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
@scripts/verify-phase-05-1-containment-local.mjs
@supabase/config.toml
@.planning/milestones/v1.0-phases/03-scoring-feed-notifications/03-10-SUMMARY.md

<interfaces>
Plan 01 owns the migration, Vitest, SQL integration, and local-runner bytes and
records their SHA-256/blob IDs plus real-local runner output/manifest/CLI/
fixture-rollback/teardown proof, source commit/tree, and preserved-stash inventory in
`05.1-01-CONTAINMENT-EVIDENCE.json`. Plan 02 owns the fresh version-2 immutable
owner receipt `05.1-02-CONTAINMENT-APPROVAL.md`, including exact source/evidence,
linked target, expected pre-migration policy, pre/post-0063 function-security,
isolated migration-view, command, exclusion, rollback, stash, and authorization
identities. Its separately labeled production counts/state/timestamps/log-window
observations are informational only and must never be compared with approval equality.

Plan 03 consumes the approval artifact only through its exact version-2 regions. The envelope markers are `<!-- containment-approval-v2-envelope-begin -->` / `<!-- containment-approval-v2-envelope-end -->`; the payload markers are `<!-- containment-approval-v2-payload-begin -->` / `<!-- containment-approval-v2-payload-end -->`; the receipt markers are `<!-- containment-owner-receipt-v2-begin -->` / `<!-- containment-owner-receipt-v2-end -->`; and the external receipt-digest markers are `<!-- containment-owner-receipt-v2-digest-begin -->` / `<!-- containment-owner-receipt-v2-digest-end -->`. The payload must contain exactly 87 ordered flat records whose key-list SHA-256 is `e63c1794d24e35301309068c91bf61a02bdce76cd679cd42d40b7505aa71351d`. The receipt must contain exactly the 13 ordered namespaced records declared by Plan 02 whose key-list SHA-256 is `4323074504d17e4ee1b001e2edd303174ab083a6362d13f6c018655b3374d46b`. Consume the envelope's unique `approval_payload_sha256` and `required_owner_signal`, the receipt's unique `receipt_approval_signal` and `receipt_approved_payload_sha256`, and the digest region's unique `approval_receipt_sha256` by marker-scoped extraction only. Unknown, duplicate, missing, out-of-order, malformed, extra-marker, ambiguous legacy actor/signal, or hash-mismatched input is `NO_DEPLOY_NEW_APPROVAL_REQUIRED`.

The canonical cwd is the repository root and the literal production CLI path is
`web/node_modules/.bin/supabase`, with its resolved path, SHA-256, and version
receipt-bound; it is never resolved relative to the isolated root. With
`SUPABASE_TELEMETRY_DISABLED=1` and a nonempty process-only access token, the
byte-stable dry-run argv is `db push --dry-run --linked --yes --workdir {fresh-isolated-root}` and deployment argv is `db push --linked --yes --workdir {fresh-isolated-root}`. Every linked CLI inventory/preflight, dry run, and push runs from repository-root cwd and includes `--workdir {fresh-isolated-root}`.
The isolated view contains exact 0001-0061 plus approved 0063 and excludes 0062.

The final pre-push safe-state predicate is aggregate-only and exact:
`building_ranking_state_count = 0`, `building_ranking_run_count = 0`, and
`pending_or_claimed_active_item_count = 0`, where active items belong to a
`building` run and have status `pending` or `claimed`. The queries use a bounded
statement timeout and reveal no row identity. They run after isolated-root
reconstruction and immediately before the exact push, together with fresh target,
migration-inventory, pre-0063 function/security, dry-run, CLI, approval, and stash
checks. The push is the next external operation; no human checkpoint or mutation
intervenes. Unsafe operational state yields `NO_DEPLOY_NATURAL_DRAIN`; immutable
drift yields `NO_DEPLOY_NEW_APPROVAL_REQUIRED`.

Commits `30955dc52da35097c744872a62de156ba3c23164` and
`a22a88b25d663a4622cbbbb98057ff6c12108c6e`, payload digest
`0d997347ba6840ad6f7e1dbf89fcfe530d2c8e7c943dcce4c6f0ce08e5c0f1d1`, and
pending digest `050492bd188ead83d18ec83ee2121104d585a95a6b7752ea33bdfe3ce9dfa3bf`
are invalid/unsealed/never-deployed audit history and cannot authorize this plan.
</interfaces>
</context>

<prohibitions>
- Do not apply `0062_phase_03_11_asset_manager_payments_workday.sql` or any migration other than approved 0063.
- Do not deploy, redeploy, or invoke any Supabase Edge Function; do not deploy or publish the web application or push source to a remote Git branch.
- Do not call score-tick or any worker manually, create a fixture, retry/cancel a run, reap a lease, repair queue/publication state, or write any ranking run/item/state row.
- Do not delete or rewrite ranking history, change retention, pause or edit a scheduler/cron job, run VACUUM or ANALYZE, reindex, compact, or perform any storage/database maintenance.
- Do not apply, pop, drop, rename, or rewrite any preserved Phase 5 stash; compare the receipt-bound stash inventory read-only before and after.
- Do not print, persist, hash into public evidence, or otherwise disclose access tokens, database passwords, service credentials, pooler URLs, user identifiers, job content, or row payloads.
- Do not edit an applied migration or improvise a down migration. A post-push mismatch blocks Plan 05.1-04 and requires a separately approved forward-only repair.
- Do not push while any final safe-state aggregate is nonzero. Record NO_DEPLOY, permit only natural scheduler-owned drain/expiry and bounded read-only rechecks, and do not invoke workers, repair queues, edit leases/runs/items/ranking state/config, or pause/change scheduling.
- Do not request a new owner approval for ordinary operational drift. Request one only when an immutable approved identity, policy, command, exclusion, rollback term, stash, or authorization scope changes.
- Do not accept or recreate approval from stale commits 30955dc/a22a88b or either prior pending digest; retain that history without revert, amendment, rewrite, or deletion.
</prohibitions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Revalidate the fresh immutable approval and isolated one-migration preflight</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-CONTAINMENT-DEPLOYMENT.md</files>
  <read_first>
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
    - .planning/debug/job-copilot-performance.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-SUMMARY.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-SUMMARY.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md
    - supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
    - web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts
    - supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
    - scripts/verify-phase-05-1-containment-local.mjs
    - supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
    - scripts/verify-phase-05-1-containment-local.mjs
    - supabase/config.toml
    - .planning/milestones/v1.0-phases/03-scoring-feed-notifications/03-10-SUMMARY.md
  </read_first>
  <action>
    Work read-only against production in this checkpoint. First require both dependency SUMMARYs, Plan 01 evidence, and a freshly sealed Plan 02 receipt. Strictly parse the exact version-2 envelope, payload, receipt, and external receipt-digest regions before using any value: require the fixed 87-key payload schema/hash, four-key envelope, 13-key namespaced receipt schema/hash, one-key digest region, exact marker and key occurrence counts, `approval_contract_version: 2`, payload `payload_contract_version: 2`, `payload_digest_policy: immutable_authorization_only`, `payload_serialization: flat_key_value_lf_v2`, and a payload digest different from `0d997347ba6840ad6f7e1dbf89fcfe530d2c8e7c943dcce4c6f0ce08e5c0f1d1` and `050492bd188ead83d18ec83ee2121104d585a95a6b7752ea33bdfe3ce9dfa3bf`. Recompute both region digests and require the marker-scoped `approval_payload_sha256`, `required_owner_signal`, `receipt_approval_signal`, `receipt_approval_signal_sha256`, `receipt_approved_payload_sha256`, `receipt_authorization_scope_sha256`, and `approval_receipt_sha256` relationships to match exactly. Recompute Plan 01 evidence/source/artifact identities; local-runner proof; target and expected migration policy; pre/post function-security identities; isolated-command contract; exclusions; rollback; authorization scope; CLI; and complete stash inventory. Compare only those immutable values with the receipt. Later planning commits, unrelated user work, and volatile production observations may differ without invalidating approval. Ambiguous, stale, or missing approval is not approval.

    Before any hosted credential is loaded or linked preflight runs, from canonical repository-root cwd rerun Plan 01's exact Vitest command and `SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-containment-local.mjs`. Require the runner to inject that exact environment into every CLI subprocess, plus exact 0001-0061-plus-0063, 0062 absence, real enqueue/finalizer/catalog PASS, synthetic rollback, and teardown PASS. Production must not be the first SQL execution; missing local prerequisites or identity drift stops without linked fallback.

    Privately load the existing gitignored access token without echoing it. Recreate a fresh `mktemp -d` isolated Supabase root from the receipt recipe with exact checksum-matched 0001-0061 and 0063 plus required config/link metadata; exclude 0062 and every later migration. Hash its ordered manifest and require receipt equality. From repository-root cwd use literal `web/node_modules/.bin/supabase`; capture remote inventory with argv `migration list --linked --workdir {fresh-isolated-root}`, then run exact dry-run command `SUPABASE_TELEMETRY_DISABLED=1 web/node_modules/.bin/supabase db push --dry-run --linked --yes --workdir {fresh-isolated-root}`. Require the expected ordered 0001-0061 policy with 0062/0063 absent, pre-0063 hosted function/security identity equality, and dry-run semantics containing only 0063. Reconfirm resolved CLI path/SHA/version. Collect current aggregate operational state only as separately labeled checkpoint-time information; do not compare its values/digest to the receipt and do not treat it as the final gate. Clean up only that validated temporary root after recording manifest/dry-run digests.

    Create the checkpoint-preflight section of `05.1-03-CONTAINMENT-DEPLOYMENT.md` with every recomputed nonsecret immutable identity, `approval_payload_sha256`, `receipt_approved_payload_sha256`, `approval_receipt_sha256`, fresh owner proof, `immutable_payload_allowlist_verified: true`, `canonical_receipt_verified: true`, `approval_field_occurrence_counts_verified: true`, local-SQL proof, expected remote policy, pre/post function-security identities, `canonical_cwd: repository-root`, literal/resolved CLI identity, exact dry-run/deployment argv, `dry_run_migrations: [0063]`, `excluded_pending_migrations: [0062]`, `checkpoint_preflight_status: PASS`, `volatile_counts_used_for_approval_equality: false`, zero prohibited counters, and rollback triggers. Present immutable parity for verification and label any operational aggregates informational/stale-before-Task-2. State that Task 2 must reconstruct again and run both gates immediately before push; this checkpoint cannot satisfy the live gate. Authentication, target, immutable byte/policy/function/security/command drift stops unchanged and requires new owner approval.
  </action>
  <verify>
    <automated>test "$(pwd -P)" = "$(git rev-parse --show-toplevel)" &amp;&amp; npm --prefix web test -- migration-0063-dismissal-tombstone-ranking-containment.test.ts deterministic-ranking-migration.test.ts deterministic-ranking-snapshot.integration.test.ts resume-routing-migration.test.ts &amp;&amp; SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-containment-local.mjs &amp;&amp; containment_deployment=.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-CONTAINMENT-DEPLOYMENT.md &amp;&amp; test -s "$containment_deployment" &amp;&amp; rg -q '^approval_contract_version: 2$' "$containment_deployment" &amp;&amp; rg -q '^fresh_owner_approval_verified: true$' "$containment_deployment" &amp;&amp; rg -q '^immutable_payload_allowlist_verified: true$' "$containment_deployment" &amp;&amp; rg -q '^canonical_receipt_verified: true$' "$containment_deployment" &amp;&amp; rg -q '^approval_field_occurrence_counts_verified: true$' "$containment_deployment" &amp;&amp; deployment_payload_sha256="$(awk -F': ' '/^approval_payload_sha256: /{count++; value=$2} END {if (count != 1) exit 61; print value}' "$containment_deployment")" &amp;&amp; deployment_receipt_payload_sha256="$(awk -F': ' '/^receipt_approved_payload_sha256: /{count++; value=$2} END {if (count != 1) exit 62; print value}' "$containment_deployment")" &amp;&amp; test "$deployment_payload_sha256" = "$deployment_receipt_payload_sha256" &amp;&amp; rg -q '^approval_receipt_sha256: [0-9a-f]{64}$' "$containment_deployment" &amp;&amp; rg -q '^volatile_counts_used_for_approval_equality: false$' "$containment_deployment" &amp;&amp; rg -q '^stale_approval_history_reusable: false$' "$containment_deployment" &amp;&amp; rg -q '^cli_environment: SUPABASE_TELEMETRY_DISABLED=1$' "$containment_deployment" &amp;&amp; rg -q '^local_sql_result: PASS$' "$containment_deployment" &amp;&amp; rg -q '^local_teardown_result: PASS$' "$containment_deployment" &amp;&amp; rg -q '^canonical_cwd: repository-root$' "$containment_deployment" &amp;&amp; rg -F 'dry_run_command_argv: web/node_modules/.bin/supabase db push --dry-run --linked --yes --workdir {fresh-isolated-root}' "$containment_deployment" &amp;&amp; rg -F 'deployment_command_argv: web/node_modules/.bin/supabase db push --linked --yes --workdir {fresh-isolated-root}' "$containment_deployment" &amp;&amp; rg -q '^checkpoint_preflight_status: PASS$' "$containment_deployment" &amp;&amp; rg -q '^final_operational_gate_pending: true$' "$containment_deployment" &amp;&amp; rg -q '^dry_run_migrations: \[0063\]$' "$containment_deployment" &amp;&amp; rg -q '^excluded_pending_migrations: \[0062\]$' "$containment_deployment" &amp;&amp; rg -q '^production_mutations_executed: 0$' "$containment_deployment"</automated>
    <human-check>Confirm the displayed immutable record matches the freshly accepted version-2 Plan 02 approval, proposes only 0063, rejects stale approval history, excludes volatile observations from equality, and retains every exclusion. This is not the final operational gate.</human-check>
  </verify>
  <acceptance_criteria>
    - Plan 01's four source identities, real-local-SQL runner proof, evidence, and Plan 02 receipt are present, digest-valid, mutually consistent, and unchanged.
    - Focused containment migration tests pass against the approved bytes.
    - The linked target, expected remote migration policy, and pre-0063 function/security identity match the approval; 0062 and 0063 are not yet applied remotely.
    - The isolated view's ordered digest matches the receipt, excludes 0062, and its fresh linked dry run proposes only 0063.
    - Any checkpoint-time counts are informational only, contain no secret/user/job payload, and are explicitly pending fresh Task 2 remeasurement; every prohibited-operation counter remains zero.
    - A human confirms immutable identity/scope parity without expanding or replacing the Plan 02 approval or substituting for Task 2's immediate gate.
  </acceptance_criteria>
  <what-built>A read-only, checksum-bound checkpoint preflight proving the fresh corrected containment approval is immutable-parity valid and excludes migration 0062; Task 2 still owns the immediate operational gate.</what-built>
  <how-to-verify>
    1. Compare the migration/test/source, fresh approval receipt, CLI, target/policy, function/security, stash, and isolated-view identities shown in the checkpoint record.
    2. Confirm the dry-run migration list contains only 0063 and explicitly excludes local-only 0062.
    3. Confirm volatile observations are excluded from approval equality, every prohibited mutation counter is zero, and Task 2 must recheck operational safe state immediately before push.
  </how-to-verify>
  <resume-signal>Reply `confirm exact immutable containment preflight` only if the displayed immutable record matches the fresh version-2 Plan 02 approval; otherwise describe the mismatch and leave production unchanged.</resume-signal>
  <done>The exact immutable one-migration checkpoint preflight is human-confirmed with the final live gate still pending, or execution stops with production unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Push only approved 0063 and record read-only natural-drain postconditions</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-CONTAINMENT-DEPLOYMENT.md</files>
  <read_first>
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-CONTAINMENT-DEPLOYMENT.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-SUMMARY.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-01-CONTAINMENT-EVIDENCE.json
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-SUMMARY.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md
    - supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
    - web/tests/migration-0063-dismissal-tombstone-ranking-containment.test.ts
    - supabase/tests/0063_dismissal_tombstone_ranking_containment.test.sql
    - scripts/verify-phase-05-1-containment-local.mjs
    - supabase/config.toml
  </read_first>
  <action>
    Proceed only after Task 1's exact resume signal and only with a fresh version-2 Plan 02 receipt. From canonical repository-root cwd rerun Plan 01's exact Vitest command and real local runner with `SUPABASE_TELEMETRY_DISABLED=1`, requiring exact 0001-0061-plus-0063, real enqueue/finalizer/catalog PASS, synthetic rollback, and teardown PASS. Recreate a fresh temporary isolated root from the approved manifest; never adapt the view, renumber the migration, change cwd/executable/environment, or omit `--workdir`. Complete all potentially slow local checks before the final production gate.

    After reconstruction, enter the final TOCTOU-critical section. Rerun every immutable check against current production and local bytes, beginning with the exact closed-schema parser for Plan 02's version-2 envelope/payload/receipt/external-digest markers and fields. Recompute the 87-key and 13-key order hashes, exact occurrence counts, payload/receipt digests, envelope signal, namespaced receipt signal and payload reference, signal hash, authorization-scope hash, and drift booleans. Then recheck source/tree/evidence/artifact SHA/blob identities; linked target; ordered remote migration policy exactly 0001..0061 with 0062/0063 absent; pre-0063 hosted function definition, owner, SECURITY DEFINER, empty search path, and grants; isolated manifest; CLI identity; stash identities; exclusions; rollback/scope; remote inventory command; and sole-0063 dry run. Record `immutable_payload_allowlist_verified_immediately_before_push: true`, `canonical_receipt_verified_immediately_before_push: true`, and `approval_field_occurrence_counts_verified_immediately_before_push: true` only after all parser checks pass. If any immutable value differs, write `immutable_gate_status: NO_DEPLOY_NEW_APPROVAL_REQUIRED`, `push_attempt_count: 0`, preserve evidence, and stop for a new exact owner approval.

    In the same final section and immediately before the push, run fresh aggregate-only queries with a 5-second statement timeout and no row identifiers. Require `building_ranking_state_count: 0`; `building_ranking_run_count: 0`; and `pending_or_claimed_active_item_count: 0`, counting only pending/claimed items belonging to building runs. Record query/output provenance and `operational_gate_checked_immediately_before_push: true`. If any value is nonzero or a query is unavailable/ambiguous, write `operational_gate_status: NO_DEPLOY_NATURAL_DRAIN`, `natural_drain_action: observe_only`, and `push_attempt_count: 0`. Permit at most three read-only full immutable-plus-operational rechecks over a bounded 15-minute window while the existing scheduler naturally completes or expires work. Do not invoke workers or repair queues; do not edit leases, runs, items, ranking state, config, or schedules; do not pause the scheduler. If still unsafe, stop and return a natural-drain checkpoint/re-entry with the same owner approval still valid. Operational drift alone never requests new approval.

    Only when both final statuses are `PASS`, execute the exact push as the next external operation with no intervening checkpoint, command, wait, or mutation: `SUPABASE_TELEMETRY_DISABLED=1 web/node_modules/.bin/supabase db push --linked --yes --workdir {fresh-isolated-root}` from canonical repository-root cwd. Execute at most one push attempt. The literal executable, resolved path/SHA/version, and argv must be byte-identical to Plan 02. Do not retry an ambiguous or failed push. Read remote inventory using the same isolated root and literal executable, record whether 0063 committed, and stop for a new owner-scoped forward decision if ambiguous.

    On success, require 0063 exactly once and 0062 absent; require the hosted `enqueue_deterministic_new_jobs(integer)` definition/security identity to equal the approved expected post-0063 identity; require no extra migration, function deployment, scheduler/config change, or other scope effect; and recheck all three protected stash commit/tree/subject identities unchanged. Capture sanitized aggregate postconditions only as observations. Any work that appears after the pre-push gate is left to finish or expire naturally and never triggers an in-plan intervention.

    Finish the mutation ledger with `manual_worker_invocations: 0`, `queue_or_run_writes: 0`, `lease_or_ranking_state_writes: 0`, `ranking_history_deletes: 0`, `scheduler_mutations: 0`, `function_deployments: 0`, `web_deployments: 0`, `vacuum_or_maintenance_commands: 0`, and `excluded_action_count: 0`. Set `deployment_status: PASS` only if every identity and bounded postcondition passes. Record `rollback_trigger_recorded: true`: a post-commit target/migration/function-definition/security/scope mismatch means `FORWARD_RESTORE_APPROVAL_REQUIRED`; preserve evidence, perform no further mutation, and require a separately tested and approved forward migration. Never edit 0063 or migration history, run a down migration, invoke a worker, repair a queue, delete history, pause scheduling, or perform maintenance in response. Remove only the validated temporary root after its final manifest digest is recorded, and redact credentials plus user/job payloads from evidence/errors.
  </action>
  <verify>
    <automated>containment_deployment=.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-CONTAINMENT-DEPLOYMENT.md &amp;&amp; rg -q '^deployment_status: PASS$' "$containment_deployment" &amp;&amp; rg -q '^approval_contract_version: 2$' "$containment_deployment" &amp;&amp; rg -q '^fresh_owner_approval_verified: true$' "$containment_deployment" &amp;&amp; rg -q '^immutable_payload_allowlist_verified_immediately_before_push: true$' "$containment_deployment" &amp;&amp; rg -q '^canonical_receipt_verified_immediately_before_push: true$' "$containment_deployment" &amp;&amp; rg -q '^approval_field_occurrence_counts_verified_immediately_before_push: true$' "$containment_deployment" &amp;&amp; deployment_payload_sha256="$(awk -F': ' '/^approval_payload_sha256: /{count++; value=$2} END {if (count != 1) exit 61; print value}' "$containment_deployment")" &amp;&amp; deployment_receipt_payload_sha256="$(awk -F': ' '/^receipt_approved_payload_sha256: /{count++; value=$2} END {if (count != 1) exit 62; print value}' "$containment_deployment")" &amp;&amp; test "$deployment_payload_sha256" = "$deployment_receipt_payload_sha256" &amp;&amp; rg -q '^approval_receipt_sha256: [0-9a-f]{64}$' "$containment_deployment" &amp;&amp; rg -q '^stale_approval_history_reusable: false$' "$containment_deployment" &amp;&amp; rg -q '^volatile_counts_used_for_approval_equality: false$' "$containment_deployment" &amp;&amp; rg -q '^immutable_gate_status: PASS$' "$containment_deployment" &amp;&amp; rg -q '^operational_gate_status: PASS$' "$containment_deployment" &amp;&amp; rg -q '^operational_gate_checked_immediately_before_push: true$' "$containment_deployment" &amp;&amp; rg -q '^building_ranking_state_count: 0$' "$containment_deployment" &amp;&amp; rg -q '^building_ranking_run_count: 0$' "$containment_deployment" &amp;&amp; rg -q '^pending_or_claimed_active_item_count: 0$' "$containment_deployment" &amp;&amp; rg -q '^push_attempt_count: 1$' "$containment_deployment" &amp;&amp; rg -q '^remote_0063_count: 1$' "$containment_deployment" &amp;&amp; rg -q '^remote_0062_count: 0$' "$containment_deployment" &amp;&amp; rg -q '^post_0063_function_security_match: true$' "$containment_deployment" &amp;&amp; rg -q '^protected_stashes_unchanged: true$' "$containment_deployment" &amp;&amp; rg -q '^canonical_cwd: repository-root$' "$containment_deployment" &amp;&amp; rg -F 'deployment_command_argv: web/node_modules/.bin/supabase db push --linked --yes --workdir {fresh-isolated-root}' "$containment_deployment" &amp;&amp; rg -q '^natural_drain_action: observe_only$' "$containment_deployment" &amp;&amp; rg -q '^manual_worker_invocations: 0$' "$containment_deployment" &amp;&amp; rg -q '^queue_or_run_writes: 0$' "$containment_deployment" &amp;&amp; rg -q '^lease_or_ranking_state_writes: 0$' "$containment_deployment" &amp;&amp; rg -q '^ranking_history_deletes: 0$' "$containment_deployment" &amp;&amp; rg -q '^scheduler_mutations: 0$' "$containment_deployment" &amp;&amp; rg -q '^function_deployments: 0$' "$containment_deployment" &amp;&amp; rg -q '^web_deployments: 0$' "$containment_deployment" &amp;&amp; rg -q '^vacuum_or_maintenance_commands: 0$' "$containment_deployment" &amp;&amp; rg -q '^excluded_action_count: 0$' "$containment_deployment" &amp;&amp; rg -q '^rollback_trigger_recorded: true$' "$containment_deployment"</automated>
  </verify>
  <acceptance_criteria>
    - A fresh owner receipt for contract version 2 passes; stale commits/digests/signals remain invalid, unsealed, never deployed, audit-only history.
    - After isolated-root reconstruction, final immutable migration/function/security/dry-run checks and fresh aggregate safe-state checks pass immediately before mutation, with no intervening checkpoint or command.
    - Any immutable drift returns NO_DEPLOY and requires new approval; any unsafe operational count returns NO_DEPLOY for natural drain and bounded read-only recheck without new approval or scheduler/worker/queue/state mutation.
    - Exactly one linked push runs from repository-root cwd through literal `web/node_modules/.bin/supabase` with receipt-bound resolved identity and `--workdir {fresh-isolated-root}`; only 0063 applies and 0062 remains remote-absent.
    - Remote migration and expected post-0063 function definition/owner/security/grants match the approved containment source; function/web deployment and scheduler identities do not change.
    - Only bounded aggregate active/building status evidence is collected; work that exists before or reappears after the gate is neither invoked nor edited and is explicitly left to finish or expire naturally per D-02/D-20.
    - All protected stashes remain exact and no excluded action or scope effect occurs.
    - The deployment record contains exact nonsecret source/target/command identities, sanitized postconditions, zero prohibited-operation counters, and a forward-only rollback trigger.
    - Any ambiguous or failed state remains fail-closed without a second push, manual recovery, cleanup, or scope expansion.
  </acceptance_criteria>
  <done>Freshly approved 0063 alone is live and source/function-security-bound after the immediate zero-active-work gate; 0062 is absent, stashes and excluded scope are unchanged, and any later work drains naturally.</done>
</task>

</tasks>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Restore ranking/Dashboard operational health | 05.1-01..12 | SET-COVERED | This chunk owns only the approved containment deployment boundary. |
| REQ | — | None — operational stabilization | — | N/A | ROADMAP assigns no requirement IDs to Phase 05.1. |
| RESEARCH | — | No Phase 05.1 RESEARCH.md | — | N/A | Diagnosis constraints are covered from the canonical debug artifact. |
| CONTEXT | D-01 | First release is containment only | 05.1-03 | COVERED | One isolated migration push; all other release artifacts prohibited. |
| CONTEXT | D-02 | Let claimed/building work finish or expire naturally | 05.1-03 | COVERED | Read-only active-run postconditions; zero manual worker/queue action. |
| CONTEXT | D-04 | Delete no history and perform no storage maintenance | 05.1-03 | COVERED | Zero-effect ledger plus explicit deletion/VACUUM/maintenance prohibitions. |
| CONTEXT | D-20 | Immutable owner payload; volatile state gated immediately pre-push | 05.1-03 | COVERED | Version-2 receipt excludes counts/state/log windows from equality; final zero-building/pending/claimed aggregate gate fails closed to natural drain only. |
| CONTEXT | D-03, D-05–D-19 | Observation, redesign, dismissal, retention, and acceptance gates | 05.1-04..12 | OTHER PLANS | Deliberately excluded from this containment-only deployment chunk per the approved outline. |
</source_coverage_audit>

<artifacts_this_phase_produces>
- `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-CONTAINMENT-DEPLOYMENT.md` with the fresh version-2 approval/source/CLI/target/isolation/function-security identities, immediate immutable and operational gate evidence, NO_DEPLOY semantics, one-push ledger, affected database-definition fingerprint, read-only natural-drain postconditions, protected-stash proof, zero prohibited-operation counters, and rollback trigger.
- No new or modified application, Edge Function, web, migration, scheduler, queue, ranking-history, retention, or maintenance artifact; Plan 01's approved 0063 bytes remain immutable.
</artifacts_this_phase_produces>

<threat_model>
ASVS level: L1. Any unmitigated HIGH or CRITICAL finding blocks the production push and plan completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutable repository/planning records → privileged release preflight | Local bytes and approval records must not substitute for the exact Plan 01/02 identities. |
| Isolated local Supabase CLI → linked production database | `SUPABASE_ACCESS_TOKEN` permits the sole approved schema mutation against one exact project. |
| Existing scheduler → ranking run/item/state | Production work continues autonomously; this plan has read-only observation authority only. |
| Volatile operational state → one allowed push | Scheduler-driven work can change after approval; fresh aggregate state must be safe immediately before push without making it owner-hash input. |
| Production/catalog output → deployment evidence/owner | Evidence must be source-bound, attributable, bounded, and free of secrets/user payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05.1-03-01 | Tampering | Production migration set | critical | mitigate | Fresh version-2 receipt, isolated view, sole-0063 dry run, one push, and immediate remote migration/function-security proof; 0062 is excluded. |
| T-05.1-03-02 | Spoofing | Linked Supabase target/source | high | mitigate | Recompute project ref, ordered migration policy, pre-0063 function/security, CLI, source/tree/blobs, approval receipt, and stashes immediately before mutation; immutable drift requires new approval. |
| T-05.1-03-03 | Repudiation | Owner approval and mutation ledger | high | mitigate | Preserve actor/signal/timestamp and receipt digest; record exact command allowlist, start/end status, remote identity, and zero-effect counters. |
| T-05.1-03-04 | Elevation of Privilege | Worker/queue/scheduler/history authority | high | mitigate | Migration-only scope; no function invocation/deploy, queue/run DML, scheduler mutation, history deletion, or maintenance; bounded SELECT-only postconditions. |
| T-05.1-03-05 | Denial of Service | Active ranking work and large history table | high | mitigate | Immediately require zero building state/runs and zero pending/claimed items for building runs; unsafe state records NO_DEPLOY, allows only bounded read-only rechecks during natural drain, and forbids whole-history counts, workers, repair, scheduler pause, and maintenance. |
| T-05.1-03-06 | Information Disclosure | Credentials and hosted evidence | high | mitigate | Keep credentials process-only; sanitize outputs to identities/counts/statuses/digests and redact user IDs, job content, URLs, tokens, passwords, and nested errors. |
| T-05.1-03-07 | Tampering | Post-push recovery | high | mitigate | Immutable applied migration plus explicit fail-closed rollback triggers; any repair requires a new forward migration and separate exact owner approval. |
| T-05.1-03-08 | Tampering / Repudiation | Stale approval history | high | mitigate | Reject commits 30955dc/a22a88b and digests 0d997347.../050492bd... as invalid, unsealed, never deployed, non-reusable audit history; require a fresh version-2 exact owner signal. |
| T-05.1-03-09 | Tampering / Race | Approval-to-push TOCTOU | high | mitigate | After reconstruction, rerun immutable migration/function/security and aggregate operational gates; when both pass, push is the next external operation with no intervening checkpoint/command and only one attempt. |
| T-05.1-03-SC | Tampering | Dependency supply chain | low | accept | No packages are installed; use the repository-pinned Supabase CLI and existing pinned test runner only. |
</threat_model>

<verification>
- Plan 01's focused containment test passes against the same migration/test bytes approved by Plan 02.
- The fresh isolated linked dry run proposes exactly 0063 while pre-existing local-only 0062 remains outside the view.
- A blocking human checkpoint verifies immutable source, target/policy, function-security, command, fresh approval, exclusions, and rollback posture; volatile observations are explicitly not equality inputs.
- After reconstruction and immediately before mutation, final immutable checks plus zero-building/pending/claimed aggregate checks pass; unsafe state returns NO_DEPLOY for natural drain/read-only recheck only.
- One isolated project-local linked push applies 0063; immediate read-only migration/function-security checks bind production to the approved source and show 0062 still unapplied.
- Function/web deployment and scheduler identities remain unchanged; active/building work is recorded read-only and left to natural completion/expiry.
- The deployment evidence records zero worker calls, queue/run writes, history deletes, scheduler mutations, function/web deployments, and maintenance commands, with no secrets or user/job payloads.
</verification>

<success_criteria>
- D-01: production receives only the containment forward migration and no incremental ranking, batching, retention, dismissal/dashboard, function, or web change.
- D-02: no worker is invoked and no run/queue state is repaired or cancelled; claimed/building work is left to finish or expire naturally.
- D-04: no ranking history is deleted and no VACUUM, ANALYZE, reindex, compaction, or storage maintenance runs.
- D-20: owner approval remains exact over immutable identities only; volatile state is freshly gated immediately before push, unsafe state permits natural drain/read-only recheck only, and only immutable drift requires new approval.
- Deployment evidence proves the fresh approved source/target/function-security identities, remote 0063 application with remote 0062 absence, unchanged stashes/excluded scope, read-only postconditions, and forward-only rollback triggers.
- Plan 05.1-04 may begin its 24-hour observation only when `deployment_status: PASS`; every mismatch blocks progression without improvised recovery.
</success_criteria>

<output>
Create `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-SUMMARY.md` when done. Record fresh version-2 approval identity, stale-history rejection, immediate immutable/operational gate results, exact one-push identity, 0063-present/0062-absent and post-0063 function-security proof, unchanged stashes/excluded scope, natural-drain posture, and forward-only recovery status.
</output>
