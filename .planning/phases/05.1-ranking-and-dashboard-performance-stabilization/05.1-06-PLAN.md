---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 06
type: execute
wave: 4
depends_on:
  - 05.1-03
files_modified:
  - web/src/pages/Dashboard.tsx
  - web/src/pages/Dashboard.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-08 and D-09: a successful dismiss RPC permanently commits the removal; refill, invalidation, and resume routing run afterward and cannot delay or reverse success."
    - "D-10: an RPC failure restores only the exact removed card at its captured page/index and shows a visible retryable error."
    - "D-11 and D-17: the target disappears immediately, only that target is pending, and every unrelated card/action remains interactive."
    - "D-16: background refill uses the existing feed route and warning/retry affordance without moving routing back into the dismissal critical path."
    - "D-21 and D-22: this local implementation depends directly on Plan 03, not historical observation or provenance."
  artifacts:
    - "web/src/pages/Dashboard.tsx contains a dismissal-specific synchronous cache transaction, exact-row rollback, per-card pending state, and detached refill/retry."
    - "web/src/pages/Dashboard.test.tsx proves success, failure, concurrency, focus, and timing/call-order behavior."
  key_links:
    - "Dismiss button → synchronous cache removal/session exclusion → dismiss_job RPC"
    - "RPC failure → current-cache patch at captured page/index → visible alert + restored Dismiss focus"
    - "RPC success → durable UI commit → detached refill/inactive invalidation → retryable warning only"
---

<objective>
Make Dashboard Dismiss immediate, per-card, exactly reversible on RPC failure, and durably settled before refill/routing work (D-08 through D-11, D-16, D-17, D-22).

Purpose: Remove user-visible coupling between a permanent lifecycle RPC and nonessential feed/refill/resume-routing work without redesigning the Dashboard.
Output: Two-file local implementation and focused tests; no deployment or external state change.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@web/src/pages/Dashboard.tsx
@web/src/pages/Dashboard.test.tsx
@web/src/lib/feed.ts

<interfaces>
- Existing cache type: `DashboardInfiniteData = InfiniteData&lt;DashboardFeedPage, string | null&gt;`.
- Existing RPC: `dismissJob(rowId)` is the durable mutation boundary.
- Existing refill routes: `backfillDashboardFeedRow(feedRequest, cursor)` for all-jobs continuation and `feedQuery.refetch()` for watchlist refresh.
- Existing feedback channels: `lifecycleError`, `queueAnnouncement`, `backfillError`, and `backfillRetry`.
- Existing focus fallbacks: per-row action refs and `tableRegionRef`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement synchronous per-card dismissal and exact rollback</name>
  <files>web/src/pages/Dashboard.tsx, web/src/pages/Dashboard.test.tsx</files>
  <read_first>
    - `web/src/pages/Dashboard.tsx`: `LifecycleMutationContext`, `removeRowFromInfiniteData`, `snapshotAndRemove`, `dismissMutation`, `refillVisibleQueue`, action refs, and Active-row buttons.
    - `web/src/pages/Dashboard.test.tsx`: current Query/Mutation mocks and accessible action assertions.
    - `web/src/lib/feed.ts`: refill calls may await resume routing and therefore must remain outside dismiss settlement.
  </read_first>
  <behavior>
    - Clicking Dismiss removes the target from rendered/cache state synchronously before any awaited cancellation or network result.
    - Two different cards can be dismissed while another dismissal is pending; unrelated Dismiss, Mark Applied, Apply, filters, and navigation controls stay enabled.
    - An in-flight feed response cannot visibly resurrect a locally pending or successfully dismissed ID.
    - RPC failure reinserts only that captured row into the current cache at its original page/index, preserves concurrent cache changes, shows the job-specific error, and focuses the restored Dismiss control.
    - RPC success leaves the row absent and announces permanent dismissal before refill/invalidation starts.
    - Refill failure leaves the dismissed row absent and exposes a working Retry control; retry success clears only the refill warning.
  </behavior>
  <action>
    Write callback-order and cache tests first. Add a dismissal-specific context containing the removed row, page index, row index, title, continuation cursor, and adjacent focus IDs; do not reuse the whole-cache `previous` snapshot used by Mark Applied. Add `dismissPendingIds` and a session `dismissedIds` exclusion set. The dismiss `onMutate` callback must be synchronous: locate/capture the target, update both sets, patch the query cache immediately, schedule focus, then fire query cancellation without awaiting it. Filter rendered feed rows through `dismissedIds` so a late in-flight page cannot resurrect the target.

    On RPC failure, remove only that ID from the two sets, patch the current cache by reinserting the captured row at the captured page/index only when absent, leave every unrelated/concurrent row untouched, show a visible error naming the role, and focus a dedicated restored Dismiss ref after render. If the original page no longer exists, insert into the nearest surviving page while preserving order and still restore exactly one row. On success, remove the ID from pending but keep it session-excluded, announce permanent dismissal, and do not await anything else.

    Remove dismissal from the global `lifecycleMutationPending` gate. A Dismiss button is disabled only when its own row ID is pending; Mark Applied retains its existing own-mutation guard but is not disabled by dismissal. Preserve Mark Applied behavior and the existing visual layout/classes; do not add a helper module, persistent telemetry store, new design system, or UI redesign.
  </action>
  <acceptance_criteria>
    - The optimistic removal occurs in the initiating turn and tests observe it in under 100 ms (D-17).
    - Concurrent unrelated cache changes survive any dismissal rollback.
    - Only the failed target returns; only its Dismiss control receives restored focus.
    - A successful target cannot be reinserted by a late query or refill.
    - No RPC/API/schema/feed contract changes and no production mutation occur in this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix web test -- Dashboard.test.tsx</automated>
  </verify>
  <done>Dismiss has immediate per-card optimistic state, exact targeted rollback, concurrent-card usability, and accessible focus/error behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detach refill and routing from durable dismiss settlement</name>
  <files>web/src/pages/Dashboard.tsx, web/src/pages/Dashboard.test.tsx</files>
  <read_first>
    - `web/src/pages/Dashboard.tsx`: `runBackfill`, `refillVisibleQueue`, dismiss success/settled callbacks, warning markup, and Retry button.
    - `web/src/lib/feed.ts`: `requestDashboardFeedPage` and resume-routing behavior that is allowed only after settlement.
  </read_first>
  <behavior>
    - The dismiss mutation promise settles at `dismiss_job`; pending state and success announcement clear without waiting for refill, feed invalidation, or resume routing.
    - Both all-jobs cursor refill and watchlist refetch execute in detached background work.
    - A background failure shows a retryable current-results warning and never converts the successful dismissal into an error or rollback.
    - Retry invokes the correct refill branch and clears the warning on success.
    - Inactive-query invalidation is also detached and cannot extend RPC settlement.
  </behavior>
  <action>
    Change dismiss `onSuccess` to a non-async durable commit callback. Set the success announcement and target pending state immediately, then launch `refillVisibleQueue(context.continuationCursor)` and inactive dashboard-feed invalidation with explicitly detached promises. Keep resume routing inside the existing feed/refill helper path, which is now after the critical boundary (D-09); do not call routing from `dismissJob` or await it in mutation callbacks.

    Make `refillVisibleQueue` own complete success/failure handling for both scopes. Store enough retry state for the existing Retry button to repeat either the all-jobs cursor backfill or watchlist refetch. A failure message must say the job remains dismissed and current results remain usable. Retry success clears the warning without changing dismissal success. Remove the dismiss `onSettled` awaited/returned invalidation path.

    Extend the existing test file with deferred promises to prove precise ordering: cache removal precedes unresolved RPC; RPC success clears the target pending state while refill remains unresolved; refill rejection leaves the row absent and enables Retry; unrelated controls remain enabled throughout; RPC failure performs exact rollback. Preserve existing feed/routing tests and production build.
  </action>
  <acceptance_criteria>
    - Dismiss settlement contains only the durable RPC latency; refill/routing/invalidation are post-settlement.
    - The background warning is visible and retryable for both Dashboard scopes.
    - Success never rolls back because refill/routing fails (D-08).
    - Existing Dashboard accessibility, filters, applied flow, and feed behavior remain passing.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix web test -- Dashboard.test.tsx feed.test.ts resume-routing-source.test.ts &amp;&amp; npm --prefix web run build &amp;&amp; git diff --check -- web/src/pages/Dashboard.tsx web/src/pages/Dashboard.test.tsx</automated>
  </verify>
  <done>The durable dismissal critical path ends at RPC success/failure, with refill and resume routing safely detached behind a visible retry path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser optimistic cache → dismiss RPC | Local removal must match durable success or exact targeted rollback. |
| Concurrent feed responses → visible rows | Late data must not resurrect pending/successful dismissals. |
| Refill/routing failure → user feedback | Background failure must not falsify durable mutation state. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05.1-06-01 | Tampering | optimistic cache rollback | high | mitigate | Capture exact row/page/index and patch current cache only; tests preserve concurrent edits. |
| T-05.1-06-02 | Denial of Service | per-card actions | medium | mitigate | Pending IDs scope disabling to one target and keep unrelated controls interactive. |
| T-05.1-06-03 | Repudiation | success versus refill warning | medium | mitigate | Separate permanent-dismiss announcement/error from post-success refill warning and retry. |
| T-05.1-06-04 | Tampering | late query response | medium | mitigate | Session exclusion filters pending/successful IDs until component teardown. |
| T-05.1-06-SC | Tampering | Tool/dependency supply chain | low | accept | No dependency install/update; existing React Query and test stack only. |
</threat_model>

<verification>
Focused Dashboard/feed/routing tests and the production build prove callback ordering, exact rollback, unrelated-card usability, detached refill, and accessible retry/focus behavior.
</verification>

<success_criteria>
- Optimistic removal is under 100 ms and per-card.
- RPC failure restores exactly the target; RPC success remains committed.
- Refill/routing/invalidation never block mutation settlement.
- No UI redesign, schema change, hosted read, or deployment occurs.
</success_criteria>

<output>
Create `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-06-SUMMARY.md` when done. Record callback-order tests, exact files/blobs, build result, D-08–D-11/D-16/D-17 coverage, `deployment_status: not_deployed`, and `hosted_reads: 0`; do not claim production P95 or post-release acceptance.
</output>
