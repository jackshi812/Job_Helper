# Phase 05.1: Containment Observation Provenance Remediation - Research

<user_constraints>
## User Constraints (from CONTEXT.md)

The following block is copied verbatim from `05.1-CONTEXT.md`. [VERIFIED: Git worktree]

DATA_A8F3C1D9_START

### Locked Decisions

### Containment Release
- **D-01:** The first production release is containment only: stop dismissal
  tombstones from qualifying as new ranking work. Do not bundle incremental
  ranking, worker batching, retention, or dashboard changes into that release.
- **D-02:** After containment deploys, let existing claimed/building work finish
  or expire naturally. Do not invoke workers manually, cancel runs, or repair
  queue state.
- **D-03:** Require a clean 24-hour production observation before any later
  ranking redesign is eligible to deploy.
- **D-04:** The containment release deletes no ranking history and performs no
  storage maintenance.
- **D-20:** Containment approval binds only immutable migration, source,
  evidence, target-policy, function-security, isolated-command, exclusion,
  rollback, stash, and authorization-scope identities. Volatile production
  counts, ranking work state, observation timestamps, and fixed-window log
  totals remain informational and are remeasured in a fail-closed gate
  immediately before deployment. Unsafe operational state permits natural
  drain and read-only rechecks only; it does not require new approval unless an
  immutable approved identity or scope changes.

### Ranking Contract
- **D-05:** Deterministic ranking remains the feed publication authority. The
  removed paid AI-scoring path must not be restored.
- **D-06:** Later `new_job` work must scale with genuine new or changed jobs and
  affected owners, never the complete open-job catalog.
- **D-07:** A genuine job must not become visible through a partial ranking
  publication; preserve the existing complete/publication correctness contract
  while removing unrelated full-universe work.

### Dismissal Interaction
- **D-08:** If the dismissal RPC succeeds but feed refill or resume routing later
  fails, keep the job dismissed and show a small retryable refresh warning. Do
  not resurrect durable server state.
- **D-09:** Consider dismissal complete when the dismissal RPC succeeds. Feed
  refill, inactive-cache reconciliation, and resume routing continue outside the
  mutation's user-blocking critical path.
- **D-10:** If the dismissal RPC fails, restore the exact optimistic card at its
  prior position and show a clear retry message.
- **D-11:** While a dismissal RPC is pending, disable lifecycle actions only on
  the targeted card. Other cards remain actionable.

### Ranking-History Retention
- **D-12:** Retain only operationally necessary item history: active/building and
  retry-dependent data plus the latest successful run needed by each owner/run
  reason. Keep seven days of lightweight run summaries, not seven days of full
  item payloads.
- **D-13:** Remove old history only in small monitored batches with abort
  thresholds for lock time, query latency, WAL/replication pressure, and errors.
- **D-14:** After deletion, prefer normal vacuum/analyze and internal page reuse.
  Further index or file-shrink maintenance is a separate capacity decision;
  `VACUUM FULL` is not part of this phase.
- **D-15:** Production deletion requires a separate owner checkpoint showing
  dry-run counts, protected-row invariants, backup/PITR readiness, batch limits,
  and rollback/abort procedures.

### Performance and Acceptance Gates
- **D-16:** Preserve the product freshness target: publication-to-dashboard P95
  within 15 minutes, including database-insert-to-visible P95 within 2 minutes.
- **D-17:** A dismissal row disappears optimistically within 100 ms, unrelated
  cards remain usable, and the targeted mutation settles at P95 within 1.5
  seconds.
- **D-18:** Before accepting the ranking redesign, observe 24 hours with zero
  HTTP 546 worker failures, at least 99% successful score ticks, and zero phantom
  `new_job` runs.
- **D-19:** Every `new_job` run must correspond to genuine arrivals; item creation
  must scale as new jobs times affected owners, with no minute-cadence growth
  during the observation window.

### the agent's Discretion
- Exact forward-migration structure, delta-selection query shape, batched staging
  API, worker batch size/concurrency, and retry implementation, provided every
  locked release boundary and acceptance gate above remains enforceable.
- Exact inline wording and placement of the retryable post-dismiss refresh
  warning, provided it is accessible and does not block unrelated job actions.
- Exact retention batch size and abort-threshold values, derived conservatively
  from dry-run and hosted read-only measurements before owner approval.

### Deferred Ideas (OUT OF SCOPE)

- Eligibility/filter tuning for the large ineligible population is a separate
  product-quality phase; it is not a performance fix.
- Connector expansion and the two connector-specific errors remain separate
  ingestion work after ranking stability is restored.
- Outreach Intelligence remains paused and isolated from this phase.

### Reviewed Todos (not folded)
- **Consolidate score tier controls** — dashboard presentation behavior, not the
  ranking-churn or dismissal-latency defect.
- **Refine company visibility controls** — feed filtering enhancement, outside
  the stabilization boundary.
- **Replace experience cap with title exclusions** — preference semantics,
  unrelated to ranking throughput.

DATA_A8F3C1D9_END
</user_constraints>

**Researched:** 2026-08-01
**Domain:** Immutable historical provenance for a read-only Supabase observation gate
**Confidence:** HIGH for repository facts and the remediation design; LOW for unqueried provider audit-history availability

## Summary

Plan 04 failed for the correct reason. Its parser requires four deployment-time runtime inputs—`score_tick_function_id`, `score_tick_function_version`, `score_tick_deployment_id`, and `scheduler_identity_sha256`—and the committed Plan 03 deployment record contains none of them. [VERIFIED: `scripts/verify-phase-05-1-containment-observation.mjs`; Git blob `a10bc1b13dfb45e15a1e9a2a2467898895063050`] Plan 02 records score-tick version `16`, an opaque `score_tick_identity_sha256`, the four expected log attributes, and query/output hashes, but it does not record either raw Edge identity, the hash serialization/preimage contract, or any scheduler digest. [VERIFIED: Git blob `93b49a491e39481540554801308bbb29c62c4dd4`] Therefore the repository can prove the containment migration, source, target, database function, and timing boundary, but it cannot presently prove the exact score-tick deployment or scheduler row at the 0063 push. [VERIFIED: Git object audit]

A supplemental plan must not edit Plan 03. It must append a separately hashed provenance record anchored to the original Plan 02/03 Git objects. [VERIFIED: Git object audit; `05.1-CONTEXT.md` established forward-only recovery] A historical recovery is valid only if a future, separately authorized read-only provider query returns an independently immutable and complete historical chain that binds the exact runtime tuple and cron row across the Plan 03 boundary. [VERIFIED: task authorization trust rule] An ordinary current function inventory, current cron row, current logs, an owner recollection, or a newly added Plan 03 field is not historical proof. [VERIFIED: task authorization trust rule]

If the provider cannot supply that chain, the narrowest safe outcome is not a waiver. The owner should approve a provenance redesign in which Plan 03 remains the immutable containment source/migration/target anchor, a newly committed read-only baseline becomes the score-tick/scheduler anchor, and Plan 04 observes that exact unchanged runtime for a fresh 24-hour window. [VERIFIED: CONTEXT D-03/D-18/D-19] This preserves every quantitative gate while truthfully labeling deployment-time runtime provenance as unrecovered. [VERIFIED: CONTEXT D-03/D-18/D-19]

**Primary recommendation:** Append Plan `05.1-13` as a Wave 4 remediation dependency before resuming Plan 04; make it produce one immutable supplemental provenance artifact with either `historical_recovered` or `current_baseline_redesign` mode, and make Plan 04 consume that artifact without changing Plans 01–03 or discarding its committed evaluator work. [VERIFIED: repository plan topology and Git history]

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|---|---|---|---|
| Preserve original evidence | Git object database | Planning artifacts | The original commit/tree/blob identities are the only local immutable anchors. [VERIFIED: Git object audit] |
| Recover historical Edge identity | Provider audit/log history | Local verifier | A later current inventory cannot attest a past deployment boundary. [VERIFIED: task authorization trust rule] |
| Recover historical scheduler identity | Provider database audit/history | Local migration evidence | Migration SQL proves intended creation bytes, not absence of later hosted changes. [VERIFIED: repository grep] |
| Decide unrecoverable fallback | Repository owner checkpoint | Planner | Changing Plan 04 from deployment-time runtime binding to a current sealed baseline is a material provenance redesign. [VERIFIED: CONTEXT D-03/D-18/D-19] |
| Enforce the 24-hour gate | Plan 04 local collector | Supabase read-only aggregates | The existing evaluator already enforces the locked window and D-18/D-19 thresholds offline. [VERIFIED: 14 passing Node tests] |

## Exact Provenance Gap

### Values Plan 04 Requires

| Required value | Where enforced | Present in committed Plan 03? | Present elsewhere? | Conclusion |
|---|---|---:|---|---|
| `score_tick_function_id` | `parseDeploymentRecord()` and inventory equality | No | Stable function UUID `ae6c147f-c3a8-417e-8057-d4105ac9aed5` appears in older Phase 03/03.4 artifacts, but not at the Plan 03 boundary. [VERIFIED: repository grep] | Historically plausible, not deployment-bound. |
| `score_tick_function_version` | `parseDeploymentRecord()` and inventory equality | No | Plan 02 and the committed diagnosis record version `16`. [VERIFIED: Git blob `93b49...`; diagnosis blob `c5fe5...`] | The version is locally recoverable, but it is not enough to identify a deployment. |
| `score_tick_deployment_id` | `parseDeploymentRecord()`, log filter, and inventory equality | No | Plan 02 records only a hash and attribute presence, not the raw value. [VERIFIED: Git blob `93b49...`] | Missing and not derivable from documented local bytes. |
| `scheduler_identity_sha256` | deployment parser and start/end `cron.job` equality | No | Older Phase 03.4 evidence records `* * * * *` and command MD5 `f90afe213d26135169ca316d8a8916c2`; migration 0019 holds intended SQL. [VERIFIED: archived rollout evidence; migration 0019] | No exact SHA-256 or no-change chain exists at Plan 03 deployment. |

The Plan 04 scheduler digest is SHA-256 over the exact hosted `jobname|schedule|command` string for the unique `score-tick-every-minute` row; a schedule expression alone is insufficient. [VERIFIED: `IDENTITY_SQL` in the Plan 04 evaluator] The Plan 04 runtime snapshot hashes `function_id`, `deployment_id`, `version`, `status`, and `verify_jwt`; the older stable function UUID is therefore not a substitute for the missing deployment ID. [VERIFIED: `runtimeIdentityFromInventory()`]

### Immutable Local Anchors

| Anchor | Commit / blob | SHA-256 | What it proves |
|---|---|---|---|
| Sealed Plan 02 approval | `80e54a8eb3f6b1c006d558dbff8815b42dec1306` / `93b49a491e39481540554801308bbb29c62c4dd4` | `6511e97d485672977efaca4e33354692eeb221bee0f032e6478f530672c36abf` | Owner-approved migration scope and the non-authorizing pre-deployment score-tick observations. [VERIFIED: Git object audit] |
| Plan 03 preflight record | `56b0def6d749635e8d569041e31ef7282ab0ca10` / `73d11814969b8d203b89c7afb4b7a51a80816e36` | `e0c1fe194cba4dc7e6e5d0d43d5634c39f75753c8d6cd14bd47d155aadff9c99` | The pre-push approval/source/target/function-security gate before mutation. [VERIFIED: Git object audit] |
| Plan 03 deployment record | `6b55aa964dad20978a3d216d7c7de33f4c5d11e3` / `a10bc1b13dfb45e15a1e9a2a2467898895063050` | `d0c5c7c4528f78adad3d49947fbdd2a9b46ec22487331fa1303cd4cb8d28b4eb` | Exact one-push 0063 result, target, database function hash, timing, and zero-effect ledger. [VERIFIED: Git object audit] |
| Plan 03 summary | `db5b5f7a3faa0f36bf6df0d553195a5c1adcd4c9` / `126fc6a185b76376c06d7e4d37e67c5b29ab9615` | `e7fb505a39292d959f2bdca8ee4cfb9332ed283098a37105e1c95b61df9538ea` | The handoff that claims Plan 04 readiness but contains no missing runtime values. [VERIFIED: Git object audit] |
| Partial Plan 04 evaluator | `1f78ece98933d8d15fcef4a4344f89fe8219b86d` / blobs `56a6748...`, `8efe56d...` | Script `bb2d2b...`; tests `f9e073...` | The already implemented fail-closed evaluator and 14-test contract. [VERIFIED: Git object audit; local test run] |
| Blocked Plan 04 artifact | `ab3d064a23d07b6723b542f85c2fd58ef62e473e` / `ee54dc391bdb51fc55ca18e36ef370acac879337` | `f5fb4da4396ed730272680a06fd17628caf7d864050ad8fb6ff3c6b2cf875779` | The failure occurred before credential loading or hosted reads. [VERIFIED: artifact contents and evaluator ordering] |

### Why Retroactive Editing Is Invalid

Editing `05.1-03-CONTAINMENT-DEPLOYMENT.md` now would create a new Git blob after the missing-value failure; it cannot show that those bytes were observed before or immediately after the 0063 push. [VERIFIED: Git content-addressing model and existing blob identities] Backdating a timestamp, copying values from a current inventory, or amending the prose would only make a new assertion about the past. [VERIFIED: task authorization trust rule] It would also break the original Plan 03 blob/SHA chain consumed by downstream artifacts and violate the phase's established forward-only recovery rule. [VERIFIED: Plan 03 summary and STATE decisions]

The correct repair is additive: preserve the original Plan 03 record byte-for-byte, record its commit/blob/SHA in a new artifact, and state exactly which missing facts were recovered, from what independent historical source, and at what later retrieval time. [VERIFIED: project evidence patterns in Phase 03.10 and Plan 05.1-02]

## Source Hierarchy and Trust Model

| Tier | Evidence | Permitted claim | Not permitted |
|---|---|---|---|
| T0 — immutable local primary | Existing Git commits, trees, blobs, and canonical SHA-256 values | “These exact project bytes existed in this commit graph.” [VERIFIED: Git object audit] | Git author/commit time as an independently notarized wall-clock time. |
| T1 — independently immutable historical provider record | A future provider audit/deployment record whose official contract proves historical immutability, complete retention, resource identity, actor/action, timestamp, and pagination completeness | Exact function/deployment/scheduler facts at the Plan 03 boundary, if all required fields and mutation channels are covered. [VERIFIED: task authorization trust rule] | Assuming such a record exists or is retained without querying and validating its contract. |
| T2 — historical runtime telemetry | Fixed-time Edge logs that expose exact `function_id`, `deployment_id`, and `version` | “This tuple executed at these recorded historical instants.” [VERIFIED: local log-query contract] | “The tuple was the deployment identity throughout the whole interval” or “cron caused the invocation” without an audit/no-change chain. |
| T3 — current provider snapshot | Current function inventory and current unique `cron.job` digest | Current state at retrieval time. [VERIFIED: local collector design] | Deployment-time state. |
| T4 — intended local configuration | Migration 0019, `supabase/config.toml`, and committed Edge source | Intended scheduler/function source contract. [VERIFIED: repository grep] | Hosted parity or absence of out-of-band changes. |
| T5 — owner statement | Fresh explicit owner checkpoint | Authorization for a redesigned baseline or to stop. [VERIFIED: project approval pattern] | Technical attestation that missing hosted facts were true in the past. |

Plan 02's `score_tick_identity_sha256`, query hash, and output hash are corroborative only. [VERIFIED: Git blob `93b49...`] No committed code or document defines the exact preimage serialization, so the digest cannot safely be decoded or compared to a guessed tuple. [VERIFIED: repository-wide pickaxe/grep] A future provider response may upgrade it only if the exact original query and canonical output transformation are independently recovered and reproduce the committed hashes byte-for-byte. [VERIFIED: cryptographic verification requirement]

## Recovery Decision

### Valid Recovery Paths

| Path | Required proof | Result |
|---|---|---|
| Provider historical deployment/audit chain | Exact project, function ID, deployment ID, version 16, ACTIVE/JWT posture, event timestamps bracketing `2026-08-01T16:39:10.434Z`–`16:39:16.746Z`, and complete evidence of no intervening function deployment/rollback. The provider history must itself have an independently immutable retention/completeness contract. [VERIFIED: Plan 03 timestamps; task trust rule] | May produce `provenance_mode: historical_recovered`. |
| Provider historical Edge logs plus provider deployment audit | Unique exact runtime tuple in invocations immediately before and after the 0063 push, no mixed tuple, plus complete deployment audit proving no change across the gap. [VERIFIED: local log attribute contract] | Valid for Edge identity; does not recover scheduler identity by itself. |
| Historical cron snapshot plus complete database audit chain | Exact unique `cron.job` name/schedule/active/command digest at or before the boundary, a later exact matching snapshot, and complete audit coverage for all migration, SQL, dashboard, CLI, and API paths capable of changing that row. [VERIFIED: local scheduler digest contract] | May recover scheduler identity. |
| Owner-reviewed current-baseline redesign | Explicitly state historical runtime identity is unrecovered; bind current migration 0063, containment DB function/security, project, score-tick runtime tuple hash, and unique scheduler hash in a new committed baseline; require exact start/end equality across a fresh 24-hour window. [VERIFIED: CONTEXT D-03/D-18/D-19] | Valid fallback that preserves gates without a deployment-time claim. |

### Invalid Recovery Paths

| Path | Why it fails |
|---|---|
| Edit Plan 03 to add current values | The new blob did not exist at deployment and launders a current snapshot into a historical record. [VERIFIED: Git object audit] |
| Treat current function inventory or current cron row as historical | It proves only retrieval-time state. [VERIFIED: task trust rule] |
| Infer deployment ID from stable function UUID or version 16 | The current collector treats function ID, deployment ID, and version as separate fields. [VERIFIED: Plan 04 evaluator] |
| Infer scheduler identity from migration 0019 alone | The migration proves initial intent but cannot exclude later manual or provider-side changes. [VERIFIED: repository migration audit] |
| Use invocation counts as scheduler proof | A manual invocation or another trigger can produce the same function log. [VERIFIED: trust-boundary analysis] |
| Brute-force the Plan 02 opaque hash | The preimage serialization and candidate domain are undocumented, so a match would not establish uniqueness or provenance. [VERIFIED: repository-wide grep] |
| Accept an owner recollection as technical evidence | Owner authority can approve a new design, not create missing provider facts. [VERIFIED: project approval pattern] |
| Loosen Plan 04 to ignore the identities | That waives a high-severity anti-spoofing gate and does not preserve the intended source-bound observation. [VERIFIED: Plan 04 threat model] |
| Start the 24-hour window before provenance is sealed | The window would again lack an immutable pre-start anchor. [VERIFIED: D-03 and Plan 04 frozen-window contract] |

## Recommended Supplemental Plan Structure

### Plan Number and Waves

Append `05.1-13-PLAN.md`; do not renumber or rewrite Plans 01–03. [VERIFIED: existing 12-plan inventory] Schedule it as **Wave 4R**, depending on `05.1-03`, and mark Plan 04 **paused/partial**, not unexecuted: its Task 1 commits `c61a359` and `1f78ece` and blocked artifact commit `ab3d064` remain intact. [VERIFIED: Git log] Resume Plan 04 as **Wave 4O** only after Plan 13 produces an offline-assertable pass artifact. Plans 05–07 remain blocked on the completed Plan 04 gate. [VERIFIED: ROADMAP dependency topology]

Update planning metadata only: add Plan 13 to ROADMAP, add `05.1-13` to Plan 04's dependencies, and describe the already committed Plan 04 source as retained implementation. [VERIFIED: current plan files] Do not mark Plan 04 complete, erase the fail artifact, squash commits, or change Plan 03. [VERIFIED: Git history preservation requirement]

### Plan 13 Tasks

1. **Offline anchor verifier.** Create a Node built-in verifier that reads the original Git objects by exact commit/path, recomputes every tabled blob/SHA, validates the Plan 02/03 timestamps and missing-field truth, and rejects a dirty/replaced anchor. It performs no network or credential access. [VERIFIED: existing Node/Git evidence pattern]
2. **Separately authorized historical probe.** Only under future explicit execution authorization, query allowlisted read-only provider history/log/database endpoints. Record canonical query hashes, provider retrieval time, retention/pagination proof, exact covered interval, result digests, and bounded nonsecret identities. Do not arm an observation window. [VERIFIED: task scope and existing read-only collector pattern]
3. **Classify, do not improvise.** Emit `historical_recovered` only if both Edge and scheduler chains pass. If either chain is missing, expired, mutable, incomplete, ambiguous, or unverifiable, emit `historical_irrecoverable` and stop before Plan 04. [VERIFIED: fail-closed requirement]
4. **Blocking owner redesign checkpoint if irrecoverable.** Present the exact gap and request approval for `current_baseline_redesign`; approval changes only the provenance model, not the D-03/D-18/D-19 thresholds, production, schedulers, or later deployment authority. [VERIFIED: CONTEXT D-03/D-18/D-19]
5. **Seal supplemental artifact.** Produce `05.1-13-CONTAINMENT-PROVENANCE.json` and an offline assertion command. In historical mode it binds the recovered historical chain; in redesign mode it binds a new current-runtime baseline and explicitly records `historical_deployment_identity_recovered: false`. [VERIFIED: recommended design]

### Plan 04 Consumption Contract

Plan 04 should take `--provenance-record <05.1-13 artifact>` in addition to the unchanged original Plan 03 path. [VERIFIED: recommended design] Its parser must recompute the original Plan 03 Git blob/SHA instead of expecting retroactively added fields. [VERIFIED: Git anchor design] The supplemental artifact should provide hashes of the expected raw Edge IDs, the expected version, the exact scheduler digest, project/migration/containment-function anchors, provenance mode, and owner-checkpoint hash when applicable. [VERIFIED: current collector field use]

At arm time, raw provider IDs stay process-only: Plan 04 reads current inventory, hashes the values, compares them with Plan 13, and uses the raw values only in the bounded log query. [VERIFIED: current redaction design] The final observation artifact continues to store only sanitized hashes. [VERIFIED: current evidence schema]

For `historical_recovered`, Plan 04 may retain the statement that the observed runtime is bound to the recovered Plan 03 deployment-time identity. [VERIFIED: recovery contract] For `current_baseline_redesign`, it must state only that the 24-hour window ran after containment deployment on the owner-approved, pre-window-sealed runtime baseline; it must not claim Plan 03 captured that runtime identity. [VERIFIED: truthfulness requirement]

## Exact Read-Only Checks and Failure Outcomes

| Check | Pass condition | Failure outcome |
|---|---|---|
| Original Git objects | Every exact commit/tree/blob/SHA in the anchor table resolves; Plan 03 has exactly zero occurrences of the four missing keys. [VERIFIED: current Git audit] | `anchor_drift` → stop; do not query provider. |
| Plan 02 corroboration | Version 16, fixed window, attribute contract, and committed hashes parse exactly from the original blob. [VERIFIED: Plan 02 blob] | `plan_02_corroboration_invalid` → stop. |
| Historical provider contract | Officially documented immutability, retention, completeness, pagination, timestamp, and field semantics cover the requested dates. This provider fact is not verified in this research because external/provider queries were prohibited. | `historical_provider_contract_unverified` → historical mode forbidden. |
| Edge historical tuple | One exact function/deployment/version tuple is present on both sides of the Plan 03 push, and complete audit history shows no deploy/rollback between them. [VERIFIED: required trust model] | `historical_score_tick_identity_missing_or_ambiguous` → historical mode forbidden. |
| Scheduler historical tuple | One exact score-tick cron row is bound, and complete database audit proves no row change across the deployment boundary. [VERIFIED: required trust model] | `historical_scheduler_identity_missing_or_ambiguous` → historical mode forbidden. |
| Current-baseline fallback | Current remote 0063 count is one, 0062 is absent, containment function/security match Plan 03, score-tick tuple is unique/ACTIVE, scheduler row is unique, and the baseline artifact is committed before Plan 04 arm. [VERIFIED: current Plan 04 identity/readiness model] | `current_baseline_not_sealable` → stop; no window. |
| Plan 04 arm | Provenance record offline assertion passes; current identity equals the sealed expected hashes; natural-drain readiness is exact. [VERIFIED: Plan 04 collector] | Drift → write sanitized fail, no re-arm, escalate. |
| Plan 04 collect | Same identity at end; 86,400 seconds; log coverage/success/546 gates; D-19 correlation; zero effects. [VERIFIED: 14 passing Node tests] | Preserve fail artifact; Plans 05–07 remain blocked. |

All future SQL must remain SELECT/CTE-only and return aggregate identities/counts/hashes, never user/job/run/item rows. [VERIFIED: Plan 04 SQL allowlist] All provider operations must be GET/read-only except the existing Management API database query POST carrying validated SELECT-only SQL. [VERIFIED: Plan 04 endpoint allowlist] Historical investigation must not invoke functions, change cron, deploy, migrate, or repair state. [VERIFIED: task authorization]

## Standard Stack

### Core

| Tool | Version | Purpose | Why Standard |
|---|---|---|---|
| Node.js built-ins (`node:test`, `node:crypto`, `fs/promises`) | `v26.3.1` | Offline verifier, canonical SHA-256, exact schema tests | Already used by Plan 04; no package installation. [VERIFIED: environment probe and source] |
| Git object database | `2.39.3` | Resolve immutable commits, trees, and blobs | Content-addressed project evidence authority. [VERIFIED: environment probe] |
| `jq` | `1.7.1-apple` | Exact supplemental/final JSON assertions | Already used by Plan 04's verification contract. [VERIFIED: environment probe and plan] |
| Repository Supabase CLI | `2.109.1` | Only if a future authorized read-only provider workflow needs the pinned client | Version and executable exist locally; no install is needed. [VERIFIED: package metadata and executable probe] |

### Supporting

| Component | Purpose | Constraint |
|---|---|---|
| Existing Plan 04 evaluator | Window, metrics, SQL/transport allowlists, and redaction | Extend additively; preserve commits and tests. [VERIFIED: Git log and local test run] |
| Provider Management/log endpoints represented in local code | Current inventory, logs, and aggregate SQL | Their historical audit/retention capability remains an open question and cannot be assumed. [VERIFIED: local code; external capability unverified] |

**Installation:** none. [VERIFIED: repository and plan scope]

## Architecture Patterns

### System Architecture Diagram

```text
Original Git objects (Plans 02/03)
            |
            v
  Offline anchor verifier ----FAIL----> STOP
            |
            v
Future separately authorized read-only historical probe
            |
     +------+------+
     |             |
 historical       irrecoverable
 chain PASS        history
     |             |
     |       owner-reviewed current-baseline redesign
     |             |
     +------v------+
  committed Plan 13 provenance artifact
            |
            v
Plan 04 arm: current identity == sealed identity + natural drain
            |
            v
immutable 24-hour window -> Plan 04 collect -> PASS or permanent FAIL
```

This flow separates “what was deployed then” from “what is running now” and never lets the latter silently substitute for the former. [VERIFIED: task trust rule]

### Recommended Project Structure

```text
scripts/
├── verify-phase-05-1-containment-provenance.mjs       # new offline/read-only verifier
├── verify-phase-05-1-containment-provenance.test.mjs  # new fail-closed tests
├── verify-phase-05-1-containment-observation.mjs      # additive provenance input only
└── verify-phase-05-1-containment-observation.test.mjs # preserve and extend existing tests

.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/
├── 05.1-13-PLAN.md
├── 05.1-13-CONTAINMENT-PROVENANCE.json
├── 05.1-03-CONTAINMENT-DEPLOYMENT.md   # unchanged historical blob
└── 05.1-04-CONTAINMENT-OBSERVATION.json
```

### Pattern 1: Additive Evidence Chain

**What:** A supplemental artifact points backward to exact immutable Git objects and forward to the consumer that needs the missing facts. [VERIFIED: recommended design]
**When to use:** When an executed historical record omitted a field that cannot be truthfully inserted later. [VERIFIED: current gap]

### Pattern 2: Hash Raw Provider IDs, Keep Them Process-Only

**What:** Store SHA-256 identities in committed/final evidence; retrieve raw values only inside the authorized process, compare their hashes, and use them only to scope provider queries. [VERIFIED: current Plan 04 redaction pattern]
**When to use:** For provider resource IDs that are needed for exact filtering but should not appear in sanitized observation output. [VERIFIED: Plan 04 evidence schema]

### Pattern 3: Explicit Provenance Modes

**What:** The artifact has mutually exclusive `historical_recovered` and `current_baseline_redesign` modes, with exact schemas and claims. [VERIFIED: recommended design]
**When to use:** To prevent a current-baseline fallback from being misread later as recovered deployment-time evidence. [VERIFIED: task trust rule]

### Anti-Patterns to Avoid

- **Mutable historical record:** Never add the missing keys to Plan 03. [VERIFIED: Git object audit]
- **Opaque hash promotion:** Never treat Plan 02's undocumented identity digest as decoded evidence. [VERIFIED: repository grep]
- **Audit-log hand waving:** “No change found” is invalid unless retention, pagination, mutation-channel coverage, and negative-query completeness are proven. [VERIFIED: research verification protocol]
- **Window laundering:** Do not reuse, slide, or restart the blocked Plan 04 window; it never armed. [VERIFIED: fail artifact]
- **Threshold weakening:** The fallback changes only provenance anchoring, never 86,400 seconds, zero 546, 99%, zero phantom runs, or genuine-arrival correlation. [VERIFIED: CONTEXT D-03/D-18/D-19]

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---|---|---|---|
| Historical immutability | A prose attestation or timestamp convention | Git object IDs plus an independently immutable provider audit chain | Prose and local timestamps can be rewritten. [VERIFIED: Git object model] |
| Cryptographic identity | Custom checksum logic | Node `createHash('sha256')` and Git blobs | Existing project pattern is tested and deterministic. [VERIFIED: current scripts] |
| Provider audit semantics | An inference from ordinary logs | Official provider contract plus exact retained audit export, if available | Runtime logs do not prove all mutation channels or scheduler ownership. [VERIFIED: trust analysis] |
| Observation restart policy | An automatic retry/window reset | Existing frozen-window fail-closed evaluator | A reset would hide a failed interval. [VERIFIED: Plan 04 contract] |

## Common Pitfalls

### Pitfall 1: Confusing Stable Function ID with Deployment ID

**What goes wrong:** The older stable UUID is copied into both fields. [VERIFIED: older verifiers call the stable `id` a “deployment ID”]
**Why it happens:** Historical project terminology predates Plan 04's separate `function_id` and `deployment_id` log attributes. [VERIFIED: repository grep]
**How to avoid:** Require an explicit future schema probe and reject equal-by-assumption fields. [VERIFIED: recommended gate]
**Warning sign:** Evidence has version 16 and one UUID but no independently named deployment field.

### Pitfall 2: Treating Absence of Repository Changes as Hosted No-Change Proof

**What goes wrong:** Git shows no scheduler or function source edit, so the hosted resource is declared unchanged. [VERIFIED: repository history]
**Why it happens:** Hosted dashboard, CLI, SQL, and provider operations can exist outside Git. [VERIFIED: project deployment patterns]
**How to avoid:** Require provider historical audit coverage or use the current-baseline redesign. [VERIFIED: trust model]
**Warning sign:** The proof never examines provider-side mutation channels.

### Pitfall 3: Using Current State to Fill a Historical Record

**What goes wrong:** A later snapshot is labeled “deployment-time.” [VERIFIED: task trust rule]
**How to avoid:** Store retrieval time and provenance mode; never alter Plan 03. [VERIFIED: recommended schema]
**Warning sign:** The first immutable record containing the identity is later than `ab3d064`.

### Pitfall 4: Overclaiming Negative Audit Results

**What goes wrong:** An empty query result is treated as proof of no mutation despite retention gaps or incomplete pagination. [VERIFIED: research verification protocol]
**How to avoid:** Fail unless the provider contract and response metadata prove the entire interval and all pages. [VERIFIED: required trust model]

## Code Examples

### Supplemental Artifact Shape

```json
{
  "schema_version": 1,
  "status": "pass",
  "provenance_mode": "historical_recovered",
  "historical_deployment_identity_recovered": true,
  "anchors": {
    "plan_02_blob_oid": "93b49a491e39481540554801308bbb29c62c4dd4",
    "plan_03_blob_oid": "a10bc1b13dfb45e15a1e9a2a2467898895063050",
    "plan_03_sha256": "d0c5c7c4528f78adad3d49947fbdd2a9b46ec22487331fa1303cd4cb8d28b4eb"
  },
  "runtime": {
    "score_tick_function_id_sha256": "<64 lowercase hex>",
    "score_tick_deployment_id_sha256": "<64 lowercase hex>",
    "score_tick_function_version": 16,
    "scheduler_identity_sha256": "<64 lowercase hex>"
  },
  "later_deployment_authorized": false
}
```

The real schema must have exact-key validation and separate shapes for historical recovery and current-baseline redesign. [VERIFIED: existing exact-schema project pattern]

### Additive Consumer Pattern

```javascript
// Source pattern: current Plan 04 parser and recommended additive remediation.
const legacy = verifyOriginalPlan03GitObject(EXACT_COMMIT, EXACT_PATH, EXACT_SHA)
const provenance = assertSupplementalProvenance(record)
const current = await readCurrentRuntimeIdentity() // future authorized read only

requireEqual(sha256(current.functionId), provenance.runtime.functionIdSha256)
requireEqual(sha256(current.deploymentId), provenance.runtime.deploymentIdSha256)
requireEqual(current.version, provenance.runtime.version)
requireEqual(current.schedulerSha256, provenance.runtime.schedulerSha256)

armOnlyAfterImmutableArtifactAndNaturalDrain(legacy, provenance, current)
```

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|---|---|---:|---|---|
| Node.js | Offline verifier/tests | ✓ | `v26.3.1` | None needed. [VERIFIED: environment probe] |
| Git | Immutable object audit | ✓ | `2.39.3` | None needed. [VERIFIED: environment probe] |
| jq | JSON gate | ✓ | `1.7.1-apple` | Node exact-schema assertion. [VERIFIED: environment probe] |
| Supabase CLI | Optional future read-only provider inspection | ✓ | `2.109.1` | Direct allowlisted Management API reads already represented in Plan 04. [VERIFIED: package metadata and local code] |
| Historical provider audit/log retention | Historical recovery branch | Not probed | Unknown | Owner-reviewed current-baseline redesign. [VERIFIED: current authorization prohibited the probe] |

**Missing dependencies with no fallback:** none for planning/offline verification. [VERIFIED: environment audit]
**Missing dependencies with fallback:** historical provider audit evidence may be unavailable; the non-waiver fallback is the current-baseline redesign. [VERIFIED: recommended design]

## Security Domain

Security enforcement is enabled at ASVS L1 in `.planning/config.json`. [VERIFIED: config]

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---|---:|---|
| V2 Authentication | No | The supplemental artifact creates no user authentication flow. [VERIFIED: scope] |
| V3 Session Management | No | No browser/user session is involved. [VERIFIED: scope] |
| V4 Access Control | Yes | Future provider access must be separately authorized and mechanically limited to read-only endpoints/SELECT-only SQL. [VERIFIED: task scope and Plan 04 allowlist] |
| V5 Input Validation | Yes | Exact-key schemas, canonical timestamps, bounded integers, hash formats, endpoint allowlist, and SQL allowlist. [VERIFIED: current evaluator] |
| V6 Cryptography | Yes | Node SHA-256 and Git object identities; no custom cryptography. [VERIFIED: current scripts] |

### Threat Model Inputs

| Threat | STRIDE | Required mitigation |
|---|---|---|
| Retroactive Plan 03 field insertion | Tampering / Repudiation | Exact original commit/blob/SHA verification; Plan 03 remains unchanged. [VERIFIED: Git anchors] |
| Current-state substitution | Spoofing | Explicit provenance mode and historical-chain gate. [VERIFIED: task trust rule] |
| Opaque-hash preimage guessing | Tampering | Treat Plan 02 hash as corroborative unless exact serialization is recovered. [VERIFIED: repository grep] |
| Provider history retention/pagination gap | Repudiation | Prove official retention, complete interval, and every page; otherwise fail. [VERIFIED: research verification protocol] |
| Mixed function deployments | Spoofing | Exact tuple on both sides plus complete deployment audit. [VERIFIED: required trust model] |
| Manual invocation misread as cron | Spoofing | Separate runtime log evidence from exact scheduler-row/audit evidence. [VERIFIED: trust analysis] |
| Privileged token misuse | Elevation of Privilege | Process-only credential, exact endpoint allowlist, SELECT-only SQL, timeouts, zero-effect counters. [VERIFIED: Plan 04 controls] |
| Raw production data leakage | Information Disclosure | Aggregate-only queries, hashed resource identities, no logs/rows/user IDs in artifacts. [VERIFIED: Plan 04 redaction tests] |
| Automatic re-arm after failure | Tampering | One sealed baseline, one immutable window, fail artifact preserved. [VERIFIED: Plan 04 contract] |

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|---|---|---|
| A1 | A Supabase project/tier may expose retained historical deployment or audit records sufficient for the preferred recovery branch. [ASSUMED] | Recovery Decision | Historical recovery may be impossible; use current-baseline redesign. |
| A2 | Any provider audit surface that exists may have a documented immutability/completeness contract adequate for deployment-time proof. [ASSUMED] | Source Hierarchy | Ordinary mutable logs would not satisfy the gate. |

No provider capability is presented as verified. External documentation and private provider queries were intentionally not fetched because this research turn was restricted to repository/Git/planning artifacts. [VERIFIED: task authorization]

## Open Questions (RESOLVED)

1. **RESOLVED — Provider retention and historical completeness**
   - What is verified: local code can query current function inventory, function-edge logs, and aggregate database SQL. [VERIFIED: Plan 04 evaluator]
   - What remains unverified: the project's provider tier, retention interval, immutability guarantees, deployment-history fields, and pagination semantics were not authorized to be queried. No provider capability is assumed.
   - Planning disposition: `historical_recovered` is permitted only when an independently verified official provider contract and the returned history prove immutable, complete retention of the full Plan 03 interval, complete pagination, and every required mutation channel. Ambiguous, missing, incomplete, parser-failed, timed-out, or transport-failed contract/history evidence yields `historical_irrecoverable`. Any current-baseline fallback requires a distinct hash-bound owner checkpoint; irrecoverability alone does not authorize it.

2. **RESOLVED — `deployment_id` semantics**
   - What is verified: older project verifiers call the stable function UUID a deployment ID, while Plan 04 expects separate `function_id` and `deployment_id` fields. [VERIFIED: repository grep]
   - What remains unverified: whether provider inventory and log surfaces expose the same independently defined per-deployment value. No equality, distinction, or field meaning is assumed.
   - Planning disposition: accept the deployment identity only when an independently verified official provider contract defines the field semantics and complete immutable history proves exact cross-source equality for the Plan 03 interval. Ambiguity, a missing or incomplete field/contract/history, parser failure, timeout, or transport failure yields `historical_irrecoverable`. The fallback remains unavailable without its distinct hash-bound owner checkpoint.

3. **RESOLVED — Independent scheduler no-change proof**
   - What is verified: migration 0019 defines the intended cron job, older evidence records its expression/command MD5, and Plan 03 records zero scheduler mutations by its executor. [VERIFIED: repository artifacts]
   - What remains unverified: whether provider-retained history independently covers every SQL, dashboard, API, CLI, migration, deploy, rollback, and scheduler-edit channel across the relevant interval. No provider audit coverage is assumed.
   - Planning disposition: historical scheduler recovery requires an independently verified official provider contract plus complete immutable history proving exact before/after scheduler equality and full mutation-channel coverage across the Plan 03 interval. Ambiguity, missing or incomplete contract/history/channel coverage, parser failure, timeout, or transport failure yields `historical_irrecoverable`. A truthful current-baseline redesign may proceed only after its distinct hash-bound owner checkpoint.

## Sources

### Primary (HIGH confidence)

- Git commits/trees/blobs `80e54a8`, `56b0def`, `6b55aa9`, `db5b5f7`, `1f78ece`, and `ab3d064` — immutable local evidence chronology and exact blob identities. [VERIFIED: Git object database]
- `05.1-02-CONTAINMENT-APPROVAL.md` — version 16, opaque identity/query/output hashes, and missing scheduler binding. [VERIFIED: Git blob `93b49...`]
- `05.1-03-CONTAINMENT-DEPLOYMENT.md` and `05.1-03-SUMMARY.md` — exact deployment boundary, database function identity, zero-effect ledger, and missing runtime identities. [VERIFIED: Git blobs `a10bc...`, `126fc...`]
- `scripts/verify-phase-05-1-containment-observation.mjs` and its tests — exact missing keys, digest definitions, read-only boundaries, and failure semantics. [VERIFIED: Git blobs and 14 passing tests]
- `supabase/migrations/0019_user_jobs_scoring.sql` and archived Phase 03.4 evidence — intended scheduler command, older exact schedule/MD5, stable function UUID, and v15 lineage. [VERIFIED: repository grep]
- `.planning/debug/job-copilot-performance.md` — v16 and explicit negative findings that exact cron rows and exact hosted bundle parity were not recovered. [VERIFIED: Git blob `c5fe5...`]

### Secondary (MEDIUM confidence)

- None. Context7/provider documentation lookup was not executed because the authorized scope was repository/Git/planning only. [VERIFIED: research execution log]

### Tertiary (LOW confidence)

- Conditional provider audit-history availability, recorded only as assumptions A1–A2 and open questions. [ASSUMED]

## Metadata

**Confidence breakdown:**
- Exact missing fields and Git anchors: HIGH — directly verified from immutable local objects and executable parser. [VERIFIED: Git object database]
- Supplemental-plan architecture: HIGH — derived from locked gates and existing fail-closed evidence patterns. [VERIFIED: CONTEXT and project scripts]
- Historical provider recovery feasibility: LOW — deliberately unqueried and conditional. [ASSUMED]
- Current-baseline fallback: HIGH as a planning recommendation — it retains every D-03/D-18/D-19 threshold and makes no deployment-time runtime claim. [VERIFIED: CONTEXT]

**Research date:** 2026-08-01
**Valid until:** 2026-08-08 for provider-history feasibility; repository/Git findings remain valid while the referenced objects exist.
