---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 12
type: execute
wave: 12
depends_on: [05.1-11]
files_modified:
  - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "No cleanup starts without a byte-valid OWNER_APPROVED Plan 11 receipt whose release, target, fixed as_of, dry-run hash, protected invariants, limits, thresholds, recovery evidence, and RPC/maintenance allowlist still match."
    - "Retention advances summary-first, then item payloads, then unprotected runs, then expired summaries through service-role-only bounded RPCs; batch size never exceeds the smaller approved value or hard cap 250."
    - "Every apply request has an idempotent request token and receipt; ambiguous response loss is resolved read-only before any replay, and resumption uses the same immutable session rather than widening scope."
    - "Protected overlap, identity drift, any error, or any approved lock-wait, latency, WAL, replication-lag, row, byte, batch, or elapsed threshold breach immediately aborts and stops further deletion."
    - "Finish requires zero protected overlap, complete summary-before-delete evidence, fixed-as_of eligible completion, retry/latest-success/current/building protection, and exact aggregate reconciliation."
    - "Only after successful finish may approved ordinary VACUUM (never FULL) and ANALYZE run on named ranking-history relations; evidence distinguishes logical deletion/reusable pages from physical file size."
    - "No worker/queue action, schema/function/web deployment, scheduler/secret change, paid AI, unrelated deletion, table rewrite, or stash mutation occurs."
  artifacts:
    - path: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md"
      provides: "Approval-bound preflight, per-batch receipts/monitoring, abort or finish disposition, maintenance ledger, and postconditions"
  key_links:
    - from: ".planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-11-RETENTION-CLEANUP-APPROVAL.md"
      to: "retention session creation"
      via: "Exact payload/receipt hashes, target, dry-run artifact, immutable session envelope, and owner signal"
      pattern: "OWNER_APPROVED|approval_payload_sha256|approval_receipt_sha256"
    - from: "public.apply_deterministic_ranking_retention_batch"
      to: "05.1-12 per-batch ledger"
      via: "Idempotent request token, returned phase/count/receipt, and after-batch threshold observations"
      pattern: "request_token|batch_receipt|lock_wait|wal_bytes|replication_lag"
    - from: "public.finish_deterministic_ranking_retention"
      to: "ordinary vacuum/analyze gate"
      via: "Finish PASS and exact reconciliation before named maintenance commands become eligible"
      pattern: "cleanup_status|VACUUM|ANALYZE"
---

<objective>
After exact owner approval, execute monitored bounded production-retention batches with immediate abort thresholds and postconditions, followed only by approved ordinary vacuum/analyze for internal page reuse and statistics; never use VACUUM FULL (D-12–D-15).

Purpose: Remove obsolete ranking payloads without endangering publication/retry state or causing an unbounded production event.
Output: One sanitized, approval-bound cleanup ledger ending in PASS or ABORTED; no implicit recovery or scope expansion.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-07-SUMMARY.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-POST-GATE-DEPLOYMENT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-POST-DEPLOYMENT-ACCEPTANCE.json
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-RETENTION-DRY-RUN.json
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-11-RETENTION-CLEANUP-APPROVAL.md
@supabase/migrations/0065_deterministic_ranking_retention.sql
@scripts/verify-phase-05-1-retention.mjs

<interfaces>
Use only the migration-0065 service-role functions `preview_deterministic_ranking_retention`, `begin_deterministic_ranking_retention`, `apply_deterministic_ranking_retention_batch`, `abort_deterministic_ranking_retention`, and `finish_deterministic_ranking_retention`. Never issue manual DELETE/UPDATE statements. The session fixes as_of, target/release/dry-run identity, phase-specific batch sizes, maximum totals, and five abort thresholds.

Each apply call carries a unique deterministic request token and returns a persisted receipt. The executor records only aggregate counts/timing/WAL/lag/status/digests. A missing response is not retried until a read-only receipt lookup proves the request absent; a recorded request is consumed as replay success without reapplying.

Protected runs are the union of active/current, building, retry-available lineage, and latest successful per owner/run kind. Summary rows contain no item payloads. Seven-day lightweight summaries expire only in their approved final phase.
</interfaces>
</context>

<prohibitions>
- Do not act on a generic approval, stale payload, changed dry run, different project, changed release, relaxed threshold, larger batch, or altered fixed as_of.
- Do not run manual DML, VACUUM FULL, CLUSTER, REINDEX, table rewrite/shrink, unbounded maintenance, or maintenance before cleanup finish PASS.
- Do not automatically retry ambiguous begin/apply/abort/finish calls; resolve persisted receipts read-only and stop when certainty is unavailable.
- Do not invoke ranking workers/RPCs outside retention, repair/retry/cancel queues, change schedulers/secrets/configuration, deploy anything, or touch unrelated tables/data.
- Do not print credentials, connection strings, raw row IDs/payloads, user/job/resume data, or provider backup output.
- Do not alter protected stashes or unrelated owner work.
</prohibitions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Revalidate exact cleanup authority and zero-effect preflight</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md</files>
  <read_first>
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-11-SUMMARY.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-11-RETENTION-CLEANUP-APPROVAL.md
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-RETENTION-DRY-RUN.json
    - .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-09-POST-GATE-DEPLOYMENT.md
    - supabase/migrations/0065_deterministic_ranking_retention.sql
    - scripts/verify-phase-05-1-retention.mjs
  </read_first>
  <action>
    Work read-only. Recompute Plan 11 payload/receipt/raw-signal hashes and require the exact owner actor/signal. Revalidate Plan 09 release/target/function definitions and Plan 10 dry-run canonical hash, READY status, fixed as_of, recovery evidence, protected overlap zero, batch/threshold envelope, and zero mutation ledger. Reconfirm all three stashes.

    Run a fresh read-only preview only to detect drift; it must match the approved protected invariants and must not widen or replace the fixed dry-run eligibility. Any release, target, protection, recovery, count, threshold, or function-grant drift stops and requires a new Plan 10/11 cycle. Record `production_mutations_executed: 0` and the exact proposed begin/apply/abort/finish plus maintenance allowlists in `05.1-12-RETENTION-CLEANUP.md`.
  </action>
  <verify>
    <automated>node scripts/verify-phase-05-1-retention.mjs --mode assert-dry-run --record .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-10-RETENTION-DRY-RUN.json &amp;&amp; rg -q '^approval_status: OWNER_APPROVED$' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-11-RETENTION-CLEANUP-APPROVAL.md &amp;&amp; rg -q '^preflight_status: PASS$' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md &amp;&amp; rg -q '^production_mutations_executed: 0$' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md</automated>
    <human-check>Confirm the displayed preflight is byte-, target-, as_of-, threshold-, recovery-, and scope-identical to the approved Plan 11 payload.</human-check>
  </verify>
  <resume-signal>Reply `confirm exact retention preflight` only if it exactly matches the prior approval; otherwise production remains unchanged.</resume-signal>
  <done>The exact deletion envelope is human-confirmed immediately before mutation, or cleanup stops unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Execute and reconcile bounded retention batches with immediate aborts</name>
  <files>.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md</files>
  <action>
    Proceed only after Task 1's exact resume signal and a final identity/protection/recovery recheck. Call `begin_deterministic_ranking_retention` once with the approved artifact SHA, fixed as_of, phase batch sizes, total caps, five thresholds, and deterministic session token. On ambiguity, query the session receipt read-only and stop unless the exact session is proven; never create a second session.

    Follow the RPC-reported phase order: materialize payload-free summaries first, delete only summarized unprotected items second, delete only empty unprotected runs third, and expire only approved summaries older than seven days last. For each batch, derive a unique token from approval/session/phase/sequence, call `apply_deterministic_ranking_retention_batch` once, validate its persisted receipt, and collect bounded aggregate lock-wait, latency, WAL, replication-lag, error, row, estimated-byte, cumulative-batch, and elapsed metrics. Between batches rerun the canonical protected-overlap preview and lightweight score-worker health aggregates.

    If any identity/protected overlap/error/threshold/total/health invariant fails, stop issuing apply calls and invoke `abort_deterministic_ranking_retention` once with the exact reason/token if authority is unambiguous. Record ABORTED and require a new owner-scoped plan for any recovery. When a response is lost, read the batch receipt: accept an exact recorded replay, apply only when proven absent and still within authority, otherwise abort/stop. Never loosen thresholds or change as_of to finish.

    Call `finish_deterministic_ranking_retention` once only when the session reports every approved fixed-as_of phase complete. Require zero protected overlap, preserved current/building/retry/latest-success rows, complete summary-before-delete mapping, seven-day summary policy, exact per-phase/cumulative reconciliation, and no worker-health regression. If and only if finish PASS and Plan 11 explicitly listed them, run ordinary `VACUUM (ANALYZE)` on the named ranking items/runs/summary relations one at a time with approved bounds; never use FULL. Capture post-maintenance planner stats and reusable/dead-space estimates, not a promised physical shrink.

    Seal the sanitized ledger with exact approval/session/dry-run hashes, per-batch aggregate receipts, any ambiguity decisions, thresholds, before/after logical counts/estimated reusable bytes, maintenance commands/status, zero out-of-scope counters, stash identities, and `cleanup_status: PASS` or `ABORTED`. Do not print row identifiers or payloads.
  </action>
  <verify>
    <automated>rg -q '^cleanup_status: PASS$' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md &amp;&amp; rg -q '^protected_overlap_after: 0$' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md &amp;&amp; rg -q '^vacuum_full_invocations: 0$' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md &amp;&amp; rg -q '^manual_worker_invocations: 0$' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md &amp;&amp; rg -q '^queue_repairs: 0$' .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md &amp;&amp; git diff --check -- .planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-12-RETENTION-CLEANUP.md</automated>
  </verify>
  <done>The exact approved fixed-as_of retention scope completed and reconciled within every threshold, or execution stopped ABORTED without scope expansion.</done>
</task>

</tasks>

<source_coverage_audit>
| Source | ID | Coverage |
|---|---|---|
| CONTEXT | D-12 | Protect active/building/retry/current/latest-success evidence and retain seven-day lightweight summaries. |
| CONTEXT | D-13 | Small resumable monitored batches with immutable abort thresholds. |
| CONTEXT | D-14 | Ordinary vacuum/analyze/internal reuse only; never VACUUM FULL. |
| CONTEXT | D-15 | Exact separate owner approval, dry run, PITR readiness, bounded execution, and abort/recovery evidence. |
</source_coverage_audit>

<threat_model>
ASVS L1; HIGH/CRITICAL findings block execution. Principal threats are stale/forged approval, protected-row deletion, replay/double-delete, threshold bypass, lock/WAL/replication overload, ambiguous RPC recovery, secret/PII leakage, and maintenance scope expansion. Exact hashes, fixed session state, persisted idempotency receipts, canonical protected preview, least-privilege RPCs, immediate aborts, aggregate evidence, and the VACUUM FULL prohibition mitigate them.
</threat_model>

<success_criteria>
- Exact approved scope is executed once through bounded idempotent RPCs, or stopped safely.
- Protected/publication/retry invariants remain intact and every count reconciles.
- Any maintenance is ordinary vacuum/analyze only and reports logical/reusable space accurately.
- No unrelated production or stash action occurs.
</success_criteria>

<output>Create `05.1-12-SUMMARY.md` with PASS or ABORTED status and no unsupported physical-space claim.</output>
