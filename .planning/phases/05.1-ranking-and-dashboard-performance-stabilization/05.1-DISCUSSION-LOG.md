# Phase 05.1: Ranking and Dashboard Performance Stabilization - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md; this log preserves alternatives considered.

**Date:** 2026-07-31
**Phase:** 05.1-ranking-and-dashboard-performance-stabilization
**Areas discussed:** first-release scope, dismissal behavior, retention policy, performance targets

---

## First-release scope

| Decision | Alternatives considered | Selected |
|----------|-------------------------|----------|
| Release contents | Containment only; containment plus incremental ranking; complete backend package | Containment only ✓ |
| Existing work | Drain naturally; pause and drain; cancel/repair runs | Drain naturally ✓ |
| Observation period | 24 hours; 6 hours; 48 hours | 24 hours ✓ |
| Cleanup coupling | No cleanup; small cleanup; full retention cleanup | No cleanup ✓ |

**Notes:** The user clarified that paid AI scoring was removed. The distinction
was recorded: deterministic ranking remains necessary for feed publication;
only paid/background AI scoring stays absent.

---

## Dismissal behavior

| Decision | Alternatives considered | Selected |
|----------|-------------------------|----------|
| Follow-up failure | Keep dismissed with retryable warning; restore row; fail silently | Keep dismissed with warning ✓ |
| Mutation completion | After dismissal RPC; after replacement row; after all refresh/routing | After RPC ✓ |
| RPC failure | Restore exact card; hide and retry; reload entire feed | Restore exact card ✓ |
| Pending scope | Target card only; every Dismiss button; all lifecycle buttons | Target card only ✓ |

**Notes:** Durable server truth wins over nonessential refresh or routing work.

---

## Retention policy

| Decision | Alternatives considered | Selected |
|----------|-------------------------|----------|
| Retained history | Minimal operational history; 30 days complete; permanent | Minimal operational history ✓ |
| Deletion mode | Small monitored batches; one transaction; downtime window | Small monitored batches ✓ |
| Space reclamation | Internal reuse first; immediate concurrent rebuild; `VACUUM FULL` | Internal reuse first ✓ |
| Approval | Separate cleanup approval; automatic after migration; bundled approval | Separate approval ✓ |

**Notes:** The user asked how much containment alone would free. It was made
explicit that containment immediately frees essentially no space; it stops the
approximately 849,000 unnecessary item evaluations per day. Reclamation belongs
to the later separately approved retention operation.

---

## Performance targets

| Decision | Alternatives considered | Selected |
|----------|-------------------------|----------|
| New-job freshness | 15-minute end-to-end and 2-minute post-insert P95; 10 minutes; 5 minutes | 15/2-minute targets ✓ |
| Dismissal responsiveness | 100 ms optimistic and 1.5-second settle P95; 2.5 seconds; 5 seconds | Immediate-feeling target ✓ |
| Worker stability | Zero 546, at least 99% success, zero phantom runs; tolerate failures; visible-only gate | Strict gate ✓ |
| Growth proof | Work proportional for 24 hours; require file shrink; no growth gate | Work-proportional gate ✓ |

**Notes:** Acceptance requires measured cardinality and latency, not a subjective
performance judgment.

## the agent's Discretion

- Internal SQL/API shape, worker batching mechanism, and conservative cleanup
  batch thresholds, within the locked release and safety gates.

## Deferred Ideas

- Eligibility/filter tuning, connector expansion, and outreach work.
