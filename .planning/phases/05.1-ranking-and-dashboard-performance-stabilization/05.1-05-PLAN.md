---
phase: 05.1-ranking-and-dashboard-performance-stabilization
plan: 05
type: execute
wave: 4
depends_on:
  - 05.1-03
files_modified:
  - supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql
  - supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql
  - web/tests/migration-0064-delta-ranking-and-batch-staging.test.ts
  - supabase/functions/_shared/deterministic-worker.ts
  - web/tests/deterministic-ranking-worker-runtime.test.ts
  - web/tests/deterministic-ranking-source.test.ts
  - scripts/verify-phase-05-1-delta-local.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-05 and D-06: a routine score tick creates work only for genuine inserted or ranking-relevant changed jobs, with total work proportional to changed jobs times affected owners and a hard batch bound."
    - "D-07: each delta run publishes its complete captured membership atomically; failed or stale batches never become partially visible."
    - "D-19: repeated empty minute-cadence ticks create no `new_job` runs/items and no linear catalog growth."
    - "D-01, D-02, and D-20: migration 0063 remains immutable and its owner/source/external_id tombstone exclusion is preserved in every delta enqueue path."
    - "D-21 and D-22: implementation starts directly from Plan 03 and is proven locally without historical provenance, hosted reads, credentials, or deployment."
  artifacts:
    - "supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql adds the bounded changed-job queue, delta-only enqueue/finalize/retry behavior, and set-based staging RPC."
    - "supabase/functions/_shared/deterministic-worker.ts evaluates a bounded claim batch and stages it in one RPC."
    - "supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql proves real PostgreSQL semantics."
    - "scripts/verify-phase-05-1-delta-local.mjs proves 0001-0061 + 0063 + 0064 against exact cached images with 0062 absent."
  key_links:
    - "ranking-relevant jobs trigger → deterministic_ranking_job_changes → enqueue_deterministic_new_jobs → deterministic_ranking_items"
    - "claimed rows → deterministic evaluator → stage_deterministic_ranking_results(jsonb) → finalize_deterministic_ranking_run"
    - "user_job_dismissals(owner/source/external_id) → enqueue exclusion and finalize publication containment"
---

<objective>
Replace routine full-catalog ranking with a bounded changed-job pipeline and one set-based staging call per claimed batch (D-05, D-06, D-07, D-19, D-22).

Purpose: Make scheduler work proportional to real change while preserving deterministic scoring, atomic publication, and migration 0063 tombstone containment.
Output: Local-only migration 0064, worker update, focused tests, and a fail-closed disposable PostgreSQL verifier. Migration 0064 remains not deployed.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-CONTEXT.md
@.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-03-SUMMARY.md
@supabase/migrations/0032_deterministic_ranking.sql
@supabase/migrations/0033_deterministic_ranking_gap_closure.sql
@supabase/migrations/0034_repair_deterministic_ranking_retry.sql
@supabase/migrations/0052_decouple_resume_routing.sql
@supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql
@supabase/functions/_shared/deterministic-worker.ts
@web/tests/deterministic-ranking-worker-runtime.test.ts
@scripts/verify-phase-05-1-containment-local.mjs

<interfaces>
- Existing worker constants: `CLAIM_BATCH_SIZE = 25`, `MAINTENANCE_BATCH_SIZE = 25`, `MAX_INVOCATION_MS = 45_000`.
- Existing worker client: `rpc(name, args)` and `from(name)` return abortable operations.
- Existing maintenance entrypoint: `enqueue_deterministic_new_jobs(batch_size integer default 25)` is service-role only.
- Existing publication entrypoint: `finalize_deterministic_ranking_run(p_run_id uuid)` returns `(status, seeded_count, published)`.
- Existing item staging identifies a claimed item by `item_id` plus ranking `revision`; the batch contract must retain these stale-write guards.
- Existing worker job inputs are `title`, `location`, `description_text`, `posted_at`, and `company_id`; only changes to scoring/tombstone identity/status fields create delta work.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the bounded changed-job database contract</name>
  <files>supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql, supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql, web/tests/migration-0064-delta-ranking-and-batch-staging.test.ts</files>
  <read_first>
    - `supabase/migrations/0063_dismissal_tombstone_ranking_containment.sql`: exact owner/source/external_id exclusion that 0064 must preserve (D-01, D-02, D-20).
    - `supabase/migrations/0032_deterministic_ranking.sql`: state/run/item tables and service-role contracts.
    - `supabase/migrations/0034_repair_deterministic_ranking_retry.sql`: failed-run retry membership.
    - `supabase/migrations/0052_decouple_resume_routing.sql`: current finalizer and routing separation.
  </read_first>
  <behavior>
    - Empty maintenance ticks produce zero runs, items, and queue growth.
    - Inserting N eligible jobs for M initialized owners produces exactly N×M queued work; updating one ranking input produces exactly M replacement queue rows.
    - Updating an unrelated job column produces no ranking work.
    - Dismissed owner/source/external_id identities are absent from queue selection, run items, and publication.
    - Each enqueue call selects at most 25 eligible owner/job rows and seeds only that membership, never the open-job catalog.
    - A failed delta retry retains only the original delta membership.
    - Batch staging accepts 1–25 unique claimed records, validates the entire payload, and commits all records or none.
    - Finalization publishes only when every captured item completed; stale or failed work cannot partially mutate visible deterministic results.
  </behavior>
  <action>
    Write tests first, then create additive migration `0064_delta_deterministic_ranking_and_batch_staging.sql`. Add a private durable table `deterministic_ranking_job_changes` keyed by `(user_id, job_id)` with a monotonic captured job-input revision, timestamps, FK cleanup, bounded lookup index, RLS enabled, and no authenticated/anon/public access. Add the smallest job/item revision columns needed to compare a queued input with the job version actually evaluated. Install one trigger that increments the job-input revision and upserts queue rows for existing ranking owners only on INSERT or `IS DISTINCT FROM` changes to `title`, `location`, `description_text`, `posted_at`, `company_id`, `status`, `source`, or `external_id`. Do not backfill the current job catalog into this queue.

    Replace `enqueue_deterministic_new_jobs(integer)` so `batch_size` is the hard total owner/job-row limit (1–25): select only queued open jobs whose owner state is idle/initialized, recheck the D-01/D-02/D-20 dismissal tombstone identity, create missing `user_jobs`, group the selected rows into one `new_job` run per owner, and seed exactly those rows. An empty queue performs no run/state write. No query in this routine may use missing `user_jobs.deterministic_revision` as a proxy for new work or seed every open job. Preserve initial/preferences explicit full-snapshot flows; routine maintenance must not invoke full-catalog recency work per D-06 and D-19.

    Replace finalization only where needed: `new_job` runs and retries sourced from them must never seed additional catalog rows; they update only completed captured items, delete queue rows only when the queued revision still equals the published captured revision, retain newer changes, and publish the complete batch in the same transaction. Keep existing full-snapshot initial/preferences semantics and resume-routing separation intact (D-07). Add service-role-only `stage_deterministic_ranking_results(p_results jsonb)` for 1–25 exact-key records. Validate array shape, unique item IDs, enum/range/breakdown rules, claim ownership/revision, and all records before one set-based update; return an exact staged count and fail atomically on any invalid/stale member. Keep the singular RPC as a compatibility adapter if existing tests/callers still require it, with identical authorization and validation.

    The static Vitest test must assert the bounded delta/trigger/batch/grant contracts and absence of retention/deletion/provider/AI additions. The pgTAP file must exercise real triggers, exact N×M counts, empty-tick idempotence, 25-row bounds/backlog continuation, changed/unrelated updates, tombstones, atomic batch rejection, delta retry, and complete-only publication. Do not edit migration 0063 or any historical migration.
  </action>
  <acceptance_criteria>
    - Routine enqueue complexity depends on queued owner/job changes, not total open jobs.
    - The queue starts empty on migration install and records only later genuine changes.
    - Migration 0063 remains byte-identical and its tombstone exclusion survives 0064.
    - All new write-capable functions are service-role only with fixed empty search paths.
    - No retention, cleanup, historical reconstruction, paid AI, or hosted operation is introduced (D-21, D-23).
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix web test -- migration-0064-delta-ranking-and-batch-staging.test.ts deterministic-ranking-migration.test.ts deterministic-ranking-gap-closure-migration.test.ts deterministic-ranking-retry-repair-migration.test.ts</automated>
  </verify>
  <done>Migration 0064 defines and statically proves a bounded genuine-change pipeline with atomic batch publication and preserved 0063 tombstones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage each claimed worker batch with one RPC</name>
  <files>supabase/functions/_shared/deterministic-worker.ts, web/tests/deterministic-ranking-worker-runtime.test.ts, web/tests/deterministic-ranking-source.test.ts</files>
  <read_first>
    - `supabase/functions/_shared/deterministic-worker.ts`: current claim/evaluate/per-row-stage/finalize/deadline flow.
    - `web/tests/deterministic-ranking-worker-runtime.test.ts`: abortable client and worker call-order fixtures.
    - `web/tests/deterministic-ranking-source.test.ts`: source-security invariants.
    - `supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql`: exact new batch RPC payload/return contract from Task 1.
  </read_first>
  <behavior>
    - One claimed batch loads jobs once, evaluates every row deterministically, and calls `stage_deterministic_ranking_results` exactly once.
    - Evaluator failures become failure records inside that same batch, not extra RPC calls.
    - Routine maintenance calls the changed-job enqueue only; it does not start catalog recency refresh.
    - A single invocation processes no more than 250 items in 25-row claims and honors the existing deadline/abort cleanup.
    - Finalization runs only after the batch stage succeeds and preserves complete-only publication.
    - `automatic_ai_scoring` remains false and no provider/model/key/accounting path appears.
  </behavior>
  <action>
    Update the runtime tests first. Refactor `processRow` into a pure result-record builder: successful evaluations map to the exact 0064 batch fields; deterministic input/evaluator failures map to an `error_code` record without making a database call. Refactor `processBatch` to load distinct jobs once, evaluate the claim rows with bounded local concurrency, submit exactly one `stage_deterministic_ranking_results` RPC containing 1–25 records, and require the returned count to equal the claim size before finalization. Treat an aborted, stale, partially acknowledged, or failed batch RPC as an invocation failure; do not silently fall back to per-item writes.

    Remove `enqueue_deterministic_recency_refresh` from automatic `runMaintenance`; preserve explicit preference/initial paths elsewhere. Keep claim size 25 and lower `MAX_ITEMS_PER_INVOCATION` from 5,000 to 250 so every minute tick has an explicit total bound while the next natural tick drains backlog. Preserve the existing 45-second deadline, abort signals, orphaned-run recovery, one job query per batch, complete-run finalization, error sanitization, service-role client, and `automatic_ai_scoring: false` response.

    Extend source tests to prove no paid/background AI path, no per-row staging RPC, no automatic recency enqueue, one batch RPC contract, 25/250 bounds, and unchanged security/deadline behavior. Do not change ranking/filter semantics.
  </action>
  <acceptance_criteria>
    - For K claim batches, staging RPC count is exactly K, not item count.
    - A routine tick without queued changes creates no work after maintenance.
    - The worker is bounded at 25 records per RPC and 250 records per invocation.
    - Existing abort, recovery, deterministic scoring, and atomic finalization tests pass.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix web test -- deterministic-ranking-worker-runtime.test.ts deterministic-ranking-source.test.ts deterministic-ranking.test.ts</automated>
  </verify>
  <done>The worker performs bounded changed-job maintenance and one atomic staging RPC per claimed batch with no paid AI.</done>
</task>

<task type="auto">
  <name>Task 3: Prove 0064 against disposable cached-image PostgreSQL</name>
  <files>scripts/verify-phase-05-1-delta-local.mjs</files>
  <read_first>
    - `scripts/verify-phase-05-1-containment-local.mjs`: isolated-root, manifest, redaction, teardown, and no-network pattern.
    - `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-02-CONTAINMENT-APPROVAL.md`: exact cached image tags/IDs and pinned CLI contract.
    - `supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql`: assertion suite to execute.
  </read_first>
  <action>
    Create a fail-closed repository-root runner using literal `web/node_modules/.bin/supabase` with `SUPABASE_TELEMETRY_DISABLED=1`. Before the first Supabase command, require these exact already-cached images and IDs: `public.ecr.aws/supabase/postgres:17.6.1.143` at `sha256:80d7b27c3e8d77cfa7226eee9508671796da214781ff15a35b3670d7ad5ee453`, and `public.ecr.aws/supabase/pg_prove:3.36` at `sha256:eda7c5e68719e9c8287e78c017118407b48df904a51c935f5ab6098b8c0bc6bc`. Fail if either is absent or differs; never pull, download, install, or use the network.

    Build a validated `mktemp -d` isolated root with exact ordered migrations 0001–0061, 0063, and 0064 plus the pgTAP test; migration 0062 and every later/unlisted migration must be absent. Run pinned CLI `start`, `db reset --local --no-seed`, `test db {absolute-test} --local`, and `stop --no-backup` with explicit `--workdir`. Detect any pull/download/install output, linked mode, hosted credential/environment use, teardown failure, or manifest drift and fail rather than fall back. Emit only sanitized local hashes, assertion-group counts, command digests, image/CLI identities, and teardown status.
  </action>
  <acceptance_criteria>
    - Real PostgreSQL proves trigger, queue, batch-stage, retry, tombstone, and publication behavior.
    - The exact local migration view contains 0001–0061 + 0063 + 0064 and excludes 0062.
    - No Docker image pull, package install, network fallback, hosted read, provider call, credential, or production mutation occurs.
    - Migration 0064 remains local and unapproved for deployment.
  </acceptance_criteria>
  <verify>
    <automated>SUPABASE_TELEMETRY_DISABLED=1 node scripts/verify-phase-05-1-delta-local.mjs &amp;&amp; git diff --check -- supabase/migrations/0064_delta_deterministic_ranking_and_batch_staging.sql supabase/tests/0064_delta_deterministic_ranking_and_batch_staging.test.sql web/tests/migration-0064-delta-ranking-and-batch-staging.test.ts supabase/functions/_shared/deterministic-worker.ts web/tests/deterministic-ranking-worker-runtime.test.ts web/tests/deterministic-ranking-source.test.ts scripts/verify-phase-05-1-delta-local.mjs</automated>
  </verify>
  <done>Migration 0064 and the worker pass focused static/runtime tests plus disposable real-PostgreSQL verification with exact cached images and zero external effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| jobs writes → ranking change queue | Untrusted or noisy job changes must not create unbounded/duplicate work. |
| worker JSON → security-definer batch RPC | Service payloads must be bounded, owned, revision-safe, and atomic. |
| dismissal tombstone → delta publication | Permanently dismissed provider identities must never reappear. |
| local harness → Docker/Supabase CLI | Verification must not download, link, or touch hosted state. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05.1-05-01 | Tampering | batch staging RPC | high | mitigate | Exact-key JSON validation, 1–25 bound, unique IDs, claim/revision checks, service-role-only grants, and all-or-none SQL update. |
| T-05.1-05-02 | Denial of Service | change queue / worker | high | mitigate | Deduplicating `(user_id,job_id)` key, 25-row enqueue/claim batches, 250-item invocation cap, and no catalog scan on empty ticks. |
| T-05.1-05-03 | Elevation of Privilege | security-definer functions | high | mitigate | Empty search path, explicit qualification, revokes from public/anon/authenticated, service-role grants only. |
| T-05.1-05-04 | Tampering | dismissed jobs | high | mitigate | Recheck exact owner/source/external_id tombstones during enqueue and publication; pgTAP proves no resurrection. |
| T-05.1-05-05 | Information Disclosure | verifier output | medium | mitigate | Local-only execution and aggregate/hash-only sanitized evidence; reject hosted credentials. |
| T-05.1-05-SC | Tampering | Tool/dependency supply chain | low | accept | No install/update; exact repository CLI and pre-existing cached image IDs are verified before execution. |
</threat_model>

<verification>
Run all task commands from the repository root. The full gate is focused Vitest suites, the cached-image local pgTAP runner, `npm --prefix web run build`, and byte verification that Plan 03 and migration 0063 remain unchanged.
</verification>

<success_criteria>
- Genuine inserted/changed jobs are the only routine ranking work; empty ticks create none.
- Work is bounded to 25 per enqueue/stage batch and 250 per worker invocation.
- Batch publication is atomic and 0063 tombstone containment remains effective.
- All verification is local with no deployment or approval claim.
</success_criteria>

<output>
Create `.planning/phases/05.1-ranking-and-dashboard-performance-stabilization/05.1-05-SUMMARY.md` when done. Record exact artifact blobs/hashes, test commands/results, cached image and CLI identities, 0062 exclusion, 0063 preservation, `deployment_status: not_deployed`, and `hosted_reads: 0`. Do not claim release approval, deployment, 24-hour acceptance, retention, or cleanup.
</output>
