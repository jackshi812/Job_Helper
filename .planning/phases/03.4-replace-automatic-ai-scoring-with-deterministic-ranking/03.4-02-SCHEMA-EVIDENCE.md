# Phase 03.4 Plan 02 Schema Evidence

evidence_mode: approved_additive_schema_push
project_ref: fjcsvajkkztvlrpdplwx
migration_file: supabase/migrations/0032_deterministic_ranking.sql
approved_local_sha256: 4441a5f20657f421628aa82a3d18ade0d40bc9d7f0566efcfa6bd9277d48ef21
remote_migration_version: "0032"
remote_migration_name: deterministic_ranking
remote_statement_count: 72
remote_statements_sha256: c4600b063effbe44efcdf414b7775e2a1e08bf7de15f80f1849f6290c2a93302
push_command: supabase db push
push_status: succeeded
captured_at: 2026-07-23T05:49:41Z
function_rollout_captured_at: 2026-07-23T05:58:10Z

## Approval and inventory

The owner approved only `supabase db push` for the exact local migration SHA-256
above. Immediately before the push, the linked project identity was
`fjcsvajkkztvlrpdplwx`, migrations `0001` through `0031` matched locally and
remotely, and `0032` was the sole pending migration. The CLI proposed only
`0032_deterministic_ranking.sql` and reported that the migration finished.

The CLI could not refresh its optional local Docker migration catalog because
Docker Desktop was not running. This did not affect the hosted transaction.
Independent post-push migration inventory reports `0001` through `0032` in both
the local and remote columns. A read-only hosted catalog query records the
remote migration as version `0032`, name `deterministic_ranking`, 72 stored
statements, and the hosted statement-array SHA-256 above. The approved source
file itself remains byte-identical to the approval SHA-256.

## Hosted schema parity

Read-only catalog inspection confirmed:

- Tables `deterministic_ranking_state`, `deterministic_ranking_runs`, and
  `deterministic_ranking_items` exist with RLS enabled.
- Only authenticated own-row SELECT policies exist on state and runs; items
  expose no authenticated table privilege or policy.
- Authenticated table privileges on the new tables are exactly SELECT on state
  and runs.
- Authenticated UPDATE columns remain bounded to existing preference inputs
  (`titles`, `locations`, include/exclude/title-exclude keywords,
  `max_required_experience`, `updated_at`) and `user_jobs.seen_at` /
  `user_jobs.dismissed_at`. No deterministic result, revision, score, tier,
  breakdown, filter, or route column is authenticated-writable.
- All 15 deterministic validator/auth/service routines exist with an empty
  search path. Every privileged routine is SECURITY DEFINER.
- `save_preferences_and_start_ranking`,
  `get_deterministic_ranking_state`,
  `retry_deterministic_ranking_run`, and
  `request_deterministic_route_refresh` grant authenticated execution.
- Claim, stage, finalize, new-job enqueue, recency enqueue, route enqueue,
  service-user route refresh, validators, and
  `initialize_deterministic_ranking_backfill(integer)` grant execution only to
  `postgres` and `service_role`; anon and authenticated cannot execute them.
- `initialize_deterministic_ranking_backfill(integer)` is owned by `postgres`,
  is SECURITY DEFINER, and has `search_path=""`.
- Deterministic columns, closed-result constraints, foreign keys, unique
  initial/retry/revision indexes, claim indexes, and the partial feed index are
  present.

Immediately after the push, all three new deterministic tables contained zero
rows (`state=0`, `runs=0`, `items=0`). No initializer, worker function, or RPC
was invoked, so initial backfill remains unstarted.

## Production containment at schema push

The deployed `score-tick` identity remained unchanged before and after the
schema push:

- function ID: `ae6c147f-c3a8-417e-8057-d4105ac9aed5`
- version: `10`
- status: `ACTIVE`
- `verify_jwt`: `false`
- hosted bundle SHA-256:
  `40d6e811359dede2594d66838aeef88b31179dd7934b3d6835d46ccb9a253351`

This is still the Plan 01 contained no-op deployment. The local deterministic
worker was not deployed or invoked during the schema stage.

## Approved Wave 2 function rollout

The owner subsequently approved exactly two deployments, in order, with
`supabase functions deploy --no-verify-jwt` and no manual invocation:

1. `extract-resume`
   - approved index SHA-256:
     `e8c437b73592b8305f89260d6c64fa6e8c20aa9e707706a70dde650eca378987`
   - approved three-file bundle-manifest SHA-256:
     `36d7f4bd42094ad1a0039cee05961b821363845a1be5e6133d82fa16b3a5214c`
   - stable function ID: `9358db1a-95fc-49bc-a684-b98fb8eceff9`
   - hosted identity advanced from version 3 to version 4
   - post-deploy status: `ACTIVE`
   - post-deploy `verify_jwt`: `false`
   - hosted bundle SHA-256:
     `69b57e1380a09e2a400ec3e15264052c209757f672968cc7c9660987790dcda8`
2. `score-tick`
   - approved index SHA-256:
     `68892a709d9a2c23e42265852c1116896b25a52a2a52f1140b21ad23ae316b79`
   - approved four-file bundle-manifest SHA-256:
     `22ad796f49374c21990eb559f7531683fdfd13924afa81ccb634d84e1727782a`
   - stable function ID: `ae6c147f-c3a8-417e-8057-d4105ac9aed5`
   - hosted identity advanced from version 10 to version 11
   - post-deploy status: `ACTIVE`
   - post-deploy `verify_jwt`: `false`
   - hosted bundle SHA-256:
     `e0eb61a9fd4e6afc2904f6fb7145a51034daefc06934bec36968190210752ed0`

Immediately before deployment, migrations `0001` through `0032` matched
locally and remotely, migration `0032` still recorded 72 hosted statements,
and the deterministic state/run/item/initial-run counts were all zero. The
same read-only checks passed after both deployments.

The existing `extract-resume-every-minute` and `score-tick-every-minute`
schedules remained active at `* * * * *`. Their command MD5 identities stayed
`f792df8d50f7d6c268f1418e41ff210b` and
`f90afe213d26135169ca316d8a8916c2`, respectively. No schedule or secret was
changed.

## Mutation ledger and exclusions

The schema stage applied only migration `0032`. The later approved Wave 2
rollout replaced only the `extract-resume` and `score-tick` Edge bundles. No
function or RPC was manually invoked. No backfill initializer, preference save,
manual user-row rewrite, scheduler change, secret change, or web deployment
occurred.

No rollback was needed: both stable identities are active with the approved
JWT setting, migration parity holds, schedules are invariant, and the
deterministic tables remain empty. No destructive down migration, manual SQL
repair, or legacy-column cleanup was authorized.
