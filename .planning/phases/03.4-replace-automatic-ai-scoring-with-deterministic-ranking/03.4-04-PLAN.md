---
phase: 03.4-replace-automatic-ai-scoring-with-deterministic-ranking
plan: 04
type: execute
wave: 4
depends_on: [03.4-02, 03.4-03]
files_modified:
  - scripts/verify-phase-03-4-release.mjs
  - scripts/verify-phase-03-4-release.test.mjs
  - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-PREFLIGHT.md
  - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-ROLLOUT-EVIDENCE.md
  - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [PREF-01, SCOR-01, SCOR-02, SCOR-03, SCOR-04, SCOR-05]
must_haves:
  truths:
    - "D-16: Every real user has one complete deterministic active revision covering all current open jobs before the web cutover."
    - "D-17/D-21/D-22: The exact released web reads only deterministic fields while preserving all Dashboard/detail behavior."
    - "D-19/D-20/D-23: Deterministic integration probes prove building/failure/new-job/recency transitions; production signed-in UAT is read-only and sees only one complete converged revision."
    - "D-24: Worker deployment, service-only initial backfill, and read-only signed-in convergence leave score-purpose usage and paid reservations exactly invariant."
    - "No schema, worker, backfill, Git, or web rollout occurs without exact-SHA owner approval; no mixed release is ever published."
  artifacts:
    - path: "scripts/verify-phase-03-4-release.mjs"
      provides: "Fail-closed exact-SHA, migration, worker, coverage, no-AI, web asset, and UAT verifier"
    - path: ".planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-ROLLOUT-EVIDENCE.md"
      provides: "Sanitized schema/function/backfill/Git/Cloudflare/cost/UAT evidence"
    - path: ".planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UAT.md"
      provides: "Signed-in deterministic ranking acceptance"
  key_links:
    - from: "deployed score-tick"
      to: "complete active deterministic revisions"
      via: "approved verifier calls service-only initialize_deterministic_ranking_backfill(25), then ordinary worker claim/stage/finalize drains every captured run behind the fail-closed complete-universe gate"
      pattern: "initialize_deterministic_ranking_backfill|claim_deterministic_ranking_work|stage_deterministic_ranking_result|finalize_deterministic_ranking_run|active_revision"
    - from: "complete backfill"
      to: "Cloudflare production"
      via: "web deployment occurs only after no-mix proof and exact SHA approval"
      pattern: "commit|deployment|sha256"
---

<objective>
Backfill all users, prove no mixed or paid automatic scoring, and release the exact deterministic worker/web build with signed-in UAT.

Purpose: Make the irreversible user-visible cutover only after complete data, exact identities, atomic behavior, and zero-cost invariance are proven.
Output: Deterministic production release, immutable evidence, UAT, and remapped requirement documentation.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-CONTEXT.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UI-SPEC.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-01-SUMMARY.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-02-SUMMARY.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-03-SUMMARY.md
@scripts/verify-phase-03-3-release.mjs

<interfaces>
- Migration 0032 must already be remotely checksum-identical from Plan 02; rerun `supabase db push` only as a blocking parity check and abort if it proposes anything.
- Deploy score-tick with `--no-verify-jwt`; its handler-level cron secret remains authoritative.
- Web release requires exact local HEAD = origin/main = successful main-branch Cloudflare production metadata and matching immutable JS SHA-256.
- Initial production runs start only through `node scripts/verify-phase-03-4-release.mjs --initialize-backfill ...`, which calls `initialize_deterministic_ranking_backfill(25)` with service credentials after exact-SHA approval. It never emits manual SQL and cannot be invoked by an authenticated end user.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble a fail-closed release preflight</name>
  <read_first>
    - scripts/verify-phase-03-3-release.mjs
    - scripts/verify-phase-03-3-release.test.mjs
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-02-SCHEMA-EVIDENCE.md
    - supabase/functions/score-tick/index.ts
  </read_first>
  <files>scripts/verify-phase-03-4-release.mjs, scripts/verify-phase-03-4-release.test.mjs, .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-PREFLIGHT.md</files>
  <behavior>
    - Missing/malformed/stale/mismatched migration, function, owner coverage, cost, Git, Cloudflare, asset, or UAT evidence exits nonzero.
    - Any visible eligible row with missing deterministic score/tier/breakdown or multiple active revisions exits nonzero.
    - Any score-purpose usage or reservation delta across worker/backfill/save/retry/maintenance exits nonzero.
    - Initializer tests fail unless the RPC is postgres-owned, service-role-only, bounded to 25 owners, idempotent, and wired to the ordinary claim/stage/finalize queue.
    - Command/API/auth failures never default to pass.
  </behavior>
  <action>Create the standard-library release verifier and negative tests. Preflight the full focused suite, complete tests, build, lint, diff, migration 0032 checksum/parity, contained worker identity, intended deterministic worker/web hashes, current real-user/open-job universe counts, active revisions, score-purpose usage, reservations, and unrelated dirty files. Add a fail-closed `--initialize-backfill` mode that is disabled unless it receives the exact approved SHA/inventory evidence, uses the service credential to call only `initialize_deterministic_ranking_backfill(25)`, validates bounded count-only responses, loops until `remaining_users = 0`, and then drives only the normal cron-authenticated worker claim/stage/finalize path; it must never execute supplied/manual SQL or expose a browser/authenticated path. Negative tests cover missing approval, wrong SHA, over-25 request/response, duplicate owner runs, partial initialization, RPC/grant drift, and command/API/auth failure. Record sanitized values and exact authorized commands in `03.4-04-PREFLIGHT.md`; do not record secrets, preferences, job text, resumes, or a pre-commit release SHA. Include source assertions for every D-24 automatic entry point and prove `extract-resume` remains extraction-only. Run controlled failure, pending-new-job, recency-expiry, Retry, and atomic-save transition cases in the deterministic snapshot/integration harness with disposable fixtures and asserted restoration. Perform no production mutation.</action>
  <acceptance_criteria>
    - Preflight is complete, sanitized, committed, and shows zero outward mutations.
    - Verifier rejects partial/mixed coverage, source capability drift, cost deltas, and identity/hash drift.
    - Integration evidence—not production mutation—covers controlled failure, pending-new-job admission, recency expiry, retry, and atomic preference transitions.
    - Full tests/build/lint pass and unrelated user files are excluded.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/verify-phase-03-4-release.test.mjs &amp;&amp; cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <done>A reproducible exact release candidate and zero-mutation preflight are ready for approval.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: [BLOCKING] Approve deterministic worker, complete backfill, and exact web cutover</name>
  <read_first>
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-PREFLIGHT.md
    - scripts/verify-phase-03-4-release.mjs
    - supabase/functions/score-tick/index.ts
  </read_first>
  <files>None</files>
  <decision>Whether to deploy the exact deterministic score-tick, build complete snapshots for every real user, enforce the no-mix gate, push the exact release SHA, deploy its web asset, and perform signed-in UAT.</decision>
  <context>Approval includes only exact worker deployment, bounded service-only `initialize_deterministic_ranking_backfill(25)` calls through the verifier, normal claim/stage/finalize draining, exact web cutover, and read-only signed-in production observation. It excludes further schema change, reset/manual SQL, legacy cleanup, scheduler/secret/provider changes, production fixtures/failure injection/preference edits/new-job creation/time manipulation, direct AI calls, watchlist-only UI, manual AI scoring, unrelated files, partial web release, and any cost-ledger delta.</context>
  <action>Present live post-Task-1 SHA, hashes, schema parity, contained/deploy worker identities, the exact verifier initializer command, its 25-owner bound/service-role grant/idempotency proof, user/job counts, backfill/no-mix queries, zero-cost invariance baseline, web identity checks, read-only production UAT steps, and exclusions. Require literal approval bound to the SHA and inventory; mutate nothing in this checkpoint.</action>
  <options>
    <option id="approve-release">
      <name>Approve exact release</name>
      <pros>Completes deterministic ranking with atomic production evidence.</pros>
      <cons>Production user ranking rows are fully backfilled and the web permanently cuts over from legacy AI fields.</cons>
    </option>
    <option id="decline-release">
      <name>Decline release</name>
      <pros>Production remains contained on the old visible feed.</pros>
      <cons>Phase 03.4 cannot complete.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Approval is explicit, SHA-bound, complete, and includes zero tolerance for mixed results or paid-score deltas.
    - No outward mutation occurs in the checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>git diff --check &amp;&amp; test -s .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-PREFLIGHT.md</automated>
    <human-check>Approve or decline the exact worker/backfill/no-mix/web/UAT scope.</human-check>
  </verify>
  <resume-signal>Type `approve deterministic release` or `decline`.</resume-signal>
  <done>The exact rollout is approved or production remains contained and unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy, backfill, cut over, and complete UAT</name>
  <read_first>
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-PREFLIGHT.md
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UI-SPEC.md
    - scripts/verify-phase-03-4-release.mjs
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-ROLLOUT-EVIDENCE.md, .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UAT.md, .planning/REQUIREMENTS.md</files>
  <action>Abort unless Task 2 approved the unchanged live SHA and all preflight identities/counters still match. Re-run full gates, including deterministic integration proofs for atomic save/build/failure/Retry, pending-new-job admission, and recency expiry; run `supabase db push` as a blocking no-op parity check and stop if any schema change is proposed. Deploy only deterministic score-tick and prove cron-secret behavior. Invoke the exact approved verifier `--initialize-backfill` mode: it alone calls service-only `initialize_deterministic_ranking_backfill(25)` in bounded batches, then normal worker invocations claim, evaluate, stage, and finalize those runs. Never execute manual SQL or accept an end-user-supplied owner. Wait until the initializer reports no remaining real owners, every current open job is terminal, zero-job owners have a complete revision, and each user has one complete active revision; fail on missing/duplicate/partial/mixed rows. Prove score-purpose usage and reservations unchanged before pushing the approved SHA to origin/main. Wait for its successful Cloudflare production deployment and verify immutable asset hash. Production signed-in UAT is read-only: for each real account, observe that ranking state has converged, the Dashboard feed matches that account's single complete active revision, no legacy/partial row appears, stored score/tier rows render, and local-only company/tier/sort/resize controls remain usable without invoking preference save, Retry, detail navigation/mark-seen, Apply, Dismiss/Restore, fixture creation, failure injection, or time manipulation. Use an authenticated read-only detail projection query to confirm stored six-row evidence belongs to the same active revision; rely on the integration/render suite for detail navigation and presentation behavior. Recheck zero cost deltas and exact identities, write sanitized evidence/UAT with integration transition evidence clearly separated from production observation, and update PREF-01/SCOR-01..05 wording/traceability to the implemented Phase 03.4 meaning without inventing IDs.</action>
  <acceptance_criteria>
    - Worker deployment precedes complete backfill; complete backfill precedes web deployment.
    - No real user sees mixed AI/deterministic or partial-rubric results.
    - Exact SHA/function/migration/Cloudflare/asset identities and zero paid-score deltas pass.
    - Integration atomic update/failure/retry/new-job/recency probes pass; production signed-in UAT remains read-only and verifies real-user convergence, deterministic-only rendering, preserved local-only UI behavior, and cost invariance.
    - Historical AI fields remain physical but are absent from active queries/UI; deferred features remain absent.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; cd .. &amp;&amp; node --test scripts/verify-phase-03-4-release.test.mjs &amp;&amp; node scripts/verify-phase-03-4-release.mjs --post-release .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-ROLLOUT-EVIDENCE.md</automated>
    <human-check>On the immutable release URL, make only safely read-only production observations: confirm the Preferences rubric display, the fully converged deterministic Dashboard/table, session-only company/tier/sort/resize controls that do not persist or mutate server state, and the authenticated read-only deterministic job-detail projection. Do not save preferences, invoke Retry, inject failure, create or use fixtures, or manipulate time. Do not trigger atomic Updating/failure/Retry behavior in production; it is proven by the named deterministic integration/render tests `web/tests/deterministic-ranking-snapshot.integration.test.ts`, `web/tests/preference-refilter-feed.integration.test.ts`, and `web/src/pages/Dashboard.test.tsx`, with their captured evidence in `03.4-04-ROLLOUT-EVIDENCE.md` and `03.4-UAT.md`.</human-check>
  </verify>
  <done>Production serves one complete deterministic ranking system with exact release/UAT evidence and no automatic AI scoring cost.</done>
</task>

</tasks>

<threat_model>
## ASVS Level

OWASP ASVS Level 1. Any HIGH or CRITICAL finding blocks rollout or acceptance.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Local CLI/Git → Supabase/Cloudflare production | Approved credentials deploy worker/data/web state. |
| Service worker → all users | Backfill writes protected per-user projections. |
| Signed-in browser → feed/detail | Production UAT reads only the owner's converged deterministic projection under RLS; mutating transition probes run in the deterministic integration harness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.4-16 | Tampering | backfill/cutover | critical | mitigate | Require complete-universe per-user revisions, no-mix query, worker-before-backfill-before-web order, and fail-closed verifier. |
| T-03.4-17 | Spoofing / Repudiation | Git/Cloudflare/function identity | high | mitigate | Bind explicit approval to exact SHA and require origin/main, production metadata, function identity, and immutable asset hash equality. |
| T-03.4-18 | Financial abuse | automatic paths/rollout | critical | mitigate | Snapshot usage/reservations and require exact invariance across approved worker/initializer/backfill operations; exercise save/retry/maintenance only in deterministic integration tests. |
| T-03.4-19 | Information disclosure | evidence/UAT | high | mitigate | Store hashes/counts/statuses only; exclude credentials, preferences, job descriptions, resumes, and user identifiers. |
| T-03.4-20 | Elevation of privilege | signed-in UAT/RLS | high | mitigate | Use own-user sessions for read-only convergence observation, preserve `auth.uid()`/RLS, and prove rejected cross-user writes in integration tests. |
| T-03.4-21 | Elevation of privilege / Denial of service | initial backfill | critical | mitigate | Invoke only the postgres-owned, service-role-only initializer through the SHA-bound verifier, cap batches at 25 owners, require idempotent uniqueness, and route all items through claim/stage/finalize. |
| T-03.4-SC | Tampering | supply chain | high | mitigate | Install no package and deploy only lockfile-backed reviewed artifacts. |
</threat_model>

## Artifacts this phase produces

- `scripts/verify-phase-03-4-release.mjs` and `.test.mjs`.
- `03.4-04-PREFLIGHT.md`, `03.4-04-ROLLOUT-EVIDENCE.md`, and `03.4-UAT.md`.
- Exact hosted deterministic score-tick identity, bounded service-only initializer evidence, complete active revision per real user, exact origin/main and Cloudflare deployment identity, immutable asset SHA-256, and no-AI cost-invariance ledger.
- Remapped existing PREF-01 and SCOR-01..05 text/traceability; no new requirement ID.

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Replace background AI scoring with transparent deterministic ranking while preserving unrelated behavior | 01-04 | COVERED |
| REQ | PREF-01 | Preferences plus editable rubric/thresholds/maximum experience | 02-04 | COVERED |
| REQ | SCOR-01 | Hard filters before deterministic publication | 01-04 | COVERED |
| REQ | SCOR-02 | Reproducible deterministic score; manual AI deferred | 01-04 | COVERED |
| REQ | SCOR-03 | Deterministic category/evidence breakdown | 01,03,04 | COVERED |
| REQ | SCOR-04 | Preserved Dashboard on deterministic fields | 02-04 | COVERED |
| REQ | SCOR-05 | Full JD plus deterministic rubric table | 03-04 | COVERED |
| RESEARCH | — | Containment before UTC reset and no-AI cost invariance | 01,04 | COVERED |
| RESEARCH | — | Pure evaluator, US/experience/title/keyword/watchlist/recency rules | 01 | COVERED |
| RESEARCH | — | Service-only initial backfill, staged runs, atomic promotion, retry, concurrent new jobs, recency expiry | 02,04 | COVERED |
| RESEARCH | — | Deterministic-only UI and complete backfill before exact web cutover | 03,04 | COVERED |
| CONTEXT | D-01..D-08 | Complete additive 100-point rubric | 01 | COVERED |
| CONTEXT | D-09 | Editable validated rubric | 02-03 | COVERED |
| CONTEXT | D-10..D-13 | Hard-filter/soft-signal boundaries | 01-02 | COVERED |
| CONTEXT | D-14..D-15 | Editable stored tiers | 01-03 | COVERED |
| CONTEXT | D-16..D-18 | Complete migration, retired AI display, deterministic evidence | 02-04 | COVERED |
| CONTEXT | D-19..D-23 | Atomic update/new-job/UI/failure behavior | 02-04 | COVERED |
| CONTEXT | D-24 | No automatic AI scoring capability/cost | 01-04 | COVERED |
| CONTEXT | D-25 | Future visible manual AI actions deferred | 01,03,04 | COVERED |
| CONTEXT deferred | — | Watchlist-only Dashboard filter | — | EXCLUDED |
</source_audit>

<verification>
Full tests/build/lint, exact schema/function/Git/Cloudflare/asset verification, bounded service-only initializer proof, complete per-user backfill/no-mix queries, zero paid-score deltas, deterministic integration transition probes, and read-only signed-in production convergence UAT all pass.
</verification>

<success_criteria>
- Every locked decision and existing remapped requirement has implementation and evidence.
- Production cannot automatically spend on job scoring.
- Users see one complete deterministic rubric revision at a time.
- Exact-SHA release and UAT pass with auth/RLS, ingestion, company controls, table/detail, Apply/Dismiss/Restore, resume management, and Phase 4 scope preserved.
</success_criteria>

<output>
Create `.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-SUMMARY.md` when done.
</output>
