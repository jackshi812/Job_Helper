---
phase: 03.4-replace-automatic-ai-scoring-with-deterministic-ranking
verified: 2026-07-23T18:14:38Z
status: gaps_found
score: 5/13 must-haves verified
behavior_unverified: 1
overrides_applied: 0
gaps:
  - truth: "Owner-approved title and explicit-foreign-location hard filters admit only eligible jobs."
    status: failed
    reason: "The synonym graph lets one shared token bridge through a multi-word acronym, and the foreign-country list is incomplete."
    artifacts:
      - path: "supabase/functions/_shared/filters.ts"
        issue: "Data Engineer weak-matches configured Data Scientist through the bidirectional ds token bridge."
      - path: "supabase/functions/_shared/deterministic-ranking.ts"
        issue: "Explicit countries including Czech Republic, Romania, and Nigeria classify as unknown instead of outside_us."
    missing:
      - "Phrase-aware directional acronym handling with false-match regressions."
      - "Reviewed exhaustive foreign-country names/aliases with table-driven classifier coverage."
  - truth: "Worker claims always reach a terminal state and each invocation is actually bounded below the scheduler cadence."
    status: failed
    reason: "A third pre-stage death leaves attempts=3 claimed work unreapable, and awaited database/RPC operations have no deadline or abort signal."
    artifacts:
      - path: "supabase/migrations/0032_deterministic_ranking.sql"
        issue: "Claim eligibility requires attempts < 3 but no expired terminal-attempt lease reaper exists."
      - path: "supabase/functions/score-tick/index.ts"
        issue: "The 45-second check runs only between awaited operations; a stalled load, stage, or finalize can exceed it."
    missing:
      - "Transactional expired-lease reaper that fails terminal-attempt items and their run/state."
      - "Deadline-scoped cancellation for every awaited PostgREST/RPC operation."
      - "Runtime-backed tests for terminal lease exhaustion and never-resolving I/O."
  - truth: "Superseded preference runs leave no permanent nonterminal work and preserve the previous complete snapshot until the new run publishes."
    status: failed
    reason: "Preference save marks the old run stale but leaves its pending/claimed items nonterminal and permanently unclaimable."
    artifacts:
      - path: "supabase/migrations/0032_deterministic_ranking.sql"
        issue: "save_preferences_and_start_ranking updates only deterministic_ranking_runs at lines 592-596."
    missing:
      - "Terminal stale/cancelled handling for every nonterminal item of a superseded run."
      - "Concurrent save-during-claim and repeated-save tests against the real SQL protocol."
  - truth: "Resume upload, deletion, and extraction preserve truthful success reporting and cannot lose deterministic route refresh requests."
    status: failed
    reason: "A generic finalizer can erase a newer route refresh, and browser helpers reject after the durable upload/delete already succeeded when only refresh signaling fails."
    artifacts:
      - path: "supabase/migrations/0032_deterministic_ranking.sql"
        issue: "finalize_deterministic_ranking_run unconditionally clears route_refresh_requested_at."
      - path: "web/src/lib/resumes.ts"
        issue: "Upload/delete throw after committed primary mutations when request_deterministic_route_refresh fails."
    missing:
      - "Consume route-refresh requests only on enqueue or use compare-and-set acknowledgment."
      - "Transactional outbox/trigger or truthful separately retryable post-commit refresh handling."
      - "Request-during-ranking and post-commit refresh-failure regressions."
  - truth: "Release verification proves the hosted score-tick bytes equal the approved local source and bundle."
    status: failed
    reason: "The live function probe copies local hashes into the hosted result instead of independently hashing deployed bytes."
    artifacts:
      - path: "scripts/verify-phase-03-4-release.mjs"
        issue: "functionProbe assigns local.indexSha256 and local.bundleManifestSha256 to hosted metadata."
    missing:
      - "Retrieve/hash the exact deployed function version independently, or fail closed when hosted bytes cannot be verified."
  - truth: "All current eligible open jobs remain available through the Dashboard up to the intended 200-row display cap."
    status: failed
    reason: "listFeed limits user_jobs to 200 before filtering closed jobs in JavaScript, allowing newer closed rows to hide valid open matches."
    artifacts:
      - path: "web/src/lib/feed.ts"
        issue: "The query has no jobs.status=open predicate before limit(200)."
    missing:
      - "Filter through an inner jobs relation before ordering/limiting, or paginate until 200 valid rows are collected."
      - "Regression with more than 200 newer closed rows followed by eligible open rows."
  - truth: "Preferences provides exact accessible validation for every persisted bounded text-array input."
    status: partial
    reason: "Browser validation mirrors count/byte bounds only for title exclusions; other preference arrays can reach the RPC with invalid shapes and no field-specific focus/error."
    artifacts:
      - path: "web/src/lib/preferences.ts"
        issue: "validateTitleExclusions is not generalized to titles, locations, include keywords, and general excludes."
      - path: "web/tests/deterministic-ranking-snapshot.integration.test.ts"
        issue: "The named integration test is an in-memory model and does not exercise SQL concurrency, leases, route timestamps, or stale items."
      - path: "web/tests/deterministic-ranking-worker-liveness.test.ts"
        issue: "The liveness test is a reduced model and does not execute worker I/O or database RPCs."
    missing:
      - "Shared client validator matching the server text-array contract, with accessible field-specific focus and errors."
      - "Disposable Postgres/Supabase integration coverage for SQL and worker concurrency/liveness invariants."
behavior_unverified_items:
  - truth: "The exact immutable production release visually conforms to the approved responsive Preferences, Dashboard, and Job Detail UI contract."
    test: "Open the immutable release in a signed-in browser at desktop and narrow widths; inspect the rubric form, update/failure notices, controls, table scrolling/resizing, and six-row detail breakdown without saving or mutating data."
    expected: "The approved UI-SPEC layout, copy, focus treatment, responsive behavior, and complete deterministic presentation are visible with no legacy AI UI."
    why_human: "The in-app browser was unavailable during release acceptance; exact asset identity and render tests cannot establish visual appearance or interaction feel."
---

# Phase 03.4: Replace Automatic AI Scoring with Deterministic Ranking Verification Report

**Phase Goal:** Remove automatic/background AI job scoring and replace it with transparent deterministic ranking while preserving ingestion, preferences, company controls, dashboard structure, job detail, apply links, dismissal, and every unrelated feature.
**Verified:** 2026-07-23T18:14:38Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

The production cutover and data convergence are real, but the phase goal is not
yet achieved. The implementation contains observable eligibility, queue
liveness, routing, feed completeness, timeout, and release-integrity defects.
These are not deferred to Phase 4 and no verification overrides exist.

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | No scheduled/background job-ranking path can call AI or reserve/account paid score work. | ✓ VERIFIED | `score-tick` has no provider/budget capability; the named source test passed independently; extraction remains separately scoped. |
| 2 | Owner-approved title and explicit-foreign-location hard filters admit only eligible jobs. | ✗ FAILED | Direct execution returned a weak `Data Scientist` match for `Data Engineer`; Czech Republic, Romania, and Nigeria returned `unknown`. |
| 3 | Identical captured inputs/time produce a reproducible score, stored tier, and six-row evidence. | ✓ VERIFIED | Pure evaluator wiring is substantive; the single byte-reproducibility test passed. |
| 4 | Building/failure/retry/supersession/lease paths preserve one complete snapshot and always terminate. | ✗ FAILED | Superseded items remain nonterminal; a third pre-stage death is unreapable. |
| 5 | Current and newly ingested eligible jobs remain available in Dashboard. | ✗ FAILED | Closed rows can consume the 200-row query limit before browser filtering. |
| 6 | Preferences exposes and accessibly validates the complete bounded ranking form. | ✗ FAILED | Rubric validation exists, but most text arrays lack matching client bounds/focus errors. |
| 7 | Active list/detail projections use stored deterministic score/tier/evidence and exclude legacy AI presentation. | ✓ VERIFIED | Deterministic-only projections are wired; the named six-row detail render test passed. |
| 8 | Existing Dashboard controls, detail sanitization, HTTPS Apply, and dismissal behavior remain wired. | ✓ VERIFIED | Actual components retain these paths and focused interaction/render tests pass. |
| 9 | Resume management/extraction and Best-fit routing remain truthfully preserved. | ✗ FAILED | A newer route request can be erased, and committed upload/delete can be reported as failed. |
| 10 | Every real owner has one complete current-open active revision before web cutover. | ✓ VERIFIED | Sanitized read-only evidence records 2/2 complete owners and zero missing/mixed/nonterminal current-open results. |
| 11 | Exact release verification proves hosted worker bytes equal approved bytes. | ✗ FAILED | Hosted function metadata is real, but its recorded hashes are copied from the local inventory. |
| 12 | Worker recovery/convergence is bounded below the one-minute scheduler cadence. | ✗ FAILED | Deadline checks do not bound awaited database/RPC I/O. |
| 13 | Exact production UI visually conforms to the approved responsive contract. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Browser visual UAT was unavailable; tests and immutable bytes are necessary but not visual proof. |

**Score:** 5/13 truths verified (1 present, behavior-unverified)

## Required Artifacts

`verify.artifacts` reported 17/17 declared artifacts present and structurally
substantive. The failures are semantic rather than missing-file/stub failures.

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `supabase/functions/_shared/deterministic-ranking.ts` | Pure owner-approved evaluator | ✗ DEFECTIVE | Reproducible, but foreign-country coverage is incomplete. |
| `supabase/functions/_shared/filters.ts` | Shared title/exclusion concepts | ✗ DEFECTIVE | Bidirectional acronym token bridge creates false title matches. |
| `supabase/migrations/0032_deterministic_ranking.sql` | Atomic protected ranking protocol | ✗ DEFECTIVE | Lease exhaustion, stale-item, and route-refresh races remain. |
| `supabase/functions/score-tick/index.ts` | Bounded deterministic worker | ✗ DEFECTIVE | Wired and deployed, but awaited I/O is not deadline-bounded. |
| `web/src/lib/preferences.ts` / `Preferences.tsx` | Complete accessible editor | ⚠️ PARTIAL | Rubric is complete; text-array preflight is incomplete. |
| `web/src/lib/feed.ts` / `Dashboard.tsx` | Complete deterministic feed | ✗ DEFECTIVE | Data flows, but limiting precedes open-job filtering. |
| `web/src/pages/JobDetail.tsx` | Six-row deterministic detail | ✓ VERIFIED | Stored score/tier/evidence render through the existing safe detail path. |
| `scripts/verify-phase-03-4-release.mjs` | Independent exact-release verifier | ✗ DEFECTIVE | Does not independently verify hosted function bytes. |
| Rollout evidence and UAT | Production convergence/identity record | ⚠️ PARTIAL | Data/cost/web identity evidence is useful; worker-byte and visual claims remain unproven. |

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| minute cron | `score-tick` | POST + `x-cron-secret` | ✓ WIRED | Handler rejects non-POST/unauthorized requests before service work. |
| worker | evaluator + claim/stage/finalize RPCs | captured inputs and service-role RPCs | ⚠️ PARTIAL | Main path is wired; lease exhaustion and stalled I/O can prevent terminal completion. |
| preference form | `save_preferences_and_start_ranking` | authenticated atomic RPC | ⚠️ PARTIAL | Normal save works; supersession leaves orphan nonterminal items. |
| resume mutation/extraction | route refresh | `request_deterministic_route_refresh` | ✗ BROKEN RACE | Generic finalization may clear a newer request. |
| Dashboard | ranking state + deterministic feed | independent polling/refetch | ✓ WIRED | Previous feed remains mounted in tested normal build/failure flows. |
| feed query | current open jobs | embedded jobs relation | ✗ PARTIAL | Open status is checked only after the query limit. |
| release verifier | hosted `score-tick` | hosted metadata/hash comparison | ✗ NOT VERIFIED | Hosted hashes are populated from local values. |

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Preferences | rubric/threshold/text arrays | preferences row → owner RPC | Yes | ⚠️ FLOWING WITH VALIDATION GAP |
| Dashboard | `FeedRow[]` | RLS `user_jobs` + embedded jobs/company | Yes | ✗ FLOWING BUT TRUNCATABLE |
| Job detail | deterministic breakdown | RLS detail projection | Yes | ✓ FLOWING |
| Worker | claimed ranking rows | service RPC → evaluator → stage/finalize | Yes | ✗ FLOWING WITH LIVENESS GAPS |
| Release verifier | hosted function identity | management metadata + local inventory | Partly | ✗ HASH PROVENANCE DISCONNECTED |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| No automatic paid-score capability | single named Vitest source test | 1 passed | ✓ PASS |
| Complete feed remains through normal build then swaps once | single named integration-model test | 1 passed | ✓ PASS, but model-only |
| Detail renders six accessible evidence rows | single named JobDetail test | 1 passed | ✓ PASS |
| Identical captured input is byte-reproducible | single named evaluator test | 1 passed | ✓ PASS |
| False-title and foreign-country adversarial cases | direct module invocation | `Data Engineer → Data Scientist weak`; three explicit countries → `unknown` | ✗ FAIL |

## Probe Execution

No conventional or explicitly declared `probe-*.sh` exists for this phase.
The production release verifier is not a substitute for independent hosted
worker-byte verification because its current function probe injects local
hashes into the hosted result.

## Requirements Coverage

| Requirement | Source Plans | Status | Evidence |
|---|---|---|---|
| PREF-01 | 02, 03, 04 | ✗ BLOCKED | Editable rubric exists, but supersession and array-validation contracts are incomplete. |
| SCOR-01 | 01, 02, 04 | ✗ BLOCKED | Title and explicit-foreign hard filters admit invalid jobs. |
| SCOR-02 | 01, 02, 04 | ✓ SATISFIED | Deterministic score is reproducible and automatic paid AI capability is absent. |
| SCOR-03 | 01, 02, 03, 04 | ✓ SATISFIED | Stored six-category earned/possible/evidence contract exists and renders. |
| SCOR-04 | 02, 03, 04 | ✗ BLOCKED | Feed pre-limit filtering and queue/liveness defects violate complete atomic availability. |
| SCOR-05 | 02, 03, 04 | ✓ SATISFIED | Full JD and same stored deterministic evidence are wired on detail. |

No Phase 03.4 requirement is orphaned from the plans.

## Code Review Findings Resolution

All 8 critical and 3 warning findings were investigated against source:

| Finding | Verdict | Verification evidence |
|---|---|---|
| CR-01 acronym title bridge | CONFIRMED | Direct module execution reproduces the false weak match. |
| CR-02 terminal claim death | CONFIRMED | SQL excludes `attempts = 3`; no lease reaper transitions it or the run to failed. |
| CR-03 superseded item orphaning | CONFIRMED | Save updates building run status only; claims join building runs. |
| CR-04 route refresh erased | CONFIRMED | Generic finalizer unconditionally clears the request timestamp. |
| CR-05 local hashes substituted | CONFIRMED | `functionProbe` copies both local hashes into hosted metadata. |
| CR-06 feed limit before open filter | CONFIRMED | `.limit(200)` precedes `deterministicVisible` browser filtering. |
| CR-07 incomplete foreign countries | CONFIRMED | Direct module execution returns `unknown` for three explicit foreign countries. |
| CR-08 unenforced I/O deadline | CONFIRMED | Awaited loads, stages, and finalizers have no abort/deadline wrapper. |
| WR-01 post-commit false failure | CONFIRMED | Upload/delete commit before route-refresh RPC and throw on that RPC. |
| WR-02 incomplete browser array bounds | CONFIRMED | Only title exclusions receive count/byte validation. |
| WR-03 model-only integration/liveness tests | CONFIRMED | Both suites implement standalone TypeScript models instead of SQL/worker execution. |

## Anti-Patterns Found

No unreferenced `TBD`, `FIXME`, `XXX`, `HACK`, placeholder, or coming-soon
markers were found in the reviewed phase files. The blockers are implemented
logic defects, not textual stubs.

## Human Verification Required

After the code gaps close, complete the deferred signed-in visual check on the
immutable release at desktop and narrow widths. The release UAT correctly
states that browser visual acceptance was unavailable; it must not be treated
as visual proof.

## Gaps Summary

Seven grouped remediation gaps block the phase goal:

1. Correct phrase-aware title matching and exhaustive explicit-country rejection.
2. Reap terminal expired claims and enforce deadlines around awaited worker I/O.
3. Terminalize superseded run items under concurrent preference saves.
4. Preserve route-refresh requests and report committed resume mutations truthfully.
5. Verify deployed Supabase function bytes independently.
6. Filter open/visible jobs before applying the 200-row feed limit.
7. Match client text-array validation to the server and add runtime-backed SQL/worker tests.

Phase 4 does not explicitly address any of these items, so none are deferred.

---

_Verified: 2026-07-23T18:14:38Z_
_Verifier: the agent (gsd-verifier; generic-agent workaround)_
