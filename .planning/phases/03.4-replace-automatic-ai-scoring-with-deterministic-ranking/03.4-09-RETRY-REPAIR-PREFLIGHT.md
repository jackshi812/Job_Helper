# Phase 03.4 Plan 09 Retry Repair Preflight

**Captured:** 2026-07-23T21:21:45Z
**Mode:** local-only repair validation; zero production contact or mutation
**Candidate code commit:** `b6f5ca5669eabad02fcaab12ac7403b9d4c3c643`
**Production baseline commit:** `a3145311018ff619254234e8a91eeab2c5df5741`
**Supabase target:** `fjcsvajkkztvlrpdplwx`
**Cloudflare target:** project `job-helper`, production branch `main`

This preflight contains no credentials, user identifiers, preference values,
resume content, job descriptions, or other private data. The final approval
checkpoint must bind the post-evidence Git `HEAD` in addition to the unchanged
artifact hashes and inventory below.

## Exact local repair inventory

<!-- repair-evidence:start -->
evidence_mode: local_retry_repair_preflight
candidate_code_commit: b6f5ca5669eabad02fcaab12ac7403b9d4c3c643
production_baseline_commit: a3145311018ff619254234e8a91eeab2c5df5741
supabase_project_ref: fjcsvajkkztvlrpdplwx
cloudflare_project: job-helper
cloudflare_branch: main
migration_path: supabase/migrations/0034_repair_deterministic_ranking_retry.sql
migration_0034_sha256: 3e55f0d4aa7cb69096ae25d89c02bba72e5eee25dfdd0bd96b557430c237b901
web_asset_path: /assets/index-CUZNltJr.js
web_asset_sha256: ca96217554c071f0de52d6c6e5a4ed6b71e8f030312d3b130f6cf772a1316bf0
repair_inventory_sha256: 766651da93e69685b1357fd315b17bfb825244877bda71b24d8b6a85f0560f36
protocol_scenarios: 5
protocol_assertions: 21
authenticated_retry_transition: pass
duplicate_retry_protection: pass
focused_tests: 16
full_test_files: 47
full_tests: 966
production_build: pass
lint: pass_with_preexisting_warning
production_contacts: 0
outward_mutations: 0
<!-- repair-evidence:end -->

The canonical inventory SHA-256 is computed from these newline-delimited
fields, in this order:

```text
migration=supabase/migrations/0034_repair_deterministic_ranking_retry.sql
migration_sha256=3e55f0d4aa7cb69096ae25d89c02bba72e5eee25dfdd0bd96b557430c237b901
web_asset=/assets/index-CUZNltJr.js
web_asset_sha256=ca96217554c071f0de52d6c6e5a4ed6b71e8f030312d3b130f6cf772a1316bf0
supabase_project=fjcsvajkkztvlrpdplwx
cloudflare_project=job-helper
cloudflare_branch=main
score_tick_deploy=excluded
extract_resume_deploy=excluded
migration_0033_rewrite=excluded
preferences_save=excluded
worker_invoke=excluded
schedules_secrets_packages=excluded
```

## Root cause and forward-only repair

The production Retry control reached the authenticated RPC, but PostgreSQL
rejected it with `42702` because the RPC's
`on conflict (run_id, user_job_id)` target collided with its `RETURNS TABLE`
output variable named `run_id`.

Migration 0034 only `create or replace`s
`public.retry_deterministic_ranking_run()`. It replaces the ambiguous inferred
item conflict target with the existing named constraint
`deterministic_ranking_items_run_id_user_job_id_key`. It preserves:

- `security definer` with an empty `search_path`;
- `auth.uid()` owner derivation and owner/state/run locks;
- the existing partial unique retry-of-run conflict guard;
- authenticated-only execution grants;
- one captured-revision retry with fail-closed duplicate behavior.

Deployed migrations 0032 and 0033 were not edited.

## Runtime and web verification

The repair was applied only in a fresh disposable local Supabase project. The
real authenticated retry created and activated one replacement run at the
failed run's exact revision. A second authenticated retry returned
`ranking_retry_unavailable`, and a service-role assertion found exactly one
retry row. All five protocol scenarios passed with 21 assertions and reported
`productionCommands: 0`; temporary containers and volumes were removed.

Focused migration/Dashboard tests passed 16/16. The complete web suite passed
47 files and 966 tests. The production build transformed 191 modules and
produced `/assets/index-CUZNltJr.js` with the exact SHA-256 above. Lint passed
with only the pre-existing `AuthProvider.tsx` Fast Refresh warning.

The Dashboard now clears stale retry errors on a new attempt, renders a
friendly failure message with `role="alert"` and `aria-live="assertive"`, and
connects the Retry button to the error using `aria-describedby`.

## Required release gate

Before any mutation, the release executor must:

1. require a clean scoped diff and recompute the Git, migration, inventory,
   and built web hashes;
2. run the linked read-only migration dry-run and abort unless it proposes
   exactly migration 0034 and no other migration;
3. confirm production still targets Supabase project
   `fjcsvajkkztvlrpdplwx` and Cloudflare `job-helper/main`;
4. obtain literal approval bound to the final commit and all hashes;
5. apply migration 0034 first, then non-force push only that approved Git
   ancestry to `origin/main`;
6. verify remote migration parity, exact Cloudflare commit/asset identity, one
   authenticated Retry transition, scheduler convergence, and paid-score
   invariance without direct row repair.

## Narrow exclusions

This repair does not authorize deploying `score-tick` or `extract-resume`,
rewriting migrations 0032/0033, invoking a worker manually, saving
preferences, changing schedules/secrets/JWT policy/packages, applying any
other migration, force-pushing Git, directly repairing rows, or including the
unrelated user-owned dirty files.

No production dry-run, schema push, function deployment, Git push, worker
invocation, preference save, or other outward mutation occurred while
creating this preflight.
