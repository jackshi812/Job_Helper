# Phase 03.4 Plan 08 Local Protocol Evidence

**Recorded:** 2026-07-23  
**Scope:** Disposable local runtime only  
**Production commands:** 0  
**Linked-project commands:** 0  
**Remote database writes:** 0

## Exact local inputs

| Input | Version / SHA-256 |
|---|---|
| Node.js | `v26.3.1` |
| npm | `11.16.0` |
| Repository-pinned Supabase CLI | `2.109.1` |
| Docker client / Colima server | `29.6.2` / `29.5.2` |
| Migration `0033_deterministic_ranking_gap_closure.sql` | `ef91eb6e0d55007f71d1607b632fcc485c7162667ae7dfdc16e9525e9a6f0735` |
| Worker entry `score-tick/index.ts` | `e6f05372624dfbd134dc7410a033642b1e0ff50916e3294b3577ac89a01f8313` |
| Actual shared loop `deterministic-worker.ts` | `6f688bd8442cf911e42777c37751a18e37d8d55834d858a2ab2f25eb6870cda0` |

No token, JWT, user identifier, resume content, or raw job description is
recorded in this artifact.

## Real SQL/RPC protocol run

Command:

```text
node scripts/verify-phase-03-4-protocol.mjs --local --fresh
```

The verifier created a new temporary Supabase project, copied and applied the
real ordered migration chain through 0033, used separate authenticated owners
and a service session, exercised the production RPCs, and removed the project,
containers, volumes, and fixtures afterward. A temporary final migration grants
the service session table access only for fixture seeding, lease aging, and
postcondition inspection; it does not replace or modify any production RPC.

| Scenario | Assertions | Result |
|---|---:|---|
| Third-attempt lease exhaustion | 5 | PASS |
| Save during claim and repeated concurrent saves | 4 | PASS |
| Route requests during preference and route finalization | 5 | PASS |
| Transactional resume insert and delete signaling | 2 | PASS |
| Authenticated cross-owner isolation | 3 | PASS |

Total: **5 scenario groups, 19 runtime assertions, all passing.**

The first runtime execution also exposed the real
`save_preferences_and_start_ranking` output-parameter ambiguity in
`ON CONFLICT (run_id, user_job_id)`. Migration 0033 now names the existing
unique constraint explicitly; the fresh rerun proves authenticated preference
saves execute successfully.

## Actual worker-loop runtime

The Vitest runtime suite imports the production shared loop. Its query adapters
are faithful thenables that reject only when the supplied AbortSignal aborts;
the tests do not reproduce the worker state machine.

| Case | Result |
|---|---|
| Ordinary claim/load/stage/finalize with one shared signal | PASS |
| Never-resolving job load | PASS — bounded abort, no stage/publication |
| Never-resolving stage | PASS — bounded abort, claim remains reclaimable |
| Never-resolving finalizer | PASS — bounded abort, no fabricated publication |

Focused actual-loop and no-AI source contract: **12/12 tests passing.**

## Repository verification

| Gate | Result |
|---|---|
| Migration source suites | 14/14 PASS |
| Full web test suite | 963/963 PASS across 46 files |
| TypeScript + production build | PASS; 191 modules transformed |
| Lint | PASS with the pre-existing `AuthProvider.tsx` Fast Refresh warning |
| `git diff --check` | PASS |
| Disposable container cleanup | PASS; no test containers remain |

The build retained only its pre-existing chunk-size advisory. No package was
installed, no function was deployed, no migration was pushed, and no real
preference was mutated.
