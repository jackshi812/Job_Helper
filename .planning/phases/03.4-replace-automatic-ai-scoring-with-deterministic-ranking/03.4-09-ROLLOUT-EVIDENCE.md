# Phase 03.4 Plan 09 Rollout Evidence

**Captured:** 2026-07-23T22:25:04Z
**Status:** automated release and convergence gates passed; visual UAT human_needed
**Approved commit:** `a3145311018ff619254234e8a91eeab2c5df5741`
**Approved inventory SHA-256:** `9bdb9ba9eeedfe704789a0681dfac1a8257eb600c76652d0fdb62085704984d3`

This is an honest partial rollout record, not passing post-release evidence.
It intentionally omits the verifier's machine-pass evidence block because
two-owner convergence and signed-in visual observation are not complete.

## Exact approval and pre-mutation checks

The owner approved the exact commit, inventory, migration, score-tick entry
and bundle, web asset, Supabase project, and Cloudflare project/branch recorded
in `03.4-09-PREFLIGHT.md`.

Immediately before mutation:

- local `HEAD` was the approved 40-character commit;
- migration 0033 SHA-256 was
  `ef91eb6e0d55007f71d1607b632fcc485c7162667ae7dfdc16e9525e9a6f0735`;
- score-tick entry SHA-256 was
  `e6f05372624dfbd134dc7410a033642b1e0ff50916e3294b3577ac89a01f8313`;
- score-tick transitive manifest SHA-256 was
  `9de4e2e05c98497b532d9c687972ed1cacc2a8e49e103c641d3202cc3dda0857`;
- immutable web asset SHA-256 was
  `379a4355ed75c0681699bfcbc0fa5d844a6b7ddfab609263a14222be317a1431`;
- the linked dry-run proposed only
  `0033_deterministic_ranking_gap_closure.sql`.

## Applied schema

The repository-pinned Supabase CLI applied only migration 0033 to project
`fjcsvajkkztvlrpdplwx`. The post-push dry-run reported the remote database up
to date, and migration listing showed exact local/remote parity from `0001`
through `0033` with no pending migration.

Hosted migration 0033 is named
`deterministic_ranking_gap_closure`, contains 31 stored statements, and has
hosted statement-array SHA-256
`deb9d34499a77a5a521126b89fe43893700c616a296465a073ce100781e40d61`.
The separately approved local source file retained its exact SHA-256 above.

The CLI emitted an optional local pg-delta catalog-cache warning after the
hosted transaction because a temporary certificate file was unavailable. The
independent hosted inventory and second dry-run confirmed the migration
transaction itself completed.

## Deployed worker

Only `score-tick` was deployed, using project
`fjcsvajkkztvlrpdplwx` with `--no-verify-jwt --use-api`.

- stable ID: `ae6c147f-c3a8-417e-8057-d4105ac9aed5`;
- version: 15;
- status: `ACTIVE`;
- `verify_jwt`: `false`;
- independently downloaded hosted entry SHA-256:
  `e6f05372624dfbd134dc7410a033642b1e0ff50916e3294b3577ac89a01f8313`;
- independently downloaded hosted transitive manifest SHA-256:
  `9de4e2e05c98497b532d9c687972ed1cacc2a8e49e103c641d3202cc3dda0857`;
- provenance: fresh hosted download bracketed by stable metadata.

Excluded `extract-resume` remained stable ID
`9358db1a-95fc-49bc-a684-b98fb8eceff9`, ACTIVE v4,
`verify_jwt=false`.

Both minute schedules remained active with unchanged identities:

- `extract-resume-every-minute`: `* * * * *`,
  command MD5 `f792df8d50f7d6c268f1418e41ff210b`;
- `score-tick-every-minute`: `* * * * *`,
  command MD5 `f90afe213d26135169ca316d8a8916c2`.

## Released web artifact

The approved commit was pushed non-force to the configured GitHub
`origin/main`. GitHub's unique `Cloudflare Pages` check completed successfully.

- Cloudflare project: `job-helper`;
- deployment ID: `2731e675-4e51-46dc-b24a-09ac8f38a5e0`;
- environment: `production`;
- status: `success`;
- branch: `main`;
- commit:
  `a3145311018ff619254234e8a91eeab2c5df5741`;
- immutable URL: `https://2731e675.job-helper-qs9.pages.dev`;
- asset path: `/assets/index-C_XNX3Tv.js`;
- local and live asset SHA-256:
  `379a4355ed75c0681699bfcbc0fa5d844a6b7ddfab609263a14222be317a1431`.

## Paid-score invariance

The post-rollout read-only snapshot exactly matched the approved paid-score
baseline:

- score-purpose usage rows: 1,040 before and after;
- prompt tokens: 8,549,414 before and after;
- output tokens: 402,591 before and after;
- reservation date: `2026-07-23` before and after;
- requests: 200 before and after;
- reservation `updated_at`: `2026-07-23T05:09:01.089Z` before and after.

No paid scoring request, worker manual invocation, preference save, schedule
change, secret change, package install, or user-data repair was performed.

## Blocking convergence result

The current open-job count advanced naturally from the approved 2,115 baseline
to 2,116 during rollout. Both real owners retain a completed active revision,
but one owner also has a failed retryable building refresh:

- active revisions present: 2;
- complete/idle active owners: 1;
- incomplete active owners: 1;
- duplicate active owners: 0;
- visible missing deterministic rows: 5;
- visible mixed-revision rows: 2;
- nonterminal open items: 0;
- failed building run error: `ranking_item_failed`;
- failed run items: 1 failed, 137 pending;
- failed state is retryable and preserves its prior completed active snapshot.

Ten recent `score-tick-every-minute` executions reported scheduler success, but
the counts did not change. This is expected from the deployed claim contract:
pending items whose owning run is already `failed` are not claimable. Waiting
for more ordinary scheduler ticks cannot make that failed refresh complete.

The approved task explicitly forbids a manual worker invocation, preference
save, Retry click, direct row repair, or other publication repair. Execution
therefore stopped fail-closed. The post-release verifier was not presented
with fabricated zero counts and has not passed.

The owner later reported accidentally clicking the displayed `Retry update`
button several times. Two read-only observations after those clicks found:

- zero retry runs created in the preceding 30 minutes;
- zero building, completed, or failed retry runs;
- the same failed state remained retryable;
- the same 137 pending and 1 failed items remained attached to the failed run;
- duplicate active owners remained zero.

Therefore the repeated browser clicks did not create duplicate work, but they
also did not reach a successful retry mutation. No server-side retry action can
be credited from the clicks. The verifier did not invoke the authenticated RPC
or worker as a substitute.

## Mutation ledger

Approved outward mutations:

1. apply only migration 0033;
2. deploy only `score-tick`;
3. non-force push only the approved commit to `origin/main`, triggering the
   exact Cloudflare production deployment.

Outward mutations outside approval: 0.

## Retry root cause and local repair candidate

A later disposable local Supabase reproduction used a real authenticated owner
session and confirmed why the reported Retry clicks created no server work:
`retry_deterministic_ranking_run` returned HTTP 400 / PostgreSQL `42702`,
`column reference "run_id" is ambiguous`.

The collision is in the deployed 0032 RPC body. Its inferred
`on conflict (run_id, user_job_id)` target conflicts with the PL/pgSQL
`RETURNS TABLE` output variable named `run_id`. Migration 0033 intentionally
left that RPC unchanged.

The forward-only local repair is migration
`0034_repair_deterministic_ranking_retry.sql`; deployed migrations 0032 and
0033 remain byte-unchanged. A fresh disposable database now proves:

- one authenticated retry creates and activates the exact captured revision;
- a second retry fails closed with `ranking_retry_unavailable`;
- exactly one retry run exists for the failed source run;
- all five protocol scenarios pass with 21 runtime assertions;
- the Dashboard announces a failed Retry action visibly with an assertive
  accessible alert instead of leaving the click silent.

This repair is local only. Migration 0034 and the repaired web asset are not
live, no worker was redeployed or manually invoked, and production was not
contacted or mutated during repair construction. Exact local approval
inventory is recorded in `03.4-09-RETRY-REPAIR-PREFLIGHT.md`.

## Approved retry repair rollout

The owner separately approved the exact narrow repair:

- Git commit:
  `7642fde8bdc55112f50f847a7e65fd39af1d7f64`;
- repair inventory SHA-256:
  `766651da93e69685b1357fd315b17bfb825244877bda71b24d8b6a85f0560f36`;
- migration 0034 SHA-256:
  `3e55f0d4aa7cb69096ae25d89c02bba72e5eee25dfdd0bd96b557430c237b901`;
- web asset SHA-256:
  `ca96217554c071f0de52d6c6e5a4ed6b71e8f030312d3b130f6cf772a1316bf0`;
- unchanged targets: Supabase `fjcsvajkkztvlrpdplwx` and Cloudflare
  `job-helper/main`.

Immediately before mutation, local `HEAD`, the migration source, canonical
inventory, and fresh production build all matched those approved identities.
The linked dry-run proposed exactly
`0034_repair_deterministic_ranking_retry.sql`.

Only migration 0034 was then applied. The CLI's optional local pg-delta cache
again warned that its temporary certificate file was unavailable after the
hosted transaction. Independent post-push checks established that the remote
database was up to date and that local/remote migration inventory matched
exactly from `0001` through `0034`.

The exact approved commit was non-force pushed from remote baseline
`a3145311018ff619254234e8a91eeab2c5df5741` to `origin/main`. GitHub's unique
`Cloudflare Pages` check completed successfully:

- deployment ID: `252b06b6-03ab-4058-a8bb-b903421a9769`;
- environment/status: `production` / `success`;
- deployment trigger: GitHub push on `main`;
- exact commit:
  `7642fde8bdc55112f50f847a7e65fd39af1d7f64`;
- immutable URL: `https://252b06b6.job-helper-qs9.pages.dev`;
- asset path: `/assets/index-CUZNltJr.js`;
- independently downloaded live asset SHA-256:
  `ca96217554c071f0de52d6c6e5a4ed6b71e8f030312d3b130f6cf772a1316bf0`.

No function was deployed or invoked. In particular, `score-tick` and
`extract-resume` were untouched. No preference was saved, no row was repaired
directly, and no schedule, secret, JWT policy, or package changed.

## Retry repair checkpoint

The first read-only snapshot after the repaired schema and web asset became
live still showed the preserved failed source run:

- queue items: 137 pending, 0 claimed, 1 failed;
- active revisions: 2;
- complete active owners: 1;
- incomplete active owners: 1;
- duplicate active owners: 0;
- visible missing deterministic rows: 6;
- visible mixed-revision rows: 2;
- nonterminal open items: 0.

This is the expected pre-Retry state. Execution did not call the authenticated
Retry RPC for the owner. The required next action is one owner-initiated
`Retry update` click in the now-live UI, followed by read-only observation of
the ordinary minute scheduler to a fixed bound.

The paid-score snapshot remained exactly unchanged from the original release
baseline: 1,040 usage rows, 8,549,414 prompt tokens, 402,591 output tokens,
reservation date `2026-07-23`, 200 requests, and reservation `updated_at`
`2026-07-23T05:09:01.089Z`.

## Owner Retry and ordinary scheduler convergence

After the repaired release was live, the owner refreshed the affected
Dashboard and reported clicking `Retry update` exactly once. Read-only hosted
queries then proved the product transition without invoking the RPC or worker
from the verifier:

- retry runs created after the repaired deployment: exactly 1;
- retry created at: `2026-07-23T22:21:53.913381Z`;
- retry run status: completed;
- retry items: 2,118 completed, 0 pending, 0 claimed, 0 failed;
- owner states: 2 idle, 0 building, 0 failed, 0 retry-available;
- duplicate active owners/revisions: 0.

Two ordinary `score-tick-every-minute` executions started after the owner
transition. Both scheduler runs succeeded; none failed. The verifier did not
invoke either Edge worker.

Final current-run and publication checks passed:

- actionable building-run items: 0 pending and 0 claimed;
- active-run items: 0 non-completed and 0 failed;
- complete active owners: 2 of 2;
- incomplete active owners: 0;
- visible missing deterministic rows: 0;
- visible mixed-revision rows: 0;
- nonterminal current-open items: 0;
- current open jobs: 2,116;
- exact completed current-open items per owner: 2,116;
- projected current-open deterministic rows per owner: 2,116;
- owners with complete current-open coverage: 2 of 2.

The superseded failed source run intentionally retains 137 pending and 1
failed historical item. They are not attached to any building or active run,
are not claimable by the worker contract, and are excluded from actionable,
active, visible, and current-open convergence counts. No historical row was
deleted or repaired to fabricate zero global history.

## Final identity and cost checks

Post-convergence read-only identity checks remained exact:

- hosted migrations 0033 and 0034 are respectively named
  `deterministic_ranking_gap_closure` and
  `repair_deterministic_ranking_retry`;
- `score-tick` remained ACTIVE v15 with stable ID
  `ae6c147f-c3a8-417e-8057-d4105ac9aed5` and `verify_jwt=false`;
- `extract-resume` remained ACTIVE v4 with stable ID
  `9358db1a-95fc-49bc-a684-b98fb8eceff9` and `verify_jwt=false`;
- both minute schedules retained their original expressions, active flags,
  and command MD5 identities;
- `origin/main` remained exact commit
  `7642fde8bdc55112f50f847a7e65fd39af1d7f64`;
- the exact-SHA Cloudflare Pages check remained completed/successful;
- the immutable repaired web asset retained the approved SHA-256.

The final paid-score snapshot again equaled every original baseline field:
1,040 usage rows, 8,549,414 prompt tokens, 402,591 output tokens, reservation
date `2026-07-23`, 200 requests, and reservation `updated_at`
`2026-07-23T05:09:01.089Z`.

The complete approved mutation ledger is:

1. apply migration 0033;
2. deploy `score-tick` v15;
3. non-force push the original approved release commit;
4. apply forward-only retry repair migration 0034;
5. non-force push exact repair commit `7642fde8`.

Mutations outside those two explicit approvals: 0. Signed-in desktop and
narrow-width visual observation remains honestly `human_needed` because no
browser surface is available to this verifier.

## Post-convergence empty-feed diagnosis

The owner reported that the affected Dashboard showed no jobs after the
successful Retry. Read-only aggregate queries proved this is the published
deterministic result of the account's current preferences, not a client-local
company/tier filter, stale cache, failed publication, or Retry corruption:

- current target titles: 0;
- current locations/include/exclude keywords: 0/0/0;
- current title-exclusion keywords: 2;
- maximum-experience signal: unset;
- active items: 2,116 completed;
- eligible/tiered active items: 0;
- ineligible active items: 2,116;
- deterministic filter counts: 2,087 `title_non_overlap` and 29
  `excluded_title_keyword`;
- server feed-query rows: 0;
- default-visible feed rows: 0.

The failed source, retry, current active run, and current preference records
all had the same sanitized settings fingerprint
`3d51f52a37c6f578d1c63e880748c484`; pairwise equality checks passed. Retry
therefore preserved the captured settings exactly.

The other owner independently had 106 eligible active results, 71 server
feed-query rows, and 70 default-visible rows, proving the released feed path is
not globally empty. The affected owner must intentionally add at least one
target title and save once to start a new ordinary preference revision if they
want matching jobs. Keywords alone cannot satisfy the strict title-eligibility
gate. The verifier did not perform that preference mutation.
