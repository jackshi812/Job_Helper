# Phase 03.4 Plan 04 Release Preflight

**Captured:** 2026-07-23T14:50:06Z  
**Mode:** read-only, zero outward mutation  
**Project:** `fjcsvajkkztvlrpdplwx`

This preflight binds the release candidate to reviewed local source, the already
hosted additive schema and contained function identities, the complete current
owner/job universe, and an exact score-purpose cost baseline. It contains only
hashes, counts, statuses, versions, and bounded command text. It contains no
credential, user identifier, preference value, job text, description, resume,
or authentication material.

The candidate Git SHA is intentionally not recorded here before the Task 1
commit. The blocking checkpoint must compute the live post-commit SHA and
release-inventory hash, present both to the owner, and require literal
SHA-bound approval before any mutation.

## Machine-verifiable evidence

<!-- evidence:start -->
evidence_mode: preflight
project_ref: fjcsvajkkztvlrpdplwx
local_migrations: 0001,0002,0003,0004,0005,0006,0007,0008,0009,0010,0011,0012,0013,0014,0015,0016,0017,0018,0019,0020,0021,0022,0023,0024,0025,0026,0027,0028,0029,0030,0031,0032
remote_migrations: 0001,0002,0003,0004,0005,0006,0007,0008,0009,0010,0011,0012,0013,0014,0015,0016,0017,0018,0019,0020,0021,0022,0023,0024,0025,0026,0027,0028,0029,0030,0031,0032
migration_0032_sha256: 4441a5f20657f421628aa82a3d18ade0d40bc9d7f0566efcfa6bd9277d48ef21
migration_0032_remote_name: deterministic_ranking
migration_0032_remote_statement_count: 72
score_tick_deployment_id: ae6c147f-c3a8-417e-8057-d4105ac9aed5
score_tick_version: 11
score_tick_status: ACTIVE
score_tick_verify_jwt: false
score_tick_index_sha256: 68892a709d9a2c23e42265852c1116896b25a52a2a52f1140b21ad23ae316b79
score_tick_bundle_manifest_sha256: eca92fe96821fdc350e8efa3261a528ed461c8a3019af0392f3c04b36026c370
extract_resume_deployment_id: 9358db1a-95fc-49bc-a684-b98fb8eceff9
extract_resume_version: 4
extract_resume_status: ACTIVE
extract_resume_verify_jwt: false
extract_resume_index_sha256: e8c437b73592b8305f89260d6c64fa6e8c20aa9e707706a70dde650eca378987
extract_resume_bundle_manifest_sha256: 406ce0a3eb3e5c1cb939108c7a53edb6a2fd2820b09935d2f9fb0f6bb693b08c
verifier_sha256: e453f4f25a6a47fa14cf7ae949c501fec486739a9dd80abd0af4366f780c22ae
verifier_test_sha256: 3d08b4c9bebffce7093c25e8de3f691ffd0f79d8c7f5eb9b31c10918764bbae8
web_asset_path: /assets/index-BYvTFcFt.js
web_asset_sha256: 24f14d89b7544f6cd01fe5372f229136077c9e2fddba9477cdbde8616c396e8b
initializer_owner: postgres
initializer_security_definer: true
initializer_search_path: empty
initializer_execute_roles: postgres,service_role
initializer_max_batch: 25
initializer_initial_unique: true
initializer_ordinary_queue: true
real_user_count: 2
open_job_count: 2116
eligible_owner_count: 2
deterministic_state_count: 0
deterministic_run_count: 0
deterministic_item_count: 0
deterministic_initial_run_count: 0
score_usage_row_count: 1040
score_usage_prompt_tokens: 8549414
score_usage_output_tokens: 402591
score_budget_date: 2026-07-23
score_budget_requests_today: 200
score_budget_updated_at: 2026-07-23T05:09:01.089Z
cost_baseline_sha256: 2f328bc082e736c23e7a3a950a4808f3466b4f6afbe85d324e530474a56c7390
controlled_failure_transition: pass
pending_new_job_transition: pass
recency_expiry_transition: pass
retry_transition: pass
atomic_preference_transition: pass
full_tests: pass
production_build: pass
lint: pass
outward_mutations: 0
<!-- evidence:end -->

## Schema, function, and initializer parity

- Local and remote migrations are exactly `0001` through `0032`.
- Hosted migration `0032` is named `deterministic_ranking`, contains 72
  statements, and the approved local file retains SHA-256
  `4441a5f20657f421628aa82a3d18ade0d40bc9d7f0566efcfa6bd9277d48ef21`.
- The read-only Management API inventory/catalog checks above pass. A separate
  CLI `db push --dry-run` attempt reached no proposal and no mutation because
  this machine's direct Postgres IPv6/DNS route failed before connection. This
  is not recorded as a pass: Task 3 must rerun the exact dry-run and abort
  unless it connects and proposes zero migrations.
- Hosted `score-tick` is the stable expected ID, ACTIVE v11, with
  `verify_jwt=false`. The reviewed local source retains the Plan 02 SHA.
- Hosted `extract-resume` is the stable expected ID, ACTIVE v4, with
  `verify_jwt=false`. Source inspection proves it accounts only
  `purpose='extract'` and requests only a free deterministic route refresh.
- `initialize_deterministic_ranking_backfill(integer)` is postgres-owned,
  SECURITY DEFINER, empty-search-path, executable only by postgres and
  service_role, rejects batches outside 1–25, has one-initial-run-per-owner
  uniqueness, creates ordinary pending ranking items, and uses the shared
  finalizer for zero-job owners.
- The verifier never accepts supplied SQL, a user-selected owner, a browser
  session, or an authenticated end-user token for initialization.

## Current universe and no-mix baseline

- Real users: 2.
- Initializer-eligible owners: 2; this equals the complete real-user universe.
- Current open jobs: 2,116.
- Deterministic state/runs/items/initial runs: `0/0/0/0`.
- Therefore no initializer or deterministic backfill has run, no active
  deterministic revision exists yet, and the local deterministic web build
  must not be deployed before complete backfill.
- Post-backfill acceptance is fail-closed unless both owners have exactly one
  completed active revision, every captured open job is terminal, zero-job
  owners are complete, and missing/mixed visible deterministic rows are zero.

## Zero-cost baseline

The exact score-purpose baseline is:

- usage rows: 1,040;
- prompt tokens: 8,549,414;
- output tokens: 402,591;
- reservation ledger: UTC date `2026-07-23`, 200 requests;
- reservation `updated_at`: `2026-07-23T05:09:01.089Z`;
- canonical baseline SHA-256:
  `2f328bc082e736c23e7a3a950a4808f3466b4f6afbe85d324e530474a56c7390`.

The initializer and every ordinary worker tick re-read this baseline. Any row,
token, reservation, date, request-count, or update-timestamp change aborts the
rollout. The reservation ledger is already at 200; deterministic work requires
and is proven to require zero additional paid capacity.

## Local deterministic transition evidence

- `web/tests/deterministic-ranking-snapshot.integration.test.ts` proves prior
  bytes survive pending, partial completion, controlled failure, and one bounded
  Retry; concurrent/new jobs block incomplete publication and remain invisible
  until committed.
- `web/tests/preference-refilter-feed.integration.test.ts` proves atomic
  preference transition, previous-feed retention, one completed-revision swap,
  and failure retention.
- `web/tests/deterministic-ranking.test.ts` proves the exact 24-hour recency
  boundary expires from 10 to 0 points.
- `web/tests/deterministic-ranking-source.test.ts` and migration tests prove
  recency maintenance uses the same captured-run, item, stage, and finalizer
  protocol.
- Focused transition/render run: 3 files, 20 tests, all passing.
- Complete web run: 43 files, 619 tests, all passing.
- Production build: passing, 191 modules transformed.
- Lint: passing with only the pre-existing `AuthProvider.tsx` Fast Refresh
  warning.
- Verifier negative suite: 10/10 passing.
- `git diff --check`: passing.

## Exact commands proposed for SHA-bound approval

The blocking checkpoint must replace each angle-bracket value with the live
post-Task-1 value it displays. Approval applies only to the resulting literal
commands and unchanged inventory:

```text
web/node_modules/.bin/supabase db push --dry-run
web/node_modules/.bin/supabase functions deploy score-tick --project-ref fjcsvajkkztvlrpdplwx --no-verify-jwt
node scripts/verify-phase-03-4-release.mjs --initialize-backfill --approved-sha <POST_TASK_SHA> --approved-inventory-sha256 <INVENTORY_SHA256> --approved-owner-count 2 --approved-open-job-count 2116 --approved-cost-baseline-sha256 2f328bc082e736c23e7a3a950a4808f3466b4f6afbe85d324e530474a56c7390
git push origin main
node scripts/verify-phase-03-4-release.mjs --post-release .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-ROLLOUT-EVIDENCE.md
```

`db push --dry-run` is a blocking no-op parity check; any proposed migration
aborts. Worker deployment must precede initialization. Initialization and
ordinary worker draining must finish with exact cost invariance before Git push.
The successful exact-main Cloudflare production deployment and immutable asset
hash must pass before read-only signed-in UAT.

## Read-only production UAT proposed

For each of the two real accounts, observe only:

1. Ranking state is idle and points to one complete active revision.
2. Dashboard rows equal that account's complete active deterministic projection.
3. Every visible row has stored score, stored tier, and stored six-row breakdown;
   no legacy AI, null, pending, partial, or mixed-revision row appears.
4. Session-only company/tier/sort/resize controls remain usable without a server
   preference mutation.
5. Preferences renders the persisted rubric without saving.
6. An authenticated read-only detail projection shows the same active revision
   and six stored evidence rows; integration/render tests remain the authority
   for navigation and mark-seen behavior.

Do not save preferences, invoke Retry, navigate through mark-seen detail
behavior, Apply, Dismiss/Restore, create jobs/users/fixtures, inject failure,
manipulate time, or call AI during production UAT.

## Explicit exclusions and preserved unrelated files

Excluded until a later explicit decision: further schema change; manual/reset
SQL; legacy cleanup; scheduler, secret, provider, or extraction changes;
production fixtures; preference edits; new-job creation; failure injection; time
manipulation; direct AI; watchlist-only UI; manual AI scoring; partial web
release; unrelated file edits; and any score-purpose cost delta.

The following pre-existing user-owned files are excluded from all staging and
edits:

- `.DS_Store`
- `.planning/phases/03.3-dashboard-filter-refinements/03.3-04-ROLLOUT-EVIDENCE.md`
- `scripts/agent-dashboard.mjs`
- `scripts/agent-dashboard.test.mjs`

No Phase 03.3 artifact was modified. No production worker, database row,
preference, job, schedule, secret, provider, Git remote, or web deployment was
mutated while assembling this preflight.
