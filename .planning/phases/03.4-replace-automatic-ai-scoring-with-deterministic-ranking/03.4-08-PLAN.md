---
phase: 03.4-replace-automatic-ai-scoring-with-deterministic-ranking
plan: 08
type: execute
wave: 5
depends_on: [03.4-04]
files_modified:
  - supabase/migrations/0033_deterministic_ranking_gap_closure.sql
  - supabase/functions/_shared/deterministic-worker.ts
  - supabase/functions/score-tick/index.ts
  - web/tests/deterministic-ranking-gap-closure-migration.test.ts
  - web/tests/deterministic-ranking-worker-runtime.test.ts
  - scripts/verify-phase-03-4-protocol.mjs
  - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-08-PROTOCOL-EVIDENCE.md
autonomous: true
gap_closure: true
requirements: [PREF-01, SCOR-01, SCOR-02, SCOR-04, SCOR-05]
must_haves:
  truths:
    - "An expired third-attempt claim atomically becomes failed and moves its owning run/state to the existing bounded retry state."
    - "Superseding a building preference run terminalizes all of its pending and claimed items, leaving no permanently nonterminal queue rows."
    - "A route-refresh request arriving during any ranking run survives that run's finalization and is consumed only when a route run is successfully enqueued."
    - "Resume metadata insert/delete transactionally records route-refresh demand, independent of a later browser RPC."
    - "Every awaited worker database/RPC operation receives a deadline abort signal and a never-resolving operation cannot hold the invocation beyond the bounded grace."
    - "Concurrency and liveness assertions execute the real migration/RPC protocol and the actual worker control loop, not a reimplemented state model."
  artifacts:
    - path: "supabase/migrations/0033_deterministic_ranking_gap_closure.sql"
      provides: "Terminal lease reaping, supersession cleanup, durable route refresh, and resume triggers"
    - path: "supabase/functions/_shared/deterministic-worker.ts"
      provides: "Actual deadline-scoped worker control loop with injectable runtime boundaries"
    - path: "web/tests/deterministic-ranking-worker-runtime.test.ts"
      provides: "Actual-loop deadline and abort regressions"
    - path: "scripts/verify-phase-03-4-protocol.mjs"
      provides: "Disposable local Supabase SQL/RPC concurrency verifier"
    - path: ".planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-08-PROTOCOL-EVIDENCE.md"
      provides: "Sanitized local runtime proof"
  key_links:
    - from: "supabase/functions/score-tick/index.ts"
      to: "supabase/functions/_shared/deterministic-worker.ts"
      via: "Cron-secret handler delegates the production control loop and passes the service client/deadline"
      pattern: "runDeterministicWorker"
    - from: "supabase/functions/_shared/deterministic-worker.ts"
      to: "public.claim_deterministic_ranking_work"
      via: "Every RPC/query builder receives the current AbortSignal"
      pattern: "abortSignal"
    - from: "web/src/lib/resumes.ts"
      to: "supabase/migrations/0033_deterministic_ranking_gap_closure.sql"
      via: "Resume row insert/delete triggers transactionally set route_refresh_requested_at"
      pattern: "route_refresh_requested_at"
    - from: "scripts/verify-phase-03-4-protocol.mjs"
      to: "supabase/migrations/0033_deterministic_ranking_gap_closure.sql"
      via: "Disposable local Supabase applies and executes the real RPC implementation with concurrent sessions"
      pattern: "claim_deterministic_ranking_work|save_preferences_and_start_ranking"
---

<objective>
Close SQL queue/routing races and enforce the worker deadline against actual awaited operations with runtime-backed proof.

Purpose: Guarantee D-19, D-20, D-23, and D-24 under hard deaths, concurrent preference saves, resume events, and stalled I/O, while preserving the previous complete snapshot.
Output: Additive/replay-safe migration changes, an actual reusable worker loop, disposable Supabase protocol verification, and sanitized evidence.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-CONTEXT.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-RESEARCH.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-PATTERNS.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-VERIFICATION.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-02-SUMMARY.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-04-SUMMARY.md
@supabase/migrations/0032_deterministic_ranking.sql
@supabase/functions/score-tick/index.ts

<interfaces>
- `claim_deterministic_ranking_work(batch_size integer)` is service-role-only and owns claim/reclaim transitions.
- `save_preferences_and_start_ranking(...)` is authenticated, owner-scoped, and starts one captured preference run.
- `finalize_deterministic_ranking_run(run_id uuid)` is the sole atomic publication authority.
- `enqueue_deterministic_route_refreshes(batch_size integer)` consumes pending route demand only while creating a route run.
- Existing item attempts are bounded at three and claimed leases expire after five minutes.
- Existing worker limits are 25 per claim/concurrency, 5,000 per invocation, and 45 seconds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make SQL leases, supersession, and route-refresh signaling terminal and race-safe</name>
  <files>supabase/migrations/0033_deterministic_ranking_gap_closure.sql, web/tests/deterministic-ranking-gap-closure-migration.test.ts, scripts/verify-phase-03-4-protocol.mjs</files>
  <read_first>
    - supabase/migrations/0032_deterministic_ranking.sql
    - web/tests/deterministic-ranking-migration.test.ts
    - web/tests/deterministic-ranking-snapshot.integration.test.ts
    - web/src/lib/resumes.ts
    - supabase/functions/extract-resume/index.ts
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-VERIFICATION.md
  </read_first>
  <behavior>
    - Claiming first reaps expired claimed items already at the attempt ceiling in the same transaction, marks their run failed, and updates only the matching building state to failed/retryable.
    - Saving preferences marks the superseded run stale and every pending/claimed item for that run terminal stale/cancelled under locks.
    - A late stage call for a superseded item is rejected as stale without corrupting the new run.
    - Generic finalization never clears route-refresh demand.
    - Route demand is cleared only as part of successfully enqueueing its route run; a newer signal survives through compare-and-set/timestamp ordering.
    - Resume insert/delete records route demand transactionally through owner-scoped triggers; extraction's existing service signal remains valid.
  </behavior>
  <action>
    Create forward-only migration `0033_deterministic_ranking_gap_closure.sql`; never edit or rewrite the already-applied migration 0032. Replace affected constraints/functions with replay-safe `create or replace`/named-constraint changes. Extend the item status constraint with one explicit terminal superseded status and update queue/release semantics to count only actionable current building work. Add a bounded terminal-lease reaper invoked inside the claim RPC before selecting new rows; lock affected items/runs/state and reuse the existing failure/retry contract without publishing partial results. In preference save, lock and terminalize every nonterminal old-run item before marking the run stale and creating the new run. Remove the generic finalizer's unconditional route timestamp clear. Capture/consume route requests only on successful route-run enqueue, using the observed request timestamp so a concurrent newer timestamp remains pending. Add security-definer-safe, schema-qualified resume metadata triggers that set route demand for the owning user in the same insert/delete transaction; do not broaden table grants or trust a caller-supplied user ID. Add a source-structure suite for migration ordering, grants, trigger ownership, and the absence of destructive changes to historical result columns.

    Build `scripts/verify-phase-03-4-protocol.mjs` with Node standard library and the repository-pinned Supabase CLI. It must create a disposable temporary Supabase workdir, apply the real migration chain, seed isolated test owners/jobs/runs, and use independent authenticated/service sessions plus concurrent transactions to test third-attempt death, save-during-claim, repeated saves, late staging, request-during-preference-run, request-during-route-run, and resume insert/delete. It must refuse a linked/production project ref and clean up local fixtures. Existing in-memory model tests remain supplemental only.
  </action>
  <verify>
    <automated>node scripts/verify-phase-03-4-protocol.mjs --local --fresh</automated>
  </verify>
  <acceptance_criteria>
    - Third-attempt lease exhaustion reaches failed/retryable with no claimed item left.
    - Repeated concurrent saves leave old items terminal and exactly one current building run.
    - Newer route requests survive older run finalization and enqueue exactly one follow-up route run.
    - Resume insert/delete sets route demand in the same transaction.
    - Cross-user/authenticated calls cannot inspect or mutate another owner's protocol state.
  </acceptance_criteria>
  <done>The real SQL protocol has no terminal-lease, supersession, or route-refresh liveness hole.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce one deadline across the actual worker control loop</name>
  <files>supabase/functions/_shared/deterministic-worker.ts, supabase/functions/score-tick/index.ts, web/tests/deterministic-ranking-worker-runtime.test.ts</files>
  <read_first>
    - supabase/functions/score-tick/index.ts
    - web/tests/deterministic-ranking-worker-liveness.test.ts
    - web/tests/deterministic-ranking-source.test.ts
    - supabase/migrations/0032_deterministic_ranking.sql
  </read_first>
  <behavior>
    - Claim, maintenance, recovery scan, job load, resume load, stage success/failure, and finalize all receive a deadline-scoped AbortSignal.
    - The worker stops claiming before deadline exhaustion and aborts in-flight fetches when remaining time expires.
    - A never-resolving load, stage, or finalize adapter produces bounded controlled termination rather than an unbounded await.
    - Aborted claimed items remain lease-reclaimable; the worker never fabricates completion or directly publishes.
    - Cron authentication, 25-way cap, 5,000-item cap, and zero automatic-AI capability remain unchanged.
  </behavior>
  <action>
    Extract the production orchestration into a package-free shared module with explicit `runDeterministicWorker` dependencies for client, monotonic clock, timeout scheduling, and deadline. The Edge handler retains environment/auth/response duties and delegates to that exact loop. Create one invocation AbortController, compute remaining time before every operation, attach `.abortSignal(signal)` to every Supabase PostgREST/RPC builder, and abort at the hard deadline with a small bounded response-cleanup margin. Do not convert aborts into staged permanent item failures; leases are the recovery boundary. Tests must import and execute the actual shared loop with faithful thenable query adapters, not copy its state machine, and cover never-resolving load/stage/finalize operations plus ordinary success.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- --run tests/deterministic-ranking-worker-runtime.test.ts tests/deterministic-ranking-source.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every awaited network-capable worker operation is signal-bound.
    - Hung load, stage, and finalize cases abort within their injected deadline plus bounded test tolerance.
    - No hung case publishes or permanently fails a reclaimable item.
    - Source contract still proves no provider, API key, paid reservation, or score-purpose usage path.
  </acceptance_criteria>
  <done>The advertised invocation bound governs awaited I/O in the actual production loop.</done>
</task>

<task type="auto">
  <name>Task 3: Record local runtime-backed protocol evidence</name>
  <files>.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-08-PROTOCOL-EVIDENCE.md</files>
  <read_first>
    - scripts/verify-phase-03-4-protocol.mjs
    - web/tests/deterministic-ranking-worker-runtime.test.ts
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-VERIFICATION.md
  </read_first>
  <action>
    Run the disposable local Supabase protocol verifier from a clean database, the actual-loop runtime suite, migration/source suites, full web suite, build, lint, and diff check. Record sanitized tool versions, migration checksum, scenarios, assertions, and pass/fail results. Do not record credentials, JWTs, user IDs, raw resume content, or raw job descriptions. This is local evidence only: do not link, push, deploy, or mutate production.
  </action>
  <verify>
    <automated>node scripts/verify-phase-03-4-protocol.mjs --local --fresh &amp;&amp; cd web &amp;&amp; npm test -- --run &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Evidence distinguishes real SQL/RPC execution from supplemental model tests.
    - Every scenario named in verification gaps 2, 3, 4, and 7 passes.
    - Migration checksum and exact local worker source hash are recorded.
    - No production command was run.
  </acceptance_criteria>
  <done>Runtime-backed local evidence is sufficient to request the exact production release approval in Plan 09.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- A terminal lease reaper and superseded-item terminal state.
- Race-safe route-refresh consumption and transactional resume triggers.
- The actual reusable deadline-scoped worker loop.
- A disposable local Supabase concurrency verifier and sanitized evidence.
</artifacts_this_phase_produces>

<threat_model>
ASVS level: L1. Block execution completion on any unmitigated high or critical finding.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.4-08-01 | Tampering | Atomic publication/queue | high | mitigate | Lock item, run, and matching owner state; keep finalizer as sole publication authority. |
| T-03.4-08-02 | Denial of Service | Expired claims and stalled I/O | high | mitigate | Bounded reaper, attempt ceiling, per-operation abort signal, and hard invocation deadline. |
| T-03.4-08-03 | Elevation of Privilege | Security-definer RPCs/triggers | high | mitigate | Empty search path, schema-qualified objects, owner derived from row/auth context, narrow grants, cross-user tests. |
| T-03.4-08-04 | Repudiation | Resume refresh signaling | medium | mitigate | Record route demand in the same database transaction as metadata mutation and preserve newer timestamps. |
| T-03.4-08-05 | Information Disclosure | Runtime evidence | medium | mitigate | Sanitize identities, tokens, resume/job content, and temporary database output. |
| T-03.4-08-SC | Tampering | Dependency supply chain | low | accept | No package installs; use lockfile-backed CLI/Vitest and platform APIs. |
</threat_model>

<verification>
- Disposable local Supabase applies the real migration and passes all concurrency scenarios.
- Actual worker loop passes success, abort, reclaim, and containment tests.
- Full suite/build/lint and `git diff --check` pass.
- Static migration grants/RLS and source no-AI gates pass.
</verification>

<success_criteria>
- Verification gaps 2, 3, 4 (database half), and 7 (runtime-test half) are closed.
- No nonterminal work can become permanently unclaimable.
- No newer route-refresh signal can be erased by an older finalizer.
- No schema/function/production mutation occurs until Plan 09 exact approval.
</success_criteria>

<output>
Create `.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-08-SUMMARY.md` when done.
</output>
