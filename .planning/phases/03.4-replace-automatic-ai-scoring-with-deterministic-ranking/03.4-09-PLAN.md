---
phase: 03.4-replace-automatic-ai-scoring-with-deterministic-ranking
plan: 09
type: execute
wave: 6
depends_on: [03.4-05, 03.4-06, 03.4-07, 03.4-08]
files_modified:
  - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-PREFLIGHT.md
  - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-ROLLOUT-EVIDENCE.md
  - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UAT.md
autonomous: false
gap_closure: true
requirements: [PREF-01, SCOR-01, SCOR-02, SCOR-03, SCOR-04, SCOR-05]
must_haves:
  truths:
    - "The owner approves one exact migration checksum, score-tick entry/bundle inventory, Git SHA, and immutable web asset before any production mutation."
    - "Production applies only the forward migration 0033, deploys only the approved score-tick bundle, and releases only the approved web SHA in schema-worker-web order."
    - "Independent downloaded hosted bytes equal the approved local score-tick source and transitive bundle."
    - "Both owners converge to complete active revisions with zero actionable/nonterminal current-run work and no paid-score ledger change."
    - "Signed-in desktop and narrow-width visual verification covers Preferences, Dashboard, and Job Detail without saving preferences or mutating user data."
  artifacts:
    - path: ".planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-PREFLIGHT.md"
      provides: "Exact immutable approval inventory and read-only baselines"
    - path: ".planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-ROLLOUT-EVIDENCE.md"
      provides: "Schema/function/web identities, convergence, and zero-cost proof"
    - path: ".planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UAT.md"
      provides: "Final signed-in visual and functional acceptance"
  key_links:
    - from: "supabase/migrations/0033_deterministic_ranking_gap_closure.sql"
      to: "production migration inventory"
      via: "Exact SHA-256 approval, linked dry-run, noninteractive push, and post-push checksum/name parity"
      pattern: "0033_deterministic_ranking_gap_closure"
    - from: "supabase/functions/score-tick/index.ts"
      to: "hosted score-tick"
      via: "Exact approved bundle deploy followed by independent remote download/hash"
      pattern: "score-tick"
    - from: "origin/main"
      to: "Cloudflare production"
      via: "Exact commit metadata and immutable JavaScript SHA-256"
      pattern: "commit|sha256"
---

<objective>
Release the verified gap closure through one exact, separately approved production operation and complete final non-mutating signed-in acceptance.

Purpose: Preserve the current stable release until local/runtime gates pass, then close all seven verification gaps with reproducible schema, function, web, data, cost, and visual evidence.
Output: Exact preflight, approved rollout evidence, and completed UAT suitable for formal Phase 03.4 re-verification.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-CONTEXT.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UI-SPEC.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-VERIFICATION.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-05-SUMMARY.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-06-SUMMARY.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-07-SUMMARY.md
@.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-08-SUMMARY.md
@scripts/verify-phase-03-4-release.mjs
@scripts/verify-phase-03-4-protocol.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble an exact read-only release inventory</name>
  <files>.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-PREFLIGHT.md</files>
  <read_first>
    - scripts/verify-phase-03-4-release.mjs
    - scripts/verify-phase-03-4-release.test.mjs
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-08-PROTOCOL-EVIDENCE.md
    - supabase/migrations/0033_deterministic_ranking_gap_closure.sql
    - supabase/functions/score-tick/index.ts
    - web/package.json
  </read_first>
  <action>
    Require a clean scoped release diff while preserving unrelated user-owned files. Run all focused/runtime/full tests, build, lint, diff checks, and the disposable local protocol verifier. Compute SHA-256 for migration 0033, score-tick entry, every transitive function dependency and bundle manifest, exact local HEAD/origin candidate, and built immutable JavaScript. Use the linked Supabase CLI only in read-only mode to run migration dry-run, capture current migration/function identities, independently download/hash current hosted bytes, and record queue/owner/cost baselines. Prove the dry-run proposes exactly migration 0033 and no other migration. Do not push, deploy, invoke the worker, save preferences, or mutate production.
  </action>
  <verify>
    <automated>node scripts/verify-phase-03-4-release.mjs --preflight .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - Preflight records exact migration, worker entry/bundle, Git, and web asset hashes.
    - Linked dry-run proposes exactly one forward migration, 0033.
    - Local runtime protocol, full suite, build, and lint pass.
    - Current hosted source is independently measured; no local value is substituted.
    - Production remains unchanged.
  </acceptance_criteria>
  <done>A single immutable release candidate and baseline are ready for explicit owner authorization.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: [BLOCKING] Approve the exact schema, worker, and web release</name>
  <read_first>
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-PREFLIGHT.md
    - scripts/verify-phase-03-4-release.mjs
  </read_first>
  <files>None</files>
  <decision>Authorize only the exact release inventory produced by Task 1.</decision>
  <context>
    Present the exact commit SHA, migration `0033_deterministic_ranking_gap_closure.sql` SHA-256, score-tick entry and transitive bundle SHA-256, built immutable web asset SHA-256, target Supabase project ref, Cloudflare project/branch, deployment order, and exclusions. Approval is required because this changes production schema/function/web state. Authentication prompts, checksum drift, extra proposed migrations, target drift, or any changed byte invalidate approval.
  </context>
  <action>Present the unchanged exact inventory and all exclusions. Require literal approval bound to its commit and checksums. Perform no schema push, function/web deployment, worker invocation, or other production mutation in this checkpoint.</action>
  <options>
    <option id="approve-exact-release">
      <name>Approve exact release</name>
      <pros>Allows the narrowly identified schema, score-tick, and web rollout plus read-only verification.</pros>
      <cons>Mutates production only within the displayed inventory.</cons>
    </option>
    <option id="defer-release">
      <name>Defer release</name>
      <pros>Keeps the current stable production release unchanged.</pros>
      <cons>Phase 03.4 remains gaps_found.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Approval explicitly names the unchanged commit, migration checksum, function entry/bundle hashes, web asset hash, and targets.
    - Any drift or additional migration invalidates approval.
    - No outward mutation occurs in the checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>git diff --check &amp;&amp; test -s .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-PREFLIGHT.md</automated>
    <human-check>Approve or defer the exact displayed schema, score-tick, and web release.</human-check>
  </verify>
  <resume-signal>Reply `approve exact release` to authorize the displayed checksums, or `defer release`.</resume-signal>
  <done>The exact inventory is explicitly approved, or production remains unchanged and execution stops.</done>
</task>

<task type="auto">
  <name>Task 3: Push the approved release, verify convergence, and complete non-mutating visual UAT</name>
  <files>.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-ROLLOUT-EVIDENCE.md, .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UAT.md</files>
  <read_first>
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-PREFLIGHT.md
    - scripts/verify-phase-03-4-release.mjs
    - supabase/migrations/0033_deterministic_ranking_gap_closure.sql
    - .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-UI-SPEC.md
  </read_first>
  <action>
    After exact approval, recompute every approved hash and abort on drift. Use the repository-pinned CLI through the safe noninteractive path: first repeat linked `db push --dry-run`, then run linked `db push --yes` only when it still proposes exactly migration 0033; verify remote migration parity immediately. Deploy only `score-tick` with the approved project ref, `--no-verify-jwt`, `--use-api`, and exact approved bundle, then read stable ID/version/status/verify_jwt and independently download/hash the hosted source. Push only the approved commit to `origin/main`, require successful checks, and bind Cloudflare production to that exact SHA and immutable asset. Do not deploy extract-resume, change schedules/secrets, install packages, or include unrelated dirty files.

    Let the approved minute scheduler use the normal bounded protocol; do not manually repair publication state. Run post-release verifier until terminal convergence or its fixed bound, requiring zero actionable pending/claimed/failed current-run items, complete current-open coverage for both owners, preserved prior snapshot on any failure, exact paid-score ledger invariance, and remote/local source identity. Use signed-in read-only sessions to verify both accounts. Then use the in-app browser at desktop and narrow widths to inspect Preferences keyword groups/rubric/validation affordances without editing or saving, Dashboard notices/controls/table scrolling/resizing, and one Job Detail six-row breakdown/apply presentation without mark-seen/dismiss/apply actions where avoidable. Record observed results honestly; if the browser is unavailable, leave visual behavior `human_needed` rather than claiming asset/test evidence as visual proof.
  </action>
  <verify>
    <automated>node scripts/verify-phase-03-4-release.mjs --post-release .planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-ROLLOUT-EVIDENCE.md</automated>
    <human-check>Signed-in desktop and narrow-width Preferences, Dashboard, and Job Detail match the Phase 03.4 UI-SPEC without saving preferences or performing user-data mutations.</human-check>
  </verify>
  <acceptance_criteria>
    - Remote migration inventory contains exact approved 0033 and no unapproved migration.
    - Hosted score-tick downloaded bytes match the approved entry and bundle hashes independently.
    - Cloudflare production commit and immutable asset match the approved Git SHA/build.
    - Queue/current-open coverage is complete for both owners with zero paid-score delta.
    - Visual UAT is explicitly passed by observation or remains honestly human_needed.
  </acceptance_criteria>
  <done>The exact approved gap closure is live, converged, cost-invariant, independently byte-proven, and visually accepted or explicitly awaiting human observation.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- An immutable exact-release approval inventory.
- Production schema/function/web identity and convergence evidence.
- Independent hosted-function byte proof.
- Final signed-in responsive UAT without preference or user-data mutation.
</artifacts_this_phase_produces>

<threat_model>
ASVS level: L1. Block execution completion on any unmitigated high or critical finding.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.4-09-01 | Tampering | Production release | critical | mitigate | Exact checksums, post-approval recomputation, one-migration dry-run, ordered rollout, and post-release identity proof. |
| T-03.4-09-02 | Spoofing | Supabase/Cloudflare targets | high | mitigate | Display and pin exact project refs, branch, function ID, and deployment commit; abort on target drift. |
| T-03.4-09-03 | Elevation of Privilege | Schema/function deployment | high | mitigate | Explicit owner checkpoint, pinned noninteractive CLI, narrow migration grants, and unchanged cron-secret boundary. |
| T-03.4-09-04 | Information Disclosure | Signed-in verification/evidence | high | mitigate | Read-only least-privilege sessions; redact tokens, IDs, resume text, and job-description bodies. |
| T-03.4-09-05 | Denial of Service | Convergence | medium | mitigate | Normal bounded scheduler path, fixed verifier bound, no manual state repair or unbounded polling. |
| T-03.4-09-SC | Tampering | Dependency supply chain | low | accept | No installs; use repository-pinned tooling and approved immutable artifacts. |
</threat_model>

<verification>
- All Plan 05-08 focused/runtime/full gates pass before approval.
- Exact migration/function/web deployment identities pass after rollout.
- Independent hosted byte comparison passes.
- Two-owner convergence and paid-ledger invariance pass.
- Visual UAT is observed or explicitly routed as human_needed.
</verification>

<success_criteria>
- All seven grouped verification gaps have code, runtime, and release evidence.
- The previously stable production state is preserved until explicit exact approval.
- No unapproved production mutation or automatic paid AI action occurs.
- Phase 03.4 is ready for formal re-verification; it is not marked complete if visual UAT remains human_needed.
</success_criteria>

<output>
Create `.planning/phases/03.4-replace-automatic-ai-scoring-with-deterministic-ranking/03.4-09-SUMMARY.md` when done.
</output>
