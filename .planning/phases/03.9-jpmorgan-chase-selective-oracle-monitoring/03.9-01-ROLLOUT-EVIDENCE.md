# Phase 03.9 JPMorgan Rollout Evidence

Status: **AUTOMATED ROLLOUT PASS — OWNER-BROWSER UAT PENDING**

The exact approved release chain deployed migrations `0045`–`0047` and the
approved `observe-connectors` and `poll-tick` function bundles. Hosted migration
history is exact through `0047`; deployed functions are Active with
`verify_jwt=false`; the catalog and company identity both use
`oracle:jpmc:CX_1001` and the canonical `/jobs` URL.

The live Oracle proof completed at `2026-07-27T00:42:06Z` with 113
deduplicated jobs across six approved title-family slices and eight result
pages. Every result was exact United States, within the seven-day provider
facet, and backed by complete detail evidence. There were no warnings, and the
selective observation set `allowMissingClosure=false`.

JPMorgan passed terminal admission, then reached Active with exactly three
accepted server-timed observations at `00:43:14Z`, `00:44:12Z`, and
`00:45:10Z`. Replay and same-window attempts did not advance activation. A
natural scheduled poll ran from `00:49:00Z` to `00:49:09Z`, persisted 113 open
jobs, closed zero jobs, and recorded no error.

One persisted sample is Oracle job `210774113`, **Analyst - Mid-Cap Financial
Sponsors Group**, in Los Angeles, posted `2026-07-24T22:08:30Z`, with Program
Analysts family evidence, a complete description, and an HTTPS employer apply
link. This is evidence for the browser check, not a substitute for owner UAT.

Morgan Stanley Eightfold, Goldman Higher, Capital One, Fidelity, Morgan
Stanley Workday, Bank of America, BlackRock, and Barclays retained their
pre-rollout identities, activation states, and job ownership. The only
permitted differences were ordinary scheduler-owned timestamps. Cleanup found
zero temporary fixtures, observations, scheduler overrides, or verifier
residue.

Automated verification passed 1,366 tests across 64 files, the production
build, rollout-specific tests, hosted parity checks, lifecycle guards, and
protected-source comparisons. The phase remains open until the owner confirms
the Watchlist row and one real JPMorgan job in the owner's own signed-in
browser.
