---
phase: 03.9-jpmorgan-chase-selective-oracle-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/branded-identities.ts
  - supabase/functions/_shared/adapters/types.ts
  - supabase/functions/_shared/adapters/scope.ts
  - supabase/functions/_shared/adapters/oracle-recruiting.ts
  - supabase/functions/_shared/connectors.ts
  - supabase/functions/observe-connectors/index.ts
  - supabase/functions/poll-tick/index.ts
  - supabase/migrations/0045_phase_03_9_jpmorgan_oracle.sql
  - web/tests/branded-identities.test.ts
  - web/tests/branded-scope.test.ts
  - web/tests/branded-adapters.test.ts
  - web/tests/observe-connectors.test.ts
  - web/tests/poll-tick.test.ts
  - web/tests/phase-03-9-migration.test.ts
  - scripts/run-phase-03-9-rollout.mjs
  - scripts/run-phase-03-9-rollout.test.mjs
  - scripts/verify-phase-03-9-hosted.mjs
  - scripts/verify-phase-03-9-hosted.test.mjs
  - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-PREFLIGHT.md
  - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-RELEASE-MANIFEST.json
  - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-HOSTED-VERIFICATION.json
  - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-ROLLOUT-VERIFICATION.json
  - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-ROLLOUT-EVIDENCE.md
autonomous: true
requirements: [DISC-01, DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "D-01 and D-03: exactly JPMorgan Chase site CX_1001 is authorized, and no sibling company or caller-controlled Oracle coordinate gains authority."
    - "D-02: /jobs is canonical; an exact /requisitions alias may resolve only to the same frozen identity."
    - "D-04: every persisted job has exact United States detail proof."
    - "D-05: only FIN, D&A, RSK, PIM, S&D, and PAA provider families are admitted."
    - "D-06: exact Less than 7 days facet proof and a validated posting timestamp are both mandatory."
    - "D-07: exact provider facets and JobFunction fields—not title or description inference—create family evidence."
    - "D-08: normal user preferences remain the downstream relevance filter without connector duplication or bypass."
    - "D-09 and D-10: all six slices and every detail reconcile under bounds; any identity, facet, count, page, ID, country, family, date, detail, cap, or deadline failure is incomplete and closes no jobs."
    - "Every complete JPMorgan rolling-window observation is credible for the selected scope but has allowMissingClosure=false, so a job aging past seven days is never falsely closed."
    - "D-11 through D-13: migration 0045 and release tooling prepare exact JPMorgan-only Experimental/Active authority, three observation windows, natural polling, precise Unsupported fallback, and an explicit hash-bound production approval checkpoint."
  artifacts:
    - path: "supabase/functions/_shared/adapters/oracle-recruiting.ts"
      provides: "Six-slice exact U.S. seven-day Oracle list/detail observation"
    - path: "supabase/migrations/0045_phase_03_9_jpmorgan_oracle.sql"
      provides: "Forward-only JPMorgan evidence, admission, observation, and claim authority"
    - path: "scripts/run-phase-03-9-rollout.mjs"
      provides: "Dry-run, exact approval, deploy, activation, cleanup, and evidence orchestration"
    - path: ".planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-RELEASE-MANIFEST.json"
      provides: "Immutable migration/function/target/action inventory for Plan 02 approval"
  key_links:
    - from: "Oracle title/date/location facets"
      to: "jobs.scope_evidence"
      via: "exact list identity, exact detail JobFunction/US/date, and recomputed digest"
      pattern: "selectedTitlesFacet|selectedPostingDatesFacet|providerCategoryLabel|externalIdDigest"
    - from: "complete selective observation"
      to: "job lifecycle"
      via: "credibleForClosure=true and allowMissingClosure=false"
      pattern: "allowMissingClosure"
    - from: "migration 0045"
      to: "shared observation and claim RPCs"
      via: "literal oracle:jpmc:CX_1001 branches without sibling branded authority"
      pattern: "oracle:jpmc:CX_1001"
---

<objective>
Build and freeze the complete local Phase 03.9 JPMorgan release.

Purpose: Replace the historical Credit-Risk-only Oracle candidate with the
owner-approved exact U.S. seven-day six-family contract, preserve truthful
rolling-window lifecycle behavior, and prepare a production release whose
bytes and finite mutations can be approved separately.

Output: Tested Oracle identity/adapter/evidence/runtime changes, forward
migration 0045, focused rollout and hosted-verification tooling, and an
immutable dry-run manifest. This plan performs no external production
mutation.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-CONTEXT.md
@.planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-RESEARCH.md
@.planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-PATTERNS.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-VERIFICATION.md

<interfaces>
The exact source key remains `oracle:jpmc:CX_1001`. The country facet is
`300000000289738` / `United States`; posting facet is `7` /
`Less than 7 days`; approved title families are `FIN` / `Finance`, `D&A` /
`Data & Analytics`, `RSK` / `Risk`, `PIM` / `Product/Investment Mgmt`, `S&D` /
`Strategy & Development`, and `PAA` / `Program Analysts & Associate`.

The existing `PollObservation` contract is retained. A clean observation is
`complete`, positive, warning-free, and `credibleForClosure=true`, but
JPMorgan always returns `allowMissingClosure=false`. The existing bounded
branded job evidence remains digest-revalidated before persistence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze exact title/date identity and source-specific evidence</name>
  <files>supabase/functions/_shared/branded-identities.ts, supabase/functions/_shared/adapters/types.ts, supabase/functions/_shared/adapters/scope.ts, web/tests/branded-identities.test.ts, web/tests/branded-scope.test.ts</files>
  <read_first>
    - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-CONTEXT.md
    - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-RESEARCH.md
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/adapters/types.ts
    - supabase/functions/_shared/adapters/scope.ts
    - web/tests/branded-identities.test.ts
    - web/tests/branded-scope.test.ts
  </read_first>
  <action>
    Per D-01 through D-08, replace the JPMorgan identity's single
    `categoryFacets` proof with immutable `titleFacets` containing the six exact
    ID/label pairs and add immutable `postingDateFacet` ID `7`, label
    `Less than 7 days`, and `recentDays: 7`. Make `/jobs` the canonical
    `publicUrl`. If `/requisitions` remains recognized, implement it only as an
    exact literal alias resolving to the same frozen object; reject query,
    fragment, subpath, host, site, or facet variants before fetch.

    Preserve the five-field branded job evidence shape. Add source-aware
    JPMorgan family mapping so Finance→Finance, Data & Analytics→Data,
    Risk→Risk, Product/Investment Mgmt→Investment, Strategy &
    Development→Strategy, and Program Analysts & Associate→Program Analysts.
    Extend the evidence term type only as required for Strategy and Program
    Analysts. Generic Morgan Stanley and Goldman matching must retain the
    historical seven-term behavior; the two new terms cannot admit either
    sibling source. Keep the external ID digest formula exactly
    `[sourceKey, externalId, normalizedProviderLabel, matchedTerm, "US"]`.

    Add deterministic identity and evidence tests for exact success, object
    immutability, both accepted URL literals, hostile variants, all six family
    mappings, digest stability, and sibling-provider non-widening.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-identities.test.ts branded-scope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "`resolveBrandedIdentity('oracle:jpmc:CX_1001')` exposes exactly six approved title facets, exact posting facet 7, exact U.S. facet, site CX_1001, and canonical `/jobs` URL."
    - "No caller-controlled Oracle host, site, path, facet, query, or fragment resolves."
    - "All six exact JobFunction labels create deterministic evidence; Strategy and Program Analysts remain ineligible for non-JPMorgan sources."
    - "Existing Credit Risk, Morgan Stanley, and Goldman evidence tests remain byte-behavior compatible."
  </acceptance_criteria>
  <done>The only authorized Oracle identity and family proof are frozen and source-specific.</done>
</task>

<task type="auto">
  <name>Task 2: Implement six-slice recent Oracle reconciliation</name>
  <files>supabase/functions/_shared/adapters/oracle-recruiting.ts, web/tests/branded-adapters.test.ts</files>
  <read_first>
    - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-RESEARCH.md
    - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-PATTERNS.md
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/adapters/oracle-recruiting.ts
    - supabase/functions/_shared/adapters/workday.ts
    - web/tests/branded-adapters.test.ts
  </read_first>
  <action>
    Per D-04 through D-10, change Oracle list requests to include
    `selectedLocationsFacet=300000000289738`,
    `selectedTitlesFacet={exactFamilyId}`, and
    `selectedPostingDatesFacet=7`. For every page require exact site, offset,
    limit, selected field echoes, and one matching location/title/date facet
    entry whose ID, label, and `TotalCount` equal the frozen slice and reported
    total. Treat extra unselected facet entries as informational, never proof.

    Parse list `JobFunction` as the selected provider-family label and require
    exact normalized equality; keep `JobFamily` only as bounded metadata.
    Require numeric stable ID, title, exact `US`, location, and parseable
    `PostedDate`. Add an injected wall-clock epoch separate from the existing
    monotonic deadline clock and reject a list or detail posting whose UTC
    calendar date falls outside the current seven-day provider window, is
    unparseable, or is implausibly future-dated.

    Paginate and reconcile each family independently, reject repeated pages or
    duplicate IDs within a slice, deduplicate the union, and reject cross-slice
    ID/title/location/family drift. Hydrate every deduplicated detail under the
    existing byte/page/job/detail/concurrency/deadline bounds. Detail must
    repeat exact ID, title, `PrimaryLocationCountry: US`, exact `JobFunction`,
    recent `ExternalPostedStartDate`, bounded location, and usable full
    description. Generate per-job evidence from exact JobFunction and aggregate
    slice digests containing source, title ID/label, U.S. facet, date
    ID/label, counts, and pages.

    Return a positive complete observation only after all six slices/details
    reconcile. Set `credibleForClosure=true` and
    `allowMissingClosure=false`. Every count, offset, facet, schema, duplicate,
    country, family, date, detail, cap, deadline, network, content-type, or
    payload failure returns bounded partial/unknown evidence and closes none.

    Generalize Oracle fixtures and cover all six families, multi-page offsets,
    union deduplication, exact request coordinates, title/date/location drift,
    stale/future/malformed dates, list/detail mismatch, foreign/missing detail,
    caps, timeouts, repeated pages, and the rolling-window closure flag.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-adapters.test.ts -t "JPMorgan Oracle"</automated>
  </verify>
  <acceptance_criteria>
    - "Every list request contains the exact server-owned country, title-family, posting-date, site, offset, and limit values."
    - "A complete six-family fixture reconciles all slices/details, deduplicates stable IDs, creates exact per-job/aggregate evidence, and returns allowMissingClosure=false."
    - "A fixture for every specified drift/failure returns credibleForClosure=false and allowMissingClosure=false with a bounded warning."
    - "No title or description text can substitute for missing provider JobFunction, U.S., date, or ID evidence."
  </acceptance_criteria>
  <done>The local adapter truthfully observes the exact recent JPMorgan scope without false closure.</done>
</task>

<task type="auto">
  <name>Task 3: Restore JPMorgan-only observation, persistence, and claim authority</name>
  <files>supabase/functions/_shared/connectors.ts, supabase/functions/observe-connectors/index.ts, supabase/functions/poll-tick/index.ts, supabase/migrations/0045_phase_03_9_jpmorgan_oracle.sql, web/tests/observe-connectors.test.ts, web/tests/poll-tick.test.ts, web/tests/phase-03-9-migration.test.ts</files>
  <read_first>
    - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-CONTEXT.md
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/observe-connectors/index.ts
    - supabase/functions/poll-tick/index.ts
    - supabase/migrations/0040_phase_03_8_branded_connectors.sql
    - supabase/migrations/0043_phase_03_8_workday_candidates.sql
    - supabase/migrations/0044_phase_03_8_selective_workday_scope_evidence.sql
    - web/tests/observe-connectors.test.ts
    - web/tests/poll-tick.test.ts
  </read_first>
  <action>
    Preserve registry re-resolution before Oracle verify, Experimental observe,
    and Active poll. Update positive branded observation validation so an exact
    JPMorgan complete/credible/positive/warning-free aggregate with
    `allowMissingClosure=false` is admissible; do not require or grant the same
    selective behavior to Eightfold or Goldman. Keep poll persistence's
    central evidence recomputation and require exact source-specific family
    mapping before any job write. Prove ordinary user preference filtering
    remains downstream and no source-specific relevance bypass is added.

    Add forward-only migration
    `0045_phase_03_9_jpmorgan_oracle.sql`; never edit 0040–0044. Replace
    `jobs_scope_evidence_check` so only `oracle:jpmc:CX_1001` accepts the six
    exact normalized provider-family/evidence-term pairs while every existing
    Workday/Eightfold/Goldman branch remains valid. Update the exact JPMorgan
    catalog URL to `/jobs`.

    Add a replay-safe, service-role-only JPMorgan terminal boundary that admits
    exactly one Experimental company row after positive proof or records a
    bounded precise Unsupported outcome with no operational authority. Do not
    re-grant the old three-company branded finalizer. Replace
    `record_connector_observation`,
    `claim_due_experimental_connectors`, and `claim_due_companies` by
    preserving every 0043/0044 provider branch and adding only
    `ats_type='oracle_recruiting'` with
    `source_key='oracle:jpmc:CX_1001'`. Retain the three-window cap,
    server-time/replay/locking rules, stagger, due cadence, and service-role
    grants. Add precise unsupported reasons for facet/date/family/scope/detail
    failures.

    Test SQL/source invariants, exact Experimental 0/3→Active 3/3 behavior,
    claim eligibility, disabled/duplicate/replay guards, evidence pairs,
    closure-disabled persistence, and unchanged Morgan Stanley, Goldman Sachs,
    Charles Schwab, public ATS, and active Workday branches.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- observe-connectors.test.ts poll-tick.test.ts phase-03-9-migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Migration 0045 is forward-only and reauthorizes only oracle:jpmc:CX_1001."
    - "Exact JPMorgan positive evidence can enter Experimental and progress through three server windows; no sibling branded source can use that authority."
    - "jobs_scope_evidence_check accepts exactly the six JPMorgan family/term pairs and preserves all existing evidence branches."
    - "Complete rolling observations persist/reopen/refresh jobs but close zero jobs from absence."
    - "All shared RPCs retain service-role-only execution, replay protection, row locking, caps, and existing source allowlists."
  </acceptance_criteria>
  <done>The complete local runtime and schema authorize only the proven selective JPMorgan source.</done>
</task>

<task type="auto">
  <name>Task 4: Build immutable dry-run release and hosted verifier</name>
  <files>scripts/run-phase-03-9-rollout.mjs, scripts/run-phase-03-9-rollout.test.mjs, scripts/verify-phase-03-9-hosted.mjs, scripts/verify-phase-03-9-hosted.test.mjs, .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-PREFLIGHT.md, .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-RELEASE-MANIFEST.json, .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-HOSTED-VERIFICATION.json, .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-ROLLOUT-VERIFICATION.json, .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-ROLLOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-RESEARCH.md
    - .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-PATTERNS.md
    - scripts/run-phase-03-8-rollout.mjs
    - scripts/verify-phase-03-8-hosted.mjs
    - supabase/migrations/0045_phase_03_9_jpmorgan_oracle.sql
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/adapters/oracle-recruiting.ts
    - supabase/functions/observe-connectors/index.ts
    - supabase/functions/poll-tick/index.ts
  </read_first>
  <action>
    Create focused Phase 03.9 release tooling using the Phase 03.8 manifest
    pattern without copying its unrelated candidate matrix. Freeze a new
    manifest UUID; source commit/object identity; Supabase project ref;
    expected hosted baseline 0001–0044; exact migration 0045 path/hash; exact
    changed Edge Function entry/transitive bundle hashes and JWT settings;
    unchanged web identity; current JPMorgan Unsupported baseline; protected
    sibling company/catalog/job fingerprints; three observation windows;
    natural-poll bounds; UAT fields; finite cleanup and ACL assertions.

    `--dry-run` must be read-only and print one exact approval string binding
    manifest, migration, function, and source hashes. Mutation mode must reject
    every other string, uncommitted release bytes, target/baseline drift,
    unexpected migrations/functions/actions, dirty verifier authority, or
    protected-row drift. Make rollout resumable from hosted parity,
    Experimental progress, Active state, or consumed terminal evidence without
    duplicating jobs/observations.

    Create hosted checks for migration/function equality, service-role ACLs,
    exact catalog/company state, evidence constraints, activation progression,
    healthy natural poll, at least one eligible persisted job, closure-disabled
    lifecycle, preference-pipeline participation, sibling regressions, and zero
    temporary residue. Seed only local fixture definitions; Plan 01 performs no
    hosted mutation.

    Run the full focused suite, full web suite, build, source diff checks, and
    dry run. Write PENDING hosted/rollout evidence and the preflight inventory.
  </action>
  <verify>
    <automated>node --test scripts/run-phase-03-9-rollout.test.mjs scripts/verify-phase-03-9-hosted.test.mjs &amp;&amp; cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; cd .. &amp;&amp; node scripts/run-phase-03-9-rollout.mjs --dry-run --manifest .planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-RELEASE-MANIFEST.json &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "Dry run emits one exact approval string and performs no network or database mutation."
    - "Manifest binds only migration 0045 and the changed verify-board/observe-connectors/poll-tick bundles required by the final diff."
    - "Hosted verifier can distinguish PENDING, exact PASS, precise Unsupported, drift, and cleanup failure."
    - "Plan 01 ends with production unchanged and all hosted/rollout evidence truthfully PENDING."
    - "The focused tests, complete web tests, build, dry run, and git diff check exit 0."
  </acceptance_criteria>
  <done>An exact local JPMorgan release is ready for a separate production decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller input → frozen Oracle identity | Only exact server-owned host/site/route/facets may reach fetch |
| Oracle list/detail → scope evidence | External fields must prove family, country, recency, identity, content, and completeness |
| Selective observation → lifecycle | A complete rolling window may refresh jobs but cannot prove missing jobs closed |
| Runtime evidence → database | Evidence is recomputed and checked before persistence or activation |
| Local release → production approval | Manifest bytes and finite actions must be immutable before mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.9-01-01 | Spoofing / SSRF | identity and URLs | high | mitigate | Literal source, object-identity re-resolution, redirect denial, hostile URL tests |
| T-03.9-01-02 | Tampering | Oracle facets | high | mitigate | Validate selected echoes and exact location/title/date ID-label-count tuples per page |
| T-03.9-01-03 | Elevation of Privilege | evidence terms | high | mitigate | Source-specific six-pair mapping in TypeScript and SQL; sibling regression tests |
| T-03.9-01-04 | Tampering | rolling lifecycle | high | mitigate | Hard-code allowMissingClosure=false and verify no absent-ID closure |
| T-03.9-01-05 | Denial of Service | list/detail fan-out | high | mitigate | Existing byte/page/job/detail/deadline/concurrency caps and bounded codes |
| T-03.9-01-06 | Elevation of Privilege | migration/RPCs | high | mitigate | Oracle exact key only, service-role grants, locking, replay and three-window caps |
| T-03.9-01-07 | Repudiation | release inventory | high | mitigate | Immutable manifest and exact approval string; PENDING evidence before approval |
| T-03.9-01-SC | Tampering | dependency supply chain | low | accept | No dependency installation; built-in fetch/URL/crypto and current test runtime only |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `OracleRecruitingBrandedIdentity.titleFacets`
- `OracleRecruitingBrandedIdentity.postingDateFacet`
- Oracle wall-clock recency option/helper and exact title/date facet validators
- Source-specific JPMorgan provider-family evidence mapping for Strategy and Program Analysts
- `supabase/migrations/0045_phase_03_9_jpmorgan_oracle.sql`
- JPMorgan-only terminal admission/unsupported SQL boundary
- Phase 03.9 migration, rollout, and hosted-verification tests
- `scripts/run-phase-03-9-rollout.mjs`
- `scripts/verify-phase-03-9-hosted.mjs`
- `03.9-01-PREFLIGHT.md`
- `03.9-01-RELEASE-MANIFEST.json`
- `03.9-01-HOSTED-VERIFICATION.json`
- `03.9-01-ROLLOUT-VERIFICATION.json`
- `03.9-01-ROLLOUT-EVIDENCE.md`
</artifacts_this_phase_produces>

<verification>
- Exact identity/scope tests prove only the six owner-approved family/date/country values.
- Oracle fixtures prove complete six-slice detail hydration and every fail-closed drift path.
- Migration/runtime tests prove exact JPMorgan-only authority and zero sibling widening.
- Full web tests and build pass.
- Dry run validates immutable release inventory and prints an exact approval signal without mutation.
</verification>

<success_criteria>
- The local connector produces complete positive JPMorgan observations with exact evidence and closure disabled.
- Migration 0045 safely prepares JPMorgan Experimental/Active authority without changing sibling providers.
- All tests/build/dry-run gates pass.
- Production remains unchanged and requires Plan 02 approval.
</success_criteria>

<output>
Create `.planning/phases/03.9-jpmorgan-chase-selective-oracle-monitoring/03.9-01-SUMMARY.md` when done.
</output>
