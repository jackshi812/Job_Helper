---
phase: 03.9-jpmorgan-chase-selective-oracle-monitoring
verified: 2026-07-28T21:22:13Z
status: passed
score: 12/12 must-haves verified
behavior_unverified: 0
overrides_applied: 2
re_verification:
  previous_status: gaps_found
  previous_score: 10/12
  gaps_closed:
    - "The documented read-only `--assert-evidence` path now validates all four manifests and their files at each recorded historical source commit."
    - "The historical rollout fixtures no longer compare immutable Phase 03.9 manifests with mutable current bundles; the focused rollout/hosted suite is 14/14 green."
    - "The owner accepted migrations 0046 and 0047 as intentional, exact, forward-only alternatives to the literal 0045-only plan contract."
    - "The owner accepted the deterministically asserted rollout chain and reproducible historical hashes as sufficient evidence without a separate retained production-approval attestation."
  gaps_remaining: []
  regressions: []
gaps: []
overrides:
  - must_have: "Migration 0045 is the only schema push, only changed Edge Functions are deployed, and hosted bytes equal the approval manifest before and after every mutation stage."
    reason: "Forward-only migrations 0046 and 0047 were exact, separately hash-bound JPMorgan repairs required to correct the Oracle catalog and canonical company identity; protected sources remained unchanged and the final contracted outcome passed."
    accepted_by: "project owner"
    accepted_at: "2026-07-28T21:22:13Z"
  - must_have: "The exact production mutation is independently proven to have followed explicit hash-bound approval."
    reason: "The owner accepts the deterministically asserted approved release chain and reproducible historical Git-object hashes as sufficient evidence even though a separate pre-mutation approval actor/signal/timestamp attestation was not retained."
    accepted_by: "project owner"
    accepted_at: "2026-07-28T21:22:13Z"
---

# Phase 03.9: JPMorgan Chase Selective Oracle Monitoring Verification Report

**Phase Goal:** Monitor JPMorgan Chase through its exact Oracle `CX_1001` contract, ingesting only United States jobs posted within seven days in approved Finance, Data & Analytics, Risk, Product/Investment Management, Strategy & Development, and Program Analysts & Associate families, while preserving safe rolling-window lifecycle behavior.

**Verified:** 2026-07-28T21:22:13Z
**Status:** passed
**Re-verification:** Yes — deterministic evidence gaps closed and owner governance override accepted

## Goal Achievement

The current ingestion and lifecycle implementation achieves the functional
selective-monitoring outcome. The repaired read-only verifier proves the
complete four-manifest rollout record against each historical source commit.
The owner has explicitly accepted the exact 0046/0047 forward-repair chain and
the retained deterministic evidence as sufficient without reconstructing a
missing historical approval attestation.

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | JPMorgan resolves only to Oracle source `oracle:jpmc:CX_1001`, site `CX_1001`, canonical `/jobs`, with only the exact `/requisitions` alias; caller/identity drift fails closed. | ✓ VERIFIED | `branded-identities.ts` freezes the exact identity and literal alias. Registry and adapter tests reject non-literal or mismatched identities. |
| 2 | Every Oracle list request carries exact United States, one approved family, and seven-day coordinates, and all six slices must reconcile before completion. | ✓ VERIFIED | `oracle-recruiting.ts` builds and validates exact finder/facet echoes, iterates all six slices, reconciles counts/pages/duplicates, and rejects drift. Focused adapter tests pass. |
| 3 | Persisted candidates have a stable requisition ID, exact U.S. location, recent posting, exact provider family, full detail, and deterministic scope evidence/digests. | ✓ VERIFIED | List and detail validators require numeric ID, matching location/family/date, nonempty detail, and aggregate/slice digests before emitting normalized jobs. |
| 4 | Only the six JPMorgan provider families enter the selective scope; Strategy and Program Analysts do not widen sibling source vocabularies. | ✓ VERIFIED | JPMorgan mappings are source-key-specific in `scope.ts`; generic sibling terms remain unchanged. Branded-scope tests pass. |
| 5 | Any slice, pagination, detail, timeout, cap, echo, or reconciliation failure produces incomplete/degraded evidence and closes no jobs. | ✓ VERIFIED | Adapter failure paths return `credibleComplete: false` and `allowMissingClosure: false`; lifecycle and poll code suppress closures. Failure-path and lifecycle tests pass. |
| 6 | A complete observation remains closure-disabled for rolling-window absence and flows through the normal shared preference/ranking pipeline. | ✓ VERIFIED | Complete adapter output still sets `allowMissingClosure: false`; poll writes shared jobs/user-job state and the dashboard feed reads the normal deterministic preference relation. |
| 7 | Database authority is exact-key, service-role-only, replay-safe, locked, capped at three activation windows, and later migrations preserve JPMorgan while adding other sources. | ✓ VERIFIED | Migrations 0045–0047 establish the exact RPC/catalog/company contract; 0048 and current 0057 retain JPMorgan clauses. Migration tests for Phases 03.9, 03.10, and the latest frontier pass. |
| 8 | Activation occurred only after positive terminal proof, exactly three accepted server windows, and a healthy natural poll with eligible persisted jobs. | ✓ VERIFIED | Hosted/rollout records show Active 3/3, three observation rows, a natural poll four minutes after activation, 113 open eligible jobs, zero closures, and no error. Hosted assertion passes. |
| 9 | Migration 0045 is the only schema push and the approved release inventory is unchanged through rollout. | ✓ VERIFIED WITH OVERRIDE | The approved final chain includes exact hash-bound 0045, 0046, and 0047 releases. The owner accepted 0046/0047 as intentional forward-only catalog/company-identity repairs that preserve protected sources and satisfy the product contract. |
| 10 | Production mutation is independently tied to explicit exact-release approval and the rollout record has a deterministic assertion path. | ✓ VERIFIED WITH OVERRIDE | All four historical manifest selections and rollout evidence assert reproducibly, and non-exact approvals fail before mutation. The owner accepted that retained evidence as sufficient despite the absence of a separate pre-mutation actor/signal/timestamp attestation. |
| 11 | Protected siblings, active Workday sources, real user/company/job state, and cleanup residue remain unchanged. | ✓ VERIFIED | Recorded before/after fingerprints are equal, later Phase 03.10 protected-source evidence agrees, and hosted/rollout records report zero verifier residue. Current migrations retain exact sibling branches. |
| 12 | Release-bound owner UAT confirmed the Watchlist state and at least one real eligible job. | ✓ VERIFIED | `03.9-UAT.json` is PASS, names release `...0004`, binds hosted/rollout SHA-256 values, records 113 eligible jobs, and passes `assertUatRecord`. |

**Score:** 12/12 truths verified (2 accepted governance overrides)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `supabase/functions/_shared/branded-identities.ts` | Frozen exact JPMorgan Oracle identity and six facets | ✓ VERIFIED | Exists, substantive, imported by the registry/adapter path, and exercised by identity tests. |
| `supabase/functions/_shared/adapters/oracle-recruiting.ts` | Exact six-slice list/detail ingestion and aggregate evidence | ✓ VERIFIED | Exists, substantive, registry-wired, and behaviorally exercised. Current file is byte-for-byte unchanged from its Phase 03.9 implementation commit. |
| `supabase/functions/_shared/adapters/scope.ts` | Source-specific family normalization and deterministic digest | ✓ VERIFIED | Exact JPMorgan labels map to six internal terms without widening generic siblings. |
| `supabase/functions/observe-connectors/index.ts` | Terminal/activation evidence recording | ✓ VERIFIED | Consumes connector observations, preserves closure-disabled evidence, and records health/activation through shared RPCs. |
| `supabase/functions/poll-tick/index.ts` | Shared poll, revalidation, persistence, and lifecycle path | ✓ VERIFIED | Recomputes branded scope evidence before persistence and does not bypass normal ranking/preferences for JPMorgan. |
| `supabase/migrations/0045_phase_03_9_jpmorgan_oracle.sql` | Exact-key authority, lifecycle, replay/locking/caps | ✓ VERIFIED | Substantive and exercised by migration tests. |
| `supabase/migrations/0046_phase_03_9_jpmorgan_catalog_repair.sql` | Forward catalog repair | ✓ VERIFIED / OVERRIDE ACCEPTED | Substantive, deployed, hash-bound, JPMorgan-specific, and explicitly accepted as an intentional alternative to the 0045-only contract. |
| `supabase/migrations/0047_phase_03_9_jpmorgan_company_identity.sql` | Forward canonical-company repair | ✓ VERIFIED / OVERRIDE ACCEPTED | Substantive, deployed, hash-bound, JPMorgan-specific, and explicitly accepted as an intentional alternative to the 0045-only contract. |
| `scripts/run-phase-03-9-rollout.mjs` | Immutable approval gate, finite rollout, cleanup, evidence assertion | ✓ VERIFIED | `--assert-evidence` validates all four manifests at their recorded commits, exact chain/order, rollout structure, activation, natural poll, protected sources, cleanup, and automated evidence without production mutation. |
| Four Phase 03.9 manifests | Frozen target/action/migration/function inventory | ✓ VERIFIED | Every recorded source commit exists and independently reproduces its commit, migration, entrypoint, and bundle hashes from Git objects. |
| Hosted, rollout, UAT, and override records | Exact-release production proof | ✓ VERIFIED WITH OVERRIDE | Hosted, rollout, and UAT assertions pass; UAT binds both evidence-file hashes. The owner accepted these records as sufficient without a separate historical approval attestation. |

### Key Link Verification

The generic key-link helper reported the conceptual PLAN labels as “source file not found”; those labels are not filesystem paths. Manual semantic verification found the following:

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Frozen JPMorgan identity | Oracle list/detail requests | Registry resolution and exact facet construction | ✓ WIRED | Registry re-resolves the frozen identity and dispatches the Oracle adapter; request and echo tests pass. |
| Oracle observation | Poll lifecycle and persistence | `PollObservation`, scope evidence, and `allowMissingClosure` | ✓ WIRED | Jobs and evidence are consumed by `poll-tick`; incomplete/rolling results cannot close missing jobs. |
| Exact migration authority | Observe/activation/poll RPCs | Exact source-key branches and service-role grants | ✓ WIRED | SQL definitions accept JPMorgan only under its literal source key and preserve replay/locking/caps. |
| Exact approval signal | Production rollout | Manifest validation, mutation gate, and owner evidence-sufficiency override | ✓ VERIFIED WITH OVERRIDE | Gate code rejects wrong approval, all historical hashes reproduce, and the owner accepted the retained chain as sufficient without claiming that the missing pre-mutation attestation exists. |
| Hosted active source | Owner UAT | Release ID plus hosted/rollout evidence hashes | ✓ WIRED | UAT references final release `...0004` and exact SHA-256 values of both evidence records. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Oracle adapter | Six slice requisition lists and detail jobs | Live Oracle `CX_1001` list/detail responses | Yes; hosted evidence records 8 pages, 113 detail jobs, six families | ✓ FLOWING |
| `poll-tick` | Normalized jobs and scope evidence | Registry-dispatched adapter observation | Yes; stored jobs are revalidated and written through shared ingestion | ✓ FLOWING |
| Dashboard preference feed | User-visible eligible job rows | Shared `jobs` → `user_jobs` → dashboard feed relation | Yes; UAT records a real eligible job and 113 eligible rows | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Exact identity, scope, adapter failures, lifecycle, poll, Phase 03.9/03.10 migrations, and latest frontier | `cd web && npm test -- branded-identities.test.ts branded-scope.test.ts branded-adapters.test.ts observe-connectors.test.ts poll-tick.test.ts phase-03-9-migration.test.ts phase-03-10-migration.test.ts paylocity-activation-frontier.test.ts` | 8 files, 232 tests passed | ✓ PASS |
| Phase 03.9 rollout and hosted verifier unit tests | `node --test scripts/run-phase-03-9-rollout.test.mjs scripts/verify-phase-03-9-hosted.test.mjs` | 14 passed, 0 failed | ✓ PASS |
| Documented rollout-evidence assertion | `node scripts/run-phase-03-9-rollout.mjs --assert-evidence .../03.9-01-ROLLOUT-VERIFICATION.json --manifest <each of four manifests>` | 4/4 selections printed exact Phase 03.9 PASS | ✓ PASS |
| Frozen release hashes | `validateManifest(..., { filesAtSourceCommit: true })` through the assertion path | 4/4 commit, migration, entrypoint, and bundle hash sets reproduced | ✓ PASS |
| Stored hosted and UAT records | `assertHostedRecord(...)` and `assertUatRecord(...)` | Both assertions passed against final manifests/evidence | ✓ PASS |

The former current-bundle drift failures are closed. Historical release checks now read migration, entrypoint, and bundle bytes from each manifest's recorded Git commit, while normal dry-run execution retains current-workspace validation.

### Probe Execution

No conventional `scripts/**/tests/probe-*.sh` probe is declared. The live Oracle/hosted probe was not rerun because this verification was explicitly bounded to local, non-network, non-production checks. The retained rollout record reports a successful six-slice live probe; its release hashes and downstream hosted/UAT bindings were independently checked as described above.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| DISC-01 | 03.9-01, 03.9-02 | Sources expose monitoring state and precise operational truth | ✓ SATISFIED | JPMorgan exact-key state, activation evidence, poll health, and UAT are present; failures degrade rather than overclaim completeness. |
| DISC-07 | 03.9-01, 03.9-02 | Deterministic selective-source identity/scope | ✓ SATISFIED | Exact site, U.S., seven-day, six-family identity and digest evidence are enforced and tested. |
| DISC-08 | 03.9-01, 03.9-02 | Safe monitoring lifecycle and reconciliation | ✓ SATISFIED | Complete slice/detail reconciliation, closure suppression, replay/locking, and health transitions are implemented and tested. |
| DISC-09 | 03.9-01, 03.9-02 | Eligible jobs enter the normal user pipeline | ✓ SATISFIED | Shared persistence/preferences remain in use; hosted and UAT records show real eligible jobs. |

`REQUIREMENTS.md` contains the requirement definitions, while the phase-to-requirement mapping is supplied by the ROADMAP and both PLAN frontmatters. No additional requirement is explicitly mapped to Phase 03.9 and orphaned from its plans.

### Anti-Patterns Found

| File | Line/Area | Pattern | Severity | Impact |
|---|---|---|---|---|
| `scripts/run-phase-03-9-rollout.mjs` | historical validation and evidence assertion | Prior stale-fixture/absent-command findings | ℹ️ Resolved | Historical bytes and the exact rollout record now validate read-only; drifted cleanup evidence is rejected by test. |
| Phase-modified implementation files | full scan | `TBD`, `FIXME`, `XXX`, user-visible placeholder, or empty implementation | ℹ️ None | No blocker debt markers or functional stubs found. |

### Human Decision Completed — Governance Override

At `2026-07-28T21:22:13Z`, the project owner supplied:

`accept Phase 03.9 historical repair override migrations=0046,0047 evidence=sufficient`

This accepts the exact forward-only repair chain and the retained deterministic
evidence as sufficient. It does not claim that a missing historical
pre-mutation approval attestation was later recovered.

### Gaps Summary

No implementation, behavioral, evidence, or governance gap remains. The
JPMorgan selective Oracle monitor is wired and data-flowing; all four manifest
selections, rollout evidence, hosted evidence, UAT, 232 focused web tests, and
14 rollout/hosted tests pass. The accepted override resolves the literal
0045-only deviation and the evidence-sufficiency decision while preserving the
fact that no separate historical pre-mutation attestation was retained.

---

_Verified: 2026-07-28T21:22:13Z_
_Verifier: the agent (gsd-verifier)_
