---
phase: 03.5-generic-workday-connector-fidelity
plan: 02
type: execute
wave: 2
depends_on: ["03.5-01"]
files_modified:
  - supabase/functions/_shared/detect.ts
  - supabase/functions/_shared/connectors.ts
  - supabase/functions/verify-board/index.ts
  - supabase/functions/poll-tick/index.ts
  - web/tests/detect.test.ts
  - web/tests/workday-connector.integration.test.ts
autonomous: true
requirements: [connector-expansion]
must_haves:
  truths:
    - "SC2: Pasting the Fidelity Form B URL makes verify-board detect Workday, parse tenant=fmr/region=wd1/site=FidelityCareers/hostForm=site, and admit it as an experimental source"
    - "SC2: Any URL that does not cleanly resolve to a live CXS endpoint (wrong host, lookalike host, extra/missing path segment, missing recruiting literal, search/hash present, http, creds, port) is rejected"
    - "SC1: Capital One Form A detection, verification, source-key derivation, and dispatch remain byte-behavior-identical"
    - "SC2: A non-allowlisted Workday tenant is rejected at both verify and poll (invalid_identity)"
  artifacts:
    - "detect.ts with a widened workday DetectResult variant and a new Form B (myworkdaysite) detection branch + identity-derived buildEndpoint"
    - "connectors.ts with widened region type and registry-driven verifyWorkday / pollConnector / deterministicSourceKey / providerRegistry.workday.poll"
    - "verify-board sourceKeyForDetection + poll-tick region type derived from the resolved identity"
    - "detect.test.ts Form B positives/negatives + integration verify/dispatch cases for Fidelity and a rejected tenant"
  key_links:
    - "detectAts Form B branch -> resolveWorkdayIdentity(tenant,region,site,'site') -> ConnectorVerification.sourceKey -> experimental companies insert"
    - "providerRegistry.workday.poll resolves identity from company fields -> pollWorkdayRecent(identity, ...) with hostForm carried so origin is reconstructed (A1)"
  prohibitions:
    - "NO suffix/wildcard/endsWith host matching — Form B host must exact-match {region}.myworkdaysite.com with an anchored region regex/allowlist (SSRF gate, detect.ts L21-23)"
    - "The Capital One tuple, region wd12, and its source key MUST stay byte-identical; add Fidelity as a second admitted path, never mutate the first"
    - "verify-board remains the authoritative auth+SSRF boundary (D-04) — do not weaken auth (Bearer + role==='authenticated') or the origin allowlist"
    - "NO supabase db push, NO supabase functions deploy, NO git push in this plan"
---

<objective>
Wire the parameterized adapter + identity registry into the live request path so a pasted Fidelity URL flows end-to-end: verify-board detects Workday (both URL shapes), parses the identity, verifies list-only against the CXS endpoint, and stages the company as experimental; and the scheduler dispatches Fidelity polls with the resolved identity. This is the thinnest end-to-end paste→verify→poll slice, proven by the integration test harness (no deploy — production boundary).

Purpose: Delivers the user-facing capability (SC2) — Fidelity is added through the existing Watchlist paste-URL UI with no new UI — while keeping Capital One and every other connector byte-behavior-identical (SC6).

Output: generalized detect.ts / connectors.ts / verify-board / poll-tick, plus Form B detection tests and Fidelity verify/dispatch integration coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-CONTEXT.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-RESEARCH.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-PATTERNS.md
@supabase/functions/_shared/workday-identities.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Form B detection + Fidelity verify/dispatch + rejection tests</name>
  <files>web/tests/detect.test.ts, web/tests/workday-connector.integration.test.ts</files>
  <read_first>
    - web/tests/detect.test.ts — Capital One positive L41-52, negative host cases L54-63, buildEndpoint workday case L134-140
    - web/tests/workday-connector.integration.test.ts — verify cases L75-99, exact-identity dispatch guard L124-164, facet fail-closed L506-521
    - supabase/functions/_shared/detect.ts — DetectResult union L3-15, UNSUPPORTED_URL_MESSAGE L17-18, exact-host SSRF comment L21-23, strictSlug L40, Capital One branch L73-83, Paylocity multi-segment branch L85-100, buildEndpoint L161-187 (workday line L183-184)
    - RESEARCH.md § 2 (detection) and § 6 (tests); PATTERNS.md § detect.ts assignments
  </read_first>
  <behavior>
    - detectAts('https://wd1.myworkdaysite.com/en-US/recruiting/fmr/FidelityCareers') returns { ats:'workday', slug:'fmr', region:'wd1', site:'FidelityCareers', hostForm:'site' }
    - detectAts('https://capitalone.wd12.myworkdayjobs.com/Capital_One') still returns the frozen { ats:'workday', slug:'capitalone', region:'wd12', site:'Capital_One', hostForm:'jobs' }
    - Form B negatives all return unsupported: lookalike host 'wd1.myworkdaysite.com.evil.example', missing/extra path segment, 'recruiting' literal absent, url.search present, url.hash present, http scheme, embedded creds, explicit port
    - buildEndpoint for Fidelity derives https://wd1.myworkdaysite.com/wday/cxs/fmr/FidelityCareers/jobs
    - Integration: verify-board handler on the Fidelity URL returns a verification with sourceKey 'workday:wd1:fmr:FidelityCareers' and a jobCount, and stages activation_state 'experimental'
    - Integration: a non-allowlisted Workday tenant URL is rejected (invalid_identity) at verify AND poll (mirror exact-identity guard L124-164)
  </behavior>
  <action>
    Add failing cases. In detect.test.ts add the Form B positive, the Capital One hostForm regression, all Form B negatives (mirror the negative style at L54-63 including the lookalike-host case), and the updated Fidelity buildEndpoint expectation. In workday-connector.integration.test.ts add: a Fidelity verify case (list-only fixture → sourceKey + jobCount + experimental staging) and a "non-allowlisted Workday tenant rejected at verify and poll" case (mirror L124-164). These assert on behavior not yet implemented, so the suites MUST fail (RED). Do not weaken existing Capital One cases.
  </action>
  <acceptance_criteria>
    - `cd web && npm run test -- detect workday-connector` FAILS on the new Form B / Fidelity / rejection cases (RED confirmed)
    - Negative cases include the lookalike-host and search/hash/http/creds/port variants
    - No existing Capital One assertion is edited
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test -- detect workday-connector 2>&1 | grep -qiE 'fail|✗|hostForm|FidelityCareers' && echo RED-OK</automated>
  </verify>
  <done>Form B detection, Fidelity verify/dispatch, and non-allowlisted-tenant rejection cases exist and fail pending implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teach detect.ts both Workday URL shapes (Form B branch)</name>
  <files>supabase/functions/_shared/detect.ts</files>
  <read_first>
    - supabase/functions/_shared/detect.ts — DetectResult union L3-15 (workday literal variant L9-14), UNSUPPORTED_URL_MESSAGE L17-18, exact-host SSRF comment L21-23, strictSlug L40, Capital One branch L73-83, Paylocity multi-segment parse L85-100, buildEndpoint L161-187 (workday line L183-184)
    - supabase/functions/_shared/workday-identities.ts — resolveWorkdayIdentity as the admission gate
    - RESEARCH.md § 2 Anti-pattern "Suffix/wildcard host matching"
  </read_first>
  <action>
    Widen the DetectResult workday variant (L9-14) from the literal 'capitalone'/'wd12'/'Capital_One' to { ats:'workday'; slug:string; region:string; site:string; hostForm:'jobs'|'site' } (values still validated by allowlist/regex, not free strings). Keep the Form A branch (L73-83) but return hostForm:'jobs' and validate region as /^wd\d{1,3}$/ (Capital One still resolves byte-identical). Add a Form B branch modeled on the Paylocity multi-segment parse (L85-100): exact-match host {region}.myworkdaysite.com with an anchored region regex (never endsWith / never suffix), parse path segments requiring exactly /{locale}/recruiting/{tenant}/{site} (4 segments, literal 'recruiting' at index 1, strictSlug tenant + site via L40), reject url.search and url.hash, and return { ats:'workday', slug:tenant, region, site, hostForm:'site' }. Gate final admission through resolveWorkdayIdentity(slug,region,site,hostForm) so an unadmitted tenant returns unsupported (fail-closed). Update buildEndpoint's workday line (L183-184) to derive `${identity.cxsRoot}/jobs` from the resolved identity. Update UNSUPPORTED_URL_MESSAGE (L17-18) copy from "the allowlisted Capital One Workday board" to generalized Workday support wording.
  </action>
  <acceptance_criteria>
    - Fidelity Form B URL detects to slug=fmr/region=wd1/site=FidelityCareers/hostForm=site; Capital One Form A unchanged with hostForm=jobs
    - Lookalike host wd1.myworkdaysite.com.evil.example and all Form B negatives return unsupported
    - buildEndpoint derives the Fidelity CXS /jobs URL from the identity, no Capital One literal
    - Host matching uses exact host comparison, not endsWith/suffix
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test -- detect 2>&1 | tail -5</automated>
  </verify>
  <done>detect.ts recognizes both Workday URL shapes, fail-closed via the identity registry, with the SSRF exact-host gate intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — registry-driven connectors dispatch + edge region widening</name>
  <files>supabase/functions/_shared/connectors.ts, supabase/functions/verify-board/index.ts, supabase/functions/poll-tick/index.ts</files>
  <read_first>
    - supabase/functions/_shared/connectors.ts — region types L29 (PollConnectorCompany) / L38 (ConnectorVerification), canonicalCareersUrl workday L69-71, deterministicSourceKey L75-83, verification()/siteToken L94, verifyWorkday L177-189, providerRegistry L202-235 (workday.poll L233), pollConnector workday re-check L254-261, Paylocity analog blocks L157-175 / L262-270
    - supabase/functions/verify-board/index.ts — sourceKeyForDetection L111-117 (workday L112), stagedActivationProviders L79-84, insert L256-274, observation L292, auth/SSRF boundary L219-235
    - supabase/functions/poll-tick/index.ts — Company.region L18, companyName ?? company.name L233, knownIds L201-205, pollConnector call L206, diagnosticCode L34-42
    - supabase/functions/_shared/workday-identities.ts and the parameterized supabase/functions/_shared/adapters/workday.ts (pollWorkdayRecent, verifyWorkdayListing signatures)
  </read_first>
  <action>
    Relax the three pinned Workday checks in connectors.ts by consulting resolveWorkdayIdentity (mirror the Paylocity registry-driven blocks L157-175 / L262-270): (1) deterministicSourceKey (L75-83) derives workday:{region}:{tenant}:{site} from the resolved identity (Capital One key stays byte-identical); (2) verifyWorkday (L177-189) resolves identity from detected, throws 'invalid_identity' when null, passes the identity into verifyWorkdayListing, and uses identity.companyName; (3) pollConnector workday re-check (L254-261) becomes an allowlist check via the registry. Update canonicalCareersUrl (L69-71) and verification() siteToken (L94) to derive from identity. Change providerRegistry.workday.poll (L233) to resolve identity from the company fields (tenant/region/site/hostForm) and call pollWorkdayRecent(identity, fetch, { knownIds }) — carrying hostForm so the origin is reconstructed (A1). Widen the region types: connectors.ts L29 and L38 and poll-tick Company.region L18 from 'eu'|'wd12'|null to admit wd1 (use `string | null`). In verify-board, change sourceKeyForDetection workday line (L112) to derive from the resolved identity (mirror Paylocity L113-115); leave stagedActivationProviders, the experimental insert, auth, and SSRF boundary unchanged (they already pass region/siteToken through). poll-tick otherwise reuses companyName ?? company.name (L233) and diagnosticCode (L34-42) as-is.
  </action>
  <acceptance_criteria>
    - `cd web && npm run test -- detect workday-connector` PASSES including Fidelity verify + non-allowlisted-tenant rejection + Capital One regression (GREEN)
    - Capital One deterministicSourceKey still returns 'workday:wd12:capitalone:Capital_One'; exact-identity dispatch guard case stays green
    - verifyWorkday throws invalid_identity for an unadmitted tenant; pollConnector rejects the same tenant
    - region type admits wd1 in connectors.ts and poll-tick; no 'eu'|'wd12' narrowing remains on the workday path
    - verify-board auth (Bearer + role==='authenticated') and SSRF origin allowlist unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test 2>&1 | tail -8</automated>
  </verify>
  <done>End-to-end paste→verify→dispatch works for Fidelity through the registry while Capital One and all other connectors stay byte-behavior-identical; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → verify-board | Authenticated, authoritative SSRF/validation boundary (D-04); pasted URL is untrusted |
| detect.ts host/path parse → identity admission | Only exact-host + admitted-tuple URLs may become pollable identities |
| pg_cron dispatch → poll-tick → connectors | Identity re-checked at dispatch (defense-in-depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-02 | Spoofing | detect.ts Form B host match | high | mitigate | Exact-host match on {region}.myworkdaysite.com with anchored region regex; never endsWith; reject lookalike hosts (test asserts wd1.myworkdaysite.com.evil.example rejected) |
| T-03.5-03 | Elevation of privilege | connectors verifyWorkday / pollConnector | high | mitigate | Both resolve identity via registry and reject (invalid_identity) any tuple not admitted; verify-board live-probe gate precedes admission |
| T-03.5-06 | Tampering | verify-board auth/SSRF boundary | high | mitigate | Auth (Bearer + role==='authenticated') and origin allowlist / redirect:'error' preserved unchanged; only identity derivation is generalized |
| T-03.5-07 | Elevation of privilege | region type widening | medium | mitigate | Region widened to admit wd1 but admission still gated by resolveWorkdayIdentity fail-closed, not by the type alone |
| T-03.5-SC | Tampering | npm/deno installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- `cd web && npm run test` full suite green
- Fidelity Form B URL admitted experimental; lookalike/malformed URLs rejected
- Capital One detection/verify/dispatch assertions unchanged
- grep confirms no endsWith host matching added to detect.ts workday branches
</verification>

<success_criteria>
Maps to ROADMAP SC1 (both URL shapes detected), SC2 (paste→verify→admit; non-CXS rejected; non-allowlisted tenant rejected), SC5 (experimental staging via existing staged-activation path), and SC6 (Capital One + others unchanged).
</success_criteria>

<artifacts_produced>
- MODIFIED `detect.ts`: widened workday DetectResult variant with hostForm; new Form B (myworkdaysite) detection branch; identity-derived buildEndpoint; generalized UNSUPPORTED_URL_MESSAGE
- MODIFIED `connectors.ts`: widened region type; registry-driven deterministicSourceKey / verifyWorkday / pollConnector / canonicalCareersUrl / providerRegistry.workday.poll
- MODIFIED `verify-board/index.ts`: identity-derived sourceKeyForDetection
- MODIFIED `poll-tick/index.ts`: widened Company.region
- MODIFIED tests: detect.test.ts Form B cases; workday-connector.integration.test.ts Fidelity verify + rejected-tenant cases
</artifacts_produced>

<output>
Create `.planning/phases/03.5-generic-workday-connector-fidelity/03.5-02-SUMMARY.md` when done
</output>
