---
context: phase
phase: 03.5-generic-workday-connector-fidelity
task: 0
total_tasks: 0
status: paused
last_updated: 2026-07-24T04:20:25.051Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These preserve the exact-approved production release and prevent silent connector failures.

- [ ] CONSTRAINT: Exact-release boundary — production is commit `7642fde8bdc55112f50f847a7e65fd39af1d7f64`; local HEAD is ahead with unpushed planning/docs. No `supabase functions deploy`, no push, no deploy without a NEW exact-release owner approval. Planning/execution of code is fine; shipping is not.
- [ ] CONSTRAINT: Capital One Workday key `workday:wd12:capitalone:Capital_One` is frozen byte-identically across 6 files + migration parity DO-blocks + tests. Generalization must add Fidelity as a SECOND registry entry — never mutate the Capital One tuple.

**Do not proceed to deploy/push until a new exact-release approval exists.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Region-check silent rejection | `companies_region_check` + TS type `region: 'eu'\|'wd12'\|null` (connectors.ts L29/L38, poll-tick L18, DB) reject Fidelity's `wd1`. Fidelity would silently never poll. | blocking | Migration 0035 + all four TS sites must widen region to admit `wd1` before Fidelity can be admitted. |
| Catalog INSERT instead of UPDATE | Fidelity row already exists in `source_coverage_catalog` as `unsupported_with_reason` (0013 L176-184) with UNIQUE `company_name` + `truthful_disposition_check`. An INSERT violates both. | blocking | Migration 0035 must UPDATE the existing Fidelity row, not INSERT. |
| Host-form ambiguity (A1) | Source-key `workday:{region}:{tenant}:{site}` cannot tell Form A `myworkdayjobs` from Form B `myworkdaysite` by region alone. Wrong origin = 404 on every poll. | blocking | Registry keys on the full tuple and carries `hostForm`/origin; planner must pick + persist the disambiguator. |
| Editing a deployed migration | Retry defect history: 0034 was forward-only after 0033 shipped. | advisory | New migration 0035 only; never edit 0016/0028/0029/0033/0034. |
| Post-fetch facet exclusion | Would need 706 detail fetches/poll (list rows carry no jobFamilyGroup) — blows egress + ~150s wall-clock. | advisory | Use INCLUSION facets for kept families (research Option b); discover facet IDs live + count-verify, reuse `category_filter_unverified` fail-closed. |

<current_state>
Phase 03.5 is post-research, pre-planning. CONTEXT.md (committed 6d48abf), RESEARCH.md, and PATTERNS.md are all written and on disk. The gsd-planner has NOT been spawned — user asked to pause after research. UI-SPEC gate was deliberately skipped (no new UI). No code has been touched; this is all planning-artifact work.
</current_state>

<completed_work>

Completed Tasks:
- Insert Phase 03.5 into ROADMAP.md + fill real goal/success criteria — Done
- Discuss phase → CONTEXT.md + DISCUSSION-LOG.md — Done (commit 6d48abf)
- gsd-phase-researcher → RESEARCH.md — Done (HIGH confidence)
- gsd-pattern-mapper → PATTERNS.md — Done (10/10 analogs, corroborates research)
</completed_work>

<remaining_work>

- Spawn gsd-planner (RESEARCH + PATTERNS ready) → PLAN.md files
- gsd-plan-checker verify loop (max 3 iterations)
- Execute Phase 03.5 (later, after owner approval)
</remaining_work>

<decisions_made>

- Generalize to the two known Workday URL shapes only; reject unresolvable URLs (D-01/D-02)
- Add via existing Watchlist paste-URL UI; verify-board auto-detects + parses (D-03/D-04)
- Category-scoped ingestion via INCLUSION facets, exclude Sales/Customer Service/Sales Support; experience-<3yr stays downstream (D-05/D-06/D-07)
- Fidelity matches Capital One activation model; migration 0035 admits new Workday identities (D-08)
- Plan with `--skip-ui`
</decisions_made>

<blockers>
- None. Production-deploy boundary is a constraint, not a blocker to planning.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03.5-generic-workday-connector-fidelity/03.5-CONTEXT.md` — locked decisions + live-verified endpoint facts
2. `.planning/phases/03.5-generic-workday-connector-fidelity/03.5-RESEARCH.md` — seams by line number, migration sequencing, facet decision, landmines
3. `.planning/phases/03.5-generic-workday-connector-fidelity/03.5-PATTERNS.md` — analog files + code excerpts (provider-identities.ts / Paylocity is the identity-registry template)

## Infrastructure State
- No project server or watcher required. Colima may still be running from earlier disposable protocol verification — not needed.
- Fidelity CXS endpoint live-verified (706 jobs, no bot wall) as of 2026-07-23.

<context>
This is a lift-literals-to-parameters refactor, not new mechanism-building. The Capital One Workday path already contains everything Fidelity needs; generalization means parameterizing origin/tenant/site (+ facet config) and adding Fidelity as a second identity-registry entry. Research + pattern-mapper independently agree on the seams and the four landmines above.
</context>

<next_action>
Start with: `/gsd-plan-phase 03.5 --skip-ui` — RESEARCH.md + PATTERNS.md already exist so it skips research/pattern-mapper and spawns gsd-planner directly. `--skip-ui` is REQUIRED (otherwise the blocking UI-SPEC gate re-fires). Then let the planner → plan-checker verify loop run.
</next_action>
