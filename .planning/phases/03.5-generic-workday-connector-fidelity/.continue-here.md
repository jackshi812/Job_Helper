---
context: phase
phase: 03.5-generic-workday-connector-fidelity
task: 0
total_tasks: 3
status: paused
last_updated: 2026-07-24T15:14:12.249Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These preserve the exact-approved production release and prevent silent connector failures.

- [ ] CONSTRAINT: Exact-release boundary — production is commit `7642fde8bdc55112f50f847a7e65fd39af1d7f64`. Wave 3 plan 03.5-04 (`autonomous:false`) applies migration 0035 and deploys verify-board + poll-tick. NO `supabase functions deploy`, NO `supabase db push`, NO push/apply without a NEW exact-release owner approval. Waves 1-2 are code-only and already committed; shipping is not done.
- [ ] CONSTRAINT: Capital One Workday key `workday:wd12:capitalone:Capital_One` stayed frozen byte-identically. Any further change must keep it so — Fidelity is a SECOND registry entry, never a mutation of the Capital One tuple.

**Do not proceed to deploy/apply/push until a new exact-release approval exists.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Region-check silent rejection | `region` domain was `eu\|wd12\|null`; Fidelity's `wd1` would be silently dropped and never poll. | blocking | Already widened in waves 1-2: migration 0035 `companies_region_check` + all four TS sites (connectors.ts L29/L38, poll-tick L18, detect variant) admit `wd1`. Do not narrow it back. |
| Catalog INSERT instead of UPDATE | Fidelity row already exists in `source_coverage_catalog` (0013 L176-184); INSERT violates UNIQUE + truthful_disposition_check. | blocking | Migration 0035 UPDATEs the row (unsupported→experimental). Never INSERT. |
| Host-form ambiguity (A1) | `workday:{region}:{tenant}:{site}` cannot tell Form A `myworkdayjobs` from Form B `myworkdaysite`; wrong origin = 404 every poll. | blocking | Registry carries `hostForm`/origin explicitly; detect.ts resolves both URL shapes via `resolveWorkdayIdentity`; origin never re-derived from region. Preserve this. |
| Editing a deployed migration | Retry defect history: 0034 was forward-only after 0033 shipped. | advisory | 0035 is new/forward-only; never edit 0013/0016/0028/0029/0033/0034. |
| Post-fetch facet exclusion | 706 detail fetches/poll blows egress + ~150s wall-clock. | advisory | Inclusion facets for kept families + fail-closed count reconciliation (`category_filter_unverified`). Already implemented in 03.5-01. |

<current_state>
Phase 03.5 is post-execution of Waves 1-2 — code complete, paused before the deploy checkpoint. 3 of 4 plans done and committed on branch `main` (13 commits, daef547..dd06431). Full web suite green (996 tests / 48 files), `tsc -b` clean. Migration 0035 is AUTHORED but NOT applied to any database. Nothing deployed or pushed. ROADMAP shows 3/4 In Progress. Ran sequentially on the main working tree (no worktree isolation) because origin/HEAD is unresolved (#683 auto-degrade).
</current_state>

<completed_work>

Completed Plans:
- 03.5-01: Frozen WorkdayIdentity registry + parameterized adapter + Fidelity inclusion-facet scoping — Done (SUMMARY on disk, commit f5638e9)
- 03.5-03: Author forward-only migration 0035 (region wd1, catalog UPDATE, auto-promotion) + watchlist Fidelity note — Done (commit bf5c7c9)
- 03.5-02: Wire detect.ts (both URL shapes) + connectors.ts + verify-board + poll-tick to the registry; widened edge region types — Done (commit dd06431)
</completed_work>

<remaining_work>

- 03.5-04 (Wave 3, `autonomous:false` — DEPLOY CHECKPOINT): apply migration 0035, deploy verify-board + poll-tick, live-verify Fidelity works and Capital One / other connectors / deterministic feed unchanged. GATED on new owner exact-release approval.
- Phase verification: spawn gsd-verifier once 03.5-04 completes (or verify code-only waves 1-2 if deploy stays deferred).
</remaining_work>

<decisions_made>

- Ran sequentially on `main` (no worktree isolation) — origin/HEAD unresolved, base-check shouldDegrade=true (#683).
- Stayed on `main` — branching_strategy=none.
- Paused after Wave 2, before the Wave 3 deploy — user request + natural owner-approval boundary.
</decisions_made>

<blockers>
- Wave 3 (03.5-04) is blocked on a NEW exact-release owner approval to cross the production deploy boundary. Not a code blocker — waves 1-2 are complete and green.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable resume state (start here)
2. `.planning/phases/03.5-generic-workday-connector-fidelity/03.5-04-PLAN.md` — the deploy checkpoint plan (what Wave 3 does)
3. `.planning/phases/03.5-generic-workday-connector-fidelity/03.5-01-SUMMARY.md`, `03.5-02-SUMMARY.md`, `03.5-03-SUMMARY.md` — exact seams/values landed in waves 1-2

## Infrastructure State
- No project server/watcher required. Migration 0035 authored, not applied. Nothing deployed. Production remains at exact-release commit 7642fde8.
- Fidelity CXS endpoint was live-verified (706 jobs, no bot wall) as of 2026-07-23.

<context>
Waves 1-2 delivered the full paste→verify→poll slice for Fidelity as a code-only change, parameterizing the Workday path over the identity registry with Capital One frozen. Everything is proven via the integration test harness only. The single remaining piece is the production deploy (03.5-04), which is intentionally owner-gated.
</context>

<next_action>
Start with: obtain a fresh owner exact-release approval. Once granted, run `/gsd-execute-phase 03.5 --wave 3` to execute 03.5-04 (apply migration 0035 + deploy verify-board/poll-tick + live-verify), then let gsd-verifier run. If deploy stays deferred, optionally spawn gsd-verifier now to verify the code-only waves. Do NOT deploy/apply/push without approval.
</next_action>
