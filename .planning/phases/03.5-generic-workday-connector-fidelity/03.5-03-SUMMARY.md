---
phase: 03.5-generic-workday-connector-fidelity
plan: 03
subsystem: db-migration
tags: [workday, fidelity, migration, postgres, allowlist, source-coverage-catalog, watchlist, tdd]

# Dependency graph
requires:
  - phase: 03.5-01 generalized-workday-connector-spine
    provides: frozen WorkdayIdentity registry with FIDELITY_WORKDAY_SOURCE_KEY = workday:wd1:fmr:FidelityCareers (tenant fmr / region wd1 / site FidelityCareers / Form B origin)
  - migration: 0028 capital_one_recent_active
    provides: single-tenant Workday identity guardrail + promotion trigger (relaxed here, not edited)
  - migration: 0029 paylocity_connector
    provides: latest record_connector_observation + claim_due_companies definitions (rebuilt via CREATE OR REPLACE here)
provides:
  - Forward-only migration 0035 admitting a fixed two-entry Workday allowlist (Capital One byte-identical + Fidelity) at the DB tier
  - Widened companies_region_check admitting any workday ^wd\d+$ region
  - workday added to the RPC 3-window stable-promotion set (Fidelity auto-promotes at progress=3; Capital One trigger untouched)
  - claim_due_companies workday disjunct widened to the source-key allowlist
  - Fidelity source_coverage_catalog row UPDATEd (never INSERTed) from unsupported_with_reason to experimental
  - Fidelity filter note in watchlist activationPresentation keyed on the Fidelity source key
affects: [03.5-04 owner-gated migration application/deploy, verify-board Fidelity admission, dashboard downstream experience filters]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Forward-only relaxation: ALTER ... DROP/ADD CONSTRAINT + CREATE OR REPLACE only; never edit a deployed migration file"
    - "Fixed N-entry identity allowlist (disjunction of exact tuples) replaces single-tenant guardrail; arbitrary tenants still rejected at DB tier"
    - "Catalog disposition change via UPDATE (UNIQUE company_name + truthful_disposition_check) — never INSERT"
    - "?raw SQL parity test asserting migration contract without applying it against a DB"

key-files:
  created:
    - supabase/migrations/0035_generic_workday_fidelity.sql
    - web/tests/migration-0035-parity.test.ts
  modified:
    - web/src/lib/watchlist.ts
    - web/src/lib/watchlist.test.ts

key-decisions:
  - "Auto-promotion realized via D-08 option (a): add 'workday' to the record_connector_observation stable-promotion set so admitted Workday tenants promote at progress=3 using the existing 30-minute window; Capital One's dedicated promote_capital_one_after_observation trigger is left in place (its RPC promotion is now redundant but idempotent — both converge on 'active')"
  - "claim_due_companies workday disjunct admits the allowlist by source_key IN (...) rather than repeating the full per-tuple board/region/site conjunction — keeps the two admitted identities claimable and rejects any other Workday tenant"
  - "Fidelity catalog UPDATE also refreshes access_evidence (the prior Cloudflare-challenge text was no longer truthful post live-verification) and careers_url to the Form B recruiting board, both satisfying the 0013 careers_url regex"

patterns-established:
  - "Parity DO-block asserts BOTH admitted identities (Capital One + Fidelity) present in the constraint def and BOTH catalog rows at their pinned experimental disposition, failing the migration transactionally on drift"

requirements-completed: [connector-expansion]

coverage:
  - id: SC3
    description: "Forward-only migration 0035 relaxes the 0028 single-identity guardrail to an allowlist keeping Capital One byte-identical and adding workday:wd1:fmr:FidelityCareers, widening the region check to admit wd1, without editing any deployed migration"
    requirement: connector-expansion
    verification:
      - kind: integration
        ref: "web/tests/migration-0035-parity.test.ts#relaxes the identity check to an allowlist / relaxes companies_region_check"
        status: pass
    human_judgment: false
  - id: SC5
    description: "Fidelity auto-promotes from experimental to active after 3 clean observation windows (workday in the RPC promotion set), preserving Capital One's promotion; Fidelity catalog row UPDATEd to experimental with its source key"
    requirement: connector-expansion
    verification:
      - kind: integration
        ref: "web/tests/migration-0035-parity.test.ts#wires Fidelity into the 3-window auto-promotion / UPDATEs (never INSERTs) the Fidelity catalog row"
        status: pass
    human_judgment: false
  - id: SC4
    description: "Watchlist shows a truthful Fidelity filter note (excludes Sales/Customer Service/Sales Support; experience filtered downstream) and reuses existing OK/Degraded/Unsupported health badges"
    requirement: connector-expansion
    verification:
      - kind: unit
        ref: "web/src/lib/watchlist.test.ts#shows a Fidelity filter note keyed on the Fidelity source key and reuses existing badges"
        status: pass
    human_judgment: false

# Metrics
duration: 5min
completed: 2026-07-24
status: complete
---

# Phase 03.5 Plan 03: Generic Workday / Fidelity DB Admission Summary

**Forward-only migration 0035 relaxes the 0028 single-tenant Workday guardrail to a fixed two-entry allowlist (Capital One byte-identical + Fidelity), widens the region check to any `^wd\d+$`, wires Fidelity into the 3-window auto-promotion, and UPDATEs Fidelity's catalog row to experimental — authored and committed, never applied.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-07-24T14:55:16Z
- **Completed:** 2026-07-24T14:59:58Z
- **Tasks:** 3
- **Files:** 4 (2 created, 2 modified)

## Accomplishments
- New `supabase/migrations/0035_generic_workday_fidelity.sql` (forward-only): relaxes `companies_region_check` to admit any workday `^wd\d+$` region (keeps `lever/eu` and null); relaxes `companies_workday_identity_check` to a two-tuple allowlist admitting the exact Capital One tuple byte-identically AND Fidelity (`board_token fmr` / `region wd1` / `site_token FidelityCareers` / `source_key workday:wd1:fmr:FidelityCareers`), both `activation_state in ('experimental','active')`.
- Rebuilt `record_connector_observation` (CREATE OR REPLACE, verbatim from 0029 except one line) adding `'workday'` to the progress=3 stable-promotion set so admitted Workday tenants auto-promote after three clean 30-minute windows; the Capital One `promote_capital_one_after_observation` trigger is left intact (not redefined/dropped).
- Rebuilt `claim_due_companies` widening the workday disjunct to `source_key in ('workday:wd12:capitalone:Capital_One','workday:wd1:fmr:FidelityCareers')` so both admitted identities become claimable once active; no other Workday tenant is claimable.
- UPDATEd (never INSERTed) the existing Fidelity `source_coverage_catalog` row from `unsupported_with_reason` to `experimental` with `source_key = workday:wd1:fmr:FidelityCareers`, `unsupported_reason = null`, `provider = Workday`, and the Form B recruiting `careers_url` — satisfying `truthful_disposition_check` and the careers_url regex.
- Parity DO-block asserts both admitted identities are present in the constraint def, the region check admits wd-regions, and both Capital One and Fidelity catalog rows sit at their pinned experimental disposition — failing the migration transactionally on drift. The 0028 zero-pre-existing-workday-jobs guard is deliberately omitted (Capital One is live in prod — A5).
- `web/src/lib/watchlist.ts`: added a Fidelity filter note in `activationPresentation` keyed on the Fidelity source key (excludes Sales / Customer Service / Sales Support; experience applied by downstream dashboard filters). Capital One note byte-identical; existing OK/Degraded/Unsupported badges reused.

## Task Commits

Each task committed atomically (TDD RED → GREEN):

1. **Task 1: RED — migration 0035 ?raw parity test** — `afdbacc` (test)
2. **Task 2: Author migration 0035 (forward-only, not applied)** — `27c6e0d` (feat)
3. **Task 3 RED — Fidelity filter-note presentation test** — `ec64995` (test)
4. **Task 3 GREEN — Fidelity filter note in watchlist.ts** — `5a3cb0f` (feat)

_TDD gate sequence present per feature: test (RED) → feat (GREEN)._

## Files Created/Modified
- `supabase/migrations/0035_generic_workday_fidelity.sql` (created) — forward-only region/identity relaxation, workday-in-promotion-set RPC rebuild, widened claim_due_companies, Fidelity catalog UPDATE, parity DO-block.
- `web/tests/migration-0035-parity.test.ts` (created) — 8 `?raw` parity assertions: identity allowlist keeps Capital One + adds Fidelity, region admits `^wd\d+$`, no zero-jobs guard, workday in promotion set, claim widened, catalog UPDATE-not-INSERT, Capital One parity pins.
- `web/src/lib/watchlist.ts` (modified) — Fidelity filter note branch in `activationPresentation`.
- `web/src/lib/watchlist.test.ts` (modified) — Fidelity presentation test + `WatchlistRow` type import.

## Decisions Made
- **Promotion via RPC set, not a new trigger (D-08 option a):** adding `'workday'` to the shared stable-promotion `case` reuses the existing 30-minute Workday windowing and covers Fidelity without duplicating Capital One's dedicated trigger. Both paths land on `'active'` idempotently, so Capital One's runtime behavior is unchanged.
- **source_key IN(...) for the claim disjunct:** keeps the allowlist enumerable and readable; still exact-match and closed to any other Workday tenant.
- **Truthful catalog refresh:** the Fidelity row's prior `access_evidence` (Cloudflare-challenge text) and `careers_url` were stale after live verification, so the UPDATE refreshes both alongside the disposition to keep `source_coverage_catalog` truthful.

## Deviations from Plan
None — plan executed exactly as written. All four blocking constraints honored: 0035 is forward-only/new (no deployed migration edited), region check widened to admit wd1 while retaining eu/null, the Fidelity catalog row was UPDATEd (never INSERTed), and the Capital One tuple/key/trigger/catalog row remain byte-identical. No zero-workday-jobs guard was copied.

## Known Stubs
None.

## Threat Flags
None — no new trust-boundary surface beyond the intended DB-tier allowlist relaxation, which is the plan's stated mitigation for T-03.5-03/08/09.

## Issues Encountered
None. Full web suite green (980 tests / 48 files) and `tsc -b` clean after Task 3.

## User Setup Required
None. **CRITICAL:** Migration 0035 is authored and committed only — it was NOT applied to any database, no `supabase db push`, no deploy/push performed. Application/deployment remains gated on a separate exact-release owner approval (owner-gated Plan 04). The production exact-release boundary (prod commit `7642fde8`) is intact.

## Next Phase Readiness
- 0035 is the DB-tier admission gate that Plan 04 applies (under owner approval) alongside the rest of the phase's deploy. Until then Fidelity cannot be admitted into `public.companies` or claimed.
- The connector spine (03.5-01) and this migration together mean verify-board can, once 0035 is applied, resolve and admit the Fidelity identity end-to-end.

## Self-Check: PASSED

- `supabase/migrations/0035_generic_workday_fidelity.sql` exists on disk.
- `web/tests/migration-0035-parity.test.ts` exists on disk.
- Commits `afdbacc`, `27c6e0d`, `ec64995`, `5a3cb0f` present in git history.
- `git status --porcelain supabase/migrations/` shows no deployed migration modified (only 0035 added, now committed).
- Full web suite green (980 passed / 48 files); `tsc -b` clean.

---
*Phase: 03.5-generic-workday-connector-fidelity*
*Completed: 2026-07-24*
