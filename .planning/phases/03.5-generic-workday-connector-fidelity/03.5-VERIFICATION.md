---
phase: 03.5-generic-workday-connector-fidelity
verified: 2026-07-24T21:19:02Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 03.5: Generic Workday Connector & Fidelity Verification Report

**Phase Goal:** As a job seeker, I want to paste a Workday career URL and be able to receive job opportunities from the URL's company, so that I can monitor that company's opportunities without repeatedly checking its career site.
**Verified:** 2026-07-24T21:19:02Z
**Status:** passed
**Re-verification:** No — initial authoritative verification after the MVP user-story guard was corrected

## User Flow Coverage

User story: “As a job seeker, I want to paste a Workday career URL and be able to receive job opportunities from the URL's company, so that I can monitor that company's opportunities without repeatedly checking its career site.”

| Step | Expected | Evidence | Status |
|---|---|---|---|
| Open Watchlist | The existing Watchlist exposes the same add-company URL flow used by other supported sources. | `web/src/lib/watchlist.ts:218-242` validates the URL and invokes `verify-board`; the existing Watchlist UI consumes this helper. | ✓ VERIFIED |
| Paste the Fidelity URL | The exact Fidelity Form B URL is recognized as Workday and parsed as tenant `fmr`, region `wd1`, site `FidelityCareers`, host form `site`. | `detect.ts:106-127`; exact positive and malformed/lookalike negative cases in `detect.test.ts:50-87`. | ✓ VERIFIED |
| Verify and add | The authenticated server verifies the live CXS listing and creates or reuses the exact Fidelity company as Experimental. | `verify-board/index.ts:222-308` preserves authenticated verification, server insert, and observation recording; `workday-connector.integration.test.ts:1303-1635` covers live-path staging and rejected tenants. | ✓ VERIFIED |
| Activate safely | Exactly three clean, distinct, server-timed observations promote Fidelity to Active; no fourth observation or timestamp manipulation is used. | `0035_generic_workday_fidelity.sql:54-247` implements the locked three-window transition. Hosted evidence records 705/705 clean observations at 18:30Z, 19:00Z, and 19:30Z, then Active with `activation_successes=3`. | ✓ VERIFIED |
| Receive scheduled opportunities | Natural scheduled polling claims the exact active identity, applies the Fidelity family scope, and persists jobs. | `claim_due_companies` → `poll-tick` → `pollConnector` → `pollWorkdayRecent` is wired. Final natural pg_cron at 20:58Z succeeded with 114 observed and 113 detailed/open Fidelity jobs. | ✓ VERIFIED |
| Outcome | The user can monitor Fidelity opportunities in the existing feed without manually checking the career site. | Every job has the exact Fidelity company relation; the feed resolves relation-first at `web/src/lib/feed.ts:121-129`. Final evidence reports zero excluded-family rows, zero bad fingerprints, and healthy source state. | ✓ VERIFIED |

The live Watchlist admission, activation, and scheduled-poll sequence was already completed under exact production approvals and documented in `03.5-04-SUMMARY.md` and the clean review records. No redundant production mutation or user action was performed during verification.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | The Workday adapter is parameterized over origin/tenant/site and correctly polls both supported Workday URL forms while Capital One remains unchanged. | ✓ VERIFIED | Frozen identities in `workday-identities.ts:20-80`; identity-derived CXS paths and SSRF checks in `workday.ts:189-225,391-513,669-950`; Form A/Form B tests pass. Capital One retains exact key `workday:wd12:capitalone:Capital_One`, legacy facets, and identity-specific eligibility. |
| 2 | Pasting the Fidelity URL through Watchlist reaches authenticated `verify-board`, admits its exact identity after CXS verification, and malformed, lookalike, or unadmitted URLs fail closed. | ✓ VERIFIED | `watchlist.ts:218-242` → `verify-board/index.ts:207-317` → `detect.ts:72-127` → `connectors.ts:202-208`. Anchored host parsing, exact registry resolution, zero-provider-fetch negative cases, experimental staging, and verify/poll rejection are exercised by passing tests. |
| 3 | Forward-only migrations admit Fidelity without rewriting any deployed migration, and the final database allowlist is NULL-safe. | ✓ VERIFIED | New migrations 0035 and 0036 exist; 0013/0016/0028/0029/0034 are byte-unchanged from the prior exact release. `0036:47-173` requires the exact tuple branch `IS TRUE` and transactionally probes NULL and unknown tuples; `0036:175-259` performs guarded identity/fingerprint repair. Hosted parity is 0001..0036 with no manual repair. |
| 4 | Fidelity ingestion excludes Sales, Customer Service, and Sales Support, while location/experience eligibility remains downstream. | ✓ VERIFIED | Exclusions are frozen by descriptor, not facet ID, in `workday-identities.ts:60-72`. `workday.ts:579-653` discovers kept facet IDs and reconciles counts; `workday.ts:689-780` proves the scoped total; `workday.ts:923-926` applies legacy eligibility only to Capital One. Tests cover ignored facets, count drift, excluded IDs, and Fidelity non-US/3+ year retention. Final production evidence reports zero excluded-family rows. |
| 5 | Fidelity follows Experimental → Active after three clean windows and reports existing OK/Degraded/Unsupported health truthfully. | ✓ VERIFIED | `0035:54-247` enforces clean server-timed evidence and three-window promotion; `watchlist.ts:105-170,258-315` derives state and reuses the three existing badges, including exact `provider_timeout` copy. Production preserved an intermediate timeout as Degraded and a later natural success cleared failures/error to 0/null. |
| 6 | Capital One, other connectors, and the deterministic feed remain unchanged, and the final production release is exactly owner-approved. | ✓ VERIFIED | Registry and dispatch keep Capital One exact; full 1,057-test suite covers all providers/feed. Final release is `edccac2dd1728a86a8ffa431f4d36bb1e10bff5c`; source under current HEAD has no changes from that SHA. Supabase is verify-board v22 JWT true and poll-tick v25 JWT false; Cloudflare deployment `9668e848-9ae0-4287-ab17-25be172b4215` serves the exact 605,440-byte asset hash. |

**Score:** 6/6 truths verified; 0 present-but-behavior-unverified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `supabase/functions/_shared/workday-identities.ts` | Exact, frozen Workday identity registry | ✓ VERIFIED | 106 substantive lines; two frozen identities; fail-closed four-field resolver; truthful Fidelity company name. |
| `supabase/functions/_shared/adapters/workday.ts` | Parameterized and bounded Workday adapter | ✓ VERIFIED | 1,046 substantive lines; identity-derived origins, strict path validation, selective details, live facets, reconciliation, and bounded partial observations. |
| `supabase/functions/_shared/detect.ts` | Exact detection for Form A and Form B | ✓ VERIFIED | Anchored exact-host regexes, strict paths, registry admission, and identity-derived endpoints. |
| `supabase/functions/_shared/connectors.ts` | Registry-driven verify and scheduled dispatch | ✓ VERIFIED | Verification resolves the detected tuple; polling reconstructs the identity from exact persisted source key and rechecks all tuple fields. |
| `supabase/functions/verify-board/index.ts` | Authenticated paste-to-verification boundary | ✓ VERIFIED | Bearer user lookup and authenticated-role gate precede provider access; verified results are server-written and observations recorded. |
| `supabase/functions/poll-tick/index.ts` | Scheduled ingestion and truthful health | ✓ VERIFIED | Claims due sources, dispatches connector, fingerprints with truthful company identity, persists jobs, and disables closure on incomplete observations. |
| `supabase/migrations/0035_generic_workday_fidelity.sql` | Forward-only admission, activation, claim, and catalog update | ✓ VERIFIED | 385 substantive lines; exact two-source allowlist, three-window promotion, Fidelity scheduling, UPDATE-not-INSERT catalog transition. |
| `supabase/migrations/0036_workday_fidelity_integrity_repair.sql` | Forward-only NULL and identity repair | ✓ VERIFIED | 264 substantive lines; `IS TRUE` allowlist, installed-constraint probes, exact guarded Fidelity company/fingerprint repair, preservation assertions. |
| `web/src/lib/watchlist.ts` | Existing Watchlist flow and truthful state presentation | ✓ VERIFIED | URL verification, operational/catalog merge, Fidelity filter note, and unchanged OK/Degraded/Unsupported presentation. |
| `web/src/lib/feed.ts` | Existing deterministic feed identity resolution | ✓ VERIFIED | Normalized company relation is the first authority, so rows with optional `source_company_name=null` still render as Fidelity. |
| Phase tests | Behavioral regression coverage | ✓ VERIFIED | 49 test files and 1,057/1,057 tests pass, including 231 focused Workday/migration/watchlist tests from review closure. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Watchlist paste | `verify-board` | `addCompany` invokes the Edge function | ✓ WIRED | Browser performs an early pure check; authenticated Edge code remains authoritative. |
| `detectAts` | Workday identity registry | Exact tuple plus host form | ✓ WIRED | Unknown tuples cannot construct an origin or endpoint. |
| `verify-board` | `record_connector_observation` | Verified exact source key and job count | ✓ WIRED | Only complete, credible, warning-free evidence advances activation. |
| `claim_due_companies` | `poll-tick` | Active exact source-key allowlist | ✓ WIRED | Fidelity and Capital One are scheduled; other Workday identities remain unclaimable. |
| `poll-tick` | Workday adapter | `pollConnector` resolves `WORKDAY_IDENTITIES[source_key]` | ✓ WIRED | Persisted tuple is rechecked before any provider request. |
| Facet discovery | Scoped Workday listing | Live kept IDs plus expected kept count | ✓ WIRED | First scoped result must equal the discovered kept count or fails `category_filter_unverified`. |
| Normalized jobs | Jobs/feed | Exact company FK and fingerprint, relation-first display | ✓ WIRED | Final hosted evidence reports exact Fidelity relation and zero bad fingerprints. |
| Approved git SHA | Supabase and Cloudflare | Function versions, deployment metadata, immutable bytes | ✓ WIRED | All production identities bind to the final accepted SHA. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Watchlist | `WatchlistRow[]` | RLS companies + source coverage catalog | Yes — hosted Fidelity row is Active/healthy | ✓ FLOWING |
| Workday adapter | `PollObservation.jobs` | Live Fidelity CXS facets, listings, and bounded detail calls | Yes — final natural run produced 113 detailed/open jobs | ✓ FLOWING |
| Poll tick | job snapshots/fingerprints | Adapter jobs + exact company relation | Yes — zero bad fingerprints and exact Fidelity FK linkage | ✓ FLOWING |
| Dashboard feed | company label and opportunities | RLS user_jobs/jobs/company projection | Yes — normalized relation precedes optional source name | ✓ FLOWING |
| Health badges | state/error/success fields | Poll observation health update and company row | Yes — timeout rendered Degraded; natural success restored OK | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Complete workspace behavior | `cd web && npm run test` | 49 files, 1,057/1,057 tests passed | ✓ PASS |
| TypeScript and production bundle | `cd web && npm run build` | `tsc -b` and Vite build passed | ✓ PASS |
| Static quality gate | `cd web && npm run lint` | Exit 0; one pre-existing Fast Refresh warning only | ✓ PASS |
| Release diff integrity | `git diff --check 7642fde8..edccac2` | No whitespace errors | ✓ PASS |
| Local immutable artifact | size + SHA-256 of `index-BtFn08zn.js` | 605,440 bytes; `f27ac0a84f678d9ba062aa9c6503784df9a42e322fed4cb33b979ad6303a6aaa` | ✓ PASS |
| Natural Fidelity schedule | Existing exact-release production evidence | Success at 20:58Z; failures 0/error null; 114 observed; 113 detailed/open | ✓ PASS |
| Capital One regression | Existing exact-release production evidence | Success at 20:55Z; failures 0/error null; observation count 16 | ✓ PASS |

### Probe Execution

No phase-declared or conventional `probe-*.sh` exists for Phase 03.5. The applicable runnable checks are the Vitest integration/migration suite and the already-completed exact-release hosted rollout verification above.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| `connector-expansion` | Plans 01-04 | Internal phase scope label used by plan frontmatter | N/A — not a formal requirement ID | It does not exist in `REQUIREMENTS.md` and is not presented as traceability. Phase achievement is verified directly against the six ROADMAP success criteria. |
| Formal Phase 03.5 IDs | — | ROADMAP explicitly says no new REQUIREMENTS.md IDs | N/A | Existing DISC-07/DISC-08/DISC-09 mappings remain assigned to Phases 02.1/03.1; this report does not invent or remap them. |

No REQUIREMENTS.md row maps an additional formal ID to Phase 03.5, so there is no orphaned Phase 03.5 requirement.

### Prohibition Checks

| Prohibition | Status | Evidence |
|---|---|---|
| Do not change the exact Capital One tuple or source key | ✓ VERIFIED | Frozen registry and production identity remain exact. |
| Do not whole-board detail fan out Fidelity | ✓ VERIFIED | Selective recent scan, known-ID shortcut, and 60-detail cap remain wired. |
| Do not hardcode Fidelity facet IDs | ✓ VERIFIED | Only excluded descriptors are frozen; IDs are discovered live. |
| Do not admit extra Workday URL shapes, lookalike hosts, or arbitrary tenants | ✓ VERIFIED | Two anchored host forms plus exact registry resolution and DB allowlist. |
| Do not weaken verify-board auth/SSRF boundaries | ✓ VERIFIED | Authenticated role, server-side verification, redirects disabled, and origin equality remain. |
| Do not rewrite deployed migrations or manually repair rows | ✓ VERIFIED | New 0035/0036 only; prior migrations unchanged; repair is transactionally guarded in 0036. |
| Do not cross production boundaries without exact owner approval | ✓ VERIFIED | Approval ordering and exact released identities are preserved in the Plan 04 production record and review closure. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| Phase release files | — | No unreferenced TBD/FIXME/XXX, TODO/HACK/placeholder, hollow handler, or hardcoded empty user-visible data found | None | No blocker or warning |

The apparent `return null`/empty-array matches are validation failure paths or parser defaults, not stubs. The original standard review found 6 Critical and 2 Warning issues; current code contains their tested fixes, and the focused re-review is clean at 0 Critical, 0 Warning, 0 Info.

### Human Verification Required

None. The user-visible hosted flow and exact production approval/activation evidence were already completed and adequately documented; repeating them would require redundant production actions without adding verification value.

### Gaps Summary

No gaps remain. The corrected MVP story is covered end-to-end, all six roadmap truths are supported by substantive and wired code plus passing behavior tests or exact hosted execution evidence, and no later milestone phase is needed to satisfy this phase goal.

---
_Verified: 2026-07-24T21:19:02Z_
_Verifier: the agent (gsd-verifier)_
