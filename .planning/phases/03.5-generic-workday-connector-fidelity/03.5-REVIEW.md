---
phase: 03.5-generic-workday-connector-fidelity
reviewed: 2026-07-24T20:50:57Z
depth: standard
re_review: focused
fix_range: 48f0a82^..edccac2
files_reviewed: 7
files_reviewed_list:
  - supabase/functions/_shared/adapters/workday.ts
  - supabase/functions/_shared/workday-identities.ts
  - supabase/migrations/0036_workday_fidelity_integrity_repair.sql
  - web/src/lib/watchlist.ts
  - web/src/lib/watchlist.test.ts
  - web/tests/migration-0036-workday-fidelity-repair.test.ts
  - web/tests/workday-connector.integration.test.ts
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
---

# Phase 03.5: Focused Code Re-Review

**Reviewed:** 2026-07-24T20:50:57Z
**Depth:** Focused re-review of prior standard findings
**Fix Range:** `48f0a82^..edccac2`
**Files Reviewed:** 7
**Status:** clean

## Summary

All six prior blockers and both warnings are resolved in the current source. The fixes preserve the locked Phase 03.5 decisions: Fidelity is scoped only by excluded job families at ingestion, experience/location eligibility remains downstream, Workday identities stay exact and fail closed, and deployed migration 0035 is repaired only through forward migration 0036.

No new blocker or warning was found in the focused fix range.

## Narrative Findings (AI reviewer)

No active findings.

## Resolution Evidence

### CR-01: Fidelity applied-facet verification — RESOLVED

`discoverKeptFacetIds` now returns the expected kept-family count, rejects negative/non-safe counts and duplicate descriptors/IDs, and the first scoped response must match that count. The regression at `web/tests/workday-connector.integration.test.ts:1199` proves an ignored facet fails with `category_filter_unverified`.

**Fix commit:** `e74c139`

### CR-02: Fidelity eligibility scope — RESOLVED

The frozen identities now carry `applyCapitalOneEligibility`. Capital One alone retains the legacy U.S./under-three-years ingestion filter; Fidelity retains recent kept-family details for downstream filtering, matching locked decision D-07. The regression at `web/tests/workday-connector.integration.test.ts:1145` covers both a non-U.S. Fidelity role and roles requiring more than three years.

**Fix commit:** `281b82a`

### CR-03: SQL NULL allowlist bypass — RESOLVED IN SOURCE

Forward-only migration 0036 replaces the Workday constraint with a branch that must evaluate `IS TRUE`. It transactionally probes NULL region, NULL site token, and an unknown non-null tuple against the installed `public.companies` constraint. Migration 0035 remains immutable.

**Fix commits:** `e0b9c85`, `a748f36`

**Rollout result:** Migration 0036 was subsequently exact-approved and applied forward-only. Hosted local/remote migration parity is `0001..0036`; migration 0035 remained immutable and no manual row repair occurred.

### CR-04: Truthful Fidelity company identity — RESOLVED

The Fidelity registry identity now uses `companyName: 'Fidelity'`, so verification, normalized jobs, and fingerprint input share the truthful identity. Migration 0036 repairs the exact existing company row, `source_company_name`, and only the proven `fmr|` fingerprint prefix while preserving the suffix; it aborts on unexpected names or prefixes.

**Fix commits:** `3ef633c`, `a748f36`

### CR-05: Listing/tombstone occurrence identity — RESOLVED

Listing collision identity now reuses `listExternalId(posting)`, normalizing legacy and numeric occurrence suffixes exactly like ingestion. Polling and verification regressions cover `_R...-1` and numeric `_...-2` collisions.

**Fix commit:** `411a4fd`

### CR-06: Overlap identity binding — RESOLVED

`equivalentListPosting` now compares canonical requisition identity in addition to path and display fields. A same-path numeric posting with conflicting bullet identity fails with `provider_identity_drift`.

**Fix commit:** `2f7796b`

### WR-01: Identity drift observation containment — RESOLVED

Polling now catches identity derivation and mapping errors and returns closure-disabled incomplete observations. Safe rows collected before later drift are retained as partial evidence with the specific `provider_identity_drift` warning.

**Fix commit:** `edccac2`

### WR-02: Provider timeout health copy — RESOLVED

Watchlist presentation maps both `timeout` and the adapter-emitted `provider_timeout` to the timeout-specific degraded message, with regression coverage.

**Fix commits:** `48f0a82`, `95e24fe`

## Validation

- Focused tests: 5 files, 231 tests passed.
- TypeScript build and Vite production build passed.
- `git diff --check 48f0a82^..edccac2` passed.
- No source, production, or tracking file was modified during re-review.

## Final Release Verification

- Exact-approved release: `edccac2dd1728a86a8ffa431f4d36bb1e10bff5c`.
- Final combined gates: 49 files, 1,057/1,057 tests; build/tsc passed; lint passed with only the pre-existing Fast Refresh warning; diff check passed.
- Supabase: migration parity `0001..0036`; `verify-board` v22 ACTIVE with JWT verification; `poll-tick` v25 ACTIVE without gateway JWT verification.
- Cloudflare: deployment `9668e848-9ae0-4287-ab17-25be172b4215`, immutable asset `/assets/index-BtFn08zn.js`, 605440 bytes, local/live SHA-256 `f27ac0a84f678d9ba062aa9c6503784df9a42e322fed4cb33b979ad6303a6aaa`.
- Natural final pg_cron run succeeded: Fidelity Active 3, 113 detailed/open jobs, zero excluded families, zero bad fingerprints, and exact Fidelity company linkage; Capital One remained healthy.

---

_Re-reviewed: 2026-07-24T20:50:57Z_
_Reviewer: the agent (gsd-code-reviewer)_
_Result: clean_
