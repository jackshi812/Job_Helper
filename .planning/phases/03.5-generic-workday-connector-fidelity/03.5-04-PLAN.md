---
phase: 03.5-generic-workday-connector-fidelity
plan: 04
type: execute
wave: 3
depends_on: ["03.5-01", "03.5-02", "03.5-03"]
files_modified: []
autonomous: false
requirements: [connector-expansion]
user_setup:
  - service: supabase
    why: "Applying migration 0035 and deploying edge functions crosses the production boundary — requires a NEW exact-release owner approval"
    dashboard_config:
      - task: "Owner grants a new exact-release approval before ANY apply/deploy"
        location: "Owner sign-off (production remains commit 7642fde8 until then)"
must_haves:
  truths:
    - "SC6: No push, apply, or deploy occurs without a NEW exact-release owner approval; production remains commit 7642fde8 until the owner approves"
    - "SC3: After approval, migration 0035 is applied forward-only; deployed migrations are never rewritten and rows are never manually repaired"
    - "SC2/SC5: After approval + deploy, pasting the Fidelity URL in the live Watchlist verifies, stages experimental, and auto-promotes after clean windows; health badges report correctly"
    - "SC6: After deploy, Capital One ingestion, all other connectors, and the deterministic feed are observed unchanged"
  artifacts:
    - "Owner-approval evidence recorded in the plan SUMMARY"
    - "Post-deploy verification evidence (Fidelity live, Capital One unchanged) in the SUMMARY"
  key_links:
    - "owner approval -> supabase db push (0035) -> supabase functions deploy (verify-board, poll-tick) -> live Fidelity paste verification"
  prohibitions:
    - "NO task in this plan runs automatically — every apply/deploy step is blocking-human and MUST wait for explicit owner approval"
    - "NEVER rewrite a deployed migration or manually repair rows (STATE.md Phase 03.4 lesson) — 0035 is applied as-is or not at all"
    - "If approval is NOT granted, STOP: leave production at 7642fde8; the authored migration + code stay committed but unapplied"
---

<objective>
Cross the production boundary — but only with a NEW exact-release owner approval. Apply the forward-only migration 0035 and deploy the two changed edge functions (verify-board, poll-tick), then verify Fidelity works live and Capital One / other connectors / the deterministic feed are unchanged.

Purpose: SC6 requires that production stays at commit 7642fde8 unless the owner grants a fresh exact-release approval. This plan is entirely owner-gated; nothing here may auto-run during execution. If approval is withheld, the phase still succeeds as "authored, not deployed" and production is untouched.

Output: recorded owner-approval evidence and post-deploy verification (or a documented decision to defer deployment).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-CONTEXT.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Owner exact-release approval decision</name>
  <action>Present the release for owner decision; do NOT proceed to any apply/deploy step until the owner explicitly selects approve. Record who approved and when. Default is defer (production stays at 7642fde8).</action>
  <decision>Grant a NEW exact-release approval to apply migration 0035 and deploy verify-board + poll-tick to production?</decision>
  <context>
    Production currently runs the exact-approved release at commit 7642fde8. Applying 0035 and deploying the generalized edge functions changes the live DB guardrail and the live connector code. Per the absolute production boundary, this requires a fresh, explicit exact-release approval from the owner — the prior approvals do NOT carry over. Plans 01-03 are committed but unapplied; the deployed functions still run the old single-tenant code until this step.
  </context>
  <options>
    <option id="approve">
      <name>Approve this exact release for production</name>
      <pros>Fidelity becomes addable live; the generalized Workday path goes live</pros>
      <cons>Mutates production DB guardrail + edge functions; must verify Capital One/feed unchanged immediately after</cons>
    </option>
    <option id="defer">
      <name>Defer deployment</name>
      <pros>Production stays untouched at 7642fde8; code+migration remain committed for a later approval</pros>
      <cons>Fidelity not live yet; phase completes as authored-not-deployed</cons>
    </option>
  </options>
  <resume-signal>Select: approve or defer (record who approved and when)</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Apply migration 0035 (owner-gated, forward-only)</name>
  <action>ONLY IF Task 1 returned approve: apply 0035 forward-only per the project's deploy procedure; never rewrite deployed migrations, never manually repair rows. IF defer: skip and leave production at 7642fde8.</action>
  <what-built>Forward-only migration 0035 (authored in Plan 03) ready to apply.</what-built>
  <how-to-verify>
    ONLY IF the prior checkpoint returned "approve". Apply the migration forward-only (e.g. `supabase db push` for 0035 against production per the project's established deploy procedure). Confirm the migration applies without aborting, the Capital One tuple/catalog/promotion parity holds, and no deployed migration was rewritten and no row was manually repaired. If "defer" was chosen, SKIP this task and leave production at 7642fde8.
  </how-to-verify>
  <resume-signal>Type "migration applied" with the confirmation output, or "skipped — deferred"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Deploy edge functions + live verification (owner-gated)</name>
  <action>ONLY IF approved and Task 2 applied: deploy verify-board + poll-tick per the project's procedure, then verify Fidelity live and confirm Capital One / other connectors / deterministic feed unchanged. IF deferred: skip.</action>
  <what-built>Generalized verify-board + poll-tick edge functions.</what-built>
  <how-to-verify>
    ONLY IF approved and the migration was applied. Deploy the two changed functions (e.g. `supabase functions deploy verify-board` and `supabase functions deploy poll-tick` per the project's procedure). Then verify live: (1) paste https://wd1.myworkdaysite.com/en-US/recruiting/fmr/FidelityCareers into the Watchlist — it detects Workday, verifies, and stages experimental; (2) confirm a non-CXS URL is still rejected; (3) confirm Capital One still polls under its exact identity and the deterministic feed is unchanged; (4) confirm health badges render OK/Degraded/Unsupported correctly. If deferred, SKIP.
  </how-to-verify>
  <resume-signal>Type "deployed + verified" with evidence, or "skipped — deferred"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approval → production mutation | Only a fresh exact-release owner approval may authorize apply/deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-10 | Elevation of privilege | unapproved production deploy | high | mitigate | Every apply/deploy task is blocking-human; nothing auto-runs; production stays at 7642fde8 absent explicit approval |
| T-03.5-11 | Tampering | deployed-migration rewrite / manual row repair | high | mitigate | 0035 is forward-only and applied as-is; deployed migrations immutable; no manual row repair (Phase 03.4 lesson) |
| T-03.5-SC | Tampering | npm/deno installs | low | accept | No package installs this phase |
</threat_model>

<verification>
- Owner approval (or defer decision) recorded with attribution
- If approved: 0035 applied forward-only without abort; verify-board + poll-tick deployed; Fidelity verifies live; Capital One + feed unchanged
- If deferred: production remains at commit 7642fde8; no apply/deploy executed
</verification>

<success_criteria>
Maps to ROADMAP SC6 (no push/deploy without a new exact-release approval; Capital One + other connectors + deterministic feed unchanged) and — when approved — SC2/SC3/SC5 verified live.
</success_criteria>

<artifacts_produced>
- Owner-approval (or defer) decision evidence in the SUMMARY
- If approved: applied migration 0035 + deployed verify-board/poll-tick + live verification evidence
</artifacts_produced>

<output>
Create `.planning/phases/03.5-generic-workday-connector-fidelity/03.5-04-SUMMARY.md` when done
</output>
