---
phase: 03.5-generic-workday-connector-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/workday-identities.ts
  - supabase/functions/_shared/adapters/workday.ts
  - web/tests/workday-connector.integration.test.ts
autonomous: true
requirements: [connector-expansion]
must_haves:
  truths:
    - "SC1: pollWorkday/pollWorkdayRecent/verifyWorkdayListing poll the correct CXS origin for BOTH Form A (myworkdayjobs subdomain) and Form B (myworkdaysite path) when given a resolved WorkdayIdentity"
    - "SC1: Capital One polling/verification behavior (source key, fetch counts, facet IDs, companyName resolution) is byte-behavior-identical after parameterization"
    - "SC4 (D-06): A Fidelity poll applies kept-jobFamilyGroup inclusion facets (all families whose descriptor is not Sales / Customer Service / Sales Support) discovered live from the returned facets array — the apply-all-other-family-facets mechanism is the chosen resolution of D-06's open technical question (Workday facets are inclusion-only)"
    - "SC4: If sum(kept counts)+sum(excluded counts) != provider total, the observation fails closed with diagnostic category_filter_unverified and retains prior jobs"
  artifacts:
    - "supabase/functions/_shared/workday-identities.ts — frozen WorkdayIdentity registry + resolveWorkdayIdentity() pure resolver"
    - "Parameterized supabase/functions/_shared/adapters/workday.ts (identity-threaded pollWorkday, pollWorkdayRecent, verifyWorkdayListing, safeDetailPath, mappers)"
    - "Fidelity + identity-registry + facet fixtures/tests in web/tests/workday-connector.integration.test.ts"
  key_links:
    - "resolveWorkdayIdentity(tenant,region,site,hostForm) -> WorkdayIdentity carrying origin/cxsRoot/publicBoard needed to reconstruct the CXS origin (A1 disambiguation)"
    - "identity.excludedJobFamilyGroups descriptors -> live facet ID discovery -> appliedFacets inclusion list -> count reconciliation"
  prohibitions:
    - "The Capital One tuple (source key workday:wd12:capitalone:Capital_One, tenant capitalone, region wd12, site Capital_One) MUST remain byte-identical — added as a second frozen registry entry alongside Fidelity, never mutated"
    - "NO whole-board detail fan-out for Fidelity (706 detail GETs/poll) — the selective recent-window model (recent postedOn window + knownIds shortcut + bounded maxDetails) MUST be preserved"
    - "NO hardcoded Fidelity facet IDs — IDs are discovered live from the facets array (they differ per tenant and can drift)"
    - "NO supabase db push, NO supabase functions deploy, NO git push in this plan"
---

<objective>
Establish the spine of the generalized Workday connector: a frozen WorkdayIdentity registry (mirroring provider-identities.ts) that admits Capital One (byte-identical) and Fidelity as two entries, and parameterize the Workday adapter over origin/tenant/site so it polls both real Workday URL shapes. Add Fidelity's category-scoped ingestion (exclude Sales / Customer Service / Sales Support via kept-family inclusion facets with fail-closed count reconciliation).

Purpose: Everything downstream (detection, connector dispatch, edge controllers, migration) threads this identity. Lifting the five hardcoded seams to parameters here — while defaulting untouched callers to the frozen Capital One identity — is what makes runtime Workday tenant adds possible without disturbing the exact-approved production release.

Output: `_shared/workday-identities.ts` (new), a parameterized `_shared/adapters/workday.ts`, and adapter/identity/facet test coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-CONTEXT.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-RESEARCH.md
@.planning/phases/03.5-generic-workday-connector-fidelity/03.5-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Fidelity identity, adapter, and facet-scoping test coverage</name>
  <files>web/tests/workday-connector.integration.test.ts</files>
  <read_first>
    - web/tests/workday-connector.integration.test.ts — imports L1-19, jsonResponse helper L41, Capital One identity contract L67-74, verify cases L75-99, exact-identity dispatch guard L124-164, degraded cases L317-386, facet fail-closed case L506-521, SQL ?raw asserts L545-560
    - supabase/functions/_shared/provider-identities.ts (whole file, 30 lines) — the registry shape being generalized
    - supabase/functions/_shared/adapters/workday.ts L49-72 (frozen constants + CAPITAL_ONE_CATEGORY_FACETS), L268/L475/L500 (companyName hardcodes)
    - CONTEXT.md § live-verified endpoint facts — Fidelity CXS total=706; excluded families Sales(108)/Customer Service(83)/Sales Support(5); kept incl. IT(269), Relationship Management(30), Investment Operations(28), Data Analytics(18), Finance(5)
  </read_first>
  <behavior>
    - resolveWorkdayIdentity('capitalone','wd12','Capital_One','jobs') returns the frozen Capital One identity with sourceKey exactly 'workday:wd12:capitalone:Capital_One'
    - resolveWorkdayIdentity('fmr','wd1','FidelityCareers','site') returns a Fidelity identity with origin https://wd1.myworkdaysite.com, cxsRoot .../wday/cxs/fmr/FidelityCareers, sourceKey 'workday:wd1:fmr:FidelityCareers', excludedJobFamilyGroups = ['Sales','Customer Service','Sales Support']
    - resolveWorkdayIdentity for any unknown tuple (e.g. 'evil','wd1','X','site') returns null
    - pollWorkdayRecent(fidelityIdentity, fetchStub) applies appliedFacets.jobFamilyGroup = live-discovered IDs of every kept family and NOT the excluded families; assert the request body sent to the list endpoint
    - When the facets array counts do not reconcile (sum kept + sum excluded != total), the observation is incomplete with diagnostic 'category_filter_unverified' and jobs are empty (fail-closed), mirroring existing case L506-521
    - Regression: existing Capital One identity/verify/dispatch/degraded assertions remain green unchanged
  </behavior>
  <action>
    Add failing fixtures and cases to the integration test. Create Fidelity list/detail/facets fixtures (Form B origin https://wd1.myworkdaysite.com/wday/cxs/fmr/FidelityCareers) modeled on the existing Capital One jsonResponse fixtures (L41+). Add: (1) resolveWorkdayIdentity positive cases for both tenants and a null case for an unknown tuple; (2) a Fidelity pollWorkdayRecent case asserting the appliedFacets inclusion list is exactly the kept-family IDs discovered from the facets array; (3) a Fidelity facet-count-mismatch case asserting category_filter_unverified fail-closed (mirror L506-521 verbatim in structure). Do NOT modify the existing Capital One assertions. These reference symbols that do not exist yet (resolveWorkdayIdentity, pollWorkdayRecent, WorkdayIdentity) so the suite MUST fail to compile/run — that is the RED state.
  </action>
  <acceptance_criteria>
    - `cd web && npm run test -- workday-connector` FAILS with unresolved references to resolveWorkdayIdentity / pollWorkdayRecent (RED confirmed)
    - New Fidelity fixtures use the myworkdaysite Form B origin, never a myworkdayjobs origin
    - No existing Capital One case is edited or deleted
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test -- workday-connector 2>&1 | grep -qiE 'resolveWorkdayIdentity|pollWorkdayRecent|is not (defined|a function)|Cannot find' && echo RED-OK</automated>
  </verify>
  <done>New Fidelity/identity/facet cases exist and fail because the production symbols are not yet implemented; Capital One cases untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Workday identity registry (workday-identities.ts)</name>
  <files>supabase/functions/_shared/workday-identities.ts</files>
  <read_first>
    - supabase/functions/_shared/provider-identities.ts (whole file) — EXACT template: exported frozen source-key const, frozen identity object, frozen keyed map, resolveX() pure resolver
    - supabase/functions/_shared/adapters/workday.ts L49-57 (CAPITAL_ONE_WORKDAY_SOURCE_KEY + origin/cxs/list constants), L69-72 (CAPITAL_ONE_CATEGORY_FACETS)
    - RESEARCH.md § Pattern 1 and Assumption A1 (registry keyed on full tuple + hostForm carries origin); PATTERNS.md § Identity registry
  </read_first>
  <action>
    Create the module mirroring provider-identities.ts structure. Export a WorkdayIdentity interface: { origin, cxsRoot, publicBoard, tenant, site, region, hostForm: 'jobs'|'site', sourceKey, companyName, keptFacetIds?, excludedJobFamilyGroups? }. Define TWO frozen entries: (A) Capital One — tenant 'capitalone', region 'wd12', site 'Capital_One', hostForm 'jobs', origin https://capitalone.wd12.myworkdayjobs.com, sourceKey exactly 'workday:wd12:capitalone:Capital_One' (re-export CAPITAL_ONE_WORKDAY_SOURCE_KEY, keeping it importable by connectors.ts/verify-board), companyName 'Capital One', keptFacetIds = the existing Analysis/Finance IDs from workday.ts L69-72 (byte-identical); (B) Fidelity — tenant 'fmr', region 'wd1', site 'FidelityCareers', hostForm 'site', origin https://wd1.myworkdaysite.com, publicBoard https://wd1.myworkdaysite.com/en-US/recruiting/fmr/FidelityCareers, sourceKey 'workday:wd1:fmr:FidelityCareers', companyName null, excludedJobFamilyGroups ['Sales','Customer Service','Sales Support']. Both cxsRoot = `${origin}/wday/cxs/${tenant}/${site}`. Provide a frozen WORKDAY_IDENTITIES map keyed on sourceKey and a pure resolveWorkdayIdentity(tenant, region, site, hostForm): WorkdayIdentity | null that returns an entry only when all four fields match an admitted entry, else null (fail-closed — per security contribution, unknown tuple/region/site rejected, not silently admitted). Keep it a pure module (no I/O). Do NOT invent a region-implies-hostForm rule (A1) — hostForm is a required lookup key.
  </action>
  <acceptance_criteria>
    - resolveWorkdayIdentity returns the Capital One entry with sourceKey byte-equal to 'workday:wd12:capitalone:Capital_One' and returns null for any unadmitted tuple
    - Fidelity entry origin is https://wd1.myworkdaysite.com and cxsRoot ends with /wday/cxs/fmr/FidelityCareers
    - Capital One keptFacetIds equal the existing L69-72 IDs; Fidelity carries excludedJobFamilyGroups, not hardcoded kept IDs
    - Module exports WorkdayIdentity, WORKDAY_IDENTITIES, resolveWorkdayIdentity, CAPITAL_ONE_WORKDAY_SOURCE_KEY
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test -- workday-connector 2>&1 | grep -qi resolveWorkdayIdentity && echo "registry referenced by tests"</automated>
  </verify>
  <done>Frozen two-entry Workday identity registry with a fail-closed pure resolver exists; Capital One tuple frozen byte-identical, Fidelity added as second entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — parameterize workday.ts over WorkdayIdentity + Fidelity facet scoping</name>
  <files>supabase/functions/_shared/adapters/workday.ts</files>
  <read_first>
    - supabase/functions/_shared/adapters/workday.ts — constants L49-72; safeDetailPath L161-198 (origin-equality invariants); requestJson L272-315; incomplete() L317-331; mapWorkdayDetail L263-268; pollWorkday L333-349 (LIST_URL L349); mapRecentListPosting L475; mapRecentDetail L500; pollCapitalOneRecent L511-596 (LIST_URL L538, facet application L542, facet reconciliation L573-596, recent window L609-618, knownIds L633-636, maxDetails L637-647); verifyWorkdayListing L685-697 (LIST_URL L697)
    - supabase/functions/_shared/workday-identities.ts (from Task 2)
    - supabase/functions/poll-tick/index.ts L233 — normalized.companyName ?? company.name (why companyName:null is safe)
    - PATTERNS.md § adapter seams + § Fail-closed category-filter verification
  </read_first>
  <action>
    Lift the five seams to parameters, defaulting untouched call sites to the frozen Capital One identity so existing callers stay byte-behavior-identical until Plan 02 threads explicit identities:
    (1) Add an `identity: WorkdayIdentity` parameter to pollWorkday, verifyWorkdayListing, and safeDetailPath (safeDetailPath also takes origin/cxsRoot/publicBoard from the identity). Replace module LIST_URL usage with `${identity.cxsRoot}/jobs`; replace PUBLIC_ORIGIN/CXS_ROOT/PUBLIC_BOARD reads in safeDetailPath with identity fields. Keep every SSRF invariant intact (starts /job/, no backslash/?/#, no %2f/%5c, no ../. segments, origin equality, no creds/port).
    (2) Generalize pollCapitalOneRecent into `pollWorkdayRecent(identity, fetchImpl, options)`; keep a thin `pollCapitalOneRecent(fetchImpl, options)` wrapper that delegates to pollWorkdayRecent(capitalOneIdentity, ...) so connectors.ts L233 caller is unchanged this wave.
    (3) Facet branch inside pollWorkdayRecent: when identity.keptFacetIds is present (Capital One), apply exactly the existing inclusion behavior (unchanged code path). When identity.excludedJobFamilyGroups is present (Fidelity), read the returned facets array (reuse the reconciliation at L573-596), take all jobFamilyGroup values whose descriptor is NOT in the excluded set as the inclusion IDs, apply them as appliedFacets.jobFamilyGroup, and assert sum(kept counts)+sum(excluded counts) === providerTotal, else incomplete([], 'category_filter_unverified', ...). New families are included by default.
    (4) companyName: set all three mappers (L268/L475/L500) to `identity.companyName` (Capital One identity supplies 'Capital One'; Fidelity supplies null so poll-tick L233 falls back to the DB company name).
    (5) Preserve the selective-import model (recent postedOn window, knownIds shortcut, bounded maxDetails) — do NOT whole-board detail-fetch Fidelity.
    Leave CAPITAL_ONE_WORKDAY_SOURCE_KEY exported from here (re-exporting from the registry is fine) so connectors.ts/verify-board imports do not break.
  </action>
  <acceptance_criteria>
    - `cd web && npm run test -- workday-connector` PASSES including new Fidelity + facet + fail-closed cases (GREEN)
    - All pre-existing Capital One assertions (identity contract, exact-identity dispatch guard L124-164, degraded cases, verify cases) remain green
    - safeDetailPath still rejects traversal/encoded/protocol-relative/cross-origin paths for both origins (existing unsafe_detail_path cases green)
    - No module-level myworkdayjobs origin remains hardcoded in any polling/verify path; origin is identity-derived
    - Fidelity path never issues more than the bounded maxDetails detail GETs per poll
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test -- workday-connector adapters detect 2>&1 | tail -5</automated>
  </verify>
  <done>workday.ts is parameterized over WorkdayIdentity for both URL shapes; Fidelity category scoping is fail-closed; Capital One behavior byte-identical; full workday test group green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter → Workday CXS origin | Server-side fetch to a tenant-specific origin; externalPath from list rows is attacker-influenceable data |
| identity registry → admitted tenants | Only admitted (tenant,region,site,hostForm) tuples may resolve to a pollable origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-01 | Tampering / Info-disclosure | safeDetailPath in workday.ts | high | mitigate | Keep all origin-equality + path invariants after parameterization; origin comes from the resolved identity, still origin-checked on every fetch |
| T-03.5-03 | Elevation of privilege | resolveWorkdayIdentity | high | mitigate | Resolver is fail-closed: any tuple not exactly matching an admitted frozen entry returns null; no free-string origin construction |
| T-03.5-04 | Info-disclosure | requestJson / incomplete() warnings | medium | mitigate | Preserve existing behavior that warnings never contain provider body (asserted L314) for the Fidelity identity too |
| T-03.5-05 | Integrity | pollWorkdayRecent facet reconciliation | high | mitigate | Fail-closed category_filter_unverified when kept+excluded counts do not equal provider total — prevents silently ingesting excluded families |
| T-03.5-SC | Tampering | npm/deno installs | low | accept | No package installs this phase (in-repo TS + SQL only); no registry surface added |
</threat_model>

<verification>
- `cd web && npm run test -- workday-connector adapters detect` green
- No hardcoded myworkdayjobs origin remains on any Fidelity path; grep the adapter for identity-derived cxsRoot usage
- Capital One source key literal appears only as the frozen registry/adapter constant
</verification>

<success_criteria>
Maps to ROADMAP SC1 (adapter parameterized over both URL shapes; Capital One unchanged) and SC4 (Fidelity category-scoped, fail-closed). Registry + parameterized adapter exist and the full workday test group is green.
</success_criteria>

<artifacts_produced>
- NEW `supabase/functions/_shared/workday-identities.ts`: `WorkdayIdentity` interface, `WORKDAY_IDENTITIES` frozen map, `resolveWorkdayIdentity()` resolver, re-exported `CAPITAL_ONE_WORKDAY_SOURCE_KEY`, new constant string `workday:wd1:fmr:FidelityCareers` (Fidelity source key), Fidelity `excludedJobFamilyGroups` = ['Sales','Customer Service','Sales Support']
- MODIFIED `workday.ts`: identity-parameterized `pollWorkday`, `verifyWorkdayListing`, `safeDetailPath`; new `pollWorkdayRecent(identity, ...)` + thin `pollCapitalOneRecent` wrapper; identity-derived `companyName` in three mappers; live facet-ID discovery + reconciliation branch for excludedJobFamilyGroups
- MODIFIED test file with Fidelity fixtures + identity/facet/fail-closed cases
</artifacts_produced>

<output>
Create `.planning/phases/03.5-generic-workday-connector-fidelity/03.5-01-SUMMARY.md` when done
</output>
