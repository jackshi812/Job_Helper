---
phase: 03-scoring-feed-notifications
plan: 08
type: execute
wave: 8
depends_on: [03-07]
files_modified:
  - supabase/migrations/0025_scoring_freshness.sql
  - supabase/functions/_shared/filters.ts
  - supabase/functions/_shared/scoring-input.ts
  - supabase/functions/score-tick/index.ts
  - supabase/functions/discovery-sweep/index.ts
  - web/tests/filters.test.ts
  - web/tests/scoring-input.test.ts
  - web/tests/company-name-ingestion.test.ts
  - scripts/verify-expected-red.mjs
autonomous: true
gap_closure: true
requirements: [PREF-01, SCOR-01, SCOR-02, SCOR-03, SCOR-04]
must_haves:
  truths:
    - "Equity Research excludes unrelated shared-token data roles while accepting reordered, plural, inflected, and configured-synonym variants."
    - "Adzuna, Greenhouse, and Ashby jobs all pass through the same post-dedup cheapFilter/preferences gate: Coffee Distributor and Product Delivery Associate filter free for Equity Research, while valid provider variants survive."
    - "Score reuse requires equality of a server-computed hash over every semantic preference, job, routed-resume extraction, model, prompt, and filter input."
    - "Preference and ready-extraction signals advance a monotonic desired revision; stale workers cannot publish or clear a newer signal, and explicitly signaled rows are safely reclaimable."
    - "A short-lived service-only maintenance latch guarantees that an active scoring verification run can claim only its two registered fixture rows while normal, missing, and mismatched run IDs claim nothing."
    - "When no latch is active or after it ends/expires, normal bounded production seeding and SKIP LOCKED claiming are unchanged."
    - "Normalized company identity is truthful and complete for displayed rows: tracked Greenhouse/Ashby jobs use their companies row, and Adzuna discovery persists its source-provided company name without fabrication."
  artifacts:
    - path: "supabase/functions/_shared/filters.ts"
      provides: "order-independent concept coverage with conservative inflections and configured synonyms"
      exports: ["cheapFilter", "SYNONYMS"]
    - path: "supabase/functions/_shared/scoring-input.ts"
      provides: "canonical job, extraction, and complete semantic scoring-input hashes"
      exports: ["SCORING_INPUT_VERSION", "jobSnapshotDigest", "resumeExtractionDigest", "scoringInputHash", "shouldRescore"]
    - path: "supabase/migrations/0025_scoring_freshness.sql"
      provides: "service-owned freshness columns, monotonic CAS contracts, and expiring two-fixture verification latch"
      contains: "scoring_verification_maintenance|desired_input_revision|verification_run_id"
    - path: "supabase/functions/score-tick/index.ts"
      provides: "strict verification-header handling and CAS-safe filter/reuse/score/error writes"
    - path: "supabase/functions/discovery-sweep/index.ts"
      provides: "bounded persistence/refresh of truthful Adzuna source company names"
  key_links:
    - from: "supabase/functions/score-tick/index.ts"
      to: "supabase/migrations/0025_scoring_freshness.sql"
      via: "optional validated verification run UUID is passed only to claim_scoring_work; captured desired revision gates every terminal write"
      pattern: "x-scoring-verification-run-id|verification_run_id|desired_input_revision"
    - from: "supabase/functions/score-tick/index.ts"
      to: "supabase/functions/_shared/scoring-input.ts"
      via: "server-loaded semantic inputs determine reuse after cheap filtering and resume routing"
      pattern: "jobSnapshotDigest|resumeExtractionDigest|scoringInputHash|shouldRescore"
    - from: "supabase/functions/score-tick/index.ts"
      to: "supabase/functions/_shared/filters.ts"
      via: "cheapFilter remains before routing, hashing, and generateStructured"
      pattern: "cheapFilter"
    - from: "supabase/functions/discovery-sweep/index.ts"
      to: "supabase/migrations/0025_scoring_freshness.sql"
      via: "normalized Adzuna companyName populates the bounded jobs.source_company_name field on insert and refresh"
      pattern: "source_company_name|companyName"
---

<objective>
Close the scoring-backend half of the Equity Research UAT gap and add a production-enforced anti-TOCTOU verification boundary.

Purpose: exclusive title intent, score freshness, concurrent signals, and later paid verification must be correct under production concurrency without broad cron-timing assumptions.
Output: deterministic RED harness, migration 0025, filter/hash/worker implementation, maintenance-latch contract, and green local backend gates. No hosted mutation, deployment, verifier creation, or paid call occurs.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-feed-notifications/03-CONTEXT.md
@.planning/phases/03-scoring-feed-notifications/03-UAT.md
@.planning/debug/equity-research-title-filter.md
@.planning/phases/03-scoring-feed-notifications/03-03-SUMMARY.md
@.planning/phases/03-scoring-feed-notifications/03-07-PLAN.md

<interfaces>
- `cheapFilter(job, prefs)` preserves exclude -> location -> title order and returns bounded `title_non_overlap` failures.
- `claim_scoring_work(batch_size integer default 12, verification_run_id uuid default null)` is service-only. An active nonexpired maintenance row changes both its seed and claim behavior; no active row preserves normal production behavior.
- The latch is a singleton service-owned contract with one UUID run id, exactly two distinct existing `user_jobs` ids, creation/expiry timestamps, and a strict maximum TTL. Authenticated/anon roles have no table or function access.
- `score-tick` always requires the existing cron secret. Only a strict UUID in `x-scoring-verification-run-id` is passed to the claim RPC; an absent header is the ordinary cron path.
- Existing batch 12, concurrency 4, attempts under 5, five-minute stale reclaim, newest-first ordering, and `FOR UPDATE SKIP LOCKED` remain bounds rather than verifier assumptions.
</interfaces>
</context>

## Phase Goal

**As a** new grad seeking an entry-level job, **I want to** review relevant jobs scored against my preferences and resume in a web dashboard, **so that** I can quickly focus on the strongest opportunities that best fit me.

## UAT Decision Override

- D-01 still permits normalization and configured synonyms, but a single shared token no longer proves a multi-concept target-title match.
- D-10 may retain history, but an old score cannot be presented as current after preference or routed-extraction inputs change.
- D-18 through D-21 remain superseded by the owner's notification removal; migration and worker tests must continue proving notification absence.

## Multi-Source Coverage Audit

| Source | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Focus strongest resume/preference-fit jobs in the web dashboard | 03-08, 03-09, 03-11 | COVERED | Backend relevance/freshness, browser focus, then exact-release UAT. |
| REQ | PREF-01 | Saved title/location/include/exclude preferences control results | 03-08, 03-09 | COVERED | Monotonic signal plus immediate cache eviction. |
| REQ | SCOR-01 | Irrelevant postings filter before AI | 03-08 | COVERED | Provider-agnostic gate and explicit Adzuna/Greenhouse/Ashby regressions. |
| REQ | SCOR-02 | Survivors score against current preferences and routed resume | 03-08, 03-11 | COVERED | Full semantic hash plus one-run hosted proof. |
| REQ | SCOR-03 | Current reasons reflect the matched resume/preferences | 03-08, 03-09, 03-11 | COVERED | Stale results hidden and CAS-protected. |
| REQ | SCOR-04 | Focused dashboard presents current matches | 03-09, 03-11 | COVERED | Focused visibility and UAT. |
| CONTEXT | D-01..D-05 | Cheap filters, preferences, and per-user independence | 03-08, 03-09 | COVERED | UAT override tightens title intent without source special cases. |
| CONTEXT | D-06..D-13 | Resume routing, scoring, privacy, model, and economy | 03-08, 03-11 | COVERED | Complete hash, one call, bounded proof. |
| CONTEXT | D-14..D-17 | Focused feed, dismissal, and detail behavior | 03-09, 03-11 | COVERED | Current/open predicate and UAT. |
| CONTEXT | D-18..D-21 | Removed notification scope | 03-08, 03-10, 03-11 | COVERED | Superseded; absence regressions and UAT retained. |
| RESEARCH | — | Cheap filter precedes the paid model for every source | 03-08 | COVERED | Score worker is source-agnostic and has no source bypass branch. |
| UAT | Test 2 gap | Equity Research excludes unrelated data/product/delivery roles | 03-08, 03-09, 03-11 | COVERED | Named cross-provider fixtures, focused convergence, exact-release rerun. |
| UAT | Company completeness | Every displayed source row has truthful normalized company identity | 03-08, 03-09, 03-11 | COVERED | Backfill/persist, unified-feed fallback, provider fixtures, UAT. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode deterministic RED contracts for title intent, semantic freshness, CAS, and maintenance isolation</name>
  <files>web/tests/filters.test.ts, web/tests/scoring-input.test.ts, web/tests/company-name-ingestion.test.ts, scripts/verify-expected-red.mjs</files>
  <read_first>
    - web/tests/filters.test.ts (current filter fixtures and cross-repository import convention)
    - supabase/functions/_shared/filters.ts (current any-token title behavior and filter order)
    - supabase/functions/score-tick/index.ts (claim, reuse, terminal-write, cron-auth, and one-call paths)
    - supabase/migrations/0019_user_jobs_scoring.sql (pipeline columns, grants, signal RPCs, and scheduler)
    - supabase/migrations/0021_claim_scoring_work_definer.sql (current bounded service-only claim)
    - supabase/migrations/0022_refilter_definer.sql (current authenticated signal boundary)
    - web/tests/notification-removal.test.ts (raw migration and notification-absence assertion conventions)
    - supabase/functions/_shared/adapters/adzuna.ts (truthful source companyName mapping)
    - supabase/functions/_shared/adapters/greenhouse.ts (normalized provider mapping)
    - supabase/functions/_shared/adapters/ashby.ts (tracked-board identity limitation)
    - supabase/functions/discovery-sweep/index.ts (Adzuna insert and exact-refresh persistence)
    - supabase/functions/poll-tick/index.ts (tracked Greenhouse/Ashby company_id mapping)
  </read_first>
  <behavior>
    - Equity Research rejects Research Data Analyst, Equity Data Analyst, Data Researcher, and Research Scientist with title_non_overlap.
    - It accepts Equity Research Analyst, Equities Research Analyst, Research Analyst - Equities, Equity Research Associate, and Equity Researcher; Software Engineer accepts Software Engineering Intern; Quantitative/Quant and Data Scientist/DS remain positive.
    - Adzuna, Greenhouse, and Ashby fixture rows share one filter path: Coffee Distributor and Product Delivery Associate are title_non_overlap for Equity Research, while valid Equity Research variants from each source pass.
    - Provider/source metadata cannot select a bypass or alternate filter; every post-dedup job executes cheapFilter and only a passing result reaches routing, hashing, or generateStructured.
    - Equal canonical inputs hash equally; changing any preference array, job title/location/description, routed resume id, extraction text/keywords/model/extracted timestamp, scoring model, prompt revision, filter revision, or scoring-input version changes the hash; order/case-only semantic no-ops do not.
    - Null/legacy or unequal hashes require scoring; only equality permits reuse.
    - Every preference or ready-extraction signal increments applicable own open rows, resets retry/claim/error state once, and makes older or attempts-exhausted rows reclaimable without removing the attempts cap.
    - A claim captures revision N; a signal advances N+1; stale filtered, reuse, scored, and error writes affect zero rows and cannot clear N+1.
    - During an active latch, late job creation, late preference signal, and late extract-reroute signal cannot expand the permitted claim set; no-ID and mismatched-ID concurrent claims return zero and do not seed; the exact run ID can claim only the two registered existing fixture IDs.
    - Expiry and explicit end restore normal seed/claim behavior; a bogus run ID still returns zero; concurrent exact-run claims remain bounded and SKIP LOCKED.
    - Authenticated roles cannot read/write the latch or write hash/revision fields, and notification schema/runtime stays absent.
    - Migration backfill copies existing joined `companies.name` into the bounded source-name field; Adzuna insert/exact-refresh persists only nonblank trimmed provider names; absent names remain null rather than fabricated.
  </behavior>
  <action>First run and record the unchanged baseline filter and notification-removal test-name inventory. Add behavior-first filter and hash fixtures plus raw migration/worker contract tests for all-open signaling, attempts reset, revision capture, monotonic CAS, grants, and the maintenance latch. Include table-driven Adzuna, Greenhouse, and Ashby examples after dedup: with target Equity Research, Coffee Distributor and Product Delivery Associate must return title_non_overlap/free filtering while valid Equity Research variants from all three pass. Add a static/integration assertion over score-tick that source/provider metadata is neither read nor branched on, cheapFilter executes for every claimed job, and generateStructured is reachable only from the passing branch. Do not add provider enumeration, source-specific matching, or a bypass. Add company-ingestion contracts proving tracked Greenhouse/Ashby rows retain normalized `company_id -> companies.name`, Adzuna's nonblank trimmed `NormalizedJob.companyName` is stored on insert and exact refresh, existing joined rows backfill truthfully, and missing source names stay null rather than becoming invented labels. Model interleavings explicitly: begin a short-lived run with two distinct existing fixture IDs; inject a late open job, an authenticated preference signal, a ready-extraction reroute signal, a normal no-header cron claim, a mismatched UUID claim, a matching claim, explicit end, and TTL expiry. Prove only the two fixture IDs are ever claimable while active and ordinary work resumes after end/expiry. Add a strict-header worker contract proving malformed UUIDs fail before claim and the existing cron secret is still mandatory.

Create `scripts/verify-expected-red.mjs` with fixed baseline and expected-failure test-name allowlists. It must support a default backend suite for this plan and a predeclared `--suite web-gap` allowlist consumed by Plan 03-09. It must run baseline names green, run only new names with Vitest JSON in a temporary directory, and accept only the intended assertion failures. Reject import/transform/collection errors, missing JSON, timeouts, crashes, unrelated failures, zero expected failures, or zero exit status. Make the not-yet-created scoring-input module an explicit expected assertion rather than an infrastructure failure. Do not edit production files, access a network, or touch hosted state.</action>
  <verify>
    <automated>node scripts/verify-expected-red.mjs</automated>
  </verify>
  <acceptance_criteria>
    - RED fixtures cover all named positive/negative inflections and every semantic hash input.
    - Cross-provider fixtures prove Adzuna, Greenhouse, and Ashby share one post-dedup preference filter; named distributor/product-delivery negatives filter free and valid Equity Research variants pass.
    - Static/integration proof shows no provider/source bypass and no survivor reaches AI before cheapFilter passes.
    - Company-ingestion tests prove truthful company backfill/persistence for Adzuna, Greenhouse, and Ashby without provider-specific scoring control flow or fabricated names.
    - Interleaving tests cover late job creation, preference and extraction signals, no-ID/mismatched/exact claims, concurrent claims, explicit end, and expiry.
    - The tests prove the latch—not timing of poll, extract, another cron, or a human—is the claim-set boundary.
    - Expected-RED handling distinguishes intended assertions from broken test infrastructure and preserves baseline green behavior.
    - No production code, hosted mutation, deployment, invocation, or paid action occurs.
  </acceptance_criteria>
  <done>The backend gap and maintenance-isolation contract exist as deterministic, infrastructure-safe failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement matching, complete freshness, monotonic CAS, and the expiring two-fixture latch</name>
  <files>supabase/migrations/0025_scoring_freshness.sql, supabase/functions/_shared/filters.ts, supabase/functions/_shared/scoring-input.ts, supabase/functions/score-tick/index.ts, supabase/functions/discovery-sweep/index.ts</files>
  <read_first>
    - web/tests/filters.test.ts (Task 1 behavior contract)
    - web/tests/scoring-input.test.ts (Task 1 hash and database/worker contract)
    - scripts/verify-expected-red.mjs (exact RED names that must turn green)
    - supabase/functions/_shared/routing.ts (routed-resume identity and tie behavior)
    - supabase/functions/_shared/openai.ts (fixed scoring model and one structured-call contract)
    - supabase/migrations/0017_preferences.sql (preference shape and ownership)
    - supabase/migrations/0018_resume_extracts.sql (extraction content, model, and extracted_at revision fields)
    - supabase/migrations/0021_claim_scoring_work_definer.sql (claim signature to replace without leaving an overload)
    - supabase/migrations/0022_refilter_definer.sql (authenticated definer signal to replace)
    - supabase/functions/score-tick/index.ts (all terminal update branches and response shape)
    - supabase/functions/discovery-sweep/index.ts (Adzuna insert/exact-return row mapping)
    - supabase/functions/poll-tick/index.ts (tracked-board company ownership mapping; read-only unless tests prove a gap)
  </read_first>
  <behavior>
    - Significant concepts from at least one preferred title all match a job-title concept, configured synonym, or conservative inflection; order does not matter and extra role suffixes are allowed.
    - Cheap filter/location/exclude failures stay before routing, hashing, and OpenAI.
    - Equal current hash clears refilter through revision CAS without OpenAI; null/different hash makes one structured call and publishes only through the same CAS.
    - Filtered writes clear obsolete scoring payload and hash while keeping a bounded filter reason.
    - Explicit signals monotonically increment all applicable open rows and reset attempt/claim/error state; every terminal write constrains id plus captured revision, and a zero-row update is discarded without a second write.
    - Active maintenance permits only an exact run UUID and its two registered fixtures; missing/mismatched callers neither seed nor claim. No active latch plus no run UUID executes the unchanged production seed/claim flow.
    - The verification header never weakens cron authentication and only exposes bounded claimed fixture UUIDs in the verification response.
    - Source/provider identity never changes filtering: all claimed jobs use the same filter input and only pass results can enter the paid survivor path.
    - Every displayed job has a truthful normalized company identity available from joined companies.name or a bounded source-provided name; unknown values are not fabricated.
  </behavior>
  <action>Make Task 1 green. Replace any-token title acceptance with order-independent concept-group coverage. Apply configured synonym groups and these conservative general variants without recursive stemming: identity; `-ies` to `-y` only when the pre-suffix stem has at least three letters; trailing `s` singularization only when the resulting base has at least four letters and does not end in `ss`; `-er` base only when the result has at least six letters; and `-ing` base only when the result has at least six letters. Preserve empty-title permissiveness and exclude -> location -> title ordering. Keep the worker provider-agnostic after dedup: do not load or branch on source metadata, and preserve the single cheapFilter gate before every routing/hash/AI survivor path.

Create migration `0025_scoring_freshness.sql`. Add nullable service-owned `scoring_input_hash`, nonnegative `desired_input_revision bigint not null default 0`, and the captured revision required by worker claims. Replace both preference/reroute signal functions so each applicable open row atomically advances revision and resets attempts, claim, and bounded error state. Keep authenticated `auth.uid()` confinement, service-only reroute, empty search paths, fully qualified objects, and authenticated denial of pipeline-column writes. Replace—not overload—the old claim function with optional `verification_run_id uuid default null`, retaining batch, poison-pill, stale-claim, newest-first, and SKIP LOCKED behavior.

Add nullable bounded `jobs.source_company_name` for truthful source-provided identity. Backfill it only from existing nonblank normalized `companies.name` joins. Update Adzuna discovery insert and exact-refresh paths to persist trimmed nonblank provider company names within the database bound; leave unavailable names null. Tracked Greenhouse/Ashby jobs continue using their existing company_id identity, with no source-specific ranking/filter branch and no invented fallback.

Add a service-role-only singleton maintenance table and begin/end functions. Store active run UUID, two distinct existing fixture `user_jobs` UUIDs, created time, and expiry. Enforce a short default TTL and strict maximum TTL in database constraints/function validation; clear expired state before begin/claim; grant no anon/authenticated access. While a nonexpired row exists, no-ID/mismatched calls return zero before seeding and exact calls claim only the two allowed existing rows. When none exists, no-ID calls use normal production behavior; any supplied unmatched/bogus run UUID returns zero. Ending requires the matching UUID, and expiry must fail safe so cleanup failure cannot block scoring indefinitely.

Add stable Web Crypto SHA-256 helpers over canonical job title/location/description, routed resume identity, extraction text/keywords/model/extracted_at, canonical preference arrays, fixed model, prompt/filter revisions, and scoring-input version. Keep desired revision out of the semantic hash. In score-tick, validate a present verification header as a strict UUID only after method and existing cron-secret checks, pass it only to claim RPC, and include bounded claimed IDs only for that controlled path. Keep cheapFilter first, compute the hash from server-loaded inputs, reuse only on equality, and CAS filtered/reuse/success/error writes on id plus claimed revision. Preserve one `generateStructured` call site, budgets, concurrency, prompt delimiters, token-only usage, bounded logs, and notification absence.</action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run tests/filters.test.ts tests/scoring-input.test.ts tests/company-name-ingestion.test.ts tests/notification-removal.test.ts &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; git diff --exit-code -- ../scripts/verify-scoring.ts</automated>
  </verify>
  <acceptance_criteria>
    - General title inflection/synonym positives and shared-token negatives pass without special-casing Equity Research strings.
    - The semantic hash covers every listed material input and only equality permits score reuse.
    - Monotonic signaling, captured revisions, safe reclaim, and CAS protect every terminal path under concurrent saves/extractions.
    - Active-latch claims are exactly two-fixture scoped under late signals/jobs and concurrent normal/mismatched calls; end/expiry returns normal behavior.
    - Latch and pipeline writes are service-owned; authenticated RLS/grant denial, privacy, paid-call bounds, and notification absence stay green.
    - Existing tracked company names are backfilled, future Adzuna observations persist truthful source names, unknown names remain null, and production scoring remains source-agnostic.
    - No hosted access, verifier creation, deployment, git push, or paid API use occurs.
  </acceptance_criteria>
  <done>The local backend makes title intent and scoring freshness correct and supplies an expiring production-enforced verification isolation boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser -> preference signal | authenticated input may advance only the caller's scoring revision |
| score-tick -> claim RPC | cron-authenticated service code supplies an optional untrusted verification header |
| verifier service -> maintenance latch | service credentials temporarily suppress normal scoring and permit exactly two owned rows |
| JD/resume/preferences -> OpenAI | private untrusted content crosses to the configured model only after free filters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-3-18 | Tampering | hash/revision publication | high | mitigate | Server-derived complete hashes and id+revision CAS gate every terminal worker write. |
| T-3-19 | Elevation | maintenance latch | critical | mitigate | Service-only grants, strict UUIDs, exactly two existing IDs, matching end, and database-enforced maximum TTL. |
| T-3-20 | Denial of Service | abandoned active latch | critical | mitigate | Claim/begin treat expired rows as inactive and TTL cannot exceed the strict maximum. |
| T-3-21 | Tampering | concurrent cron or late signals | critical | mitigate | Active latch blocks seeding and all nonmatching claims independent of poll/extract/human timing. |
| T-3-22 | Information Disclosure | scoring evidence/logs | high | mitigate | Hashes and responses expose only bounded IDs/counts; raw preference, JD, resume, and prompt data are never logged. |
| T-3-23 | Tampering | notification reintroduction | medium | mitigate | Existing notification-removal regression remains in the green gate. |
| T-3-33 | Spoofing | fabricated company identity | high | mitigate | Only normalized companies.name or trimmed source-provided values are stored; unknown stays null and is excluded from display in Plan 03-09. |
</threat_model>

<verification>
Run the expected-RED harness before implementation, then the named filter/hash/notification suite, build, lint, and legacy-verifier immutability check. All commands are local and no-network.
</verification>

<success_criteria>
- Exclusive title matching and complete semantic freshness are deterministic and general.
- Concurrent signals cannot be lost or overwritten by stale workers.
- The production claim contract itself confines an active verification run to exactly two fixtures and safely expires.
- Normal production behavior is unchanged with no active latch.
- No notification artifact or outward/paid action is introduced.
- Company identity is truthfully mapped for all three requested sources without separate provider ranking/filter behavior.
</success_criteria>

<output>
Create `.planning/phases/03-scoring-feed-notifications/03-08-SUMMARY.md` with RED/GREEN evidence, latch invariants, changed migration/function signatures, and confirmation of zero hosted or paid actions.
</output>
