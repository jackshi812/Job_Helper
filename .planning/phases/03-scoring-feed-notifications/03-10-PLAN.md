---
phase: 03-scoring-feed-notifications
plan: 10
type: execute
wave: 10
depends_on: [03-09]
files_modified:
  - scripts/verify-scoring-freshness.ts
  - scripts/verify-scoring-freshness.test.mjs
  - scripts/verify-scoring-evidence.mjs
  - scripts/verify-scoring-evidence.test.mjs
  - .planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md
autonomous: false
gap_closure: true
requirements: [PREF-01, SCOR-01, SCOR-02, SCOR-03, SCOR-04]
must_haves:
  truths:
    - "The dedicated verifier is import-safe and locally proves no-network state ordering, failure cleanup, one-invocation guarding, and maintenance-latch use."
    - "The production verifier pauses and snapshots score cron as defense in depth, but the active expiring latch is the enforced guarantee that normal cron, late poll jobs, late preference changes, and extraction reroutes cannot escape the two-fixture claim set."
    - "Migration 0025, score-tick, and the focused web feed roll out only after a dedicated rollout-only approval that excludes verifier execution and paid calls."
    - "Rollout evidence binds exact migration, Edge Function, origin commit, Cloudflare deployment, and immutable asset identities while notification artifacts remain absent."
  artifacts:
    - path: "scripts/verify-scoring-freshness.ts"
      provides: "fail-closed one-shot verifier using cron pause plus the expiring two-fixture maintenance latch"
    - path: "scripts/verify-scoring-freshness.test.mjs"
      provides: "no-network tests for ordering, TOCTOU injection, TTL cleanup, one-call guard, and exact restoration"
    - path: "scripts/verify-scoring-evidence.mjs"
      provides: "no-network schema and cross-artifact validator for rollout and paid-proof evidence"
    - path: ".planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md"
      provides: "exact approved release identity, local safety proof, notification absence, and zero paid invocations"
  key_links:
    - from: "scripts/verify-scoring-freshness.ts"
      to: "supabase/migrations/0025_scoring_freshness.sql"
      via: "service-only begin/end maintenance functions register exactly two fixture user_job IDs with a bounded TTL"
      pattern: "begin_scoring_verification|end_scoring_verification|verification_run_id"
    - from: "scripts/verify-scoring-freshness.ts"
      to: "supabase/functions/score-tick/index.ts"
      via: "one cron-secret-authenticated request carries x-scoring-verification-run-id and proves the controlled claimed set"
      pattern: "x-scoring-verification-run-id"
    - from: ".planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md"
      to: "web/src/lib/feed.ts"
      via: "git SHA, Cloudflare deployment id, asset URL, and SHA-256 bind the focused-feed release"
      pattern: "git_sha|cloudflare_deployment_id|asset_sha256"
---

<objective>
Create the dedicated verifier and prove its safety locally, then obtain separate rollout-only approval and release the already-green correction without running paid proof.

Purpose: verifier design, production rollout, and paid execution remain distinct trust decisions; production claim isolation is enforced by the expiring latch rather than timing assumptions.
Output: verifier source/tests and exact rollout evidence. This plan never runs the production verifier or manually invokes score-tick.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-scoring-feed-notifications/03-UAT.md
@.planning/phases/03-scoring-feed-notifications/03-08-SUMMARY.md
@.planning/phases/03-scoring-feed-notifications/03-09-SUMMARY.md
@supabase/migrations/0025_scoring_freshness.sql
@supabase/functions/score-tick/index.ts
@scripts/verify-scoring.ts

<interfaces>
- Existing `scripts/.env` supplies the current Supabase client/service, Management API, project-ref, cron-secret, and seeded-user variables; values are never printed.
- The Management API database-query endpoint is used to snapshot/pause/restore the unique `score-tick-every-minute` cron row.
- Migration 0025 exposes service-only begin/end maintenance functions and an optional verification UUID on `claim_scoring_work`.
- The dedicated production command is reserved for Plan 03-11: `node --env-file=scripts/.env --experimental-strip-types scripts/verify-scoring-freshness.ts`.
- Poll and extraction may remain active during proof because a nonexpired latch makes normal/no-ID scoring claims return zero, prevents normal seeding, and permits only the registered fixtures to the exact run UUID.
</interfaces>
</context>

## Phase Goal

**As a** new grad seeking an entry-level job, **I want to** review relevant jobs scored against my preferences and resume in a web dashboard, **so that** I can quickly focus on the strongest opportunities that best fit me.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the latch-backed verifier and no-network failure harness</name>
  <files>scripts/verify-scoring-freshness.ts, scripts/verify-scoring-freshness.test.mjs, scripts/verify-scoring-evidence.mjs, scripts/verify-scoring-evidence.test.mjs</files>
  <read_first>
    - scripts/verify-scoring.ts (client/auth/assertion conventions only; keep byte-for-byte unchanged)
    - supabase/migrations/0025_scoring_freshness.sql (maintenance begin/end, TTL, claim, revision, and grant contracts)
    - supabase/functions/score-tick/index.ts (verification header, response, batch, auth, and terminal behavior)
    - supabase/migrations/0019_user_jobs_scoring.sql (score cron and ai_usage schema)
    - .planning/phases/03-scoring-feed-notifications/03-08-SUMMARY.md (implemented backend identities and green gates)
    - .planning/phases/03-scoring-feed-notifications/03-09-SUMMARY.md (implemented browser slice and green gates)
  </read_first>
  <behavior>
    - Import/help/unit-test modes perform zero real network calls and never run environment-driven main.
    - Before setup, the verifier snapshots the unique score cron, pauses it through Management SQL, re-reads exact fields, and proves already-started score work is quiescent; any ambiguity stops before data mutation.
    - It pre-seeds and snapshots potentially affected rows/preferences as defense in depth, then creates exactly two collision-checked fixture user_jobs and no other fixture user_job.
    - It begins one short-TTL maintenance run with those two IDs before the authenticated preference signal; after signaling it protects nonfixtures, injects late job/preference/extract-reroute events, and proves no-ID plus mismatched-ID claims return zero.
    - Poll/extract need not be paused: active-latch behavior, not timing, forbids their resulting work from entering the scoring claim set and forbids normal claim seeding.
    - The exact score-tick request carries the matching run UUID once; its controlled response claims exactly the positive and negative fixture IDs.
    - A hard invocation counter allows one process-level tick call with no retry, polling, fallback verifier, or second invocation.
    - Nested cleanup ends/expires the latch first, restores all snapshotted data/preferences, deletes fixtures/seeds, proves zero residue, then restores every cron field last; cleanup continues after injected failures.
  </behavior>
  <action>Create an import-safe dependency-injected verifier state machine. First use Management API SQL to select exactly one score cron row, snapshot all identity/schedule/execution fields, pause it if active, re-read it, and prove no in-flight current score claim remains. Treat this as defense in depth, not the anti-TOCTOU boundary. Abort before setup on missing variables, non-2xx SQL, ambiguous cron inventory, unavailable alter_job, unprovable quiescence, or later restoration mismatch.

Pre-seed every current user/open-job pair and track inserted rows separately; snapshot complete target-user rows, unrelated current claim candidates, and target preferences. Create collision-checked positive Equity Research and shared-token negative jobs plus exactly two corresponding target-user fixture rows. Generate one run UUID and begin the service-only maintenance latch with those two IDs and a TTL comfortably below the database maximum before calling the authenticated refilter signal. After the signal, protect nonfixtures as defense in depth. In both local adapters and the production flow, inject a collision-checked late open job, a second authenticated preference signal, and a service-role ready-extraction reroute signal while the latch is active; track every mutation for cleanup. Then prove authenticated direct writes to hash/revision/latch fields are denied and no-ID plus mismatched-ID direct claims return zero without seeding the late job. Do not issue a direct matching claim in production: the sole score-tick request with the matching header is the only matching claim attempt, and its bounded response must identify exactly the two registered fixtures. Local tests also cover the matching path through the tick adapter and expired latch behavior.

Snapshot global score-purpose ai_usage immediately before the one call. Assert the negative fixture filters free, the positive fixture is freshly scored at the claimed revision, and later Plan 03-11 can prove one owned delta and zero others. Implement nested finally cleanup so latch end/expiry is attempted even if scoring fails, all pre-existing rows/preferences restore exactly, tracked seeds/fixtures are deleted, zero residue is proved, and cron restores/re-reads exactly last. TTL expiry remains the database fail-safe if process cleanup fails. Emit only bounded IDs, revisions, digests, counts, and release identities.

Create no-network node tests with fake Management API, Supabase, and tick adapters. Cover success and injected failure at every state; no mutation before pause proof; exactly two fixture rows; latch-before-signal; signal-before-protect; live late-event isolation; authenticated write denial; no-ID/mismatch zero work; no direct production matching claim; matching two-ID claim only through the sole tick adapter invocation; expiry/end normal restoration; one invocation maximum; complete nested cleanup; cron restoration last/exact. Add static no-retry/no-poll guards and prove `scripts/verify-scoring.ts` unchanged.

Create `scripts/verify-scoring-evidence.mjs` plus tests as a no-network validator with `--rollout` and `--paid` modes. Rollout mode must require local/origin SHA equality, migration 0025, deployed score-tick and Cloudflare/asset identities, latch table/functions/grants/TTL inventory, notification runtime/schema/secrets/client/UI absence, and zero verifier/manual-tick/maintenance/OpenAI counters. Paid mode must additionally require exact release identity equality with rollout evidence, one invocation, two fixtures, live late-job/preference/reroute isolation, no-ID/mismatch zero work, authenticated write denial, positive/negative outcomes, one owned global usage delta, zero others, exact row/preference restoration, latch end/expiry, cron restoration, and zero residue. Reject missing, duplicate, malformed, contradictory, or mismatched fields. Do not run the production command.</action>
  <verify>
    <automated>node --experimental-strip-types --test scripts/verify-scoring-freshness.test.mjs scripts/verify-scoring-evidence.test.mjs &amp;&amp; git diff --exit-code -- scripts/verify-scoring.ts</automated>
  </verify>
  <acceptance_criteria>
    - Local tests prove latch enforcement under every required late event and concurrent normal/mismatched claim without real network.
    - Cron pause/quiescence and preseed/protect remain defense in depth; no test relies on poll/extract/human timing for claim isolation.
    - Exactly two fixture user_jobs are registered before the authenticated signal, and only matching controlled score-tick may claim them.
    - Cleanup attempts latch end, exact data/preferences restoration, fixture deletion, zero residue, and exact cron restoration on every injected failure; TTL is the final fail-safe.
    - One-call and legacy-verifier immutability guards pass; neither verifier nor OpenAI is invoked.
  </acceptance_criteria>
  <done>The dedicated verifier is locally proven fail-closed, latch-isolated, one-shot, and exactly restorable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve only the production and frontend rollout</name>
  <files></files>
  <read_first>
    - .planning/phases/03-scoring-feed-notifications/03-08-SUMMARY.md (backend diff, maintenance TTL, and local gates)
    - .planning/phases/03-scoring-feed-notifications/03-09-SUMMARY.md (web diff and local gates)
    - scripts/verify-scoring-freshness.test.mjs (local isolation-safety result)
    - supabase/migrations/0025_scoring_freshness.sql (production schema/RPC/latch change)
    - supabase/functions/score-tick/index.ts (Edge Function diff)
    - web/src/lib/feed.ts (focused-feed diff)
    - web/src/pages/Preferences.tsx (cache invalidation diff)
  </read_first>
  <action>Present the exact migration 0025, score-tick, frontend, verifier/safety-test diff, maintenance-latch maximum TTL and production effect, green local commands, proposed commit, and four outward operations in the human check. Explicitly exclude verifier execution, manual score-tick invocation, fixture creation, and OpenAI spend. Stop after requesting this new scope-specific approval; perform no implementation or outward action in this checkpoint.</action>
  <verify>
    <human-check>Approve or decline only: (1) migration 0025 production push, (2) score-tick deployment, (3) exact commit push to origin/main, and (4) resulting Cloudflare production deployment plus read-only release/notification-absence checks. This does not authorize the verifier or any paid call.</human-check>
  </verify>
  <acceptance_criteria>
    - The user sees exact diffs, checks, commit, latch/TTL behavior, four rollout operations, and expected effect.
    - Approval explicitly excludes paid proof, verifier/tick invocation, fixtures, retries, and loops.
    - Task 3 remains blocked without explicit `approve rollout` for this exact scope.
  </acceptance_criteria>
  <done>The user explicitly approves the rollout-only scope, or execution stops with production unchanged.</done>
  <what-built>A locally green scoring-freshness correction and no-network-tested verifier that has never run against production.</what-built>
  <how-to-verify>Review exact release content and confirm the four rollout operations contain no verifier execution or paid call.</how-to-verify>
  <resume-signal>Reply `approve rollout` to authorize only the listed rollout, or decline.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Perform only the approved rollout and bind exact release evidence</name>
  <files>.planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03-scoring-feed-notifications/03-08-SUMMARY.md (approved backend content and gates)
    - .planning/phases/03-scoring-feed-notifications/03-09-SUMMARY.md (approved frontend content and gates)
    - supabase/migrations/0025_scoring_freshness.sql (approved migration)
    - supabase/config.toml (linked project and function deployment configuration)
    - .planning/phases/03-scoring-feed-notifications/.continue-here.md (current provider/release identifiers)
    - scripts/verify-scoring-freshness.ts (read-only confirmation that it must not run here)
  </read_first>
  <action>Proceed only after exact rollout approval. Apply migration 0025, deploy only score-tick with its existing cron-secret boundary, push the exact approved commit to origin/main, and allow/trigger only that commit's Cloudflare production build. Never invoke score-tick or either verifier, create fixtures/latch rows, or call OpenAI.

Use read-only provider checks to write exact local/origin git SHA equality, migration head 0025, deployed score-tick inventory/version, Cloudflare project/deployment/URL/status, immutable asset path/SHA-256, and maintenance table/function/grant/TTL inventory to `03-10-ROLLOUT-EVIDENCE.md`. Record notification runtime/schema/secrets/client/UI absence and the local safety command/result. Record `paid_verifier_runs: 0`, `manual_score_tick_invocations: 0`, `maintenance_runs_started: 0`, and `openai_calls_by_plan_03_10: 0`. Stop on failure without substitute deployment or verifier execution.</action>
  <verify>
    <automated>node scripts/verify-scoring-evidence.mjs --rollout .planning/phases/03-scoring-feed-notifications/03-10-ROLLOUT-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Only the four approved outward operations occur.
    - Exact backend/frontend identities and deployed maintenance-latch contract are recorded.
    - Notification absence remains enforced.
    - No verifier, manual tick, maintenance run, fixture, or paid call occurs.
    - Rollout failure stops without expanding approval scope.
  </acceptance_criteria>
  <done>The exact approved correction is live and evidenced with zero verifier runs or paid effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verifier -> Management API | project token can inspect and pause/restore production score cron |
| verifier -> maintenance latch | service credentials can temporarily block normal scoring and admit two rows |
| local release -> Supabase/Cloudflare | rollout approval permits production schema/function/frontend mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-3-28 | Tampering | verifier claim isolation | critical | mitigate | Production latch admits only exact run plus two IDs; no-ID/mismatch gets zero independent of late events. |
| T-3-29 | Denial of Service | latch or cron left active/paused | critical | mitigate | Strict database TTL, finally end, and exact outermost cron restoration/re-read. |
| T-3-30 | Repudiation | rollout approval creep | critical | mitigate | Dedicated checkpoint names four rollout operations and explicitly excludes verifier/paid actions. |
| T-3-31 | Tampering | mutable deployed release | high | mitigate | Evidence binds migration, Edge, git, Cloudflare, URL, and immutable asset hash. |
| T-3-32 | Information Disclosure | verifier/evidence output | high | mitigate | Only IDs, revisions, digests, counts, statuses, and release identities are emitted. |
</threat_model>

<verification>
Task 1 is local/no-network. Task 2 performs no implementation. Task 3 may perform only the explicitly approved rollout and read-only evidence checks; it records zero paid/manual invocations.
</verification>

<success_criteria>
- The verifier is locally proven to rely on the production latch for TOCTOU isolation.
- Rollout starts only after exact rollout-only approval.
- Exact backend/frontend release and notification absence are evidenced.
- No verifier, score-tick, fixture, maintenance run, or paid call occurs in this plan.
</success_criteria>

<output>
Create `.planning/phases/03-scoring-feed-notifications/03-10-SUMMARY.md` after rollout evidence passes and explicitly record zero paid verifier runs. Do not begin Plan 03-11 without a separate paid-run approval.
</output>
