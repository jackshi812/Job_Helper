---
context: phase
phase: 03-scoring-feed-notifications
task: 1
total_tasks: 2
status: paused
last_updated: 2026-07-20T05:01:40.129Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Sole matching claim — The production freshness verifier must not directly call the matching `claim_scoring_work` path before `score-tick`; the one authorized `score-tick` invocation is the sole matching claim proof.

**Do not proceed until this constraint is acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Pre-claiming verifier fixtures | A direct matching claim consumes the two fixture rows, leaving the later one-shot `score-tick` unable to claim them. | blocking | Follow Plan 03-10: direct probes are no-ID/mismatch only; matching proof occurs only through the sole tick adapter/request. |
| Collapsing approval scopes | Deployment approval does not authorize fixtures, the verifier, `score-tick`, or paid AI use. | advisory | Plan 03-10 rollout and Plan 03-11 paid proof use separate explicit approvals. |

<current_state>
Phase 3 core/feed-only implementation is deployed. Plan 07 is summarized as implementation complete with a diagnosed UAT gap. Plans 03-08 through 03-11 are written, structurally valid, decision-complete, and passed the final checker. No gap source implementation has started. Planning commit: `b3a0d39725c2c2cd4600a3ea2540c365589e61ec`.
</current_state>

<completed_work>

Completed this session:
- Converted the Phase 3 goal to the approved MVP user story.
- Ran UAT: dashboard and hosted scoring verification passed; preference-title test failed for `Equity Research` showing data roles.
- Diagnosed permissive one-token title matching, partial asynchronous refiltering, stale score reuse, and preference-unaware feed visibility.
- Added the approved stronger provider-agnostic filter and truthful company-name fix to the plan; kept one unified dashboard and deferred provider-specific pages.
- Created Plans 03-08 through 03-11 and resolved all checker blockers, including verifier TOCTOU and sole-claim sequencing.
- Added `03-07-SUMMARY.md`, updated ROADMAP/STATE, and committed the planning set as `b3a0d39`.
</completed_work>

<remaining_work>

- Plan 03-08: write RED contracts; implement title relevance, complete scoring hash/freshness, monotonic CAS, company-name persistence, and the short-TTL two-fixture latch.
- Plan 03-09: implement current-preference feed visibility, truthful company display, and save-time cache invalidation.
- Plan 03-10: build/test the fail-closed verifier, request rollout-only approval, deploy, and bind release evidence.
- Plan 03-11: request separate paid-run approval, run exactly one verifier process, then resume UAT one test at a time.
</remaining_work>

<decisions_made>

- Target-title filtering is provider-agnostic across Adzuna, Greenhouse, and Ashby; source metadata never selects a relevance bypass.
- `Coffee Distributor`, `Product Delivery Associate`, and shared-token data roles must filter free for an `Equity Research` target.
- Company names come only from normalized `companies.name` or source-provided names; unknown names stay unknown and are never fabricated.
- Adzuna/provider jobs remain in the unified dashboard. Separate source pages/tabs are deferred.
- Notifications remain removed and must not be reintroduced.
- Rollout approval and the one paid verifier run are separate human decisions.
</decisions_made>

<blockers>
- No current technical blocker. Plans 03-10 and 03-11 contain future human approval checkpoints.
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-scoring-feed-notifications/03-08-PLAN.md` — first executable gap plan and exact test/implementation contract.
2. `.planning/phases/03-scoring-feed-notifications/03-UAT.md` — failed user truth and pending UAT sequence.
3. `.planning/debug/equity-research-title-filter.md` — root-cause evidence.
4. `.planning/phases/03-scoring-feed-notifications/03-09-PLAN.md` — web/feed slice.
5. `.planning/phases/03-scoring-feed-notifications/03-10-PLAN.md` — verifier and rollout boundary.
6. `.planning/phases/03-scoring-feed-notifications/03-11-PLAN.md` — paid proof and final UAT boundary.
7. `.planning/METHODOLOGY.md` if present — apply project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)

- Do not use broad `! vitest` RED gates; use `scripts/verify-expected-red.mjs` with exact expected-failure allowlists.
- Do not treat one shared title token as a match for a multi-concept target title.
- Do not reuse scores unless the full semantic scoring-input hash matches.
- Do not fabricate company names or introduce source-specific filtering branches.
- Do not directly perform the production matching claim before the sole `score-tick` invocation.

## Infrastructure State

- Branch: `main`; planning commit: `b3a0d39`.
- Hosted feed-only baseline: origin commit `2547608b8bf25e7bdb779bdf00542c524cd9ffc3`, migration head 0024, Cloudflare deployment `d2cc3239-c5da-4c83-836a-a17ce050853a`.
- Live asset `/assets/index-DN4oCAxv.js`, SHA-256 `8050578007096e08af609653d30f4363e0b0a90162d9c5e8c33b0a37646703f1`.
- Notifications absent from hosted schema/function/secrets/frontend.
- No servers, watchers, or asynchronous external jobs are running.
- Preserve unrelated user work: modified `.DS_Store`; untracked `scripts/agent-dashboard.mjs` and `scripts/agent-dashboard.test.mjs`.

## Pre-Execution Critique Required

- Design artifacts: Plans 03-08 through 03-11.
- Critique result: complete; final plan checker passed after the sole matching-claim sequencing correction.
- Gate: satisfied. Execution may begin at Plan 03-08 after acknowledging the blocking constraint above.

<context>
The gap is larger than a keyword tweak: preference changes must invalidate semantic scores safely under concurrent workers, the focused feed must stop exposing stale rows, and production proof must avoid paying for or claiming unrelated work. The four plans deliberately separate backend correctness, browser behavior, rollout, and paid proof.
</context>

<next_action>
Start with: `$gsd-execute-phase 3 --gaps-only`, beginning Plan 03-08 and making no hosted/deployment/paid action before its later explicit approval checkpoints.
</next_action>
