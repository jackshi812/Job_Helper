---
context: phase
phase: 03-scoring-feed-notifications
task: 3
total_tasks: 7
status: in_progress
last_updated: 2026-07-19T14:39:21.416Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Carried from Phase 02.1 failures. They govern Plan 07 hosted verification. Acknowledge each before any hosted mutation.

- [ ] CONSTRAINT: Production acceptance must be exact-SHA-bound — a mutable deployment URL previously made stale code indistinguishable from the intended release. Prove origin SHA + deployment/function identity + immutable asset/source hash before accepting hosted results.
- [ ] CONSTRAINT: Hosted evidence must cover the complete data set — Supabase's 1,000-row default truncated a verifier snapshot and produced 550 false drift findings. Keyset/range-paginate deterministic evidence and fail closed on incomplete/non-monotonic pages.
- [ ] CONSTRAINT: Do not loop across quota/eligibility windows — Phase 02.1 exhausted the Adzuna allowance during verification. One explicitly-approved hosted invocation at a time; stop at the checkpoint; never sleep-loop, auto-retry, or alter counters.

**Local Plan 03/04/05/06 work needs no acknowledgment. Do NOT perform hosted mutations or paid calls (beyond already-completed local code) until these boxes are acknowledged at Plan 07.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Treat a mutable production URL as release proof | 02.1 UAT could have accepted stale deployment state. | blocking | Bind every hosted result to exact SHA, deployment/function version, and immutable asset/source hash. |
| Reconcile only the provider-default first page | A 1,000-row snapshot created 550 false immutable-drift flags. | blocking | Keyset/range paginate complete snapshots; fail closed on missing/non-monotonic IDs. |
| Wait or retry through real quota/activation windows | Hosted verification consumed quota and invited repeated runs. | blocking | One approved run per checkpoint; stop and resume after the real UTC/server eligibility boundary. |
| Auto-approve human visual/push/email/assistive results | — | advisory | Present each for explicit human confirmation; never self-approve. |
| Call the deferred 02.1 removal behavior passed | Offline removal hung at `Removing…`, disabled both buttons, no in-modal error. | advisory | Keep Phase 02.1 `gaps_found`; fix later with a bounded timeout/cancellation path and rerun UAT 5-8. Out of Phase 03 scope. |

<current_state>
Phase 03 Wave 1 is complete. Provider was switched Gemini→OpenAI (gpt-5.4 nano, Responses API, strict Structured Outputs, store:false) and committed as the plan contract before any AI code (62fdaf6). Plans 03-01 and 03-02 executed and committed. Wave 2 (Plan 03-03) has NOT started. HEAD e2d5fbb. Working tree clean except the always-excluded user files (.DS_Store, scripts/agent-dashboard.mjs, scripts/agent-dashboard.test.mjs).
</current_state>

<completed_work>

- Plan 03-01 (autonomous, local) — DONE. Commits 9b76141→1268c25. Per-user preferences table FILE `0017_preferences.sql` (not pushed); pure `_shared/filters.ts` cheapFilter implementing D-01 fuzzy title / D-02 exclude-hard-include-soft / D-03 location leniency, zero Deno/npm/jsr specifiers, 17 fixtures; preferences lib + chip-input page + route/nav; schema carries notify_threshold default 75, quiet hours, digest_time, timezone. Gate: 322 tests, build, lint all green.
- Provider revision — DONE. Commit 62fdaf6. Plans 02/03/07 + CONTEXT/RESEARCH/PATTERNS switched to OpenAI gpt-5.4 nano. Residual "Gemini" strings in RESEARCH/CONTEXT/DISCUSSION-LOG are self-marked superseded history.
- Plan 03-02 (non-autonomous) — DONE. Commits e325d25→e2d5fbb. `_shared/openai.ts` Responses wrapper (generateStructured, model constants), `_shared/docx.ts` (bounded extractDocxText, magic-byte detectFormat, zip-bomb guards), `extract-resume/index.ts` (claim_resume_extractions SKIP LOCKED, batch cap 3), migration FILE `0018_resume_extracts.sql` (resume_extracts + ai_usage, FK on delete cascade, mark_user_jobs_for_reroute on 'ready'), `scripts/verify-openai.ts`. One APPROVED live gpt-5.4-nano smoke call SUCCEEDED first try (input 100 / output 45 tokens, ~$0.000076); no retry. Gate: 322 tests, build, lint green.
</completed_work>

<remaining_work>

- Plan 03-03 (autonomous, local) — NOT started. Deps 01+02 satisfied. score-tick claim→filter→route→score→persist worker, `_shared/routing.ts`, migration `0019_user_jobs_scoring.sql`, refilter/reroute RPCs. No paid call during local execution (scoring OpenAI calls happen at hosted runtime only).
- Plan 03-04 (non-autonomous) — NOT started. Dep 03. Scored feed + DOMPurify job detail. Adds dompurify@3.4.12 pin.
- Plan 03-05 (autonomous) — NOT started. Dep 03. notify-tick exactly-once push/email, quiet-hours, migration `0020_notifications.sql`. Resend key needed only for delivery proof (Plan 07).
- Plan 03-06 (autonomous) — NOT started. Deps 01+05. sw.js, push lib, Settings notifications section.
- Plan 03-07 (non-autonomous) — NOT started. Deps all. VAPID/secrets, migrations push, function+frontend deploy, exact-SHA hosted verify, human UAT. Every external mutation requires explicit approval.
- Optional later: fix the Phase 02.1 removal-modal timeout and rerun UAT 5-8.
</remaining_work>

<decisions_made>

- Provider = OpenAI gpt-5.4 nano (Responses API, strict Structured Outputs, store:false), replacing Gemini. Revised the plan contract first rather than inline-diverging. Honors near-zero-cost budget; store:false + OpenAI no-train-by-default covers the resume-text privacy concern D-11 originally protected.
- Keep migrations 0017-0020 fixed; write FILES locally only; no hosted push before Plan 07.
- Worktree isolation disabled this run (fork base unresolved, #683) — executors run sequentially on main.
- Production stays pinned at SHA ee21a719; local Phase 03 work must not imply a deploy.
- Keep the Phase 02.1 removal-modal hang as a deferred gap, out of Phase 03 scope.
</decisions_made>

<blockers>

- No blocker to local Wave 2-4 execution.
- Plan 05/07 email delivery proof blocked until RESEND_API_KEY + RESEND_FROM (verified sender) provided.
- Plan 07 deploy blocked until VAPID keys, Cloudflare/Supabase secrets, and explicit per-mutation approval provided.
</blockers>

## Required Reading (in order)

1. This file — safety boundaries and position.
2. `.planning/phases/03-scoring-feed-notifications/03-03-PLAN.md` — next execution contract.
3. `.planning/phases/03-scoring-feed-notifications/03-02-SUMMARY.md` and `03-01-SUMMARY.md` — what Wave 1 built.
4. `.planning/phases/03-scoring-feed-notifications/03-CONTEXT.md` — D-01..D-21 incl. revised OpenAI decisions D-11..D-13.
5. `.planning/phases/03-scoring-feed-notifications/03-04..07-PLAN.md` — remaining contracts.

## Critical Anti-Patterns (do NOT repeat these)

- Do not push migrations or deploy anything before the approved Plan 07 checkpoint.
- Do not renumber migrations dynamically; use 0017-0020 exactly.
- Do not reintroduce Gemini endpoints/responseSchema/GEMINI_* — provider is OpenAI (`_shared/openai.ts`).
- Do not auto-approve human visual/push/email results.
- Do not mutate first-sight job identity during reroute/refilter — flag rows only.
- Do not stage/commit .DS_Store or scripts/agent-dashboard.* — user-owned, leave untracked.

## Infrastructure State

- Supabase project `fjcsvajkkztvlrpdplwx`; hosted migrations 0001-0016 aligned. 0017/0018 are LOCAL FILES only (not pushed).
- `poll-tick` v11 deployed, verify_jwt=false, handler cron-secret authoritative.
- Cloudflare production deploy `8b07ab41-46a3-4689-89bd-1c5326dc402e`, SHA `ee21a719a610dec45b6523491c406aa39dd973d8`, asset `assets/index-Dir9gIBI.js` SHA-256 `ae31c0d9f838798ed42f7428593ca6cd2006e419b32a65ec06d7883bc2e79573`.
- `OPENAI_API_KEY` present in gitignored scripts/.env; NOT yet set as a Supabase edge secret (Plan 07).
- No background servers, watchers, or async external jobs running.

<context>
Resume by executing Wave 2 = Plan 03-03 (local, autonomous, no spend). Then Wave 3 (03-04 feed, 03-05 notify) — stop at the Resend checkpoint for 05's delivery portion / defer to 07. Then Wave 4 (03-06). Then Wave 5 (03-07) — the deploy/verify wave where every external mutation and each live verifier run needs explicit user approval and the three blocking constraints above apply. Executors run sequentially on main; spawn gsd-executor per plan.
</context>

<next_action>
Start with: `$gsd-execute-phase 03` (resumes at Plan 03-03).
</next_action>
