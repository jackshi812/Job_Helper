---
context: phase
phase: 03-scoring-feed-notifications
task: 0
total_tasks: 7
status: paused-before-execution
last_updated: 2026-07-19T03:06:05.631Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below is grounded in a failed verification or a blocking plan review. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Revise Phase 03 before execution — commit `a007ead` records unresolved HIGH-risk cross-plan defects. Run `$gsd-plan-phase 03 --reviews`, update the plans, and require checker/review convergence before executing any plan.
- [ ] CONSTRAINT: Production acceptance must be exact-SHA-bound — Phase 02.1 exposed stale/mutable deployment evidence. For hosted Phase 03 acceptance, prove origin SHA, deployment identity, immutable asset/function source, and auth boundary before UAT.
- [ ] CONSTRAINT: Hosted evidence must capture the complete data set — Phase 02.1's verifier silently truncated jobs at Supabase's 1,000-row default. Paginate deterministic evidence queries and add churn tests before relying on reconciliation.
- [ ] CONSTRAINT: Do not loop across quota or eligibility windows — Phase 02.1 exhausted its Adzuna allowance during verification. Stop at a checkpoint and resume after a real server-time boundary; never sleep-loop or alter quota counters.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Execute reviewed plans without incorporating review | The Phase 03 review found migration, paid-call ownership, rescoring, notification, feed-query, and production-verifier defects. | blocking | Replan with `--reviews`; run checker/review convergence before execution. |
| Treat a mutable production URL as release proof | Earlier UAT could have accepted stale deployment state. | blocking | Bind every hosted result to exact SHA, deployment/function version, and immutable asset/source hash. |
| Reconcile only the provider-default first page | A 1,000-row snapshot window created 550 false immutable-drift flags when new rows displaced old rows. | blocking | Keyset/range paginate complete snapshots; fail closed on missing or non-monotonic IDs. |
| Wait or retry through real quota/activation windows | Hosted verification consumed quota and previously tempted repeated runs. | blocking | One approved run per checkpoint; stop and resume after the actual UTC/server eligibility boundary. |
| Call the deferred removal behavior passed | Offline removal remained indefinitely at `Removing…`, disabled both buttons, and showed no in-modal error. | advisory | Keep Phase 02.1 `gaps_found`; fix later with a bounded timeout/cancellation path and rerun UAT 5-8. |

<current_state>
Phase 02.1 execution is concluded: 13/13 plans ran. Automated and hosted proof passed, but human UAT found one user-deferred removal-modal gap, so the phase truthfully remains `gaps_found`.

Phase 03 is discussed, researched, UI-specified, planned as seven plans, and reviewed. Execution has NOT started. The current plan set is not ready to execute because `03-REVIEWS.md` identifies multiple HIGH-risk correctness defects.

Repository position before the pause commit:
- Local `main`: `a007ead` (`docs: cross-AI review for phase 3`)
- `origin/main` / Cloudflare production: `ee21a719a610dec45b6523491c406aa39dd973d8`
- Unrelated dirty files: `.DS_Store`, `scripts/agent-dashboard.mjs`, and `scripts/agent-dashboard.test.mjs`; preserve them.
</current_state>

<completed_work>

Completed this session:
- Executed Phase 02.1 gap plans 09-13.
- Repaired fingerprint repost identity mutation and verifier snapshot pagination with RED/GREEN tests.
- Deployed only `poll-tick` v11 and proved Cloudflare production SHA/asset identity.
- Final pipeline invocation `b5aebea9-3e37-4e64-b3e1-821a9a354473` passed probes 1-17 with zero restoration conflicts, residue, immutable drift, unexplained diffs, or redaction failures.
- Source coverage completed with `connectors=verified idempotent=true`.
- Local gate passed: 301 tests, build, and lint.
- Human UAT checks 1-4 passed. Check 5 reproduced the offline removal hang; Curi Capital remained intact after safe recovery. The user deferred the fix and checks 6-8.
- Phase 03 plans 01-07 were created (`d3ae8b7`) and reviewed (`a007ead`).
</completed_work>

<remaining_work>

1. Run `$gsd-plan-phase 03 --reviews` and revise all seven plans.
2. Resolve at minimum:
   - fixed Phase 03 migration allocation rather than self-incrementing offsets;
   - atomic exclusive resume-extraction claims to prevent duplicate paid calls;
   - extraction-completion refilter/rescore and waiting-state semantics;
   - atomic AI-budget reservation and notification delivery claims/retries;
   - correct feed company join, bounded list/detail queries, and sanitizer tests;
   - a fixture-isolated production verifier that cannot notify real users.
3. Rerun plan checker/cross-AI review until blocking concerns converge.
4. Execute revised Phase 03 plans 01-07.
5. Optional later gap cycle: `$gsd-plan-phase 02.1 --gaps` for the removal timeout and UAT checks 5-8.
</remaining_work>

<decisions_made>

- User chose to defer the Phase 02.1 modal timeout; retain it as a known gap, not a pass.
- Production stays on exact SHA `ee21a719`; local pagination hardening changes only the verifier.
- The Adzuna operational cutoff remains 75/day; it was not raised to accelerate verification.
- Phase 03 must extend the approved Tailwind/zinc UI system and use pinned `dompurify@3.4.12` as the only planned frontend dependency.
- Do not plan the deferred star/shortlist, three-resume comparison, or Pro rescore UI.
</decisions_made>

<blockers>

- Phase 03 plan review is unresolved. Current plans are HIGH risk and must not execute as written.
- Phase 02.1 remains `gaps_found` by explicit accepted deferral; this does not prevent the user from moving on, but must remain visible.
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-scoring-feed-notifications/03-REVIEWS.md` — blocking review findings to incorporate.
2. `.planning/phases/03-scoring-feed-notifications/03-CONTEXT.md` — user decisions D-14 through D-21.
3. `.planning/phases/03-scoring-feed-notifications/03-RESEARCH.md` — Gemini, DOCX, push, digest, and security constraints.
4. `.planning/phases/03-scoring-feed-notifications/03-UI-SPEC.md` — approved visual/accessibility contract.
5. `.planning/phases/03-scoring-feed-notifications/03-PATTERNS.md` — codebase pattern assignments; re-check stale Phase 02.1 caveats.
6. `.planning/phases/03-scoring-feed-notifications/03-01-PLAN.md` through `03-07-PLAN.md` — current plans to revise.
7. `.planning/phases/02.1-source-coverage-expansion/02.1-13-SUMMARY.md` and `02.1-UAT.md` — accepted deferred gap.
8. `.planning/STATE.md` and `.planning/ROADMAP.md` — canonical project position.

## Critical Anti-Patterns (do NOT repeat these)

- Do not auto-approve a human visual/assistive result.
- Do not rerun hosted commands after a failed approved invocation without new explicit approval.
- Do not infer gateway rejection from handler output; distinguish gateway versus handler entry with direct evidence.
- Do not mutate first-sight job identity during repost/fingerprint merges; only `last_seen_at` may advance.
- Do not trust partial evidence snapshots; paginate the complete set.

## Infrastructure State

- Supabase project: `fjcsvajkkztvlrpdplwx`; migrations 0001-0016 were aligned during Phase 02.1.
- `poll-tick` v11 is deployed with `verify_jwt=false`; handler cron-secret enforcement remains authoritative.
- Cloudflare production deployment: `8b07ab41-46a3-4689-89bd-1c5326dc402e`, SHA `ee21a719a610dec45b6523491c406aa39dd973d8`.
- Production asset: `assets/index-Dir9gIBI.js`, SHA-256 `ae31c0d9f838798ed42f7428593ca6cd2006e419b32a65ec06d7883bc2e79573`.
- Edge source manifest: `33addcaaa514633b93f919c6c9b3978f962098a03be6f0c594857ff988d749f1`.
- No background servers, watchers, agents, or async external jobs are intentionally running.
- A controllable in-app browser was unavailable; the user completed UAT in Chrome manually.

## Pre-Execution Critique Required

- Design artifacts: `03-01-PLAN.md` through `03-07-PLAN.md`.
- Critique artifact: `03-REVIEWS.md` at commit `a007ead`.
- Critique focus: migration ordering, exclusive paid work claims, prerequisite waiting/rescore triggers, atomic budgets and notifications, feed relationship/query correctness, retry/idempotency, XSS coverage, and fixture isolation.
- Gate: Do NOT begin Phase 03 execution until the plans are revised and plan-check/review convergence passes.

<context>
The immediate goal is not to fix the deferred Watchlist modal. The user explicitly chose to move on. Phase 03 has strong initial design context but the new Codex review found real cross-plan defects. Resume at plan revision, preserve all Phase 02.1 safety boundaries, and do not let the existence of seven plan files be mistaken for execution readiness.
</context>

<next_action>
Start with: `$gsd-plan-phase 03 --reviews`
</next_action>
