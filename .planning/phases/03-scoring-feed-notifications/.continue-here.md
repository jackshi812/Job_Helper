---
context: phase
phase: 03-scoring-feed-notifications
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-07-19T20:30:00-05:00
---

# Phase 03 Plan 07 Feed-only Checkpoint

## Owner Decision

Notifications were removed from product scope on 2026-07-19. Scored jobs remain available through the dashboard feed. Do not rebuild push or email alerts.

## Local Work

- Notification Edge Function, shared sender helpers, browser push client, service worker, UI controls, tests, verifier, VAPID generator, and local VAPID key removed.
- Migration 0024 unschedules the notification cron, preserves account deletion, drops notification RPC/tables, and removes notification preference columns.
- Scoring verifier and active project/planning docs changed to feed-only.
- Preserve user files: `.DS_Store`, `scripts/agent-dashboard.mjs`, `scripts/agent-dashboard.test.mjs`.

## Hosted State Still Active

- Remote migration head is 0023 until approval to push 0024.
- Hosted `notify-tick` version 1, its cron, notification data, subscriptions, and notification secrets still exist.
- Production frontend still contains the old notification UI/service-worker asset until the local release is pushed and rebuilt.

## Next Work

1. Finish local tests/build/lint and commit the removal without staging preserved user files.
2. Ask for explicit approval before hosted cleanup because migration 0024 permanently deletes notification data/settings.
3. With approval: push migration 0024, delete `notify-tick`, remove notification-only secrets/environment, push `origin/main`, and verify the exact deployed artifact.
4. Run one approved hosted scoring verifier invocation.
5. Complete feed/scoring human UAT and write `03-07-SUMMARY.md`.
