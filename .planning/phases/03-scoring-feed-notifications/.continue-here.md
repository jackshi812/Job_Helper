---
context: phase
phase: 03-scoring-feed-notifications
task: 7
total_tasks: 7
status: in_progress
last_updated: 2026-07-19T15:29:40.447Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Carried from Phase 02.1 failures. They govern ALL of Plan 07 part B (hosted deploy + verify). Acknowledge each before any hosted mutation.

- [ ] CONSTRAINT: Production acceptance must be exact-SHA-bound — a mutable deployment URL previously made stale code indistinguishable from the intended release. Prove origin SHA + deployment/function identity + immutable asset/source hash before accepting hosted results.
- [ ] CONSTRAINT: Hosted evidence must cover the complete data set — Supabase's 1,000-row default truncated a verifier snapshot and produced 550 false drift findings. Keyset/range-paginate deterministic evidence and fail closed on incomplete/non-monotonic pages.
- [ ] CONSTRAINT: Do not loop across quota/eligibility windows — Phase 02.1 exhausted the Adzuna allowance during verification. One explicitly-approved hosted invocation at a time; stop at the checkpoint; never sleep-loop, auto-retry, or alter counters.

**All Phase 03 local code is done. Do NOT push migrations, deploy functions/frontend, set secrets, or run hosted verifiers until these boxes are acknowledged AND the user approves each step.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Treat a mutable production URL as release proof | 02.1 UAT could have accepted stale deployment state. | blocking | Bind every hosted result to exact SHA, deployment/function version, and immutable asset/source hash. |
| Reconcile only the provider-default first page | A 1,000-row snapshot created 550 false immutable-drift flags. | blocking | Keyset/range paginate complete snapshots; fail closed on missing/non-monotonic IDs. |
| Wait or retry through real quota/activation windows | Hosted verification consumed quota and invited repeated runs. | blocking | One approved run per checkpoint; stop and resume after the real UTC/server eligibility boundary. |
| Auto-approve human visual/push/email/assistive results | — | advisory | Present each for explicit human confirmation; never self-approve. Plan 07 human UAT (feed visuals, adversarial sanitizer fixtures, tab-closed push) is required. |
| Call the deferred 02.1 removal behavior passed | Offline removal hung at `Removing…`, disabled both buttons, no in-modal error. | advisory | Keep Phase 02.1 `gaps_found`; fix later with a bounded timeout/cancellation path and rerun UAT 5-8. Out of Phase 03 scope. |

<current_state>
Phase 03 Waves 1-4 are COMPLETE. All local code for Plans 03-01 through 03-06 is written, tested, and committed (25 plan commits; full suite 371 tests green; build + lint clean). Only Plan 03-07 remains, and it has NOT started. Migrations 0017-0020 are LOCAL FILES ONLY (never pushed). The three new edge functions (extract-resume, score-tick, notify-tick) exist in-repo but are NOT deployed. HEAD after Wave 4 = 961e56c (a WIP handoff commit sits on top of it). Working tree clean except the always-excluded user files (.DS_Store, scripts/agent-dashboard.mjs, scripts/agent-dashboard.test.mjs).
</current_state>

<completed_work>

- 03-01 (commits 9b76141→1268c25): preferences table FILE 0017 + pure cheapFilter (D-01/D-02/D-03).
- Provider revision (62fdaf6): Gemini→OpenAI gpt-5.4 nano across Plans 02/03/07 + CONTEXT/RESEARCH/PATTERNS.
- 03-02 (e325d25→e2d5fbb): _shared/openai.ts Responses wrapper + _shared/docx.ts + extract-resume + 0018 (resume_extracts, ai_usage, FK cascade, mark_user_jobs_for_reroute). One approved live gpt-5.4-nano smoke call succeeded (~$0.000076).
- 03-03 (b0c4e49→0f876b3): user_jobs + claim_scoring_work SKIP LOCKED + _shared/routing.ts + score-tick + refilter/reroute hooks (migration 0019). Scoring via generateStructured, cheapFilter gates AI, poll-tick untouched.
- 03-04 (1c20610→53bb154): feed lib + Dashboard feed table + JobDetail (DOMPurify.sanitize) + dompurify@3.4.12 pin. Per-user seen/dismiss.
- 03-05 (33004d9→34f8674): 0020 notifications (UNIQUE(user_id,job_id,channel), claim_notifications SKIP LOCKED, retry_at/attempts, delete_my_data) + quiet-hours.ts + webpush.ts + notify-tick (Idempotency-Key digest, terminal-only failure, dry-run transport).
- 03-06 (ae324e3→961e56c): vanilla sw.js (push + notificationclick focus-existing-tab) + push.ts (enable/status/disable, dead-sub detection) + Settings Notifications section (threshold slider/quiet-hours/digest time).
</completed_work>

<remaining_work>

Plan 03-07 only. Two parts:

**Part A — LOCAL, no external action, no spend (can run immediately on resume):**
- scripts/generate-vapid-keys.mjs (P-256 keypair, importVapidKeys-compatible JSON + base64url public key)
- scripts/verify-scoring.ts (hosted RLS + pipeline assertions; POST /functions/v1/score-tick with x-cron-secret; keyset-paginated, exact-SHA-bound; >=60 lines)
- scripts/verify-notifications.ts (hosted notify-tick: uniqueness, digest idempotency; >=40 lines)
- web/.env.example (documents VITE_VAPID_PUBLIC_KEY etc.)

**Part B — HOSTED, each step needs explicit user approval + setup:**
1. User provides RESEND_API_KEY + RESEND_FROM (verified sender/domain).
2. Generate VAPID keypair (local), record public/private.
3. Set edge secrets: OPENAI_API_KEY, VAPID_KEYS, RESEND_API_KEY, RESEND_FROM, SITE_URL.
4. Re-verify Phase 02.1 final state FIRST (migration head, jobs schema) — discharge the parallel-execution caveat.
5. Push migrations 0017-0020 to hosted DB.
6. Deploy edge functions extract-resume, score-tick, notify-tick with verify_jwt=false.
7. Deploy frontend (Cloudflare Pages) with VITE_VAPID_PUBLIC_KEY.
8. Run hosted verifiers (one approved invocation each, exact-SHA-bound, complete-paginated evidence, fail-closed).
9. Human UAT: real job flows extraction→filter→route→score→user_jobs(reasons+gaps)→notification row→received browser push with tab closed; cross-user isolation on preferences/user_jobs/push_subscriptions/notifications with independent publishable-key sessions.
- Optional later: fix Phase 02.1 removal-modal timeout, rerun UAT 5-8.
</remaining_work>

<decisions_made>

- AI provider = OpenAI gpt-5.4 nano (Responses API, strict Structured Outputs, store:false). Revised contract first. Budget + privacy covered.
- Migrations 0017-0020 fixed; local files only; no push before Plan 07.
- Worktree isolation disabled this run (#683); sequential executors on main.
- Production pinned at SHA ee21a719; local Phase 03 must not imply a deploy.
- Keep Phase 02.1 removal-modal hang as a deferred gap, out of scope.
</decisions_made>

<blockers>

- No blocker to Plan 07 part A (local scripts).
- Part B fully blocked until RESEND_API_KEY + RESEND_FROM provided AND user approves each external mutation/verifier run.
</blockers>

## Required Reading (in order)

1. This file — safety boundaries and position.
2. `.planning/phases/03-scoring-feed-notifications/03-07-PLAN.md` — deploy/verify contract.
3. `.planning/phases/03-scoring-feed-notifications/03-05-SUMMARY.md` and `03-06-SUMMARY.md` — notify/push contracts (VAPID_KEYS, SITE_URL, RESEND_* env expected).
4. `.planning/phases/03-scoring-feed-notifications/03-CONTEXT.md` — D-11..D-21.
5. `.planning/phases/02.1-source-coverage-expansion/02.1-13-SUMMARY.md` — 02.1 final hosted state to re-verify before push.

## Critical Anti-Patterns (do NOT repeat these)

- Do not push migrations or deploy before per-step user approval at Plan 07.
- Do not renumber migrations; 0017-0020 exactly.
- Do not reintroduce Gemini endpoints/responseSchema/GEMINI_* — provider is OpenAI (`_shared/openai.ts`).
- Do not accept hosted results by mutable URL — exact-SHA-bind.
- Do not trust the first evidence page — paginate complete, fail closed.
- Do not retry/loop a failed hosted invocation without new explicit approval.
- Do not auto-approve human visual/push/email UAT.
- Do not stage/commit .DS_Store or scripts/agent-dashboard.* — leave untracked.

## Infrastructure State

- Supabase project `fjcsvajkkztvlrpdplwx`; hosted migrations 0001-0016 aligned. 0017-0020 are LOCAL FILES only (not pushed).
- `poll-tick` v11 deployed, verify_jwt=false, handler cron-secret authoritative. extract-resume/score-tick/notify-tick NOT deployed.
- Cloudflare production deploy `8b07ab41-46a3-4689-89bd-1c5326dc402e`, SHA `ee21a719a610dec45b6523491c406aa39dd973d8`, asset `assets/index-Dir9gIBI.js` SHA-256 `ae31c0d9f838798ed42f7428593ca6cd2006e419b32a65ec06d7883bc2e79573`.
- `OPENAI_API_KEY` present in gitignored scripts/.env; NOT yet a Supabase edge secret. No RESEND_* or VAPID keys yet.
- No background servers, watchers, or async external jobs running.

<context>
Resume by executing Plan 03-07 part A (local scripts — safe, no external action). Then STOP at the first hosted mutation and drive part B interactively: collect Resend creds, then walk each external step (secrets → 02.1 re-verify → migrations push → function deploys → frontend deploy → hosted verifiers → human UAT) with explicit user approval, one approved hosted invocation at a time, exact-SHA-bound and fail-closed on incomplete evidence. Executors run sequentially on main; branching_strategy=none.
</context>

<next_action>
Start with: `$gsd-execute-phase 03` (resumes at Plan 03-07; do part A local scripts, then stop for part B approvals).
</next_action>
