---
context: phase
phase: 03-scoring-feed-notifications
task: 1
total_tasks: 7
status: paused-at-human-checkpoint
last_updated: 2026-07-19T04:05:48.251Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints came from failures during Phase 02.1. The executor must acknowledge them before changing hosted state.

- [ ] CONSTRAINT: Production acceptance must be exact-SHA-bound — mutable deployment URLs previously made stale code indistinguishable from the intended release. Prove the origin SHA, deployment/function identity, and immutable asset/source hash before accepting hosted results.
- [ ] CONSTRAINT: Hosted evidence must cover the complete data set — Supabase's 1,000-row default truncated a verifier snapshot and produced false drift findings. Paginate deterministic evidence queries and fail closed on incomplete/non-monotonic pages.
- [ ] CONSTRAINT: Do not loop across quota or eligibility windows — Phase 02.1 exhausted the Adzuna allowance during verification. Run one explicitly approved hosted invocation at a time and stop at the checkpoint; never sleep-loop, auto-retry, or alter counters.

**Do not perform hosted mutations or paid calls until these boxes are acknowledged. Local Plan 01 work may begin after reading this handoff.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Treat a mutable production URL as release proof | Earlier UAT could have accepted stale deployment state. | blocking | Bind every hosted result to exact SHA, deployment/function version, and immutable asset/source hash. |
| Reconcile only the provider-default first page | A 1,000-row snapshot window created 550 false immutable-drift flags. | blocking | Keyset/range paginate complete snapshots; fail closed on missing or non-monotonic IDs. |
| Wait or retry through real quota/activation windows | Hosted verification consumed quota and invited repeated runs. | blocking | One approved run per checkpoint; stop and resume after the real UTC/server eligibility boundary. |
| Repeat external convergence attempts | Claude export was denied twice by tenant privacy policy; no second independent review artifact can be produced in this environment. | advisory | Accept the user's decision to proceed with the revised plans; do not retry export or call an in-process Claude review independent. |
| Call the deferred removal behavior passed | Offline removal remained indefinitely at `Removing…`, disabled both buttons, and showed no in-modal error. | advisory | Keep Phase 02.1 `gaps_found`; address later with a bounded timeout/cancellation path and rerun UAT 5-8. |

<current_state>
Phase 02.1 execution is concluded: all 13 plans ran. Automated and hosted proof passed, but human UAT found one user-deferred removal-modal gap, so the phase truthfully remains `gaps_found`. That gap is not Phase 03 scope.

Phase 03 is discussed, researched, UI-specified, planned as seven plans, reviewed, and revised. Claude completed Plan 01 at commit `1268c25`; execution is 1/7 plans, now at Plan 02 Task 1. Codex review commit `a007ead` identified cross-plan correctness defects; commit `22049a9` revised all plans to address them. Review-convergence attempts could not produce a second independent review because tenant privacy policy denied external Claude export twice. The user explicitly accepted that remaining review risk and chose to continue execution.

Repository position at the provider revision:
- Local `main`: `1268c25` (`docs(03-01): add preferences & cheap-filters plan summary`)
- `origin/main` / Cloudflare production: `ee21a719a610dec45b6523491c406aa39dd973d8`
- Unrelated user-owned dirt: `.DS_Store`, `scripts/agent-dashboard.mjs`, and `scripts/agent-dashboard.test.mjs`; preserve and do not stage them.
</current_state>

<completed_work>

Completed before handoff:
- Phase 02.1 gap closure: 13/13 plans executed; final pipeline invocation `b5aebea9-3e37-4e64-b3e1-821a9a354473` passed probes 1-17 with all envelope gates clean.
- Source coverage proof completed with `connectors=verified idempotent=true`; local gate passed 301 tests, build, and lint.
- Human UAT checks 1-4 passed; check 5 reproduced the deferred offline removal hang. The user chose to move on.
- Phase 03 context, research, UI contract, patterns, and seven plans created in `d3ae8b7`.
- Cross-AI/Codex plan review recorded in `a007ead`.
- All seven Phase 03 plans revised in `22049a9`: fixed migrations 0017-0020, atomic claims/budgets/delivery, extraction-ready rerouting, correct feed joins, retry-safe push/digest behavior, isolated production verification, bounded DOCX parsing, word-boundary excludes, and service-worker origin checks.
- Plan review convergence enabled in `2b056c8`; independent external Claude review remained unavailable due privacy policy, not unresolved plan edits.
- Provider revised on 2026-07-19 before execution: GPT-5.4 nano through OpenAI Responses (`store:false`, strict Structured Outputs, reasoning none) replaces Gemini in CONTEXT, Plans 02/03/07, patterns, research addendum, and this handoff. A 20-pair quality eval gates any later GPT-5.6 Luna upgrade.
- Phase 03 Plan 01 completed at `1268c25`: migration 0017 remains local-only; 322 tests, build, and lint passed; no hosted state or paid API was touched.
</completed_work>

<remaining_work>

1. Execute `03-02-PLAN.md`: start at Task 1 for the project-scoped OpenAI API key/API billing checkpoint, then build DOCX extraction and the OpenAI Responses wrapper after explicit paid-call approval.
2. Execute `03-03-PLAN.md`: atomic scoring pipeline and rerouting/rescore behavior.
3. Execute `03-04-PLAN.md`: scored feed and sanitized job detail.
4. Execute `03-05-PLAN.md`: exactly-once push/email notification backend; pause for Resend setup.
5. Execute `03-06-PLAN.md`: push enablement, service worker, and alert settings.
6. Execute `03-07-PLAN.md`: VAPID/secrets, migrations, deployment, exact-SHA verification, 20-pair model-quality gate, and human UAT; every external mutation requires explicit approval.
7. Optional later gap cycle: fix the Phase 02.1 removal timeout and rerun UAT checks 5-8.
</remaining_work>

<decisions_made>

- Proceed with Phase 03 execution despite the unavailable second independent review; the revised Codex-reviewed plans are the accepted execution baseline.
- Use GPT-5.4 nano for extraction/scoring, with GPT-5.6 Luna only as an eval-backed configuration upgrade. ChatGPT Pro does not include API usage.
- Do not retry external Claude export. A Claude executor may inspect the plans, but that is execution-time judgment, not independent convergence evidence.
- Preserve fixed migration allocation: `0017_preferences.sql`, `0018_resume_extracts.sql`, `0019_user_jobs_scoring.sql`, `0020_notifications.sql`.
- No push, deploy, schema migration, paid OpenAI call, or live production verifier run without explicit user approval at the plan checkpoint.
- Preserve first-sight job identity; repost/fingerprint merges may advance only `last_seen_at`.
- Keep the Phase 02.1 removal-modal issue as a known deferred gap, not a pass and not Phase 03 scope.
- Production remains on exact SHA `ee21a719`; local Phase 3 execution must not imply production deployment.
- Adzuna operational cutoff remains 75/day.
- Continue the approved Tailwind/zinc UI system and pinned `dompurify@3.4.12`; exclude deferred star/shortlist, three-resume comparison, and Pro-rescore UI.
</decisions_made>

<blockers>

- No immediate blocker to local Plan 01 execution.
- Plan 02 live verification is blocked until the user supplies a project-scoped `OPENAI_API_KEY` from an API-billed OpenAI project in gitignored `scripts/.env`, records the non-secret project/key identifier, and approves the paid smoke call. ChatGPT Pro does not include API usage.
- Plan 05/07 delivery proof requires `RESEND_API_KEY`, `RESEND_FROM`, and a verified Resend sender/domain.
- Plan 07 deployment requires VAPID keys, Cloudflare/Supabase secrets, and explicit approval for schema push, function deployment, frontend deployment, and each live verifier invocation.
- Independent review convergence is unavailable because external Claude export was policy-denied. The user accepted this as residual risk; it is not an execution blocker.
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-scoring-feed-notifications/.continue-here.md` — authoritative handoff and safety boundaries.
2. `.planning/phases/03-scoring-feed-notifications/03-01-PLAN.md` through `03-07-PLAN.md` — revised execution contract.
3. `.planning/phases/03-scoring-feed-notifications/03-CONTEXT.md` — user decisions D-14 through D-21.
4. `.planning/phases/03-scoring-feed-notifications/03-RESEARCH.md` — read the 2026-07-19 OpenAI provider-revision section first; older Gemini sections are historical, while DOCX, push, digest, and security constraints remain applicable.
5. `.planning/phases/03-scoring-feed-notifications/03-UI-SPEC.md` — approved visual/accessibility contract.
6. `.planning/phases/03-scoring-feed-notifications/03-PATTERNS.md` — codebase pattern assignments.
7. `.planning/phases/03-scoring-feed-notifications/03-REVIEWS.md` plus commit `22049a9` — original findings and their plan revisions.
8. `.planning/phases/02.1-source-coverage-expansion/02.1-13-SUMMARY.md` and `02.1-UAT.md` — accepted deferred gap and hosted proof.
9. `.planning/STATE.md` and `.planning/ROADMAP.md` — wider project position; note that STATE continuity may lag this handoff.

## Critical Anti-Patterns (do NOT repeat these)

- Do not auto-approve any human visual, browser-push, email, or assistive result.
- Do not rerun a hosted command after an approved invocation fails without new explicit approval.
- Do not infer gateway rejection from handler output; prove gateway versus handler entry separately.
- Do not mutate first-sight job identity during repost/fingerprint merges.
- Do not trust partial evidence snapshots; paginate the complete set.
- Do not renumber migrations dynamically; use 0017-0020 exactly.
- Do not deploy from a dirty or ambiguous SHA, and do not use a mutable URL alone as proof.

## Infrastructure State

- Supabase project: `fjcsvajkkztvlrpdplwx`; migrations 0001-0016 are aligned.
- `poll-tick` v11 is deployed with `verify_jwt=false`; handler cron-secret enforcement is authoritative.
- Cloudflare production deployment: `8b07ab41-46a3-4689-89bd-1c5326dc402e`, SHA `ee21a719a610dec45b6523491c406aa39dd973d8`.
- Production asset: `assets/index-Dir9gIBI.js`, SHA-256 `ae31c0d9f838798ed42f7428593ca6cd2006e419b32a65ec06d7883bc2e79573`.
- Edge source manifest: `33addcaaa514633b93f919c6c9b3978f962098a03be6f0c594857ff988d749f1`.
- No background servers, watchers, agents, or async external jobs are intentionally running.

## Pre-Execution Critique Status

- Design artifacts: `03-01-PLAN.md` through `03-07-PLAN.md`.
- Critique artifact: `03-REVIEWS.md` at `a007ead`.
- Revision artifact: all plans updated at `22049a9` to address the critique.
- Independent convergence status: unavailable after two privacy-policy denials; no convergence artifact or count exists.
- Gate: the user explicitly accepted this residual review risk and authorized handoff for execution. The executor should read the critique and revisions, then follow plan checkpoints; do not require or fabricate a convergence pass.

<context>
The immediate goal is to let Claude execute Phase 03 from the revised plans. Start locally with Plan 01. Plans 01 and 02 share wave 1, but Plan 02 contains a human key/paid-service checkpoint; do not let parallelization bypass it. Preserve all Phase 02.1 safety boundaries and unrelated worktree files. Stop at each human/external mutation checkpoint instead of assuming authorization.
</context>

<next_action>
Start with: `$gsd-execute-phase 03` and resume at Plan 02 Task 1; Plan 01 is complete.
</next_action>
