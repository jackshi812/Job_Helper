---
phase: 03-scoring-feed-notifications
plan: 07
type: execute
wave: 5
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06]
files_modified:
  - scripts/generate-vapid-keys.mjs
  - scripts/verify-scoring.ts
  - scripts/verify-notifications.ts
  - web/.env.example
autonomous: false
requirements: [PREF-01, RESU-01, SCOR-01, SCOR-02, SCOR-03, SCOR-04, SCOR-05, NOTF-01, NOTF-02, NOTF-03, NOTF-04]
user_setup:
  - service: resend
    why: "Email digest + fallback sender (D-20)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend dashboard -> API Keys"
    dashboard_config:
      - task: "Verify a sending domain or use the onboarding sender; note the from address for RESEND_FROM"
        location: "Resend dashboard -> Domains"
  - service: gemini
    why: "Paid-tier key provisioned in Plan 02 must now become an edge secret"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Already in scripts/.env from Plan 02 checkpoint"
must_haves:
  truths:
    - "All Phase 3 migrations are pushed to the hosted database and all three new edge functions are deployed with verify_jwt disabled ([BLOCKING] — verification is false-positive without this)"
    - "Cross-user isolation is proven on hosted preferences, user_jobs, push_subscriptions, and notifications with independent publishable-key sessions (Phase 1 pattern)"
    - "A real job flows end-to-end on hosted infra: extraction → filter → route → score → user_jobs row with reasons and gaps → notification row → received browser push (with tab closed) — the phase's core promise"
    - "The 02.1 final state was re-verified before any push (migration head, jobs schema) — parallel-execution caveat discharged"
  artifacts:
    - path: "scripts/verify-scoring.ts"
      provides: "hosted RLS + pipeline assertions for scoring tables and score-tick"
      min_lines: 60
    - path: "scripts/verify-notifications.ts"
      provides: "hosted notify-tick assertions: uniqueness, digest idempotency"
      min_lines: 40
    - path: "scripts/generate-vapid-keys.mjs"
      provides: "P-256 VAPID keypair generator emitting importVapidKeys-compatible JSON + base64url public key"
      contains: "P-256"
  key_links:
    - from: "scripts/verify-scoring.ts"
      to: "hosted score-tick"
      via: "POST /functions/v1/score-tick with x-cron-secret from gitignored env"
      pattern: "score-tick"
    - from: "web/.env.example"
      to: "web/src/lib/push.ts"
      via: "VITE_VAPID_PUBLIC_KEY documented for the Pages build"
      pattern: "VITE_VAPID_PUBLIC_KEY"
---

<objective>
Ship it and prove it: generate VAPID keys, set all edge secrets, push the Phase 3 schema ([BLOCKING] — builds pass without it, hosted reality does not), deploy score-tick/notify-tick/extract-resume, then prove the full promise on hosted infrastructure — RLS isolation, extraction, scoring with reasons/gaps, exactly-once notifications, and a real browser push received with the tab closed — ending in the human UAT checkpoint.

Purpose: Phases 1–2 established that hosted proof is a separate discipline from green builds; this plan is Phase 3's 02-07/02.1-07 equivalent.
Output: VAPID keygen + two verification scripts, updated web/.env.example, deployed + proven hosted system.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-feed-notifications/03-CONTEXT.md
@.planning/phases/03-scoring-feed-notifications/03-RESEARCH.md
@.planning/phases/03-scoring-feed-notifications/03-PATTERNS.md
</context>

## Phase Goal

**As a** job seeker, **I want to** be alerted within minutes of a relevant new posting — with cheap filters gating AI cost and AI scoring survivors against my own resume and preferences — **so that** only deduplicated matches above my threshold ever notify me.

<tasks>

<task type="auto">
  <name>Task 1: VAPID keygen + secrets + 02.1 re-verification + [BLOCKING] db push + function deploy</name>
  <files>scripts/generate-vapid-keys.mjs, web/.env.example</files>
  <read_first>
    - .planning/phases/02.1-source-coverage-expansion/02.1-VERIFICATION.md (final 02.1 state — MUST read before pushing; if absent or 02.1 plans 12–13 are still executing, STOP and surface to the user rather than pushing over an in-flight phase)
    - supabase/migrations/ directory listing (final head number — apply the Phase 3 numbering rule: if 02.1 landed migrations past 0016, renumber the four Phase 3 migration files by the OFFSET while preserving their relative order)
    - https://github.com/negrel/webpush — src of exportVapidKeys/importVapidKeys (confirm the JSON shape the generator must emit: JWK pair)
    - supabase/config.toml if present, and how Phase 2 deployed functions with JWT verification disabled (02-07/02.1-07 SUMMARYs in .planning/phases/)
    - scripts/verify-pipeline.ts (env conventions: scripts/.env holds project URL, cron secret, service/publishable keys)
  </read_first>
  <action>
    1. GATE — 02.1 reconciliation: confirm 02.1's VERIFICATION.md exists and its recorded migration head; run `ls supabase/migrations | sort | tail -1`; if head > the numbers used by Plans 01/02/03/05, rename the four Phase 3 migration files applying the OFFSET rule (git mv, preserving order preferences < resume_extracts < user_jobs_scoring < notifications). Confirm no Phase 3 file collides with a 02.1 migration number. Also confirm `public.jobs` still has the columns feed.ts and score-tick select (title, location, description_text, description_html, status, first_seen_at, apply-URL and posted-time columns) — RESEARCH Pitfall 8.
    2. Create `scripts/generate-vapid-keys.mjs` (node:crypto webcrypto): generate an ECDSA P-256 keypair, export publicKey and privateKey as JWKs, print `{ publicKey: <jwk>, privateKey: <jwk> }` JSON to a gitignored file `scripts/vapid-keys.json` (confirm gitignore covers it; add an entry if not), plus the base64url-encoded uncompressed public key point for the browser (`VITE_VAPID_PUBLIC_KEY`). Before finalizing, verify the JSON field shape against @negrel/webpush's importVapidKeys/exportVapidKeys source and adjust the generator to emit exactly that shape.
    3. Secrets: using the Supabase CLI from web devDependencies (`npx supabase --workdir .` per established project usage; requires SUPABASE_ACCESS_TOKEN in the environment for non-TTY): `npx supabase secrets set GEMINI_API_KEY=... RESEND_API_KEY=... RESEND_FROM=... VAPID_KEYS=<json> SITE_URL=<deployed Pages origin> AI_DAILY_SCORE_CAP=200` (values sourced from scripts/.env and scripts/vapid-keys.json; never echo secret values into logs or commit them). CRON_SECRET already exists from Phase 2. If RESEND_API_KEY or RESEND_FROM are missing from scripts/.env, pause with a checkpoint asking the user to add them (Resend dashboard) before proceeding.
    4. Append `VITE_VAPID_PUBLIC_KEY=` (with the generated value as a comment placeholder, not the real key if the file is committed — check whether web/.env.example is committed and keep real values only in gitignored web/.env and the Cloudflare Pages env settings) to `web/.env.example`; set the real value in local web/.env and note that the Cloudflare Pages dashboard env var must be updated at next deploy.
    5. **[BLOCKING] Schema push:** `npx supabase db push` (non-TTY: ensure SUPABASE_ACCESS_TOKEN is set; if interactive prompts cannot be suppressed, stop and hand the exact command to the user). Then verify remotely: query the hosted DB (service key from scripts/.env) confirming tables preferences, resume_extracts, ai_usage, user_jobs, push_subscriptions, notifications exist and cron.job contains score-tick-every-minute, notify-tick-every-minute, extract-resume-every-minute.
    6. Deploy: `npx supabase functions deploy score-tick notify-tick extract-resume --no-verify-jwt` (match the exact deploy invocation style recorded in the 02-07/02.1-07 summaries).
  </action>
  <verify>
    <automated>node --env-file=scripts/.env -e "console.log('remote table check runs inside verify-scoring in Task 2')" && test -f scripts/generate-vapid-keys.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The four Phase 3 migrations carry final (possibly offset) numbers strictly greater than the 02.1 head, in the fixed relative order
    - `npx supabase db push` completed against the hosted project; remote query lists all six new tables and three new cron jobs
    - Three functions deployed with JWT verification disabled; `curl -X POST` without x-cron-secret returns 401 for each
    - scripts/vapid-keys.json is gitignored; no secret value appears in any committed file or command log
  </acceptance_criteria>
  <done>Hosted schema + functions + secrets are live; the false-positive verification window is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Hosted verification scripts — RLS, scoring pipeline, notification uniqueness</name>
  <files>scripts/verify-scoring.ts, scripts/verify-notifications.ts</files>
  <read_first>
    - scripts/verify-rls.ts (independent publishable-key two-session isolation pattern — never a privileged client for RLS proof)
    - scripts/verify-pipeline.ts (header/import/env conventions; restore-on-abort discipline from 02.1 verifier work)
    - supabase/functions/score-tick/index.ts and notify-tick/index.ts (response JSON shapes to assert)
  </read_first>
  <action>
    Create `scripts/verify-scoring.ts` (run: `node --env-file=scripts/.env scripts/verify-scoring.ts`), numbered probes, exit non-zero on first failure:
    1. RLS: with two independent publishable-key sessions (both invited users), assert user A cannot select/update user B's rows in preferences, user_jobs, push_subscriptions, notifications; assert authenticated update on user_jobs score column is rejected while seen_at update on an own row succeeds (column-limited grant proof); assert anon key sees zero rows everywhere.
    2. Extraction: POST /functions/v1/extract-resume with x-cron-secret; poll resume_extracts (service key) until each existing DOCX resume has status 'ready' with non-empty keywords; assert ai_usage gained purpose 'extract' rows with token counts and no text content column at all.
    3. Scoring: POST /functions/v1/score-tick repeatedly (respecting batch cap) until claimed drains to 0 for the recent window; assert user_jobs has rows for both users; assert at least one 'filtered' row carries a valid filter_reason and at least one 'scored' row has score 0–100, tier matching the D-07 boundaries, 3–5 reasons, gaps object with the four category keys, and a routed_resume_id.
    4. Refilter: as user A, call rpc mark_recent_jobs_for_refilter; assert needs_refilter flips true only on A's recent rows; run score-tick; assert flags clear and scored_at unchanged where routing/filter outcome did not change (Pitfall 6 economy).
    Create `scripts/verify-notifications.ts`:
    1. Setup: as user A (publishable session), temporarily lower notify_threshold to 1 via preferences upsert (record the prior value; restore at the end even on abort — 02.1 restore discipline).
    2. POST /functions/v1/notify-tick; assert notifications rows exist for A's scored jobs with channel 'push' and (given no live subscription in CI context) transition to 'failed' + fallback 'email' rows, OR 'sent' when a subscription exists; assert re-running notify-tick creates zero additional rows for the same (user, job, channel) — NOTF-04 uniqueness probe.
    3. Digest: set digest_time to one minute ago local + last_digest_date null; run notify-tick; assert last_digest_date updated and digest rows inserted at most once across two consecutive runs (Idempotency-Key + bookkeeping proof).
    4. Restore all mutated preference values; print a PASS summary table.
  </action>
  <verify>
    <automated>node --env-file=scripts/.env scripts/verify-scoring.ts && node --env-file=scripts/.env scripts/verify-notifications.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both scripts exit 0 against the hosted project with every numbered probe printed as PASS
    - RLS probes use publishable-key sessions only; service key appears only in pipeline-state assertions
    - Both scripts restore every mutated row (threshold, digest fields) in a finally block
  </acceptance_criteria>
  <done>Hosted proof: isolation, extraction, scoring quality gates, exactly-once notifications, digest idempotency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end human UAT — the promise, live</name>
  <what-built>The complete Phase 3 system on hosted infra: preferences, cheap filters, resume-routed Gemini scoring, match feed, job detail with gap panel, instant push, digest email, and tuning controls. (Deploy the SPA to Cloudflare Pages first if the local changes are not yet live — push to main and confirm the Pages build, including the new VITE_VAPID_PUBLIC_KEY env var, per the 02.1-08 SHA-matched deploy discipline.)</what-built>
  <action>Human-only UAT of the five ROADMAP Phase 3 success criteria on the deployed app: preferences drive filtering, feed and detail render scored matches, push arrives with the tab closed, digest email arrives, quiet hours and threshold tuning are respected. Judgment calls (reason quality, visual fidelity, notification receipt on the user's own device) cannot be automated.</action>
  <verify>All six numbered UAT steps observed passing by the user on the deployed URL</verify>
  <done>User has typed "approved" confirming every UAT step, or gaps have been recorded for closure planning.</done>
  <how-to-verify>
    1. Open the deployed app. Preferences: add target titles (e.g. "data scientist", "quantitative researcher"), a location, one include and one exclude keyword; save — expect `Preferences saved — recent jobs re-filtering.`
    2. Dashboard within ~2–3 minutes: feed shows scored matches — score + tier badges, match reasons, `Best fit:` resume chips, relative posted times, working `Apply ↗` links. Toggle `All jobs`: filtered rows appear greyed with reasons like `excluded keyword: …`.
    3. Open a job: full JD renders (formatted, no raw HTML tags visible), gap panel shows categorized gap chips + covered list, reasons read as sensible plain-language bullets grounded in YOUR resume. New badge cleared back on the feed.
    4. Settings ▸ Notifications: click `Enable push on this device`, accept the permission prompt, see `Push enabled on this device`. Temporarily drag the threshold slider low (e.g. 10) and save; close the app tab (keep the browser running); within ~2 minutes a push should arrive (`Strong match: …` or `{n} strong matches`); clicking it opens the job detail/feed. Restore your threshold to 75 after.
    5. Set digest time to a few minutes from now and save; confirm one digest email arrives at your account address listing Strong+Good matches with working apply links, then set your real digest time.
    6. Confirm quiet hours: set a window covering now, lower threshold, verify NO push arrives during the window (rows queue), then clear the window and verify the queued matches arrive as one combined push.
  </how-to-verify>
  <resume-signal>Type "approved" when all six checks pass, or describe exactly which step failed and what you observed</resume-signal>
</task>

</tasks>

## Artifacts this phase produces (Plan 07)

- Script `scripts/generate-vapid-keys.mjs` (P-256 JWK pair generator; outputs gitignored `scripts/vapid-keys.json` + base64url public key)
- Script `scripts/verify-scoring.ts` (hosted probes: RLS isolation, extraction, scoring quality, refilter economy)
- Script `scripts/verify-notifications.ts` (hosted probes: uniqueness, fallback, digest idempotency)
- `web/.env.example` entry `VITE_VAPID_PUBLIC_KEY`
- Edge secrets: `GEMINI_API_KEY`, `RESEND_API_KEY`, `RESEND_FROM`, `VAPID_KEYS`, `SITE_URL`, `AI_DAILY_SCORE_CAP`
- Hosted state: Phase 3 migrations applied; deployed functions `score-tick`, `notify-tick`, `extract-resume`; cron jobs `score-tick-every-minute`, `notify-tick-every-minute`, `extract-resume-every-minute`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → hosted project | secrets set + schema push with privileged credentials |
| verification scripts → hosted data | probes must not leak or destroy user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-27 | Information disclosure | secrets in logs/commits | critical | mitigate | vapid-keys.json + scripts/.env gitignored (asserted); secrets set via CLI without echoing values; .env.example carries names only |
| T-3-28 | Information disclosure | resume PII on wrong Gemini tier | critical | mitigate | GEMINI_API_KEY is the Plan 02 billing-verified key; verify-scoring asserts ai_usage has no content columns |
| T-3-29 | Tampering | pushing over in-flight 02.1 state | high | mitigate | Task 1 gate: stop unless 02.1 VERIFICATION.md is final; renumber-by-offset rule; jobs-schema reconciliation |
| T-3-30 | Elevation | RLS regressions on new tables | high | mitigate | Publishable-key two-session probes incl. column-grant proof before UAT |
| T-3-31 | Spoofing | unauthenticated cron endpoints | high | mitigate | Post-deploy 401 curl probe on all three functions |
</threat_model>

<verification>
- `node --env-file=scripts/.env scripts/verify-scoring.ts` and `verify-notifications.ts` both exit 0 against hosted
- 401 probes on all three functions without x-cron-secret
- Human UAT checkpoint approved (push received with tab closed; digest email received; quiet hours respected)
</verification>

<success_criteria>
- All 11 phase requirements proven on hosted infrastructure with real data; ROADMAP Phase 3 success criteria 1–5 each observable by the user; no schema/function drift left undeployed
</success_criteria>

<output>
Create `.planning/phases/03-scoring-feed-notifications/03-07-SUMMARY.md` when done
</output>
