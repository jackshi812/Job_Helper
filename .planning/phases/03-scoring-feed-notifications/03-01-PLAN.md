---
phase: 03-scoring-feed-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0017_preferences.sql
  - supabase/functions/_shared/filters.ts
  - web/tests/filters.test.ts
  - web/src/lib/preferences.ts
  - web/src/lib/preferences.test.ts
  - web/src/pages/Preferences.tsx
  - web/src/main.tsx
  - web/src/components/Shell.tsx
autonomous: true
requirements: [PREF-01, SCOR-01, NOTF-03]
must_haves:
  truths:
    - "User can set target titles, locations, include keywords, and exclude keywords on a Preferences page and save them (PREF-01, D-05)"
    - "Cheap filter logic implements D-01 fuzzy title overlap, D-02 exclude-hard/include-soft, D-03 location leniency, and is proven by unit tests before any AI call exists (SCOR-01)"
    - "Preferences schema carries the per-user notify threshold (default 75), quiet hours, digest time, and timezone that NOTF-03 tuning will edit"
  artifacts:
    - path: "supabase/migrations/0017_preferences.sql"
      provides: "per-user preferences table with Phase-1 RLS style"
      contains: "notify_threshold integer not null default 75"
    - path: "supabase/functions/_shared/filters.ts"
      provides: "pure cheapFilter module (no Deno globals, no npm:/jsr: specifiers)"
      exports: ["cheapFilter", "SYNONYMS"]
    - path: "web/src/pages/Preferences.tsx"
      provides: "chip-input preferences form per UI-SPEC"
      min_lines: 80
    - path: "web/tests/filters.test.ts"
      provides: "D-01/D-02/D-03 semantics fixtures"
      min_lines: 40
  key_links:
    - from: "web/src/pages/Preferences.tsx"
      to: "web/src/lib/preferences.ts"
      via: "savePreferences / loadPreferences imports"
      pattern: "from '\\.\\./lib/preferences'"
    - from: "web/src/lib/preferences.ts"
      to: "supabase/migrations/0017_preferences.sql"
      via: "supabase.from('preferences') upsert on user_id primary key"
      pattern: "from\\('preferences'\\)"
    - from: "web/src/main.tsx"
      to: "web/src/pages/Preferences.tsx"
      via: "route 'preferences' inside the RequireAuth Shell group"
      pattern: "path=\"preferences\""
---

<objective>
Preferences vertical slice: the user can define job preferences (target titles, locations, include/exclude keywords) end-to-end — form → typed lib → per-user RLS table — and the pure cheap-filter module that those preferences drive is written and unit-proven. Implements PREF-01, the SCOR-01 filter semantics (D-01, D-02, D-03), per-user isolation (D-05), and lays the NOTF-03 tunable columns (threshold default 75 per D-07, quiet hours, digest time, timezone) in the same preferences row so Plan 06 only adds UI.

Purpose: preferences are the root input of the entire scoring pipeline; filters gate every AI dollar.
Output: migration 0017 (nominal), `_shared/filters.ts` + tests, `lib/preferences.ts`, `pages/Preferences.tsx`, nav + route wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-feed-notifications/03-CONTEXT.md
@.planning/phases/03-scoring-feed-notifications/03-UI-SPEC.md
@.planning/phases/03-scoring-feed-notifications/03-PATTERNS.md
</context>

## Phase Goal

**As a** job seeker, **I want to** be alerted within minutes of a relevant new posting — with cheap filters gating AI cost and AI scoring survivors against my own resume and preferences — **so that** only deduplicated matches above my threshold ever notify me.

## Parallel-Execution Guard (applies to every task)

Phase 02.1 executes in parallel via Codex. Do NOT modify `supabase/functions/poll-tick/index.ts`, `supabase/functions/_shared/lifecycle.ts`, `supabase/functions/_shared/connectors.ts`, `supabase/functions/_shared/adapters/*`, or migrations 0012–0016. Copy patterns from them; never edit them. Never enumerate `jobs.source` values anywhere in Phase 3 code.

**Migration numbering rule:** baseline head at planning time is `0016_workday_experimental.sql`. Before writing any migration, run `ls supabase/migrations | sort | tail -1`. Compute OFFSET = (current numeric head) − 16. This plan's nominal `0017_preferences.sql` becomes `00(17+OFFSET)_preferences.sql` when OFFSET > 0. All Phase 3 plans apply the same rule, preserving relative order: preferences(17) < resume_extracts(18) < user_jobs_scoring(19) < notifications(20).

<tasks>

<task type="auto">
  <name>Task 1: Preferences table migration with Phase-1 RLS style</name>
  <files>supabase/migrations/0017_preferences.sql</files>
  <read_first>
    - supabase/migrations/0002_resumes.sql (the locked per-user RLS pattern: default auth.uid() FK cascade, per-operation `(select auth.uid())` policies, grant block, user_id index)
    - supabase/migrations/0006_jobs_pipeline.sql (house style for header comments explaining security decisions)
    - .planning/phases/03-scoring-feed-notifications/03-PATTERNS.md (section "Migrations: per-user tables")
  </read_first>
  <action>
    Apply the migration numbering rule above, then create the preferences migration. Table `public.preferences` (implements PREF-01 storage per D-05 one-row-per-user, plus NOTF-03 tuning columns per D-07/D-19/D-21):
    - `user_id uuid primary key default auth.uid() references auth.users (id) on delete cascade`
    - `titles text[] not null default '{}'` — target titles
    - `locations text[] not null default '{}'`
    - `include_keywords text[] not null default '{}'` — soft boost only (D-02)
    - `exclude_keywords text[] not null default '{}'` — hard discard (D-02)
    - `notify_threshold integer not null default 75 check (notify_threshold between 0 and 100)` (D-07 default)
    - `quiet_start time` , `quiet_end time` — nullable = no quiet hours (D-19)
    - `digest_time time not null default '08:00'` (D-20)
    - `timezone text not null default 'America/Chicago'` — IANA zone, never UTC offsets (RESEARCH Pitfall 4)
    - `updated_at timestamptz not null default now()`
    - Array-size sanity checks: `check (cardinality(titles) <= 50)`, same for locations/include_keywords/exclude_keywords (input-validation cap, ASVS V5).
    Enable RLS, `grant select, insert, update, delete ... to authenticated`, and write the four per-operation own-row policies exactly in the 0002_resumes.sql shape (`preferences_select_own`, `preferences_insert_own`, `preferences_update_own`, `preferences_delete_own`). No anon access. Add the house-style header comment stating the security decision (per-user isolation via RLS; shared job pool is filtered per user, D-05). Do NOT reference user_jobs here — it does not exist yet (Plan 03).
  </action>
  <verify>
    <automated>test "$(grep -c 'select auth.uid()' supabase/migrations/*_preferences.sql)" -ge 4 && grep -q 'notify_threshold integer not null default 75' supabase/migrations/*_preferences.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists with the numbering-rule-resolved name and creates `public.preferences` with `user_id uuid primary key`
    - File contains `notify_threshold integer not null default 75` and `timezone text not null default 'America/Chicago'`
    - Four RLS policies present, each using `(select auth.uid()) = user_id`; `alter table public.preferences enable row level security` present
    - No mention of `user_jobs` anywhere in the file
  </acceptance_criteria>
  <done>Preferences migration compiles the D-05/D-07/NOTF-03 schema in the locked Phase-1 RLS style.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure cheap-filter module with D-01/D-02/D-03 semantics</name>
  <files>supabase/functions/_shared/filters.ts, web/tests/filters.test.ts</files>
  <read_first>
    - supabase/functions/_shared/dedup.ts (the normalize() philosophy D-01 fuzzy matching starts from)
    - supabase/functions/_shared/lifecycle.ts (pure-planning-function shape: typed rows in, decision out, zero I/O)
    - web/tests/dedup.test.ts (cross-repo Vitest import pattern: `../../supabase/functions/_shared/dedup`)
  </read_first>
  <behavior>
    - excludes hard-discard: prefs.excludeKeywords=['staff'] + title 'Staff Engineer' → { pass: false, reason: 'excluded_keyword', detail: 'staff' } (D-02)
    - include keywords never discard: prefs.includeKeywords=['python'] + JD without python → pass (D-02)
    - synonym title overlap: prefs title 'Quantitative Researcher' matches job title 'Quant Researcher' (D-01 synonyms)
    - clear title non-overlap discards: prefs titles ['data scientist'] vs job 'Registered Nurse' → { pass: false, reason: 'title_non_overlap' }
    - empty prefs.titles → title check passes (nothing to mismatch)
    - location: 'Chicago, IL' passes prefs ['chicago']; 'Remote - US' passes any prefs; blank/null location passes; 'London, UK' vs prefs ['chicago'] with no remote mention → { pass: false, reason: 'wrong_location' } (D-03)
    - order proof: a job hitting an exclude keyword AND wrong location reports 'excluded_keyword' (exclude checked first)
  </behavior>
  <action>
    Create `supabase/functions/_shared/filters.ts` as a PURE module — no I/O, no Deno globals, no npm:/jsr: specifiers, so `web/tests/` can import it cross-repo (dedup.ts precedent). Export:
    - `export type FilterOutcome = { pass: true; matchedIncludeKeywords: string[] } | { pass: false; reason: 'excluded_keyword' | 'wrong_location' | 'title_non_overlap'; detail: string }`
    - `export interface FilterJobInput { title: string; location: string | null; descriptionText: string }`
    - `export interface FilterPreferences { titles: string[]; locations: string[]; includeKeywords: string[]; excludeKeywords: string[] }`
    - `export const SYNONYMS: Record<string, string[]>` seeded with at least: quant↔quantitative, sr↔senior, jr↔junior, ml↔machine learning, swe↔software engineer, ds↔data scientist, eng↔engineer, engr↔engineer, mgr↔manager (D-01 named pairs plus Claude-discretion extensions)
    - `export function cheapFilter(job: FilterJobInput, prefs: FilterPreferences): FilterOutcome`
    Implementation rules (Claude's discretion within D-01/D-02/D-03):
    - Reuse the dedup.ts normalize approach (lowercase, strip parentheticals, non-alphanumeric → space, collapse whitespace).
    - Check order is fixed: exclude keywords first (normalized substring match against normalized title + descriptionText → hard discard, detail = the keyword), then location, then title.
    - Location passes when: prefs.locations is empty, OR job.location is null/blank, OR any normalized preferred location token sequence appears in the normalized job location, OR the normalized title/location/description contains the token 'remote'. Discard only the remaining clear-mismatch case (D-03).
    - Title: tokenize normalized titles, drop stopwords (of, the, and, a, an, for, to, in), expand tokens through SYNONYMS in both directions, pass when any preference title shares at least one significant expanded token with the job title. Empty prefs.titles → pass. Only clear non-overlap discards (D-01).
    - `matchedIncludeKeywords` = includeKeywords found in normalized title+description; flows into the scoring prompt later, never used to discard (D-02).
    Write `web/tests/filters.test.ts` first (RED), covering every behavior case above, then implement to GREEN.
  </action>
  <verify>
    <automated>cd web && npx vitest run tests/filters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vitest run tests/filters.test.ts` passes with at least 8 test cases including all behaviors listed above
    - `filters.ts` contains no `Deno.`, no `npm:`, no `jsr:` occurrences (cross-repo import safety)
    - `cheapFilter` returns 'excluded_keyword' before 'wrong_location' when both would apply
  </acceptance_criteria>
  <done>SCOR-01 filter semantics (D-01/D-02/D-03) are implemented pure and fixture-proven; filtered reasons are bounded to the three D-04 codes.</done>
</task>

<task type="auto">
  <name>Task 3: Preferences lib, page, nav, and route</name>
  <files>web/src/lib/preferences.ts, web/src/lib/preferences.test.ts, web/src/pages/Preferences.tsx, web/src/main.tsx, web/src/components/Shell.tsx</files>
  <read_first>
    - web/src/lib/resumes.ts (small CRUD lib shape: column constant, typed record, throw-on-error functions)
    - web/src/pages/Settings.tsx (form idiom: pending/message/error state trio, try/finally submit handler, input + button classes; exported plain action functions for testability)
    - web/src/lib/pipeline.test.ts (supabase client mock pattern: `vi.mock('./supabase', () => ({ supabase: {} }))`)
    - web/src/main.tsx and web/src/components/Shell.tsx (route group and `navigation` array to extend)
    - .planning/phases/03-scoring-feed-notifications/03-UI-SPEC.md (Preferences component contract, copy strings, chip spec)
  </read_first>
  <action>
    Create `web/src/lib/preferences.ts` following resumes.ts: `PREFERENCE_COLUMNS` constant matching the migration columns exactly (snake_case), `interface PreferencesRecord`, `loadPreferences(): Promise<PreferencesRecord | null>` (`.from('preferences').select(...).maybeSingle()`), `savePreferences(input): Promise<void>` (`.upsert` keyed on user_id, throw on error). Export a pure `parseChips(raw: string): string[]` helper (split on comma, trim, dedupe, drop empties) and unit-test it in `preferences.test.ts` with the supabase mock pattern. Do NOT call any RPC on save yet — the refilter RPC arrives in Plan 03 (which will edit this file).
    Create `web/src/pages/Preferences.tsx` per UI-SPEC: single-column form of `grid gap-1.5` label/input stacks; four chip inputs (target titles, locations, include keywords, exclude keywords) where Enter or comma adds a chip; each chip is the neutral badge style (`inline-flex items-center gap-1.5 rounded-full border border-zinc-200 bg-zinc-50 px-2.5 py-0.5 text-xs font-semibold text-zinc-600 dark:border-zinc-800 dark:bg-zinc-950 dark:text-zinc-400`) with a real `<button>` × remove control carrying `aria-label={'Remove ' + keyword}` and a ≥36px hit area (`min-h-9` wrapper or padding) — UI-SPEC checker FLAG. Helper texts verbatim: exclude group `Any hit discards the posting before scoring.`; include group `Boost scoring — never discard.`; form helper `Matching starts once you save at least one target title.` Save button = primary accent (`bg-zinc-900 text-white ... dark:bg-zinc-100 dark:text-zinc-900`) labeled `Save preferences`, `disabled:cursor-wait disabled:opacity-60` while pending. On success show inline `Preferences saved — recent jobs re-filtering.` as `text-sm text-zinc-600`; on failure `Couldn't save preferences. Your changes are still in the form — retry.` Every interactive element gets the standard focus-visible outline classes. Load existing row on mount via TanStack Query `queryKey: ['preferences']`.
    Wire routing: add `<Route path="preferences" element={<Preferences />} />` inside the RequireAuth/Shell group in `web/src/main.tsx`; add `{ label: 'Preferences', to: '/preferences' }` to the `navigation` array in `Shell.tsx` between Dashboard and Watchlist.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/preferences.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc -b + vite) passes in web/
    - `preferences.ts` exports `loadPreferences`, `savePreferences`, `parseChips`; column constant lists titles, locations, include_keywords, exclude_keywords, notify_threshold, quiet_start, quiet_end, digest_time, timezone
    - Preferences.tsx contains `aria-label={` with a Remove-keyword template on chip buttons and the three helper-text strings verbatim
    - main.tsx contains `path="preferences"`; Shell navigation array contains a Preferences entry
    - No toast/confirm-dialog additions (UI-SPEC: no destructive actions this phase)
  </acceptance_criteria>
  <done>PREF-01 slice is clickable end-to-end in the SPA (form → lib → RLS table) pending the hosted schema push in Plan 07.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 01)

- Table `public.preferences` (columns: user_id, titles, locations, include_keywords, exclude_keywords, notify_threshold, quiet_start, quiet_end, digest_time, timezone, updated_at) + RLS policies `preferences_select_own`, `preferences_insert_own`, `preferences_update_own`, `preferences_delete_own`
- Module `supabase/functions/_shared/filters.ts`: `cheapFilter()`, `SYNONYMS`, types `FilterOutcome`, `FilterJobInput`, `FilterPreferences`
- Module `web/src/lib/preferences.ts`: `loadPreferences()`, `savePreferences()`, `parseChips()`, `PREFERENCE_COLUMNS`, `PreferencesRecord`
- Component `web/src/pages/Preferences.tsx` (`Preferences`), new SPA route `/preferences`, Shell nav entry `Preferences`
- Test files `web/tests/filters.test.ts`, `web/src/lib/preferences.test.ts`
- Migration file `supabase/migrations/0017_preferences.sql` (nominal number; numbering rule applies)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → preferences table | authenticated user writes own preference row |
| user A ↔ user B | shared job pool, strictly separated preference rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Information disclosure | preferences table | high | mitigate | Own-row RLS on all four operations, `(select auth.uid())` pattern from 0002; no anon grants; hosted cross-user denial proven in Plan 07 verification |
| T-3-02 | Tampering | preference arrays as filter input | medium | mitigate | cardinality checks (≤50) in the migration; parseChips trims/dedupes; filters normalize all input before matching |
| T-3-03 | Elevation | forged preference row for another user | high | mitigate | insert/update policies `with check ((select auth.uid()) = user_id)`; user_id defaults to auth.uid() |
</threat_model>

<verification>
- `cd web && npx vitest run tests/filters.test.ts src/lib/preferences.test.ts` green
- `cd web && npm run build` green
- Migration file passes visual structure check against 0002_resumes.sql pattern (4 policies, RLS enabled, grants)
</verification>

<success_criteria>
- D-01/D-02/D-03 filter semantics unit-proven; D-05 per-user preferences schema exists; PREF-01 UI complete per UI-SPEC; NOTF-03 columns present with D-07 default 75
</success_criteria>

<output>
Create `.planning/phases/03-scoring-feed-notifications/03-01-SUMMARY.md` when done
</output>
