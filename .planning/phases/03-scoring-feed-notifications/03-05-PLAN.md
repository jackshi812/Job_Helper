---
phase: 03-scoring-feed-notifications
plan: 05
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - supabase/migrations/0020_notifications.sql
  - supabase/functions/_shared/quiet-hours.ts
  - web/tests/quiet-hours.test.ts
  - supabase/functions/_shared/webpush.ts
  - supabase/functions/notify-tick/index.ts
autonomous: true
requirements: [NOTF-01, NOTF-02, NOTF-03, NOTF-04]
user_setup:
  - service: resend
    why: "Email backup for strong matches + daily digest (D-20); free tier 100/day"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend dashboard -> API Keys (account with a verified sender/domain or onboarding sender). Set as edge secret in Plan 07."
must_haves:
  truths:
    - "Strong matches (score ≥ user threshold, default 75) trigger ONE burst-collapsed push per tick per user; everything else waits for the daily digest (D-18, NOTF-01)"
    - "Strong matches during quiet hours queue and fire as one combined push at quiet-hours end — nothing suppressed, nothing lost (D-19, NOTF-03)"
    - "One daily digest email covers new Strong+Good matches with a Resend Idempotency-Key; individual email fallback fires only when a Strong push cannot be delivered (D-20, NOTF-02)"
    - "The notifications UNIQUE (user_id, job_id, channel) constraint makes double-notify structurally impossible (NOTF-04)"
  artifacts:
    - path: "supabase/migrations/0020_notifications.sql"
      provides: "push_subscriptions + notifications tables, notify-tick cron, delete_my_data replacement"
      contains: "unique (user_id, job_id, channel)"
    - path: "supabase/functions/_shared/quiet-hours.ts"
      provides: "pure DST-safe user-local quiet-window math"
      exports: ["quietHoursState", "userLocalDate", "userLocalMinutes"]
    - path: "supabase/functions/_shared/webpush.ts"
      provides: "@negrel/webpush wrapper returning sent|gone|failed"
      exports: ["sendPush"]
    - path: "supabase/functions/notify-tick/index.ts"
      provides: "enqueue → quiet-check → collapse → push → fallback → digest dispatcher"
      contains: "Idempotency-Key"
  key_links:
    - from: "supabase/functions/notify-tick/index.ts"
      to: "user_jobs table (Plan 03)"
      via: "scan for scored rows ≥ user threshold lacking notification rows"
      pattern: "from\\('user_jobs'\\)"
    - from: "supabase/functions/notify-tick/index.ts"
      to: "supabase/functions/_shared/webpush.ts"
      via: "sendPush per subscription; gone → delete row + email fallback"
      pattern: "sendPush"
    - from: "supabase/functions/notify-tick/index.ts"
      to: "supabase/functions/_shared/quiet-hours.ts"
      via: "quietHoursState gate before any push send"
      pattern: "quietHoursState"
---

<objective>
Notification backend vertical slice: a per-minute `notify-tick` dispatcher turns scored rows into exactly-once notifications — burst-collapsed instant push for Strong matches (D-18), quiet-hours queueing with combined release (D-19), one daily digest email plus push-failure fallback email under the Resend 100/day cap (D-20), all enforced structurally by UNIQUE (user_id, job_id, channel) (NOTF-04). Ships the pure DST-safe quiet-hours module and the Deno web-push wrapper.

Purpose: this closes the project's core promise — discovery-to-notification in minutes — for the matches that matter.
Output: migration 0020 (push_subscriptions, notifications, cron, delete_my_data update), `_shared/quiet-hours.ts` + DST tests, `_shared/webpush.ts`, `notify-tick` edge function.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-feed-notifications/03-CONTEXT.md
@.planning/phases/03-scoring-feed-notifications/03-RESEARCH.md
@.planning/phases/03-scoring-feed-notifications/03-UI-SPEC.md
@.planning/phases/03-scoring-feed-notifications/03-PATTERNS.md
</context>

## Phase Goal

**As a** job seeker, **I want to** be alerted within minutes of a relevant new posting — with cheap filters gating AI cost and AI scoring survivors against my own resume and preferences — **so that** only deduplicated matches above my threshold ever notify me.

## Parallel-Execution Guard

Do NOT modify 02.1-owned files. **Migration numbering rule:** baseline head 0016; OFFSET = numeric head − 16; nominal `0020_notifications.sql` becomes `00(20+OFFSET)_...`; must sort after the Plan 01/02/03 migrations.

<tasks>

<task type="auto">
  <name>Task 1: push_subscriptions + notifications schema, notify-tick cron, delete_my_data replacement</name>
  <files>supabase/migrations/0020_notifications.sql</files>
  <read_first>
    - supabase/migrations/0002_resumes.sql (per-user CRUD RLS style for push_subscriptions)
    - supabase/migrations/0006_jobs_pipeline.sql (service-written table grants; cron block to copy)
    - supabase/migrations/0004_delete_my_data.sql (the function to REPLACE — its comment mandates appending new user-owned tables)
    - The Plan 01 preferences migration (this migration alters that table)
  </read_first>
  <action>
    Apply the numbering rule, then one migration:
    1. `public.push_subscriptions` (per-user CRUD, 0002 style): `id uuid primary key default gen_random_uuid()`, `user_id uuid not null default auth.uid() references auth.users (id) on delete cascade`, `endpoint text not null unique`, `subscription jsonb not null` (full PushSubscription JSON incl. keys.p256dh/keys.auth), `user_agent text`, `created_at timestamptz not null default now()`, `last_success_at timestamptz`. RLS + four own-row policies (`push_subscriptions_*_own`) + grants to authenticated; service role reads for sending. Header comment: endpoint secrets are per-user credentials — RLS blocks cross-user reads (push payloads never carry secrets regardless).
    2. `public.notifications` (service-written, user-readable, 0006 style): `id uuid primary key default gen_random_uuid()`, `user_id uuid not null references auth.users (id) on delete cascade`, `job_id uuid not null references public.jobs (id) on delete cascade`, `channel text not null check (channel in ('push','email','digest'))`, `status text not null default 'queued' check (status in ('queued','sent','failed'))`, `created_at timestamptz not null default now()`, `sent_at timestamptz`, `error_code text`, and the NOTF-04 enforcement: `unique (user_id, job_id, channel)`. RLS enabled; revoke all from anon/authenticated; grant select to authenticated with own-row policy `notifications_select_own`. Header comment: the unique constraint IS the double-notify prevention (NOTF-04) — it survives crashes and retries where in-memory tracking would not.
    3. `alter table public.preferences add column last_digest_date date;` — digest-sent-today bookkeeping (service role writes it; existing user policies unaffected).
    4. Cron: copy the 0006 block, job name `notify-tick-every-minute`, path `/functions/v1/notify-tick`, timeout 120000.
    5. `create or replace function public.delete_my_data()` — reproduce the 0004 body (resumes delete + its standing comment) and append deletes for `public.preferences`, `public.user_jobs`, `public.push_subscriptions`, `public.notifications`, each `where user_id = (select auth.uid())`. Keep `security invoker`, `set search_path = ''`, and the revoke/grant tail identical to 0004. resume_extracts dies via FK cascade from resumes — note that in a comment.
  </action>
  <verify>
    <automated>grep -q "unique (user_id, job_id, channel)" supabase/migrations/*_notifications.sql && grep -q "delete_my_data" supabase/migrations/*_notifications.sql && grep -q "notify-tick" supabase/migrations/*_notifications.sql</automated>
  </verify>
  <acceptance_criteria>
    - notifications table has `unique (user_id, job_id, channel)` and the three-channel check
    - push_subscriptions has `endpoint text not null unique` and four own-row policies
    - delete_my_data() now deletes from preferences, user_jobs, push_subscriptions, notifications (plus resumes from 0004)
    - Cron block posts to `/functions/v1/notify-tick` using vault secrets project_url/cron_secret
  </acceptance_criteria>
  <done>Notification persistence + AUTH-04 deletion coverage complete; NOTF-04 is a database constraint, not code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure quiet-hours module (DST-safe) + webpush wrapper</name>
  <files>supabase/functions/_shared/quiet-hours.ts, web/tests/quiet-hours.test.ts, supabase/functions/_shared/webpush.ts</files>
  <read_first>
    - supabase/functions/_shared/lifecycle.ts (pure-function shape: data in, decision out, now as parameter, injectable clock)
    - web/tests/lifecycle.test.ts (fixture style for time-based pure logic)
    - .planning/phases/03-scoring-feed-notifications/03-RESEARCH.md (Pattern 3 — VERIFIED @negrel/webpush 0.5.0 API: importVapidKeys, ApplicationServer.new, subscribe().pushTextMessage, PushMessageError.isGone() plus response.status 404; Pitfall 4 DST)
  </read_first>
  <behavior>
    - quietHoursState({quietStart:'22:00', quietEnd:'07:00', timezone:'America/Chicago'}, nowIso at 23:30 Chicago) → { inQuietWindow: true } (midnight-wrapping window)
    - same prefs at 08:00 Chicago → { inQuietWindow: false }
    - null quietStart or quietEnd → always { inQuietWindow: false } (no quiet hours configured)
    - DST spring-forward: 2026-03-08 window behavior stays anchored to local wall-clock (a 06:59 local instant is still quiet for a 22:00–07:00 window); DST fall-back 2026-11-01 likewise — two explicit fixtures with exact UTC instants
    - userLocalDate('America/Chicago', nowIso) returns the YYYY-MM-DD local date used for digest dedup
    - digestDue: local time 08:05 with digest_time '08:00' and last_digest_date ≠ today → true; last_digest_date = today → false
  </behavior>
  <action>
    Create `supabase/functions/_shared/quiet-hours.ts` — PURE, Vitest-importable (no Deno globals):
    - `export function userLocalMinutes(timezone: string, nowIso: string): number` — minutes-since-midnight in the user's zone via `Intl.DateTimeFormat('en-US', { timeZone: timezone, hour12: false, hour: '2-digit', minute: '2-digit' })` (never UTC-offset arithmetic — Pitfall 4).
    - `export function userLocalDate(timezone: string, nowIso: string): string` — YYYY-MM-DD via Intl with the same zone.
    - `export function quietHoursState(prefs: { quietStart: string | null; quietEnd: string | null; timezone: string }, nowIso: string): { inQuietWindow: boolean }` — parse HH:MM strings to minutes; non-wrapping window start<end is inclusive-start/exclusive-end; midnight-wrapping window (start>end) is quiet when now >= start OR now < end.
    - `export function digestDue(prefs: { digestTime: string; timezone: string; lastDigestDate: string | null }, nowIso: string): boolean` — true when local minutes >= digest_time minutes AND lastDigestDate !== today's local date.
    Write `web/tests/quiet-hours.test.ts` first (RED) with the behavior fixtures above including both 2026 DST boundary dates as exact UTC ISO instants, then implement to GREEN.
    Create `supabase/functions/_shared/webpush.ts` (Deno-only — NEVER imported by web code or the pure modules):
    - `import * as webpush from "jsr:@negrel/webpush@0.5.0"`
    - Module-level lazy singleton: `importVapidKeys(JSON.parse(Deno.env.get('VAPID_KEYS')!), { extractable: false })` then `ApplicationServer.new({ contactInformation: 'mailto:jk8839888@gmail.com', vapidKeys })`.
    - `export async function sendPush(subscriptionJson: unknown, payload: { title: string; body: string; url: string; tag?: string }): Promise<'sent' | 'gone' | 'failed'>` — `appServer.subscribe(subscriptionJson).pushTextMessage(JSON.stringify(payload), {})`; catch PushMessageError: return 'gone' when `err.isGone()` OR `err.response.status === 404` (RESEARCH: some push services use 404 for dead endpoints), else 'failed'. Payload carries title/body/url/tag only — never resume content, JD text, or secrets (unaudited-crypto mitigation).
  </action>
  <verify>
    <automated>cd web && npx vitest run tests/quiet-hours.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - quiet-hours tests green including both DST fixtures with exact UTC instants
    - quiet-hours.ts contains `Intl.DateTimeFormat` and no `Deno.` references; webpush.ts contains `jsr:@negrel/webpush@0.5.0` and both the isGone() and 404 checks
    - sendPush payload type has exactly title/body/url/tag fields
  </acceptance_criteria>
  <done>D-19 window math is DST-proof and NOTF-01 sending has a verified-API wrapper with dead-subscription detection.</done>
</task>

<task type="auto">
  <name>Task 3: notify-tick dispatcher — enqueue, collapse, push, fallback, digest</name>
  <files>supabase/functions/notify-tick/index.ts</files>
  <read_first>
    - supabase/functions/poll-tick/index.ts (skeleton: cron gate, admin client, counters, bounded errors — do NOT edit)
    - supabase/functions/_shared/quiet-hours.ts and _shared/webpush.ts (this plan's Task 2)
    - The Plan 03 migration (user_jobs columns) and this plan's Task 1 migration (notifications/push_subscriptions columns)
    - .planning/phases/03-scoring-feed-notifications/03-RESEARCH.md (Resend digest code example with Idempotency-Key; architecture diagram notify-tick branch)
    - .planning/phases/03-scoring-feed-notifications/03-UI-SPEC.md (push/digest copy contract: titles, bodies, click URLs, digest table columns and footer)
  </read_first>
  <action>
    Create `supabase/functions/notify-tick/index.ts` on the poll-tick skeleton (POST-only, x-cron-secret gate, service-role client). Flow per tick:
    1. ENQUEUE (NOTF-04 gate): select scored, non-dismissed user_jobs rows joined to each user's preferences where `score >= coalesce(preferences.notify_threshold, 75)` and `scored_at > now() - interval '24 hours'` and no notifications row exists for (user_id, job_id, 'push'); insert notifications rows channel 'push' status 'queued' with `on conflict (user_id, job_id, channel) do nothing`. Scoring already runs only on post-dedup jobs rows — notifications therefore fire only for deduplicated, scored jobs above threshold (NOTF-04 complete).
    2. Load users having queued push rows; per user (Promise.allSettled):
       a. Quiet gate (D-19): `quietHoursState` from the user's preferences (timezone default 'America/Chicago'); in quiet window → leave rows queued (they release as one combined push at window end — this same loop next non-quiet tick).
       b. Burst collapse (D-18): all queued push rows for the user become ONE payload — single row: title `Strong match: {job title}`, body `{company} · score {score}`, url `/jobs/{user_jobs.id}`; multiple: title `{n} strong matches`, body `{top job title} and {n-1} more`, url `/` (UI-SPEC copy contract; url paths are SPA-relative, prefixed with the site origin from env `SITE_URL`).
       c. Send via `sendPush` to EVERY push_subscriptions row for the user. Any 'gone' → delete that subscription row. At least one 'sent' → mark the queued rows status 'sent', sent_at now, update subscription last_success_at.
       d. Email fallback (D-20): user has zero subscriptions OR every send returned gone/failed → mark push rows 'failed' with error_code 'push_undeliverable', insert notifications rows channel 'email' (on conflict do nothing), and send ONE collapsed fallback email via Resend REST (`POST https://api.resend.com/emails`, Bearer RESEND_API_KEY, from env `RESEND_FROM`, to the user's auth email fetched via `admin.auth.admin.getUserById`, subject = the push title, minimal HTML list of the matches with apply links); mark email rows sent/failed accordingly.
    3. DIGEST (D-20): per user where `digestDue({ digestTime, timezone, lastDigestDate }, nowIso)`: collect Strong+Good user_jobs (score >= 50) scored in the last 24h; if any, send ONE Resend email with header `Idempotency-Key: digest-{userId}-{localDate}` (24h server-side dedup), subject `{n} new job matches`, inline-styled dense HTML table (title · company · score/tier · apply link) on #f8fafc with white table, footer `Sent by Job Copilot · tune alerts in Settings` (UI-SPEC digest contract, light theme only); insert notifications rows channel 'digest' per included job (on conflict do nothing); update preferences.last_digest_date to the local date regardless of empty/sent so the check runs once daily.
    4. Bounded error codes only (resend_http_429, push_send_failed, digest_send_failed); never log subscription endpoints, email bodies, or JD/resume content. Response: `{ enqueued, pushed_users, fallback_emails, digests_sent, subscriptions_pruned, failed }`.
    Resend cap math stays well under 100/day by design: 1 digest/user/day + rare fallbacks (D-20 ~2–5/day) — add a per-tick email send cap of 20 as a hard safety valve with error_code 'email_cap_reached'.
  </action>
  <verify>
    <automated>cd web && npm run build && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - notify-tick/index.ts contains the x-cron-secret gate, `on conflict (user_id, job_id, channel) do nothing` semantics (via upsert options or raw insert), `Idempotency-Key`, and the per-tick email cap
    - Push payload titles/bodies match the UI-SPEC copy contract strings exactly (template literals for Strong match / {n} strong matches)
    - Quiet-window branch performs no sends and no status changes (rows stay queued)
    - Existing suite still green
  </acceptance_criteria>
  <done>NOTF-01/02/03-evaluation/04 dispatcher complete: exactly-once, burst-collapsed, quiet-hours-respecting, cap-safe.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 05)

- Table `public.push_subscriptions` (id, user_id, endpoint unique, subscription, user_agent, created_at, last_success_at) + policies `push_subscriptions_*_own`
- Table `public.notifications` (id, user_id, job_id, channel, status, created_at, sent_at, error_code) + `unique (user_id, job_id, channel)` + policy `notifications_select_own`
- Column `public.preferences.last_digest_date`
- Replaced function `public.delete_my_data()` (now covers preferences, user_jobs, push_subscriptions, notifications)
- pg_cron job `notify-tick-every-minute`
- Module `supabase/functions/_shared/quiet-hours.ts`: `quietHoursState()`, `userLocalMinutes()`, `userLocalDate()`, `digestDue()`
- Module `supabase/functions/_shared/webpush.ts`: `sendPush()`
- Edge function `notify-tick` (`supabase/functions/notify-tick/index.ts`) with env knobs `SITE_URL`, `RESEND_FROM`, `VAPID_KEYS`, `RESEND_API_KEY`
- Test file `web/tests/quiet-hours.test.ts`
- Migration file `supabase/migrations/0020_notifications.sql` (nominal number; numbering rule applies)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → notify-tick | forged invocation |
| edge fn → push services / Resend | outbound sends with per-user credentials |
| user A ↔ user B | subscription rows and notification logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-18 | Spoofing | notify-tick invocation | high | mitigate | x-cron-secret gate (0006 pattern), verify_jwt off |
| T-3-19 | Information disclosure | push_subscriptions cross-user read | high | mitigate | Own-row RLS all operations; service-role-only reads for sending |
| T-3-20 | DoS (user attention) | notification spam / replay | high | mitigate | UNIQUE (user_id, job_id, channel), burst collapse (D-18), Resend Idempotency-Key, per-tick email cap 20 |
| T-3-21 | Information disclosure | sensitive data in push payloads | medium | mitigate | Payload restricted to title/company/score/url; unaudited-crypto lib risk accepted for non-sensitive payloads per research audit |
| T-3-22 | DoS (quota) | Resend 100/day exhaustion | medium | mitigate | Digest-first design (D-20), fallback-only individual emails, hard cap valve |
| T-3-23 | Repudiation | did-we-notify disputes | low | accept | notifications audit rows (sent_at, error_code) suffice for 2 users |
</threat_model>

<verification>
- `cd web && npx vitest run tests/quiet-hours.test.ts` green (incl. DST fixtures)
- `cd web && npm run build && npx vitest run` green
- Hosted dispatch proof (real push + digest) lands in Plan 07 after deploy + secrets
</verification>

<success_criteria>
- D-18/D-19/D-20 dispatch semantics implemented; NOTF-04 enforced by constraint; Resend cap structurally protected; 02.1 files untouched
</success_criteria>

<output>
Create `.planning/phases/03-scoring-feed-notifications/03-05-SUMMARY.md` when done
</output>
