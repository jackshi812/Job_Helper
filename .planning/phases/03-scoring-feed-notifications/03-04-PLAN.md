---
phase: 03-scoring-feed-notifications
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/src/lib/feed.ts
  - web/src/lib/feed.test.ts
  - web/src/pages/Dashboard.tsx
  - web/src/pages/JobDetail.tsx
  - web/src/main.tsx
autonomous: false
requirements: [SCOR-04, SCOR-05]
must_haves:
  truths:
    - "Dashboard shows a dense feed of matches: New badge, title link, company, score+tier, first reason, best-fit resume chip, relative posted-time, Apply ↗, Dismiss — newest-first with score-sortable header (SCOR-04, D-14)"
    - "Default view shows Strong+Good only; Weak and filtered rows (with reasons) live behind the All jobs toggle; dismissed rows behind Show dismissed (D-15, D-16)"
    - "Job detail renders the full JD snapshot ONLY through DOMPurify.sanitize plus a categorized keyword-gap panel and covered list computed against the routed resume (SCOR-05, D-17)"
    - "Seen/dismiss state is per-user and never affects the other user (D-16)"
  artifacts:
    - path: "web/src/lib/feed.ts"
      provides: "user_jobs ⋈ jobs queries, tier presentation, seen/dismiss mutations"
      exports: ["listFeed", "getFeedJob", "markSeen", "dismissJob", "tierPresentation", "filteredReasonLabel"]
    - path: "web/src/pages/Dashboard.tsx"
      provides: "match feed table replacing the stub"
      min_lines: 120
    - path: "web/src/pages/JobDetail.tsx"
      provides: "JD snapshot + gap panel view at /jobs/:id"
      contains: "DOMPurify.sanitize"
  key_links:
    - from: "web/src/pages/Dashboard.tsx"
      to: "web/src/lib/feed.ts"
      via: "useQuery(['feed', filters]) + dismiss/seen mutations"
      pattern: "listFeed"
    - from: "web/src/lib/feed.ts"
      to: "user_jobs table (Plan 03)"
      via: "supabase.from('user_jobs').select with embedded jobs(...)"
      pattern: "from\\('user_jobs'\\)"
    - from: "web/src/pages/JobDetail.tsx"
      to: "dompurify"
      via: "sanitize before dangerouslySetInnerHTML"
      pattern: "DOMPurify\\.sanitize"
---

<objective>
Feed and job-detail vertical slice: the user sees scored matches on the Dashboard (SCOR-04) and opens a job detail with the sanitized full JD snapshot and the advisory categorized keyword-gap panel (SCOR-05). Implements D-14 (newest-first, score-sortable), D-15 (Strong+Good default, all-jobs toggle), D-16 (New badge, per-user dismiss), D-17 (gap panel grouped skills/tools/certs/domain plus covered), and D-10's stale-score marker — all inside the established zinc dense-table system per the approved UI-SPEC.

Purpose: this is where scoring becomes visible value; the feed is the Dashboard focal point (UI-SPEC checker flag).
Output: dompurify dependency (pinned 3.4.12), `lib/feed.ts` + tests, rebuilt `Dashboard.tsx`, new `JobDetail.tsx` + `/jobs/:id` route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-feed-notifications/03-CONTEXT.md
@.planning/phases/03-scoring-feed-notifications/03-UI-SPEC.md
@.planning/phases/03-scoring-feed-notifications/03-PATTERNS.md
</context>

## Phase Goal

**As a** job seeker, **I want to** be alerted within minutes of a relevant new posting — with cheap filters gating AI cost and AI scoring survivors against my own resume and preferences — **so that** only deduplicated matches above my threshold ever notify me.

## Parallel-Execution Guard

Do NOT modify 02.1-owned files (poll-tick, lifecycle, connectors, adapters, migrations 0012–0016). Feed queries operate on job rows source-agnostically — never enumerate `jobs.source` values.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — dompurify@3.4.12</name>
  <what-built>Nothing yet. The research package audit flagged `dompurify` [SUS] on the too-new heuristic only (3.4.12 published 2026-07-11, <14 days before audit). It is the canonical cure53 sanitizer (48.2M weekly downloads, no postinstall), but the [SUS] verdict requires a blocking human confirmation before install.</what-built>
  <action>Human-only legitimacy confirmation before the package-manager install in Task 2: review the npm page for dompurify (publisher cure53, download volume, version 3.4.12 present) and explicitly approve the exact pin. This checkpoint is never auto-approvable ([SUS] disposition rule).</action>
  <verify>User has replied "approved" (or supplied an alternate pin) after checking npmjs.com/package/dompurify</verify>
  <done>Human approval recorded for installing dompurify at an exact pinned version.</done>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/dompurify — confirm the publisher is cure53, weekly downloads are in the tens of millions, and version 3.4.12 exists.
    2. Optionally cross-check https://github.com/cure53/DOMPurify releases for 3.4.12.
    3. Approve installing the exact pin `dompurify@3.4.12` into web/.
  </how-to-verify>
  <resume-signal>Type "approved" to install dompurify@3.4.12, or name a different version to pin</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feed data lib with tier presentation and seen/dismiss mutations</name>
  <files>web/package.json, web/package-lock.json, web/src/lib/feed.ts, web/src/lib/feed.test.ts</files>
  <read_first>
    - web/src/lib/watchlist.ts (the architecture to copy: exported column-list constant, typed snake_case records, pure presentation mappers, throw-on-error query/mutation functions, safeCareersUrl https-only guard)
    - supabase/migrations/0006_jobs_pipeline.sql plus 02.1 migrations 0012+ (exact jobs column names for the embedded select — verify at execution time)
    - The Plan 03 migration file (user_jobs columns: score, tier, reasons, gaps, covered, filter_reason, filter_detail, routed_resume_id, runner_up_resume_id, scored_at, seen_at, dismissed_at, status)
    - web/src/lib/pipeline.test.ts (supabase mock pattern for lib tests)
    - .planning/phases/03-scoring-feed-notifications/03-UI-SPEC.md (tier labels, filtered reason label formats)
  </read_first>
  <behavior>
    - tierPresentation(82) → { label: 'Strong', badge: 'emerald' variant }; tierPresentation(60) → Good/neutral; tierPresentation(30) → { label: 'Weak', badge: null } (plain text-zinc-500, no fill — UI-SPEC)
    - filteredReasonLabel({reason:'excluded_keyword', detail:'staff'}) → 'excluded keyword: staff'; wrong_location → 'location mismatch'; title_non_overlap → 'title mismatch' (UI-SPEC copy, D-04)
    - defaultVisible(row): scored rows with score ≥ 50 and not dismissed → true; Weak, filtered, and dismissed rows → false (D-15/D-16)
  </behavior>
  <action>
    First run `npm install dompurify@3.4.12 --save-exact` in web/ (approved in Task 1; types ship with v3, no @types package).
    Create `web/src/lib/feed.ts` on the watchlist.ts architecture:
    - `FEED_COLUMNS` constant selecting user_jobs columns plus embedded `jobs(...)` columns (id, title, location, absolute apply URL column, posted/first_seen timestamps, status, description_html, description_text — use the EXACT names read from migrations; alias nothing).
    - `export interface FeedRow` typed to match, including nested job record.
    - Pure mappers (unit-tested, no supabase import needed): `tierPresentation(score: number | null)`, `filteredReasonLabel(row)`, `defaultVisible(row)`, `relativePostedTime(row)` preferring the job's posted timestamp and falling back to first_seen (RESEARCH: `posted_at` falling back to `first_seen_at`), and `safeApplyUrl(url)` copying the https-only guard from watchlist.ts.
    - Queries: `listFeed(): Promise<FeedRow[]>` — `.from('user_jobs').select(FEED_COLUMNS)` with embedded jobs, ordered newest-first by the job timestamp, RLS scoping to the current user automatically (D-16 per-user state); `getFeedJob(userJobId: string): Promise<FeedRow>` single row for the detail view.
    - Mutations: `markSeen(userJobId)` sets seen_at = now ISO when null; `dismissJob(userJobId)` sets dismissed_at; `undismissJob(userJobId)` clears it. All `.update` on user_jobs (only seen_at/dismissed_at are grant-writable — Plan 03 column grants), throw on error.
    Write `web/src/lib/feed.test.ts` first (RED) for the pure mappers with the behavior fixtures above (mock supabase module per pipeline.test.ts), then implement to GREEN.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/feed.test.ts && grep -q '"dompurify": "3.4.12"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json pins `"dompurify": "3.4.12"` exactly (no caret)
    - feed.test.ts green covering tierPresentation boundaries (75/74/50/49), all three filteredReasonLabel codes, and defaultVisible logic
    - feed.ts embedded select uses only column names that exist in the migrations (no invented columns)
  </acceptance_criteria>
  <done>Typed feed data layer with D-07/D-15/D-16 presentation logic proven by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard feed table + JobDetail with sanitized JD and gap panel</name>
  <files>web/src/pages/Dashboard.tsx, web/src/pages/JobDetail.tsx, web/src/main.tsx</files>
  <read_first>
    - web/src/pages/Watchlist.tsx (dense table wholesale: scroll-region wrapper, thead classes, tri-state loading/error/empty rendering, StatusBadge component, relativeTime, boundedErrorMessage, useQuery/useMutation wiring with refetchInterval 60_000)
    - web/src/pages/Settings.tsx (card-section layout for JobDetail sections)
    - web/src/main.tsx (route group — note Plan 01 already added the preferences route)
    - .planning/phases/03-scoring-feed-notifications/03-UI-SPEC.md (Component Inventory: exact feed columns, filter bar, badges, copy strings; Interaction contract: New-badge lifecycle, optimistic dismiss)
  </read_first>
  <action>
    Rebuild `web/src/pages/Dashboard.tsx` (replaces the 8-line stub) per UI-SPEC:
    - `useQuery({ queryKey: ['feed', filters], queryFn: listFeed, refetchInterval: 60_000 })`; client-side filter/sort state: view mode default (Strong+Good via defaultVisible) vs `All jobs` toggle (reveals Weak + filtered rows, D-15) and `Show dismissed` (D-16); sort default newest-first with a score-sort `<button>` inside the score `<th>` carrying `aria-sort` (D-14).
    - Filter bar above the table: `flex flex-wrap gap-3` row of bordered secondary-style toggle buttons; active toggle uses the accent-filled primary style (UI-SPEC).
    - Table columns in order: New badge (blue informational badge, only when seen_at is null) · job title as underlined link to `/jobs/{userJobId}` · company · score numeral (`text-sm font-semibold`) + tier badge (emerald Strong / neutral Good / plain zinc-500 Weak text) · first match reason (`line-clamp-2 text-xs`, plain text — never HTML) · best-fit resume chip `Best fit: {resume name}` with near-tie secondary `also fits {name}` as text-xs (D-06) · relative posted-time in a `<time dateTime title>` element · `Apply ↗` external link (safeApplyUrl, `target="_blank" rel="noreferrer"`) · `Dismiss` button with `aria-label={'Dismiss ' + jobTitle}` (UI-SPEC checker FLAG).
    - Filtered rows (visible only under All jobs): entire row `text-zinc-500`, filter reason as `text-xs` secondary text via filteredReasonLabel; no color coding.
    - Dismiss mutation: optimistic removal from default view (onMutate cache update or immediate invalidate), no confirmation dialog; dismissed rows reappear under Show dismissed. Empty state: heading `No matches yet`, body copy verbatim from UI-SPEC with underlined link to /preferences when no preferences row exists (reuse loadPreferences). Error state: `Couldn't load your matches. Check your connection and retry.` + Retry button calling refetch. Loading: single `p-4 text-sm text-zinc-600` "Loading…" row. Resume names for chips: map routed_resume_id → filename via a `['resumes']` query (listResumes from lib/resumes.ts).
    Create `web/src/pages/JobDetail.tsx` at route `/jobs/:id` (id = user_jobs id):
    - Header: job title (text-xl Display), meta line company · location · posted-time (`text-sm text-zinc-600`), score + tier badge, `Apply ↗` link. Stale-score marker: `scored {relative time}` as `text-xs text-zinc-500` (D-10).
    - On mount (successful load): call `markSeen` then invalidate `['feed']` — New badge clears when detail opens or row dismissed, not on feed render (UI-SPEC lifecycle).
    - Section card 1 — keyword gap panel (D-17): heading noting advisory nature and the routed resume (`Gaps vs your {resume name} resume — advisory only`); groups skills / tools / certs / domain as neutral chips for gaps, then a covered list as emerald chips; empty-panel copy `No gaps found against your {resume name} resume.`
    - Section card 2 — match reasons: 3–5 plain-text bullets (D-09); never rendered as HTML.
    - Section card 3 — full JD snapshot: `import DOMPurify from 'dompurify'`; render `dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(job.description_html, { FORBID_TAGS: ['style', 'form'] }) }}` inside a bordered card; when description_html is null fall back to `<pre className="whitespace-pre-wrap text-sm">{description_text}</pre>`. The raw description_html string must never reach the DOM unsanitized.
    - Route: add `<Route path="jobs/:id" element={<JobDetail />} />` inside the RequireAuth/Shell group in main.tsx.
  </action>
  <verify>
    <automated>cd web && npm run build && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` green; full vitest suite green
    - Dashboard.tsx contains the All jobs and Show dismissed toggles, `aria-sort` on the score header button, and `aria-label={'Dismiss '` on dismiss buttons
    - JobDetail.tsx contains exactly one `dangerouslySetInnerHTML` and it wraps `DOMPurify.sanitize`; a `<pre>` fallback exists for null description_html
    - main.tsx contains `path="jobs/:id"`
    - No star/shortlist control, no 3-resume comparison view, no Pro-rescore UI anywhere (deferred items)
  </acceptance_criteria>
  <done>SCOR-04 feed and SCOR-05 detail are fully navigable per UI-SPEC; XSS boundary enforced at render.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 04)

- Dependency `dompurify@3.4.12` (exact pin) in web/package.json
- Module `web/src/lib/feed.ts`: `listFeed()`, `getFeedJob()`, `markSeen()`, `dismissJob()`, `undismissJob()`, `tierPresentation()`, `filteredReasonLabel()`, `defaultVisible()`, `relativePostedTime()`, `safeApplyUrl()`, `FEED_COLUMNS`, `FeedRow`
- Component `web/src/pages/Dashboard.tsx` (feed table — replaces stub)
- Component `web/src/pages/JobDetail.tsx` (`JobDetail`), new SPA route `/jobs/:id`
- Test file `web/src/lib/feed.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ATS-supplied description_html → DOM | untrusted third-party HTML rendered in the SPA |
| npm registry → build | new frontend dependency |
| user A ↔ user B | per-user seen/dismiss state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-15 | Tampering/Elevation | XSS via description_html | critical | mitigate | DOMPurify.sanitize with FORBID_TAGS ['style','form'] at every render; `<pre>` text fallback; reasons/gap chips rendered as plain text only |
| T-3-16 | Tampering | apply-link javascript:/data: URL | medium | mitigate | safeApplyUrl https-only guard copied from watchlist.ts; rel="noreferrer" on external links |
| T-3-SC | Tampering | npm install dompurify | high | mitigate | Blocking human legitimacy checkpoint (Task 1) + exact-version pin 3.4.12; no other packages installed this phase |
| T-3-17 | Information disclosure | cross-user feed reads | high | mitigate | RLS own-row select on user_jobs (Plan 03); no service keys in SPA |
</threat_model>

<verification>
- `cd web && npx vitest run` green including feed.test.ts
- `cd web && npm run build` green
- Manual UAT of visuals deferred to Plan 07 human checkpoint (hosted, with real scored data)
</verification>

<success_criteria>
- Feed + detail render scored data per D-14..D-17 and UI-SPEC contract; sanitization is the only path for JD HTML; deferred features absent
</success_criteria>

<output>
Create `.planning/phases/03-scoring-feed-notifications/03-04-SUMMARY.md` when done
</output>
