---
phase: 04-application-tracker
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - .planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md
autonomous: true
requirements: [TRAK-01, TRAK-02, TRAK-03, TRAK-04]
user_setup:
  - service: supabase
    why: "Approved non-interactive migration 0053 push, Management API catalog parity, ephemeral service-role fixture control, and two ordinary-user hosted proof"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Existing account access token supplied only to the execution environment; never persist or print it"
      - name: SUPABASE_URL
        source: "Existing linked project URL in the gitignored scripts/.env"
      - name: SUPABASE_PUBLISHABLE_KEY
        source: "Existing publishable key in the gitignored scripts/.env; used for the two ordinary-user sessions"
must_haves:
  truths:
    - "The exact approved migration 0053 is pushed only after checksum/target/sole-pending revalidation and before any hosted behavior fixture exists."
    - "Executable catalog inspection proves the deployed migration row plus exact table, column, RLS, policy, grant, constraint, index, trigger, function, ACL, Dashboard projection, and backfill contract before behavior verification starts."
    - "The fixture controller obtains service authority non-interactively, keeps it only in memory, and is constrained to the approved manifest IDs/tables for setup, exact source removal, finally cleanup, and zero-residue inspection."
    - "Two independent publishable-key sessions prove own/cross-user table and RPC isolation, resume ownership/deletion, event projection/final-event protection, idempotent Mark Applied, and snapshot survival after fixture-source removal."
    - "Hosted proof executes the exact six-argument manual-create contract and its two-field record result, then proves Dashboard applied history keeps the earliest chronological Applied date after a later repeated Applied event."
    - "Any ASVS L1 HIGH/CRITICAL parity, isolation, cleanup, secret-handling, or residue failure blocks PASS and all later Phase 04 plans."
  artifacts:
    - path: ".planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md"
      provides: "Sanitized migration push, exact catalog parity, backfill, ordinary-session behavior, fixture cleanup, and zero-residue evidence"
  key_links:
    - from: "supabase/migrations/0053_application_tracker.sql"
      to: "scripts/verify-tracker-schema.ts"
      via: "approved local checksum and canonical object inventory compared to hosted catalogs"
      pattern: "0053_application_tracker"
    - from: "scripts/verify-tracker-schema.ts"
      to: "scripts/verify-tracker-rls.ts"
      via: "catalog PASS digest gates every fixture or behavior action"
      pattern: "catalog_evidence_sha256"
    - from: "scripts/verify-tracker-rls.ts"
      to: "public.mark_job_applied"
      via: "ordinary user A invokes the RPC twice against the exact seeded user-job while user B performs cross-owner probes"
      pattern: "mark_job_applied"
---

## Phase Goal

**As a** signed-in user, **I want to** track system-discovered and manually found positions through one durable lifecycle, **so that** application history, follow-ups, preserved job context, and optional resume links remain available in one place.

<objective>
Push the exact approved migration, prove its hosted definitions and authorization boundaries in the required order, and leave zero verifier residue.

Purpose: Establish a fail-closed production schema/security foundation before the complete Tracker and Dashboard interfaces consume it.

Output: Migration 0053 applied to the approved linked project plus sanitized, executable hosted catalog/behavior evidence.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-application-tracker/04-CONTEXT.md
@.planning/phases/04-application-tracker/04-RESEARCH.md
@.planning/phases/04-application-tracker/04-PATTERNS.md
@.planning/phases/04-application-tracker/04-UI-SPEC.md
@.planning/phases/04-application-tracker/04-01-SUMMARY.md
@.planning/phases/04-application-tracker/04-02-SUMMARY.md
@.planning/phases/04-application-tracker/04-02-PREFLIGHT.md
@supabase/migrations/0053_application_tracker.sql
@scripts/verify-tracker-schema.ts
@scripts/verify-tracker-rls.ts
@web/tests/migration-0053-application-tracker.test.ts
@web/tests/verify-tracker-schema.test.ts
@web/tests/verify-tracker-rls.test.ts

<interfaces>
Schema execution consumes only Plan 02's literal approval signal and uses:
- `web/node_modules/.bin/supabase db push --linked --dry-run`
- `web/node_modules/.bin/supabase db push --linked --yes`

Hosted catalog parity runs next:
- `node --env-file=scripts/.env --experimental-strip-types scripts/verify-tracker-schema.ts --mode assert-hosted --migration supabase/migrations/0053_application_tracker.sql --preflight .planning/phases/04-application-tracker/04-02-PREFLIGHT.md --evidence .planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md`

Only after that command returns PASS may behavior run:
- `node --env-file=scripts/.env --experimental-strip-types scripts/verify-tracker-rls.ts --mode hosted --preflight .planning/phases/04-application-tracker/04-02-PREFLIGHT.md --catalog-evidence .planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md --evidence .planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md`

The schema inspector uses the Management API database-query endpoint with
`SUPABASE_ACCESS_TOKEN`. The behavior verifier derives a service-role key
through the approved project-local CLI call, retains it only in memory, and
uses it only for manifest-owned setup/source removal/cleanup. All behavior and
RLS assertions use two independent publishable-key user sessions.

The deployed contracts asserted by both commands are:
- `create_manual_application(p_company text, p_title text, p_apply_url text, p_notes text, p_stage text, p_occurred_on date) returns table(application_id uuid, duplicate_warning boolean)`, exactly one result row.
- `dashboard_applied_applications() returns table(application_id uuid, company_name text, job_title text, location text, apply_url text, applied_on date, current_stage text, current_stage_date date)`, where `apply_url` is nullable/safe and `applied_on` comes from the earliest Applied event under `ORDER BY occurred_on ASC, created_at ASC, id ASC LIMIT 1`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [BLOCKING] Push migration 0053, prove exact catalogs, then prove two-user hosted isolation</name>
  <files>.planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/04-application-tracker/04-02-PREFLIGHT.md
    - .planning/phases/04-application-tracker/04-02-SUMMARY.md
    - supabase/migrations/0053_application_tracker.sql
    - scripts/verify-tracker-schema.ts
    - scripts/verify-tracker-rls.ts
    - web/tests/migration-0053-application-tracker.test.ts
    - web/tests/verify-tracker-schema.test.ts
    - web/tests/verify-tracker-rls.test.ts
  </read_first>
  <behavior>
    - "Remote migration order is exactly through 0053 and the hosted 0053 statement inventory matches the approved local migration checksum/canonical digest."
    - "Catalog assertions cover both tracker tables; every column/default/nullability/RLS flag; all constraints/indexes; the projection trigger/function; every narrow RPC including the exact six manual-create parameters and two return fields; `dashboard_feed_page`; the exact eight-field `dashboard_applied_applications`; policies; table/column/routine grants; definer/search-path/ACL metadata; and legacy backfill count parity."
    - "Behavior startup rejects a missing/stale catalog PASS digest, changed migration/verifier/manifest checksum, project drift, fixture collision, unexpected row, or expanded mutation table/ID/count."
    - "Ordinary user A and B can read/mutate only their own applications/events through tables and every RPC; guessed cross-user application/event/resume identifiers fail closed."
    - "Ordinary-session manual creation sends company/title/HTTPS URL/notes/selected stage/selected date under the six exact parameter names, receives one `application_id` plus boolean `duplicate_warning`, permits the warning without blocking, and remains owner-isolated."
    - "Ordinary-session Dashboard projection reads the exact eight named fields; a later repeated Applied event changes current stage/date as appropriate but does not change the original `applied_on`."
    - "User A's repeated Mark Applied call returns one application/event, source `user_jobs` and `jobs` removal leaves its snapshot/current stage readable, resume deletion clears only the optional link, event projection follows chronological edits, and final-event deletion fails recoverably."
    - "Finally cleanup executes on success and failure and proves no manifest-owned auth user, company, job, user-job, resume, application, or event remains."
  </behavior>
  <action>
    Re-run the focused migration and both verifier contract suites before any hosted mutation. Require Plan 02's exact literal approval and revalidate the unchanged linked project ref, migration/verifier/fixture-manifest hashes, CLI identity, local/remote migration inventory, legacy-applied baseline, and sole-pending dry run. Stop on drift, ambiguity, missing environment values, or any HIGH/CRITICAL finding. With `SUPABASE_ACCESS_TOKEN` present but never printed, execute only the approved project-local `db push --linked --yes`; immediately run `migration list --linked` and require exactly 0053 after 0052.

    Before any behavior script or fixture action, run the exact `verify-tracker-schema.ts --mode assert-hosted` command from `<interfaces>`. Require the Management API query to prove the approved 0053 migration statement digest and deterministic canonical parity for every reviewed table/column/RLS/policy/grant/constraint/index/trigger/function definition, function owner/security/search-path/ACL, `dashboard_feed_page`, and one-to-one legacy backfill counts. Explicitly compare `create_manual_application` by parameter name/order/type and `application_id uuid, duplicate_warning boolean` result, and compare `dashboard_applied_applications` by all eight result names/order/types plus its safe-URL and earliest-Applied ordering definition. Compare against the Plan 02 baseline and local canonical inventory. The script must write only sanitized object names, booleans, counts, versions, and SHA-256 values. Any missing/extra/changed object or privilege blocks behavior; static migration tests or migration-list presence cannot substitute for this gate.

    Only after catalog PASS, run the exact `verify-tracker-rls.ts --mode hosted` command from `<interfaces>`. Require it to recompute all approved hashes and the catalog evidence digest, obtain `service_role` non-interactively through the approved CLI API-key command, hold/redact it in memory, collision-check the exact fixture namespace/IDs, and seed only the approved two Auth users plus literal company/job/user-job/resume fixtures. Every Mark Applied, own/cross-owner table/RPC/resume assertion, event mutation, final-event rejection, and snapshot read must use the two ordinary publishable-key sessions. Manual creation must call exactly `p_company`, `p_title`, `p_apply_url`, `p_notes`, `p_stage`, and `p_occurred_on`, reject required/HTTPS failures, and accept exactly one `{ application_id, duplicate_warning }` result while proving the duplicate warning does not block. Query `dashboard_applied_applications` before and after appending a later Applied event and require all eight fields, safe nullable URL behavior, and unchanged earliest `applied_on`. The privileged controller may only remove the exact fixture `user_jobs`/`jobs` for the source-loss probe, perform finally cleanup of manifest-owned rows, delete the two exact Auth users last, and query zero residue. It must refuse broad predicates, real-row matches, unexpected counts, authority leakage, or cleanup that is not both ID- and namespace-bound.

    Record command versions, approved hashes/target, migration order, catalog inventory digest, object/ACL assertion labels, legacy baseline/backfill counts, ordinary-session probe labels, exact source-removal result, cleanup labels/counts, and zero-residue PASS in `04-03-SCHEMA-EVIDENCE.md`. Never record SQL bodies, endpoints, service/publishable/access tokens, JWTs, passwords, auth/user/application UUIDs, emails, notes, resume data, job descriptions, or unrelated worktree content. If cleanup or residue proof fails, evidence remains FAIL and Plans 04–05 do not execute. Do not attempt schema rollback, manual Dashboard SQL, interactive login, reset/repair, package install, provider call, polling change, AI call, or resume generation.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- tests/migration-0053-application-tracker.test.ts tests/verify-tracker-schema.test.ts tests/verify-tracker-rls.test.ts &amp;&amp; cd .. &amp;&amp; web/node_modules/.bin/supabase migration list --linked &amp;&amp; node --env-file=scripts/.env --experimental-strip-types scripts/verify-tracker-schema.ts --mode assert-hosted --migration supabase/migrations/0053_application_tracker.sql --preflight .planning/phases/04-application-tracker/04-02-PREFLIGHT.md --evidence .planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md &amp;&amp; node --env-file=scripts/.env --experimental-strip-types scripts/verify-tracker-rls.ts --mode hosted --preflight .planning/phases/04-application-tracker/04-02-PREFLIGHT.md --catalog-evidence .planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md --evidence .planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md &amp;&amp; test -s .planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - "The exact approved non-TTY command applies only migration 0053 after 0052."
    - "The executable catalog command exits 0 before behavior starts and proves migration, object definition, RLS/policy, table/column/routine ACL, constraint/index, trigger/function, Dashboard projection, and legacy-backfill parity—not merely local SQL or migration-list presence."
    - "The service-role key is obtained non-interactively, remains memory-only and redacted, and can affect only checksum-bound manifest fixtures during setup, source removal, finally cleanup, and residue inspection."
    - "Every behavioral isolation assertion runs through two ordinary publishable-key sessions; own probes succeed and cross-user table/RPC/resume probes fail closed."
    - "Idempotent Mark Applied, manual required/HTTPS/duplicate behavior, source-loss snapshot retention, resume unlink-on-delete, event projection, repeated stages, and final-event rejection pass."
    - "Hosted catalog and behavior evidence agree on the exact manual-create signature/result and Dashboard applied eight-field/earliest-event contract; no consumer-specific coercion or inferred date remains."
    - "Cleanup executes on every exit and reports zero manifest-owned rows across auth users, companies, jobs, user_jobs, resumes, applications, and application_stage_events."
    - "Evidence contains only sanitized labels/counts/hashes and no secret, endpoint, identifier, raw user content, or unrelated worktree data."
  </acceptance_criteria>
  <done>Migration 0053 is live in the intended environment with exact catalog parity, proven ordinary-user isolation, and zero verifier residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

OWASP ASVS Level 1; any HIGH or CRITICAL finding blocks completion.

| Boundary | Description |
|---|---|
| Approved migration → hosted Supabase | Exact reviewed schema bytes cross a privileged non-interactive production boundary |
| Management API → PostgreSQL catalogs | A privileged read channel must prove exact deployed definitions without leaking raw content |
| Access token → ephemeral service-role key | Key discovery crosses a high-authority secret boundary |
| Privileged controller → fixture tables/Auth | Only checksum-bound manifest IDs may be created, source-removed, cleaned, or residue-checked |
| Ordinary user A → ordinary user B identifiers | All behavior/RLS probes must remain session-scoped and fail cross-owner |
| Verifier output → repository | Evidence must expose only sanitized labels, counts, and hashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-03-01 | Tampering / Repudiation | schema push | high | mitigate | Exact approval, hash/target revalidation, sole-pending dry run, non-TTY push, immediate migration parity |
| T-04-03-02 | Tampering | hosted catalog parity | high | mitigate | Blocking Management API catalog assertion over definitions, security metadata, ACLs, Dashboard projections, and backfill counts before behavior |
| T-04-03-03 | Elevation of Privilege / Information Disclosure | service-role discovery | high | mitigate | Project-local CLI under access token, memory-only parsing, recursive redaction, no persistence or argument logging |
| T-04-03-04 | Tampering | fixture setup/source removal/cleanup | high | mitigate | Approved literal manifest, collision checks, ID+namespace predicates, table/count allowlist, FK-safe finally cleanup, zero residue |
| T-04-03-05 | Information Disclosure / Elevation of Privilege | applications/events/RPCs | high | mitigate | Independent publishable-key sessions, unfiltered/targeted cross-user probes, exact function ACL checks |
| T-04-03-06 | Elevation of Privilege | resume linking | high | mitigate | Owner-coupled FK/RPC verification and cross-user link rejection through ordinary sessions |
| T-04-03-07 | Tampering | event projection | high | mitigate | Hosted append/edit/delete/repeat/final-event probes plus exact trigger/function catalog parity |
| T-04-03-08 | Information Disclosure / Repudiation | evidence | medium | mitigate | Allowlisted sanitized fields, recursive secret/content scan, FAIL retained on cleanup or residue failure |
| T-04-03-SC | Tampering | dependency supply chain | low | accept | No package install; execute repository-pinned CLI/runtime and project-owned scripts only |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `.planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md`
  - `Approval binding`: exact migration/target/order/verifier/fixture hashes
  - `Catalog PASS`: hosted table/column/policy/grant/constraint/index/trigger/function labels, exact manual-create signature/result label, exact Dashboard applied return/earliest-date label, ACL/search-path/security labels, backfill digest/counts
  - `Ordinary behavior PASS`: two-user RLS/RPC labels, six-parameter/two-result manual create, duplicate warning, eight-field Dashboard applied row, repeated-Applied original-date stability, Mark Applied/source-loss/resume/event/final-event labels
  - `Privileged fixture bounds`: manifest-only setup, exact source removal, finally cleanup labels/counts
  - `Zero residue`: auth/users/company/job/user-job/resume/application/event zero counts and sanitized PASS/FAIL status
- No new runtime symbol, SQL object, script mode, or CLI flag is created in this plan; it executes the exact `assert-hosted` and `hosted` interfaces produced by Plan 02.
</artifacts_this_phase_produces>

<source_coverage>
## Multi-source coverage audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable tracker persistence in the intended environment | 04-03 | COVERED | Hosted schema/security foundation |
| REQ | TRAK-01 | Exact lifecycle/event persistence | 04-03 | COVERED | Catalog + behavior proof |
| REQ | TRAK-02 | Owner-safe manual persistence boundary | 04-03 | COVERED | RPC and isolation proof |
| REQ | TRAK-03 | Owner-safe notes mutation boundary | 04-03 | COVERED | RPC/ACL isolation proof |
| REQ | TRAK-04 | Snapshot retention and optional owned resume | 04-03 | COVERED | Source-removal/resume proof |
| CONTEXT | D-01, D-04, D-05, D-14, D-15, D-23 | Atomic Mark Applied, durable membership, exact stages/date projection, final-event and resume-delete invariants | 04-03 | COVERED | Ordinary-session hosted assertions |
| RESEARCH | — | Forward migration, event projection, backfill, hardened RLS/RPC | 04-03 | COVERED | Exact catalog plus two-user proof |
</source_coverage>

<verification>
- Schema push occurs only after exact approval and before hosted catalog/behavior verification.
- The executable hosted catalog command passes before fixture seeding or behavior.
- Ordinary-user isolation and functional probes pass with bounded privileged setup/source removal/cleanup.
- Evidence reports zero fixture residue and contains no secrets or raw user content.
</verification>

<success_criteria>
- The intended Supabase project runs exact migration 0053 with independently asserted object/ACL/backfill parity.
- Two ordinary users prove complete tracker authorization and critical lifecycle behavior.
- Every disposable fixture is removed exactly, and any HIGH/CRITICAL or cleanup failure blocks Plans 04–05.
</success_criteria>

<output>
Create `.planning/phases/04-application-tracker/04-03-SUMMARY.md` when done.
</output>
