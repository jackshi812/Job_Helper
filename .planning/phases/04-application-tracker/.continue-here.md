---
context: phase
phase: 04-application-tracker
task: 1
total_tasks: 1
status: in_progress
last_updated: 2026-07-28T04:50:19.776Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Opaque hosted timeout — `verify-tracker-rls.ts` turns all 30-second fetch aborts into the same generic `TimeoutError` and emits no partial evidence. Add non-sensitive step labels and elapsed timings, with tests, before another hosted attempt.
- [ ] CONSTRAINT: Auth deletion is not self-proving — Admin DELETE returned repeated 504s, and 200 log entries did not reliably prove durable absence. Independently GET both exact manifest users and audit all seven relations after every attempt.
- [ ] CONSTRAINT: Approval is byte-bound — any verifier instrumentation changes the approved hash. Recompute the complete affected envelope and obtain fresh exact owner approval before creating hosted fixtures.
- [ ] CONSTRAINT: Production mutation authority stays at root — typed child executors did not inherit owner approval. Children may prepare read-only evidence, but the root context must execute the exact approved hosted command.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Blind verifier retry | Repeated unchanged runs recreated the two disposable users, timed out at the same opaque boundary, and yielded no new diagnosis. | blocking | Instrument named steps and timings first; prove the diagnostics redact credentials and user content with focused tests. |
| Trusting DELETE status or logs | Auth Admin DELETE produced mixed 504 and 200 events while later exact reads could still find active fixture users. | blocking | Require exact GET 404 for both manifest users plus zero counts across all seven relations. |
| Reusing an old approval after code changes | Verifier bytes and behavior-scope hashes are part of the production authorization envelope. | blocking | Recompute hashes and request a new literal approval after instrumentation is final. |
| Child-owned hosted mutation | The executor's escalation reviewer did not recognize approval copied into the child prompt. | advisory | Keep child work read-only and return the sanitized exact command for root execution. |

<current_state>
Phase 04 is paused in Plan 04-03, Task 1. Plans 04-01 and 04-02 are complete (2 of 5 plans). Migration 0053 is live on the intended Supabase project, remote migration order is exact through 0053, and the repaired hosted schema/catalog verifier passed. The two-user ordinary-session behavior proof is incomplete because the verifier repeatedly aborts near 30 seconds with an unlabeled fetch timeout. No `04-03-RLS-EVIDENCE.json` or `04-03-SUMMARY.md` exists.

The pause boundary is clean: the latest owner-approved guarded SQL cleanup passed, then an independent audit returned 404 for both disposable Auth users and zero rows in `application_stage_events`, `applications`, `user_jobs`, `resumes`, `jobs`, `companies`, and `auth.users`. Do not start Plan 04-04.
</current_state>

<completed_work>

Completed Tasks:
- Plan 04-01: Atomic Dashboard-to-Tracker vertical slice — Done in `8e8e3e5`.
- Plan 04-02: Checksum-bound production preflight and approval — Done in `83ec94f`.
- Plan 04-03 Task 1: Applied migration 0053 and confirmed exact remote order.
- Plan 04-03 Task 1: Reproduced and repaired schema-verifier catalog drift in `5418025` and `c9718d2`.
- Plan 04-03 Task 1: Passed focused tests, the 1,474-test web suite, build, lint, diff, hosted catalog/ACL/RLS-definition/function/backfill verification, and committed the approval envelope in `a7b97cd`.
- Plan 04-03 Task 1: Removed all failed-verifier residue and independently confirmed the clean seven-relation baseline.
</completed_work>

<remaining_work>

- Plan 04-03 Task 1: Instrument `verify-tracker-rls.ts` with credential-safe step labels and elapsed timings; add focused RED/GREEN tests.
- Plan 04-03 Task 1: Recompute the behavior verifier/test/scope hashes and every affected approval binding; obtain fresh exact owner approval.
- Plan 04-03 Task 1: Recheck the clean production baseline, run the hosted behavior proof once from the root context, independently verify cleanup, write sanitized PASS evidence, and create `04-03-SUMMARY.md`.
- Plan 04-04: Not started.
- Plan 04-05: Not started.
- Phase 04: Run final regression, review, and goal verification after all five plans complete.
</remaining_work>

<decisions_made>

- Do not rerun the unchanged behavior verifier because it cannot localize the timeout and each attempt creates cleanup risk without adding evidence.
- Guarded SQL cleanup remains an exact-fixture fallback only after manifest identity, exact count, and storage-zero validation; broad SQL cleanup remains forbidden.
- Independent GET 404 plus seven-relation zero residue is authoritative; dashboard toasts, API status alone, and Auth log messages are not.
- Any production mutation after instrumentation must use a fresh checksum-bound approval and execute from the root context.
- Wave 4 remains gated on a complete Plan 04-03 behavior PASS and zero residue.
</decisions_made>

<blockers>

- Hosted behavior timeout: exact failing HTTP operation is still unknown. Structural mitigation is verifier instrumentation plus tests before retry.
- Auth Admin cleanup reliability: project-specific deletes can exceed the gateway deadline. Structural mitigation is exact post-delete reads and seven-relation auditing, with the narrowly authorized SQL fallback only when every guard passes.
- Approval drift: instrumentation invalidates the current behavior-verifier approval. A new exact owner approval is required after hashes are finalized.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable pause state, blockers, decisions, and next action.
2. `.planning/phases/04-application-tracker/04-03-PLAN.md` — exact hosted proof contract and authority boundary.
3. `.planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md` — committed schema/catalog PASS binding.
4. `.planning/phases/04-application-tracker/04-03-CATALOG-EVIDENCE.json` — sanitized canonical hosted catalog evidence.
5. `scripts/verify-tracker-rls.ts` — opaque timeout source and fixture/cleanup controller.
6. `web/tests/verify-tracker-rls.test.ts` — behavior verifier authority, isolation, cleanup, and redaction contracts.
7. `.planning/phases/04-application-tracker/04-02-SUMMARY.md` and `.planning/phases/04-application-tracker/04-02-PREFLIGHT.md` — original approval envelope and exclusions.
8. `.planning/phases/04-application-tracker/04-CONTEXT.md` and `.planning/phases/04-application-tracker/04-UI-SPEC.md` — locked product and UI decisions for remaining plans.

## Critical Anti-Patterns (do NOT repeat these)

- Rerun unchanged verifier → first add step-aware, non-sensitive diagnostics and tests.
- Accept Auth DELETE/log success as cleanup proof → independently verify exact 404s and seven zero counts.
- Modify approval-bound verifier and reuse prior approval → recompute the envelope and obtain a new literal approval.
- Let a child agent perform the hosted mutation → root executes the sanitized, exact approved command.
- Start Plan 04-04 while Plan 04-03 lacks behavior PASS evidence → keep the wave gate closed.

## Infrastructure State

- Supabase target: `fjcsvajkkztvlrpdplwx`.
- Remote schema: migration `0053_application_tracker.sql` is installed after 0052; hosted catalog verification passed.
- Production residue at pause: both disposable Auth users return 404; `application_stage_events=0`, `applications=0`, `user_jobs=0`, `resumes=0`, `jobs=0`, `companies=0`, `auth.users=0`.
- Evidence: `04-03-SCHEMA-EVIDENCE.md` and `04-03-CATALOG-EVIDENCE.json` exist; behavior PASS evidence and `04-03-SUMMARY.md` do not.
- Approval binding: migration SHA-256 `7da3c2215eb00fbee410388b79ce5dddf2e589ad9f176ad3d31c7f543ed923c0`; repaired schema verifier SHA-256 `e6d303172dbd206a394aec33df941bb9627bf541a899f0e894293761e4820d26`; current behavior verifier SHA-256 `2dede3082e76f33e4b6dcf79848a40140a4ee47d37b7044965f8b182962a531b`. The behavior hash must change before retry.
- Runtime: no agents, servers, watchers, or async external jobs remain active.
- Worktree: unrelated user-owned `.DS_Store`, Phase 03.8 `.gitkeep`, `scripts/agent-dashboard.mjs`, `scripts/agent-dashboard.test.mjs`, and `web/zh` remain unmodified and unstaged.

<context>
The migration itself and deployed catalog are no longer suspect. The next investigation should make the hosted behavior verifier observable enough to identify which fetch crosses the 30-second deadline without exposing endpoints, credentials, fixture identities, or user content. Preserve its fail-closed lineage and cleanup rules. After the instrumentation tests pass, freeze a new approval envelope; do not create fixtures until the owner approves those exact bytes.
</context>

<next_action>
Start with: acknowledge the four blocking constraints, confirm the seven-relation production baseline is still clean, then add non-sensitive step labels and elapsed timings to `scripts/verify-tracker-rls.ts` with focused tests.
</next_action>
