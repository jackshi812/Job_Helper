---
context: phase
phase: 04-application-tracker
task: 3
total_tasks: 5
status: paused
last_updated: 2026-07-28T03:09:33.252Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Revision-loop stall — the checker issue count increased from 3 to 4 after revision iteration 2 — obtain the user's choice to continue revision or accept the current plans.
- [ ] CONSTRAINT: Plans are not execution-ready — four cross-plan/UI/fixture inconsistencies remain — do not run `$gsd-execute-phase 4` until the plan checker returns `## VERIFICATION PASSED` or the user explicitly accepts the gaps.
- [ ] CONSTRAINT: Preserve the dirty worktree — unrelated modified/untracked files belong to the user — stage only explicit Phase 4 handoff/planning files.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Contract drift across waves | SQL result names, TypeScript parsers, catalog assertions, hosted probes, and UI consumers diverged during revisions. | blocking | Define each contract once in Plan 01, then mechanically compare every downstream occurrence before checker dispatch. |
| Predeclaring generated IDs | The fixture manifest required literal application/event UUIDs even though ordinary RPCs generate them at runtime. | blocking | Predeclare only seedable IDs; approve a bounded derivation rule and retain returned IDs in a memory-only lineage allowlist. |
| Editing locked UI behavior in planning | The plan made the initial stage date editable before save despite the approved UI-SPEC requiring automatic current date. | blocking | Treat `04-UI-SPEC.md` as binding and run a decision-by-decision diff before the next checker pass. |
| Broad git staging | The worktree contains unrelated user changes. | advisory | Use explicit file lists for every commit and inspect `git diff --cached` before committing. |

<current_state>
Phase 4 is paused during plan-checker convergence, before implementation. Five sequential plans exist in waves 1–5. Two planner revisions were committed, but the latest independent checker found 3 blockers and 1 warning, increasing the issue count and triggering GSD's mandatory stall gate.
</current_state>

<completed_work>

Completed Tasks:
- Approved `04-UI-SPEC.md` after a 6/6 UI checker pass (`444762f`).
- Used existing technical research and created `04-PATTERNS.md`; current migration head is 0052, so Phase 4 targets `0053_application_tracker.sql`.
- Created five sequential plan files covering atomic Mark Applied, schema preflight, hosted proof, tracker spreadsheet/timeline, and Dashboard integration.
- Resolved earlier issues around research questions, privileged fixture authority, hosted catalog inspection, plan size, manual-create RPC shape, Dashboard applied-date semantics, and artifact inventories.
- Committed plan drafts/revisions in `f514815`, `3195719`, and `5c1912c`.
</completed_work>

<remaining_work>

- Obtain the user's choice at the revision-loop stall gate. Continuing revision is recommended.
- If continuing, address exactly these latest findings:
  1. Replace `company_name`/`job_title` with SQL result names `company`/`title` across Plans 01, 02, 03, and 05; camel-case TypeScript properties may map explicitly.
  2. Keep `p_occurred_on` in manual creation but automatically submit the current date; remove all pre-save stage-date editing and update Plan 01/04 truths, tests, actions, and criteria.
  3. Remove literal generated application/event UUIDs from the immutable fixture manifest. Capture RPC-returned/queried IDs into a memory-only lineage allowlist constrained by owners, parents, namespace, and exact counts.
  4. State consistently that fixture cleanup covers six public tables plus `auth.users`, seven relations total.
- Rerun the typed `gsd-plan-checker`; do not proceed until it passes or the user explicitly accepts the gaps.
- After a pass: run requirement and decision coverage gates, update STATE to Ready to execute, annotate ROADMAP dependencies, commit only explicit Phase 4 planning/state files, and offer `$gsd-execute-phase 4`.
</remaining_work>

<decisions_made>

- Resume tailoring is deleted from Phase 4; the user will do it manually. No AI API, web scraping, provider calls, or external polling belong in this phase.
- Tracker stages are exactly Ready to Apply, Applied, Outreach Sent, Interview, Offer, and Rejected, with approved semantic colors.
- Dashboard Mark Applied atomically creates/updates a durable tracker application at Applied.
- Manual additions are Tracker-only and require company, title, and HTTPS URL.
- Stage changes automatically create dated events; current stage derives from the latest event; the final event cannot be deleted.
- Tracker is Excel-like with inline autosave, cell-local Saving/Saved/Retry, active filters, pin-to-top, notes, optional resume link, and an expandable horizontal timeline.
- Migration 0053 must be pushed only after checksum/target approval and must be followed by catalog parity and two-user RLS verification.
</decisions_made>

<blockers>
- Human decision: continue plan revision or accept current gaps. Recommended: continue revision.
- Technical: the four checker findings listed under Remaining Work.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable pause state and exact next action.
2. `.planning/phases/04-application-tracker/04-CONTEXT.md` — locked user decisions D-01–D-23.
3. `.planning/phases/04-application-tracker/04-UI-SPEC.md` — approved binding UI/interaction contract.
4. `.planning/phases/04-application-tracker/04-RESEARCH.md` — implementation research and resolved questions.
5. `.planning/phases/04-application-tracker/04-PATTERNS.md` — current code analogs and migration numbering.
6. `.planning/phases/04-application-tracker/04-01-PLAN.md` through `04-05-PLAN.md` — current unapproved execution plans.
7. `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` — requirement mapping and phase sequencing.

## Critical Anti-Patterns (do NOT repeat these)

- CONTRACT DRIFT: Do not change a contract in one plan without updating migration, client parser, catalog verifier, hosted behavior verifier, fixtures, and every UI consumer.
- GENERATED-ID MANIFEST: Do not require immutable literal UUIDs for records created by server-generated-ID RPCs; approve a bounded runtime derivation rule.
- UI-SPEC OVERRIDE: Do not make the pre-save stage date editable; it defaults to the current date and is read-only until creation.
- DIRTY-WORKTREE CAPTURE: Do not stage unrelated modified/untracked files.

## Infrastructure State

- No Phase 4 application code has been implemented.
- No Supabase migration has been created, pushed, or applied for Phase 4.
- No external async jobs are running.
- The managed `caffeinate -dimsu` exec session ended while this pause handoff was finalized; no managed background process remains.
- Uncommitted user/unrelated files are listed in `.planning/HANDOFF.json`; preserve them.

## Pre-Execution Critique Required

- Design artifacts: `.planning/phases/04-application-tracker/04-01-PLAN.md` through `04-05-PLAN.md`
- Critique focus: exact projection field names, automatic initial stage date, runtime-derived fixture lineage, and seven-relation cleanup inventory.
- Gate: Do NOT begin execution until a typed `gsd-plan-checker` returns `## VERIFICATION PASSED` or the user explicitly accepts the remaining gaps.

<context>
The desired end state is a purely coded, durable application tracker with no resume tailoring or external-source work. The current five-wave decomposition is sound, but cross-plan details are not yet consistent enough for safe execution. Resume at the checker stall decision, not at implementation.
</context>

<next_action>
Start with: ask the user whether to continue plan revision (recommended) or accept the current plans; if continuing, revise only the four latest findings and rerun the checker.
</next_action>
