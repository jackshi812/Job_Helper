---
context: phase
phase: 04-application-tracker
task: 1
total_tasks: 1
status: awaiting_approval
last_updated: 2026-07-28T14:49:14Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: Opaque hosted timeout — `verify-tracker-rls.ts` turns all 30-second fetch aborts into the same generic `TimeoutError` and emits no partial evidence. Add non-sensitive step labels and elapsed timings, with tests, before another hosted attempt.
- [x] CONSTRAINT: Auth deletion is not self-proving — Admin DELETE returned repeated 504s, and 200 log entries did not reliably prove durable absence. Independently GET both exact manifest users and audit all seven relations after every attempt.
- [x] CONSTRAINT: Approval is byte-bound — any verifier instrumentation changes the approved hash. Recompute the complete affected envelope and obtain fresh exact owner approval before creating hosted fixtures.
- [x] CONSTRAINT: Production mutation authority stays at root — typed child executors did not inherit owner approval. Children may prepare read-only evidence, but the root context must execute the exact approved hosted command.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Blind verifier retry | Repeated unchanged runs recreated the two disposable users, timed out at the same opaque boundary, and yielded no new diagnosis. | blocking | Instrument named steps and timings first; prove the diagnostics redact credentials and user content with focused tests. |
| Trusting DELETE status or logs | Auth Admin DELETE produced mixed 504 and 200 events while later exact reads could still find active fixture users. | blocking | Require exact GET 404 for both manifest users plus zero counts across all seven relations. |
| Reusing an old approval after code changes | Verifier bytes and behavior-scope hashes are part of the production authorization envelope. | blocking | Recompute hashes and request a new literal approval after instrumentation is final. |
| Child-owned hosted mutation | The executor's escalation reviewer did not recognize approval copied into the child prompt. | advisory | Keep child work read-only and return the sanitized exact command for root execution. |

<current_state>
Phase 04 is awaiting exact owner approval for the repaired hosted behavior
proof in Plan 04-03, Task 1. Plans
04-01 and 04-02 are complete (2 of 5 plans). Migration 0053 is live on the
intended Supabase project, remote migration order is exact through 0053, and
the refreshed read-only hosted schema/catalog verifier passed. The two-user
ordinary-session behavior proof is still incomplete; no
`04-03-RLS-EVIDENCE.json` or `04-03-SUMMARY.md` exists.

The third approved retry seeded all direct fixtures and established both
ordinary sessions, then stopped at the first `mark_job_applied` request with
PostgreSQL `42702` (`ambiguous_column`). The bounded SQL cleanup verified both
manifest identities, zero storage/public residue, and an exact two-user
deletion; an independent audit then returned 404 for both users and zero for
all seven relations. RED `9a90ab2` and GREEN `62e9783` add the forward 0054
repair and bind it into both verifiers. The exact owner-approved push succeeded,
remote order is exact through 0054, and the post-repair catalog verifier passed.
All 1,480 tests pass. Do not start Plan 04-04.
</current_state>

<completed_work>

Completed Tasks:
- Plan 04-01: Atomic Dashboard-to-Tracker vertical slice — Done in `8e8e3e5`.
- Plan 04-02: Checksum-bound production preflight and approval — Done in `83ec94f`.
- Plan 04-03 Task 1: Applied migration 0053 and confirmed exact remote order.
- Plan 04-03 Task 1: Reproduced and repaired schema-verifier catalog drift in `5418025` and `c9718d2`.
- Plan 04-03 Task 1: Passed focused tests, the 1,474-test web suite, build, lint, diff, hosted catalog/ACL/RLS-definition/function/backfill verification, and committed the approval envelope in `a7b97cd`.
- Plan 04-03 Task 1: Removed all failed-verifier residue and independently confirmed the clean seven-relation baseline.
- Plan 04-03 Task 1: Added credential-safe, allowlisted request-step diagnostics with elapsed timing in RED `26d0c52` and GREEN `c7d7473`.
- Plan 04-03 Task 1: Passed 3 focused files / 22 tests, the complete 71-file / 1,476-test suite, build, lint, and diff checks.
- Plan 04-03 Task 1: Refreshed read-only hosted catalog PASS and rebound the exact behavior approval envelope.
- Plan 04-03 Task 1: The approved retry localized `fixture.seed.companies`; fail-closed cleanup and an independent audit restored seven-relation zero residue.
- Plan 04-03 Task 1: Added RED `e721b96` and GREEN `7450faf` for required company `careers_url` and `source_key`, then passed 22 focused and 1,476 full-suite tests, build, and lint.
- Plan 04-03 Task 1: Refreshed the read-only catalog and a second exact approval envelope.
- Plan 04-03 Task 1: The second approved retry seeded all direct fixtures and established both ordinary sessions, then localized failure to the first `mark_job_applied` request; exact guarded cleanup and independent audit restored zero residue.
- Plan 04-03 Task 1: Added RED `0031a1a` and GREEN `64d5df5` to retain only a bounded remote code and preserve the primary proof failure across cleanup.
- Plan 04-03 Task 1: Passed 3 focused files / 23 tests, the complete 71-file / 1,477-test suite, build, lint, and refreshed read-only hosted catalog verification.
- Plan 04-03 Task 1: The third approved proof retained PostgreSQL code `42702` at the first Mark Applied RPC; guarded cleanup plus independent audit restored zero residue across all fixture relations and storage.
- Plan 04-03 Task 1: Added RED `9a90ab2` and GREEN `62e9783` for sole-pending migration 0054, replacing only `mark_job_applied` with an unambiguous `target_application_id`.
- Plan 04-03 Task 1: Passed 3 focused files / 17 tests, the complete 72-file / 1,480-test suite, build, lint, linked migration inventory, and sole-pending 0054 dry run.
- Plan 04-03 Task 1: Pushed exact owner-approved migration 0054, verified remote order, and passed the post-repair hosted catalog/function/RLS/ACL/backfill assertion.
</completed_work>

<remaining_work>

- Plan 04-03 Task 1: Obtain fresh exact owner approval for the post-0054 behavior envelope.
- Plan 04-03 Task 1: Run the hosted behavior proof once from the root context, independently verify cleanup, write sanitized PASS evidence, and create `04-03-SUMMARY.md`.
- Plan 04-04: Not started.
- Plan 04-05: Not started.
- Phase 04: Run final regression, review, and goal verification after all five plans complete.
</remaining_work>

<decisions_made>

- Do not rerun the unchanged behavior verifier because it cannot localize the timeout and each attempt creates cleanup risk without adding evidence.
- Guarded SQL cleanup remains an exact-fixture fallback only after manifest identity, exact count, and storage-zero validation; broad SQL cleanup remains forbidden.
- Independent GET 404 plus seven-relation zero residue is authoritative; dashboard toasts, API status alone, and Auth log messages are not.
- Any production mutation after instrumentation must use a fresh checksum-bound approval and execute from the root context.
- Wave 4 remains gated on a complete Plan 04-03 behavior PASS and zero residue.
</decisions_made>

<blockers>

- Hosted behavior proof: migration 0054 repaired PostgreSQL `42702` and the post-repair catalog passed; exact byte-bound approval is required before recreating disposable behavior fixtures.
- Auth Admin cleanup reliability: project-specific deletes can exceed the gateway deadline. Structural mitigation is exact post-delete reads and seven-relation auditing, with the narrowly authorized SQL fallback only when every guard passes.
- Approval checkpoint: post-0054 hashes, hosted catalog digest, behavior scope,
  and zero residue are finalized; exact owner approval is required before the
  repaired behavior proof.
</blockers>

## Required Reading (in order)

1. `.planning/phases/04-application-tracker/04-03-SCHEMA-EVIDENCE.md` — current post-0054 behavior approval signal and machine binding.
2. `.planning/phases/04-application-tracker/04-03-RPC-REPAIR-PREFLIGHT.md` — consumed migration 0054 approval and dry-run evidence.
3. `.planning/phases/04-application-tracker/04-03-PLAN.md` — exact hosted proof contract and authority boundary.
4. `scripts/verify-tracker-rls.ts` — instrumented fixture/behavior/cleanup controller.
5. `web/tests/verify-tracker-rls.test.ts` — behavior verifier authority, isolation, cleanup, redaction, and diagnostic contracts.
6. `.planning/phases/04-application-tracker/04-02-SUMMARY.md` and `.planning/phases/04-application-tracker/04-02-PREFLIGHT.md` — original schema-push approval envelope and exclusions.
7. `.planning/phases/04-application-tracker/04-CONTEXT.md` and `.planning/phases/04-application-tracker/04-UI-SPEC.md` — locked product and UI decisions for remaining plans.

## Critical Anti-Patterns (do NOT repeat these)

- Rerun unchanged verifier → first add step-aware, non-sensitive diagnostics and tests.
- Accept Auth DELETE/log success as cleanup proof → independently verify exact 404s and seven zero counts.
- Modify approval-bound verifier and reuse prior approval → recompute the envelope and obtain a new literal approval.
- Let a child agent perform the hosted mutation → root executes the sanitized, exact approved command.
- Start Plan 04-04 while Plan 04-03 lacks behavior PASS evidence → keep the wave gate closed.

## Infrastructure State

- Supabase target: `fjcsvajkkztvlrpdplwx`.
- Remote schema: migration `0054_mark_job_applied_ambiguity.sql` is installed after 0053; post-repair hosted catalog verification passed.
- Production residue at pause: both disposable Auth users return 404; `application_stage_events=0`, `applications=0`, `user_jobs=0`, `resumes=0`, `jobs=0`, `companies=0`, `auth.users=0`.
- Evidence: `04-03-SCHEMA-EVIDENCE.md` and `04-03-CATALOG-EVIDENCE.json` exist; behavior PASS evidence and `04-03-SUMMARY.md` do not.
- Behavior approval binding: source commit `62e97831eb61b28aa46c0d86657b1d55e94848aa`; repair migration SHA-256 `111fc68f01dd0658cd2536c4fb6abfd99ba25179e70b98ef158e3ab22753a5c3`; schema verifier SHA-256 `74216fdc290a863b1e1f756e665b99f657795aad45f8d2ceafdb87a999c77349`; behavior verifier SHA-256 `3c1ceec5ba40767816cf5aea78c748bf8c7ad760b36c713cb814e87300555d14`; catalog evidence SHA-256 `c208d0aa04760fe91c5f76c35615333a5f417efb7ac09ba94b98b8a45d4a1d03`; behavior scope SHA-256 `a9cfc81a7bf1d005dbe3495898046dc6c2c8e4c2f5c77bb79ef4922d269f3b51`.
- Runtime: no agents, servers, watchers, or async external jobs remain active.
- Worktree: unrelated user-owned `.DS_Store`, Phase 03.8 `.gitkeep`, `scripts/agent-dashboard.mjs`, `scripts/agent-dashboard.test.mjs`, and `web/zh` remain unmodified and unstaged.

<context>
The migration itself and deployed catalog are no longer suspect. The hosted
behavior verifier identifies each fetch with a static safe label and timing,
retains only bounded HTTP/database codes, and preserves the first proof
failure without exposing endpoints, credentials, fixture identities, response
content, or user content. Its fail-closed lineage and cleanup rules remain
unchanged. Do not create fixtures until the owner approves the exact refreshed
signal.
</context>

<next_action>
Start with: require the exact literal approval signal in
`04-03-SCHEMA-EVIDENCE.md`, recompute every bound digest, target, remote tail,
catalog self-digest, and behavior scope, then run the hosted behavior verifier
once from the root context. Independently verify exact zero residue afterward.
</next_action>
