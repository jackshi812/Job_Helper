---
phase: 01-foundation-access
plan: 2
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - web/src/pages/Resumes.tsx
  - web/src/components/ConfirmDialog.tsx
  - web/src/lib/resumes.ts
  - scripts/verify-rls.ts
autonomous: true
requirements: [AUTH-03, AUTH-04]
must_haves:
  truths:
    - "A signed-in user can upload a DOCX/PDF resume, see it in a list, download it, and hard-delete it (single confirm dialog)"
    - "A deleted resume is gone from BOTH the resumes table and the storage bucket"
    - "Logged in as either account, no query or API call can read, modify, delete, or re-parent the other user's resume rows or files — proven by an executable two-account script"
  artifacts:
    - path: "web/src/pages/Resumes.tsx"
      provides: "Real upload/list/download/delete UI (walking-skeleton DB+storage read/write)"
      contains: "delete"
      min_lines: 60
    - path: "web/src/lib/resumes.ts"
      provides: "Storage-first delete helper + upload helper with UUID paths"
      contains: "crypto.randomUUID"
    - path: "web/src/components/ConfirmDialog.tsx"
      provides: "Single-confirm dialog for per-item deletes (D-11)"
      min_lines: 15
    - path: "scripts/verify-rls.ts"
      provides: "Two-account cross-access proof for AUTH-03 (success criterion 3)"
      contains: "CROSS-USER"
      min_lines: 60
  key_links:
    - from: "web/src/pages/Resumes.tsx"
      to: "storage bucket resumes"
      via: "supabase.storage.from('resumes') upload/download/remove"
      pattern: "storage\\.from\\(['\"]resumes"
    - from: "web/src/pages/Resumes.tsx"
      to: "public.resumes table"
      via: "supabase.from('resumes') select/insert/delete under RLS"
      pattern: "from\\(['\"]resumes['\"]\\)"
    - from: "scripts/verify-rls.ts"
      to: "supabase/migrations/0002_resumes.sql"
      via: "live probes against the four per-operation policies incl. re-parent attempt"
      pattern: "user_id"
---

<objective>
First real feature slice on the skeleton: the Resumes page becomes a working upload/list/download/delete surface backed by the private `resumes` bucket and RLS-scoped `resumes` table (per-item hard delete per D-09/D-10 with a single confirm per D-11). Then prove AUTH-03 the way the roadmap demands — an executable two-account cross-access script hitting the API directly, not a UI spot-check.

Purpose: This is the phase's real DB read/write + real UI interaction (walking-skeleton checklist), and it turns the RLS template from SQL text into verified isolation.
Output: Working Resumes page; `scripts/verify-rls.ts` passing with zero cross-user leaks.
</objective>

## Phase Goal

**As an** invited user, **I want to** securely log in to the deployed app, **so that** every row of my data stays isolated and under my own control.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-access/01-CONTEXT.md
@.planning/phases/01-foundation-access/01-RESEARCH.md
@.planning/phases/01-foundation-access/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

This plan's share of the Phase 1 artifact list (full list in 01-01-PLAN.md):

- **Files:** `web/src/lib/resumes.ts`, `web/src/components/ConfirmDialog.tsx`, `scripts/verify-rls.ts`; rewrites stub `web/src/pages/Resumes.tsx`
- **Exported symbols:** `uploadResume(file)`, `listResumes()`, `downloadResume(storagePath)`, `deleteResume({ id, storagePath })` (`web/src/lib/resumes.ts`); `ConfirmDialog` component (props: `title`, `message`, `confirmLabel`, `onConfirm`, `onCancel`); react-query key `['resumes']`
- **No new DB objects** — consumes plan 01's `public.resumes`, bucket `resumes`, and their policies

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resumes vertical slice — upload, list, download, per-item hard delete</name>
  <files>web/src/pages/Resumes.tsx, web/src/components/ConfirmDialog.tsx, web/src/lib/resumes.ts</files>
  <read_first>
    - web/src/pages/Resumes.tsx (current coming-soon stub being replaced)
    - web/src/lib/supabase.ts (shared client to import)
    - web/src/components/Shell.tsx (styling conventions from plan 01 — neutral, dense, D-15)
    - .planning/phases/01-foundation-access/01-RESEARCH.md (Pattern 4 storage paths, Pattern 5 storage-first delete, Pitfall 3 silent remove())
  </read_first>
  <behavior>
    - Upload of a .docx file as the signed-in user creates one storage object at `{user.id}/{uuid}.docx` and one resumes row (filename = original name, storage_path = object path, size_bytes = file.size); the list refreshes to show it
    - Delete of a listed resume, after confirming the dialog, removes the storage object FIRST, verifies the remove() response actually lists the removed path, then deletes the row; the item disappears from the list
    - If remove() resolves with an empty array (policy denial masquerading as success — RESEARCH Pitfall 3 / A1), the delete surfaces an error and the row is NOT deleted
    - Upload of a file with a disallowed extension is rejected client-side before any network call (server-side bucket MIME allowlist is the real gate)
  </behavior>
  <action>
    Replace the Resumes stub with the real page (per D-09 granular delete, D-10 hard delete, D-11 single confirm).

    `web/src/lib/resumes.ts` — data helpers all UI goes through:
    - `uploadResume(file: File)`: client-side accept only `.docx`/`.pdf`; storage path `${user.id}/${crypto.randomUUID()}.${ext}` — the original filename is NEVER used as a storage path (ASVS V5; it is stored only as the `filename` column). `supabase.storage.from('resumes').upload(path, file, { contentType })`, then insert `{ filename, storage_path, size_bytes }` into `resumes` (RLS default fills `user_id`). If the row insert fails after a successful upload, remove the uploaded object before rethrowing so no orphan file survives (Pitfall 3 inverse).
    - `listResumes()`: select own rows ordered `created_at desc`. Do not add a `.eq('user_id', ...)` filter — RLS scopes it; app-level filters are UX aids, never the boundary (RESEARCH anti-pattern).
    - `downloadResume(storagePath)`: `storage.download(storagePath)` → object URL (respects SELECT policy; no signed URLs needed in Phase 1).
    - `deleteResume({ id, storagePath })` — storage FIRST, then row (RESEARCH Pattern 5): call `remove([storagePath])`, then assert the returned data array contains exactly that path; throw "storage delete incomplete" if not (empty-array silent failure, A1); only then `delete from resumes where id = ...`.

    `web/src/components/ConfirmDialog.tsx`: small accessible dialog (title, message, confirm/cancel buttons), promise- or callback-based; used for every per-item delete (D-11 — single confirm, no type-to-confirm at item level).

    `web/src/pages/Resumes.tsx`: react-query `useQuery({ queryKey: ['resumes'] })` over `listResumes`; mutations for upload/delete invalidating `['resumes']`. Dense table (D-15): filename, human-readable size, upload date, download + delete actions. File input labeled for DOCX/PDF. Empty state: one quiet sentence inviting the first upload. Errors render inline (upload rejected by bucket MIME/size limits, delete failures).
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
    <human-check>
      <test>Log in as user 1 at the dev server, upload a small .docx, download it, then delete it via the confirm dialog</test>
      <expected>File appears in the table after upload; download returns the same file; after delete it is gone from the table and from Supabase Dashboard → Storage → resumes/{user1-id}/</expected>
      <why_human>Real file round-trip through browser file pickers and blob downloads — not scriptable from the executor sandbox</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0
    - `grep -c "crypto.randomUUID" web/src/lib/resumes.ts` ≥ 1 (UUID storage paths, never user filenames)
    - `grep -n "remove(" web/src/lib/resumes.ts` appears at an earlier line than the row-delete call inside `deleteResume` (storage-first ordering)
    - `web/src/lib/resumes.ts` contains an explicit length/content check on the `remove()` response before the row delete
    - `grep -c "ConfirmDialog" web/src/pages/Resumes.tsx` ≥ 1
    - `grep -rn "eq('user_id'" web/src/lib/resumes.ts | wc -l` equals 0 (RLS is the boundary, no app-level owner filter in list)
  </acceptance_criteria>
  <done>Resumes page is a working end-to-end slice: real storage writes, real RLS-scoped table reads/writes, hard per-item delete with confirm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-account RLS cross-access proof (AUTH-03, roadmap success criterion 3)</name>
  <files>scripts/verify-rls.ts</files>
  <read_first>
    - .planning/phases/01-foundation-access/01-RESEARCH.md (Code Example 5 probe structure, Pitfall 4 RLS gap variants, Security Domain threat patterns)
    - scripts/verify-auth.ts (env loading + client construction conventions from plan 01)
    - supabase/migrations/0002_resumes.sql and 0003_storage.sql (exact policy names/predicates being probed)
  </read_first>
  <behavior>
    - With user A's probe row inserted, user B's unfiltered `select * from resumes` returns 0 rows
    - User B's targeted select/update/delete by A's row id each affect 0 rows
    - User A's attempt to `update resumes set user_id = <B's uid>` on their own row fails or affects 0 rows (WITH CHECK re-parent guard, T-01-04)
    - User B cannot download, list, or upload into `resumes/{A-uid}/...` storage paths
    - Script exits 0 only when every probe passes; any leak prints a line containing "CROSS-USER" and exits non-zero
  </behavior>
  <action>
    Write and run `scripts/verify-rls.ts` per RESEARCH Code Example 5, extended to storage. Two independent clients built with the PUBLISHABLE key only (`persistSession: false`); sign in A = USER1_EMAIL/SEED_PASSWORD_1, B = USER2_EMAIL/SEED_PASSWORD_2 from `scripts/.env`.

    Probes (each an assert with a printed PASS/FAIL line; failures tagged "CROSS-USER LEAK"):
    1. A inserts a probe row (`filename` 'rls-probe.docx', `storage_path` `${uidA}/rls-probe-${Date.now()}`) and A uploads a tiny probe blob to their own folder.
    2. B `select *` on resumes → 0 rows (Pitfall 4 variant 1: enabled-but-leaky).
    3. B targeted `select`/`update`/`delete` using A's probe row id → 0 rows affected each.
    4. A `update ... set user_id = uidB` on the probe row → error or 0 rows (WITH CHECK, Pitfall 4 variant 3).
    5. B `storage.download(A's probe path)` → error; B `storage.from('resumes').list(uidA)` → empty; B upload into `${uidA}/intruder.docx` → error (storage policies from 0003).
    6. Cleanup as A: remove probe object (assert removed array non-empty) + delete probe row.

    Run with `node --env-file=scripts/.env scripts/verify-rls.ts`. Never test isolation via the SQL editor or the secret key — service role bypasses RLS (RESEARCH Pitfall 4 warning).
  </action>
  <verify>
    <automated>node --env-file=scripts/.env scripts/verify-rls.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --env-file=scripts/.env scripts/verify-rls.ts` exits 0 with a printed PASS line for every probe listed in the action
    - Script contains zero references to the secret key env var name; only the publishable key is used (`grep -c "SUPABASE_SECRET_KEY" scripts/verify-rls.ts` equals 0)
    - Re-parent probe (update of user_id to the other uid) is present and passes
    - Storage probes cover download, list, and upload against the other user's folder
  </acceptance_criteria>
  <done>AUTH-03 proven with both accounts at the API layer: reads, writes, deletes, re-parenting, and storage access are all isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (user JWT) → PostgREST | All table access crosses RLS here |
| Browser (user JWT) → Storage API | All file access crosses storage.objects policies here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information disclosure | IDOR via PostgREST (targeted by-id reads) | critical | mitigate | Per-operation RLS policies from 0002; verify-rls probes targeted select/update/delete by the other user's row id |
| T-01-04 | Tampering | Row re-parenting via UPDATE | high | mitigate | WITH CHECK on UPDATE policy; live re-parent probe in verify-rls |
| T-01-05 | Tampering/DoS | Malicious/oversized upload | medium | mitigate | Server-side bucket config: 5 MB `file_size_limit` + DOCX/PDF MIME allowlist (0003); client validation is UX only |
| T-01-11 | Information disclosure | Cross-user storage access (download/list/upload) | critical | mitigate | Per-user-folder `storage.foldername` policies; all three probed as user B in verify-rls |
| T-01-12 | Repudiation | Silent failed delete (remove() empty array) | medium | mitigate | deleteResume asserts removed-path presence before row delete; probe cleanup asserts non-empty removed array |
</threat_model>

<verification>
- `cd web && npm run build` green
- `scripts/verify-rls.ts` exits 0 — zero cross-user leaks across table and storage probes
- Human check (end-of-phase batch): resume upload/download/delete round trip as user 1
</verification>

<success_criteria>
- AUTH-03: verified with both accounts via executable script — no read/modify/delete/re-parent path across users on rows or files
- AUTH-04 (granular half): per-item resume delete removes DB row AND storage object, with D-11 confirm
- Walking-skeleton checklist: real DB read+write and real UI interaction now exist
</success_criteria>

<output>
Create `.planning/phases/01-foundation-access/01-02-SUMMARY.md` when done
</output>
