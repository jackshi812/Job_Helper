---
phase: 03.3-dashboard-filter-refinements
plan: 04
type: execute
wave: 2
depends_on: [03.3-01, 03.3-02, 03.3-03]
files_modified:
  - scripts/verify-phase-03-3-release.mjs
  - scripts/verify-phase-03-3-release.test.mjs
  - .planning/phases/03.3-dashboard-filter-refinements/03.3-04-PREFLIGHT.md
  - .planning/phases/03.3-dashboard-filter-refinements/03.3-04-ROLLOUT-EVIDENCE.md
  - .planning/phases/03.3-dashboard-filter-refinements/03.3-UAT.md
autonomous: false
requirements: [PREF-01, SCOR-01, SCOR-03, SCOR-04]
must_haves:
  truths:
    - "D-01 through D-07 are locally green before migration 0031 is applied, and production receives additive schema compatibility before score-tick can emit the new reason."
    - "D-08 through D-12 are released only from the exact reviewed source revision after the compatible worker is live."
    - "The production rollout order is migration 0031, score-tick with its existing cron-secret boundary, exact web release, then signed-in UAT."
    - "No destructive schema reset/push, legacy cleanup, provider expansion, package installation, unbounded all-user requeue, manual tick/verifier, fixture, or separately manufactured paid proof occurs; the owner explicitly approves the numeric ceiling for ordinary scheduled effects from exactly two UAT preference saves."
    - "Production acceptance is fail-closed on remote migration parity/order, approved filter-v4 Edge identity, exact origin/main and Cloudflare commit/environment/status metadata, and a local-versus-immutable web asset SHA-256 match."
  artifacts:
    - path: ".planning/phases/03.3-dashboard-filter-refinements/03.3-04-ROLLOUT-EVIDENCE.md"
      provides: "Approval inventory, schema/function/web ordering, exact identities, immutable hash, and mutation ledger"
    - path: ".planning/phases/03.3-dashboard-filter-refinements/03.3-04-PREFLIGHT.md"
      provides: "Committed read-only inventory and bounded UAT-effect baseline; the approvable release SHA is derived after this file is committed"
    - path: ".planning/phases/03.3-dashboard-filter-refinements/03.3-UAT.md"
      provides: "Signed-in preference, company, tier, accessibility, and preserved-table acceptance results"
    - path: "scripts/verify-phase-03-3-release.mjs"
      provides: "Read-only fail-closed assertions over migration, function, Git, Cloudflare, immutable asset, and bounded UAT-effect evidence"
    - path: "scripts/verify-phase-03-3-release.test.mjs"
      provides: "Malformed, missing, mismatched, duplicate, and over-budget release-evidence rejection coverage"
  key_links:
    - from: "supabase/migrations/0031_dashboard_filter_refinements.sql"
      to: "supabase/functions/score-tick/index.ts"
      via: "remote 0031 parity precedes filter-v4 Edge deployment"
      pattern: "0031|filter-v4"
    - from: "supabase/functions/score-tick/index.ts"
      to: "web/src/pages/Preferences.tsx"
      via: "worker is live before the web can save/use title_exclude_keywords"
      pattern: "title_exclude_keywords"
    - from: "release commit"
      to: "Cloudflare production asset"
      via: "GitHub check/deployment metadata and matching immutable JavaScript SHA-256"
      pattern: "commit|deployment|sha256"
    - from: "two approved owner preference saves"
      to: "ordinary scheduled score-tick effects"
      via: "pre/post revision, request-budget, per-user ai_usage, convergence, and preference-snapshot hash counters"
      pattern: "uat_paid_reservation_ceiling|preferences_restored_exactly|needs_refilter"
---

<objective>
Prove the complete Phase 03.3 source, obtain owner approval, apply the mandatory additive schema push, deploy the updated worker and exact web release in safe order, and finish signed-in UAT.

Purpose: Prevent false-green local completion and rolling-release breakage while giving the owner a precise outward-mutation boundary.
Output: Green full release candidate, approved migration/function/web rollout, immutable production evidence, and completed Phase 03.3 UAT.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-CONTEXT.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-RESEARCH.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-PATTERNS.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-UI-SPEC.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-01-SUMMARY.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-02-SUMMARY.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-03-SUMMARY.md
@.planning/phases/03.2-dashboard-precision-company-visibility/03.2-03-SUMMARY.md
@.planning/phases/03.2-dashboard-precision-company-visibility/03.2-04-SUMMARY.md
@supabase/migrations/0031_dashboard_filter_refinements.sql
@supabase/functions/score-tick/index.ts
@scripts/verify-scoring.ts

<interfaces>
- Pinned local CLI: `web/node_modules/.bin/supabase`, executed from repository root or the established equivalent `cd web &amp;&amp; npx supabase` resolving the locked devDependency 2.109.1.
- Hosted project reference: `fjcsvajkkztvlrpdplwx`; `score-tick` deploy retains `--no-verify-jwt` because the function's existing `x-cron-secret` guard is authoritative.
- Mandatory deployment order: remote migration 0031 parity → score-tick deployment → exact committed web release → production UAT.
- The schema action is additive `db push --linked`; `db reset`, destructive repair, manual dashboard SQL, and broad migration application are prohibited.
</interfaces>
</context>

<scope_fence>
Approval covers only migration 0031, deployment of score-tick, push of the exact reviewed release commit to origin/main, the resulting Cloudflare production build, read-only identity checks, and signed-in UAT with exactly two owner preference saves: persist empty once and restore the exact pre-UAT array once. Those saves reuse D-07's ordinary scheduler and may cause paid scoring up to the explicit numeric `uat_paid_reservation_ceiling` approved at Task 2; every reservation/usage/revision/restoration counter must be recorded. Approval does not cover a database reset, legacy-column/reason removal, data rewrite, all-user requeue, manual score-tick invocation, scoring verifier run, fixture creation, direct OpenAI invocation, effects beyond the approved ceiling, provider/polling change, secret change, scheduler pause, budget-ledger mutation, or Workday expansion.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble and prove the exact release candidate</name>
  <read_first>
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-01-SUMMARY.md
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-02-SUMMARY.md
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-03-SUMMARY.md
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-UI-SPEC.md
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-RESEARCH.md
  </read_first>
  <files>scripts/verify-phase-03-3-release.mjs, scripts/verify-phase-03-3-release.test.mjs, .planning/phases/03.3-dashboard-filter-refinements/03.3-04-PREFLIGHT.md</files>
  <behavior>
    - Missing, duplicate, malformed, stale, out-of-order, or unequal migration/function/Git evidence exits nonzero.
    - A Cloudflare deployment outside production, not successful, off main, or bound to another SHA exits nonzero; a local/live immutable JavaScript hash mismatch exits nonzero.
    - Any save count other than two, changed preference snapshot hash, unconverged affected row, concurrent other-user usage, or reservation/owner-usage delta above approved `B` exits nonzero.
    - Command, API, credential, parse, or file errors propagate as failures and are never replaced by passing defaults.
  </behavior>
  <action>Run the focused and complete test/build/lint/diff gates. Confirm migration 0031 is the sole intended pending migration, record its SHA-256, confirm only score-tick needs deployment, and exclude package changes, active experience-cap usage, Dashboard-filter persistence, provider changes, and unrelated workspace files. Create `verify-phase-03-3-release.mjs` and its Node tests with standard-library APIs and fail-closed `--identity` plus `--post-release` modes. They must reject missing, malformed, duplicate, stale, out-of-order, mismatched, or command/API-error evidence and verify migration order/parity, filter-v4 function identity, approved local/origin SHA, successful main-branch Cloudflare production metadata, immutable local/live asset SHA-256 equality, and the completed UAT mutation ledger. Create `03.3-04-PREFLIGHT.md` with local gates, build/migration hashes, linked project, remote migration/function pre-state, intended commands, zero pre-approval effects, and the narrow mutation inventory, but intentionally omit an approved release SHA because committing Task 1 changes HEAD. Read the signed-in owner's open-row count `N`, current reservations `R`, proven deployed daily cap `C`, owner/other usage, revision/refilter counters, and only a SHA-256 of the pre-UAT title-exclusion array. Compute `B = min(10 × N, max(0, C - R))`; if any counter or `C` is unavailable, Task 2 cannot approve. Do not mutate production in this task.</action>
  <acceptance_criteria>
    - All focused and full tests pass; production build and lint exit successfully with no new warning class.
    - Migration inspection shows 0031 as the sole Phase 03.3 schema delta after remote 0030.
    - The reviewed release set excludes `.DS_Store`, `scripts/agent-dashboard.mjs`, `scripts/agent-dashboard.test.mjs`, and every unrelated user change.
    - The committed preflight identifies build/migration hashes and enumerates every authorized and explicitly unauthorized outward effect without claiming a pre-commit release SHA.
    - The fail-closed verifier tests reject wrong migration order/parity, stale function identity, origin SHA drift, non-production/failed/wrong-SHA Cloudflare metadata, live/local asset mismatch, missing counters, restoration mismatch, and any effect above the approved ceiling.
    - The preflight records concrete `N`, `R`, `C`, and computed `B`, zero preference saves before approval, baseline owner/other score-usage and revision/refilter counters, and only a hash of the preference snapshot.
    - No schema, function, Git remote, Cloudflare, job row, preference row, or paid service is mutated before approval.
  </acceptance_criteria>
  <verify>
    <automated>git diff --check &amp;&amp; node --test scripts/verify-phase-03-3-release.test.mjs &amp;&amp; cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <done>A reproducible candidate and committed bounded preflight exist with a green local gate and zero outward mutations; the exact approvable SHA is derived from live HEAD after this task's commit.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: [BLOCKING] Approve or decline the exact schema, worker, and web rollout</name>
  <read_first>
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-04-PREFLIGHT.md
    - supabase/migrations/0031_dashboard_filter_refinements.sql
    - supabase/functions/score-tick/index.ts
    - .planning/phases/03.2-dashboard-precision-company-visibility/03.2-03-SUMMARY.md
  </read_first>
  <files>None</files>
  <decision>Whether to authorize migration 0031, deployment of only score-tick, the exact reviewed origin/main release, its resulting Cloudflare production deployment, and signed-in owner UAT.</decision>
  <context>Task 1 has produced and committed a locally green preflight containing the migration checksum, test/build/lint results, linked migration/function state, intended commands, durable effects, prohibited actions, `N`, `R`, `C`, baseline usage/revision/refilter counters, preference snapshot hash, and `B`. The exact approvable release SHA is the live post-Task-1 `git rev-parse HEAD`, not a SHA recorded before that commit. Approval includes the bounded existing refilter lifecycle and does not authorize destructive schema work, legacy cleanup, global requeue, manual score-tick/verifier execution, fixtures, provider changes, scheduler/budget manipulation, or a separate paid proof.</context>
  <action>Require a clean release-relevant tree after Task 1's commit and derive the exact candidate using live `git rev-parse HEAD`. Present that 40-character SHA, the committed preflight, the ordered schema → score-tick → exact origin/main/Cloudflare commands, retained legacy compatibility, excluded unrelated files, exactly two owner preference saves, `N`, `R`, `C`, `B`, potential ordinary-scheduler OpenAI effects, counters, and restoration proof. Bind the literal owner decision to the displayed live SHA. Stop without writing rollout evidence or performing any remote/schema/function/Git/preference/paid mutation.</action>
  <options>
    <option id="approve">
      <name>Approve exact rollout</name>
      <pros>Task 3 may perform only the recorded schema → worker → exact web release and two signed-in saves with measured ordinary refilter convergence.</pros>
      <cons>Production gains the seeded own-row array and filter-v4 behavior; scheduled processing after the two saves may reserve up to the displayed numeric ceiling `B` and may make corresponding OpenAI calls.</cons>
    </option>
    <option id="decline">
      <name>Decline production mutation</name>
      <pros>Production remains unchanged while all locally verified work is retained.</pros>
      <cons>Phase 03.3 cannot claim deployed behavior or production UAT.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The owner sees the exact commit, checksum, local gates, outward commands/effects, compatibility posture, and exclusions before deciding.
    - Approval is explicit and limited to the named ordered release plus ordinary signed-in UAT; silence or ambiguous assent is not approval.
    - The literal approval covers exactly two owner preference saves and the displayed numeric `B`; it does not cover an unspecified or zero-effect claim.
    - No production or remote mutation occurs in this checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>git diff --check &amp;&amp; test -s .planning/phases/03.3-dashboard-filter-refinements/03.3-04-PREFLIGHT.md</automated>
    <human-check>Choose approve or decline after reviewing the exact migration, score-tick, origin/main, Cloudflare, two-save refilter, numeric paid-effect ceiling, counters, restoration proof, and UAT effects.</human-check>
  </verify>
  <resume-signal>Type `approve rollout` to authorize only the exact recorded scope, or `decline`.</resume-signal>
  <done>The owner explicitly approves the exact rollout/UAT scope, or declines and production remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply schema → score-tick → exact web release, then complete signed-in UAT</name>
  <read_first>
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-UI-SPEC.md
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-04-PREFLIGHT.md
    - .planning/phases/03.2-dashboard-precision-company-visibility/03.2-UAT.md
    - supabase/migrations/0031_dashboard_filter_refinements.sql
    - supabase/functions/score-tick/index.ts
  </read_first>
  <files>.planning/phases/03.3-dashboard-filter-refinements/03.3-04-ROLLOUT-EVIDENCE.md, .planning/phases/03.3-dashboard-filter-refinements/03.3-UAT.md</files>
  <action>Abort unless Task 2 records literal `approve rollout` bound to the displayed live post-Task-1 SHA and `B`, current `git rev-parse HEAD` still equals that approved SHA, the release-relevant tree is clean, and migration checksum, remote pre-state, `N`, `R`, `C`, usage/revision/refilter baselines, and preference snapshot hash still match the committed preflight. Re-run the full local gate and exclude unrelated user files. Apply migration 0031 with linked `db push`, deploy only score-tick with the existing cron-secret posture, push only the approved SHA to origin/main, and wait for that SHA's Cloudflare production deployment. After those identities exist, create `03.3-04-ROLLOUT-EVIDENCE.md` and record the approved SHA, migration/function/Git/Cloudflare identities, immutable asset hash, and bounded mutation ledger; this is the first point at which the evidence file may claim the approved SHA. Run fail-closed `--identity` verification before UAT. Then perform exactly two owner preference saves (empty, then exact restoration), observe convergence, record all required reservation/usage/revision/refilter counters, enforce `B`, require unchanged other-user usage and exact snapshot-hash restoration, and never replay on mismatch. Complete the specified signed-in preference, company, score-tier, accessibility, table/detail/resizing, contrast, and responsive checks without manufacturing job data. Finally run `--post-release` verification over the completed evidence and UAT ledger. Every auth, metadata, identity, hash, convergence, restoration, or effect-bound mismatch stops acceptance.</action>
  <acceptance_criteria>
    - Remote 0031 parity precedes the new score-tick identity, which precedes the exact Cloudflare asset and signed-in UAT.
    - The automated post-release verifier proves local/remote migration parity with 0031 exactly once after 0030, the approved filter-v4 function identity, origin/main at the approved SHA, Cloudflare production success at that SHA, and byte-identical local/immutable JavaScript.
    - The deployed function retains the cron-secret boundary; no destructive schema action, legacy cleanup, global requeue, manual tick/verifier, fixture, direct paid call, scheduler/budget manipulation, provider change, or unrelated-file push occurs.
    - Exactly two owner preference saves occur; ordinary scheduled reservations and owner score usage stay within approved `B`, other-user usage remains unchanged during measurement, all affected rows converge after restoration, and the preference snapshot hash is restored exactly.
    - D-01 through D-07 preference/filter behavior is accepted on the exact release without explicit-empty resurrection and with the pre-UAT preference array restored.
    - D-08/D-09 full-list staged company behavior and zero state are accepted.
    - D-10/D-11 dropdown semantics, keyboard focus, immediate updates, and session reset are accepted.
    - D-12 Phase 03.2 table/feed regressions remain accepted, and unavailable natural-data cases are identified as automation-proven rather than falsely observed.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; cd .. &amp;&amp; node --test scripts/verify-phase-03-3-release.test.mjs &amp;&amp; node scripts/verify-phase-03-3-release.mjs --post-release .planning/phases/03.3-dashboard-filter-refinements/03.3-04-ROLLOUT-EVIDENCE.md</automated>
    <human-check>Against the release-specific Cloudflare URL, verify save-empty/refilter/restore; staged full-list company actions and zero state; tier All/None/partial keyboard/focus/AND behavior; and preserved table/detail/resizing/responsive behavior.</human-check>
  </verify>
  <done>Production has the compatible schema, filter-v4 worker, and exact reviewed web asset in order; `03.3-UAT.md` binds honest signed-in acceptance to that immutable release; and all four phase requirements have automated plus proportional human evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local migration/CLI → hosted PostgreSQL | Approved credentials can mutate the production schema and must be constrained to one additive migration. |
| Local Edge source → hosted score-tick | A privileged scheduled worker is replaced while retaining its handler-level shared-secret boundary. |
| Git main → Cloudflare production | The connected build publishes user-facing assets and must match reviewed source exactly. |
| Signed-in browser → production preferences/feed | Human UAT changes the owner's own preference row and observes RLS-scoped data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.3-15 | Tampering / Elevation of privilege | production schema push | critical | mitigate | Require blocking owner approval, exact single pending migration, pinned CLI, additive linked db push, and a fail-closed local/remote list assertion proving 0031 occurs exactly once immediately after 0030; prohibit reset/manual SQL. |
| T-03.3-16 | Spoofing | score-tick deployment identity | high | mitigate | Deploy only the named function/project with `--no-verify-jwt`; require the new approved hosted identity/version plus local `filter-v4` source and preserved in-function cron-secret check without invoking the function. |
| T-03.3-17 | Tampering / Repudiation | Git/Cloudflare release | high | mitigate | Push only reviewed files; fail closed unless local HEAD, origin/main, Cloudflare production success/branch/commit metadata, immutable URL, and local/live JavaScript SHA-256 all bind to the approved release. |
| T-03.3-18 | Information disclosure | rollout evidence | medium | mitigate | Record IDs/hashes/statuses only; never persist access tokens, database passwords, cron secrets, preference values, job descriptions, resumes, prompts, or responses. |
| T-03.3-19 | Denial of service / financial abuse | refilter/scoring verification | high | mitigate | Explicitly approve exactly two owner saves and numeric `B = min(10 × N, max(0, C - R))`; record pre/after global reservations, owner/other usage, revisions, convergence, and preference hashes; fail on drift/breach without replay; prohibit global requeue, manual tick/verifier, fixtures, direct OpenAI, scheduler pause, or budget mutation. |
| T-03.3-SC | Tampering | package supply chain | high | mitigate | Install no packages; use the pinned existing Supabase CLI and locked web dependencies only. |
</threat_model>

## Artifacts this phase produces

- `.planning/phases/03.3-dashboard-filter-refinements/03.3-04-ROLLOUT-EVIDENCE.md`: new exact release and mutation-evidence artifact.
- `.planning/phases/03.3-dashboard-filter-refinements/03.3-04-PREFLIGHT.md`: committed read-only inventory and bounded UAT-effect baseline that intentionally omits the pre-commit release SHA.
- `.planning/phases/03.3-dashboard-filter-refinements/03.3-UAT.md`: new signed-in acceptance ledger.
- `scripts/verify-phase-03-3-release.mjs`: fail-closed read-only production identity/hash/effect verifier modeled on the existing scoring-evidence validator.
- `scripts/verify-phase-03-3-release.test.mjs`: negative-path coverage for every critical release and UAT ledger assertion.
- Hosted migration identity for `0031_dashboard_filter_refinements.sql` in project `fjcsvajkkztvlrpdplwx`.
- Hosted score-tick filter-v4 deployment identity with existing cron-secret posture.
- Exact origin/main release SHA, Cloudflare production deployment ID/URL, JavaScript asset path, and matching local/live SHA-256.
- Exact two-save ordinary-refilter authorization with numeric ceiling and before/after reservation, owner/other usage, revision, convergence, and preference-hash restoration evidence.
- No new runtime symbol or package is introduced by this release plan; it publishes the symbols enumerated in Plans 01-03.

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Replace experience cap with title exclusions, add staged company bulk actions, and consolidate score tiers accessibly | 01-04 | COVERED | Backend, preference UI, Dashboard UI, ordered rollout, and UAT are all explicit. |
| REQ | PREF-01 | User-set target/location/include/exclude preferences | 02, 04 | COVERED | Dedicated title exclusions preserve the existing preference/save/refilter lifecycle. |
| REQ | SCOR-01 | Provider-neutral cheap filters before AI | 01, 04 | COVERED | Literal title-only gate and pre-AI worker order are tested and deployed schema-first. |
| REQ | SCOR-03 | Plain-language match reasons | 01, 03, 04 | COVERED | Scored detail reasons remain; new filtered reason is bounded/read-compatible. |
| REQ | SCOR-04 | Dashboard score/feed/detail/apply surface | 03, 04 | COVERED | Controls change while counts, table, Location, detail reasons, sorting, links, resizing, and dismissal are preserved. |
| RESEARCH | additive compatibility | Seed array; retain dormant physical cap and legacy reason during rollout | 01, 04 | COVERED | Structural tests plus schema→worker→web rollout; destructive cleanup excluded. |
| RESEARCH | literal title tokenizer | Whole-token phrases with one narrow PhD fold, no user regex/fuzzy matching | 01 | COVERED | Table-driven positive/negative/parenthetical/title-only matrix. |
| RESEARCH | semantic freshness | Canonical new preference hash and filter-v4; pre-AI order/CAS preserved | 01 | COVERED | Worker/hash/source-order assertions and refilter integration. |
| RESEARCH | no-row vs stored empty | Browser defaults only when preference row is absent | 02 | COVERED | Service and page tests pin both states. |
| RESEARCH | hidden-key bulk semantics | Full current list, stale-key-safe disabled states, new companies selected | 03 | COVERED | Pure immutable helpers and UI composition. |
| RESEARCH | native checkbox popover | One summary trigger, native group, focus/outside lifecycle, no persistence | 03, 04 | COVERED | Automated semantics plus exact-release human interaction UAT. |
| RESEARCH | verification/release | Focused/full gates and owner-approved schema→worker→web release | 04 | COVERED | Blocking checkpoint, fail-closed live identity/hash assertions, bounded ordinary-refilter ledger, and signed-in UAT. |
| CONTEXT | D-01 | Replace active experience cap with dedicated title exclusions | 01, 02 | COVERED | Active schema/filter/hash/worker/browser contract replaced. |
| CONTEXT | D-02 | Seed editable president and PhD for existing/new users | 01, 02 | COVERED | Database default plus no-row browser default. |
| CONTEXT | D-03 | Case-insensitive whole-word/phrase boundaries | 01 | COVERED | Literal token matcher and matrix. |
| CONTEXT | D-04 | Narrow PhD punctuation/spacing variants only | 01 | COVERED | Fixed fold with doctorate/doctoral negatives. |
| CONTEXT | D-05 | president anywhere as a whole word | 01 | COVERED | VP/SVP positives and presidential negative. |
| CONTEXT | D-06 | Empty list valid and excludes nothing | 01, 02, 04 | COVERED | Schema, filter, save/refresh, and UAT. |
| CONTEXT | D-07 | Existing refilter lifecycle and safe bounded reason transition | 01, 02, 04 | COVERED | Additive compatibility, filter-v4, exact payload/detail bounds, two-save scheduled convergence ledger, snapshot restore, and ordered rollout. |
| CONTEXT | D-08 | Clear/Select all affect full draft and apply only via Show results | 03, 04 | COVERED | Pure helpers, UI staging, signed-in UAT. |
| CONTEXT | D-09 | Zero companies, truthful disabled states, new-company default | 03, 04 | COVERED | Stale-key tests, approved empty copy, UAT. |
| CONTEXT | D-10 | One accessible score-tier multi-select | 03, 04 | COVERED | Native checkbox group and interaction UAT. |
| CONTEXT | D-11 | All default; zero-to-three; session-only; AND semantics | 03, 04 | COVERED | Existing set model preserved and fully tested. |
| CONTEXT | D-12 | Preserve thresholds, Weak ownership, count/dismiss/sort/table | 03, 04 | COVERED | Regression suite and signed-in acceptance. |
| CONTEXT deferred | Workday pilot | Provider expansion | — | EXCLUDED | Explicitly outside Phase 03.3. |
</source_audit>

<verification>
1. Local focused and complete test/build/lint gates pass before approval and again before UAT acceptance.
2. The release verifier parses the linked migration state and fails unless local/remote parity holds through 0031 with 0031 exactly once immediately after 0030.
3. The verifier fails unless the hosted score-tick identity is the newly approved filter-v4 deployment and its source authorization posture remains exact.
4. Direct origin and Cloudflare metadata assertions plus immutable local/live asset hashing bind production to the reviewed source SHA; command/API errors cannot default to success.
5. Signed-in UAT performs exactly two approved preference saves, measures ordinary scheduler reservations and per-user score usage within `B`, proves convergence and exact restoration, and exercises company, tier, accessibility, and Phase 03.2 regressions without manual ticks, hosted verifier runs, or fixtures.
</verification>

<success_criteria>
- All four requirement IDs and D-01 through D-12 have implementation, automated verification, and release evidence.
- The schema push is complete, additive, exact, and precedes every consumer deployment.
- Production score-tick uses filter-v4 before the exact new web asset is published.
- The exact release passes signed-in preference, company, tier, accessibility, and preserved-table checks.
- D-07's ordinary lifecycle converges after both saves; preference state is restored exactly and measured scheduled paid effects remain within the owner-approved numeric ceiling.
- Rollout evidence contains no secret or user-content leakage, accounts for authorized scheduled effects, and records zero unauthorized effects.
- The deferred Workday provider expansion remains untouched.
</success_criteria>

<output>
Create `.planning/phases/03.3-dashboard-filter-refinements/03.3-04-SUMMARY.md` when done.
</output>
