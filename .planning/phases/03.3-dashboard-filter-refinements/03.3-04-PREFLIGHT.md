# Phase 03.3 Plan 04 Release Preflight

Captured read-only at `2026-07-23T02:00:42Z` for hosted project `fjcsvajkkztvlrpdplwx`.

This artifact intentionally does **not** record or approve a release Git SHA. Committing this preflight changes `HEAD`; Task 2 must derive and display the live 40-character post-commit `git rev-parse HEAD`, confirm the release-relevant tree is clean, and bind any literal `approve rollout` response to that value.

## Local release gates

| Gate | Result |
|---|---|
| Release-verifier Node tests | PASS — 13/13 |
| Focused Phase 03.3 web tests | PASS — 132/132 across 8 files |
| Complete web regression suite | PASS — 553/553 across 38 files |
| Production build | PASS — Vite 8.1.1, 191 modules |
| Lint | PASS — only the pre-existing `AuthProvider.tsx` Fast Refresh warning |
| `git diff --check` | PASS |

The production build emitted the existing chunk-size advisory and no new warning class. No package was installed or changed.

### Candidate artifact hashes

| Artifact | SHA-256 |
|---|---|
| `supabase/migrations/0031_dashboard_filter_refinements.sql` | `adc0d625ea48926ebc2fc5b601b66c4dfae259399331b7f05dd8976fb124616e` |
| `web/dist/assets/index-COk208cg.js` | `005f10ba5f6f103feb7a93a1ec3589eb74791ee78be3c0d57b907e39acac7cfc` |
| `web/dist/assets/index-B3sZdtG6.css` | `b35f1de6cd1b3fa7c4863d4ea38fb3b47d644f8cb24122bcf6008d2c9a45e0ec` |
| `web/dist/index.html` | `a1504931625d061693bd0b42d89281938a2315ee822d46c40a604d9f837bff2a` |

## Read-only hosted pre-state

### Schema

- Pinned CLI: `web/node_modules/.bin/supabase` version `2.109.1`.
- Linked migration inventory has exact local/remote parity from `0001` through `0030`.
- `0031` is the only local migration absent remotely and is the sole Phase 03.3 schema delta.
- The hosted `preferences.title_exclude_keywords` column is absent, as required before the additive rollout.

### Edge function

- Function: `score-tick` only.
- Stable hosted function ID: `ae6c147f-c3a8-417e-8057-d4105ac9aed5`.
- Current hosted version: `8`; status: `ACTIVE`; `verify_jwt`: `false`.
- Current bundle SHA-256 metadata: `d9c91aee5d94cf0bd7bbbb5278c5d0fbac06f5947c71a161af0655ba5dac7630`.
- The candidate source is `filter-v4` and retains its handler-level `x-cron-secret` guard. Shared filter/hash modules are bundled through `score-tick`; no other Edge function needs deployment.

### Git and Cloudflare

- Current remote `origin/main` before approval is `895356f76edd8bbdb34a253460ad5152fbc16310`.
- Current Cloudflare production deployment is `7c804087-c656-499f-a3ec-3ad91a71fac4`, successful at `https://7c804087.job-helper-qs9.pages.dev`, and bound by the prior GitHub check/deployment trigger to that same `main` commit.
- The next Cloudflare production build is connected to a GitHub push on `main`; no direct Cloudflare mutation command is authorized.

## Bounded owner UAT baseline

All values below came from one read-only aggregate SQL capture. No job title, description, resume, prompt, response, preference value, secret, or user email was returned or recorded.

| Symbol / counter | Value | Meaning |
|---|---:|---|
| `N` | 1937 | Owner `user_jobs` rows joined to currently open jobs |
| `R` | 143 | Current UTC-day `score_request_budget.requests_today` reservations |
| `C` | 200 | Proven deployed daily cap |
| `B` | 57 | `min(10 × 1937, max(0, 200 - 143))` |
| Owner score usage today | 143 | Owner `ai_usage` rows with purpose `score` |
| Other-user score usage today | 0 | Non-owner `ai_usage` rows with purpose `score` |
| Global score usage today | 143 | Cross-check equal to the current reservation ledger |
| Owner open rows needing refilter | 0 | Baseline convergence counter |
| Owner open revision sum | 41536 | Baseline mutation counter |
| Owner open maximum revision | 25 | Baseline revision bound |
| Owner recent active claims | 0 | Claims in the previous five minutes |
| Owner preference rows | 1 | Exact own-row cardinality |

`C = 200` is proven by the hosted `AI_DAILY_SCORE_CAP` secret digest `27badc983df1780b60c2b3fa9d3a19a00e46aac798451f0febdca52920faaddf`, which equals SHA-256 of the configured value `200`, and by the current source's post-July-20 configured-cap path. No secret value was fetched from the platform.

Because migration `0031` has not yet run, the hosted title-exclusion column cannot be read before approval. The additive migration deterministically populates the existing owner row. The projected pre-UAT snapshot SHA-256, using the release verifier's JSON-array encoding, is `d38a4165970dc6006b3d5b1e8dfdefabd126becb1470c76f559134051767b21a`. Task 3 must read and hash the live post-migration array before the first save and abort unless it matches this value; only hashes, never the array value, may enter evidence.

### Authorized two-save effect ceiling

After identity verification, signed-in UAT may perform exactly two owner preference saves:

1. Persist the empty title-exclusion list once.
2. Restore the exact pre-UAT array once.

Each save reuses the existing own-user refilter RPC and ordinary scheduler. Across both saves combined, reservations and owner score usage may increase by at most `B = 57`; corresponding ordinary scheduled OpenAI calls may occur up to that ceiling. The release must fail closed if another user's score usage changes during the measurement window, if any counter decreases, if any affected row remains unconverged, if the revision deltas do not equal the recorded affected-row counts, or if the final preference snapshot hash differs from the pre-UAT hash. No replay is authorized after a mismatch.

## Ordered commands after literal approval

These are the only outward release commands authorized by Task 2, in this order, with the displayed post-Task-1 SHA substituted for `$APPROVED_SHA`:

```bash
# 1. Additive schema compatibility only
set -a; source scripts/.env; set +a
web/node_modules/.bin/supabase db push --linked

# 2. Only the compatible worker, retaining its handler-level cron-secret guard
web/node_modules/.bin/supabase functions deploy score-tick \
  --project-ref fjcsvajkkztvlrpdplwx --no-verify-jwt

# 3. Exact reviewed main release; connected Cloudflare production build follows
test "$(git rev-parse HEAD)" = "$APPROVED_SHA"
git push origin "$APPROVED_SHA":main

# 4. Read-only exact-SHA release identity checks
gh api "repos/jackshi812/Job_Helper/commits/$APPROVED_SHA/check-runs"
node --env-file=scripts/.env scripts/verify-phase-03-3-release.mjs \
  --identity .planning/phases/03.3-dashboard-filter-refinements/03.3-04-ROLLOUT-EVIDENCE.md
```

The GitHub-triggered Cloudflare build is the sole Cloudflare mutation. Task 3 must wait for one successful `Cloudflare Pages` check, query that deployment's direct Cloudflare metadata, and prove the immutable JavaScript bytes match the local build before UAT.

## Compatibility posture

- Migration `0031` is additive: it adds a bounded non-null array with a default and widens the filter-reason constraint.
- The physical `max_required_experience` column and historical `experience_above_max` reason remain for rolling and stored-row compatibility.
- Active browser, worker, semantic-hash, and cheap-filter source no longer uses the experience cap.
- `excluded_title_keyword` is added without removing legacy reason readability.
- Schema precedes `filter-v4`; `filter-v4` precedes the web release.
- Dashboard company and score-tier filters remain session-only. The only Dashboard local persistence is the already approved versioned column-width map.
- Provider registry, polling, ingestion, authentication, RLS, scheduler, budget configuration, and package manifests are unchanged.

## Reviewed release set and exclusions

The reviewed product release changes only the Phase 03.3 preference/filter/worker, Dashboard controls, tests, migration, compatible scoring verifier, and release verifier/planning evidence already enumerated in Plans 01–04. The following unrelated workspace files are explicitly excluded and remain unstaged:

- `.DS_Store`
- `scripts/agent-dashboard.mjs`
- `scripts/agent-dashboard.test.mjs`

No package manifest or lockfile changed. No provider adapter, provider registry, polling cadence, deployment configuration, or Dashboard filter-persistence surface changed.

## Zero pre-approval effects and explicit prohibitions

Task 1 performed read-only metadata/counter queries, local tests/build/lint, and local commits only.

| Effect before approval | Count |
|---|---:|
| Schema pushes / resets / repairs / manual SQL | 0 |
| Edge function deployments | 0 |
| Git pushes or Cloudflare deployments | 0 |
| Owner preference saves | 0 |
| Job or preference row mutations | 0 |
| Paid-service / direct OpenAI calls caused by Task 1 | 0 |
| Manual score-tick invocations | 0 |
| Scoring-verifier invocations | 0 |
| Fixture creation | 0 |
| Global requeues | 0 |
| Scheduler, secret, or budget-ledger configuration changes | 0 |

Approval does not authorize a database reset, migration repair, manual dashboard SQL, legacy cleanup, data rewrite, all-user requeue, manual `score-tick`, scoring verifier, fixture, direct OpenAI call, scheduler pause/change, budget-ledger mutation, secret change, provider/polling change, Workday expansion, package installation, effects above `B = 57`, a third preference save, or any unrelated-file push.
