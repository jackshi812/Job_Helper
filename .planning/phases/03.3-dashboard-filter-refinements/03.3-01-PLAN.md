---
phase: 03.3-dashboard-filter-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0031_dashboard_filter_refinements.sql
  - supabase/functions/_shared/filters.ts
  - supabase/functions/_shared/scoring-input.ts
  - supabase/functions/score-tick/index.ts
  - scripts/verify-scoring.ts
  - web/tests/filters.test.ts
  - web/tests/scoring-input.test.ts
  - web/tests/preference-refilter-feed.integration.test.ts
autonomous: true
requirements: [PREF-01, SCOR-01, SCOR-03]
must_haves:
  truths:
    - "D-01/D-07: active scoring replaces the experience cap with a distinct persisted title-only exclusion gate while retaining only dormant rollout compatibility for the old physical column and stored reason."
    - "D-02/D-06: the schema seeds president and PhD for existing and omitted-field rows, while an explicitly stored empty array is valid and means no title exclusion."
    - "D-03/D-05: president is matched case-insensitively as a whole word anywhere in a job title, including Vice President and Senior Vice President, without substring false positives."
    - "D-04: PhD, Ph.D., and Ph D are equivalent literal title tokens, while doctorate and doctoral remain distinct."
    - "The title exclusion gate remains provider-neutral and executes before routing, semantic-hash reuse, paid-budget reservation, and AI."
  artifacts:
    - path: "supabase/migrations/0031_dashboard_filter_refinements.sql"
      provides: "Additive seeded title_exclude_keywords column and rolling-compatible bounded filter-reason constraint"
    - path: "supabase/functions/_shared/filters.ts"
      provides: "Literal title tokenizer and title-only exclusion outcome"
      exports: ["FilterPreferences", "FilterOutcome", "cheapFilter"]
    - path: "supabase/functions/_shared/scoring-input.ts"
      provides: "Canonical title-exclusion participation in the semantic input hash"
      exports: ["scoringInputHash"]
    - path: "supabase/functions/score-tick/index.ts"
      provides: "filter-v4 worker projection and provider-neutral pre-AI application"
    - path: "scripts/verify-scoring.ts"
      provides: "Hosted verifier snapshot/restore and accepted-reason support for title exclusions"
  key_links:
    - from: "supabase/migrations/0031_dashboard_filter_refinements.sql"
      to: "supabase/functions/score-tick/index.ts"
      via: "title_exclude_keywords projection and excluded_title_keyword persistence"
      pattern: "title_exclude_keywords|excluded_title_keyword"
    - from: "supabase/functions/score-tick/index.ts"
      to: "supabase/functions/_shared/filters.ts"
      via: "preferencesToFilter followed by cheapFilter before any paid path"
      pattern: "preferencesToFilter|cheapFilter"
    - from: "supabase/functions/score-tick/index.ts"
      to: "supabase/functions/_shared/scoring-input.ts"
      via: "filter-v4 and canonical title exclusions invalidate stale semantic reuse"
      pattern: "filter-v4|scoringInputHash"
---

<objective>
Replace active experience-cap filtering with a safe, provider-neutral title-exclusion contract from database through worker, scoring hash, and hosted verifier.

Purpose: Fulfill D-01 through D-07 without breaking cached assets, the currently deployed worker, or historical filtered rows during the ordered rollout.
Output: Additive migration 0031, literal title matcher, filter-v4 semantic contract, compatible verifier support, and focused regression coverage.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-CONTEXT.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-RESEARCH.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-PATTERNS.md
@.planning/phases/03.3-dashboard-filter-refinements/03.3-UI-SPEC.md
@.planning/phases/03.2-dashboard-precision-company-visibility/03.2-01-SUMMARY.md
@supabase/migrations/0017_preferences.sql
@supabase/migrations/0025_scoring_freshness.sql
@supabase/migrations/0030_dashboard_precision.sql
@supabase/functions/_shared/filters.ts
@supabase/functions/_shared/scoring-input.ts
@supabase/functions/score-tick/index.ts
@scripts/verify-scoring.ts

<interfaces>
- Database field: `preferences.title_exclude_keywords text[] not null default array['president', 'PhD']::text[]`, cardinality at most 50.
- Active filter field: `FilterPreferences.titleExcludeKeywords: string[]`.
- New bounded outcome: `excluded_title_keyword`; retain `experience_above_max` only in the database/browser read compatibility set.
- Worker revision: `SCORING_FILTER_REVISION = 'filter-v4'`.
- Safe rollout order is schema compatibility, updated score-tick, then exact web release; this plan changes source only and Plan 04 applies/deploys it.
</interfaces>
</context>

<scope_fence>
Do not drop `max_required_experience`, remove the legacy database constraint member, rewrite historical rows, requeue all users globally, add provider-specific branches, install packages, or expand Workday/provider coverage. The old physical field and reason are dormant compatibility only; new active source must not select, save, hash, evaluate, or emit them.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the additive seeded preference and compatible reason migration</name>
  <read_first>
    - supabase/migrations/0017_preferences.sql
    - supabase/migrations/0030_dashboard_precision.sql
    - web/tests/scoring-input.test.ts
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-RESEARCH.md
  </read_first>
  <files>supabase/migrations/0031_dashboard_filter_refinements.sql, web/tests/scoring-input.test.ts</files>
  <behavior>
    - D-02: Migration 0031 gives existing rows and omitted-field inserts the exact ordered array `president`, `PhD`.
    - D-06: The column is non-null, capped at 50 elements, and accepts an explicit empty array without restoring seeds.
    - D-07: The bounded user_jobs reason set admits `excluded_title_keyword` and retains `experience_above_max`; the physical experience column remains present.
  </behavior>
  <action>Write the structural migration assertions first. Create migration 0031 using the established `alter table public.preferences add column` array/check pattern and recreate `user_jobs_filter_reason_check` with the four active values `excluded_title_keyword`, `excluded_keyword`, `wrong_location`, and `title_non_overlap`, plus legacy-compatible `experience_above_max`. Keep the migration additive in the sequential migration chain; do not alter existing RLS/grants or mutate preference rows outside PostgreSQL's constant default behavior. The structural tests must pin the exact seeds, non-null/cardinality guard, compatible reason set, and absence of a destructive legacy-column operation, implementing D-02, D-06, and the rollout part of D-07.</action>
  <acceptance_criteria>
    - `0031_dashboard_filter_refinements.sql` exists after migration 0030 and adds exactly one active preference field.
    - Existing and future omitted-field rows resolve to `['president', 'PhD']`; explicitly stored `[]` remains valid.
    - Historical `experience_above_max` rows and the currently deployed worker remain schema-compatible.
    - No RLS policy, grant, scheduler, provider, or user row is changed by this source task.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm test -- tests/scoring-input.test.ts</automated>
  </verify>
  <done>Migration 0031 is structurally proven additive, seeded, bounded, explicit-empty-safe, and rolling-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace experience parsing with literal title-only exclusions</name>
  <read_first>
    - supabase/functions/_shared/filters.ts
    - web/tests/filters.test.ts
    - web/tests/preference-refilter-feed.integration.test.ts
    - .planning/phases/03.3-dashboard-filter-refinements/03.3-CONTEXT.md
  </read_first>
  <files>supabase/functions/_shared/filters.ts, web/tests/filters.test.ts, web/tests/preference-refilter-feed.integration.test.ts</files>
  <behavior>
    - D-03/D-05: `president` excludes President, Vice President, Senior Vice-President, and case variants, but not `presidential`.
    - D-04: `PhD`, `Ph.D.`, `Ph D`, and parenthetical forms exclude; `doctorate` and `doctoral` do not.
    - D-01/D-06: Hits are title-only, an empty exclusion list passes this gate, and the general title-plus-description exclusion behavior remains separate.
    - A short or multi-word configured literal matches only complete contiguous tokens and never a substring inside a longer token.
  </behavior>
  <action>Replace `maxRequiredExperience` in the active filter contract with `titleExcludeKeywords`. Remove the exported experience parser and its gate. Add a dedicated literal title tokenizer that performs NFKC/case normalization, folds only bounded PhD punctuation/spacing variants before ordinary punctuation-to-space tokenization, preserves parenthetical text, and passes token arrays to the existing `containsTokenSequence`; never compile user input into a regex. Run this title-only gate first and return `excluded_title_keyword` with a bounded normalized detail, then preserve general exclusion, location, title-intent, and include-signal order. Replace the experience test suite with the complete D-01/D-03/D-04/D-05/D-06 matrix and update the preference-refilter integration to prove Vice President/PhD convergence, the Doctoral Researcher negative, explicit-empty behavior, and truthful Updating state until refilter convergence.</action>
  <acceptance_criteria>
    - Active filtering contains no required-years parsing or cap evaluation.
    - The title-exclusion matcher is literal, title-only, case-insensitive, whole-word/contiguous-phrase safe, and narrow for PhD variants.
    - General exclusions still search title plus description and keep their existing bounded reason.
    - The title gate precedes every other cheap filter and a failed title exclusion cannot reach paid scoring.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm test -- tests/filters.test.ts tests/preference-refilter-feed.integration.test.ts</automated>
  </verify>
  <done>D-01 and D-03 through D-06 are represented by passing table-driven and convergence tests with no provider-specific path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire filter-v4, canonical hashing, reason compatibility, and verifier restoration</name>
  <read_first>
    - supabase/functions/_shared/scoring-input.ts
    - supabase/functions/score-tick/index.ts
    - scripts/verify-scoring.ts
    - web/tests/scoring-input.test.ts
  </read_first>
  <files>supabase/functions/_shared/scoring-input.ts, supabase/functions/score-tick/index.ts, scripts/verify-scoring.ts, web/tests/scoring-input.test.ts</files>
  <behavior>
    - D-07: `score-tick` explicitly selects/maps `title_exclude_keywords`, uses `filter-v4`, and never actively projects or emits the retired experience contract.
    - Changing the canonical title-exclusion set changes the semantic hash; order, case, Unicode-equivalent, and duplicate-only changes do not.
    - `cheapFilter` remains before resume routing, semantic-hash reuse, request-budget reservation, and AI with the existing revision CAS unchanged.
    - Hosted verification recognizes the new reason while safely snapshotting/restoring legacy rows during rollout; Plan 03 owns browser reason presentation compatibility.
  </behavior>
  <action>Replace the experience field in semantic preference hashing with canonical `titleExcludeKeywords`, retaining existing canonical-array rules. Update `PreferencesRow`, `preferencesToFilter`, and the explicit score-tick projection, bump the filter revision to `filter-v4`, and preserve the exact provider-neutral pre-AI ordering and revision-CAS write. Extend `verify-scoring.ts` accepted reasons and preference snapshot/select/restore payload with `title_exclude_keywords`, and change its guaranteed free-filter fixture to the title exclusion without widening the one-shot/cleanup boundary. Add hash variants, source-order assertions, compatibility assertions, and worker checks implementing D-07; keep logs content-free and do not change tier thresholds or scoring-reason detail behavior required by SCOR-03. Plan 03 owns the separate browser `FilterReason` compatibility update so this task stays within four modified files.</action>
  <acceptance_criteria>
    - Real title-exclusion changes invalidate semantic reuse under filter-v4.
    - Every claimed provider job shares one cheap-filter call before routing/hash/budget/AI.
    - The new worker can write `excluded_title_keyword` only after migration 0031 is applied.
    - Hosted verifier cleanup restores the exact caller preference snapshot including an explicit empty title-exclusion list.
    - Hosted verifier snapshots can restore historical experience reasons, but active worker/filter/hash source does not use the retired cap/reason.
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; npm test -- tests/scoring-input.test.ts</automated>
  </verify>
  <done>The end-to-end backend contract is filter-v4, canonical, provider-neutral, pre-AI, bounded, verifier-safe, and rolling-release compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated preference row → service-role worker | User-controlled arrays cross from RLS-owned storage into privileged filtering and paid-scoring admission. |
| Job/provider data → shared cheap filter | Untrusted titles and descriptions from every source enter one normalization path. |
| Local source → hosted schema/function rollout | The new reason may be emitted only after the compatible constraint is live. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.3-01 | Denial of service / Tampering | `filters.ts` title keywords | high | mitigate | Cap the stored array at 50, tokenize bounded strings, reuse array comparison, and permit only a fixed PhD-fold regex that contains no user interpolation. |
| T-03.3-02 | Tampering / Elevation of privilege | `score-tick` paid path | high | mitigate | Keep one provider-neutral cheapFilter call before routing, hash reuse, budget reservation, and AI; pin ordering structurally and retain revision CAS publication. |
| T-03.3-03 | Information disclosure | worker/verifier logging and snapshots | medium | mitigate | Preserve content-free bounded logs and exact snapshot/restore cleanup; never log preference arrays, titles, descriptions, resumes, prompts, or responses. |
| T-03.3-04 | Tampering | migration/worker rollout ordering | high | mitigate | Plan 04 blocks deployment until owner approval, applies migration 0031 first, then deploys score-tick, then web, with exact identity checks. |
| T-03.3-05 | Repudiation | legacy compatibility state | low | accept | Structural tests and release evidence document that the old column/reason are retained only to keep historical rows and rolling clients readable. |
| T-03.3-SC | Tampering | package supply chain | high | mitigate | Install no packages; use only the existing locked stack and native language/platform APIs. |
</threat_model>

## Artifacts this phase produces

- `supabase/migrations/0031_dashboard_filter_refinements.sql`: new additive migration path.
- `preferences.title_exclude_keywords`: new persisted database column with exact seeded default and cardinality guard.
- `FilterPreferences.titleExcludeKeywords`: replacement active preference property.
- `FilterOutcome.reason = 'excluded_title_keyword'`: new bounded active filter outcome.
- `literalTitleTokens` and the title-exclusion lookup helper in `supabase/functions/_shared/filters.ts`: narrow literal matching symbols; keep module visibility unless tests require an export.
- `SCORING_FILTER_REVISION = 'filter-v4'`: new worker/hash revision constant value.
- Updated `PreferencesRow.title_exclude_keywords` and `preferencesToFilter` wiring in `score-tick/index.ts`.
- Updated `PreferencesSnapshot.title_exclude_keywords` and reason allowlist in `scripts/verify-scoring.ts`.

<verification>
1. Run the three task-focused test commands after their respective RED/GREEN changes.
2. Run `cd web &amp;&amp; npm test -- tests/filters.test.ts tests/scoring-input.test.ts tests/preference-refilter-feed.integration.test.ts` as the integrated Plan 01 gate.
3. Confirm migration 0031 is not applied and no Edge/web deployment occurs in this plan; Plan 04 owns the blocking outward rollout.
4. Inspect the final source diff for provider branches, unbounded logging, package changes, historical-row rewrites, or active experience-cap use.
</verification>

<success_criteria>
- Persisted title exclusions drive one safe, literal, provider-neutral pre-AI gate.
- Exact seeds and explicit empty semantics are represented in schema and tests.
- PhD variants and president whole-word behavior match D-03 through D-05 without fuzzy expansion.
- Semantic score reuse changes only for real title-exclusion/filter-v4 differences.
- Old physical compatibility survives the rollout while new active source stops using it.
- SCOR-01 and the filtered-reason compatibility portion of SCOR-03 remain fully covered.
</success_criteria>

<output>
Create `.planning/phases/03.3-dashboard-filter-refinements/03.3-01-SUMMARY.md` when done.
</output>
