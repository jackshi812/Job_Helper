---
phase: 05-outreach-feasibility-gate
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - .planning/phases/05-outreach-feasibility-gate/05-DECISION.json
  - .planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "Only the verbatim owner response from the blocking Plan 05-03 checkpoint can transition the decision to `RIGHTS_NO_GO_ACCEPTED`."
    - "Immediately after that checkpoint, the controlled worktree, staged Git index, and full Phase 5 baseline-to-HEAD commit range prove zero provider calls, fixtures, raw results, production mutations, forbidden hits, symlinks, and unexpected survivors."
    - "Final evidence preserves `NOT_RUN_RIGHTS_NO_GO`, disabled production outreach, and the exact Phase-5-only milestone summary `- ⛔ **v1.1 Outreach Intelligence** — Phase 5 only; rights no-go accepted; no production outreach search; any redesign requires a separately scoped owner decision`, with no remaining structured or prose Phase 6/7 material."
    - "The supported GSD roadmap transition removes future Phases 7 then 6, after which both plan and execute initialization for each removed phase strictly report `phase_found === false`; every exposed `phase_number`, `phase_name`, `phase_slug`, and `expected_phase_dir` is null; plan initialization reports `has_plans === false` and `plan_count === 0`; execute initialization reports `plan_count === 0`; every exposed plan/summary collection is empty; and every exposed `incomplete_count === 0`."
    - "The post-no-go owner handoff neutrally lists user-pasted LinkedIn URLs, non-LinkedIn public professional profiles, and stopping outreach while selecting and implementing none, per D-13."
    - "The dormant runner remains unreferenced and unreachable under the committed rights matrix."
  artifacts:
    - path: ".planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json"
      provides: "Post-owner local/Git zero-call and zero-residue evidence bound to the stable decision contract"
    - path: ".planning/phases/05-outreach-feasibility-gate/05-DECISION.json"
      provides: "Final exact owner-attested `RIGHTS_NO_GO_ACCEPTED` record"
    - path: ".planning/ROADMAP.md"
      provides: "Exact Phase-5-only rights-no-go-accepted milestone summary and stopped v1.1 roadmap, with no structured or prose Phase 6/7 material, consumed by strict GSD plan/execute initialization checks"
    - path: ".planning/STATE.md"
      provides: "Current Phase 5 state with one remaining v1.1 roadmap phase"
  key_links:
    - from: ".planning/phases/05-outreach-feasibility-gate/05-03-SUMMARY.md"
      to: ".planning/phases/05-outreach-feasibility-gate/05-DECISION.json"
      via: "verbatim checkpoint response must equal exactOwnerAttestation byte-for-byte"
      pattern: "owner_checkpoint_05-03"
    - from: ".planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json"
      to: ".planning/phases/05-outreach-feasibility-gate/05-DECISION.json"
      via: "final decision stores and validates the canonical residue digest"
      pattern: "zero_residue_sha256"
    - from: ".planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md"
      to: ".planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json"
      via: "the latest committed plan revision anchors the exact Phase 5 implementation commit range inspected alongside worktree and index"
      pattern: "phase_plan_baseline_sha"
    - from: ".planning/phases/05-outreach-feasibility-gate/05-DECISION.json"
      to: ".planning/ROADMAP.md"
      via: "only the accepted no-go plus residue proof authorizes supported `phase.remove 7` then `phase.remove 6`; the repair replaces the old Phases 5–7 planning banner with the exact Phase-5-only stopped summary and removes all Phase 6/7 prose/structure, which both GSD initializers must expose through strict false/null/zero/empty missing-phase contracts"
      pattern: "phase\\.remove|rights no-go accepted|phase_found"
  prohibitions:
    - "No final decision is written if the checkpoint summary lacks the verbatim exact owner signal."
    - "No residue failure is converted to pass or silently deleted."
    - "No `GO`, quality result, provider-side deletion claim, fallback design, later-phase authorization, or production search enablement is recorded."
    - "No passive decision/status field is claimed as a GSD admission control; no direct phase-directory deletion, `--force`, REQUIREMENTS/PROJECT mutation, or automatic restoration is permitted."
    - "No production, schema, package, UI, Edge Function, scheduler, database, or provider mutation occurs."
---

<objective>
Finalize the owner-attested rights no-go after post-checkpoint zero-residue proof, then enforce the stop through the supported GSD roadmap transition so downstream planning and execution are unavailable.

Purpose: D-13 requires the failed gate to stop the milestone without an automatic pivot; D-17 requires immediate post-decision cleanup/absence proof; OUTR-05 must remain `NOT_RUN_RIGHTS_NO_GO` rather than a fabricated quality result. Passive JSON fields do not stop GSD, so the accepted decision must drive a supported roadmap mutation.
Output: `05-ZERO-RESIDUE.json`, final `05-DECISION.json`, a stopped one-phase v1.1 roadmap/state, and complete offline verification that Phase 6 and Phase 7 cannot be planned or executed.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md
@.planning/phases/05-outreach-feasibility-gate/05-01-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-02-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-03-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json
@.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json
@.planning/phases/05-outreach-feasibility-gate/05-DECISION.json
@scripts/outreach-feasibility/rights-gate.mjs
@scripts/outreach-feasibility/decision-evidence.mjs
@scripts/outreach-feasibility/residue-check.mjs
@scripts/outreach-feasibility/dormant/spike-runner.mjs

<interfaces>
From Plan 05-02:

- `exactOwnerAttestation(rightsEvidenceSha256): string`
- `assertDecisionRecord({ matrix, qualityReport, decision, residue?, requireAccepted? }): DecisionRecord`
- `scanOwnedSurfaces(options): Promise&lt;ResidueScan&gt;`
- `buildZeroResidueRecord({ matrix, qualityReport, decision, scan }): ZeroResidueRecord`
- `assertZeroResidueRecord(record, expectedDigests): ZeroResidueRecord`
- `runConditionalSpike(...)` — import-only; real matrix returns zero-effect `NOT_RUN_RIGHTS_NO_GO`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the immediate post-owner zero-residue proof</name>
  <read_first>
    - `.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-03-SUMMARY.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `scripts/outreach-feasibility/residue-check.mjs`
    - `scripts/outreach-feasibility/residue-check.test.mjs`
    - `scripts/outreach-feasibility/dormant/spike-runner.test.mjs`
  </read_first>
  <files>.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json</files>
  <action>
Before generating evidence, use `exactOwnerAttestation()` to compare the verbatim Plan 05-03 checkpoint response with the current rights digest. Stop if the summary lacks checkpoint provenance or differs by any byte; do not infer approval from plan completion.

Then run the allowlisted residue scan immediately, per D-17. Resolve the immutable `phase_plan_baseline_sha` from the latest commit containing `05-01-PLAN.md`, require it to be an ancestor of `HEAD`, and inspect all three concrete surfaces defined in Plan 05-02: current worktree status/files, staged index paths/blobs, and every bounded path/blob in the `phase_plan_baseline_sha..HEAD` revision range. Under the D-12 no-go path, no corpus or raw response should ever have existed: a matching transient prefix, symlink, unexpected survivor, forbidden nested evidence value, staged leak, historical Phase 5 commit leak, or nonzero provider/fixture/raw/production count is a hard failure and must not be silently removed or converted to pass.

Create `05-ZERO-RESIDUE.json` with `status: PASS`, `scope: LOCAL_AND_GIT_ONLY`, `provider_side_retention: NOT_ASSERTED`, the exact scan inventory, zero `provider_call_count`, `fixture_count`, `raw_result_count`, `production_mutation_count`, `forbidden_hit_count`, `unexpected_survivor_count`, and `symlink_count`, plus the rights/quality/stable-decision-contract digests. Its exact `git_surfaces` object records sanitized worktree status/path/blob counts, index staged-path/blob counts, and Phase 5 commit-range base/head SHAs with commit/path/blob counts. Compute `zero_residue_sha256` from a canonical projection that excludes its own digest. This is worktree/index/commit-range proof only; it cannot resolve Tavily retention or provider-side deletion ambiguity.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/residue-check.test.mjs scripts/outreach-feasibility/dormant/spike-runner.test.mjs &amp;&amp; env -u TAVILY_API_KEY node scripts/outreach-feasibility/residue-check.mjs --assert-zero --repo-root . --phase-dir .planning/phases/05-outreach-feasibility-gate --phase-plan .planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md --decision .planning/phases/05-outreach-feasibility-gate/05-DECISION.json --record .planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The task refuses to start without a byte-exact owner checkpoint signal bound to the current rights digest.</criterion>
    <criterion>The committed record is `PASS`, scoped to `LOCAL_AND_GIT_ONLY`, states provider retention is not asserted, and contains seven exact zero counts.</criterion>
    <criterion>The record proves the worktree, staged index, and exact Phase 5 plan-baseline-to-HEAD range were inspected, and the focused tests detect isolated staged and removed-but-committed leaks in temporary repositories.</criterion>
    <criterion>The residue digest recomputes from the non-self-referential canonical projection and binds the rights, quality, and decision-contract digests.</criterion>
    <criterion>The real-matrix dormant-runner test again proves zero secret, corpus, request-build, and fetch calls.</criterion>
  </acceptance_criteria>
  <done>The owner-attested no-go has immediate, machine-readable zero-call and zero-local-residue proof without overstating provider-side deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize and verify the stopped-milestone decision chain</name>
  <read_first>
    - `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json` (this task finalizes it)
    - `.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-03-SUMMARY.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md`
    - `scripts/outreach-feasibility/decision-evidence.mjs`
    - `scripts/outreach-feasibility/decision-evidence.test.mjs`
    - `scripts/outreach-feasibility/rights-gate.test.mjs`
    - `scripts/outreach-feasibility/residue-check.test.mjs`
    - `scripts/outreach-feasibility/dormant/spike-runner.test.mjs`
  </read_first>
  <files>.planning/phases/05-outreach-feasibility-gate/05-DECISION.json</files>
  <action>
Copy only the verbatim owner response from the blocking Plan 05-03 checkpoint into `owner_attestation`, set `owner_attestation_source: owner_checkpoint_05-03`, set `status: RIGHTS_NO_GO_ACCEPTED`, and bind the validated `zero_residue_sha256`. Preserve the stable `decision_contract_sha256`; do not recompute it over owner/residue fields.

Keep `rights_status: RIGHTS_NO_GO`, `quality_status: NOT_RUN_RIGHTS_NO_GO`, `search_authorized: false`, `production_outreach_enabled: false`, `outreach_milestone_status: STOPPED_RIGHTS_NO_GO`, `phase_6_authorized: false`, `phase_7_authorized: false`, and all four effect counts at zero. Preserve the exact ordered `redesign_handoff_options` from Plan 05-02—`user-pasted LinkedIn URLs`, `non-LinkedIn public professional profiles`, and `stopping outreach`—with `redesign_selection: null`.

Do not present the options as an active continuation yet. Task 3 presents the neutral handoff only after the supported GSD stop transition succeeds. Phase 5 selects, recommends, and implements none.

Run the complete four-file offline test suite with the provider key absent, then assert the final decision with `--require-accepted`, the no-go quality report, and zero residue. Reject any attempt to relabel the missing quality spike as pass/fail, authorize a later phase, or change an effect count. The completed evidence chain consists only of the sanitized rights matrix, no-run quality report, owner-attested no-go decision, and local/Git zero-residue record.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/rights-gate.test.mjs scripts/outreach-feasibility/decision-evidence.test.mjs scripts/outreach-feasibility/residue-check.test.mjs scripts/outreach-feasibility/dormant/spike-runner.test.mjs &amp;&amp; env -u TAVILY_API_KEY node scripts/outreach-feasibility/decision-evidence.mjs --assert-decision --matrix .planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json --quality-report .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json --decision .planning/phases/05-outreach-feasibility-gate/05-DECISION.json --require-accepted &amp;&amp; env -u TAVILY_API_KEY node scripts/outreach-feasibility/rights-gate.mjs --assert-no-go --matrix .planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json --quality-report .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json &amp;&amp; env -u TAVILY_API_KEY node scripts/outreach-feasibility/residue-check.mjs --assert-zero --repo-root . --phase-dir .planning/phases/05-outreach-feasibility-gate --phase-plan .planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md --decision .planning/phases/05-outreach-feasibility-gate/05-DECISION.json --record .planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Final decision validation passes only for the verbatim checkpoint response, exact current evidence digests, accepted no-go status, and validated residue digest.</criterion>
    <criterion>The final record contains false search/production/Phase 6/Phase 7 authorization and `STOPPED_RIGHTS_NO_GO` milestone status.</criterion>
    <criterion>The final record and owner-facing handoff contain exactly the three D-13 redesign categories, keep `redesign_selection: null`, and authorize or implement none.</criterion>
    <criterion>The quality status remains exactly `NOT_RUN_RIGHTS_NO_GO`; no case result or fabricated quality conclusion is present.</criterion>
    <criterion>All four focused test files and all three artifact assertion CLIs pass offline with the provider key absent.</criterion>
  </acceptance_criteria>
  <done>The accepted decision chain is final, exact, zero-effect, and ready for the supported GSD structural stop in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Remove downstream phases through the GSD-consumed stop transition</name>
  <read_first>
    - `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/ROADMAP.md`
    - `.planning/STATE.md`
    - `.planning/REQUIREMENTS.md` (read-only invariant)
    - `.planning/PROJECT.md` (read-only invariant)
    - `/Users/jackshi/.codex/gsd-core/workflows/remove-phase.md`
  </read_first>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Re-run the accepted-decision and zero-residue assertion CLIs before changing roadmap state. Preflight `init.phase-op 7`, `init.phase-op 6`, and `state.json`: current phase must be 5; both target phases must still be future roadmap members with no phase directory, no plans, and no summaries. Snapshot SHA-256 for `.planning/REQUIREMENTS.md` and `.planning/PROJECT.md` and require both to remain byte-identical. Abort on any drift; never use `--force`.

Invoke the supported noninteractive handler through `/Users/jackshi/.codex/gsd-core/bin/gsd-tools.cjs`, highest phase first: `query phase.remove 7`, then `query phase.remove 6`. Both calls are required because removing Phase 6 first would renumber the old Phase 7 into Phase 6. Require each result to report the expected `removed` value, `directory_deleted: null`, empty renamed-directory/file arrays, and both `roadmap_updated` and `state_updated` true. The exact owner no-go from Plan 05-03 supplies authorization for this structural stop; do not treat passive fields in `05-DECISION.json` or STATE status text as the consumer.

The current terminal-phase removal handler also consumes the trailing Progress/footer and leaves old milestone overview prose, so make one scoped repair to `.planning/ROADMAP.md` after both supported removals. Keep Phase 5 as the sole v1.1 phase; restore `## Progress`, the exact table header `| Phase | Milestone | Plans Complete | Status | Completed |`, the exact separator `|-------|-----------|----------------|--------|-----------|`, an execution order containing only Phase 5, exactly one Phase 5 progress row, the terminal `---` separator, and the exact roadmap footer `*Roadmap created: 2026-07-28 for milestone v1.1 Outreach Intelligence*`. Replace the current milestone-summary line `- 🚧 **v1.1 Outreach Intelligence** — Phases 5–7; planning` with exactly `- ⛔ **v1.1 Outreach Intelligence** — Phase 5 only; rights no-go accepted; no production outreach search; any redesign requires a separately scoped owner decision`. Replace the v1.1 overview paragraph immediately after `## Overview` with this exact single-line text: `v1.1 stopped at the accepted Phase 5 rights no-go. No production outreach search is authorized or implemented; a separately scoped owner redesign is required before any further outreach work.` Remove every remaining structured or prose reference to Phase 6, Phase 7, or the old Phases 5–7 range from `.planning/ROADMAP.md`; do not recreate a downstream heading, checklist item, details section, progress row, plan, or directory. Do not alter `.planning/REQUIREMENTS.md` or `.planning/PROJECT.md`; their pending requirements remain historical scope, not executable roadmap membership.

Read back `init.plan-phase` and `init.execute-phase` for both Phase 6 and Phase 7. Use strict equality without numeric or string coercion. For each initializer require `phase_found === false`; require every exposed missing-phase identity field among `phase_number`, `phase_name`, `phase_slug`, and `expected_phase_dir` to be exactly null. For each plan initializer additionally require `has_plans === false` and `plan_count === 0`; for each execute initializer require `plan_count === 0`. For both initializer types require every exposed plan or summary array to be an actual empty array, and require `incomplete_count === 0` whenever that field is exposed. After those read-only GSD admission checks pass, present the owner-facing neutral handoff with exactly these separately scoped categories: `user-pasted LinkedIn URLs`, `non-LinkedIn public professional profiles`, and `stopping outreach`. State that none is selected, recommended, authorized, or implemented. Record the same neutral handoff and the successful GSD readbacks in `05-04-SUMMARY.md`. Restoration is not a toggle: only a later separately authorized decision may revert this task's atomic structural commit or create newly planned phases through supported phase-add tooling.
  </action>
  <verify>
    <automated>node -e "const {execFileSync}=require('node:child_process'); const {readFileSync,readdirSync}=require('node:fs'); const tool='/Users/jackshi/.codex/gsd-core/bin/gsd-tools.cjs'; const q=(...a)=&gt;JSON.parse(execFileSync(process.execPath,[tool,'query',...a],{encoding:'utf8'})); const r5=q('roadmap.get-phase','5'),r6=q('roadmap.get-phase','6'),r7=q('roadmap.get-phase','7'),p6=q('init.plan-phase','6'),p7=q('init.plan-phase','7'),e6=q('init.execute-phase','6'),e7=q('init.execute-phase','7'),a=q('roadmap.analyze'),s=q('state.json'); const assertIdentityNull=(n,kind,x)=&gt;{for(const k of ['phase_number','phase_name','phase_slug','expected_phase_dir']) if(Object.hasOwn(x,k)&amp;&amp;x[k]!==null) throw Error(kind+' '+k+' remains for Phase '+n)}; const assertCollectionsEmpty=(n,kind,x)=&gt;{for(const [k,v] of Object.entries(x)) if(Array.isArray(v)&amp;&amp;/(?:plan|summar)/i.test(k)&amp;&amp;v.length!==0) throw Error(kind+' '+k+' is not empty for Phase '+n); if(Object.hasOwn(x,'incomplete_count')&amp;&amp;x.incomplete_count!==0) throw Error(kind+' incomplete_count is not strict zero for Phase '+n)}; const assertPlanUnavailable=(n,x)=&gt;{if(x.phase_found!==false||x.has_plans!==false||x.plan_count!==0) throw Error('plan admission contract invalid for Phase '+n); assertIdentityNull(n,'plan',x); assertCollectionsEmpty(n,'plan',x)}; const assertExecuteUnavailable=(n,x)=&gt;{if(x.phase_found!==false||x.plan_count!==0) throw Error('execute admission contract invalid for Phase '+n); assertIdentityNull(n,'execute',x); assertCollectionsEmpty(n,'execute',x)}; assertPlanUnavailable(6,p6); assertPlanUnavailable(7,p7); assertExecuteUnavailable(6,e6); assertExecuteUnavailable(7,e7); if(r5.found!==true||r6.found!==false||r7.found!==false||a.phase_count!==1||!(a.next_phase===null||a.next_phase===''||a.next_phase===undefined)||s.current_phase!=='5'||s.progress?.total_phases!==1) throw Error('terminal roadmap state invalid'); const road=readFileSync('.planning/ROADMAP.md','utf8').replace(/\\r\\n/g,'\\n'),lines=road.trimEnd().split('\\n'),dirs=readdirSync('.planning/phases',{withFileTypes:true}).filter(d=&gt;d.isDirectory()).map(d=&gt;d.name); const progress='## Progress',order='**Execution Order:** Phase 5',header='| Phase | Milestone | Plans Complete | Status | Completed |',separator='|-------|-----------|----------------|--------|-----------|',milestone='- ⛔ **v1.1 Outreach Intelligence** — Phase 5 only; rights no-go accepted; no production outreach search; any redesign requires a separately scoped owner decision',oldMilestone='- 🚧 **v1.1 Outreach Intelligence** — Phases 5–7; planning',overview='v1.1 stopped at the accepted Phase 5 rights no-go. No production outreach search is authorized or implemented; a separately scoped owner redesign is required before any further outreach work.',footer='*Roadmap created: 2026-07-28 for milestone v1.1 Outreach Intelligence*',count=v=&gt;lines.filter(line=&gt;line===v).length,phase5Rows=lines.filter(line=&gt;/^\\| 5\\. Outreach Feasibility Gate \\| v1\\.1 \\| [^|]+ \\| [^|]+ \\| [^|]+ \\|$/.test(line)); if(count(progress)!==1||count(order)!==1||count(header)!==1||count(separator)!==1||count(milestone)!==1||phase5Rows.length!==1) throw Error('complete stopped Phase-5-only roadmap structure missing'); if(road.includes(oldMilestone)||/\\bPhase(?:s)?\\s+(?:6|7)\\b/.test(road)||/\\bPhases\\s+5[–-]7\\b/.test(road)) throw Error('old milestone range or downstream roadmap prose remains'); const overviewAt=lines.indexOf('## Overview'); if(overviewAt&lt;0||lines[overviewAt+1]!==''||lines[overviewAt+2]!==overview||lines[overviewAt+3]!==''||lines[overviewAt+4]!=='## Phases') throw Error('stopped v1.1 overview missing'); if(lines.some(line=&gt;/^#{2,4} Phase (?:6|7):/.test(line)||/^- \\[[ xX]\\] \\*\\*Phase (?:6|7):/.test(line)||/^\\| (?:6|7)\\./.test(line))||dirs.some(d=&gt;/^0[67]-/.test(d))) throw Error('downstream roadmap residue'); const expectedTail=[progress,'',order,'',header,separator,phase5Rows[0],'','---',footer],progressAt=lines.indexOf(progress); if(JSON.stringify(lines.slice(progressAt))!==JSON.stringify(expectedTail)) throw Error('terminal Progress/footer structure invalid');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Both future phases pass the unstarted/no-directory preflight and are removed by supported `phase.remove` calls in 7-then-6 order without `--force`.</criterion>
    <criterion>`roadmap.get-phase 6/7`, `init.plan-phase 6/7`, and `init.execute-phase 6/7` all report unavailable; for both removed phases each initializer returns strict `phase_found === false`, every exposed `phase_number`, `phase_name`, `phase_slug`, and `expected_phase_dir` is exactly null, plan initialization returns `has_plans === false` and `plan_count === 0`, execute initialization returns `plan_count === 0`, every exposed plan/summary array is empty, and every exposed `incomplete_count === 0`.</criterion>
    <criterion>`roadmap.analyze` reports one phase and no next phase; STATE remains on Phase 5 with one v1.1 phase.</criterion>
    <criterion>The repaired roadmap contains exactly the milestone summary `- ⛔ **v1.1 Outreach Intelligence** — Phase 5 only; rights no-go accepted; no production outreach search; any redesign requires a separately scoped owner decision`, exactly one `## Progress` heading, the exact table header/separator, one Phase 5 row, the Phase-5-only execution order, the exact terminal separator/footer, and the exact stopped v1.1 overview; the old `Phases 5–7; planning` text and every structured or prose Phase 6/7 reference, plan, and directory are absent.</criterion>
    <criterion>REQUIREMENTS.md and PROJECT.md remain byte-identical, and the owner receives exactly the three neutral D-13 choices with none selected or implemented.</criterion>
  </acceptance_criteria>
  <done>The actual GSD plan and execute entrypoints return the exact false/null/zero/empty missing-phase contract for both Phase 6 and Phase 7, and the terminal roadmap contains only the exact Phase-5-only rights-no-go-accepted milestone summary with no old range or downstream phase material; recovery requires a separately authorized structural commit or newly planned phases.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json`
  - Core fields: `schema_version`, `phase`, `status`, `scope`, `provider_side_retention`, `scanned_roots`, exact `git_surfaces` worktree/index/phase-commit-range inventory, `provider_call_count`, `fixture_count`, `raw_result_count`, `production_mutation_count`, `forbidden_hit_count`, `unexpected_survivor_count`, `symlink_count`, `rights_evidence_sha256`, `quality_evidence_sha256`, `decision_contract_sha256`, `zero_residue_sha256`, `checked_at`.
- `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json` (finalized)
  - Final field values: `status: RIGHTS_NO_GO_ACCEPTED`, verbatim `owner_attestation`, `owner_attestation_source: owner_checkpoint_05-03`, validated `zero_residue_sha256`, exact neutral `redesign_handoff_options`, and `redesign_selection: null`; all other no-go/zero/disabled fields remain unchanged.
- `.planning/phases/05-outreach-feasibility-gate/05-04-SUMMARY.md` (executor output).
- `.planning/ROADMAP.md` and `.planning/STATE.md` (updated only through supported phase removals plus the scoped terminal Progress/overview repair).
- No new function, class, CLI flag, package, database object, UI route, API route, migration, production configuration, or provider effect is added in this plan.

## Schema Push Gate

No schema push is required. The complete Phase 5 plan set contains no `supabase/migrations/*.sql` file and intentionally excludes every production schema/UI/function path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner checkpoint summary → final decision | Only the verbatim, provenance-marked owner response may finalize the record. |
| Controlled worktree/index/Phase 5 commit range → residue evidence | Absence claims must inspect current, staged, and committed surfaces while remaining narrow, deterministic, sanitized, and non-destructive. |
| Accepted evidence chain → GSD roadmap admission | Supported Phase 7 then Phase 6 removal makes downstream roadmap membership—not passive JSON/status fields—the enforced boundary. |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Tampering / Elevation | Final gate chain | high | mitigate | Re-run rights, dormant-runner, decision, and residue assertions; any provider-effect or authorization drift blocks completion. |
| T-05-04-02 | Information Disclosure | Final artifacts | high | mitigate | Exact sanitized schemas, empty quality cases, recursive residue checks, key-absent execution, and zero effect counts prevent key/raw response leakage. |
| T-05-04-03 | Information Disclosure | `05-ZERO-RESIDUE.json` | high | mitigate | Seven zero counts plus exact worktree/index/plan-baseline-to-HEAD inventory prove controlled absence; temporary repositories exercise staged and historical leaks without printing rejected values or claiming provider-side retention/deletion. |
| T-05-04-04 | Spoofing / Tampering | `05-DECISION.json` | high | mitigate | Byte-exact checkpoint response, provenance field, rights digest, stable decision-contract digest, and residue digest reject forged/stale owner input. |
| T-05-04-05 | Tampering | Evidence chain | high | mitigate | Canonical non-self-referential digests and cross-artifact assertions detect source, quality, decision, or residue drift. |
| T-05-04-06 | Elevation | Later-phase/dormant admission and redesign handoff | high | mitigate | Accepted no-go plus zero residue gates supported 7-then-6 removal; roadmap/init readbacks prove Phase 6 and Phase 7 absent from plan and execute admission, while the real-matrix zero-effect test and exact neutral handoff with null selection keep dormant code and every redesign option unauthorized. |
| T-05-04-07 | Tampering / Denial of Service | `phase.remove` transition | high | mitigate | Unstarted/no-directory preflight, high-to-low order, no `--force`, exact handler-result checks, scoped terminal roadmap repair, read-only PROJECT/REQUIREMENTS digests, and one atomic task commit prevent accidental deletion, renumbering, or partial admission state. |
| T-05-04-SC | Tampering | Dependency supply chain | low | accept | No dependency or package-manager operation occurs. |
</threat_model>

<verification>
Run all four Phase 5 test files plus the final rights, decision, and residue assertion CLIs with the provider key absent. Then require the supported roadmap/state transition, the exact repaired terminal roadmap structure, and all `roadmap.get-phase`, plan-init, execute-init, roadmap-analysis, and state readbacks to prove Phase 6 and Phase 7 remain unavailable.
</verification>

<success_criteria>
- OUTR-04 closes as an owner-attested `RIGHTS_NO_GO`; production search remains disabled.
- OUTR-05 closes truthfully as `NOT_RUN_RIGHTS_NO_GO` because D-12 barred the representative search.
- Seven final zero counts plus explicit worktree, staged-index, and Phase 5 commit-range inspection prove no provider effect and no controlled local/Git third-party residue.
- The outreach milestone is structurally stopped: future Phases 7 then 6 are removed through supported GSD handlers, and both downstream initialization paths report Phase 6 and Phase 7 unavailable pending a separately scoped owner decision.
- The owner receives exactly the three D-13 redesign categories with no selection or implementation.
- No production or schema file was planned or changed; no schema push is required.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Owner can make an evidence-backed go/no-go before production result collection | 01–04 | COVERED | Current evidence yields exact owner-attested no-go; collection stays disabled and the GSD roadmap has no downstream executable phase. |
| REQ | OUTR-04 | Rights permit exact display/persistence/cache/deletion flow or production remains disabled | 01, 03, 04 | COVERED | Current LinkedIn prohibition and Tavily ambiguity produce binding no-go; local and provider deletion boundaries remain distinct. |
| REQ | OUTR-05 | Representative quality evidence plus explicit owner decision | 01–04 | COVERED | D-12 bars the quality run; evidence is truthfully `NOT_RUN_RIGHTS_NO_GO`, with dormant contract and explicit owner no-go. |
| RESEARCH | — | Current result is `RIGHTS_NO_GO` | 01, 04 | COVERED | Matrix/evaluator/final decision preserve the binding result. |
| RESEARCH | — | Zero provider calls, secrets, raw data, fixtures, and production effects | 01, 02, 04 | COVERED | Injected-effect tests, exact zero counters, and worktree/index/Phase 5 commit-range inspection prove the controlled boundary. |
| RESEARCH | — | Four operational questions resolve to `RIGHTS_NO_GO`; written clarification is a future reopen condition | 01, 04 | COVERED | Research preserves every question/source fact while removing them as unresolved Phase 5 inputs. |
| RESEARCH | — | Dormant path cannot activate unless every required operation is explicit `ALLOW` | 01, 02, 04 | COVERED | Exact seven-row conjunction plus digest-bound structured authorization precede every effect. |
| RESEARCH | — | Owner attestation is exact, current-evidence-bound, and not agent-generated | 02–04 | COVERED | Pending contract, blocking-human checkpoint, and final byte-exact assertion. |
| RESEARCH | — | Failed gate stops the active outreach roadmap before downstream execution | 04 | COVERED | Supported 7-then-6 phase removal plus plan/execute init readbacks enforce the stop. |
| CONTEXT | D-01 | Eight cases: six real plus two controls | 02 | COVERED | Encoded in dormant `SPIKE_CONTRACT`; no cases created under no-go. |
| CONTEXT | D-02 | Three-company, role-family, max-two-per-company real-case mix | 02 | COVERED | Encoded and offline-tested as dormant validation constraints. |
| CONTEXT | D-03 | Known-positive and known-negative controls | 02 | COVERED | Encoded in dormant contract; no control data created. |
| CONTEXT | D-04 | Disposable corpus, no production queries/mutations | 02, 04 | COVERED | Conditional injection only; current path proves fixture and production counts zero. |
| CONTEXT | D-05 | Four-of-six plus positive found and negative rejected | 02 | COVERED | Encoded in dormant gate; not evaluated because rights failed. |
| CONTEXT | D-06 | Current-company and meaningful-role-fit evidence | 02 | COVERED | Encoded as required dormant qualifying evidence. |
| CONTEXT | D-07 | Maximum three physical provider searches per case | 02 | COVERED | Encoded in dormant contract; actual calls remain zero. |
| CONTEXT | D-08 | One bounded retry and honest `coverage_unknown` | 02 | COVERED | Encoded in dormant contract; no runtime outcome fabricated. |
| CONTEXT | D-09 | Clear prohibition is automatic no-go | 01, 04 | COVERED | Strict rights evaluator and final decision. |
| CONTEXT | D-10 | Ambiguity is no-go absent written clarification | 01, 02 | COVERED | Ambiguous rows fail closed; only explicit current `ALLOW` can admit dormant effects. |
| CONTEXT | D-11 | Exact URL/reason operations required; cache optional | 01, 03 | COVERED | Seven required lifecycle operations include local deletion and provider retention/deletion; optional cache is separately unauthorized and owner-reviewed. |
| CONTEXT | D-12 | Rights before quality; both must pass for go | 01, 02, 04 | COVERED | Quality is `NOT_RUN_RIGHTS_NO_GO`; all provider callbacks remain untouched. |
| CONTEXT | D-13 | Failed gate stops milestone; return neutral choices without automatic fallback | 02–04 | COVERED | Supported phase removals make Phase 6 and Phase 7 unavailable through both plan and execute admission, while the handoff returns exactly user-pasted LinkedIn URLs, non-LinkedIn public professional profiles, or stopping outreach with no selection or implementation. |
| CONTEXT | D-14 | Minimal disposable real-case fields only | 02 | COVERED | Dormant allowlist encoded; current fixture count is zero. |
| CONTEXT | D-15 | Raw responses transient and deleted before sanitized report | 01–04 | COVERED | Local deletion and provider retention/deletion are distinct reviewed rows; dormant cleanup is encoded; current raw-result count/residue is zero because no call ran. |
| CONTEXT | D-16 | Anonymous sanitized committed quality report | 01, 02 | COVERED | Current report has empty cases, zero counts, no candidate data, and no fabricated outcome. |
| CONTEXT | D-17 | Post-owner cleanup and automated zero-residue assertion | 02, 04 | COVERED | Immediate dependent plan creates digest-bound evidence across the worktree, staged index, and exact Phase 5 commit range. |

Excluded without gaps: CONTEXT deferred fallback ideas and reviewed unrelated todos; REQUIREMENTS assigned to Phases 6–7; RESEARCH items explicitly conditional on future written permission.

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-04-SUMMARY.md` when done.
</output>
