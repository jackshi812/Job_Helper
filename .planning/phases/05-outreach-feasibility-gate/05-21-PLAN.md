---
phase: 05-outreach-feasibility-gate
plan: 21
type: execute
wave: 18
depends_on:
  - 05-19
files_modified:
  - scripts/outreach-feasibility/rights-gate.mjs
  - scripts/outreach-feasibility/rights-gate.test.mjs
  - scripts/outreach-feasibility/evidence-integrity.mjs
  - scripts/outreach-feasibility/evidence-integrity.test.mjs
  - scripts/outreach-feasibility/owner-authorization.mjs
  - scripts/outreach-feasibility/owner-authorization.test.mjs
  - scripts/outreach-feasibility/owner-checkpoint.mjs
  - scripts/outreach-feasibility/owner-checkpoint.test.mjs
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05

must_haves:
  truths:
    - "No rights matrix can authorize an eighth inclusive UTC calendar date; the final millisecond of date seven is the last admissible instant and the first millisecond of date eight fails closed."
    - "Every rights source ID is bound to its exact approved official HTTPS origin/path contract, so rehashing a substituted URL cannot turn edited evidence into permission."
    - "The review lifecycle accepts only the byte-pinned immutable-source 20-path, 7-critical/4-warning issues_found report followed by one exact ordered, duplicate-free live 20-path clean report with zero findings; mutable live 05-REVIEW.md is never the source fixture."
    - "Fresh owner verification recomputes the signed stopped-decision payload digest before invoking OpenSSH, rejects signature/trust substitution, and bounds legacy checkpoint stdin without changing the existing public proof bytes."
  artifacts:
    - path: "scripts/outreach-feasibility/rights-gate.mjs"
      provides: "Seven-inclusive-date authorization boundary and exact rights-source projection"
      exports:
        - "MAX_RIGHTS_VALIDITY_DAYS"
        - "OFFICIAL_RIGHTS_SOURCE_CONTRACTS"
        - "inspectRightsMatrix"
        - "validateRightsMatrix"
    - path: "scripts/outreach-feasibility/evidence-integrity.mjs"
      provides: "One canonical 20-file review inventory and exact issues_found-to-clean lifecycle validator"
      exports:
        - "PHASE_5_REVIEWED_PATHS"
        - "assertPhase5ReviewLifecycle"
    - path: "scripts/outreach-feasibility/owner-authorization.mjs"
      provides: "Pre-OpenSSH stopped-payload digest reconstruction and existing path/trust checks"
    - path: "scripts/outreach-feasibility/owner-checkpoint.mjs"
      provides: "Bounded raw stdin accumulation for historical integrity-only checkpoint input"
      exports:
        - "MAX_OWNER_CHECKPOINT_BYTES"
  key_links:
    - from: "scripts/outreach-feasibility/rights-gate.mjs"
      to: ".planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json"
      via: "structural inspection preserves the signed historical no-go while authorization eligibility enforces the seven-date/source contract"
      pattern: "inspectRightsMatrix|validateRightsMatrix"
    - from: "scripts/outreach-feasibility/evidence-integrity.mjs"
      to: ".planning/phases/05-outreach-feasibility-gate/05-REVIEW.md"
      via: "immutable source-head/byte-exact deterministic prior counters, issue IDs, and scope followed by the future live exact clean scope"
      pattern: "PHASE_5_REVIEWED_PATHS|assertPhase5ReviewLifecycle"
    - from: "scripts/outreach-feasibility/owner-authorization.mjs"
      to: ".planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json"
      via: "canonical stopped payload rehash before ssh-keygen verification"
      pattern: "stopped_decision_payload_sha256"
---

<objective>
Repair rights-time admission, canonical review scope, and owner-proof input validation without changing the already signed stopped-decision evidence.

Purpose: OUTR-04 and OUTR-05 cannot close while an eighth date, substituted rights origin, stale 15-file review contract, or internally inconsistent signed request can pass a trust boundary.
Output: Failing-first boundary/property tests, one exact 20-file review lifecycle, strict official-source bindings, stopped-payload recomputation, and bounded checkpoint input.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-19-SUMMARY.md
@scripts/outreach-feasibility/rights-gate.mjs
@scripts/outreach-feasibility/rights-gate.test.mjs
@scripts/outreach-feasibility/evidence-integrity.mjs
@scripts/outreach-feasibility/evidence-integrity.test.mjs
@scripts/outreach-feasibility/owner-authorization.mjs
@scripts/outreach-feasibility/owner-authorization.test.mjs
@scripts/outreach-feasibility/owner-checkpoint.mjs
@scripts/outreach-feasibility/owner-checkpoint.test.mjs

<interfaces>
Existing contracts to preserve:
- `evaluateRights(matrix, options)` returns zero-effect RIGHTS_NO_GO on every invalid/expired/prohibited/ambiguous state.
- `assertNoGoQualityReport(...)` validates the committed historical no-run report without authorizing a search.
- `assertPhase5ReviewLifecycle({ priorReviewBytes, finalReviewBytes })` is shared by residue and terminal consumers.
- `verifyOwnerAuthorization(...)` accepts only the canonical raw request/signature plus repository-local pinned public trust.
- The source review contract is the byte-pinned immutable source-head Git blob (or a byte-exact deterministic fixture with the same asserted SHA-256), with ordered 20-path scope and counters critical 7, warning 4, info 0, total 11. The mutable live `05-REVIEW.md` path is reserved for the future clean post-hook report.
</interfaces>
</context>

<prohibitions>
- Do not edit `05-RIGHTS-MATRIX.json`, `05-QUALITY-REPORT.json`, the owner authorization request/signature, or any trust artifact in this plan.
- Do not access, invoke, copy, expose, or commit `/Users/jackshi/.ssh/job_copilot_owner_signing`. Ephemeral test-owned keys may be created only inside temporary test directories and may not replace the repository public proof.
- Do not call provider/network endpoints, run the representative spike, create candidate fixtures/raw results, mutate production, add a schema push, or enable later phases.
- Do not invoke code review or overwrite `05-REVIEW.md`; the execute-phase post-hook remains its sole canonical writer.
- Do not invoke the live authoritative terminal audit, complete the phase, or mutate roadmap/state tracking.
- Do not stash, clean, reset, or alter unrelated owner work.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce seven inclusive dates and exact official rights sources</name>
  <read_first>
    - scripts/outreach-feasibility/rights-gate.mjs
    - scripts/outreach-feasibility/rights-gate.test.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json
    - .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json
    - .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json
    - .planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
  </read_first>
  <files>scripts/outreach-feasibility/rights-gate.mjs, scripts/outreach-feasibility/rights-gate.test.mjs</files>
  <behavior>
    - "RED: an all-ALLOW matrix researched on date one is eligible at 23:59:59.999Z on date seven and returns RIGHTS_NO_GO at 00:00:00.000Z on date eight."
    - "RED: deterministic property cases spanning month, year, and leap-day boundaries prove exactly seven inclusive UTC dates and reject every larger declared/effective horizon."
    - "RED: swapping source IDs/URLs, changing host/subdomain/path, adding query/fragment/credentials, or using path normalization aliases fails before PASS even after all SHA-256 values are recomputed."
    - "RED: the committed eight-date declaration remains inspectable as signed historical no-go evidence but is never authorization-eligible; its current no-run report still validates and all effect counters remain zero."
  </behavior>
  <action>
    Write the final-millisecond, first-millisecond, and deterministic property regressions first. Split finite structural/hash inspection from authorization eligibility: `inspectRightsMatrix` validates exact schema, canonical timestamps, hashes, source bindings, and operations; `validateRightsMatrix` additionally enforces an authorization horizon of at most `MAX_RIGHTS_VALIDITY_DAYS - 1` UTC date differences. `evaluateRights` catches any authorization-ineligible matrix and returns the existing zero-effect no-go.

    Preserve the already signed historical artifact bytes by allowing `assertNoGoQualityReport` to inspect and bind an authorization-ineligible matrix only when the computed verdict is RIGHTS_NO_GO, search authorization is false, status is NOT_RUN_RIGHTS_NO_GO, cases are empty, and all effect counters are zero. This compatibility path must have no PASS branch and must not weaken prospective authorization.

    Export one frozen `OFFICIAL_RIGHTS_SOURCE_CONTRACTS` map for the eight current source IDs. Each entry binds the exact credential-free HTTPS host and normalized pathname shown in the committed matrix; reject query, fragment, alternate port, alternate subdomain, trailing-path alias, cross-ID URL, or clause-family mismatch before an operation can be ALLOW. This implements D-09, D-10, and D-12 without changing the matrix/request/signature payload.
  </action>
  <acceptance_criteria>
    - Date seven's last millisecond is the absolute authorization boundary and date eight's first millisecond is denied across all generated date cases.
    - Rehashed source substitution cannot produce PASS.
    - The committed rights/no-run artifacts remain byte-identical and validate only as stopped historical no-go evidence.
    - No test reads a provider key or performs a network request.
  </acceptance_criteria>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/rights-gate.test.mjs scripts/outreach-feasibility/dormant/spike-runner.test.mjs</automated>
  </verify>
  <done>The rights boundary admits no eighth inclusive date or substituted official source, while the existing signed no-go remains inspectable and effect-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require the real exact 20-file review lifecycle</name>
  <read_first>
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - scripts/outreach-feasibility/evidence-integrity.test.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
    - .planning/phases/05-outreach-feasibility-gate/05-19-SUMMARY.md
  </read_first>
  <files>scripts/outreach-feasibility/evidence-integrity.mjs, scripts/outreach-feasibility/evidence-integrity.test.mjs</files>
  <behavior>
    - "RED: the byte-pinned immutable-source 20-file review with status issues_found, counters 7/4/0/11, CR-01 through CR-07, and WR-01 through WR-04 is the only accepted prior review."
    - "RED: the final review must equal the same 20-path array exactly in order and count, contain no duplicates/extras/omissions, have clean status, zero counters, and no CR/BL/WR/IN body finding IDs."
    - "RED: reordered, duplicate, omitted authorization/trust, extra, obsolete 15-file, counter/body mismatch, BL critical, and IN info fixtures fail closed."
    - "GREEN: one pinned issues_found-to-synthetic-clean lifecycle succeeds without obtaining the prior bytes from or rewriting the mutable live review."
  </behavior>
  <action>
    Replace the obsolete duplicated review list with one exported frozen `PHASE_5_REVIEWED_PATHS` equal byte-for-byte and order-for-order to the 20 paths in the immutable source issues report, including `owner-authorization.mjs`, its test, and all three public trust artifacts. Require exact deep equality for both prior and final arrays, plus explicit count and uniqueness checks; coverage-as-a-superset is insufficient.

    Update the immutable prior-review contract to the pinned `issues_found` report: critical 7, warning 4, info 0, total 11; CR-01 through CR-07 and WR-01 through WR-04. Obtain those prior bytes only from an immutable source-head Git blob or an embedded byte-exact deterministic fixture whose SHA-256 is asserted against that blob. Never read the mutable live `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md` to create the issues fixture; that live path is reserved for the future post-hook clean report. Parse CR and BL as critical, WR as warning, and IN as info; require unique IDs and reconcile per-tier/body counts with frontmatter. The clean report must carry zero counters and no recognized finding ID.

    Add a complete-suite-compatible regression in an isolated repository where the live `05-REVIEW.md` is already exact clean 20/20 while the prior issues bytes still come from the immutable source blob/deterministic fixture. Prove lifecycle validation succeeds without rewriting or treating the live clean report as the prior report.
  </action>
  <acceptance_criteria>
    - One canonical exported ordered list contains exactly the 20 required source/test/trust paths.
    - The pinned source-head issues_found report is admitted as immutable prior lineage without reading mutable live review bytes.
    - Only exact ordered unique 20/20 clean zero-finding final bytes are admitted.
    - A repository whose live `05-REVIEW.md` is already clean passes the lifecycle regression using the separate pinned prior source.
    - Every obsolete/superset/reordered/duplicate/body-count drift fixture is rejected.
  </acceptance_criteria>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/evidence-integrity.test.mjs</automated>
  </verify>
  <done>The shared review lifecycle models the real post-hook transition and cannot accept stale scope, hidden findings, or merely inclusive coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close signed-request and checkpoint input gaps</name>
  <read_first>
    - scripts/outreach-feasibility/owner-authorization.mjs
    - scripts/outreach-feasibility/owner-authorization.test.mjs
    - scripts/outreach-feasibility/owner-checkpoint.mjs
    - scripts/outreach-feasibility/owner-checkpoint.test.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json
    - .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig
    - scripts/outreach-feasibility/trust/owner-trust-anchor.json
    - scripts/outreach-feasibility/trust/phase-05-owner.pub
    - scripts/outreach-feasibility/trust/phase-05-owner.allowed_signers.txt
  </read_first>
  <files>scripts/outreach-feasibility/owner-authorization.mjs, scripts/outreach-feasibility/owner-authorization.test.mjs, scripts/outreach-feasibility/owner-checkpoint.mjs, scripts/outreach-feasibility/owner-checkpoint.test.mjs</files>
  <behavior>
    - "RED: a canonical, self-rehashed, validly signed request whose stopped-decision field changes without the stopped payload digest changing fails before ssh-keygen verification."
    - "RED: request, signature, trust anchor, public key, or allowed-signers symlink/writable/path/content substitution fails closed and emits no authenticated proof."
    - "RED: checkpoint stdin exceeding MAX_OWNER_CHECKPOINT_BYTES is rejected while streaming before complete accumulation and creates no receipt."
    - "GREEN: the existing repository public request/signature verifies with the pinned principal, namespace, fingerprint, active anchor, and stopped payload; all public bytes remain unchanged."
  </behavior>
  <action>
    In the request parser, reconstruct the canonical stopped-decision payload from the finite signed fields and compare its SHA-256 with `stopped_decision_payload_sha256` before spawning OpenSSH. Keep the existing full evidence comparison in `assertOwnerAuthorizationRequest`; this new parser check prevents a bare signature verifier from returning authenticated proof for an internally inconsistent request. Expand substitution tests across every public trust path and preserve exact regular-file/non-writable/canonical-basename checks. This authenticates the stopped, zero-effect decision required by D-09, D-10, D-12, and D-13.

    Export a small fixed `MAX_OWNER_CHECKPOINT_BYTES` derived from the finite expected response envelope. Change raw stdin reading to count bytes per chunk, abort immediately on overflow, and never concatenate an oversized stream. Test overflow, exact limit, split multibyte chunks, early child exit, zero receipt creation, and unchanged canonical success. Per D-13, this checkpoint remains integrity-only history; do not create a new owner action.
  </action>
  <acceptance_criteria>
    - Stopped-payload digest mismatch fails before OpenSSH.
    - Every trust/signature path substitution remains fail-closed.
    - Oversized stdin is rejected incrementally with no receipt artifact.
    - Existing public authorization still verifies and all five public proof/trust files are byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/owner-authorization.test.mjs scripts/outreach-feasibility/owner-checkpoint.test.mjs</automated>
  </verify>
  <done>Owner authentication proves the internally consistent stopped payload under exact public trust, and historical checkpoint input is bounded without a new signature or checkpoint.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

- Export `OFFICIAL_RIGHTS_SOURCE_CONTRACTS`, `inspectRightsMatrix(...)`, and the existing strict `validateRightsMatrix(...)` from `rights-gate.mjs`.
- `OFFICIAL_RIGHTS_SOURCE_CONTRACTS` fields per source ID: `protocol`, `hostname`, `pathname`, and `clause_family`.
- Export `PHASE_5_REVIEWED_PATHS` from `evidence-integrity.mjs`; it contains the exact ordered 20 paths from the byte-pinned immutable source review.
- Tests obtain the 7/4/0/11 issues report only from the immutable source-head Git blob or its byte-exact SHA-256-asserted deterministic fixture; the live review path is clean-output-only.
- Review lifecycle recognizes body IDs `CR-NN`, `BL-NN`, `WR-NN`, and `IN-NN` and reconciles them to `critical`, `warning`, `info`, and `total`.
- Export `MAX_OWNER_CHECKPOINT_BYTES` from `owner-checkpoint.mjs`.
- No new persistent path, CLI flag, evidence field, signature, trust identity, external dependency, or schema file is introduced.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| rights JSON → search authorization | Rehashed local evidence must not extend time or substitute official origins. |
| source review → post-hook review | Only the real exact 20-file issues_found report may transition to the exact clean report. |
| raw request/signature/trust → authenticated proof | Internal payload consistency and exact public trust must precede OpenSSH acceptance. |
| stdin → local checkpoint parser | Untrusted local input must be bounded before accumulation. |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-21-01 | Elevation of Privilege | Rights validity | high | mitigate | Strict seven-inclusive-date authorization window with boundary/property tests and zero-effect denial on overlong matrices. |
| T-05-21-02 | Spoofing | Rights official source | high | mitigate | Exact source-ID to HTTPS host/path/clause projection; reject rehashed substitutions before PASS. |
| T-05-21-03 | Tampering | Canonical review scope | high | mitigate | Exact ordered unique 20-path equality, exact real prior counters/IDs, and zero-finding clean final report. |
| T-05-21-04 | Repudiation | Review finding body | medium | mitigate | Parse CR/BL/WR/IN IDs and reconcile unique per-tier body inventory to frontmatter counts. |
| T-05-21-05 | Spoofing | Owner signature/trust | critical | mitigate | Reconstruct signed stopped payload before OpenSSH and retain exact regular non-writable canonical public trust files. |
| T-05-21-06 | Denial of Service | Checkpoint stdin | medium | mitigate | Fixed byte ceiling enforced during streaming before concatenation. |
| T-05-21-SC | Tampering | Dependency supply chain | low | accept | Node built-ins and system OpenSSH only; no package installation occurs. |
</threat_model>

<verification>
Run all three focused gates with network/provider credentials absent. Tests may use temporary test-owned SSH keys, but must never access the owner's private key or modify the committed public proof. Do not invoke the live review or terminal audit.
</verification>

<success_criteria>
- No eighth inclusive rights date can authorize search.
- Rehashed official-source substitution cannot produce PASS.
- The exact real 20-file issues_found-to-clean lifecycle is the only accepted review transition.
- Signed stopped-payload inconsistency and public trust substitution fail before authentication.
- Existing public owner proof and stopped zero-effect artifacts remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-21-SUMMARY.md` with exactly the eight modified source/test paths in `key-files`. Do not include `05-REVIEW.md`, public proof files, trust files, tracking documents, or terminal-audit claims as modified outputs.
</output>
