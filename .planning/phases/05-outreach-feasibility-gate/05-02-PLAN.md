---
phase: 05-outreach-feasibility-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/outreach-feasibility/dormant/spike-runner.mjs
  - scripts/outreach-feasibility/dormant/spike-runner.test.mjs
  - scripts/outreach-feasibility/decision-evidence.mjs
  - scripts/outreach-feasibility/decision-evidence.test.mjs
  - scripts/outreach-feasibility/residue-check.mjs
  - scripts/outreach-feasibility/residue-check.test.mjs
  - .planning/phases/05-outreach-feasibility-gate/05-DECISION.json
autonomous: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "The eight-case D-01 through D-08 quality contract remains explicit but dormant; the committed `RIGHTS_NO_GO` matrix reaches no injected effect."
    - "The dormant runner evaluates all required written permissions before secret loading, corpus creation, request construction, or provider fetch."
    - "The decision record remains `PENDING_OWNER_ATTESTATION` with `owner_attestation: null`; the agent cannot pre-attest it."
    - "Local/Git residue is proven across current worktree files, staged index blobs, and every Phase 5 commit after the committed plan baseline without following symlinks, scanning broad user directories, echoing sensitive matches, or claiming provider-side deletion."
  artifacts:
    - path: "scripts/outreach-feasibility/dormant/spike-runner.mjs"
      provides: "Import-only, injected conditional spike contract preserving D-01 through D-08 and D-14 through D-16"
      exports:
        - SPIKE_CONTRACT
        - runConditionalSpike
    - path: "scripts/outreach-feasibility/decision-evidence.mjs"
      provides: "Stable decision-payload digest, exact no-go owner literal, pending/final record validation"
      exports:
        - decisionPayload
        - exactOwnerAttestation
        - assertDecisionRecord
    - path: "scripts/outreach-feasibility/residue-check.mjs"
      provides: "Allowlisted worktree, Git-index, and Phase 5 commit-range residue scanning with exact zero-record validation"
      exports:
        - scanOwnedSurfaces
        - buildZeroResidueRecord
        - assertZeroResidueRecord
    - path: ".planning/phases/05-outreach-feasibility-gate/05-DECISION.json"
      provides: "No-go decision contract awaiting a real owner checkpoint"
  key_links:
    - from: "scripts/outreach-feasibility/dormant/spike-runner.mjs"
      to: "scripts/outreach-feasibility/rights-gate.mjs"
      via: "runConditionalSpike calls evaluateRights before every injected effect"
      pattern: "evaluateRights"
    - from: ".planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json"
      to: ".planning/phases/05-outreach-feasibility-gate/05-DECISION.json"
      via: "decisionPayload and exactOwnerAttestation bind the current rights digest"
      pattern: "rights_evidence_sha256"
    - from: "scripts/outreach-feasibility/residue-check.mjs"
      to: ".planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json"
      via: "Plan 04 writes the final record only after the owner checkpoint"
      pattern: "buildZeroResidueRecord"
    - from: ".planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md"
      to: "scripts/outreach-feasibility/residue-check.mjs"
      via: "the latest commit containing the Phase 5 plan file is the immutable pre-execution baseline for the inspected baseline..HEAD commit range"
      pattern: "phase_plan_baseline_sha"
  prohibitions:
    - "No real or controlled application fixture, raw result, candidate URL, query, cache, API key, or provider request is created."
    - "No quality pass/fail outcome is inferred from `NOT_RUN_RIGHTS_NO_GO`."
    - "No dormant module is connected to a CLI, package script, production import, browser, Supabase function, or scheduler."
    - "No pending decision contains an owner attestation or authorizes Phase 6, Phase 7, or production outreach."
---

<objective>
Preserve the exact conditional quality-spike contract without activating it, and prepare tamper-evident owner-decision and post-decision residue tooling.

Purpose: D-01 through D-08 and D-14 through D-16 remain trackable as a dormant, offline-tested contract while D-12 blocks execution; D-13 requires an explicit owner no-go; D-17 requires post-decision zero-residue proof.
Output: An import-only conditional runner, a pending exact-attestation decision record, and an allowlisted local/Git residue verifier—implemented with Node built-ins and no provider effect.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md
@.planning/phases/05-outreach-feasibility-gate/05-01-SUMMARY.md
@scripts/outreach-feasibility/rights-gate.mjs
@scripts/outreach-feasibility/rights-gate.test.mjs
@scripts/run-phase-03-9-rollout.mjs
@scripts/run-phase-03-9-rollout.test.mjs
@scripts/verify-phase-03-9-hosted.mjs
@scripts/verify-phase-03-9-hosted.test.mjs
@scripts/verify-expected-red.mjs

<interfaces>
From `scripts/outreach-feasibility/rights-gate.mjs` created by Plan 05-01:

- `REQUIRED_OPERATIONS: readonly string[]`
- `sha256Json(value): string` — canonical JSON plus final newline
- `validateRightsMatrix(matrix, { now? }): ValidatedRightsMatrix`
- `evaluateRights(matrix, { now? }): { status: 'PASS' | 'RIGHTS_NO_GO', search_authorized: boolean, rights_evidence_sha256: string, quality_status: 'READY' | 'NOT_RUN_RIGHTS_NO_GO', provider_call_count: number, fixture_count: number, raw_result_count: number, production_mutation_count: number }`
- `assertNoGoQualityReport(matrix, report): NoGoQualityReport`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the unreachable eight-case quality-spike contract</name>
  <read_first>
    - `scripts/outreach-feasibility/dormant/spike-runner.mjs` (create if absent; this task owns it)
    - `scripts/outreach-feasibility/dormant/spike-runner.test.mjs` (create if absent; this task owns it)
    - `scripts/outreach-feasibility/rights-gate.mjs`
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md`
    - `scripts/run-phase-03-9-rollout.test.mjs`
    - `web/tests/branded-connectors.integration.test.ts`
    - `supabase/functions/_shared/connectors.ts`
  </read_first>
  <files>scripts/outreach-feasibility/dormant/spike-runner.mjs, scripts/outreach-feasibility/dormant/spike-runner.test.mjs</files>
  <behavior>
    - Real committed matrix → `NOT_RUN_RIGHTS_NO_GO` and zero invocations of `readSecret`, `createCorpus`, `buildRequest`, and `fetchImpl`.
    - Any one required row missing, stale, digest-drifted, `PROHIBIT`, `AMBIGUOUS`, or not exactly `ALLOW` → the same zero-effect no-run result.
    - Synthetic all-`ALLOW` matrix without a digest-bound `ACCEPT_RIGHTS` authorization → zero effects.
    - Synthetic all-`ALLOW` matrix plus matching structured authorization → injected mocks may run, proving the conditional boundary without a network request or real data.
  </behavior>
  <action>
Write the denial-path tests first, then export a deeply frozen `SPIKE_CONTRACT` without containing any real application or candidate data. Encode every dormant rule explicitly: D-01: eight total cases; D-02: six real cases spanning at least three companies, risk/finance and software/technical roles, and at most two cases per company; D-03: one known-positive and one known-negative control; D-04: disposable-corpus-only input with no production query or mutation; D-05: at least four of six real passes plus positive-found and negative-rejected; D-06: qualifying current-company and meaningful-role-fit evidence while shared history is optional; D-07: maximum three physical calls per case; D-08: maximum one retry inside that same cap and persistent provider/evidence failure classified `coverage_unknown`; D-14: exact minimal fixture fields; D-15: raw responses exist only transiently for labeling/owner review and are removed before the sanitized report; and D-16: the anonymous committed-report allowlist.

Export `runConditionalSpike({ matrix, spikeAuthorization, now, readSecret, createCorpus, buildRequest, fetchImpl })`. Its first operation is `evaluateRights`. It returns the exact no-run record immediately unless all seven required matrix rows are current, digest-valid, and `ALLOW` and `spikeAuthorization` equals structured `ACCEPT_RIGHTS` bound to that exact digest. Only after both checks may it call the injected functions in the listed order.

Keep the module import-only: no CLI main block, provider origin, environment lookup, request body, real query wording, filesystem path, or package/production import. Synthetic tests may reach mocks only. The real matrix and every denial mutation must keep every injected effect count at zero, preserving D-12 and the binding `RIGHTS_NO_GO`.

Follow the injected no-effect/no-fetch invariant from `web/tests/branded-connectors.integration.test.ts`: for the real matrix and every denial mutation, assert that none of the injected effect callbacks—including `fetchImpl`—is called, rather than trusting only the returned counters.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/dormant/spike-runner.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`SPIKE_CONTRACT` mechanically encodes every quantitative and semantic constraint from D-01 through D-08 plus the data boundary from D-14 through D-16.</criterion>
    <criterion>The real-matrix test records zero secret, corpus, request-build, and fetch invocations.</criterion>
    <criterion>Every single denial mutation remains zero-effect; only a synthetic all-`ALLOW`, digest-bound authorization reaches injected mocks.</criterion>
    <criterion>No code outside `scripts/outreach-feasibility/dormant/` imports this module.</criterion>
  </acceptance_criteria>
  <done>The complete quality-spike contract is preserved and offline-testable but unreachable under the current evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prepare the exact evidence-bound pending owner decision</name>
  <read_first>
    - `scripts/outreach-feasibility/decision-evidence.mjs` (create if absent; this task owns it)
    - `scripts/outreach-feasibility/decision-evidence.test.mjs` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md`
    - `scripts/verify-phase-03-9-hosted.mjs`
    - `scripts/verify-phase-03-9-hosted.test.mjs`
  </read_first>
  <files>scripts/outreach-feasibility/decision-evidence.mjs, scripts/outreach-feasibility/decision-evidence.test.mjs, .planning/phases/05-outreach-feasibility-gate/05-DECISION.json</files>
  <behavior>
    - Before owner input → exact status `PENDING_OWNER_ATTESTATION`, `owner_attestation: null`, and all authorization flags false.
    - Pending and accepted no-go records → exact neutral `redesign_handoff_options` of `user-pasted LinkedIn URLs`, `non-LinkedIn public professional profiles`, and `stopping outreach`, with `redesign_selection: null`.
    - Exact owner literal derived from the current rights digest → eligible for later `RIGHTS_NO_GO_ACCEPTED`; punctuation, case, whitespace, or digest drift is rejected.
    - Any `GO`, quality-run claim, nonzero effect count, missing disabled/blocked field, selected or reordered redesign option, stale evidence, or unknown field → validation failure.
    - Adding owner text before the blocking checkpoint → pending-record validation failure.
  </behavior>
  <action>
Write tests first, then implement `decisionPayload`, `exactOwnerAttestation`, and `assertDecisionRecord` using `sha256Json` from the rights module. Avoid a digest cycle: `decisionPayload` returns a stable projection containing the rights and no-run quality digests/statuses, four zero counters, `search_authorized: false`, `production_outreach_enabled: false`, `outreach_milestone_status: STOPPED_RIGHTS_NO_GO`, and explicit `phase_6_authorized: false` / `phase_7_authorized: false`; `decision_contract_sha256` hashes only that projection.

Per D-13, include the exact ordered `redesign_handoff_options` array `["user-pasted LinkedIn URLs", "non-LinkedIn public professional profiles", "stopping outreach"]` and `redesign_selection: null` in that stable projection. These are neutral, separately scoped owner choices only: this phase selects, recommends, or implements none. Tests must reject a missing, added, reordered, or selected option.

Derive exactly this required owner literal, substituting only the actual rights digest: `I ACCEPT PHASE 5 RIGHTS NO-GO &lt;rights_evidence_sha256&gt;; production outreach search remains disabled; the outreach milestone stops pending a separately scoped owner decision.` The script may compute and print the required literal, but it must never copy it into `owner_attestation`.

Create `05-DECISION.json` with exact status `PENDING_OWNER_ATTESTATION`, `owner_attestation: null`, `owner_attestation_source: null`, `zero_residue_sha256: null`, the stable decision-contract digest, the exact required literal, the three neutral handoff options, and a null selection. D-13 forbids an automatic fallback. Neither pending nor accepted no-go states may authorize search.

Add an import-safe CLI with `--assert-decision`, `--matrix`, `--quality-report`, `--decision`, `--print-required-attestation`, and optional `--require-accepted`. The current plan validates pending state without `--require-accepted`; Plan 04 uses it only after the owner checkpoint.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/decision-evidence.test.mjs &amp;&amp; env -u TAVILY_API_KEY node scripts/outreach-feasibility/decision-evidence.mjs --assert-decision --matrix .planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json --quality-report .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json --decision .planning/phases/05-outreach-feasibility-gate/05-DECISION.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The committed decision is pending, carries null owner/residue fields, and has false search/production/later-phase authorization fields.</criterion>
    <criterion>The stable record carries exactly the three D-13 redesign categories in order and a null selection; tests reject any implicit choice or implementation state.</criterion>
    <criterion>Tests reject fabricated owner input, one-character digest drift, punctuation/case/whitespace drift, `GO`, a quality-run claim, and every nonzero effect count.</criterion>
    <criterion>The stable decision payload has no circular dependency on the later residue digest.</criterion>
    <criterion>The CLI can print the required literal but never persists it as owner-supplied input.</criterion>
  </acceptance_criteria>
  <done>The exact no-go decision contract is ready for owner review while remaining mechanically impossible to pre-attest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement narrow local and Git zero-residue verification</name>
  <read_first>
    - `scripts/outreach-feasibility/residue-check.mjs` (create if absent; this task owns it)
    - `scripts/outreach-feasibility/residue-check.test.mjs` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md`
    - `scripts/verify-expected-red.mjs`
    - `scripts/run-phase-03-9-rollout.mjs`
    - `scripts/run-phase-03-9-rollout.test.mjs`
  </read_first>
  <files>scripts/outreach-feasibility/residue-check.mjs, scripts/outreach-feasibility/residue-check.test.mjs</files>
  <behavior>
    - Clean no-go evidence and no verifier-owned transient roots → exact zero-residue `PASS`.
    - Nested raw-result/secret-like fields, candidate-profile paths, unexpected transient survivors, symlinks, or nonzero effect counters → failure without echoing the rejected value.
    - A forbidden staged blob that is absent from the current worktree → failure attributed only to the sanitized `index` surface.
    - A forbidden blob committed after the Phase 5 plan baseline and later removed from the current tree → failure attributed only to the sanitized `phase_commit_range` surface.
    - Workspace root, home root, filesystem root, empty root, or unapproved scan target → refusal before traversal or deletion.
    - Record scope remains `LOCAL_AND_GIT_ONLY`; provider-side retention is explicitly `NOT_ASSERTED`.
  </behavior>
  <action>
Write temp-owned tests first, with every fixture and temporary Git repository created by `mkdtemp` and removed in `finally`. Implement `scanOwnedSurfaces`, `buildZeroResidueRecord`, and `assertZeroResidueRecord` using `node:fs/promises`, `node:path`, `node:os`, `node:crypto`, and `execFile` from `node:child_process`; invoke Git without a shell and never interpolate a path or revision into a command string.

Define two literal Git pathspec roots: `scripts/outreach-feasibility/` and the generated `*.json` artifacts directly under `.planning/phases/05-outreach-feasibility-gate/`; exclude planning Markdown because the research and plans legitimately name provider terms and candidate-path validators. Resolve `phase_plan_baseline_sha` with `git log -1 --format=%H -- .planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md`, require one full commit SHA, and fail if that commit is not an ancestor of `HEAD`. This committed plan baseline is the pre-execution anchor, so the inspected Git revision range is `phase_plan_baseline_sha..HEAD`, covering every Phase 5 implementation commit without relying on dates or commit-message matching.

Inspect three concrete Git/local surfaces. For the current worktree, invoke `git status --porcelain=v1 -z --untracked-files=all --` with the two literal pathspec arguments, plus bounded filesystem reads of expected generated JSON and named verifier-owned transient prefixes. For the index, invoke `git diff --cached --name-only -z --` with those pathspec arguments and inspect each staged generated-JSON blob through the index object using `git show` with the colon-prefixed relative path, handling staged deletions without falling back to the worktree. For the Phase 5 commit range, invoke `git rev-list --reverse phase_plan_baseline_sha..HEAD --` with the same pathspec arguments, enumerate each commit's bounded tree paths, and inspect the generated-JSON blob at that exact commit rather than the current file. Across all three surfaces, reject unexpected transient/corpus/raw-result paths, symlinks in the worktree, and forbidden candidate/raw/secret fields or candidate-style profile paths in generated JSON. Allow the exact planned source/test paths and sanitized JSON artifact names. Do not recursively scan or delete the workspace root, home root, filesystem root, or any unresolved path.

Tests must initialize a temporary repository with local test-only Git identity and a clean baseline commit. Add one case that stages a generated-JSON leak, then replaces the worktree copy with sanitized content so only index inspection can catch it. Add another that commits a generated-JSON leak, commits its removal, and proves the clean current worktree/index still fail because the leak exists in the baseline-to-HEAD range. Assert errors and CLI JSON expose only surface labels, relative paths, counts, and digests—never the rejected value.

The future record must include four zero effect counters, `forbidden_hit_count: 0`, `unexpected_survivor_count: 0`, `symlink_count: 0`, exact scanned-root inventory, `scope: LOCAL_AND_GIT_ONLY`, and `provider_side_retention: NOT_ASSERTED`. Add exact `git_surfaces` evidence: `worktree` status/path/blob counts; `index` staged-path/blob counts; and `phase_commit_range` with `base_sha`, `head_sha`, commit/path/blob counts. It binds the rights/quality/decision-contract digests; the final decision later binds the residue digest, avoiding a cycle.

Add an import-safe CLI with `--assert-zero`, `--repo-root`, `--phase-dir`, `--phase-plan`, `--decision`, and `--record`. This task tests the writer/validator but does not create the final record; D-17 requires Plan 04 to run it immediately after the owner checkpoint.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/residue-check.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Tests pass for a clean no-go surface and fail for nested forbidden data, a candidate profile path, a symlink, an unexpected file, each unsafe root, and each nonzero counter.</criterion>
    <criterion>Temporary-repository tests independently catch a staged leak hidden by a clean worktree and a committed leak removed from the current tree, proving index and baseline-to-HEAD history are inspected rather than inferred.</criterion>
    <criterion>All test-owned temporary directories are removed in `finally`, including failure cases.</criterion>
    <criterion>No error or JSON output includes the rejected secret/raw/candidate value; output is limited to sanitized surface labels, relative paths, counts, and digests.</criterion>
    <criterion>The record schema inventories worktree, index, and the exact Phase 5 commit range, limits its claim to those controlled local/Git surfaces, and makes no provider-side deletion claim.</criterion>
  </acceptance_criteria>
  <done>The post-owner zero-residue assertion is deterministic, narrowly scoped, safe against broad traversal/deletion, and ready for Plan 04.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/outreach-feasibility/dormant/spike-runner.mjs`
  - Constant/function: `SPIKE_CONTRACT`, `runConditionalSpike`.
  - No CLI flag or active import is created.
- `scripts/outreach-feasibility/dormant/spike-runner.test.mjs`.
- `scripts/outreach-feasibility/decision-evidence.mjs`
  - Functions: `decisionPayload`, `exactOwnerAttestation`, `assertDecisionRecord`.
  - CLI flags: `--assert-decision`, `--matrix`, `--quality-report`, `--decision`, `--print-required-attestation`, `--require-accepted`.
- `scripts/outreach-feasibility/decision-evidence.test.mjs`.
- `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`
  - Core fields: `schema_version`, `phase`, `status`, `rights_status`, `quality_status`, `search_authorized`, `production_outreach_enabled`, `outreach_milestone_status`, `phase_6_authorized`, `phase_7_authorized`, four effect counters, evidence digests, `decision_contract_sha256`, `required_owner_attestation`, `owner_attestation`, `owner_attestation_source`, `zero_residue_sha256`, exact `redesign_handoff_options`, and null `redesign_selection`.
- `scripts/outreach-feasibility/residue-check.mjs`
  - Functions: `scanOwnedSurfaces`, `buildZeroResidueRecord`, `assertZeroResidueRecord`.
  - CLI flags: `--assert-zero`, `--repo-root`, `--phase-dir`, `--phase-plan`, `--decision`, `--record`.
- `scripts/outreach-feasibility/residue-check.test.mjs`.
- `.planning/phases/05-outreach-feasibility-gate/05-02-SUMMARY.md` (executor output).
- No fixture, raw-result, cache, package, class, database object, production function, UI route, or API route is created.

## Schema Push Gate

No schema push is required. This plan contains no `supabase/migrations/*.sql` path and all work remains in local scripts/tests and planning evidence.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rights evaluator → dormant injected effects | All permission and evidence checks must complete before any secret/corpus/request/fetch callback. |
| Evidence files → decision contract | Mutable or malformed evidence could forge a stale or widened decision. |
| Owner checkpoint → final decision | The required literal may be computed by code, but only the owner can supply the attestation. |
| Worktree/index/Phase 5 commit range → residue record | Third-party/candidate residue must be detected in current, staged, and committed forms without unsafe broad traversal or disclosure. |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Tampering / Elevation | `runConditionalSpike` | high | mitigate | `evaluateRights` plus digest-bound structured authorization execute before all four injected effects; every denial path asserts zero calls. |
| T-05-02-02 | Information Disclosure | Secret boundary | high | mitigate | The dormant module has no environment lookup or provider implementation; the real matrix prevents the injected secret callback from running. |
| T-05-02-03 | Information Disclosure | Evidence/residue files and Git objects | high | mitigate | Exact schemas, recursive forbidden-data checks, transient-prefix inventory, explicit worktree/index/baseline-to-HEAD inspection, temporary staged/committed leak tests, sanitized errors, and empty no-run cases prevent raw response persistence. |
| T-05-02-04 | Spoofing / Tampering | `05-DECISION.json` | high | mitigate | Pending state requires null attestation; exact punctuation/case/digest matching and a blocking owner checkpoint prevent pre-attestation and stale reuse. |
| T-05-02-05 | Tampering | Decision/evidence hashes | high | mitigate | One shared canonical hash, validity cutoff, stable decision payload projection, and cross-artifact digest assertions detect evidence/source drift without a digest cycle. |
| T-05-02-06 | Elevation | Dormant activation / redesign handoff | high | mitigate | Every required row must be current/digest-valid/`ALLOW` and authorization must match the same digest before any injected callback; the three neutral D-13 options remain exact with a null selection, so current evidence and the handoff authorize nothing. |
| T-05-02-SC | Tampering | Dependency supply chain | low | accept | No package is installed; only Node built-ins and existing repository analogs are used. |
</threat_model>

<verification>
Run the three focused `node:test` files with the provider key absent. Confirm the real matrix yields zero injected effects, the decision stays pending with null owner attestation, and the residue tests reject every forbidden/unsafe mutation including isolated staged-index and removed-but-committed leaks in temporary repositories.
</verification>

<success_criteria>
- D-01 through D-08 remain explicit and testable without executing a live search.
- D-14 through D-16 are encoded as conditional data/report contracts while current artifacts contain no application or candidate data.
- D-13 is represented by a pending, exact, evidence-bound no-go contract with both later-phase authorization fields false; dependent Plan 05-04 performs the supported GSD structural stop only after owner acceptance and residue proof.
- D-17 has a tested post-checkpoint zero-residue mechanism ready for execution.
- No key, network, package, production, schema, UI, or database effect occurs.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-02-SUMMARY.md` when done.
</output>
