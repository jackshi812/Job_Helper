---
phase: 05-outreach-feasibility-gate
plan: 16
type: execute
wave: 14
depends_on:
  - 05-15
files_modified:
  - .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig
autonomous: false
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "Only the owner invokes the private key to sign the exact request file under namespace job-copilot-phase-05-owner-v1."
    - "The repository receives only the resulting armored detached SSHSIG bytes and never reads or copies the private key."
    - "The signature passes exact principal jackshi812, namespace job-copilot-phase-05-owner-v1, key/fingerprint, exact-byte, nonce, time-window, and revocation checks before downstream execution resumes."
  artifacts:
    - path: .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig
      provides: "Owner-produced armored OpenSSH detached signature"
  key_links:
    - from: .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig
      to: .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json
      via: "ssh-keygen -Y verify over exact request bytes"
      pattern: "BEGIN SSH SIGNATURE"
---

<objective>
Obtain and verify the owner's fresh detached signature without exposing the private key to the repository or agent.

Purpose: Establish authenticated owner acceptance of the exact stopped Phase 5 decision bytes, explicitly non-retroactive to the old receipt.
Output: One canonical armored SSHSIG file verified against the pinned owner trust anchor.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json
@scripts/outreach-feasibility/trust/owner-trust-anchor.json
@scripts/outreach-feasibility/owner-authorization.mjs
@scripts/outreach-feasibility/trust/phase-05-owner.pub
@scripts/outreach-feasibility/trust/phase-05-owner.allowed_signers.txt
</context>

<prohibitions>
- The agent/executor must never run a command that reads, opens, copies, hashes, stats, signs with, or changes `/Users/jackshi/.ssh/job_copilot_owner_signing`.
- Do not substitute `git commit -S`, a commit signature, Git config, chat text, pasted approval text, the old receipt, or a newly generated key.
- Do not modify the request after signing or normalize/re-emit its bytes for verification.
- Do not accept base64 alone, JSON-wrapped proof, a public-key echo, multiple signatures, malformed armor, or a signature produced for another principal/namespace/message.
- Do not regenerate accepted evidence, call a provider/network, or invoke the authoritative live terminal audit.
</prohibitions>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Owner signs the exact fresh request with the private key</name>
  <files>.planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig</files>
  <read_first>
    - .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json
    - scripts/outreach-feasibility/trust/owner-trust-anchor.json
    - scripts/outreach-feasibility/owner-authorization.mjs
  </read_first>
  <action>
    Before pausing, the executor runs only the public request assertion from Plan 05-15 and confirms the `.sig` destination is absent. Then print the exact command below once. The owner runs it locally in their own terminal; the executor must not run it or inspect the private-key path. Accept only the file produced at the exact `.sig` destination. After the owner resumes, run the automated public verification below. If any check fails, stop with `OWNER_SIGNATURE_INVALID`; do not request raw private material, rewrite the request, or continue to Plan 05-17.
  </action>
  <instructions>
    Run exactly:

    `/usr/bin/ssh-keygen -Y sign -f /Users/jackshi/.ssh/job_copilot_owner_signing -n job-copilot-phase-05-owner-v1 /Users/jackshi/Desktop/Job_Copilot/.planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json`

    This writes `/Users/jackshi/Desktop/Job_Copilot/.planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig`. Do not paste the private key or its contents.
  </instructions>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node scripts/outreach-feasibility/owner-authorization.mjs --verify-authorization --request .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json --signature .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig --trust-anchor scripts/outreach-feasibility/trust/owner-trust-anchor.json --public-key scripts/outreach-feasibility/trust/phase-05-owner.pub --allowed-signers scripts/outreach-feasibility/trust/phase-05-owner.allowed_signers.txt</automated>
    <human-check>Confirm you personally ran the displayed command and supplied only the generated detached `.sig` file.</human-check>
  </verify>
  <resume-signal>Type `signed` after the exact `.sig` file exists, or `abort` to keep OWNER_AUTHENTICATION_REQUIRED.</resume-signal>
  <done>The owner-produced detached signature verifies over the exact request bytes under the pinned principal, namespace, ED25519 key, time window, and active trust state.</done>
</task>

</tasks>

<artifacts>
- `05-OWNER-AUTHORIZATION-REQUEST.json.sig` — fresh owner-produced public signature bytes; no private material.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| owner private key → repository | Only a detached public signature may cross; private material never does. |
| owner action → executor continuation | Downstream plans resume only after exact public verification succeeds. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-16-01 | Information Disclosure | Private signing key | critical | mitigate | Human-only exact command; agents receive no private-key operation or read permission. |
| T-05-16-02 | Spoofing | Supplied signature | critical | mitigate | Verify exact request/principal/namespace/key/fingerprint before resume. |
| T-05-16-03 | Tampering | Request after signing | critical | mitigate | Verify raw file bytes and signed request self-hash; reject any mutation. |
| T-05-16-SC | Tampering | Dependency supply chain | low | accept | Uses system OpenSSH and the already-pinned public key only. |
</threat_model>

<verification>
The public verifier must pass offline. Confirm no command other than the owner-run signing command references the private-key path and no accepted evidence changed.
</verification>

<success_criteria>
- The owner alone signs the exact fresh request.
- Only canonical armored signature bytes enter the repository.
- Public verification succeeds before any accepted-evidence path resumes.
- Aborting or verification failure keeps OWNER_AUTHENTICATION_REQUIRED and all no-go controls.
</success_criteria>

<output>
After successful verification, create `.planning/phases/05-outreach-feasibility-gate/05-16-SUMMARY.md` with only the public detached-signature artifact in `key-files`. If the owner aborts or verification fails, stop without a summary claiming authentication.
</output>
