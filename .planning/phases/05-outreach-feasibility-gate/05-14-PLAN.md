---
phase: 05-outreach-feasibility-gate
plan: 14
type: execute
wave: 12
depends_on:
  - 05-11
  - 05-12
  - 05-13
files_modified:
  - scripts/outreach-feasibility/owner-checkpoint.mjs
  - scripts/outreach-feasibility/owner-checkpoint.test.mjs
  - scripts/outreach-feasibility/owner-authorization.mjs
  - scripts/outreach-feasibility/owner-authorization.test.mjs
  - scripts/outreach-feasibility/trust/phase-05-owner.pub
  - scripts/outreach-feasibility/trust/phase-05-owner.allowed_signers.txt
  - scripts/outreach-feasibility/trust/owner-trust-anchor.json
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "The historical self-hashed owner receipt remains integrity-only and cannot independently authorize accepted evidence."
    - "Fresh owner authorization is accepted only as an OpenSSH ED25519 SSHSIG over the exact request bytes under principal jackshi812 and namespace job-copilot-phase-05-owner-v1."
    - "The verifier pins the exact owner public key, SHA-256 fingerprint, GitHub signing-key record, key not-before instant, and active/non-revoked state."
    - "Wrong key, principal, namespace, payload, nonce, time window, revoked anchor, missing anchor, or missing public-key material fails closed."
  artifacts:
    - path: scripts/outreach-feasibility/owner-checkpoint.mjs
      provides: "Integrity-only validation for the old unsigned receipt"
    - path: scripts/outreach-feasibility/owner-authorization.mjs
      provides: "Fresh request builder and OpenSSH SSHSIG verifier"
    - path: scripts/outreach-feasibility/trust/phase-05-owner.pub
      provides: "Pinned public ED25519 key bytes"
    - path: scripts/outreach-feasibility/trust/phase-05-owner.allowed_signers.txt
      provides: "Exact principal and namespace restriction for ssh-keygen -Y verify"
    - path: scripts/outreach-feasibility/trust/owner-trust-anchor.json
      provides: "Reviewable owner-selected public trust-anchor provenance and revocation state"
  key_links:
    - from: scripts/outreach-feasibility/owner-authorization.mjs
      to: scripts/outreach-feasibility/trust/phase-05-owner.allowed_signers.txt
      via: "ssh-keygen -Y verify with exact principal and namespace"
      pattern: "job-copilot-phase-05-owner-v1"
    - from: scripts/outreach-feasibility/owner-authorization.mjs
      to: scripts/outreach-feasibility/trust/owner-trust-anchor.json
      via: "public-key, fingerprint, provenance, not-before, and revocation validation"
      pattern: "SHA256:FPrmyBVv"
---

<objective>
Implement the selected fresh OpenSSH owner-authentication mechanism while preserving the old receipt as integrity-only evidence.

Purpose: Replace forgeable self-hash authority with an independently anchored, byte-exact ED25519 signature verifier before any accepted-evidence path is reopened.
Output: Pinned public trust material, an exact fresh-request/SSHSIG contract, and adversarial offline tests.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json
@.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json
@scripts/outreach-feasibility/owner-checkpoint.mjs
@scripts/outreach-feasibility/owner-checkpoint.test.mjs
</context>

<prohibitions>
- Never read, copy, stat, hash, print, or commit `/Users/jackshi/.ssh/job_copilot_owner_signing`; only the owner may invoke it at Plan 05-16's checkpoint.
- Do not call GitHub or any network endpoint. The owner-selected GitHub record is pinned public provenance, not a runtime network dependency.
- Do not treat Git identity, Git config, unsigned commits, chat text, raw stdin, hashes, nonce possession, or the historical receipt as authentication.
- Do not add a verifier callback, boolean, environment override, alternate principal/namespace, key-discovery fallback, or permissive compatibility branch.
- Do not regenerate accepted evidence or invoke the authoritative live terminal audit.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve the historical receipt as integrity-only</name>
  <read_first>
    - scripts/outreach-feasibility/owner-checkpoint.mjs
    - scripts/outreach-feasibility/owner-checkpoint.test.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json
    - .planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json
  </read_first>
  <files>scripts/outreach-feasibility/owner-checkpoint.mjs, scripts/outreach-feasibility/owner-checkpoint.test.mjs</files>
  <behavior>
    - "The old request/response/self-hash chain remains structurally and byte-integrity valid."
    - "Its CLI/API result is INTEGRITY_ONLY_NOT_AUTHENTICATED."
    - "Arbitrary replacement response bytes plus recomputed hashes never produce an authenticated result."
  </behavior>
  <action>
    Rename or wrap the historical public validator so its canonical result scope is `integrity_only`; keep a compatibility export only for structural callers and never let that return value satisfy fresh authorization. Make `--assert-record` report `INTEGRITY_ONLY_NOT_AUTHENTICATED`. Preserve the committed schema-version-1 request and receipt bytes unchanged. Add a forged/rehashed fixture proving every old hash can be recomputed around attacker-selected response bytes without crossing the authentication boundary.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/owner-checkpoint.test.mjs</automated>
  </verify>
  <done>The historical receipt is usable only for integrity/lineage inspection and cannot authorize acceptance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin and verify the selected fresh owner signer</name>
  <read_first>
    - .planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json
    - .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json
    - .planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json
    - .planning/phases/05-outreach-feasibility-gate/05-CONTRACT-RECONCILIATION.json
  </read_first>
  <files>scripts/outreach-feasibility/owner-authorization.mjs, scripts/outreach-feasibility/owner-authorization.test.mjs, scripts/outreach-feasibility/trust/phase-05-owner.pub, scripts/outreach-feasibility/trust/phase-05-owner.allowed_signers.txt, scripts/outreach-feasibility/trust/owner-trust-anchor.json</files>
  <behavior>
    - "Only jackshi812, namespace job-copilot-phase-05-owner-v1, and the pinned ED25519 key verify."
    - "The public key fingerprint must equal SHA256:FPrmyBVv+PxnI9UpEajtjjV3B4bQQqFcyL1duuN+IhI."
    - "Request issued_at and verification time must be on or after 2026-07-30T03:39:43.371Z and within a maximum seven-day request window."
    - "Only one canonical armored SSHSIG document with a single trailing LF is accepted."
    - "Inactive/revoked trust status, non-null revoked_at, missing/mismatched anchor/public/allowed-signers files, or any request/signature mutation fails closed."
  </behavior>
  <action>
    Create `phase-05-owner.pub` with the exact owner-selected public line `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIG2D1nrByiQxldOhXWZnd4TlyKq67wGblKkgCNyWtGC8` and one LF. Create the allowed-signers file with the sole line `jackshi812 namespaces="job-copilot-phase-05-owner-v1" ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIG2D1nrByiQxldOhXWZnd4TlyKq67wGblKkgCNyWtGC8` plus one LF. Create `scripts/outreach-feasibility/trust/owner-trust-anchor.json` as trust-anchor schema version 1 pinning GitHub account `jackshi812`, Git identity `jackshi812 &lt;jack.s@wustl.edu&gt;`, SSH signing-key record ID `1081409`, title `Job Copilot owner signing`, created_at/not_before `2026-07-30T03:39:43.371Z`, the public GitHub URL, ED25519 algorithm, exact key/fingerprint, `status: ACTIVE`, and `revoked_at: null`. Keep all three artifacts public and reviewable under `scripts/outreach-feasibility/trust/`; no copy may exist under `.planning/`.

    Implement strict finite request parsing, canonical JSON serialization with one LF, raw-byte SHA-256, nonce format of exactly 64 lowercase hex characters, and a maximum seven-day issued-to-expires window. Implement verification by spawning absolute `/usr/bin/ssh-keygen -Y verify` with the pinned allowed-signers file, principal, namespace, and signature file while supplying the exact request bytes on stdin. Before spawning, compare all three public trust artifacts, run `/usr/bin/ssh-keygen -lf ... -E sha256`, and enforce active/non-revoked/not-before state. Reject missing tools/files, symlinks, unexpected permissions/types, noncanonical request bytes, malformed/extra armored signature text, stdout/stderr ambiguity, and nonzero verifier exit.

    Tests may generate temporary test-owned keys only inside temporary directories. Cover wrong key, principal, namespace, request byte, nonce, issued_at, expires_at, stale verification time, revoked status, missing key, mismatched fingerprint, malformed armor, and extra bytes. Production code must never generate keys or access a private key.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/owner-authorization.test.mjs scripts/outreach-feasibility/owner-checkpoint.test.mjs</automated>
  </verify>
  <done>The pinned public trust anchor verifies exactly one fresh OpenSSH SSHSIG contract and rejects every adversarial identity, content, time, and revocation variant.</done>
</task>

</tasks>

<artifacts>
- `owner-checkpoint.mjs` and test — historical integrity-only boundary.
- `owner-authorization.mjs` and test — fresh canonical request and SSHSIG verification.
- `phase-05-owner.pub`, `phase-05-owner.allowed_signers.txt`, and `owner-trust-anchor.json` — exact public trust material under the reviewable source tree only.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| historical self-hash → accepted authority | Integrity-only bytes must never authenticate the owner. |
| request/signature → owner principal | Exact bytes must verify under the pinned principal, namespace, ED25519 key, and active trust state. |
| public verifier → owner private key | The repository may consume public key/signature bytes but must never access the private key. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-14-01 | Spoofing | SSHSIG verifier | critical | mitigate | Pin principal, namespace, key bytes, fingerprint, and GitHub record; test wrong variants. |
| T-05-14-02 | Tampering | Authorization request/signature | critical | mitigate | Verify exact canonical request bytes and strict single armored signature bytes. |
| T-05-14-03 | Repudiation | Key lifecycle | high | mitigate | Enforce not-before, bounded request window, active status, null revocation, and missing-key failure. |
| T-05-14-04 | Information Disclosure | Owner private key | critical | mitigate | Production code and agents receive no private-key read path; signing is isolated to the owner checkpoint. |
| T-05-14-SC | Tampering | Dependency supply chain | low | accept | Uses Node built-ins and system OpenSSH only; no package installation. |
</threat_model>

<verification>
Run both task commands. Confirm no test reads `/Users/jackshi/.ssh/job_copilot_owner_signing`, no network/provider call occurs, and no accepted evidence changes.
</verification>

<success_criteria>
- The old receipt is explicitly integrity-only.
- Fresh authorization requires the exact selected ED25519 trust anchor, principal, namespace, bytes, time window, and active state.
- All required adversarial variants fail closed.
- RIGHTS_NO_GO / NOT_RUN_RIGHTS_NO_GO and zero provider effects remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-14-SUMMARY.md` when done. Its frontmatter `key-files.created` / `key-files.modified` must include every source, test, and trust path changed by this plan—including all three `scripts/outreach-feasibility/trust/` files—because the mandatory execute-phase code-review post hook derives its exact source scope from SUMMARY frontmatter.
</output>
