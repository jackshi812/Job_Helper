---
phase: 05-outreach-feasibility-gate
plan: 12
type: execute
wave: 10
depends_on:
  - 05-09
files_modified:
  - scripts/outreach-feasibility/decision-evidence.mjs
  - scripts/outreach-feasibility/decision-evidence.test.mjs
  - scripts/outreach-feasibility/evidence-integrity.mjs
  - scripts/outreach-feasibility/evidence-integrity.test.mjs
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "A fault before, between, or after the two publication renames restores the exact prior decision/residue pair byte-for-byte."
    - "Accepted zero-residue v2 evidence rejects either source-snapshot digest when it differs from the corresponding Git-surface digest, even after the self-hash is recomputed."
    - "Evidence timestamps are exact canonical UTC instants; calendar rollover strings and normalized impossible dates are rejected."
    - "Tests do not depend on the wall clock crossing the committed seven-day rights window."
  artifacts:
    - path: scripts/outreach-feasibility/decision-evidence.mjs
      provides: "Rollback-safe paired publication with internal fault injection"
    - path: scripts/outreach-feasibility/evidence-integrity.mjs
      provides: "Complete v2 cross-field equality checks and canonical timestamp validation"
    - path: scripts/outreach-feasibility/decision-evidence.test.mjs
      provides: "Failure-point and exact-prior-pair recovery tests"
    - path: scripts/outreach-feasibility/evidence-integrity.test.mjs
      provides: "Independent cross-field mutation and impossible-date rejection tests"
  key_links:
    - from: scripts/outreach-feasibility/decision-evidence.mjs
      to: .planning/phases/05-outreach-feasibility-gate/05-DECISION.json
      via: "the finalizer publishes the decision only as one recoverable pair with zero residue"
      pattern: "writeAtomicPair"
    - from: scripts/outreach-feasibility/evidence-integrity.mjs
      to: .planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json
      via: "source_snapshot digests must equal their phase-range and source-tree counterparts"
      pattern: "controlled_tree_sha256"
---

<objective>
Make accepted-evidence publication recoverable as a pair and complete the schema's missing source-snapshot equalities and canonical-time checks.

Purpose: Close the verifier's atomicity and cross-field integrity blockers while addressing the impossible-date and wall-clock review warnings without weakening the seven-day policy.
Output: Rollback-safe publication, strict v2 validation, and deterministic offline regressions.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-09-SUMMARY.md
@scripts/outreach-feasibility/decision-evidence.mjs
@scripts/outreach-feasibility/decision-evidence.test.mjs
@scripts/outreach-feasibility/evidence-integrity.mjs
@scripts/outreach-feasibility/evidence-integrity.test.mjs
</context>

<prohibitions>
- Do not regenerate or edit the committed decision, zero-residue, owner-checkpoint, rights, quality, baseline, or reconciliation JSON artifacts.
- Do not weaken the seven-day rights-evidence validity rule or add a runtime/CLI clock override.
- Do not accept two independent successful renames as a sufficient publication transaction.
- Do not install a package; use Node built-ins and same-directory temporary/backup files.
- Do not run the authoritative live `--terminal-audit`.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish decision and residue with verified rollback</name>
  <read_first>
    - scripts/outreach-feasibility/decision-evidence.mjs
    - scripts/outreach-feasibility/decision-evidence.test.mjs
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - scripts/outreach-feasibility/residue-check.mjs
  </read_first>
  <files>scripts/outreach-feasibility/decision-evidence.mjs, scripts/outreach-feasibility/decision-evidence.test.mjs</files>
  <behavior>
    - "Failure before publication leaves both prior files unchanged."
    - "Failure immediately after the residue publication step restores both exact prior byte sequences."
    - "Failure at the decision publication step restores both exact prior byte sequences."
    - "Failure during post-publication readback restores both exact prior byte sequences."
    - "Success leaves the validated new pair and no temporary or backup residue."
  </behavior>
  <action>
    Write fault-injection tests before changing the publisher. Refactor `writeAtomicPair` into a same-directory recoverable transaction: serialize and validate the new pair first; read and retain each destination's exact prior bytes/existence and mode; create exclusive staged files and durable same-directory prior-version backups; flush staged files and the directory; publish the residue and decision with named, test-injectable one-shot fault points; read both destinations back and compare exact staged bytes; then remove backups only after the pair has passed readback. On any error, restore both original destinations from the preserved backups (or restore absence), flush the directory, verify exact prior bytes/existence, clean transient files, and rethrow the original failure. If rollback itself cannot be verified, preserve recovery backups and emit a distinct hard failure rather than claiming success. Keep fault injection available only to direct module tests, never as a CLI option.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/decision-evidence.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    Every named failure point proves exact byte-for-byte restoration of both pre-existing artifacts, and the success path proves a coherent new pair with no leftover transaction files.
  </acceptance_criteria>
  <done>All injected publication failures restore the exact prior pair and successful publication leaves one coherent pair without transaction residue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce v2 digest equality and canonical UTC timestamps</name>
  <read_first>
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - scripts/outreach-feasibility/evidence-integrity.test.mjs
    - scripts/outreach-feasibility/decision-evidence.mjs
    - scripts/outreach-feasibility/decision-evidence.test.mjs
  </read_first>
  <files>scripts/outreach-feasibility/evidence-integrity.mjs, scripts/outreach-feasibility/evidence-integrity.test.mjs, scripts/outreach-feasibility/decision-evidence.mjs, scripts/outreach-feasibility/decision-evidence.test.mjs</files>
  <behavior>
    - "Changing only source_snapshot.controlled_tree_sha256 and recomputing zero_residue_sha256 is rejected."
    - "Changing only source_snapshot.baseline_to_source_history_sha256 and recomputing zero_residue_sha256 is rejected."
    - "Changing the counterpart Git-surface field independently is also rejected."
    - "A timestamp is accepted only when parsing it and serializing it with toISOString returns the exact original string."
    - "Tests drive pure/finalizer code with an explicit fixed Date while production CLI behavior continues to use the real clock."
  </behavior>
  <action>
    Add the two missing equalities inside `validateZeroV2`: controlled-tree digest equals the source-head-tree digest, and baseline-to-source-history digest equals the phase-commit-range inventory digest. Test each side independently after recomputing the record self-hash so the equality, not the hash, causes rejection. Replace parse-only UTC checks with an exported canonical validator that requires the existing millisecond-Z syntax plus exact `new Date(value).toISOString() === value`; cover impossible day/month rollover, non-leap February 29, offsets, missing milliseconds, and canonical valid instants. Thread an optional `now` dependency through direct finalizer APIs for tests only; do not expose a CLI flag or environment bypass, and do not alter the rights validity duration.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/evidence-integrity.test.mjs scripts/outreach-feasibility/decision-evidence.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    Self-consistent but cross-field-contradictory v2 records and normalized impossible timestamps fail with specific validation errors, while canonical records and fixed-clock direct tests pass.
  </acceptance_criteria>
  <done>Both missing digest equalities and canonical UTC round-trip validation are enforced by independently mutated regressions.</done>
</task>

</tasks>

<artifacts>
- `scripts/outreach-feasibility/decision-evidence.mjs` — verified rollback publisher and fixed-clock direct API.
- `scripts/outreach-feasibility/decision-evidence.test.mjs` — publication fault matrix and time-stable finalizer tests.
- `scripts/outreach-feasibility/evidence-integrity.mjs` — complete cross-field and canonical-time validation.
- `scripts/outreach-feasibility/evidence-integrity.test.mjs` — recomputed-self-hash contradiction and calendar-rollover regressions.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| in-memory accepted pair → filesystem | A partial or failed publication must not replace only one artifact. |
| JSON fields → integrity conclusion | A valid self-hash must not hide contradictory duplicated source identities. |
| timestamp text → time policy | Parser normalization must not convert an impossible date into accepted evidence. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-12-01 | Tampering | `writeAtomicPair` | high | mitigate | Preserve exact prior pair, inject failures at each publication boundary, and verify rollback before rethrowing. |
| T-05-12-02 | Tampering | `validateZeroV2` | high | mitigate | Enforce both duplicated digest equalities after individual schema/digest checks. |
| T-05-12-03 | Repudiation | Evidence timestamps | medium | mitigate | Accept only canonical exact UTC instants and reject parser-normalized calendar rollovers. |
| T-05-12-04 | Denial of Service | Rights-window tests | low | mitigate | Inject fixed time only into direct test APIs while preserving production wall-clock enforcement. |
| T-05-12-SC | Tampering | Dependency supply chain | low | accept | No package installation or dependency change is permitted by this plan. |
</threat_model>

<verification>
Run both task commands and:
`env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/evidence-integrity.test.mjs scripts/outreach-feasibility/decision-evidence.test.mjs`

Do not invoke the authoritative live terminal audit.
</verification>

<success_criteria>
- A failed paired publication cannot leave a split pair after verified rollback.
- Both missing v2 source-digest equalities are enforced against independently mutated records.
- Impossible UTC dates are rejected without weakening valid canonical timestamps.
- Offline tests are deterministic beyond the committed rights window; runtime policy still uses real time.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-12-SUMMARY.md` when done. Its frontmatter `key-files.created` / `key-files.modified` must include every source and test path changed by this plan so the mandatory execute-phase post-hook review receives complete SUMMARY-derived scope.
</output>
