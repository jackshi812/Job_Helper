---
phase: 05-outreach-feasibility-gate
plan: 20
type: execute
wave: 19
depends_on:
  - 05-21
files_modified:
  - scripts/outreach-feasibility/decision-evidence.mjs
  - scripts/outreach-feasibility/decision-evidence.test.mjs
  - scripts/outreach-feasibility/residue-check.mjs
  - scripts/outreach-feasibility/residue-check.test.mjs
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05

must_haves:
  truths:
    - "After process termination at any durable publish boundary, the next accepted-pair reader recovers before returning and observes exactly one mutually coherent old or new decision/residue generation."
    - "Concurrent accepted-pair writers serialize, concurrent readers cannot observe a split generation, and an active writer is never mistaken for an abandoned transaction."
    - "Pair publication and recovery reject symlink/path substitution, retain the legacy two-path public artifact interface, and leave no lock, journal, stage, backup, or restore residue after success."
    - "The existing public owner request, detached signature, trust anchor, public key, and allowed-signers bytes remain unchanged; the stopped RIGHTS_NO_GO / NOT_RUN_RIGHTS_NO_GO branch remains the only publishable outcome."
  artifacts:
    - path: "scripts/outreach-feasibility/decision-evidence.mjs"
      provides: "Journaled, locked, durable accepted-pair publication through the existing writeAtomicPair interface"
      exports:
        - "PAIR_PUBLICATION_BOUNDARIES"
        - "writeAtomicPair"
    - path: "scripts/outreach-feasibility/residue-check.mjs"
      provides: "Mandatory accepted-pair recovery and coherent reader used before either artifact is consumed"
      exports:
        - "recoverAcceptedEvidencePair"
        - "readAcceptedEvidencePair"
    - path: "scripts/outreach-feasibility/decision-evidence.test.mjs"
      provides: "Subprocess-kill, restart recovery, concurrency, symlink, and public-proof preservation regressions"
    - path: "scripts/outreach-feasibility/residue-check.test.mjs"
      provides: "Real --assert-zero interrupted-journal recovery-before-both-reads and malformed-recovery fail-closed regressions"
  key_links:
    - from: "scripts/outreach-feasibility/decision-evidence.mjs"
      to: "scripts/outreach-feasibility/residue-check.mjs"
      via: "writeAtomicPair uses the shared lock/journal recovery contract before staging and publishing"
      pattern: "recoverAcceptedEvidencePair|readAcceptedEvidencePair"
    - from: "scripts/outreach-feasibility/decision-evidence.test.mjs"
      to: "scripts/outreach-feasibility/decision-evidence.mjs"
      via: "SIGKILL subprocess probes at every exported durable boundary followed by mandatory recovery"
      pattern: "PAIR_PUBLICATION_BOUNDARIES"
---

<objective>
Close the crash-consistency and concurrency blocker in accepted decision/residue publication while preserving the already authenticated stopped decision.

Purpose: Caught exceptions are insufficient when the process dies between the two canonical renames. OUTR-04 and OUTR-05 need one recoverable state transition whose readers never accept a split pair.
Output: A durable journal/lock protocol behind the existing two artifact paths, mandatory recovery/read interfaces, and failing-first subprocess/concurrency regressions.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-19-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-21-SUMMARY.md
@scripts/outreach-feasibility/decision-evidence.mjs
@scripts/outreach-feasibility/decision-evidence.test.mjs
@scripts/outreach-feasibility/residue-check.mjs
@scripts/outreach-feasibility/residue-check.test.mjs

<interfaces>
Existing contracts to preserve:
- `writeAtomicPair({ decision, record, decisionPath, recordPath, injectFault })` is the only accepted-pair writer.
- `PAIR_PUBLICATION_FAULT_POINTS` currently models caught in-process failures; replace it with the complete durable-boundary inventory exported as `PAIR_PUBLICATION_BOUNDARIES`.
- Canonical outputs remain `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json` and `.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json`.
- `finalizeAcceptedEvidence(...)` verifies the raw public SSHSIG before Git resolution, scanning, construction, or publication.
- `assertAuthenticatedAcceptedEvidence(...)` and `assertRecordMatchesLiveScan(...)` remain the semantic pair-coherence gates after byte recovery.
</interfaces>
</context>

<prohibitions>
- Do not access, invoke, copy, expose, or commit `/Users/jackshi/.ssh/job_copilot_owner_signing`; do not create a replacement signature or alter the existing public request/signature/trust artifacts.
- Do not run a provider request, network search, representative spike, fixture creation, raw-result collection, production mutation, schema push, or production outreach code.
- Do not invoke code review, overwrite `05-REVIEW.md`, mark Phase 5 complete, mutate roadmap/state tracking, or invoke the live authoritative terminal audit.
- Do not use a success-only cleanup protocol. Recovery must be mandatory before accepted-pair reads and writes.
- Do not follow symlinks, place transaction state outside the canonical Phase 5 directory, or use unresolved paths for destructive cleanup.
- Do not stash, clean, reset, delete, or otherwise alter unrelated owner work.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove process-kill and concurrent publication recovery</name>
  <read_first>
    - scripts/outreach-feasibility/decision-evidence.mjs
    - scripts/outreach-feasibility/decision-evidence.test.mjs
    - scripts/outreach-feasibility/residue-check.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-DECISION.json
    - .planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json
    - .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json
    - .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig
  </read_first>
  <files>scripts/outreach-feasibility/decision-evidence.mjs, scripts/outreach-feasibility/decision-evidence.test.mjs, scripts/outreach-feasibility/residue-check.mjs</files>
  <behavior>
    - "RED: for every exported file-fsync, journal-fsync, directory-fsync, and canonical-rename boundary, a subprocess killed with SIGKILL leaves a restart that recovers to one pair whose cross-hashes and schemas agree."
    - "RED: two writers targeting the same canonical pair serialize; readers started before, during, and after each writer return only the complete prior or complete committed generation."
    - "RED: an active lock fails or waits within a bounded interval without being stolen; an abandoned lock plus valid journal is recovered; malformed or ambiguous recovery state fails closed with backups preserved."
    - "RED: a symlinked output, lock, journal, stage, backup, restore, or substituted parent directory is rejected before any canonical rename."
    - "GREEN: ordinary injected exceptions retain the prior exact bytes/modes/absence behavior already covered by the test suite."
  </behavior>
  <action>
    Write the subprocess and multi-process regressions first and confirm they fail against the current two-rename implementation. Drive every boundary from one exported `PAIR_PUBLICATION_BOUNDARIES` inventory so the kill matrix cannot silently omit a newly added fsync or rename.

    Implement a same-directory exclusive lock and write-ahead journal behind `writeAtomicPair`. The fixed journal records a finite schema containing transaction ID, state, exact canonical basenames, prior existence/modes/digests, next digests, and exact stage/backup basenames; reject unknown fields, absolute names, separators, control bytes, links, non-regular files, wrong ownership directory, and digest drift. Stage both next files and exact prior backups with exclusive creation, fsync every file, fsync the directory after each durable namespace change, advance the journal through prepared, record-published, and committed states by atomic replacement, then verify both canonical bytes and semantic cross-binding before cleanup.

    Add `recoverAcceptedEvidencePair` and `readAcceptedEvidencePair` in `residue-check.mjs` so every recovery decision is shared by writers and downstream readers. Recovery under the same lock must deterministically finish a fully committed new pair or restore the exact prior pair from verified backups when publication is incomplete; it must never expose a split pair, silently discard ambiguous state, or delete the only recovery copies. Use bounded lock acquisition and PID liveness only to distinguish an active owner from an abandoned transaction; PID ambiguity fails closed. This preserves the rights-first no-go and zero-effect stop required by D-09, D-10, D-12, and D-13.
  </action>
  <acceptance_criteria>
    - Every emitted durable boundary is covered by a subprocess SIGKILL case and recovery produces a semantically valid old or new pair.
    - Concurrent writers/readers complete without split generations, lost updates, journal residue, or lock theft.
    - Success leaves only the two canonical files; failure with unprovable recovery retains verified backups and returns a bounded error.
    - Canonical paths and all transaction paths reject symlinks and parent/path substitution.
  </acceptance_criteria>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/decision-evidence.test.mjs</automated>
  </verify>
  <done>The existing two-path accepted-evidence interface is backed by a durable, restartable, concurrency-safe journal/lock protocol proven at every fsync and publish boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make recovery precede finalizer and assertion reads</name>
  <read_first>
    - scripts/outreach-feasibility/decision-evidence.mjs
    - scripts/outreach-feasibility/decision-evidence.test.mjs
    - scripts/outreach-feasibility/residue-check.mjs
    - scripts/outreach-feasibility/residue-check.test.mjs
    - scripts/outreach-feasibility/owner-authorization.mjs
    - scripts/outreach-feasibility/evidence-integrity.mjs
  </read_first>
  <files>scripts/outreach-feasibility/decision-evidence.mjs, scripts/outreach-feasibility/decision-evidence.test.mjs, scripts/outreach-feasibility/residue-check.mjs, scripts/outreach-feasibility/residue-check.test.mjs</files>
  <behavior>
    - "RED: `--assert-decision` and `--finalize-accepted` presented with an interrupted journal recover before parsing either canonical artifact."
    - "RED: `residue-check.mjs --assert-zero` presented with an interrupted journal recovers before both the decision and zero-residue reads; a malformed, ambiguous, or substituted journal fails closed before either artifact is parsed."
    - "RED: the complete accepted-pair consumer inventory is covered: `writeAtomicPair` pre-write recovery, `finalizeAcceptedEvidence`, decision CLI `--assert-decision`, decision CLI `--finalize-accepted`, residue CLI `--assert-zero`, and terminal `runContractValidation`/`runTerminalAudit` (the terminal consumers are integration-tested again in Plan 05-23)."
    - "RED: a malformed journal, substituted public proof path, or failed raw SSHSIG verification returns before Git resolution, scan, stage, journal, lock cleanup, or canonical publication."
    - "RED: all public owner request/signature/trust bytes have the same SHA-256 before and after every passing and failing finalizer case."
    - "GREEN: the only accepted output remains RIGHTS_NO_GO_ACCEPTED with NOT_RUN_RIGHTS_NO_GO and all five execution/case counters equal to zero per D-09, D-10, D-12, and D-13."
  </behavior>
  <action>
    Add failing CLI/finalizer cases that install each recoverable interrupted state, then call the real assertion/finalizer entry point rather than the helper directly. Route those entry points through `readAcceptedEvidencePair` before JSON parsing or semantic assertions. Explicitly maintain one inventory of every accepted-pair consumer: `writeAtomicPair` before a write, `finalizeAcceptedEvidence`, the decision CLI `--assert-decision` and `--finalize-accepted` branches, residue CLI `--assert-zero`, and terminal `runContractValidation`/`runTerminalAudit`. Unit-test the first five here and leave explicit integration assertions for both terminal functions in Plan 05-23. Any new consumer must use the same recovery-before-read API and extend this inventory.

    In `residue-check.test.mjs`, create real interrupted-journal CLI regressions for `residue-check.mjs --assert-zero`: one recoverable prepared/published state must prove recovery completes before both canonical artifact reads, and malformed, ambiguous, digest-drifted, or symlink-substituted recovery state must fail closed before either JSON parser or live scan runs. Assert that the failure retains the only verified recovery copies. Do not simulate the CLI by calling the helper directly.

    Preserve the stronger ordering already established in Plan 05-17: raw request/signature verification and exact canonical public trust paths precede Git, scan, construction, journal creation, or publication.

    Snapshot SHA-256 and modes of the repository-local owner request, signature, trust anchor, public key, and allowed-signers files in tests and require byte-for-byte preservation on success and failure. Per D-09, D-10, D-12, and D-13, do not introduce a new signing path or checkpoint: this task changes publication mechanics only, not the signed stopped-decision payload.
  </action>
  <acceptance_criteria>
    - All enumerated accepted-pair consumers recover or fail closed before consuming a canonical pair.
    - The real `residue-check.mjs --assert-zero` CLI proves recovery before both reads and rejects malformed recovery state without parsing either artifact.
    - Authentication failure has zero pair-publication or Git/scan effects.
    - Existing public proof artifacts remain byte- and mode-identical.
    - No production/provider/spike capability becomes reachable.
  </acceptance_criteria>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/decision-evidence.test.mjs scripts/outreach-feasibility/residue-check.test.mjs scripts/outreach-feasibility/owner-authorization.test.mjs</automated>
  </verify>
  <done>Every enumerated accepted-pair reader, including the real residue `--assert-zero` CLI, recovers before both artifact reads or fails closed while preserving the already verified public owner proof unchanged.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

- Export `PAIR_PUBLICATION_BOUNDARIES` from `decision-evidence.mjs`; it enumerates every stage-file fsync, backup-file fsync, journal fsync, directory fsync, canonical rename, readback, and cleanup boundary used by kill tests.
- Preserve exported `writeAtomicPair(...)`, now implemented through the durable journal/lock protocol.
- Export `recoverAcceptedEvidencePair({ decisionPath, recordPath, ... })` and `readAcceptedEvidencePair({ decisionPath, recordPath, ... })` from `residue-check.mjs`.
- `residue-check.test.mjs` owns the interrupted-journal `--assert-zero` CLI recovery-before-both-reads and malformed-recovery fail-closed regressions.
- Add transient canonical path `.planning/phases/05-outreach-feasibility-gate/.05-accepted-evidence.lock`.
- Add transient canonical path `.planning/phases/05-outreach-feasibility-gate/.05-accepted-evidence.journal.json`.
- Add transaction-ID-scoped `.stage`, `.backup`, and `.restore` files only within the canonical Phase 5 directory; successful execution and proven recovery remove them.
- Journal fields: `schema_version`, `transaction_id`, `state`, `decision_basename`, `record_basename`, `prior_decision`, `prior_record`, `next_decision_sha256`, `next_record_sha256`, `decision_stage_basename`, `record_stage_basename`, `decision_backup_basename`, and `record_backup_basename`.
- No new CLI flag, external dependency, persistent evidence field, schema file, provider capability, or signature artifact is introduced.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| concurrent processes → canonical evidence pair | Multiple local readers/writers must not interleave two generations. |
| transaction files → accepted artifacts | Journal, stages, and backups are untrusted until exact path, type, digest, and state validation succeeds. |
| public owner proof → finalizer | Existing repository-local SSHSIG authority must remain the only authentication source. |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-20-01 | Tampering | Pair publication after process kill | critical | mitigate | Durable journal states, file and directory fsyncs, mandatory restart recovery, semantic pair readback, and SIGKILL tests at every boundary. |
| T-05-20-02 | Tampering | Concurrent writers/readers | high | mitigate | One exclusive canonical lock, bounded active-owner handling, serial publication, and multi-process old-or-new coherence tests. |
| T-05-20-03 | Spoofing | Symlink/path substitution | high | mitigate | Canonical real parent, exact basenames, lstat/no-follow checks, exclusive creation, and rejection of links or non-regular files before mutation. |
| T-05-20-04 | Repudiation | Ambiguous recovery state | high | mitigate | Preserve verified backups and fail with journal state intact whenever neither complete-old nor complete-new generation can be proven. |
| T-05-20-05 | Elevation of Privilege | Owner proof substitution | critical | mitigate | Keep raw SSHSIG verification and exact repository-local trust paths before all publication effects; snapshot public bytes in tests. |
| T-05-20-SC | Tampering | Dependency supply chain | low | accept | Node built-ins and system Git/OpenSSH only; no package installation occurs. |
</threat_model>

<verification>
Run both focused test commands with `TAVILY_API_KEY` unset. The tests must be offline, use only test-owned temporary repositories, never access the owner private key, and leave no transaction files. Do not run code review or the authoritative terminal audit.
</verification>

<success_criteria>
- Process termination at every durable boundary is recoverable to exactly one coherent pair.
- Concurrent readers and writers cannot observe or create split generations.
- Symlink/path substitution and ambiguous journals fail closed.
- Existing public owner proof and stopped no-go semantics are unchanged.
- OUTR-04 and OUTR-05 remain production-disabled with zero provider/spike/production effects.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-20-SUMMARY.md` with only the four modified source/test paths in `key-files`. Do not claim canonical review, final verification, phase completion, or terminal-audit success.
</output>
