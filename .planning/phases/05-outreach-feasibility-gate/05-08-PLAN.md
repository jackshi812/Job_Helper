---
phase: 05-outreach-feasibility-gate
plan: 08
type: execute
wave: 7
depends_on:
  - 05-06
files_modified:
  - scripts/outreach-feasibility/residue-check.mjs
  - scripts/outreach-feasibility/residue-check.test.mjs
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "Every allowed source and JSON blob is byte-inspected in the worktree, full index, and every commit from the pinned baseline through the recorded source head."
    - "Historical JSON is accepted only through its exact path/version/state schema; realistic provider `results/name/title/url/content` payloads cannot hide behind an allowed filename."
    - "The original pinned baseline remains authoritative even after a later `05-01-PLAN.md` edit."
    - "An asserted residue record must match a reconstructed source snapshot, live worktree/index state, accepted decision digest, and owner-checkpoint digest."
    - "Post-record Phase 5 administrative commits are scanned live and cryptographically inventoried under an exact path/state transition policy covering the accepted evidence pair, stable reconciliation, 05-09 summary, review, verification, ROADMAP, REQUIREMENTS, and STATE; verification lineage begins only from its committed source-head tree state and permits exactly one exact `gaps_found`→`passed` write or one exact absent→`passed` write, while unrelated paths remain outside the residue claim and cannot make the audit require a globally clean repository."
    - "Newline, carriage-return, tab, ANSI, NUL, and other control characters cannot forge reported paths or diagnostics."
  artifacts:
    - path: "scripts/outreach-feasibility/residue-check.mjs"
      provides: "Byte-complete, baseline-pinned, live-bound residue scanning and comparison"
      exports:
        - "scanOwnedSurfaces({ repoRoot, phaseDir, baseline, sourceHeadSha }): Promise<ResidueScan>"
        - "buildZeroResidueRecord({ matrix, qualityReport, decisionContract, ownerCheckpoint, baseline, scan }): ZeroResidueRecordV2"
        - "assertRecordMatchesLiveScan(record, liveScan, { decision, ownerCheckpoint }): ZeroResidueRecordV2"
    - path: "scripts/outreach-feasibility/residue-check.test.mjs"
      provides: "Worktree/index/history/provider-payload/baseline/live-tail/committed-verification-lineage/control-path bypass coverage"
  key_links:
    - from: ".planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json"
      to: "scripts/outreach-feasibility/residue-check.mjs"
      via: "all history enumeration begins at the exact asserted base commit/blob, never latest-plan discovery"
      pattern: "assertExecutionBaseline"
    - from: "scripts/outreach-feasibility/residue-check.mjs"
      to: "scripts/outreach-feasibility/evidence-integrity.mjs"
      via: "every JSON blob is dispatched by exact path/state before sensitive-pattern defense"
      pattern: "assertArtifactSchema"
    - from: ".planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json"
      to: "live repository state"
      via: "`assertRecordMatchesLiveScan` reconstructs the source snapshot and validates every administrative-tail commit through current HEAD"
      pattern: "assertRecordMatchesLiveScan"
  prohibitions:
    - "Do not skip source bytes because a path is allowlisted or because the file is not JSON."
    - "Do not use a denylist as the primary JSON validator; exact per-path schemas run first."
    - "Do not rediscover the baseline with `git log`, a date, or a commit message."
    - "Do not require the archived record to equal the commit that contains itself; use a source snapshot plus live administrative-tail validation."
    - "Do not print raw matched values or unsanitized filenames."
    - "Do not delete files, traverse broad user directories, call a provider, or touch production/schema paths."
---

<objective>
Replace the hollow residue proof with byte-complete, immutable-baseline-bound, live-validated evidence.

Purpose: The current scanner skips source contents, admits ordinary provider payloads under allowed JSON paths, moves its baseline, discards its live scan, and renders crafted paths unsafely. D-17 and the accepted OUTR-04/OUTR-05 no-go require a proof that those bypasses cannot survive.
Output: A rebuilt scanner and focused negative/property suite; final record/decision regeneration occurs only after owner receipt and decision hardening in Plan 05-09.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-02-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-04-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-06-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json
@.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json
@.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json
@.planning/phases/05-outreach-feasibility-gate/05-DECISION.json
@.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json
@scripts/outreach-feasibility/evidence-integrity.mjs
@scripts/outreach-feasibility/evidence-integrity.test.mjs
@scripts/outreach-feasibility/residue-check.mjs
@scripts/outreach-feasibility/residue-check.test.mjs
@scripts/outreach-feasibility/rights-gate.mjs
@scripts/outreach-feasibility/decision-evidence.mjs

<interfaces>
From Plan 05-06:

- `assertExecutionBaseline({ record, repoRoot, sourceHeadSha? }): Promise&lt;ExecutionBaseline&gt;`
- `assertArtifactSchema(relativePath, parsedArtifact): ParsedArtifact`
- `assertZeroResidueRecord(record, expectedDigests): ZeroResidueRecordV2`

Target scanner contract owned by this plan:

- `scanOwnedSurfaces({ repoRoot, phaseDir, baseline, sourceHeadSha }): Promise&lt;ResidueScan&gt;`
- `buildZeroResidueRecord({ matrix, qualityReport, decisionContract, ownerCheckpoint, baseline, scan }): ZeroResidueRecordV2`
- `assertRecordMatchesLiveScan(record, liveScan, { decision, ownerCheckpoint }): ZeroResidueRecordV2`

Target assertion CLI contract owned by this plan:

- mode flag: `--assert-zero`
- required path flags: `--repo-root`, `--phase-dir`, `--baseline-record`, `--matrix`, `--quality-report`, `--decision`, `--owner-checkpoint`, `--record`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild byte-complete scanning around the pinned source snapshot</name>
  <read_first>
    - `scripts/outreach-feasibility/residue-check.mjs`
    - `scripts/outreach-feasibility/residue-check.test.mjs`
    - `scripts/outreach-feasibility/evidence-integrity.mjs`
    - `scripts/outreach-feasibility/evidence-integrity.test.mjs`
    - `.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md`
  </read_first>
  <files>scripts/outreach-feasibility/residue-check.mjs</files>
  <behavior>
    - clean allowlisted source/JSON bytes across all three surfaces → zero sensitive hits and deterministic inventory digests.
    - sensitive bytes added to any existing allowlisted `.mjs` in worktree, index, or a later-cleaned historical commit → rejection attributed to the correct sanitized surface.
    - realistic provider response JSON under any allowed artifact path → exact-schema rejection before defense-in-depth pattern handling.
    - pinned base/plan-blob drift or a later plan edit → original baseline remains selected and tampering is rejected.
    - stale/substituted record, decision digest drift, owner-checkpoint digest drift, or source-snapshot mismatch → live assertion failure.
    - exact administrative transitions after the source head → accepted only after every commit/blob is scanned; any source path in that tail → failure.
    - committed `05-VERIFICATION.md` at the source head with exact `gaps_found` state → exactly one post-source-head committed write of the exact bounded `passed` report is accepted.
    - `05-VERIFICATION.md` absent from the committed source-head tree, even when a current untracked `gaps_found` file exists → exactly one post-source-head committed creation of the exact bounded `passed` report is accepted; the untracked file never becomes the committed initial state.
  </behavior>
  <action>
Refactor the scanner to import `assertExecutionBaseline`, `assertArtifactSchema`, and `assertZeroResidueRecord` from `evidence-integrity.mjs`. Remove its import of `assertDecisionRecord`; scanner/record integrity must stay dependency-neutral so Plan 05-09 can make the decision finalizer consume the scanner without a circular import. Replace `--phase-plan`/`git log -1` discovery with required `--baseline-record` input. Validate the exact pinned commit and plan blob before traversal.

Expand the source allowlist to the complete current/planned local evidence set: existing rights/decision/residue/dormant modules/tests; `evidence-integrity.mjs` and its test; `owner-checkpoint.mjs` and its test; `terminal-audit.mjs` and its test from Plan 05-10; and the final `adversarial-regression.test.mjs`. Expand the JSON allowlist to rights, quality, decision, zero-residue, execution-baseline, owner-checkpoint request/receipt, and stable contract-reconciliation artifacts. Directories remain finite and literal. Plan 05-10 must execute before Plan 05-09 freezes `source_snapshot.head_sha`, so both terminal-audit source files and `05-10-SUMMARY.md` are inside the immutable source snapshot rather than its administrative tail.

For the worktree, lstat every entry under both controlled roots, reject symlinks/unexpected paths, and read bounded bytes from every allowed source and JSON file whether tracked, unchanged, or modified. For the index, use `git ls-files -s -z` to enumerate every stage-0 controlled blob, reject non-regular modes/stages, and read every blob by object ID. For history, enumerate every commit in `base_sha..source_head_sha`; at every commit use `git ls-tree -r -z --full-tree` for both literal roots, reject symlink/non-blob modes and unexpected paths, and inspect every referenced blob, deduplicating only by object ID after recording each path/commit appearance. Do not inspect only changed paths.

Source inspection must enforce the same bounded byte cap as JSON, fatal UTF-8 decoding, no NUL/disallowed controls, and sensitive-value rules for provider credentials, private-key/token forms, actual candidate-profile URLs, and embedded provider-response structures. Where current verifier tests or regex source code intentionally contain a synthetic match, use a finite per-path allowlist of SHA-256 digests of the exact matched token; never exempt an entire file or regex family.

For JSON, call `assertArtifactSchema(relativePath, parsed)` first. Then recursively apply sensitive key/value patterns for secret/candidate/raw/provider variants across snake_case, camelCase, kebab-case, and nested values. A realistic `{results:[{name,title,url,content}]}` object must fail because it is outside every exact artifact schema even when none of the old denylist keys appears.

Compute canonical inventories/digests for worktree, index, baseline-to-source history, and the exact source-head controlled tree. `scanOwnedSurfaces({ repoRoot, phaseDir, baseline, sourceHeadSha })` must also return a live `administrative_tail` covering `source_head_sha..HEAD` with exact `from_source_head_sha`, current `head_sha`, commit count, path count, blob count, canonical inventory SHA-256, and the validated transition for every appearance.

The Phase 5 administrative-tail path allowlist is literal and closed:

- `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`
- `.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json`
- `.planning/phases/05-outreach-feasibility-gate/05-CONTRACT-RECONCILIATION.json`
- `.planning/phases/05-outreach-feasibility-gate/05-09-SUMMARY.md`
- `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md`
- `.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md`
- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`
- `.planning/STATE.md`

Permit only these transitions: decision and zero-residue move once from their exact legacy schemas to their exact accepted schema-version-2 forms; reconciliation moves once from absent to its exact accepted stable semantic/evidence schema; `05-09-SUMMARY.md` moves once from absent to one bounded UTF-8 GSD summary with the required frontmatter/title/self-check structure; `05-REVIEW.md` may move from the pre-gap report to the code-review workflow's final bounded report. For `05-VERIFICATION.md`, derive the initial state exclusively from the committed `source_head_sha` tree/blob, never from the index or current worktree: accept exactly either (a) one exact committed `gaps_found` report at the source head followed by one post-source-head changed-path event whose exact bounded verifier report has `status: passed`, or (b) no path at the committed source head followed by one post-source-head creation whose exact bounded verifier report has `status: passed`. A current untracked `05-VERIFICATION.md` is still bounded, UTF-8/content scanned as a worktree surface but cannot count as the committed initial state, a transition, or a write in this lineage. Reject every other committed initial state; any second post-source-head creation/change; absent→`gaps_found`→`passed`; `passed`→`gaps_found` reversal; deletion/recreation; a final report whose status is not `passed`; or any initial/final schema, structure, field, or content drift. Preserve the existing workflow timing: pre-verifier live comparison does not promote the untracked report into history, and the separate authoritative post-return terminal audit requires one of the two completed committed lineages. ROADMAP may change only the scoped Phase 5 criterion, declared 05-01-through-05-10 plan inventory/checkmarks/count, Phase 5 progress row, and `phase.complete` bookkeeping while retaining the accepted rights no-go and absence of Phase 6/7; REQUIREMENTS may change only OUTR-05's conditional-versus-terminal reconciliation and completion/traceability bookkeeping while leaving later requirements pending; STATE may change only canonical GSD position/progress/metric/session/decision fields for Plan 05-09 and phase completion. Validate every in-scope Markdown state by exact structural predicates plus bounded fatal UTF-8/sensitive-content scanning, and hash every in-scope tail blob into the live inventory. Reject repeat creation, reversal, unknown fields/content, any source/test path after `source_head_sha`, or any unexpected path beneath the two controlled Phase 5 roots. Ignore paths outside those roots and the three exact shared tracking documents: they are explicitly outside the local/Git residue claim and must not be stashed, cleaned, committed, or required clean. `source_changes_allowed` remains false.

The archived record binds the immutable source snapshot and stores that exact allowlist/transition policy, not its own future commit or a predicted tail digest. `buildZeroResidueRecord({ matrix, qualityReport, decisionContract, ownerCheckpoint, baseline, scan })` must serialize that policy and bind the exact decision-contract/receipt/baseline digests. `assertRecordMatchesLiveScan(record, liveScan, { decision, ownerCheckpoint })` must validate static inventories/digests, seven zero counters, decision/receipt digests, source-head ancestry, every exact live administrative transition, the live tail inventory SHA-256, and current controlled worktree/index bytes.

Sanitize every diagnostic path with JSON escaping (or a stable digest plus sanitized surface) before interpolation. Reject or escape C0/C1 and ANSI control characters; never echo the matched content. Implement the exact assertion CLI flags `--assert-zero`, `--repo-root`, `--phase-dir`, `--baseline-record`, `--matrix`, `--quality-report`, `--decision`, `--owner-checkpoint`, and `--record`; reject missing, duplicate, or unknown flags. The CLI must use the captured live scan, validate the full matrix/quality/decision/owner/record chain, invoke `assertRecordMatchesLiveScan`, and print the record source head, current live head, administrative-tail commit/path/blob counts, and administrative-tail inventory SHA-256 without printing sensitive values. It must not discard `scanOwnedSurfaces()` output.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/residue-check.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Worktree, index, and historical enumeration read bounded bytes for every allowed source and JSON path/blob, including unchanged source files.</criterion>
    <criterion>Exact JSON schemas run before sensitive-pattern defense and reject ordinary provider result shapes under every allowed artifact filename.</criterion>
    <criterion>The original pinned baseline/plan blob remains authoritative after later plan edits.</criterion>
    <criterion>Live comparison consumes the scan result, verifies decision and owner-checkpoint digests, and supports only the nine literal Phase 5 administrative paths and exact transitions without a self-referential HEAD field; verification starts from the committed source-head tree and accepts only one exact `gaps_found`→`passed` write or one exact absent→`passed` write.</criterion>
    <criterion>The scanner owns the exact `scanOwnedSurfaces({ repoRoot, phaseDir, baseline, sourceHeadSha })`, `buildZeroResidueRecord({ matrix, qualityReport, decisionContract, ownerCheckpoint, baseline, scan })`, and `assertRecordMatchesLiveScan(record, liveScan, { decision, ownerCheckpoint })` signatures plus the complete assertion CLI flag set.</criterion>
    <criterion>All reported paths are escaped/digested and matched sensitive values never appear in output.</criterion>
  </acceptance_criteria>
  <done>The residue proof measures every claimed byte surface, remains anchored to immutable history, and can be compared truthfully against live repository state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add negative/property regressions for every residue bypass and warning</name>
  <read_first>
    - `scripts/outreach-feasibility/residue-check.test.mjs`
    - `scripts/outreach-feasibility/residue-check.mjs`
    - `scripts/outreach-feasibility/evidence-integrity.test.mjs`
    - `scripts/outreach-feasibility/evidence-integrity.mjs`
    - `.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md`
  </read_first>
  <files>scripts/outreach-feasibility/residue-check.test.mjs</files>
  <behavior>
    - allowlisted source leak in worktree/index/history → failure on the exact surface.
    - realistic provider payload under each allowed JSON artifact type → schema failure.
    - leak committed, removed, then followed by a plan edit → still found from the pinned baseline.
    - archived record whose source head/tree/inventory differs from live reconstruction → failure.
    - exact administrative tail only → pass; source edit or unknown JSON in tail → failure.
    - committed source-head `gaps_found` verification → one exact committed `passed` write passes; any other initial state, repeated write, reversal, non-`passed` final report, or schema/content drift fails.
    - committed source-head verification absence plus a current untracked `gaps_found` report → the untracked file is ignored for lineage, one exact committed `passed` creation passes, and treating the untracked file as the initial committed state fails the regression.
    - filenames containing newline, carriage return, tab, ESC/ANSI, or other controls → output contains only escaped/digested form and cannot forge a second diagnostic line.
  </behavior>
  <action>
Rebuild the temporary-repository fixtures around the exact pinned-baseline schema rather than `git log -1`. Keep all repositories beneath `mkdtemp` and remove them in `finally`.

Add table-driven cases that place a forbidden but non-secret synthetic token in an already allowed `.mjs` path in the worktree, stage it and restore clean worktree bytes, and commit then remove it before the source head. Add realistic nested provider objects using `results`, `name`, `title`, `url`, and `content` to every allowed JSON path/state and assert exact-schema rejection without echoing values.

Add the baseline-movement regression: commit a source/JSON leak after the pinned base, remove it, edit `05-01-PLAN.md` in a later commit, and prove scanning still begins at the original base and finds the leak. Add stale/live-binding mutations for source head, tree digest, range digest, decision digest, owner receipt digest, and every zero counter/surface. Add both passing verification-tail lineages: an exact committed `gaps_found` report at `source_head_sha` followed by one exact committed `passed` report, and no committed verification path at `source_head_sha` plus a current untracked `gaps_found` file followed by one exact committed `passed` creation. In the absent-lineage fixture, assert the untracked report remains outside committed lineage determination and is not normalized, deleted, staged, or committed by the scan. Add table-driven rejections for every other committed initial state; two or more post-source-head changes, including absent→`gaps_found`→`passed`; reversal; deletion/recreation; non-`passed` final status; and initial/final schema, structure, field, or content drift. Keep the existing passing administrative-tail fixture's final review and phase-completion tracking, followed by failing source-edit and unknown-artifact tails. Add an unrelated dirty/untracked/committed path fixture and prove it is excluded from the Phase 5 inventory without being changed or causing failure.

Create filenames containing newline, carriage return, tab, ESC plus an ANSI sequence, and another C0 control supported by the test filesystem. Assert the error has one diagnostic line, contains escaped sequences or a path digest, and never contains raw control bytes or the sensitive matched value.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/residue-check.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Tests independently reproduce and close review CR-02, CR-03, CR-04, CR-06, and WR-01.</criterion>
    <criterion>Tests pass both committed-source-head verification lineages and reject every other initial state, repeated write, reversal, deletion/recreation, non-`passed` final state, and schema/content drift without treating the current untracked report as committed history.</criterion>
    <criterion>Every surface-specific test verifies sensitive contents are absent from diagnostics.</criterion>
    <criterion>Temporary files/repositories are removed on both pass and failure.</criterion>
  </acceptance_criteria>
  <done>Every validated residue blocker and the path-sanitization warning has an executable, held-out negative regression.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/outreach-feasibility/residue-check.mjs`
  - Exact export: `scanOwnedSurfaces({ repoRoot, phaseDir, baseline, sourceHeadSha }): Promise<ResidueScan>`.
  - Exact export: `buildZeroResidueRecord({ matrix, qualityReport, decisionContract, ownerCheckpoint, baseline, scan }): ZeroResidueRecordV2`.
  - Exact export: `assertRecordMatchesLiveScan(record, liveScan, { decision, ownerCheckpoint }): ZeroResidueRecordV2`.
  - New behavior: full worktree/index/history source+JSON byte reads; exact artifact schemas; pinned baseline; source snapshot; live administrative tail with committed-source-head-only verification lineage; path escaping; decision/owner receipt binding.
  - Exact assertion CLI flags: `--assert-zero`, `--repo-root`, `--phase-dir`, `--baseline-record`, `--matrix`, `--quality-report`, `--decision`, `--owner-checkpoint`, `--record`.
  - Exact live administrative-tail allowlist: `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`, `.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json`, `.planning/phases/05-outreach-feasibility-gate/05-CONTRACT-RECONCILIATION.json`, `.planning/phases/05-outreach-feasibility-gate/05-09-SUMMARY.md`, `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md`, `.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`.
- `scripts/outreach-feasibility/residue-check.test.mjs`
  - New worktree/index/history source-byte, provider-payload, baseline-movement, stale-live, administrative-tail, committed-`gaps_found`→single-`passed`, committed-absent→single-`passed`, invalid-verification-lineage, and control-path regressions.
- `.planning/phases/05-outreach-feasibility-gate/05-08-SUMMARY.md` (executor output).
- No provider call, fixture, package, migration, database object, production route, UI/API surface, or schema object.

## Schema Push Gate

No schema push is required. This plan modifies local Node evidence tooling/tests only and contains no recognized schema path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worktree/index/history bytes → scanner | Allowlisted paths remain untrusted; every source/JSON blob requires bounded content inspection. |
| Pinned baseline → source snapshot | History coverage must not move after a leak or later plan edit. |
| Archived record → live repository | Static evidence must match reconstructed source state, current decision/owner digests, and all later administrative commits; current untracked files cannot impersonate committed source-head lineage. |
| Crafted filename → diagnostics | Untrusted path bytes must not forge terminal/JSON output or disclose matched data. |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08-01 | Information Disclosure | Source/JSON residue | high | mitigate | Bounded byte reads for every worktree/index/history blob, exact path/schema allowlists, strict UTF-8, sensitive-pattern defense, and surface-specific negative tests block hidden candidate/provider data. |
| T-05-08-02 | Tampering | Baseline/history | high | mitigate | Exact pinned commit and plan-blob proof plus full per-commit tree enumeration prevent a later plan edit from excluding leak history. |
| T-05-08-03 | Tampering / Elevation | Live assertion | high | mitigate | Source snapshot/tree/range digests, seven counters, decision and owner receipt digests, current worktree/index scan, and exact administrative-tail replay block stale/substituted records; verification initial state is resolved only from the committed source-head tree, not an untracked worktree report. |
| T-05-08-04 | Repudiation | Post-record commits | high | mitigate | The record names a pre-record source head while every later controlled commit is scanned live under an exact finite transition policy; verification permits only committed `gaps_found`→one exact `passed` write or committed absence→one exact `passed` write, rejects all repeated/reversed/drifted lineages, and reports current live head separately. |
| T-05-08-05 | Tampering / Injection | Diagnostic paths | medium | mitigate | JSON escaping/path digests, control-byte rejection, single-line errors, and newline/ANSI tests prevent forged output. |
| T-05-08-06 | Denial of Service | Filesystem/Git scanning | medium | mitigate | Literal roots, finite paths, per-blob size limits, object-ID deduplication, max buffers, and no symlink following keep scans bounded. |
| T-05-08-SC | Tampering | Dependency supply chain | low | accept | No package-manager operation occurs; only Node/Git built-ins are used. |
</threat_model>

<verification>
Run the residue and evidence-integrity tests offline. Do not regenerate the final record yet; Plan 05-09 does so after the fresh owner receipt and accepted-decision hardening exist. The tests must prove the exact builder/scan/assert signatures, reject missing/unknown assertion CLI flags, accept only the nine literal Phase 5 administrative paths with their exact transitions, and cryptographically inventory every allowed tail blob. For verification specifically, prove both committed-source-head lineages (`gaps_found`→one exact `passed` write and absent→one exact `passed` write), prove a current untracked report cannot seed committed lineage, and reject every other initial state, repeated write, reversal, deletion/recreation, non-`passed` final report, or schema/content drift. Ignore and preserve unrelated paths outside the claim.
</verification>

<success_criteria>
- Every allowed source/JSON byte is inspected across worktree, index, and pinned history.
- Exact schemas reject realistic provider payloads.
- The baseline cannot move and the live scan cannot be discarded.
- Stable reconciliation, ROADMAP, REQUIREMENTS, STATE, Plan 05-09 summary, final review, and final verification remain cryptographically and live verifiable as the complete finite Phase 5 administrative tail without a self-referential record HEAD; final verification accepts exactly committed `gaps_found`→one exact `passed` write or committed absence→one exact `passed` write, never derives initial lineage from the current untracked report, and rejects all other/repeated/reversed/drifted states while unrelated repository state remains out of scope and untouched.
- Crafted filenames cannot corrupt or forge diagnostics.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-08-SUMMARY.md` when done.
</output>
