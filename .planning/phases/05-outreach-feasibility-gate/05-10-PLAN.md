---
phase: 05-outreach-feasibility-gate
plan: 10
type: execute
wave: 8
depends_on:
  - 05-08
files_modified:
  - scripts/outreach-feasibility/terminal-audit.mjs
  - scripts/outreach-feasibility/terminal-audit.test.mjs
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "A repository-local terminal-audit command can validate the accepted Phase 5 rights no-go from the live repository without writing a file, Git ref, index refresh, evidence artifact, summary, or commit."
    - "The authoritative terminal audit is an operator/orchestrator step run only after `$gsd-execute-phase 5 --gaps-only` has fully returned from plan closeout, wave-post gates, code review, verification, `phase.complete`, tracking sync, project/todo maintenance, and post hooks."
    - "Plan 05-10 only implements and tests the command and its exact runbook; its task verification is not represented as the authoritative post-execution audit."
    - "The terminal audit validates stable semantic/evidence lineage, computes mutable ROADMAP/REQUIREMENTS/STATE/PROJECT/REVIEW/VERIFICATION fingerprints from live bytes, and reports them only to stdout."
    - "Only exact Phase 5-owned paths must be clean; unrelated dirty, staged, untracked, or committed work is ignored by the Phase 5 claim and remains byte-for-byte unchanged."
    - "Production outreach remains disabled with `NOT_RUN_RIGHTS_NO_GO`, zero provider calls/fixtures/raw results/production mutations, no Phase 6/7 restoration, and no redesign selection."
  artifacts:
    - path: "scripts/outreach-feasibility/terminal-audit.mjs"
      provides: "Read-only stable-contract validator, lifecycle-complete terminal audit, ephemeral fingerprint reporter, and exact post-execution runbook"
      exports:
        - TERMINAL_AUDIT_COMMAND
        - TERMINAL_AUDIT_RUNBOOK
        - extractRoadmapTerminalContract
        - extractRequirementsTerminalContract
        - buildContractReconciliation
        - assertContractReconciliation
        - runTerminalAudit
    - path: "scripts/outreach-feasibility/terminal-audit.test.mjs"
      provides: "Offline final-state, premature-run, scoped-cleanliness, unrelated-work preservation, CLI, and no-write regressions"
  key_links:
    - from: "scripts/outreach-feasibility/terminal-audit.mjs"
      to: "scripts/outreach-feasibility/residue-check.mjs"
      via: "`runTerminalAudit` reconstructs the live scan from `residue.source_snapshot.head_sha` and passes it to `assertRecordMatchesLiveScan`"
      pattern: "assertRecordMatchesLiveScan"
    - from: "scripts/outreach-feasibility/terminal-audit.mjs"
      to: ".planning/phases/05-outreach-feasibility-gate/05-CONTRACT-RECONCILIATION.json"
      via: "live ROADMAP/REQUIREMENTS semantic projections and accepted evidence digests are recomputed against the stable reconciliation"
      pattern: "assertContractReconciliation"
    - from: "TERMINAL_AUDIT_RUNBOOK"
      to: "$gsd-execute-phase 5 --gaps-only"
      via: "the exact authoritative command is explicitly sequenced after execute-phase returns; no unsupported workflow override or hook is claimed"
      pattern: "TERMINAL_AUDIT_COMMAND"
  prohibitions:
    - "Do not modify, wrap, fork, or suppress mandatory execute-plan or execute-phase closeout."
    - "Do not claim Plan 05-10 verification, its SUMMARY, code review, verifier output, or `phase.complete` ran the authoritative terminal audit."
    - "Do not persist final HEAD or raw mutable-document fingerprints in reconciliation/evidence JSON."
    - "Do not require the whole repository to be clean, and do not stash, clean, reset, stage, commit, or delete unrelated work."
    - "Do not call a provider, read `TAVILY_API_KEY`, create fixtures/results, authorize production outreach, restore Phase 6/7, or choose a redesign."
---

<objective>
Implement and test the read-only Phase 5 terminal auditor plus the exact post-execution invocation contract.

Purpose: Mandatory GSD plan and phase closeout continues after every plan task verification, so no PLAN.md replay can certify the final repository. The command created here gives the operator a supported final check after the orchestrator has returned, without asking the executor to skip lifecycle work.
Output: An offline terminal-audit CLI, stable reconciliation helpers consumed by Plan 05-09, adversarial no-write/scoped-cleanliness tests, and an exact after-return runbook.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-06-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-08-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json
@.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json
@.planning/phases/05-outreach-feasibility-gate/05-DECISION.json
@.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json
@.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json
@.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json
@.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json
@scripts/outreach-feasibility/rights-gate.mjs
@scripts/outreach-feasibility/evidence-integrity.mjs
@scripts/outreach-feasibility/owner-checkpoint.mjs
@scripts/outreach-feasibility/decision-evidence.mjs
@scripts/outreach-feasibility/residue-check.mjs
@/Users/jackshi/.codex/gsd-core/workflows/execute-phase.md

<interfaces>
Exact residue contracts from Plan 05-08:

- `scanOwnedSurfaces({ repoRoot, phaseDir, baseline, sourceHeadSha }): Promise&lt;ResidueScan&gt;`
- `assertRecordMatchesLiveScan(record, liveScan, { decision, ownerCheckpoint }): ZeroResidueRecordV2`
- assertion CLI flags: `--assert-zero`, `--repo-root`, `--phase-dir`, `--baseline-record`, `--matrix`, `--quality-report`, `--decision`, `--owner-checkpoint`, `--record`

Exact accepted-lineage contracts from Plans 05-06/05-09:

- `resolveCheckpointedDecisionContractSha256({ decision, matrix, qualityReport }): string`
- `assertOwnerCheckpointRecord({ request, record, baseline, matrix, qualityReport, checkpointedDecisionContractSha256 }): OwnerCheckpointRecord`
- `assertDecisionRecord({ matrix, qualityReport, decision, residue, request, ownerCheckpoint, baseline, requireAccepted, now }): AcceptedDecision`
- `assertArtifactSchema(relativePath, artifact): artifact`

New exact terminal-audit CLI contract:

- modes: exactly one of `--print-runbook`, `--validate-contract`, `--terminal-audit`
- path flags for validation/audit: `--repo-root`, `--phase-dir`
- authoritative command: `env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node scripts/outreach-feasibility/terminal-audit.mjs --terminal-audit --repo-root . --phase-dir .planning/phases/05-outreach-feasibility-gate`
- `--validate-contract` is an intermediate semantic/evidence check for Plan 05-09 and is never authoritative.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement stable reconciliation and the read-only terminal audit</name>
  <read_first>
    - `scripts/outreach-feasibility/terminal-audit.mjs` (create; this task owns it)
    - `scripts/outreach-feasibility/terminal-audit.test.mjs` (create; this task owns it)
    - `scripts/outreach-feasibility/evidence-integrity.mjs`
    - `scripts/outreach-feasibility/owner-checkpoint.mjs`
    - `scripts/outreach-feasibility/decision-evidence.mjs`
    - `scripts/outreach-feasibility/residue-check.mjs`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md`
    - `/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md`
    - `/Users/jackshi/.codex/gsd-core/workflows/execute-phase.md`
  </read_first>
  <files>scripts/outreach-feasibility/terminal-audit.mjs, scripts/outreach-feasibility/terminal-audit.test.mjs</files>
  <behavior>
    - Stable accepted evidence plus reconciled Phase 5 semantics → contract validation passes without requiring final review/verification/phase-complete state.
    - Final accepted evidence, `05-REVIEW.md` clean, `05-VERIFICATION.md` passed, Phase 5 complete tracking, and zero Phase-owned porcelain entries → terminal audit exits zero and emits one sanitized JSON report.
    - Missing/stale evidence, changed semantic projection, non-clean review, non-passed verification, incomplete phase tracking, later Phase 5 source mutation, or Phase-owned dirty/untracked state → nonzero exit with no writes.
    - Unrelated modified/staged/untracked paths → terminal audit still passes, reports their canonical count/digest, and leaves their exact porcelain-v1 `-z` buffer unchanged.
    - Any attempt to supply multiple modes, missing path flags, duplicate flags, or unknown flags → rejection before file/Git inspection.
  </behavior>
  <action>
Write failing tests first using only `node:test`, Node built-ins, and test-owned temporary Git repositories. Implement `extractRoadmapTerminalContract(roadmapText)` and `extractRequirementsTerminalContract(requirementsText)` as exact finite projections: include the Phase 5 goal, the conditional representative-spike intent, the accepted `RIGHTS_NO_GO`/`NOT_RUN_RIGHTS_NO_GO` terminal branch, stopped/no-production/no-Phase-6-or-7 posture, and OUTR-04/OUTR-05 identity/completion semantics; exclude plan checkmarks/counts, progress rows, dates, current-position text, metrics, session data, review prose, and other lifecycle-mutated bytes.

Implement pure `buildContractReconciliation({ matrix, qualityReport, decision, ownerCheckpoint, residue, roadmapText, requirementsText })` and `assertContractReconciliation(...)`. The exact record fields are the Plan 05-06 schema ending in `roadmap_semantic_sha256`, `requirements_semantic_sha256`, and `contract_reconciliation_sha256`; it stores no raw document hash and no final HEAD. Preserve D-09/D-10/D-12/D-13 literally, bind the current rights/no-run/accepted checkpointed-v1-to-accepted-v2/zero-residue digests, keep all five counts zero, and set `quality_claim: "NONE"`. The builder is pure; the terminal-audit CLI must never write the returned object.

Implement `runTerminalAudit({ repoRoot, phaseDir })` as a read-only live scan. Resolve only the exact repository-local Phase 5 evidence paths. Set `GIT_OPTIONAL_LOCKS=0` on every Git subprocess and never invoke a Git command that updates refs, the index, or objects. Capture `HEAD` and raw `git status --porcelain=v1 -z --untracked-files=all` bytes before validation. Parse rename/copy entries without losing either path. Classify Phase 5-owned state as the literal `scripts/outreach-feasibility/` tree, the literal phase directory, and `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/PROJECT.md`; require zero owned entries but permit every other entry. Canonicalize the unrelated entries without printing paths or contents and retain only count plus SHA-256.

Validate the current matrix/quality exact no-go; resolve and assert the durable checkpointed-v1 lineage from the accepted-v2 decision; assert request/owner receipt, accepted decision, stable reconciliation, and complete residue chain; reconstruct `scanOwnedSurfaces` from `residue.source_snapshot.head_sha`; and pass its exact result to `assertRecordMatchesLiveScan`. Require final `05-REVIEW.md` status `clean` or `skipped`, final `05-VERIFICATION.md` status `passed`, Phase 5 10/10 complete ROADMAP/STATE bookkeeping, completed OUTR-04/OUTR-05, stopped Phase-5-only milestone, absent Phase 6/7, false production/search/later-phase flags, null redesign selection, empty quality cases, five zero effect/case counts, and provider retention `NOT_ASSERTED`.

Compute SHA-256 from the live raw bytes of ROADMAP, REQUIREMENTS, STATE, PROJECT, REVIEW, and VERIFICATION only after all assertions pass. Re-read `HEAD` and the raw porcelain buffer; require unchanged HEAD, unchanged complete porcelain buffer, zero Phase 5-owned entries, and byte-identical unrelated entry buffer. Emit one bounded JSON object to stdout with `status: "TERMINAL_AUDIT_PASS"`, `authoritative: true`, live HEAD, evidence digests, live administrative-tail counts/digest, the six ephemeral raw-document fingerprints, and unrelated-worktree count/digest. Do not store the report or require a follow-up artifact/commit.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test --test-concurrency=1 scripts/outreach-feasibility/terminal-audit.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Contract reconciliation binds stable semantic projections and evidence lineage without storing mutable raw-document fingerprints or a predicted final HEAD.</criterion>
    <criterion>The terminal audit reuses the exact receipt, decision, residue, and live-scan interfaces and rejects every nonterminal or drifted Phase 5 state.</criterion>
    <criterion>The command requires only Phase 5-owned paths clean, tolerates unrelated status entries, and proves its full before/after status buffer and HEAD are unchanged.</criterion>
    <criterion>The command prints final document fingerprints ephemerally and creates no artifact.</criterion>
  </acceptance_criteria>
  <done>A read-only command can validate the live post-lifecycle Phase 5 terminal no-go without relying on pre-final mutable hashes or global repository cleanliness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the exact after-return runbook and premature-run failures</name>
  <read_first>
    - `scripts/outreach-feasibility/terminal-audit.mjs`
    - `scripts/outreach-feasibility/terminal-audit.test.mjs`
    - `/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md`
    - `/Users/jackshi/.codex/gsd-core/workflows/execute-phase.md`
  </read_first>
  <files>scripts/outreach-feasibility/terminal-audit.mjs, scripts/outreach-feasibility/terminal-audit.test.mjs</files>
  <behavior>
    - `--print-runbook` → prints the exact two-command order and states that the second command is authoritative only after the first command has fully returned.
    - Calling `--terminal-audit` before final code review, verifier output, phase completion, tracking/project maintenance, or execute-phase return → bounded lifecycle-incomplete failure and no state mutation.
    - Calling `--validate-contract` during Plan 05-09 → validates only stable semantics/evidence and emits `authoritative: false`.
    - Successful terminal invocation against a final-state fixture → stdout only, no new file, no changed file bytes/mode, no changed refs/HEAD, and no changed complete porcelain buffer.
  </behavior>
  <action>
Export byte-exact `TERMINAL_AUDIT_COMMAND` and `TERMINAL_AUDIT_RUNBOOK`. The runbook must say: first run `$gsd-execute-phase 5 --gaps-only` and wait until it returns after every standard closeout/post-phase operation; then, without making another Phase 5 mutation, run `env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node scripts/outreach-feasibility/terminal-audit.mjs --terminal-audit --repo-root . --phase-dir .planning/phases/05-outreach-feasibility-gate`. State that Plan 05-10's own tests and SUMMARY are implementation evidence only, no GSD hook invokes the audit, successful stdout is the final authoritative check, stdout must not be saved as a required evidence artifact, and no commit follows it.

Implement exactly three mutually exclusive CLI modes: `--print-runbook`, `--validate-contract`, and `--terminal-audit`. `--print-runbook` needs no repository read. `--validate-contract` accepts only `--repo-root` and `--phase-dir`, runs stable contract/evidence assertions, emits `authoritative: false`, and does not require final lifecycle artifacts. `--terminal-audit` accepts the same two path flags and performs Task 1's complete live check. Reject every extra, missing, duplicate, or positional argument.

Extend tests with filesystem mode/byte inventories, raw Git ref/HEAD capture, complete NUL-delimited status capture, and a subprocess invocation of each mode. Prove the final-state command changes none of them; prove premature final-state variants fail independently for missing review, non-clean review, missing verification, non-passed verification, incomplete ROADMAP/STATE, dirty Phase 5 source/evidence, and post-source-head code changes. Include unrelated tracked modification, staged modification, and untracked Unicode/control-safe filenames in the passing fixture; assert their exact bytes/status remain unchanged and their names never appear in stdout/stderr.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test --test-concurrency=1 scripts/outreach-feasibility/terminal-audit.test.mjs &amp;&amp; env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node scripts/outreach-feasibility/terminal-audit.mjs --print-runbook</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The printed runbook contains the exact execute-phase command, exact terminal-audit command, strict after-return ordering, and no unsupported terminal-hook claim.</criterion>
    <criterion>Intermediate contract validation is clearly non-authoritative and final validation fails until lifecycle-complete artifacts exist.</criterion>
    <criterion>Successful audit fixtures prove stdout-only operation and preservation of unrelated dirty/staged/untracked state.</criterion>
  </acceptance_criteria>
  <done>The operator has one exact supported post-execution command, and tests prevent Plan 05-10 or any premature lifecycle state from being mislabeled as the terminal audit.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/outreach-feasibility/terminal-audit.mjs`
  - Exact exports: `TERMINAL_AUDIT_COMMAND`, `TERMINAL_AUDIT_RUNBOOK`, `extractRoadmapTerminalContract`, `extractRequirementsTerminalContract`, `buildContractReconciliation`, `assertContractReconciliation`, `runTerminalAudit`.
  - Exact modes: `--print-runbook`, `--validate-contract`, `--terminal-audit`.
  - Exact path flags: `--repo-root`, `--phase-dir`.
  - Final mode reads live final state, writes nothing, requires only Phase 5-owned paths clean, and emits ephemeral fingerprints to stdout.
- `scripts/outreach-feasibility/terminal-audit.test.mjs`
  - Offline final-state and premature-state fixtures; stable-reconciliation, live-tail, scoped-cleanliness, unrelated-preservation, CLI, and no-write coverage.
- `.planning/phases/05-outreach-feasibility-gate/05-10-SUMMARY.md` (normal executor output created before Plan 05-09).
- No terminal result artifact, stored final HEAD, raw final-document fingerprint field, provider call, fixture, quality result, production mutation, package, migration, database object, production route, or schema object.

## Schema Push Gate

No schema push is required. This plan adds local Node evidence tooling/tests only and contains no recognized schema path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD lifecycle → operator terminal audit | Mandatory plan/phase writers must finish and execute-phase must return before the separate authoritative command. |
| Live repository → audit report | Current evidence, lifecycle artifacts, Git surfaces, and document bytes are untrusted until recomputed at invocation time. |
| Phase 5 scope → unrelated worktree state | The audit may assert only owned paths and must preserve unrelated dirty/staged/untracked work. |
| Audit process → stdout | Final fingerprints may be reported ephemerally but must not create a new evidence/commit cycle. |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10-01 | Tampering / Repudiation | Lifecycle ordering | high | mitigate | Exact after-return runbook plus required final review, verification, phase-complete, tracking, and project-state predicates reject premature invocation without overriding GSD. |
| T-05-10-02 | Tampering | Stable reconciliation | high | mitigate | Exact semantic projections, accepted evidence cross-digests, canonical self-hash, and no stored raw mutable hashes keep reconciliation valid across mandatory bookkeeping. |
| T-05-10-03 | Elevation | Rights no-go | high | mitigate | Live matrix, no-run quality, owner receipt, accepted decision, complete residue, stopped milestone, null redesign, and zero-effect assertions independently preserve the no-go. |
| T-05-10-04 | Tampering / Denial of Service | Worktree cleanliness | high | mitigate | NUL-safe scoped status parsing requires only exact Phase 5 paths clean; unrelated entries are hashed, preserved, and never stashed/cleaned/committed. |
| T-05-10-05 | Information Disclosure | Audit output | high | mitigate | Output contains bounded statuses/counts/digests only; no paths, owner bytes, provider data, queries, candidate data, or matched contents are printed. |
| T-05-10-06 | Tampering | Read-only execution | medium | mitigate | `GIT_OPTIONAL_LOCKS=0`, read-only Git commands, before/after HEAD and complete porcelain equality, and filesystem/ref regression fixtures detect writes. |
| T-05-10-SC | Tampering | Dependency supply chain | low | accept | No package-manager operation occurs; installed Node and Git built-ins are the only dependencies. |
</threat_model>

<verification>
Run `terminal-audit.test.mjs` and `--print-runbook` only. These checks prove implementation behavior against offline fixtures and are not the authoritative audit. Plan 05-10 must then close normally through task commits, SUMMARY creation, STATE/ROADMAP/REQUIREMENTS updates, and metadata commit; execute-phase must continue normally.
</verification>

<success_criteria>
- The command detects every known Phase 5 evidence/lifecycle drift from live state and writes nothing.
- Stable reconciliation survives later GSD bookkeeping because it excludes mutable raw-byte fingerprints.
- Unrelated dirty/staged/untracked work is tolerated and preserved exactly.
- Production outreach remains disabled and no provider/live spike/result/redesign/later-phase authorization is introduced.
- No PLAN.md claims authority to skip execute-plan/execute-phase closeout.
</success_criteria>

## Post-execution authoritative check

After `$gsd-execute-phase 5 --gaps-only` fully returns, the operator must run:

`env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node scripts/outreach-feasibility/terminal-audit.mjs --terminal-audit --repo-root . --phase-dir .planning/phases/05-outreach-feasibility-gate`

This separate stdout-only invocation is the authoritative final check. Plan 05-10 verification is not.

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-10-SUMMARY.md` through the normal execute-plan workflow. Then continue all remaining plans and mandatory execute-phase closeout. Only after `$gsd-execute-phase 5 --gaps-only` returns, run the exact read-only terminal-audit command above; do not create or commit an audit-result artifact.
</output>
