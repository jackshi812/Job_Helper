---
phase: 05-outreach-feasibility-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/outreach-feasibility/rights-gate.mjs
  - scripts/outreach-feasibility/rights-gate.test.mjs
  - .planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json
  - .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json
autonomous: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "The exact current LinkedIn/Tavily evidence is machine-readably `RIGHTS_NO_GO` under D-09, D-10, D-11, and D-12."
    - "The reviewed flow distinguishes required local raw-response deletion from the unresolved provider-side retention/deletion boundary, and treats optional company-level caching separately."
    - "Quality is recorded as `NOT_RUN_RIGHTS_NO_GO`, never fabricated as pass, failure, `no_match`, or `coverage_unknown`."
    - "The committed no-go record reports zero provider calls, fixtures, raw results, and production mutations."
    - "Missing, duplicate, stale, digest-drifted, `PROHIBIT`, `AMBIGUOUS`, or unknown required permission rows can never authorize search."
  artifacts:
    - path: "scripts/outreach-feasibility/rights-gate.mjs"
      provides: "Canonical hashing, exact rights-matrix validation, fail-closed evaluation, and no-run report validation"
      exports:
        - REQUIRED_OPERATIONS
        - canonical
        - sha256Json
        - validateRightsMatrix
        - evaluateRights
        - buildNoGoQualityReport
        - assertNoGoQualityReport
    - path: "scripts/outreach-feasibility/rights-gate.test.mjs"
      provides: "Offline denial-path and zero-effect regression coverage"
    - path: ".planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json"
      provides: "Sanitized current-source operation matrix and canonical aggregate digest"
    - path: ".planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json"
      provides: "Truthful rights-blocked no-run quality evidence for OUTR-05"
  key_links:
    - from: ".planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json"
      to: "scripts/outreach-feasibility/rights-gate.mjs"
      via: "validateRightsMatrix and evaluateRights recompute the canonical digest and exact required-operation set"
      pattern: "validateRightsMatrix|evaluateRights"
    - from: "scripts/outreach-feasibility/rights-gate.mjs"
      to: ".planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json"
      via: "buildNoGoQualityReport and assertNoGoQualityReport bind the no-run report to the rights digest"
      pattern: "NOT_RUN_RIGHTS_NO_GO"
  prohibitions:
    - "No Tavily/provider request, API-key read, candidate query, fixture creation, or raw-result handling occurs."
    - "No public visibility fact is treated as permission."
    - "No owner acknowledgement can override a prohibited, ambiguous, missing, stale, or drifted permission row."
    - "No production UI, schema, Edge Function, database, cache, scheduler, or application-search file is touched."
---

<objective>
Encode the binding rights determination and truthful no-run quality evidence for OUTR-04 and OUTR-05.

Purpose: D-09 and D-10 make the current prohibitions and ambiguities an automatic no-go; D-11 defines the exact operations that needed permission; D-12 requires this gate to stop the live quality spike before request one.
Output: A sanitized, digest-bound rights matrix; a fail-closed evaluator with offline tests; and `05-QUALITY-REPORT.json` with `NOT_RUN_RIGHTS_NO_GO` plus four zero effect counters.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md
@scripts/verify-phase-03-9-hosted.mjs
@scripts/verify-phase-03-9-hosted.test.mjs
@scripts/verify-scoring-evidence.mjs
@scripts/verify-scoring-evidence.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the sanitized operation-by-operation rights matrix</name>
  <read_first>
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md`
    - `scripts/verify-scoring-evidence.mjs`
  </read_first>
  <files>.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json</files>
  <action>
Create a versioned finite JSON schema for the evidence reviewed on 2026-07-28, valid through the research cutoff of 2026-08-04. Use exact top-level keys `schema_version`, `phase`, `researched_at`, `valid_until`, `sources`, `operations`, and `rights_evidence_sha256`. Each `sources` row has exactly `source_id`, `official_url`, `source_date_marker`, `retrieved_at`, `clause_id`, `short_paraphrase`, and `evidence_sha256`; compute that row digest over the other six keys using the same canonical JSON plus final-newline rule as the aggregate. Each `operations` row has exactly `operation`, `required`, `status`, and `evidence_refs`, where every reference resolves to a unique `source_id`. Store no full HTML or long policy quotation.

Represent each of the seven required operations exactly once: `public_search`, `transient_owner_review`, `persist_profile_url`, `persist_title_reason`, `manual_networking_purpose`, `delete_local_raw_responses`, and `provider_retention`. Each has `required: true`, an exact status from `ALLOW | PROHIBIT | AMBIGUOUS | NOT_APPLICABLE`, and one or more official evidence references. Set the local-deletion row to `ALLOW` as required D-15 hygiene without implying that deletion cures an unauthorized acquisition. Set the provider-retention row to `AMBIGUOUS` because provider-side query/output retention and deletion limitations remain unresolved; local cleanup must not upgrade it. Preserve the current LinkedIn prohibitions and Tavily ambiguities from the research; do not infer permission from public visibility or technical API support, per D-09 and D-10. Record exactly one `company_level_cache` row separately with `required: false`, status `AMBIGUOUS`, and current evidence references, leaving it unauthorized under D-11.

Define `rights_evidence_sha256` over a canonical projection that excludes the digest field itself, sorts object keys recursively, preserves array order, serializes JSON, and appends one final newline before SHA-256. State that this digest detects evidence-record drift but does not authenticate the owner or provide legal permission.
  </action>
  <verify>
    <automated>node -e "const {createHash}=require('node:crypto'); const m=require('./.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json'); const exact=(o,k)=&gt;{if(JSON.stringify(Object.keys(o).sort())!==JSON.stringify([...k].sort())) throw Error('schema')}; const canonical=v=&gt;Array.isArray(v)?v.map(canonical):v&amp;&amp;typeof v==='object'?Object.fromEntries(Object.keys(v).sort().map(k=&gt;[k,canonical(v[k])])):v; const hash=v=&gt;createHash('sha256').update(JSON.stringify(canonical(v))+'\\n','utf8').digest('hex'); const required=['public_search','transient_owner_review','persist_profile_url','persist_title_reason','manual_networking_purpose','delete_local_raw_responses','provider_retention']; exact(m,['schema_version','phase','researched_at','valid_until','sources','operations','rights_evidence_sha256']); if(m.schema_version!==1||m.phase!=='05'||m.researched_at!=='2026-07-28'||m.valid_until!=='2026-08-04'||!Array.isArray(m.sources)||m.sources.length===0||!Array.isArray(m.operations)||m.operations.length!==8) throw Error('shape'); const ids=new Set; for(const s of m.sources){exact(s,['source_id','official_url','source_date_marker','retrieved_at','clause_id','short_paraphrase','evidence_sha256']); if(typeof s.source_id!=='string'||ids.has(s.source_id)||!s.official_url.startsWith('https://')) throw Error('source'); ids.add(s.source_id); const {evidence_sha256,...body}=s; if(!/^[0-9a-f]{64}$/.test(evidence_sha256)||evidence_sha256!==hash(body)) throw Error('evidence digest')} for(const o of m.operations){exact(o,['operation','required','status','evidence_refs']); if(!['ALLOW','PROHIBIT','AMBIGUOUS','NOT_APPLICABLE'].includes(o.status)||!Array.isArray(o.evidence_refs)||o.evidence_refs.length===0||o.evidence_refs.some(r=&gt;!ids.has(r))) throw Error('operation')} if(required.some(n=&gt;m.operations.filter(o=&gt;o.operation===n&amp;&amp;o.required===true).length!==1)||m.operations.filter(o=&gt;o.required===true).length!==required.length) throw Error('required set'); const local=m.operations.filter(o=&gt;o.operation==='delete_local_raw_responses'&amp;&amp;o.required===true&amp;&amp;o.status==='ALLOW'), provider=m.operations.filter(o=&gt;o.operation==='provider_retention'&amp;&amp;o.required===true&amp;&amp;o.status==='AMBIGUOUS'), cache=m.operations.filter(o=&gt;o.operation==='company_level_cache'&amp;&amp;o.required===false&amp;&amp;o.status==='AMBIGUOUS'); if(local.length!==1||provider.length!==1||cache.length!==1||m.operations.every(o=&gt;!o.required||o.status==='ALLOW')) throw Error('classification'); const {rights_evidence_sha256,...body}=m; if(!/^[0-9a-f]{64}$/.test(rights_evidence_sha256)||rights_evidence_sha256!==hash(body)) throw Error('aggregate digest');"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The automated command proves every top-level, source-evidence, and operation object has its exact key set; all seven required rows are unique; every evidence reference resolves; and at least one required status blocks.</criterion>
    <criterion>Every source-evidence digest and the aggregate rights digest recompute from the documented canonical projection.</criterion>
    <criterion>The JSON contains only sanitized source metadata, clause identifiers, short paraphrases, statuses, and digests—no policy-page body or candidate data.</criterion>
    <criterion>The required local-deletion row, ambiguous provider-retention/deletion row, and exactly one optional ambiguous cache row are distinct and plainly classified for owner review, per D-11 and D-15.</criterion>
  </acceptance_criteria>
  <done>The exact researched flow is represented as a finite, sanitized, digest-bound rights matrix whose current rows cannot be interpreted as permission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the fail-closed rights gate and no-run quality evidence</name>
  <read_first>
    - `scripts/outreach-feasibility/rights-gate.mjs` (create if absent; this task owns it)
    - `scripts/outreach-feasibility/rights-gate.test.mjs` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-PATTERNS.md`
    - `scripts/verify-phase-03-9-hosted.mjs`
    - `scripts/verify-phase-03-9-hosted.test.mjs`
    - `scripts/verify-scoring-evidence.mjs`
  </read_first>
  <files>scripts/outreach-feasibility/rights-gate.mjs, scripts/outreach-feasibility/rights-gate.test.mjs, .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json</files>
  <behavior>
    - Current committed matrix → `status: RIGHTS_NO_GO`, `search_authorized: false`, `quality_status: NOT_RUN_RIGHTS_NO_GO`, and every effect counter equals zero.
    - Synthetic matrix with exactly one current, digest-valid `ALLOW` row for every required operation → rights `PASS`; owner acknowledgement is not accepted as a substitute for any permission row.
    - Missing, duplicate, extra, unknown-status, stale, malformed, or digest-drifted evidence → `RIGHTS_NO_GO` before any effect.
    - No-run report with any case outcome, candidate-like data, or nonzero effect counter → validation failure.
  </behavior>
  <action>
Write the failing `node:test` cases first, then implement ESM exports `REQUIRED_OPERATIONS`, `canonical`, `sha256Json`, `validateRightsMatrix`, `evaluateRights`, `buildNoGoQualityReport`, and `assertNoGoQualityReport` using Node built-ins only. Use one shared canonical-hash implementation with the research-required final newline; do not copy the older analog's newline-free hash.

`validateRightsMatrix` must reject unknown top-level/operation/evidence keys, duplicate or missing required operations, unresolved evidence references, malformed per-source or aggregate digests, unknown statuses, and evidence past `valid_until`. It must require the distinct local-deletion and provider-retention rows plus the single optional cache row defined by Task 1. `evaluateRights` may return `PASS` only when every required operation is current, digest-valid, and exactly `ALLOW`; all other states return `RIGHTS_NO_GO`. It must not accept an owner override.

Create `05-QUALITY-REPORT.json` from the current matrix with exact fields `status: NOT_RUN_RIGHTS_NO_GO`, `rights_status: RIGHTS_NO_GO`, `search_authorized: false`, `cases: []`, and zero `provider_call_count`, `fixture_count`, `raw_result_count`, and `production_mutation_count`. Bind it to `rights_evidence_sha256` and its own canonical quality-evidence digest. Per D-05 through D-08, do not manufacture threshold results or case outcomes when D-12 prevented execution.

Add an import-safe CLI using `fileURLToPath`/`pathToFileURL` equality. Support `--assert-no-go`, `--matrix`, and `--quality-report`; the command validates the committed artifacts and prints only sanitized status/count JSON. The module must not import a provider SDK, read any provider environment variable, construct a provider request, or perform network/file cleanup work.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/rights-gate.test.mjs &amp;&amp; env -u TAVILY_API_KEY node scripts/outreach-feasibility/rights-gate.mjs --assert-no-go --matrix .planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json --quality-report .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The real-matrix test proves `RIGHTS_NO_GO`, `NOT_RUN_RIGHTS_NO_GO`, and four exact zero counters.</criterion>
    <criterion>One-field mutation tests prove every missing, duplicate, stale, malformed, drifted, prohibited, ambiguous, and unknown permission state fails closed.</criterion>
    <criterion>The no-run report validator rejects every fabricated case outcome, candidate-data field/value, nonzero effect count, or rights-digest mismatch.</criterion>
    <criterion>The focused test and CLI command pass with the provider key removed from the environment and without network access.</criterion>
  </acceptance_criteria>
  <done>The current rights evidence deterministically stops the spike and produces truthful, machine-readable no-run quality evidence for OUTR-05.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/outreach-feasibility/rights-gate.mjs`
  - Constants/functions: `REQUIRED_OPERATIONS`, `canonical`, `sha256Json`, `validateRightsMatrix`, `evaluateRights`, `buildNoGoQualityReport`, `assertNoGoQualityReport`.
  - CLI flags: `--assert-no-go`, `--matrix`, `--quality-report`.
- `scripts/outreach-feasibility/rights-gate.test.mjs`.
- `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
  - Core fields: `schema_version`, `phase`, `researched_at`, `valid_until`, `sources`, `operations`, `rights_evidence_sha256`.
- `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
  - Core fields: `schema_version`, `phase`, `status`, `rights_status`, `search_authorized`, `rights_evidence_sha256`, `quality_evidence_sha256`, `cases`, `provider_call_count`, `fixture_count`, `raw_result_count`, `production_mutation_count`.
- `.planning/phases/05-outreach-feasibility-gate/05-01-SUMMARY.md` (executor output).
- No class, package, database object, UI route, API route, provider request, or production symbol is added.

## Schema Push Gate

No schema push is required. This plan contains no `supabase/migrations/*.sql` path and deliberately excludes production schema, UI, and functions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official-source research → committed matrix | External legal/provider evidence becomes a sanitized local decision input. |
| Matrix → rights evaluator | Untrusted or drifted JSON could otherwise widen authorization. |
| Rights verdict → conditional future provider boundary | A no-go must stop before any secret, corpus, request, or provider effect. |
| Evidence → owner decision | Digest-bound evidence must remain distinct from owner identity/attestation. |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Tampering / Elevation | `evaluateRights` | high | mitigate | Exact required-operation set, strict statuses, currentness, canonical digests, and denial-path tests make accidental provider admission impossible under the current matrix. |
| T-05-01-02 | Information Disclosure | `rights-gate.mjs` | high | mitigate | The module has no provider-key read, request construction, SDK, or network import; focused tests run with the key removed. |
| T-05-01-03 | Information Disclosure | `05-QUALITY-REPORT.json` | high | mitigate | Finite exact schema, recursive candidate-data rejection, empty cases, and four zero counters prevent raw third-party response persistence. |
| T-05-01-04 | Spoofing / Tampering | Future owner acknowledgement | high | mitigate | Owner input is not accepted in this plan and cannot upgrade any permission row; later exact attestation binds the rights digest. |
| T-05-01-05 | Tampering | `05-RIGHTS-MATRIX.json` | high | mitigate | Source/date/clause metadata, validity cutoff, per-row digests, and canonical aggregate digest make source/evidence drift fail closed. |
| T-05-01-06 | Elevation | Dormant spike admission | high | mitigate | `PASS` requires every named required operation to be explicitly `ALLOW`; missing, ambiguous, prohibited, stale, or drifted rows return `RIGHTS_NO_GO`. |
| T-05-01-SC | Tampering | Dependency supply chain | low | accept | No package-manager operation occurs; implementation uses installed Node built-ins only. |
</threat_model>

<verification>
Run the focused rights test and `--assert-no-go` CLI with `TAVILY_API_KEY` absent. Confirm the CLI emits only `RIGHTS_NO_GO`, `NOT_RUN_RIGHTS_NO_GO`, `search_authorized: false`, and four zero effect counters.
</verification>

<success_criteria>
- OUTR-04 is resolved to the current binding no-go without treating public visibility or technical capability as permission.
- OUTR-05 is truthfully represented as not run because its D-12 prerequisite failed.
- The rights/no-run artifacts are digest-bound, offline-verifiable, and contain no candidate data.
- No production file or schema is modified and no provider effect is possible.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-01-SUMMARY.md` when done.
</output>
