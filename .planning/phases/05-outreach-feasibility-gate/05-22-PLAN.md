---
phase: 05-outreach-feasibility-gate
plan: 22
type: execute
wave: 20
depends_on:
  - 05-20
files_modified:
  - scripts/outreach-feasibility/residue-check.mjs
  - scripts/outreach-feasibility/residue-check.test.mjs
  - scripts/outreach-feasibility/evidence-integrity.mjs
  - scripts/outreach-feasibility/evidence-integrity.test.mjs
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05

must_haves:
  truths:
    - "The live administrative scanner admits the actual 05-19 plan/summary transition and every later contiguous gap-plan/summary pair only after exact frontmatter, inventory, creation-once, and byte-immutability validation."
    - "Zero-residue validation deep-compares the recorded and live controlled worktree/index projections, including exact sorted dirty/staged path lists and inventory hashes, and requires zero controlled dirty and staged entries."
    - "Pre-existing unrelated modified, staged, and untracked owner work remains outside the Phase 5 claim and is byte-for-byte and porcelain-byte-for-porcelain-byte unchanged on every pass and failure."
    - "ROADMAP, REQUIREMENTS, and STATE transitions permit only exact Phase 5 named fields/nodes; unrelated checkboxes, phase rows, requirements, decisions, or state keys cannot be filtered away."
    - "A passed verification report has a positive N/N score equal to its complete uniquely numbered must-have table, every must-have is verified, and OUTR-04/OUTR-05 are explicitly verified."
    - "The immutable source-lineage residue remains validated byte-exactly as schema v3, while every newly published and terminally consumed residue record is schema v4 with status_paths, staged_paths, and administrative_tail_policy.allowed_paths; migration is deterministic and tested."
  artifacts:
    - path: "scripts/outreach-feasibility/residue-check.mjs"
      provides: "Dynamic exact administrative inventory, controlled worktree/index binding, exact tracking projections, and complete verification parser"
      exports:
        - "deriveAdministrativeTailPolicy"
        - "assertCompleteVerificationDocument"
        - "scanOwnedSurfaces"
        - "assertRecordMatchesLiveScan"
    - path: "scripts/outreach-feasibility/residue-check.test.mjs"
      provides: "Actual 05-19+ lifecycle, dirty-surface, unrelated-dirt, tracking-mutation, and verification-forgery regressions"
    - path: "scripts/outreach-feasibility/evidence-integrity.mjs"
      provides: "Exact immutable v3 source validator plus strengthened v4 authenticated residue schema for explicit path lists and dynamic finite tail policy"
    - path: "scripts/outreach-feasibility/evidence-integrity.test.mjs"
      provides: "Exact strengthened residue schema and mutation coverage"
  key_links:
    - from: "scripts/outreach-feasibility/residue-check.mjs"
      to: ".planning/phases/05-outreach-feasibility-gate/05-19-PLAN.md"
      via: "contiguous plan inventory and exact plan/summary ID/frontmatter pairing"
      pattern: "deriveAdministrativeTailPolicy"
    - from: "scripts/outreach-feasibility/residue-check.mjs"
      to: ".planning/ROADMAP.md"
      via: "named Phase 5 structured projection with all other bytes immutable"
      pattern: "assertTrackingTransition"
    - from: "scripts/outreach-feasibility/residue-check.mjs"
      to: ".planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md"
      via: "status, score, must-have inventory, and requirements agree"
      pattern: "assertCompleteVerificationDocument"
---

<objective>
Make live zero-residue and administrative lifecycle evidence match the actual repository while protecting unrelated owner work and rejecting forged completion.

Purpose: The current scanner rejects its own 05-19 summary, ignores authenticated dirty/index surfaces, hides unrelated tracking changes behind broad filters, and accepts syntactic verification passes. OUTR-04 and OUTR-05 need exact live convergence rather than global repository cleanliness.
Output: Dynamic finite tail validation, strengthened controlled-surface schema, exact planning projections, complete verification semantics, and regressions against the real 05-19+ lifecycle.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-19-PLAN.md
@.planning/phases/05-outreach-feasibility-gate/05-19-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-20-PLAN.md
@.planning/phases/05-outreach-feasibility-gate/05-21-PLAN.md
@scripts/outreach-feasibility/residue-check.mjs
@scripts/outreach-feasibility/residue-check.test.mjs
@scripts/outreach-feasibility/evidence-integrity.mjs
@scripts/outreach-feasibility/evidence-integrity.test.mjs

<interfaces>
Contracts available from dependency plans:
- `readAcceptedEvidencePair(...)` and `recoverAcceptedEvidencePair(...)` recover the locked journal before pair consumption.
- `PHASE_5_REVIEWED_PATHS` is the sole exact ordered 20-file review inventory.
- `assertPhase5ReviewLifecycle(...)` requires the byte-pinned immutable-source 7-critical/4-warning issues_found report followed by exact live clean 20/20 bytes; tests never obtain prior issues bytes from mutable live `05-REVIEW.md`.
Existing residue contracts:
- `scanOwnedSurfaces({ repoRoot, phaseDir, baseline, sourceHeadSha })` reconstructs source/history/worktree/index/tail state.
- `buildZeroResidueRecord(...)` persists the bounded local/Git claim.
- `assertRecordMatchesLiveScan(record, liveScan, ...)` is the final live equality gate.
- The immutable base remains `e1d592e8b574ae3e474ce44661b3970954ef00d9` and the exact 05-01 plan blob.
- Residue schema v3 is immutable source lineage only. New publication uses schema v4, whose canonical added fields are `git_surfaces.worktree.status_paths`, `git_surfaces.index.staged_paths`, and `administrative_tail_policy.allowed_paths`.
</interfaces>
</context>

<prohibitions>
- Do not require global repository cleanliness. Only exact Phase 5 controlled paths may be required clean.
- Never run `git stash`, `git clean`, `git reset`, broad checkout/restore, or any command that changes unrelated modified/staged/untracked owner work.
- Do not access the owner private key, create a signature, modify the public request/signature/trust artifacts, call a provider/network endpoint, run a spike, create fixtures/raw results, or mutate production.
- Do not invoke or overwrite the canonical review, mark the phase complete, run schema push, or invoke the live authoritative terminal audit.
- Do not accept regex/line-class filtering as a tracking comparison. Every mutable field/node must be named, parsed uniquely, projected, and range-checked.
- Do not solve self-reference by ignoring the entire worktree/index. Exclude only the exact separately authenticated pair and administrative paths from the static inventory; validate those excluded paths through their dedicated pair/tail gates.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate the actual 05-19+ administrative plan/summary tail</name>
  <read_first>
    - scripts/outreach-feasibility/residue-check.mjs
    - scripts/outreach-feasibility/residue-check.test.mjs
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - scripts/outreach-feasibility/evidence-integrity.test.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-19-PLAN.md
    - .planning/phases/05-outreach-feasibility-gate/05-19-SUMMARY.md
  </read_first>
  <files>scripts/outreach-feasibility/residue-check.mjs, scripts/outreach-feasibility/residue-check.test.mjs, scripts/outreach-feasibility/evidence-integrity.mjs, scripts/outreach-feasibility/evidence-integrity.test.mjs</files>
  <behavior>
    - "RED: a fixture reproducing source HEAD followed by the real 05-19 summary, the byte-pinned immutable-source issues_found review, and later Phase 5 administrative commits is admitted up to the required pending/clean lifecycle state without reading mutable live 05-REVIEW.md for prior bytes."
    - "RED: source inventories ending before 05-19 and inventories continuing through 05-20+ admit only contiguous exact PLAN/SUMMARY pairs whose IDs, frontmatter, requirements, gap_closure flag, status, and self-check agree."
    - "RED: skipped IDs, summary-without-plan, plan-without-summary at terminal completion, wrong phase/plan, edited summary, duplicate creation, deletion, unrelated phase path, code/source edit, or plan outside the discovered finite range fails closed."
    - "GREEN: the dynamic allowed-path array written into the residue policy is exact, sorted, finite, and deep-equal to the live derived policy."
    - "RED: exact immutable schema v3 source bytes validate only as lineage/migration input; final publication or terminal consumption of v3 is rejected, and deterministic v3-to-v4 migration produces the required canonical v4 fields without altering the v3 source bytes."
  </behavior>
  <action>
    Replace the fixed 05-09 summary special case with `deriveAdministrativeTailPolicy`. Derive the source and final Phase 5 PLAN/SUMMARY inventories from Git trees, require two-digit contiguous IDs starting at 01, and admit only exact newly appearing/matching plan-summary pairs between the source inventory and final inventory. A plan validator must require filename/frontmatter agreement, `phase: 05-outreach-feasibility-gate`, `type: execute`, `gap_closure: true` for post-19 plans, both OUTR-04 and OUTR-05 requirements, and no checkpoint-owned review/audit authority. A summary validator must require matching phase/plan, `status: complete`, and `## Self-Check: PASSED`. Existing source summaries are immutable; new plan/summary files are create-once and cannot be edited/deleted.

    Build failing fixtures from the actual 05-19 plan/summary bytes and the byte-pinned immutable source-head 20-file issues_found report, then extend them with exact 05-20+ administrative pairs. The issues bytes must come from that immutable Git blob or a byte-exact deterministic fixture with an asserted SHA-256, never from mutable live `05-REVIEW.md`; the live path is reserved for the future clean post-hook report. Preserve the narrow source-change rule: only derived plan/summary documents plus decision, residue, reconciliation, review, verification, and exact tracking documents may appear after the source head. Store the resulting exact `administrative_tail_policy.allowed_paths` in both the scan and authenticated policy; do not accept globs or prefix-only authorization.

    Define residue schema v4 as the only newly publishable/terminal schema. It retains every existing v3 field and adds finite validation for `git_surfaces.worktree.status_paths`, `git_surfaces.index.staged_paths`, and canonical `administrative_tail_policy.allowed_paths`. Preserve a separate exact immutable v3 validator only for source lineage; it must accept the historical bytes without rewriting or widening them. Add a tested deterministic v3-to-v4 migration that derives the new lists/policy from the authenticated live scan, rejects unknown/malformed v3 input, never copies unauthenticated attacker-provided v4 fields, and cannot publish v3 as final. The existing owner signature and stopped payload remain unchanged; no signing path or private-key access is introduced. Per D-13 and D-17, this is a source/tail evidence repair, not new search authority, and it must preserve zero retained candidate or provider data.
  </action>
  <acceptance_criteria>
    - The actual 05-19 summary transition passes the isolated lineage regression.
    - Contiguous 05-20+ plan/summary pairs are derived and validated exactly.
    - Any gap, mismatch, edit, deletion, duplicate, unknown path, or source edit fails.
    - Persisted and live `administrative_tail_policy.allowed_paths` arrays are exact, sorted, unique, finite, and equal.
    - Exact v3 bytes remain valid as immutable lineage only, deterministic v3-to-v4 migration is covered, and only schema v4 can be newly published or terminally consumed.
  </acceptance_criteria>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/residue-check.test.mjs scripts/outreach-feasibility/evidence-integrity.test.mjs</automated>
  </verify>
  <done>The live scanner understands the real final 05-19 transition and later contiguous gap plans without broadening administrative-tail authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind clean controlled worktree and index projections</name>
  <read_first>
    - scripts/outreach-feasibility/residue-check.mjs
    - scripts/outreach-feasibility/residue-check.test.mjs
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json
    - .planning/phases/05-outreach-feasibility-gate/05-DECISION.json
  </read_first>
  <files>scripts/outreach-feasibility/residue-check.mjs, scripts/outreach-feasibility/residue-check.test.mjs, scripts/outreach-feasibility/evidence-integrity.mjs, scripts/outreach-feasibility/evidence-integrity.test.mjs</files>
  <behavior>
    - "RED: independent mutation of worktree dirty count, exact sorted status path list, static path/blob count, or inventory SHA-256 fails live equality."
    - "RED: independent mutation of index staged count, exact sorted staged path list, static path/blob count, or inventory SHA-256 fails live equality."
    - "RED: one controlled modified/untracked path or staged path blocks PASS; unrelated modified, staged, renamed, deleted, and untracked paths remain permitted and byte/porcelain identical."
    - "RED: symlinked controlled or transaction path fails, while self-referential pair and exact administrative paths are excluded only from the static inventory and remain validated by pair/tail gates."
    - "RED: v4 rejects missing, duplicate, unsorted, noncanonical, or extra `status_paths`, `staged_paths`, or `administrative_tail_policy.allowed_paths`; exact v3 source input migrates to the same v4 record as direct construction."
  </behavior>
  <action>
    Extend worktree and index surfaces with exact sorted unique `status_paths` and `staged_paths`. Preserve status semantics, including both sides of rename/copy entries, and count the exact controlled porcelain entries rather than all repository dirt. Project the static controlled inventory by excluding only the two authenticated pair files and exact paths admitted by the derived administrative-tail policy; those bytes remain mandatory through pair recovery/schema/cross-hash and tail transition validation.

    In `assertRecordMatchesLiveScan`, require recorded/live deep equality for both projected worktree and index surfaces and explicitly require controlled `status_entry_count === 0`, `status_paths` empty, `staged_path_count === 0`, and `staged_paths` empty. Keep the immutable source/history comparisons. Update the finite v4 schema keys and record hashing so count, path-list, and inventory mutations are independently authenticated. Keep canonical naming identical in constructors, validators, hashes, migration, tests, and consumers: `administrative_tail_policy.allowed_paths` is the only accepted administrative-tail field; reject every noncanonical alias.

    Add test-owned repositories containing unrelated tracked modifications, staged content, rename/delete state, Unicode/control-safe filenames, and untracked files. Capture their full porcelain bytes and file bytes before each passing/failing scan and require exact preservation afterward. Per D-14 through D-17, Phase 5 code must issue only path-scoped read-only Git commands; it must never normalize or clean the owner worktree, surface unrelated data, or retain raw records.
  </action>
  <acceptance_criteria>
    - Every recorded/live worktree and index field is deep-compared.
    - Any controlled dirty or staged path prevents PASS.
    - Self-referential/administrative exclusions are exact and independently validated.
    - Unrelated owner porcelain and bytes are unchanged on success and every failure.
  </acceptance_criteria>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/residue-check.test.mjs</automated>
  </verify>
  <done>Zero-residue PASS proves clean controlled worktree/index surfaces without imposing or manufacturing global repository cleanliness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace broad tracking filters and forged verification passes</name>
  <read_first>
    - scripts/outreach-feasibility/residue-check.mjs
    - scripts/outreach-feasibility/residue-check.test.mjs
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
  </read_first>
  <files>scripts/outreach-feasibility/residue-check.mjs, scripts/outreach-feasibility/residue-check.test.mjs</files>
  <behavior>
    - "RED: mutating an unrelated ROADMAP milestone/phase/checkbox, OUTR-06 or another REQUIREMENTS row, or STATE milestone/decision/todo key is detected even when the changed line resembles an allowed completion/status line."
    - "RED: duplicate headings/keys, moved nodes, ambiguous Phase 5 sections, and extra mutable-looking lines fail rather than being discarded."
    - "RED: passed reports with 0/0, 0/N, partial N/M, denominator/table mismatch, missing/duplicate truth IDs, any non-verified truth row, or missing/non-verified OUTR-04/OUTR-05 coverage fail."
    - "GREEN: the current 13/21 gaps report parses as incomplete source lineage and an exact positive N/N report with N matching the complete must-have inventory parses as passed."
  </behavior>
  <action>
    Remove `immutableLines` and every broad mutable regex. Parse one exact Phase 5 ROADMAP section and permit only named plan-count counters, exact Phase 5 plan checklist rows/wave references, and the Phase 5 progress row. Parse REQUIREMENTS and permit only the OUTR-04/OUTR-05 checkbox plus their traceability status cells and exact coverage counters when those values are orchestrator-owned. Parse STATE frontmatter and body uniquely and permit only the named current Phase 5 status/position/progress/activity fields plus exact Phase 05 plan metric/session-continuity entries. Replace permitted values with typed sentinels and byte-compare all remaining content; reject duplicate/missing/out-of-range nodes.

    Export `assertCompleteVerificationDocument`. Parse score numerator/denominator as safe positive integers, parse the Observable Truths inventory as uniquely numbered 1..N rows with nonempty truth/status/evidence cells, and require the numerator to equal both denominator and row count for `status: passed`. Every passed row must be a verified status. Parse Requirements Coverage and require exactly OUTR-04 and OUTR-05 as verified/complete. For the source `gaps_found` report, require its numerator to equal its actually verified truth rows and its denominator to equal the complete table, so even the prior lineage is semantic rather than syntactic. This proves the stopped, no-quality-claim outcome of D-12 and D-13 without admitting any data prohibited by D-14 through D-17.
  </action>
  <acceptance_criteria>
    - Only exact named Phase 5 tracking nodes may change; every other byte remains equal.
    - Unrelated checkbox/status/completion lookalikes are rejected.
    - A passed verification report proves positive N/N and a complete matching verified truth inventory.
    - OUTR-04 and OUTR-05 must both appear with verified terminal status.
  </acceptance_criteria>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/residue-check.test.mjs</automated>
  </verify>
  <done>Administrative tracking and verification completion are exact structured claims; broad filtering and score forgery cannot hide incomplete or unrelated mutations.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

- Export `deriveAdministrativeTailPolicy(...)` from `residue-check.mjs`.
- Export `assertCompleteVerificationDocument(text, expectedStatus)` from `residue-check.mjs`.
- Define residue schema v4 for all new publication and terminal consumption; it includes `git_surfaces.worktree.status_paths`, `git_surfaces.index.staged_paths`, and `administrative_tail_policy.allowed_paths`.
- Retain an exact immutable schema v3 source-lineage validator and a deterministic tested v3-to-v4 migration; v3 is never accepted as a newly published or terminal record.
- Add `administrative_tail_policy.allowed_paths` as the exact sorted finite live policy derived from source/final Phase 5 inventories.
- Add administrative transition kind `plan_summary_contiguous_once` for exact post-source PLAN/SUMMARY pairs.
- Add `git_surfaces.worktree.status_paths` and `git_surfaces.index.staged_paths` to authenticated residue records; both are sorted unique controlled path arrays.
- Retain `status_entry_count`, `staged_path_count`, `path_count`, `blob_count`, and `inventory_sha256`, now deep-compared against live projected surfaces.
- Tracking projections cover exact ROADMAP Phase 5 counters/checklists/progress row, REQUIREMENTS OUTR-04/OUTR-05 rows/traceability/coverage counters, and named STATE Phase 5 position/progress/activity/metric/session fields.
- Verification inventory fields are parsed from existing frontmatter `status` and `score`, the `### Observable Truths` rows, and `### Requirements Coverage` rows; no new report field or CLI flag is introduced.
- No new persistent path, dependency, signature, schema file, provider capability, or production artifact is introduced.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git source head → administrative tail | Only exact contiguous plan/summary and named closeout transitions may follow source code. |
| owner worktree/index → Phase 5 residue claim | Controlled dirt must block while unrelated owner work remains untouched and undisclosed. |
| planning documents → immutable source projection | Only exact named Phase 5 fields may change. |
| verifier Markdown → terminal completion | Passed status must be backed by complete N/N must-have and requirement inventories. |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-22-01 | Tampering | 05-19+ administrative tail | high | mitigate | Derive a finite contiguous inventory; exact PLAN/SUMMARY validators; reject edits, gaps, deletions, unknown paths, and source changes. |
| T-05-22-02 | Tampering | Controlled worktree/index | high | mitigate | Persist and deep-compare exact path lists/counts/hashes and require zero controlled dirty/staged entries. |
| T-05-22-03 | Information Disclosure | Unrelated owner dirt | high | mitigate | Path-scoped Git reads, sanitized aggregate claims, and byte/porcelain preservation tests; no cleanup or path disclosure. |
| T-05-22-04 | Spoofing | Symlink/path substitution | high | mitigate | lstat/no-follow checks for controlled and transaction paths plus canonical root/path derivation. |
| T-05-22-05 | Tampering | Tracking comparison | high | mitigate | Unique structured projections for exact named fields and byte equality for everything else. |
| T-05-22-06 | Spoofing | Verification score/inventory | high | mitigate | Positive N/N equality, complete numbered truth rows, all verified statuses, and explicit OUTR-04/OUTR-05 terminal coverage. |
| T-05-22-07 | Repudiation | Stale review scope | high | mitigate | Reuse the one exported exact 20-file lifecycle and byte-pinned immutable issues_found lineage from Plan 05-21. |
| T-05-22-08 | Tampering | Residue schema migration | high | mitigate | Byte-exact immutable v3 lineage validation, deterministic v3-to-v4 derivation, canonical v4-only publication/terminal consumption, and mutation regressions for every added path field. |
| T-05-22-SC | Tampering | Dependency supply chain | low | accept | Node built-ins and Git only; no package installation occurs. |
</threat_model>

<verification>
Run all focused tests against temporary repositories with `TAVILY_API_KEY` absent. Test both actual 05-19 bytes and later exact gap-plan fixtures. Snapshot unrelated porcelain/file bytes before every scanner call and compare after. Do not change the live review, tracking files, or accepted artifacts in this plan.
</verification>

<success_criteria>
- Actual 05-19 and later contiguous plan/summary lifecycle fixtures pass exact validation.
- Controlled worktree/index dirt cannot coexist with PASS.
- Unrelated dirty owner work remains byte-for-byte untouched and outside the claim.
- Broad tracking filters are gone and unrelated planning mutations fail.
- Passed verification requires a real complete N/N must-have inventory and verified OUTR-04/OUTR-05 coverage.
- Immutable v3 residue lineage is preserved exactly and all newly published/terminal residue is canonical schema v4.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-22-SUMMARY.md` with the four modified source/test paths in `key-files`. Do not claim that the live review, verification, tracking completion, or authoritative terminal audit has occurred.
</output>
