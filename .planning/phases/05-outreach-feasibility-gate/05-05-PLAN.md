---
phase: 05-outreach-feasibility-gate
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - scripts/outreach-feasibility/rights-gate.mjs
  - scripts/outreach-feasibility/rights-gate.test.mjs
  - scripts/outreach-feasibility/dormant/spike-runner.test.mjs
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "Evidence researched or retrieved after the evaluation clock can never make any required operation current or authorize the dormant spike."
    - "A source retrieval outside the matrix's approved research-through-validity window can never reach `PASS`."
    - "A rights matrix whose validity horizon exceeds the approved seven-day interval can never reach `PASS`."
    - "Every rejected chronology/horizon mutation reaches zero secret, corpus, request-build, and provider-fetch callbacks."
  artifacts:
    - path: "scripts/outreach-feasibility/rights-gate.mjs"
      provides: "Strict ISO chronology and maximum-horizon validation before rights admission"
      exports:
        - MAX_RIGHTS_VALIDITY_DAYS
        - validateRightsMatrix
        - evaluateRights
    - path: "scripts/outreach-feasibility/rights-gate.test.mjs"
      provides: "Boundary/property coverage for future, out-of-window, and excessive-horizon evidence"
    - path: "scripts/outreach-feasibility/dormant/spike-runner.test.mjs"
      provides: "Injected-effect proof that invalid chronology remains zero-effect"
  key_links:
    - from: "scripts/outreach-feasibility/rights-gate.mjs"
      to: "scripts/outreach-feasibility/dormant/spike-runner.mjs"
      via: "`runConditionalSpike` consumes `evaluateRights`; chronology failure must return before every injected callback"
      pattern: "evaluateRights"
  prohibitions:
    - "Do not alter the current `PROHIBIT`/`AMBIGUOUS` rights classifications or make owner acknowledgement a permission source."
    - "Do not call a provider, read `TAVILY_API_KEY`, create a representative fixture, or fabricate a quality result."
    - "Do not add production UI, schema, Edge Function, database, cache, scheduler, or application-search behavior."
---

<objective>
Close the future-evidence authorization bypass while preserving the accepted rights-first no-go for OUTR-04 and OUTR-05.

Purpose: Current date parsing proves expiry only in one direction. Per D-09, D-10, and D-12, evidence that has not yet been obtained, was retrieved outside its approved window, or claims an excessive validity horizon must fail before any search-related effect.
Output: Strict chronology/horizon validation plus offline boundary and zero-effect regression coverage.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-01-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-02-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json
@scripts/outreach-feasibility/rights-gate.mjs
@scripts/outreach-feasibility/rights-gate.test.mjs
@scripts/outreach-feasibility/dormant/spike-runner.mjs
@scripts/outreach-feasibility/dormant/spike-runner.test.mjs

<interfaces>
Current rights interfaces:

- `validateRightsMatrix(matrix, { now? }): ValidatedRightsMatrix`
- `evaluateRights(matrix, { now? }): RightsVerdict`
- `sha256Json(value): string`
- `runConditionalSpike({ matrix, spikeAuthorization, now, readSecret, createCorpus, buildRequest, fetchImpl }): Promise&lt;SpikeResult&gt;`

Current matrix window:

- `researched_at: "2026-07-28"`
- `valid_until: "2026-08-04"`
- every committed `retrieved_at: "2026-07-28T00:00:00Z"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce current, in-window, bounded rights evidence</name>
  <read_first>
    - `scripts/outreach-feasibility/rights-gate.mjs`
    - `scripts/outreach-feasibility/rights-gate.test.mjs`
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md`
  </read_first>
  <files>scripts/outreach-feasibility/rights-gate.mjs, scripts/outreach-feasibility/rights-gate.test.mjs</files>
  <behavior>
    - Current committed matrix at a 2026-07-29 clock → still validates and evaluates to `RIGHTS_NO_GO`.
    - `researched_at` after `now` by one millisecond/day → validation failure and `RIGHTS_NO_GO`.
    - any `retrieved_at` after `now`, before the start of `researched_at`, or after the inclusive end of `valid_until` → validation failure and `RIGHTS_NO_GO`.
    - `valid_until` more than seven UTC calendar days after `researched_at` → validation failure and `RIGHTS_NO_GO`; the exact current 2026-07-28 through 2026-08-04 interval remains valid.
    - malformed or offset-bearing retrieval timestamps outside the one accepted canonical UTC ISO form → validation failure.
  </behavior>
  <action>
Write the chronology boundary tests first, re-signing every synthetic source row and aggregate matrix after mutation so each test isolates time validation rather than digest drift. Export `MAX_RIGHTS_VALIDITY_DAYS = 7`. Parse `researched_at` and `valid_until` as their existing canonical UTC dates; define the maximum horizon as `validUntilStart - researchedAtStart <= 7 * 24h`, while the operational validity end remains the final millisecond of `valid_until`.

Replace the permissive `Date.parse`-only retrieval check with one strict UTC ISO timestamp parser accepting `YYYY-MM-DDTHH:mm:ssZ` or the same form with exactly three fractional-second digits. Pass `researchedAt`, `validityEnd`, and `nowMs` into source validation. Require every `retrieved_at` to fall on or after the research start, on or before the inclusive validity end, and on or before `nowMs`. Require the research start itself to be on or before `nowMs`. Retain the existing stale check, exact schemas, digest checks, and current operation classifications.

Keep `evaluateRights` fail-closed: every thrown chronology/horizon error returns `RIGHTS_NO_GO`, `search_authorized: false`, `NOT_RUN_RIGHTS_NO_GO`, and all four zero counters. Per D-09/D-10, do not introduce a tolerance, owner override, or technical-capability exception.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/rights-gate.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The exact current matrix remains valid at the committed evaluation clock and remains a rights no-go.</criterion>
    <criterion>One-boundary mutation tests reject future research, future retrieval, retrieval before research, retrieval after validity, excessive horizon, and malformed retrieval timestamps after all digests are recomputed.</criterion>
    <criterion>Every rejected matrix returns all four effect counters as zero and never returns `PASS`.</criterion>
  </acceptance_criteria>
  <done>The rights evaluator cannot treat future, out-of-window, or excessively durable evidence as current permission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove invalid chronology cannot reach injected spike effects</name>
  <read_first>
    - `scripts/outreach-feasibility/dormant/spike-runner.test.mjs`
    - `scripts/outreach-feasibility/dormant/spike-runner.mjs`
    - `scripts/outreach-feasibility/rights-gate.mjs`
    - `scripts/outreach-feasibility/rights-gate.test.mjs`
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md`
  </read_first>
  <files>scripts/outreach-feasibility/dormant/spike-runner.test.mjs</files>
  <behavior>
    - Synthetic all-`ALLOW` evidence with future research or retrieval plus otherwise exact structured authorization → exact no-run result and zero injected effects.
    - Synthetic all-`ALLOW` evidence with retrieval outside the approved window or a horizon one day too long → exact no-run result and zero injected effects.
    - Every chronology mutation invokes none of `readSecret`, `createCorpus`, `buildRequest`, or `fetchImpl`.
  </behavior>
  <action>
Extend the dormant runner's offline tests with a table-driven held-out chronology matrix. Start from the synthetic all-`ALLOW` matrix, mutate one time property, recompute per-source and aggregate digests, and supply the matching structured `ACCEPT_RIGHTS` object so authorization shape cannot mask a chronology bug. Cover future `researched_at`, future `retrieved_at`, retrieval before the research start, retrieval after the validity end, and an eight-day research-to-valid-until interval.

For every case, call `runConditionalSpike` with spies for all four injected effects and assert the exact `RIGHTS_NO_GO`/`NOT_RUN_RIGHTS_NO_GO` result, four zero counters, and an empty ordered call list. Do not add a real provider origin, request body, environment lookup, corpus, or network-capable test.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/dormant/spike-runner.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All chronology/horizon denial cases carry internally consistent digests and exact structured authorization yet still reach no injected callback.</criterion>
    <criterion>The current committed matrix remains zero-effect and no live provider/test fixture is introduced.</criterion>
  </acceptance_criteria>
  <done>Future or out-of-window evidence is proven incapable of authorizing a provider effect, not merely incapable of producing a `PASS` label.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/outreach-feasibility/rights-gate.mjs`
  - New export: `MAX_RIGHTS_VALIDITY_DAYS`.
  - Hardened behavior: strict UTC retrieval parsing; research/retrieval not-after-now checks; retrieval-window checks; seven-day maximum horizon.
- `scripts/outreach-feasibility/rights-gate.test.mjs`
  - New boundary/property cases for future research, future retrieval, out-of-window retrieval, malformed UTC timestamps, and excessive validity.
- `scripts/outreach-feasibility/dormant/spike-runner.test.mjs`
  - New held-out injected-effect cases for every chronology/horizon bypass.
- `.planning/phases/05-outreach-feasibility-gate/05-05-SUMMARY.md` (executor output).
- No new package, class, database object, migration, UI route, API route, provider request, fixture, or production symbol.

## Schema Push Gate

No schema push is required. This plan contains no recognized schema path and changes only local evidence code/tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence timestamps → rights evaluator | Untrusted dates could make not-yet-obtained or overlong evidence appear current. |
| Rights verdict → dormant effects | A chronology bypass could otherwise reach secret, corpus, request, or provider callbacks. |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05-01 | Tampering / Elevation | `validateRightsMatrix` | high | mitigate | Strict UTC parsing, not-after-now checks, approved-window checks, seven-day horizon, re-signed negative fixtures, and fail-closed evaluation block fabricated currentness. |
| T-05-05-02 | Elevation | `runConditionalSpike` | high | mitigate | Matching structured authorization is supplied in negative tests, while all four injected callbacks remain uncalled for every invalid time property. |
| T-05-05-03 | Information Disclosure | Provider boundary | high | mitigate | Tests remove the provider key, use only injected spies, and assert zero secret/corpus/request/fetch effects. |
| T-05-05-04 | Tampering | Current no-go evidence | medium | mitigate | Current matrix classifications and canonical digests remain unchanged; this plan narrows only evidence currentness. |
| T-05-05-SC | Tampering | Dependency supply chain | low | accept | No package-manager operation occurs; only installed Node built-ins are used. |
</threat_model>

<verification>
Run both focused test files with `TAVILY_API_KEY` removed. Confirm the committed matrix remains `RIGHTS_NO_GO` and every future/window/horizon mutation reaches zero injected effects.
</verification>

<success_criteria>
- Future-dated `researched_at` or `retrieved_at` cannot reach `PASS`.
- Retrieval outside the approved evidence window and validity beyond seven days cannot reach `PASS`.
- Invalid time evidence cannot reach any dormant injected effect.
- Production outreach remains disabled and quality remains `NOT_RUN_RIGHTS_NO_GO`.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-05-SUMMARY.md` when done.
</output>
