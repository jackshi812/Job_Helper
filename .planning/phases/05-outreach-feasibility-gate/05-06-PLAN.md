---
phase: 05-outreach-feasibility-gate
plan: 06
type: execute
wave: 6
depends_on:
  - 05-05
files_modified:
  - scripts/outreach-feasibility/evidence-integrity.mjs
  - scripts/outreach-feasibility/evidence-integrity.test.mjs
  - scripts/outreach-feasibility/owner-checkpoint.mjs
  - scripts/outreach-feasibility/owner-checkpoint.test.mjs
  - .planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json
  - .planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "The Phase 5 history anchor is the immutable commit `e1d592e8b574ae3e474ce44661b3970954ef00d9` plus the exact `05-01-PLAN.md` blob hash, never a newly discovered latest plan edit."
    - "Every allowed Phase 5 JSON artifact has an exact version/state schema and self-hash contract usable on worktree, index, and historical blobs."
    - "A one-time blocking-human request binds the current rights, quality, immutable checkpointed-v1 decision-contract, and baseline digests before the owner responds."
    - "No code can create an accepted owner receipt without exact raw stdin bytes matching the one-time request; trim, case, punctuation, Unicode, or line-ending changes are rejected."
    - "The checkpointed-v1 decision digest remains independently recomputable from an accepted v2 decision after the legacy v1 file is overwritten."
  artifacts:
    - path: "scripts/outreach-feasibility/evidence-integrity.mjs"
      provides: "Dependency-neutral exact artifact/baseline/zero-residue integrity validation"
      exports:
        - assertExecutionBaseline
        - assertArtifactSchema
        - assertZeroResidueRecord
        - checkpointedDecisionPayloadFromAccepted
        - resolveCheckpointedDecisionContractSha256
    - path: ".planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json"
      provides: "Immutable pre-execution Phase 5 Git and plan-blob anchor"
    - path: "scripts/outreach-feasibility/owner-checkpoint.mjs"
      provides: "One-time request, byte-exact stdin receipt writer, and independent receipt validator"
      exports:
        - exactOwnerCheckpointResponse
        - assertOwnerCheckpointRequest
        - assertOwnerCheckpointRecord
    - path: ".planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json"
      provides: "Nonce- and evidence-bound pending request for Plan 05-07"
  key_links:
    - from: ".planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json"
      to: "scripts/outreach-feasibility/evidence-integrity.mjs"
      via: "Git commit and exact plan blob are recomputed and compared before scanning"
      pattern: "assertExecutionBaseline"
    - from: ".planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json"
      to: ".planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json"
      via: "Plan 05-07 feeds the owner's raw response bytes to the single-purpose recorder"
      pattern: "assertOwnerCheckpointRecord"
  prohibitions:
    - "Do not use `git log -1`, dates, commit messages, or a mutable plan revision to rediscover the Phase 5 baseline."
    - "Do not create `05-OWNER-CHECKPOINT.json`, copy the expected response, or pre-populate owner input in this autonomous plan."
    - "Do not trim, normalize, case-fold, paraphrase, synthesize, or shell-interpolate owner response bytes."
    - "Do not authorize search, run the representative spike, call a provider, or change production/schema files."
---

<objective>
Create the immutable baseline and independent owner-checkpoint evidence contracts required to repair the accepted no-go trust chain.

Purpose: The current baseline can move when a plan is edited, and `owner_attestation_source` is only a self-declared string. OUTR-04/OUTR-05 need a real blocking-human boundary and exact evidence integrity before accepted status can be trusted.
Output: A pinned baseline artifact, dependency-neutral exact artifact validators, and a one-time pending owner-checkpoint request/recorder that does not create the owner response.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md
@.planning/phases/05-outreach-feasibility-gate/05-03-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-04-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json
@.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json
@.planning/phases/05-outreach-feasibility-gate/05-DECISION.json
@.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json
@scripts/outreach-feasibility/rights-gate.mjs
@scripts/outreach-feasibility/decision-evidence.mjs
@scripts/outreach-feasibility/residue-check.mjs

<interfaces>
Pinned Git facts verified during planning:

- Phase 5 pre-execution plan revision: `e1d592e8b574ae3e474ce44661b3970954ef00d9`
- SHA-256 of `.planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md` at that revision: `2a7b1050772e674a3f880dbf5d2b8a96ae5dd48fb8e183a05ba8272a7ff6aba0`

Current evidence identifiers:

- rights digest: `05-RIGHTS-MATRIX.json.rights_evidence_sha256`
- quality digest: `05-QUALITY-REPORT.json.quality_evidence_sha256`
- checkpointed v1 no-go decision digest: current `05-DECISION.json.decision_contract_sha256`, persisted as `checkpointed_decision_contract_sha256` in the request, receipt, and accepted v2 decision
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the execution baseline and exact artifact integrity schemas</name>
  <read_first>
    - `scripts/outreach-feasibility/evidence-integrity.mjs` (create if absent; this task owns it)
    - `scripts/outreach-feasibility/evidence-integrity.test.mjs` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-01-PLAN.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json`
    - `scripts/outreach-feasibility/rights-gate.mjs`
    - `scripts/outreach-feasibility/decision-evidence.mjs`
    - `scripts/outreach-feasibility/residue-check.mjs`
    - `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md`
  </read_first>
  <files>scripts/outreach-feasibility/evidence-integrity.mjs, scripts/outreach-feasibility/evidence-integrity.test.mjs, .planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json</files>
  <behavior>
    - Exact pinned commit plus exact plan blob → baseline `PASS`.
    - changed base SHA, plan path, plan blob, unknown field, self-hash, non-ancestor commit, or missing Git object → rejection.
    - Historical/current rights, quality, decision v1/v2, zero-residue v1/v2, baseline, checkpoint request/receipt, and contract-reconciliation artifacts → only their finite exact schemas/states pass.
    - zero-residue v2 with any omitted/extra surface, counter, digest, baseline, owner-checkpoint, snapshot, tail-policy, or self-hash field → rejection.
    - Accepted decision v2 → its exact unchanged v1 no-go projection recomputes the immutable checkpointed digest; any projection-field, lineage-field, request, or receipt drift is rejected.
  </behavior>
  <action>
Write failing tests first, then create a dependency-neutral ESM module using Node built-ins and `sha256Json` only. Export `assertExecutionBaseline`, `assertArtifactSchema`, and `assertZeroResidueRecord`. Keep it free of filesystem traversal, owner-response recording, decision admission, and provider behavior so `decision-evidence.mjs` and `residue-check.mjs` can both import it without a circular dependency.

Create `05-EXECUTION-BASELINE.json` with an exact finite schema containing version/phase/status, `base_sha: e1d592e8b574ae3e474ce44661b3970954ef00d9`, the exact plan path, `plan_blob_sha256: 2a7b1050772e674a3f880dbf5d2b8a96ae5dd48fb8e183a05ba8272a7ff6aba0`, and a canonical `baseline_evidence_sha256` over every preceding field. `assertExecutionBaseline` must use `execFile` without a shell to prove the commit exists, is an ancestor of the supplied source/live head, and contains the exact plan blob bytes/hash. It must never run `git log` or select a later plan edit.

Implement explicit schema-version/state allowlists for every allowed Phase 5 JSON path. Historical v1 decision/zero-residue records remain readable only under their exact old keys and self-hashes; they are not sufficient for final accepted validation. Define a v2 zero-residue schema that requires: pinned baseline fields/digest; `source_snapshot` head/tree/range digests; exact worktree/index/history inventories and digests; an explicit administrative-tail policy; seven zero counters; rights/quality/decision-contract/owner-checkpoint digests; checked time; and canonical self-hash. Reject unknown nested keys. Keep sensitive-pattern checks separate as defense in depth for the scanner in Plan 05-08.

Export `checkpointedDecisionPayloadFromAccepted({ decision, matrix, qualityReport })`. It must reconstruct the exact v1 stable payload with `schema_version: 1` and only these keys: `schema_version`, `phase`, `rights_status`, `quality_status`, `search_authorized`, `production_outreach_enabled`, `outreach_milestone_status`, `phase_6_authorized`, `phase_7_authorized`, `provider_call_count`, `fixture_count`, `raw_result_count`, `production_mutation_count`, `rights_evidence_sha256`, `quality_evidence_sha256`, `redesign_handoff_options`, and `redesign_selection`. It must compare every projected value to the current matrix/no-run quality contract before hashing. Export `resolveCheckpointedDecisionContractSha256({ decision, matrix, qualityReport })`: for exact v1 it returns the validated `decision_contract_sha256`; for accepted v2 it hashes that projection, requires equality with `decision.checkpointed_decision_contract_sha256`, and returns the recomputed digest. This dependency-neutral helper is the only v1/v2 lineage resolver used by owner and decision validators after finalization.
  </action>
  <verify>
    <automated>node --test --test-concurrency=1 scripts/outreach-feasibility/evidence-integrity.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The baseline validator proves the exact pinned commit and exact plan blob, and rejects a later edit even when it is the newest commit touching the plan.</criterion>
    <criterion>`assertArtifactSchema` has a finite path/state registry for every current or planned Phase 5 JSON artifact and rejects realistic unrecognized payload fields.</criterion>
    <criterion>`assertZeroResidueRecord` rejects incomplete seven-field stubs, omitted/extra counters or surfaces, nonzero counters, digest drift, and self-hash drift.</criterion>
  </acceptance_criteria>
  <done>Phase 5 has an immutable history anchor and dependency-neutral, exact evidence schemas suitable for both decision admission and live residue scanning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the one-time owner checkpoint request and raw-byte recorder</name>
  <read_first>
    - `scripts/outreach-feasibility/owner-checkpoint.mjs` (create if absent; this task owns it)
    - `scripts/outreach-feasibility/owner-checkpoint.test.mjs` (create if absent; this task owns it)
    - `.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json` (create if absent; this task owns it)
    - `scripts/outreach-feasibility/evidence-integrity.mjs`
    - `.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-03-SUMMARY.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md`
  </read_first>
  <files>scripts/outreach-feasibility/owner-checkpoint.mjs, scripts/outreach-feasibility/owner-checkpoint.test.mjs, .planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json</files>
  <behavior>
    - One cryptographically random 32-byte nonce plus exact current evidence/baseline digests → one pending request with a valid self-hash.
    - `--print-required-response` derives but never persists the expected owner response.
    - `--record-response` reads raw stdin until EOF and creates the receipt only when those exact bytes match; any added/removed byte, newline, case, punctuation, Unicode normalization, or stale nonce/digest fails without an output file.
    - A receipt preserves the exact response as base64 plus its SHA-256 and binds request/evidence/checkpoint metadata plus the immutable checkpointed-v1 decision digest; it cannot be overwritten.
    - `--assert-record` validates against either the pre-finalization v1 decision or the post-finalization accepted v2 decision by recomputing the same checkpointed-v1 digest; it never needs a separate legacy decision file.
  </behavior>
  <action>
Write tests first, then export `exactOwnerCheckpointResponse`, `assertOwnerCheckpointRequest`, and `assertOwnerCheckpointRecord`. The request exact schema is `schema_version`, `phase`, `status`, `checkpoint_plan`, `checkpoint_task`, `gate`, `nonce`, `rights_evidence_sha256`, `quality_evidence_sha256`, `checkpointed_decision_contract_sha256`, `baseline_evidence_sha256`, `required_response_sha256`, and `owner_checkpoint_request_sha256`. Require `checkpoint_plan: 05-07`, the exact checkpoint task name, `gate: blocking-human`, status `AWAITING_OWNER_RESPONSE`, a `randomBytes(32)` lowercase-hex nonce, the current rights/quality/baseline digests, and the checkpointed digest resolved from the exact current v1 decision. The request self-hash covers every preceding field.

The exact response must reconfirm only the already accepted rights-first no-go, `NOT_RUN_RIGHTS_NO_GO`, production-disabled state, and one-time nonce/evidence digests. It must not authorize search, claim the representative spike ran, select a redesign, or claim provider-side deletion.

Define `assertOwnerCheckpointRequest({ request, baseline, matrix, qualityReport, checkpointedDecisionContractSha256 })` and `assertOwnerCheckpointRecord({ request, record, baseline, matrix, qualityReport, checkpointedDecisionContractSha256 })`; neither accepts a legacy decision object. The receipt exact schema is `schema_version`, `phase`, `status`, `checkpoint_plan`, `checkpoint_task`, `gate`, `owner_checkpoint_request_sha256`, `nonce`, `rights_evidence_sha256`, `quality_evidence_sha256`, `checkpointed_decision_contract_sha256`, `baseline_evidence_sha256`, `owner_response_utf8_base64`, `owner_response_sha256`, `received_at`, and `owner_checkpoint_evidence_sha256`. Require status `OWNER_RESPONSE_RECORDED`, exact equality with request/evidence/checkpoint values, and a self-hash over every preceding field.

Add import-safe CLI commands `--create-request`, `--assert-request`, `--print-required-response`, `--record-response`, and `--assert-record`, with exact path flags `--request`, `--record`, `--baseline`, `--matrix`, `--quality-report`, and `--decision`. Each command resolves the checkpointed digest through `resolveCheckpointedDecisionContractSha256({ decision, matrix, qualityReport })`; therefore the same `--assert-record` invocation works after `05-DECISION.json` is replaced by v2. `--record-response` must consume raw stdin bytes directly, compare them byte-for-byte without trim/normalization/line-ending conversion, and use exclusive file creation so an existing receipt cannot be replaced. Never accept a source-label string as proof. Do not provide a CLI option that accepts the response as a command-line argument.

Create only the pending request in this plan through `--create-request`. Do not create the receipt or send derived bytes to the recorder; Plan 05-07 owns the blocking-human response boundary.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node --test --test-concurrency=1 scripts/outreach-feasibility/owner-checkpoint.test.mjs &amp;&amp; env -u TAVILY_API_KEY node scripts/outreach-feasibility/owner-checkpoint.mjs --assert-request --request .planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json --baseline .planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json --matrix .planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json --quality-report .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json --decision .planning/phases/05-outreach-feasibility-gate/05-DECISION.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The committed request is pending, one-time, nonce-bound, and cross-bound to current rights, quality, immutable checkpointed-v1 decision, and baseline evidence.</criterion>
    <criterion>Tests prove the recorder rejects every byte-level drift and creates no file on failure.</criterion>
    <criterion>Tests replace the v1 decision with an accepted v2 fixture and prove `--assert-record` still recomputes and validates the request/receipt lineage without any legacy file.</criterion>
    <criterion>No `05-OWNER-CHECKPOINT.json` exists at plan completion and no agent-generated response is fed to the recorder.</criterion>
  </acceptance_criteria>
  <done>A fresh blocking-human checkpoint can independently preserve exact owner bytes without relying on `05-03-SUMMARY.md` or a self-declared source marker.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/outreach-feasibility/evidence-integrity.mjs`
  - New functions/interfaces: `assertExecutionBaseline({ record, repoRoot, sourceHeadSha? })`, `assertArtifactSchema(relativePath, parsedArtifact)`, `assertZeroResidueRecord(record, expectedDigests)`, `checkpointedDecisionPayloadFromAccepted({ decision, matrix, qualityReport })`, and `resolveCheckpointedDecisionContractSha256({ decision, matrix, qualityReport })`.
  - New finite schemas: rights, quality, decision v1/v2, zero-residue v1/v2, baseline, checkpoint request/receipt, and contract reconciliation.
  - Exact decision-v2 fields: `schema_version`, `phase`, `rights_status`, `quality_status`, `search_authorized`, `production_outreach_enabled`, `outreach_milestone_status`, `phase_6_authorized`, `phase_7_authorized`, `provider_call_count`, `fixture_count`, `raw_result_count`, `production_mutation_count`, `rights_evidence_sha256`, `quality_evidence_sha256`, `redesign_handoff_options`, `redesign_selection`, `checkpointed_decision_contract_sha256`, `owner_checkpoint_evidence_sha256`, `status`, `decision_contract_sha256`, `required_owner_attestation`, `owner_attestation`, `zero_residue_sha256`.
  - Exact zero-residue-v2 top-level fields: `schema_version`, `phase`, `status`, `scope`, `provider_side_retention`, `scanned_roots`, `baseline`, `source_snapshot`, `git_surfaces`, `administrative_tail_policy`, `provider_call_count`, `fixture_count`, `raw_result_count`, `production_mutation_count`, `forbidden_hit_count`, `unexpected_survivor_count`, `symlink_count`, `rights_evidence_sha256`, `quality_evidence_sha256`, `decision_contract_sha256`, `owner_checkpoint_evidence_sha256`, `baseline_evidence_sha256`, `checked_at`, `zero_residue_sha256`.
  - Exact zero-residue-v2 nested fields: `baseline` = `base_sha`, `plan_path`, `plan_blob_sha256`, `baseline_evidence_sha256`; `source_snapshot` = `head_sha`, `controlled_tree_sha256`, `baseline_to_source_history_sha256`; `git_surfaces.worktree` = `status_entry_count`, `path_count`, `blob_count`, `inventory_sha256`; `git_surfaces.index` = `staged_path_count`, `path_count`, `blob_count`, `inventory_sha256`; `git_surfaces.phase_commit_range` = `base_sha`, `head_sha`, `commit_count`, `path_count`, `blob_count`, `inventory_sha256`; `git_surfaces.source_head_tree` = `head_sha`, `path_count`, `blob_count`, `tree_sha256`; `administrative_tail_policy` = `from_source_head_sha`, `allowed_paths`, `allowed_state_transitions`, `source_changes_allowed`.
  - Exact contract-reconciliation fields: `schema_version`, `phase`, `requirement_id`, `status`, `original_representative_spike_intent`, `rights_prerequisite`, `quality_status`, `spike_executed`, `representative_case_count`, `provider_call_count`, `fixture_count`, `raw_result_count`, `production_mutation_count`, `quality_claim`, `d09_resolution`, `d10_resolution`, `d12_resolution`, `d13_resolution`, `rights_evidence_sha256`, `quality_evidence_sha256`, `checkpointed_decision_contract_sha256`, `decision_contract_sha256`, `owner_checkpoint_evidence_sha256`, `zero_residue_sha256`, `roadmap_semantic_sha256`, `requirements_semantic_sha256`, `contract_reconciliation_sha256`. The semantic digests cover only the stable Phase 5 goal/success-criteria and OUTR-04/OUTR-05 contract projections; mutable plan checkmarks, progress, timestamps, and lifecycle bookkeeping are excluded. Raw live-document fingerprints are never stored in this artifact.
- `scripts/outreach-feasibility/evidence-integrity.test.mjs`.
- `.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json`
  - Exact fields: `schema_version`, `phase`, `status`, `base_sha`, `plan_path`, `plan_blob_sha256`, `baseline_evidence_sha256`.
- `scripts/outreach-feasibility/owner-checkpoint.mjs`
  - New functions/interfaces: `exactOwnerCheckpointResponse(request)`, `assertOwnerCheckpointRequest({ request, baseline, matrix, qualityReport, checkpointedDecisionContractSha256 })`, and `assertOwnerCheckpointRecord({ request, record, baseline, matrix, qualityReport, checkpointedDecisionContractSha256 })`.
  - Exact CLI flags: `--create-request`, `--assert-request`, `--print-required-response`, `--record-response`, `--assert-record`, `--request`, `--record`, `--baseline`, `--matrix`, `--quality-report`, `--decision`.
- `scripts/outreach-feasibility/owner-checkpoint.test.mjs`.
- `.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json`
  - Exact fields: `schema_version`, `phase`, `status`, `checkpoint_plan`, `checkpoint_task`, `gate`, `nonce`, `rights_evidence_sha256`, `quality_evidence_sha256`, `checkpointed_decision_contract_sha256`, `baseline_evidence_sha256`, `required_response_sha256`, `owner_checkpoint_request_sha256`.
- `.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json` (schema defined here; created only by Plan 05-07)
  - Exact fields: `schema_version`, `phase`, `status`, `checkpoint_plan`, `checkpoint_task`, `gate`, `owner_checkpoint_request_sha256`, `nonce`, `rights_evidence_sha256`, `quality_evidence_sha256`, `checkpointed_decision_contract_sha256`, `baseline_evidence_sha256`, `owner_response_utf8_base64`, `owner_response_sha256`, `received_at`, `owner_checkpoint_evidence_sha256`.
- `.planning/phases/05-outreach-feasibility-gate/05-06-SUMMARY.md` (executor output).
- No checkpoint receipt, provider call, fixture, package, migration, database object, production route, or schema object is created.

## Schema Push Gate

No schema push is required. This plan adds local Node evidence modules/tests and planning JSON only; no recognized schema path is present.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git history → pinned baseline | A movable plan commit could exclude an earlier leak unless commit and blob are immutable inputs. |
| JSON blobs → evidence validators | Historical/current artifacts are untrusted until exact schema and self-hash checks pass. |
| Blocking-human response → receipt recorder | Only raw owner-supplied stdin bytes may create independently preserved checkpoint evidence. |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | Tampering | Execution baseline | high | mitigate | Exact commit, ancestor proof, exact plan path/blob SHA-256, and canonical baseline self-hash replace movable `git log -1` discovery. |
| T-05-06-02 | Tampering / Elevation | Evidence schemas | high | mitigate | Dependency-neutral finite schemas reject omitted, extra, transitional, nonzero, or self-hash-drifted records before decision or residue admission. |
| T-05-06-03 | Spoofing | Owner checkpoint | high | mitigate | Random one-time nonce, blocking-human gate metadata, raw stdin comparison, exclusive receipt creation, response SHA-256/base64, and immutable checkpointed-v1 lineage binding establish an independent boundary that survives accepted-v2 replacement. |
| T-05-06-04 | Repudiation | Owner receipt | high | mitigate | Exact raw response bytes, request digest, checkpoint plan/task/gate, receive time, and receipt self-hash make the preserved event auditable. |
| T-05-06-05 | Information Disclosure | Recorder/CLI | medium | mitigate | The response contains no candidate/provider data, command arguments never carry owner bytes, and errors report only bounded field names/status. |
| T-05-06-SC | Tampering | Dependency supply chain | low | accept | No package installation occurs; Node built-ins provide crypto, stdin, filesystem, tests, and Git subprocess execution. |
</threat_model>

<verification>
Run both focused tests, assert the pinned baseline against Git, and assert the pending request against every current evidence digest. The tests must also validate a receipt using an accepted-v2-only fixture after the legacy decision is removed. Confirm the receipt file is absent in the real plan worktree.
</verification>

<success_criteria>
- The baseline is permanently pinned to the verified pre-execution plan commit and blob.
- Exact evidence schemas are reusable without a circular dependency.
- A one-time owner request is ready and binds all current no-go evidence.
- The request/receipt checkpointed-v1 digest can be recomputed from accepted v2 without a legacy decision file.
- No owner response or receipt is manufactured before the blocking checkpoint.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-06-SUMMARY.md` when done.
</output>
