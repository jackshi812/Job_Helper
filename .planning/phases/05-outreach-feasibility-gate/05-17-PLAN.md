---
phase: 05-outreach-feasibility-gate
plan: 17
type: execute
wave: 15
depends_on:
  - 05-16
files_modified:
  - scripts/outreach-feasibility/evidence-integrity.mjs
  - scripts/outreach-feasibility/evidence-integrity.test.mjs
  - scripts/outreach-feasibility/decision-evidence.mjs
  - scripts/outreach-feasibility/decision-evidence.test.mjs
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "Accepted decision schema v3 requires fresh cryptographic verification and binds the raw request/signature digests, principal, namespace, key fingerprint, nonce, and signed stopped-decision payload."
    - "The old receipt digest remains lineage labeled integrity-only and cannot satisfy or replace any fresh authorization field."
    - "Finalization verifies fresh authorization before Git scanning, candidate construction, staging, rename, or publication."
    - "Authentication failure, stale/revoked/missing trust, or artifact mismatch preserves the prior decision/residue pair byte-for-byte."
  artifacts:
    - path: scripts/outreach-feasibility/evidence-integrity.mjs
      provides: "Accepted-v3 authenticated decision and residue schemas"
    - path: scripts/outreach-feasibility/decision-evidence.mjs
      provides: "Pre-effect authenticated finalizer"
    - path: scripts/outreach-feasibility/decision-evidence.test.mjs
      provides: "No-scan/no-write and authenticated publication regressions"
  key_links:
    - from: scripts/outreach-feasibility/decision-evidence.mjs
      to: scripts/outreach-feasibility/owner-authorization.mjs
      via: "fresh SSHSIG verification before all effects"
      pattern: "verifyOwnerAuthorization"
    - from: scripts/outreach-feasibility/evidence-integrity.mjs
      to: .planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig
      via: "raw request/signature SHA-256 and exact authenticated metadata"
      pattern: "owner_authorization_signature_sha256"
---

<objective>
Make fresh owner authentication mandatory in accepted schemas and the finalizer, then bind it into the stopped decision/residue pair.

Purpose: Turn the verified signature into non-bypassable accepted-evidence authority without granting any provider, spike, redesign, or production capability.
Output: Authenticated schema-v3 contracts and a rollback-safe, pre-effect finalizer.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json
@.planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json
@.planning/phases/05-outreach-feasibility-gate/05-OWNER-AUTHORIZATION-REQUEST.json.sig
@scripts/outreach-feasibility/trust/owner-trust-anchor.json
@scripts/outreach-feasibility/owner-authorization.mjs
@scripts/outreach-feasibility/evidence-integrity.mjs
@scripts/outreach-feasibility/decision-evidence.mjs
</context>

<prohibitions>
- Do not add a preverified boolean, caller verifier, trust-on-first-use path, alternate key/principal/namespace, environment bypass, or structural-only accepted branch.
- Do not call the old owner-checkpoint validator authentication; retain its digest solely as historical integrity lineage.
- Do not authorize search, a quality run, redesign, Phase 6/7, production outreach, provider/network access, secret access, fixture creation, or mutation.
- Do not read the private key or invoke signing.
- Do not regenerate live evidence in this plan and do not invoke the authoritative live terminal audit.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require fresh authorization in accepted schema v3</name>
  <read_first>
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - scripts/outreach-feasibility/evidence-integrity.test.mjs
    - scripts/outreach-feasibility/owner-authorization.mjs
    - scripts/outreach-feasibility/owner-checkpoint.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-DECISION.json
    - .planning/phases/05-outreach-feasibility-gate/05-ZERO-RESIDUE.json
  </read_first>
  <files>scripts/outreach-feasibility/evidence-integrity.mjs, scripts/outreach-feasibility/evidence-integrity.test.mjs</files>
  <behavior>
    - "Structural validators can inspect historical schema versions 1 and 2 without calling them authenticated."
    - "Accepted v3 requires cryptographically verified raw request/signature bytes and exact trust metadata."
    - "V3 binds request SHA-256, signature SHA-256, principal, namespace, key fingerprint, nonce SHA-256, issued_at, verified_at, signed stopped-payload SHA-256, and legacy integrity-receipt SHA-256."
    - "Any missing/mismatched/rehashed field fails at the authentication boundary."
  </behavior>
  <action>
    Add exact schema-version-3 decision and zero-residue fields for the fresh authorization. Require `verifyOwnerAuthorization` to consume the raw request/signature plus pinned trust artifacts before `requireAccepted` can pass. Bind the verified principal `jackshi812`, namespace `job-copilot-phase-05-owner-v1`, exact key fingerprint, raw request/signature digests, nonce digest, issued/verified instants, and stopped-decision-payload digest. Retain `owner_checkpoint_evidence_sha256` under an explicitly integrity-only lineage contract; it cannot substitute for any authenticated field. Keep schema versions 1 and 2 structural and non-authoritative. Test deletion, independent mutation/re-hash, swapped signature, old receipt substitution, stale request, revoked anchor, reviewable trust-anchor path drift, and exact authenticated schema-version-3 success using temporary test-owned signing material.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/evidence-integrity.test.mjs scripts/outreach-feasibility/owner-authorization.test.mjs scripts/outreach-feasibility/owner-checkpoint.test.mjs</automated>
  </verify>
  <done>Only a freshly verified SSHSIG can satisfy accepted schema v3; old evidence remains structural/integrity lineage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate authenticated finalization before every effect</name>
  <read_first>
    - scripts/outreach-feasibility/decision-evidence.mjs
    - scripts/outreach-feasibility/decision-evidence.test.mjs
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - scripts/outreach-feasibility/owner-authorization.mjs
    - scripts/outreach-feasibility/residue-check.mjs
  </read_first>
  <files>scripts/outreach-feasibility/decision-evidence.mjs, scripts/outreach-feasibility/decision-evidence.test.mjs</files>
  <behavior>
    - "finalizeAcceptedEvidence verifies the fresh signature before gitHead, scanOwnedSurfaces, record construction, staging, or rename."
    - "Success constructs a RIGHTS_NO_GO / NOT_RUN_RIGHTS_NO_GO v3 decision/residue pair with five zero counters and fresh authorization digests."
    - "Every authentication or publication fault restores the exact prior pair."
    - "CLI accepted validation requires all fresh request/signature/trust paths and rejects extra, missing, duplicate, or private-key arguments."
  </behavior>
  <action>
    Extend the finalizer and `--assert-decision --require-accepted` with exact public flags `--owner-authorization-request`, `--owner-authorization-signature`, `--owner-trust-anchor`, `--owner-public-key`, and `--owner-allowed-signers`; the canonical trust-anchor argument is `scripts/outreach-feasibility/trust/owner-trust-anchor.json`. Invoke fresh verification before any Git/filesystem effect. Build only the existing stopped no-go candidate and copy verified metadata/digests into decision/residue v3; keep the old checkpoint request/receipt inputs solely for lineage checks. Reuse Plan 05-12's recoverable pair publication and extend its fault matrix so authentication failure occurs before transaction setup while all later publication failures restore exact prior bytes/existence/modes. Reject a signature file path equal to any private-key path, all unknown flags, planning-directory trust-anchor substitutes, and all non-public proof substitutes.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/decision-evidence.test.mjs scripts/outreach-feasibility/evidence-integrity.test.mjs</automated>
  </verify>
  <done>The finalizer can publish only an authenticated stopped v3 pair and proves exact no-effect failure plus rollback safety.</done>
</task>

</tasks>

<artifacts>
- `evidence-integrity.mjs` and test — authenticated v3 schema boundary.
- `decision-evidence.mjs` and test — pre-effect verification and recoverable accepted pair publication.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verified SSHSIG → accepted schema | Cryptographic proof must be bound field-by-field, never reduced to a boolean. |
| accepted request → filesystem finalizer | Authentication must pass before all Git scans, builds, stages, and writes. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-17-01 | Spoofing | Accepted v3 schema | critical | mitigate | Reverify raw proof and bind exact principal/namespace/key/request/signature/nonce metadata. |
| T-05-17-02 | Elevation of Privilege | Finalizer | critical | mitigate | Authenticate before every effect and admit only the stopped no-go payload. |
| T-05-17-03 | Tampering | Published evidence pair | high | mitigate | Reuse verified rollback transaction and fault-test exact prior restoration. |
| T-05-17-04 | Repudiation | Legacy receipt lineage | high | mitigate | Label legacy digest integrity-only and require independent fresh authorization fields. |
| T-05-17-SC | Tampering | Dependency supply chain | low | accept | No package installation; Node built-ins and system OpenSSH only. |
</threat_model>

<verification>
Run both task commands and the full owner authorization suite offline. Do not execute live finalization or terminal audit in this plan.
</verification>

<success_criteria>
- Accepted v3 cannot exist without fresh raw SSHSIG verification.
- The finalizer verifies before all effects and publishes only the stopped zero-effect branch.
- Historical receipt evidence remains non-authoritative.
- Faults preserve the prior pair exactly.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-17-SUMMARY.md` when done. Its frontmatter `key-files.created` / `key-files.modified` must include every source and test path changed by this plan so the mandatory execute-phase post-hook review receives complete SUMMARY-derived scope.
</output>
