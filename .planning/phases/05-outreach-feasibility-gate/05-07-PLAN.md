---
phase: 05-outreach-feasibility-gate
plan: 07
type: execute
wave: 7
depends_on:
  - 05-06
files_modified:
  - .planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json
autonomous: false
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "The owner freshly reviews the exact rights-first terminal no-go, no-run quality state, production-disabled posture, and immutable baseline-bound checkpoint request."
    - "Only the owner's exact raw response bytes to this blocking-human checkpoint create `05-OWNER-CHECKPOINT.json`."
    - "The receipt is independently validated from request/evidence digests, the immutable checkpointed-v1 decision digest, and exact response bytes; it does not rely on `05-03-SUMMARY.md` or `owner_attestation_source`."
    - "No mismatch, generic approval, `GO`, whitespace/case/punctuation change, or request to run the spike resumes execution."
  artifacts:
    - path: ".planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json"
      provides: "One-time byte-exact owner checkpoint receipt created only after the blocking-human response"
    - path: ".planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json"
      provides: "Nonce- and evidence-bound request presented to the owner"
  key_links:
    - from: "blocking-human owner response"
      to: ".planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json"
      via: "`owner-checkpoint.mjs --record-response` consumes the runtime response as raw stdin and writes exclusively on an exact byte match"
      pattern: "owner_checkpoint_evidence_sha256"
  prohibitions:
    - "The agent must not type, paste, synthesize, trim, normalize, paraphrase, or substitute the required response for the owner."
    - "Do not pass owner response bytes as a shell argument, environment variable, command substitution, `echo`, or JSON string."
    - "Do not modify the accepted decision, zero-residue record, roadmap, requirements, production code, or schema in this plan."
    - "Do not run a provider call or representative quality spike."
---

<objective>
Obtain and independently preserve a fresh owner checkpoint for the already accepted Phase 5 rights-first terminal no-go.

Purpose: The prior summary proves string equality only through an agent-authored report. This plan creates the real trust boundary required by OUTR-04/OUTR-05: a one-time blocking-human response recorded byte-for-byte by a narrow stdin-only tool.
Output: `05-OWNER-CHECKPOINT.json`, or no artifact and a still-blocked plan if the owner does not provide the exact response.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-06-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json
@.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json
@.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json
@.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json
@.planning/phases/05-outreach-feasibility-gate/05-DECISION.json
@scripts/outreach-feasibility/evidence-integrity.mjs
@scripts/outreach-feasibility/owner-checkpoint.mjs
@scripts/outreach-feasibility/owner-checkpoint.test.mjs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Preserve the owner's one-time raw-byte no-go reconfirmation</name>
  <read_first>
    - `.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-DECISION.json`
    - `.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md`
    - `.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md`
    - `scripts/outreach-feasibility/owner-checkpoint.mjs`
    - `scripts/outreach-feasibility/evidence-integrity.mjs`
  </read_first>
  <files>.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json</files>
  <action>
Before prompting, run the request/baseline/current-rights assertions with `TAVILY_API_KEY` removed. Present the owner with the exact evidence-bound state: `RIGHTS_NO_GO`; `NOT_RUN_RIGHTS_NO_GO`; zero provider calls, fixtures, raw results, and production mutations; production outreach disabled; no representative quality claim; provider-side retention `NOT_ASSERTED`; and the structurally stopped Phase-5-only milestone. Explain that this checkpoint only repairs provenance for the already accepted terminal no-go and cannot authorize search or select a redesign.

Run `owner-checkpoint.mjs --print-required-response` against the committed request and evidence files. Display its stdout once. Do not type, paste, save, or send that text as the owner. Wait for a fresh user response.

Only after a runtime-delivered owner response, start `owner-checkpoint.mjs --record-response` in an interactive stdin session with the exact request/evidence/output paths. Send exactly the bytes inside the runtime-provided owner response to the process stdin, without trimming, adding a newline, changing encoding, shell interpolation, or copying the printed expected text; then send EOF. The recorder itself compares the bytes and uses exclusive creation. If it rejects, leave `05-OWNER-CHECKPOINT.json` absent and keep the checkpoint blocked.

After successful creation, run `--assert-record` against the receipt, request, baseline, matrix, quality report, and current decision. The CLI must resolve the current v1 `decision_contract_sha256` as the exact `checkpointed_decision_contract_sha256` and pass that digest into the decision-object-free `assertOwnerCheckpointRecord({ request, record, baseline, matrix, qualityReport, checkpointedDecisionContractSha256 })` interface. Record only the receipt digest and successful blocking-human provenance in `05-07-SUMMARY.md`; never reproduce or normalize owner bytes in the summary because the receipt already preserves them as base64/hash.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY node scripts/outreach-feasibility/owner-checkpoint.mjs --assert-request --request .planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json --baseline .planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json --matrix .planning/phases/05-outreach-feasibility-gate/05-RIGHTS-MATRIX.json --quality-report .planning/phases/05-outreach-feasibility-gate/05-QUALITY-REPORT.json --decision .planning/phases/05-outreach-feasibility-gate/05-DECISION.json</automated>
    <human-check>
      1. Confirm the request is bound to the exact current rights, no-run quality, stable decision, and immutable baseline digests.
      2. Confirm production outreach remains disabled, the representative spike did not run, and this response cannot authorize it.
      3. If reconfirming the terminal no-go, supply the exact one-time response printed by the CLI; any other response intentionally leaves the checkpoint blocked.
    </human-check>
  </verify>
  <acceptance_criteria>
    <criterion>The blocking-human response is supplied freshly by the owner and is not generated or normalized by the executor.</criterion>
    <criterion>`05-OWNER-CHECKPOINT.json` exists only after the stdin-only recorder accepts an exact byte match and passes `--assert-record`.</criterion>
    <criterion>The receipt binds the request, rights, quality, immutable checkpointed-v1 decision digest, baseline, checkpoint plan/task/gate, response bytes/hash, receive time, and its own canonical digest.</criterion>
    <criterion>No decision, residue, roadmap, requirements, provider, production, or schema state changes in this plan.</criterion>
  </acceptance_criteria>
  <resume-signal>Supply the exact one-time response printed by `owner-checkpoint.mjs --print-required-response`; any mismatch leaves this plan blocked.</resume-signal>
  <done>The owner's fresh, byte-exact no-go reconfirmation is independently preserved at the blocking-human boundary for downstream accepted-decision validation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT.json`
  - Exact fields: `schema_version`, `phase`, `status`, `checkpoint_plan`, `checkpoint_task`, `gate`, `owner_checkpoint_request_sha256`, `nonce`, `rights_evidence_sha256`, `quality_evidence_sha256`, `checkpointed_decision_contract_sha256`, `baseline_evidence_sha256`, `owner_response_utf8_base64`, `owner_response_sha256`, `received_at`, `owner_checkpoint_evidence_sha256`.
- `.planning/phases/05-outreach-feasibility-gate/05-OWNER-CHECKPOINT-REQUEST.json` (consumed from Plan 05-06, not modified here)
  - Exact fields: `schema_version`, `phase`, `status`, `checkpoint_plan`, `checkpoint_task`, `gate`, `nonce`, `rights_evidence_sha256`, `quality_evidence_sha256`, `checkpointed_decision_contract_sha256`, `baseline_evidence_sha256`, `required_response_sha256`, `owner_checkpoint_request_sha256`.
- `.planning/phases/05-outreach-feasibility-gate/05-EXECUTION-BASELINE.json` (consumed from Plan 05-06, not modified here)
  - Exact fields: `schema_version`, `phase`, `status`, `base_sha`, `plan_path`, `plan_blob_sha256`, `baseline_evidence_sha256`.
- `scripts/outreach-feasibility/owner-checkpoint.mjs` (consumed from Plan 05-06, not modified here)
  - Exact function used: `assertOwnerCheckpointRecord({ request, record, baseline, matrix, qualityReport, checkpointedDecisionContractSha256 })`.
  - Exact CLI flags used: `--print-required-response`, `--record-response`, `--assert-record`, `--request`, `--record`, `--baseline`, `--matrix`, `--quality-report`, `--decision`.
- `.planning/phases/05-outreach-feasibility-gate/05-07-SUMMARY.md`
  - Records only receipt digest and checkpoint provenance, not reconstructed owner text.
- No new function, class, CLI flag, package, migration, database object, UI/API route, provider request, fixture, or production symbol.

## Schema Push Gate

No schema push is required. This is a local blocking-human evidence checkpoint with no recognized schema path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Current no-go evidence → owner | The owner must see the exact no-run/disabled posture bound by the one-time request. |
| Runtime owner response → stdin recorder | Only fresh user bytes may cross; agent-generated expected text is forbidden input. |
| Receipt → downstream decision | Exact receipt validation replaces reliance on an agent-authored summary/source string. |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-01 | Spoofing | Blocking-human response | high | mitigate | Fresh owner interaction, one-time nonce, stdin-only byte comparison, exclusive receipt creation, and no executor substitution establish the owner boundary. |
| T-05-07-02 | Tampering | Receipt | high | mitigate | Request/evidence/checkpoint identifiers, immutable checkpointed-v1 decision digest, base64 response, response SHA-256, receive time, exact schema, and canonical receipt digest reject alteration and remain valid after accepted-v2 replacement. |
| T-05-07-03 | Elevation | No-go reconfirmation | high | mitigate | Required text explicitly preserves no-run/disabled state; it contains no search authorization, quality claim, or redesign selection. |
| T-05-07-04 | Repudiation | Checkpoint provenance | high | mitigate | The receipt names Plan 05-07, exact task, blocking-human gate, request digest, and received time independently of the summary. |
| T-05-07-05 | Information Disclosure | Summary/logs | medium | mitigate | The summary records only receipt digest/provenance; owner bytes live in exact base64/hash fields and contain no third-party/provider data. |
| T-05-07-SC | Tampering | Dependency supply chain | low | accept | No package installation occurs. |
</threat_model>

<verification>
Pre-checkpoint request assertion must pass. Post-response receipt assertion must pass before the plan closes; otherwise no receipt exists and execution remains blocked.
</verification>

<success_criteria>
- Fresh owner action crosses a real blocking-human boundary.
- The exact response is preserved independently from summaries and source labels.
- The receipt authorizes only terminal no-go provenance repair.
- Provider calls, quality execution, production outreach, and schema changes remain absent.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-07-SUMMARY.md` when done, recording the receipt digest and checkpoint provenance without reconstructing owner input.
</output>
