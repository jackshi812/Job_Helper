---
phase: 05-outreach-feasibility-gate
plan: 13
type: execute
wave: 11
depends_on:
  - 05-12
files_modified:
  - scripts/outreach-feasibility/evidence-integrity.mjs
  - scripts/outreach-feasibility/evidence-integrity.test.mjs
  - scripts/outreach-feasibility/residue-check.mjs
  - scripts/outreach-feasibility/residue-check.test.mjs
  - scripts/outreach-feasibility/terminal-audit.mjs
  - scripts/outreach-feasibility/terminal-audit.test.mjs
autonomous: true
gap_closure: true
requirements:
  - OUTR-04
  - OUTR-05
must_haves:
  truths:
    - "Every Phase 5 source-review consumer recognizes one final status: clean."
    - "The immutable source-snapshot review remains the issues_found record for CR-01 through CR-04; the final canonical clean report contains zero findings and need not repeat resolved CR entries."
    - "The final clean report covers every file listed by the immutable prior review, so zero findings represent a complete re-review of the affected source and tests."
    - "The statuses skipped, passed, and no_issues cannot satisfy the Phase 5 final source-review gate."
    - "Residue and terminal validation share the same lifecycle helper and canonical timestamp parser."
    - "Terminal tests remain offline and time-stable without a production clock override."
  artifacts:
    - path: scripts/outreach-feasibility/evidence-integrity.mjs
      provides: "Canonical prior-issues-to-zero-finding-clean lifecycle and timestamp interfaces"
    - path: scripts/outreach-feasibility/residue-check.mjs
      provides: "Administrative-tail enforcement of issues_found-to-clean"
    - path: scripts/outreach-feasibility/terminal-audit.mjs
      provides: "Terminal lifecycle enforcement using the same canonical helper"
    - path: scripts/outreach-feasibility/terminal-audit.test.mjs
      provides: "Clean-only lifecycle and current-window temporary-fixture tests"
  key_links:
    - from: scripts/outreach-feasibility/residue-check.mjs
      to: scripts/outreach-feasibility/evidence-integrity.mjs
      via: "shared review transition and canonical UTC validators"
      pattern: "clean"
    - from: scripts/outreach-feasibility/terminal-audit.mjs
      to: scripts/outreach-feasibility/evidence-integrity.mjs
      via: "shared final-review status contract"
      pattern: "review"
---

<objective>
Unify Phase 5 review lifecycle semantics and make residue and terminal consumers compare the immutable prior findings report with a canonical zero-finding clean re-review.

Purpose: Close the verifier's inconsistent review-status blocker and proportionately address skipped-review and timestamp/expiry warnings.
Output: One shared clean-only lifecycle, residue/terminal enforcement, and stable offline tests.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-outreach-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-outreach-feasibility-gate/05-RESEARCH.md
@.planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
@.planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
@.planning/phases/05-outreach-feasibility-gate/05-08-SUMMARY.md
@.planning/phases/05-outreach-feasibility-gate/05-10-SUMMARY.md
@scripts/outreach-feasibility/evidence-integrity.mjs
@scripts/outreach-feasibility/evidence-integrity.test.mjs
@scripts/outreach-feasibility/residue-check.mjs
@scripts/outreach-feasibility/residue-check.test.mjs
@scripts/outreach-feasibility/terminal-audit.mjs
@scripts/outreach-feasibility/terminal-audit.test.mjs
</context>

<prohibitions>
- Do not map skipped, passed, or no_issues to clean.
- Do not rewrite the existing `05-REVIEW.md` to clean in this plan; only the mandatory execute-phase post hook may create the live canonical re-review after every source repair, trust artifact, and plan SUMMARY exists.
- Do not relax current-source immutability, administrative-tail allowlists, zero-effect counters, or the rights evidence window.
- Do not hardcode Phase 5 back to ten plans; lifecycle validation must derive the active plan/summary set where it checks completion.
- Do not run the authoritative live `--terminal-audit`; isolated fixture invocation inside the test suite is non-authoritative.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the canonical Phase 5 review lifecycle</name>
  <read_first>
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - scripts/outreach-feasibility/evidence-integrity.test.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
    - .planning/phases/05-outreach-feasibility-gate/05-VERIFICATION.md
  </read_first>
  <files>scripts/outreach-feasibility/evidence-integrity.mjs, scripts/outreach-feasibility/evidence-integrity.test.mjs</files>
  <behavior>
    - "The prior review is loaded from the immutable source snapshot and must retain issues_found, the original nonzero finding counters, CR-01 through CR-04, and its exact files_reviewed_list."
    - "The final canonical report passes with status clean, critical/warning/info/total all zero, and no CR-01 through CR-04 sections."
    - "The final files_reviewed_list must cover every path from the immutable prior report, contain no duplicates, and agree with files_reviewed."
    - "Final statuses skipped, passed, no_issues, and issues_found are rejected."
    - "A clean report with a nonzero finding counter or incomplete affected-file coverage is rejected."
  </behavior>
  <action>
    Add and export one Phase 5 review lifecycle validator alongside the shared canonical UTC validator from Plan 05-12. Give it the immutable prior review bytes and candidate final review bytes as distinct inputs. Require the prior report to be the source-snapshot `issues_found` record with its original nonzero counters, CR-01 through CR-04, and exact 15-path `files_reviewed_list`. Require a candidate final report to use the canonical `clean` status, zero every finding counter, report no findings, and include a duplicate-free `files_reviewed_list` covering every prior-reviewed path with a matching `files_reviewed` count. Do not require, synthesize, or accept resolved CR entries as a substitute for a canonical clean report; CR identifiers belong to the immutable prior report. Keep parsing strict and finite and introduce no aliases. Unit-test synthetic canonical-clean bytes with no Critical Issues section, each rejected former status, each nonzero final counter, and each missing affected path. These tests define acceptance but do not create or rewrite the live report.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/evidence-integrity.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    One exported helper owns the lifecycle rule, and its tests prove that only the immutable issues_found report followed by a complete zero-finding canonical clean report can pass.
  </acceptance_criteria>
  <done>The shared lifecycle helper accepts only immutable issues_found evidence followed by a complete zero-finding canonical clean report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the shared lifecycle in residue evidence</name>
  <read_first>
    - scripts/outreach-feasibility/residue-check.mjs
    - scripts/outreach-feasibility/residue-check.test.mjs
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - .planning/phases/05-outreach-feasibility-gate/05-REVIEW.md
  </read_first>
  <files>scripts/outreach-feasibility/residue-check.mjs, scripts/outreach-feasibility/residue-check.test.mjs</files>
  <behavior>
    - "The administrative tail loads the prior review blob at source_snapshot.head_sha and accepts its issues_found record transitioning to a complete zero-finding clean live report."
    - "The former passed/no_issues finals and any skipped final are rejected."
    - "A final clean report missing any prior-reviewed path or carrying any nonzero finding count is rejected."
    - "Residue checked_at uses the canonical exact UTC validator."
  </behavior>
  <action>
    Remove the residue checker's local review-status aliases and import the canonical lifecycle helper. Read `05-REVIEW.md` from the immutable `source_snapshot.head_sha` Git tree and pass those exact prior bytes plus the live final bytes to the helper. Enforce the prior CR-01 through CR-04 record only in that immutable blob; require the live report to be canonical `clean` with zero findings and complete coverage of the prior 15-path review list, without resolved CR sections. Keep the existing one-review-change limit, immutable-line rules, and allowed-path checks. Replace the local parse-only `checked_at` acceptance with the shared canonical UTC validator. Extend tests for each rejected former status, nonzero final counters, missing affected-file coverage, impossible timestamps, and a canonical clean report with no CR entries.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/residue-check.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    Residue evidence cannot certify a skipped, alias-status, nonzero-finding, or incomplete-coverage review and cannot accept a parser-normalized timestamp; the immutable-prior-to-canonical-clean fixture passes.
  </acceptance_criteria>
  <done>Residue validation uses the immutable-prior-to-zero-finding-clean transition and canonical timestamp contract with aliases and incomplete coverage rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply clean-only lifecycle and stable clocks to terminal tests</name>
  <read_first>
    - scripts/outreach-feasibility/terminal-audit.mjs
    - scripts/outreach-feasibility/terminal-audit.test.mjs
    - scripts/outreach-feasibility/evidence-integrity.mjs
    - scripts/outreach-feasibility/residue-check.mjs
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <files>scripts/outreach-feasibility/terminal-audit.mjs, scripts/outreach-feasibility/terminal-audit.test.mjs</files>
  <behavior>
    - "Terminal lifecycle rejects skipped, passed, no_issues, and unresolved issues_found review documents."
    - "Terminal lifecycle accepts only a complete zero-finding clean report after validation against the immutable source-snapshot issues_found report."
    - "Plan/summary completion checks derive the Phase 5 plan inventory rather than requiring 10/10 literals."
    - "CLI fixture tests create a temporary internally consistent rights/evidence window relative to test time instead of relying on committed evidence remaining unexpired."
  </behavior>
  <action>
    Replace the terminal audit's local clean-or-skipped check with the shared canonical review helper. Resolve the prior `05-REVIEW.md` bytes from `residue.source_snapshot.head_sha`, compare them with the live report, and accept only the complete zero-finding clean form; do not look for resolved CR entries in the clean document. Derive expected Phase 5 plan and summary completion from the active plan files rather than fixed ten-plan tokens, while still requiring ROADMAP/STATE to agree with that inventory and keeping Phase 6/7 absent. Rebuild isolated CLI test fixtures with a current temporary rights window and recomputed dependent digests so tests remain valid after the committed evidence expires; do not add a production CLI clock flag, environment bypass, or longer validity window. Test terminal behavior only in temporary repositories and do not point `--terminal-audit` at the live workspace.
  </action>
  <verify>
    <automated>env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/terminal-audit.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    Terminal fixture tests prove clean-only lifecycle semantics and remain deterministic beyond 2026-08-05 without changing real runtime expiry enforcement.
  </acceptance_criteria>
  <done>Terminal lifecycle is clean-only, derives plan inventory, and passes isolated current-window tests without a runtime clock bypass.</done>
</task>

</tasks>

<artifacts>
- `scripts/outreach-feasibility/evidence-integrity.mjs` — single canonical immutable-prior-to-zero-finding-clean review lifecycle interface.
- `scripts/outreach-feasibility/residue-check.mjs` — source-to-final review transition enforcement.
- `scripts/outreach-feasibility/terminal-audit.mjs` — clean-only terminal lifecycle and derived plan inventory.
- Their three test files — former-status rejection, CR resolution, impossible-time, and current-window fixture coverage.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| review frontmatter → terminal conclusion | Human-authored status text must not gain finality through inconsistent aliases. |
| source snapshot → administrative tail | The immutable findings report is preserved as resolution lineage while the live canonical clean report proves zero findings and complete affected-file coverage. |
| test clock → production time policy | Stable tests must not introduce a runtime expiry bypass. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-13-01 | Tampering | Review status parsers | high | mitigate | Centralize the sole final status and reject all aliases in every consumer. |
| T-05-13-02 | Repudiation | Findings resolution | high | mitigate | Preserve CR-01..CR-04 in the immutable prior report and require a complete zero-finding clean re-review over every prior-reviewed file. |
| T-05-13-03 | Tampering | Administrative tail | high | mitigate | Preserve finite allowed paths and immutable content while using the shared transition validator. |
| T-05-13-04 | Elevation of Privilege | Test-time clock control | medium | mitigate | Generate current temporary evidence only in isolated tests; expose no production override. |
| T-05-13-SC | Tampering | Dependency supply chain | low | accept | No package installation or dependency change is permitted by this plan. |
</threat_model>

<verification>
Run all three task commands and:
`env -u TAVILY_API_KEY GIT_OPTIONAL_LOCKS=0 node --test scripts/outreach-feasibility/evidence-integrity.test.mjs scripts/outreach-feasibility/residue-check.test.mjs scripts/outreach-feasibility/terminal-audit.test.mjs`

Any test invocation of `--terminal-audit` must target a temporary isolated repository, never the authoritative live workspace.
</verification>

<success_criteria>
- `clean` is the only final review status across schema, residue, and terminal consumers.
- The immutable prior report retains CR-01..CR-04 while the final canonical clean report has zero findings, complete affected-file coverage, and no required resolved-CR sections.
- Review aliases and skipped reviews fail.
- Timestamp and expiry tests are strict, offline, and wall-clock-stable without a runtime bypass.
</success_criteria>

<output>
Create `.planning/phases/05-outreach-feasibility-gate/05-13-SUMMARY.md` when done. Its frontmatter `key-files.created` / `key-files.modified` must include every source and test path changed by this plan so the mandatory execute-phase post-hook review receives complete SUMMARY-derived scope.
</output>
