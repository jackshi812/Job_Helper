---
phase: 03.8-monitor-and-poll-the-branded-banking-companies-currently-on-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/branded-identities.ts
  - supabase/functions/_shared/adapters/scope.ts
  - supabase/functions/_shared/adapters/types.ts
  - supabase/functions/_shared/bounded-pool.ts
  - web/tests/branded-identities.test.ts
  - web/tests/branded-scope.test.ts
  - web/tests/bounded-pool.test.ts
autonomous: true
requirements: [DISC-01, DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "D-01 through D-10: only Morgan Stanley, JPMorgan Chase, and Goldman Sachs receive candidate identities, while the frozen ten-company roster and primary-portal boundary remain explicit."
    - "D-07, D-08, and D-26: caller-controlled network coordinates cannot become fetch authority; exact server-owned identities are resolved before network access."
    - "D-11 through D-16: one shared predicate enforces U.S. detail evidence, the seven exact provider-category terms, Unicode whole-word/phrase semantics, and bounded execution."
    - "D-03: Capital One and Fidelity identity bytes remain unchanged and appear only in regression assertions."
  artifacts:
    - path: "supabase/functions/_shared/branded-identities.ts"
      provides: "Frozen discriminated identities for the three evidence-backed candidates"
      exports: ["BRANDED_IDENTITIES", "resolveBrandedIdentity", "resolveBrandedPublicUrl"]
    - path: "supabase/functions/_shared/adapters/scope.ts"
      provides: "Provider-category and U.S. detail eligibility predicates"
      exports: ["ALLOWED_BRANDED_CATEGORY_TERMS", "matchesAllowedProviderCategory", "hasUnitedStatesDetailEvidence"]
    - path: "supabase/functions/_shared/bounded-pool.ts"
      provides: "Deadline-aware bounded worker pool"
      exports: ["runBoundedPool", "BoundedPoolDeadlineError"]
  key_links:
    - from: "supabase/functions/_shared/branded-identities.ts"
      to: "provider adapters and dispatch"
      via: "exact source-key/public-URL resolution"
      pattern: "resolveBranded(Identity|PublicUrl)"
    - from: "supabase/functions/_shared/adapters/scope.ts"
      to: "normalized provider jobs"
      via: "provider category plus detail-country evidence"
      pattern: "matchesAllowedProviderCategory|hasUnitedStatesDetailEvidence"
---

<objective>
Create the immutable identity, eligibility, evidence, and concurrency contracts every Phase 03.8 connector must obey.

Purpose: Make the hardest security and completeness constraints executable before any candidate adapter can fetch or activate.
Output: Frozen candidate identities, shared scope/evidence types, a bounded pool, and deterministic hostile-input/regression tests.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-RESEARCH.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md

<interfaces>
Existing `PollObservation` carries `jobs`, `completeness`, `credibleForClosure`, optional `allowMissingClosure`, `pageCount`, optional `expectedCount`, and bounded `warnings`.
Existing Workday authorization exposes exact frozen identities through `WORKDAY_IDENTITIES` and `resolveWorkdayIdentity(...)`; Capital One is `workday:wd12:capitalone:Capital_One` and Fidelity is `workday:wd1:fmr:FidelityCareers`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze exact branded identities and hostile URL rejection</name>
  <files>supabase/functions/_shared/branded-identities.ts, web/tests/branded-identities.test.ts</files>
  <read_first>
    - supabase/functions/_shared/branded-identities.ts
    - web/tests/branded-identities.test.ts
    - supabase/functions/_shared/workday-identities.ts
    - web/tests/detect.test.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "Exact reviewed source keys resolve to immutable Morgan Stanley/Eightfold, JPMorgan Chase/Oracle CX_1001, and Goldman Higher identities."
    - "Altered scheme, credentials, port, host, path, site, facet, operation, query, fragment, redirect target, or source key resolves unsupported before fetch."
    - "The other seven D-01 targets have no executable identity; Capital One and Fidelity constants serialize byte-identically to their pre-phase values."
  </behavior>
  <action>
    Per D-01 through D-10, define a frozen discriminated `BrandedIdentity` union and literal constants for `eightfold:morganstanley`, `oracle:jpmc:CX_1001`, and `goldman_higher:roles`. Store every host, path, official public URL, domain/site identifier, country value, fixed facet ID plus expected label, GraphQL operation identity, and transport bound in server code. Export exact source-key and exact primary-public-URL resolvers that return null for any mismatch; do not export URL constructors accepting caller-selected network fields. Do not create identities for Bank of America, Citi, BlackRock, Wells Fargo, UBS, Barclays, or Charles Schwab. Pin Capital One/Fidelity serializations in regression tests without changing their modules.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-identities.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Exactly three Phase 03.8 candidate source keys exist and map to the exact primary portal evidence."
    - "Host/path/site/facet/operation/lookalike/credential/redirect mutations are rejected before the injected fetch can run."
    - "No executable identity exists for any of the seven Unsupported targets."
    - "Capital One and Fidelity source-key/identity regression assertions pass with no production-code changes to `workday-identities.ts`."
  </acceptance_criteria>
  <done>The server owns every candidate network coordinate and rejects all unreviewed identities fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce provider-category and U.S. detail scope</name>
  <files>supabase/functions/_shared/adapters/scope.ts, supabase/functions/_shared/adapters/types.ts, web/tests/branded-scope.test.ts</files>
  <read_first>
    - supabase/functions/_shared/adapters/scope.ts
    - supabase/functions/_shared/adapters/types.ts
    - web/tests/branded-scope.test.ts
    - supabase/functions/_shared/routing.ts
    - supabase/functions/_shared/lifecycle.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "`Data & Analytics` and `Capital Markets Technology` match; `Database Administration` does not."
    - "All seven D-12 terms match case-insensitively across NFKC, punctuation, whitespace, and Unicode letter/number boundaries."
    - "Missing provider category or missing detail-country evidence is ineligible; title and description never substitute."
    - "Any incomplete slice/detail evidence forces `credibleForClosure=false` and `allowMissingClosure=false`."
  </behavior>
  <action>
    Per D-11 through D-15, add pure NFKC normalization and Unicode whole-word/whole-phrase matching for only Data, Technology, Finance, Investment, Research, Risk, and Capital Markets. Accept only a provider-owned category/job-family label and exact job-detail United States evidence; expose no title/description fallback. Extend `NormalizedJob` source literals for `eightfold`, `oracle_recruiting`, and `goldman_higher`, and extend observation evidence only with bounded immutable per-slice/category/country provenance keyed to normalized external IDs. Keep lifecycle compatibility and make partial, drifted, missing, or zero-page evidence closure-ineligible.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- branded-scope.test.ts lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "All locked D-12/D-13 examples and Unicode boundary cases pass."
    - "Tests prove missing category, missing country, title-only matches, and description-only matches fail closed."
    - "The shared observation surface carries bounded source/category/country evidence without creating provider-specific persistence."
    - "Existing lifecycle tests remain green."
  </acceptance_criteria>
  <done>Every adapter shares one exact, test-proven U.S. and provider-category eligibility contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bound nested provider work with one deadline</name>
  <files>supabase/functions/_shared/bounded-pool.ts, web/tests/bounded-pool.test.ts</files>
  <read_first>
    - supabase/functions/_shared/bounded-pool.ts
    - web/tests/bounded-pool.test.ts
    - supabase/functions/_shared/deterministic-worker.ts
    - supabase/functions/poll-tick/index.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-RESEARCH.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "At most the configured number of operations run concurrently and results retain input order."
    - "The monotonic deadline prevents new scheduling, aborts outstanding work, and returns a typed bounded failure."
    - "One operation failure is captured independently and does not cancel unrelated companies unless the shared deadline expires."
  </behavior>
  <action>
    Per D-16, implement a dependency-free generic worker pool with injected monotonic clock, configurable concurrency, a single `AbortController`, and explicit deadline checks before scheduling each operation. Use research defaults only as configurable constants: company concurrency 2, page concurrency 2, detail concurrency 4, page cap 100, and stop-scheduling at 120 seconds. Never accept these limits from a public request or identity. Preserve settled per-item outcomes so D-18/D-26 failure isolation can be enforced by callers.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- bounded-pool.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Deterministic tests measure peak concurrency at or below each configured limit."
    - "No work begins after the deadline and outstanding operations receive the same abort signal."
    - "Non-deadline failures remain item-local and output ordering is deterministic."
    - "No package or lockfile changes are introduced."
  </acceptance_criteria>
  <done>Provider and company concurrency is mechanically bounded and deadline-aware.</done>
</task>

</tasks>

## Artifacts this phase produces

- `BrandedIdentity`, `BRANDED_IDENTITIES`, `resolveBrandedIdentity`, and `resolveBrandedPublicUrl`.
- `ALLOWED_BRANDED_CATEGORY_TERMS`, `matchesAllowedProviderCategory`, and `hasUnitedStatesDetailEvidence`.
- Phase 03.8 provider literals and bounded per-slice/category/country evidence on the normalized adapter contract.
- `runBoundedPool`, `BoundedPoolOptions`, and `BoundedPoolDeadlineError`.
- Deterministic identity, scope, Unicode-boundary, regression, concurrency, and deadline tests.
- No migration, production flag, package, executable identity for an Unsupported target, or UI control.

<threat_model>
ASVS level 1 applies; every high-severity threat below is blocking until its mitigation is implemented and verified.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Catalog/browser/database identity → server fetch | Untrusted or drifted identity data must not select any host, path, tenant, facet, or operation. |
| Provider labels/details → normalized jobs | External category and country evidence controls ingestion and closure scope. |
| Scheduler → upstream providers | Nested concurrency and runtime must remain bounded on the free-tier runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.8-01-01 | Spoofing / Elevation of Privilege | branded identity resolver | high | mitigate | Frozen exact source/public-URL tuples; hostile tuple tests; no generic network constructor. |
| T-03.8-01-02 | Information Disclosure | provider transport identity | high | mitigate | Reject credentials and caller-controlled hosts/paths/facets before fetch; no secret-bearing fields or logs. |
| T-03.8-01-03 | Spoofing | redirect handling contract | high | mitigate | Identity requires the exact reviewed host and downstream adapters use redirect denial. |
| T-03.8-01-04 | Tampering | category/country eligibility | high | mitigate | Provider-owned label plus detail-country predicates; missing evidence fails closed; title/description cannot widen scope. |
| T-03.8-01-05 | Tampering | incomplete scope evidence | high | mitigate | Per-slice evidence and explicit closure-ineligible flags on any missing/drifted detail. |
| T-03.8-01-06 | Denial of Service | nested provider work | high | mitigate | Fixed server-side concurrency, page caps, monotonic deadline, shared abort, and settled item isolation. |
| T-03.8-01-07 | Elevation of Privilege | Capital One/Fidelity registry | high | mitigate | Byte-stability regression assertions and no modifications to their identity module. |
| T-03.8-01-SC | Tampering | dependency supply chain | low | accept | No package installation; runtime built-ins and existing pinned test tooling only. |
</threat_model>

<verification>
- Run the three focused Vitest suites and existing lifecycle/detection regressions.
- Confirm `git diff --check` and that `web/package.json`/lockfile are unchanged.
- Inspect exported identities to confirm exactly three Phase 03.8 candidates and no Unsupported target identity.
</verification>

<success_criteria>
- Exact identities, U.S./category scope, bounded evidence, and concurrency are deterministic and reusable.
- Caller-controlled endpoint/tenant/facet data, category/country bypasses, and resource exhaustion fail before network or persistence.
- D-03 Capital One/Fidelity regression coverage is present without re-onboarding behavior.
</success_criteria>

<output>
Create `.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-01-SUMMARY.md` when done.
</output>
