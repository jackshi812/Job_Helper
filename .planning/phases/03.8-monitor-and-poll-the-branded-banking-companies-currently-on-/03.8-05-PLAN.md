---
phase: 03.8-monitor-and-poll-the-branded-banking-companies-currently-on-
plan: 05
type: execute
wave: 5
depends_on: [03.8-04]
files_modified:
  - supabase/migrations/0040_phase_03_8_branded_connectors.sql
  - web/tests/phase-03-8-migration.test.ts
  - scripts/verify-phase-03-8-hosted.mjs
  - scripts/verify-phase-03-8-hosted.test.mjs
  - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-PREFLIGHT.md
  - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-RELEASE-MANIFEST.json
  - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-HOSTED-VERIFICATION.json
  - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-ROLLOUT-EVIDENCE.md
autonomous: false
requirements: [DISC-01, DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "No schema/function/hosted verification mutation occurs until the owner approves one exact source commit, migration checksum, function bundle inventory, Supabase target, current web identity, and bounded provider-proof scope."
    - "The approved non-TTY schema command uses `SUPABASE_ACCESS_TOKEN` and `web/node_modules/.bin/supabase db push --linked`; local tests never stand in for hosted migration parity."
    - "Hosted production is proven at exact migration 0040 and exact verify-board, observe-connectors, and poll-tick bundles before any activation windows are collected."
    - "Migration deployment pre-admits no candidate; hosted parity proves the exact service-role terminal RPC, positive/negative fixture transitions, durable scope provenance, and exact bounded schedule before live family proof."
    - "D-16, D-18, and D-26: a one-use manifest-bound verifier authority injects only fixed fault enums into exact verifier-owned disposable rows through the real lifecycle/health persistence path; it cannot address a real company, real job, URL, provider identity, or network destination."
    - "The verifier authority is finite, compare-and-swap cleaned, and irreversibly consumed/revoked after the all-family proof; exact-release assertions reject any residue or remaining execute grant."
    - "Every drift, partial push, unexpected migration, wrong target, failed restore, or missing credential stops the rollout without fabricating PASS."
  artifacts:
    - path: "scripts/verify-phase-03-8-hosted.mjs"
      provides: "Bounded exact-release preflight, hosted parity, rollout, cleanup, and UAT assertion tool"
    - path: ".planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-RELEASE-MANIFEST.json"
      provides: "Immutable approved release identity and bounded mutation inventory"
    - path: ".planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-HOSTED-VERIFICATION.json"
      provides: "Machine-readable initial hosted schema/function/catalog authorization proof"
    - path: "supabase/migrations/0040_phase_03_8_branded_connectors.sql"
      provides: "One-use manifest-bound verifier run/fixture ownership and service-role-only lifecycle fault RPCs"
      contains: "begin_phase_03_8_verifier_run|exercise_phase_03_8_verifier_fault|finish_phase_03_8_verifier_run"
  key_links:
    - from: "owner-approved manifest"
      to: "production Supabase"
      via: "hash recheck, dry-run, non-TTY db push, migration list, exact function deploy/download"
      pattern: "0040_phase_03_8_branded_connectors|observe-connectors|poll-tick|verify-board"
    - from: "manifest-owned verifier authority and fixed fixture IDs"
      to: "production lifecycle/health persistence"
      via: "service-role-only fixed-enum RPC, version CAS, finite expiry/call budget, guarded cleanup, and terminal grant revocation"
      pattern: "begin_phase_03_8_verifier_run|exercise_phase_03_8_verifier_fault|finish_phase_03_8_verifier_run"
---

<objective>
Cross the production schema/function boundary only after exact owner approval, then prove immutable hosted parity and safe initial state.

Purpose: Ensure production rollout and restore safety are evidence-bound rather than inferred from local tests or mutable deployment aliases.
Output: Preflight/manifest, a one-use verifier-owned lifecycle fault authority, blocking approval, migration 0040 schema push, exact function deployment, and machine-readable hosted baseline proof.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-01-SUMMARY.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-02-SUMMARY.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-03-SUMMARY.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-04-SUMMARY.md
@.planning/phases/03.6-us-only-workday-expansion-nasdaq-s-p-global-morningstar-stat/03.6-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build exact-release preflight and bounded hosted verifier</name>
  <files>supabase/migrations/0040_phase_03_8_branded_connectors.sql, web/tests/phase-03-8-migration.test.ts, scripts/verify-phase-03-8-hosted.mjs, scripts/verify-phase-03-8-hosted.test.mjs, .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-PREFLIGHT.md, .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-RELEASE-MANIFEST.json, .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-HOSTED-VERIFICATION.json, .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-ROLLOUT-EVIDENCE.md</files>
  <read_first>
    - scripts/verify-phase-03-8-hosted.mjs
    - scripts/verify-phase-03-8-hosted.test.mjs
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-PREFLIGHT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-RELEASE-MANIFEST.json
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-HOSTED-VERIFICATION.json
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-ROLLOUT-EVIDENCE.md
    - scripts/verify-phase-03-6-hosted.mjs
    - supabase/migrations/0040_phase_03_8_branded_connectors.sql
    - web/tests/phase-03-8-migration.test.ts
    - supabase/functions/_shared/lifecycle.ts
    - supabase/functions/poll-tick/index.ts
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/verify-board/index.ts
    - supabase/functions/observe-connectors/index.ts
    - supabase/functions/poll-tick/index.ts
    - web/package.json
  </read_first>
  <behavior>
    - "Manifest validation rejects missing/extra keys, mutable targets, hash drift, unbounded counts/time, non-frozen companies, and secrets."
    - "Hosted assertions distinguish preflight, deployed parity, per-family rollout, cleanup, and UAT; no section may prefill PASS."
    - "All verifier-created rows/observations are exact-ID/namespace bounded and restored/deleted in `finally`, with before/after protected snapshots."
    - "Only service role can begin, exercise, or finish the exact manifest authority; anonymous/authenticated calls, unknown run/fixture IDs, expired runs, exhausted budgets, stale versions, and calls after finish fail without mutation."
    - "The only accepted fault values are `incomplete_observation`, `provider_schema_error`, `provider_timeout`, and `clean_recovery`; the RPC accepts no URL, host, provider/source identity, network destination, real company ID, or real job ID."
    - "Each fault drives the same persisted lifecycle/health state transition used by polling on fixed disposable rows: jobs remain open and health degrades; `clean_recovery` restores healthy state; finish CAS-deletes fixtures and the consumed run row, and revokes verifier RPC execution."
  </behavior>
  <action>
    Run focused/full tests, build, lint, and `git diff --check`. Create a clean temporary source-only release worktree from the currently accepted production commit; apply only Phase 03.8 production source and exclude `.planning`, `.DS_Store`, `scripts/agent-dashboard.mjs`, `scripts/agent-dashboard.test.mjs`, `web/zh`, and all unrelated user dirt. Compute SHA-256 for migration 0040, exact function entrypoints and every transitive shared module, source-only commit, and the unchanged or rebuilt immutable web asset used for UAT. Use authenticated linked Supabase read-only commands to record project ref, remote migrations 0001–0039, function identities/JWT settings, catalog/company/observation/job/health/scheduler/RLS/grant baselines, exact Capital One/Fidelity rows, and `web/node_modules/.bin/supabase db push --linked --dry-run` proving only 0040 is proposed.

    Extend migration 0040 with exact symbols `phase_03_8_verifier_runs`, `phase_03_8_verifier_fixtures`, `begin_phase_03_8_verifier_run`, `exercise_phase_03_8_verifier_fault`, and `finish_phase_03_8_verifier_run`. The run row uses one compile-time `release_manifest_id`, one manifest-listed `run_id`, three manifest-listed fixture company/job UUID sets, `armed|running|consumed` state, server timestamps, expiry no later than 20 minutes, a fixed maximum of twelve exercise calls, and monotonically incremented fixture versions. `begin` may move the single latch from armed to running once and may seed only those disposable fixture rows with verifier ownership FKs; it must reject collisions with any pre-existing company/job row. `exercise` accepts only the run ID, one fixed fixture enum (`eightfold_fixture`, `oracle_fixture`, `goldman_fixture`), one fixed fault enum (`incomplete_observation`, `provider_schema_error`, `provider_timeout`, `clean_recovery`), and `p_expected_version`. Internally map the fixture enum to the literal IDs; do not accept a company/job ID, provider/source key, URL, host, path, or network field. Apply the same persisted job-retention and source-health state transition used by the polling lifecycle: each fault is incomplete/non-credible/closure-forbidden, leaves seeded jobs open, advances Degraded health without cross-row writes, and `clean_recovery` returns the fixture to Active/healthy with an advanced heartbeat. `finish` must require exact expected versions, delete only ownership-joined fixture observations/jobs/health/company rows by CAS, transition the latch to consumed, revoke service-role EXECUTE on all three verifier RPCs, and delete the exact consumed run/fixture ownership rows before commit. Every function is `security definer`, uses `search_path = ''`, checks server time/call budget/ownership, and is revoked from public/authenticated; `begin` only updates the migration-seeded exact run row and no reset/rearm/insert authority exists. Add migration tests for literal IDs/enums, grants, collision/expiry/budget/version/real-row rejection, fault/no-closure/recovery transitions, cleanup order, zero verifier rows, consumed-latch evidence returned by `finish`, and post-finish permission denial.

    Create a strict release manifest containing the exact source commit, migration/function/transitive hashes, Supabase target, immutable web deployment/asset identity, three candidate identities, ten-company initial and terminal outcome matrices, protected-source fingerprints, the compile-time `release_manifest_id`, one run ID, the three exact disposable fixture ID sets, finite 20-minute/twelve-call/row/observation/deadline ceilings, the ten-minute cadence/one-minute cron/four-minute initial-stagger/120-second reserve contract, and the exact approved production actions. Implement verifier modes `--preflight`, `--assert-hosted`, `--rollout-family`, `--assert-rollout`, and `--assert-uat`. It must re-read hosted identities before mutation, reject secrets/unbounded scope, never install packages, never use secondary portals, record due/claim/completion/feed timestamps during family rollout, and use `finally` to call `finish_phase_03_8_verifier_run` after the Plan 06 proof or perform equivalent exact CAS cleanup if a fault step fails. Seed all evidence files as PENDING templates. Unit tests must prove arbitrary nonempty manifests/evidence cannot pass, unknown IDs/enums and stale versions cause zero mutation, cleanup failure prevents PASS, and the exact consumed/no-grant terminal state is required.
  </action>
  <verify>
    <automated>node --test scripts/verify-phase-03-8-hosted.test.mjs &amp;&amp; cd web &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; cd .. &amp;&amp; git diff --check &amp;&amp; node scripts/verify-phase-03-8-hosted.mjs --preflight --manifest .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-RELEASE-MANIFEST.json</automated>
  </verify>
  <acceptance_criteria>
    - "Preflight identifies one clean source-only commit and proves planning/unrelated files absent."
    - "Dry-run proposes only migration 0040; remote migration/function/catalog/company/scheduler/RLS/grant baselines are recorded read-only."
    - "Manifest has exact hashes/targets/roster/limits/actions, contains no secret values, and rejects any drift."
    - "Verifier has finite provider/row/observation/time ceilings, exact fixture ownership, guarded cleanup/restore, and no prefilled PASS."
    - "The migration exposes only the exact one-use service-role verifier symbols and four fixed fault values; no input can select a real row, provider identity, URL, host, or network destination."
    - "Tests prove faulted disposable rows retain open jobs and enter Degraded health, clean recovery returns Active/healthy, and finish leaves zero verifier rows plus revoked execute grants."
    - "Verifier tests reject invalid positive/negative terminal transitions and any timestamp chain exceeding 15 minutes."
    - "No production mutation, function deploy, source push, provider observation, or activation occurs."
  </acceptance_criteria>
  <done>One immutable, fully bounded Phase 03.8 release inventory is ready for owner authorization.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: [BLOCKING] Approve the exact schema push, function release, and bounded hosted proof</name>
  <files>None</files>
  <read_first>
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-PREFLIGHT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-RELEASE-MANIFEST.json
    - supabase/migrations/0040_phase_03_8_branded_connectors.sql
    - scripts/verify-phase-03-8-hosted.mjs
  </read_first>
  <decision>Authorize or defer only the displayed source-only Phase 03.8 schema/functions and finite hosted verification/activation operations.</decision>
  <context>
    This is the mandatory production schema-push gate after every schema file is finalized and before any hosted verification. Display the source commit; migration 0040 checksum; verify-board, observe-connectors, poll-tick entry/transitive hashes and JWT settings; Supabase target; immutable web identity; exact three candidate identities; ten catalog outcomes; Capital One/Fidelity protected fingerprints; finite observation/poll/deadline/fixture limits; and cleanup/restore protocol. Any byte, target, dry-run, migration set, identity, or action drift invalidates approval.
  </context>
  <action>
    Require a fresh explicit response approving the exact manifest. State that the schema command is `SUPABASE_ACCESS_TOKEN`-authenticated `web/node_modules/.bin/supabase db push --linked`, with no interactive login or TTY dependency. The approval must also enumerate deployment of only verify-board, observe-connectors, and poll-tick; verifier-owned hosted checks of both service-role terminal RPC branches; and Plan 06's exact-source live proof, positive admission or negative Unsupported transition, three-window activation, manifest-bound one-use disposable-row fault/recovery proof, irreversible verifier-authority consumption/revocation, and ≤15-minute natural-poll evidence. Perform no mutation inside this checkpoint.
  </action>
  <options>
    <option id="approve-exact-release">
      <name>Approve exact release</name>
      <pros>Allows only the checksum-bound schema/functions and finite evidence operations.</pros>
      <cons>Mutates the displayed production Supabase target and can activate only sources that earn three positive windows.</cons>
    </option>
    <option id="defer-release">
      <name>Defer release</name>
      <pros>Keeps production unchanged.</pros>
      <cons>Phase 03.8 remains locally implemented but cannot complete.</cons>
    </option>
  </options>
  <verify>
    <automated>node scripts/verify-phase-03-8-hosted.mjs --preflight --manifest .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-RELEASE-MANIFEST.json</automated>
    <human-check>Approve or defer the exact displayed schema/function release and bounded hosted mutation scope.</human-check>
  </verify>
  <acceptance_criteria>
    - "Approval binds exact source/migration/function/web hashes, Supabase target, roster identities, protected baselines, finite bounds, and cleanup actions."
    - "The only schema command approved is non-TTY `web/node_modules/.bin/supabase db push --linked` with `SUPABASE_ACCESS_TOKEN` present."
    - "Any drift or unexpected migration/action stops and requires a new approval."
    - "The approved mutation inventory includes only exact-source service-role terminal transitions with bounded reason/evidence digests; no generic catalog/company repair is authorized."
    - "The approved verifier inventory names the one release manifest ID, run ID, three disposable fixture ID sets, four fault values, 20-minute/twelve-call limits, CAS cleanup, and terminal grant revocation; it authorizes no real-company fault."
    - "No production mutation occurs at the checkpoint."
  </acceptance_criteria>
  <resume-signal>Reply exactly `approve exact Phase 03.8 release` for the displayed inventory, or `defer release`.</resume-signal>
  <done>The exact production boundary is explicitly approved, or rollout stops unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Push migration 0040, deploy exact functions, and prove hosted parity</name>
  <files>.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-HOSTED-VERIFICATION.json, .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-ROLLOUT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-HOSTED-VERIFICATION.json
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-ROLLOUT-EVIDENCE.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-PREFLIGHT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-RELEASE-MANIFEST.json
    - scripts/verify-phase-03-8-hosted.mjs
    - supabase/migrations/0040_phase_03_8_branded_connectors.sql
    - supabase/functions/verify-board/index.ts
    - supabase/functions/observe-connectors/index.ts
    - supabase/functions/poll-tick/index.ts
  </read_first>
  <action>
    Only after the exact approval signal, recompute every manifest hash/target and stop on drift. Require `SUPABASE_ACCESS_TOKEN` without printing it; rerun `web/node_modules/.bin/supabase db push --linked --dry-run` and require only 0040, then run `web/node_modules/.bin/supabase db push --linked`. Immediately run linked migration listing and prove exact local/remote parity through 0040 before any behavior probe. Deploy only the approved verify-board, observe-connectors, and poll-tick bundles with manifest JWT settings, then independently download/inspect hosted source or bundle identities and exact versions. Push/deploy only the clean approved source commit; planning and unrelated files remain excluded.

    Run the verifier's hosted mode to prove migration/function/web identity, ten initial catalog outcomes, all three candidates Unsupported with `pending_current_live_contract_proof` and no operational authority, seven fixed negative targets unmonitored, constrained `jobs.scope_evidence`, service-only terminal/observation/claim RPC grants, separate bounded claims, exact ten-minute cadence/one-minute cron/four-minute initial stagger, cron secret rejection, unknown/cross-company identity rejection before fetch, and unchanged Capital One/Fidelity plus existing provider/job/health/scheduler fingerprints. With exact terminal-transition fixtures only, prove the terminal RPC's positive branch produces Experimental 0/3/off, its negative branch produces precise Unsupported with no claimable source, and cleanup restores the baseline. Separately prove the verifier run/fixture schema, fixed IDs/enums, armed one-use latch, service-role-only grants, finite expiry/call budget, and zero fixture rows without beginning or consuming the Plan 06 run. Do not collect an accepted activation window for a real candidate in this task. Write machine-readable PASS only for initial hosted parity and zero-residue checks; leave each family rollout, disposable-row fault/recovery proof, verifier-authority revocation, and UAT PENDING. On any failure, preserve truthful failure evidence, execute guarded cleanup/restore, and do not claim schema rollback.
  </action>
  <verify>
    <automated>node scripts/verify-phase-03-8-hosted.mjs --assert-hosted .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-HOSTED-VERIFICATION.json --manifest .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-RELEASE-MANIFEST.json</automated>
  </verify>
  <acceptance_criteria>
    - "Remote migrations exactly match local 0001–0040 and hosted function bytes/settings match the approved manifest."
    - "All ten initial catalog outcomes, exact candidate pending-proof state, seven fixed Unsupported states, terminal transition branches, scope provenance, grants, claims, schedule constants, and zero-result rejection pass hosted checks."
    - "Capital One/Fidelity and existing provider/job/health/scheduler fingerprints are unchanged."
    - "No activation window is accepted, no verifier fixture remains, and family/UAT statuses remain PENDING."
    - "The one-use fault authority is installed but not begun: exact armed latch and service-only ACLs are proven, all disposable fixture rows are absent, and the Plan 06 run remains available exactly once."
    - "Any failed push/deploy/probe is recorded as FAIL with restore/cleanup outcome; local success is never substituted for hosted parity."
  </acceptance_criteria>
  <done>The exact approved backend release is live, hosted-parity verified, and ready for provider-family observation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/verify-phase-03-8-hosted.mjs` and its Node test suite.
- `03.8-05-PREFLIGHT.md`, `03.8-05-RELEASE-MANIFEST.json`, `03.8-05-HOSTED-VERIFICATION.json`, and `03.8-05-ROLLOUT-EVIDENCE.md`.
- One owner-approved source-only production release identity.
- Production migration parity through 0040.
- Exact hosted verify-board, observe-connectors, and poll-tick bundles.
- Manifest-bound `phase_03_8_verifier_runs` / `phase_03_8_verifier_fixtures` ownership plus one-use begin/exercise/finish RPCs with fixed fault enums.
- Initial hosted catalog/auth/claim/scheduler/protected-source PASS evidence.
- No package, speculative activation, schema rollback claim, or planning-file deployment.

<threat_model>
ASVS level 1 applies; every high-severity threat below is blocking until its mitigation is implemented and verified.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Local source/manifest → production target | Mutable files and aliases must not substitute for the approved bytes/target. |
| Human approval → schema/function mutation | Only the exact approved inventory may cross production boundaries. |
| Verifier → privileged production data | Bounded probes must not leak secrets, exceed scope, or leave residue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.8-05-01 | Spoofing | release target/identity | high | mitigate | Bind source commit, migration/function/transitive hashes, project ref, web deployment/asset, and recheck before mutation. |
| T-03.8-05-02 | Tampering | schema push | high | mitigate | Dry-run only 0040, explicit owner gate, non-TTY token boundary, immediate linked migration parity. |
| T-03.8-05-03 | Information Disclosure | credentials/logs/evidence | high | mitigate | Presence-only secret checks, redaction, bounded codes/counts, manifest rejects secret values. |
| T-03.8-05-04 | Elevation of Privilege | hosted verifier | high | mitigate | Exact fixture namespace/IDs, finite action inventory, protected baselines, service-only RPC verification. |
| T-03.8-05-05 | Tampering | catalog/operational identity drift | high | mitigate | Registry/catalog/company/claim comparison against the immutable ten-company matrix. |
| T-03.8-05-06 | Denial of Service | provider proof | high | mitigate | Finite request/row/observation/deadline ceilings and no activation observations in the parity task. |
| T-03.8-05-07 | Repudiation | rollout/restore | high | mitigate | Timestamped machine-readable results, guarded finally cleanup, CAS scheduler restore, exact before/after hashes. |
| T-03.8-05-08 | Elevation of Privilege | WAF/access bypass | high | mitigate | Verifier uses only approved anonymous structured contracts and records Unsupported rather than attempting challenges/HTML/session bootstrap. |
| T-03.8-05-09 | Elevation of Privilege / Tampering | hosted fault authority | critical | mitigate | Compile-time manifest/run/fixture IDs, fixed fault enums, service-only grants, 20-minute/twelve-call bounds, ownership FKs, version CAS, real-row collision rejection, consumed latch, and terminal execute revocation. |
| T-03.8-05-SC | Tampering | dependency supply chain | low | accept | No installation; source-only release uses existing lockfile/tooling. |
</threat_model>

<verification>
- Pre-approval: full tests/build/lint, clean source-only diff, exact hashes, linked read-only baselines, and only-0040 dry-run.
- Post-approval: non-TTY db push, exact migration list, exact function deployment/download, and hosted assertion mode.
- Confirm all family rollout, fault/recovery, authority-revocation, and UAT statuses remain PENDING after initial parity; the one-use verifier latch is armed with zero fixtures.
</verification>

<success_criteria>
- Production schema and functions exactly match the owner-approved release.
- Hosted parity, authorization, catalog truth, claim separation, zero-result rejection, and protected-source baselines pass with zero residue.
- The exact one-use fault authority is manifest-bound, service-role-only, finite, unable to select real/network identities, locally tested, hosted-parity checked, and still unconsumed for Plan 06.
- No provider is credited with activation or natural polling yet.
</success_criteria>

<output>
Create `.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-05-SUMMARY.md` when done.
</output>
