# Phase 03.8 Plan 05 — Exact Release Preflight

Regenerated at `2026-07-26T15:19:28Z`. This repair run made no schema push,
function deployment, scheduler change, provider observation, candidate
activation, row mutation, source push, or Cloudflare deployment.

## Gate status

**READY FOR A FRESH OWNER RELEASE DECISION.** The prior release approval was
consumed by a failed schema-push attempt. Migration `0040`, the verifier script,
the source-only candidate, and the manifest now have new hashes. No prior
approval applies to these bytes.

The repaired release still requires a fresh authorized dry run before retrying
the push. Its exact preflight command is:

`SUPABASE_ACCESS_TOKEN`-authenticated
`web/node_modules/.bin/supabase db push --linked --dry-run`

Fresh dry-run status: **PENDING EXPLICIT EGRESS APPROVAL**. The earlier dry run
proved only the superseded migration checksum and cannot authorize this repaired
release. Any migration other than `0040`, byte, target, identity, or action drift
must stop and require another approval.

## Failed push diagnosis and hosted no-partial-state proof

The approved push reached the `jobs_scope_evidence_check` statement and stopped
with PostgreSQL `SQLSTATE 42883`: `function digest(bytea, unknown) does not
exist`. Hosted pgcrypto is installed in schema `extensions`; the migration
called bare `digest(...)` under a search path that did not resolve that schema.

The unreleased migration now installs pgcrypto with `WITH SCHEMA extensions`
when absent and calls `extensions.digest(...)` explicitly. A disposable
Supabase PostgreSQL 17 check with `search_path = public, pg_catalog` proved the
bare signature unresolved, the qualified signature present, and the repaired
scope digest a 64-character lowercase hexadecimal SHA-256.

An authenticated, read-only hosted query then proved:

- remote migration history remains exactly `0001`–`0039`; `0040` is not recorded;
- `companies.next_poll_at` and `jobs.scope_evidence` are absent;
- all three branded constraint expansions are absent;
- the terminal/verifier tables and six new exact RPCs are absent;
- all three branded candidate company rows and the observe cron are absent.

This is direct evidence of no detected `0040` residue. It is not a claim that a
schema rollback command ran. No function deployment or provider observation ran
after the failed push.

## Immutable source candidate

- Accepted production source: `baf81fcaa72443995f227b118d10eec90da25679`
- Direct-child source-only commit: `82b4956cefdba79c5874d2b7b0019c1de652e18e`
- Raw commit-object SHA-256: `47b2e36022d03f1a3863b21c66d0560d4c131d1876bd63e2108c1abe8dd53282`
- Clean isolated worktree: `/private/tmp/job-copilot-038-release-repair`
- Candidate pushed: **no**
- Changed paths: 28 exact `supabase/**` and `web/**` paths listed in the manifest
- Manifest object SHA-256:
  `f8534846a08139114589e3f2e4cb70788d3caf252a3b111f7f6e781c25f983bf`

The candidate excludes `.planning`, `.DS_Store`, both dashboard-agent scripts,
`web/zh`, provider credentials, secondary portals, and all other user dirt.
Its full web suite passed 61 files / 1,304 tests, TypeScript/Vite build passed,
lint passed with the two pre-existing warnings, and `git diff --check` passed.

## Exact release artifacts

### Migration

| Artifact | SHA-256 | Remote state |
|---|---|---|
| `0040_phase_03_8_branded_connectors.sql` | `3ebc906b974061ec87c10b396b3505f8b7b80b2ec1a5c8d143cd4f1a18e0461d` | local only |

The read-only hosted check returned exact remote `0001`–`0039`; local `0040`
has no remote record.

### Functions

| Function | Entry SHA-256 | Transitive manifest SHA-256 | JWT | Current hosted |
|---|---|---|---|---|
| `verify-board` | `558685a237b2efad872de874b95efe7441fe392a0ea5341307391091156b79e8` | `f31118ad65b89ddf5ada50900d901d7640bf0cf72875d857ee5a5bcfbaa79994` | true | ID `b1f7f9ea-4423-49c9-97bc-946b374b5a1d`, v27, ACTIVE |
| `observe-connectors` | `17a64f51015f977948a5a2506ca1ba22ccbead5279c4366b56c49eadc9bb098d` | `87f94be6e86e534ae23e8fb75349402c64a5da85fb71c4a59a30bf0afe58956b` | false | absent |
| `poll-tick` | `71f41745ea12ac950fdf04cd83f630adced3fbe78fd31491e802e7465e780ecc` | `722c0bb16d35f499804b46775838e0ccd3586d4f6df9f998d827b2fcd51edc44` | false | ID `623f09fa-9ff8-4736-a1ae-69b052cf20a9`, v30, ACTIVE |

Every transitive file and SHA-256 is enumerated in the release manifest.

### Current immutable web identity

- Deployment: `b8b33556-1b6c-4a42-bada-e54e4a2f6855`
- URL: `https://b8b33556.job-helper-qs9.pages.dev`
- Git source: `baf81fcaa72443995f227b118d10eec90da25679`
- Asset: `/assets/index-DSR6ZHoJ.js`
- Bytes / SHA-256: `622411` /
  `8e28857fb560b5997865201c75226069e71a973a5fe66451726d52a7d7a41f1f`

## Production target and read-only baselines

- Supabase project: `job-copilot` / `fjcsvajkkztvlrpdplwx`
- Cloudflare account/project: `c3989e46150d3e2dda972af220a69867` /
  `job-helper`
- Production branch/domain: `main` / `job-helper-qs9.pages.dev`
- Catalog: `db322515f61236cd8c1caff6682ec967738d3b2fff4cbc8e414003a46d49114c`
- Companies: `ee749635de0e0325551071bf97933bca9b30336d83cd2d82d54aa0fa0bf5e07e`
- Observations: `0105dd43ee1a2ef29d8b134381763ca316cf7ad101056525a711d8e2e625c894`
- Jobs: `116b20e5cb98874a89ad80e673ace21b2df2f4b5d5c352b2767108e16a14e559`
- Health: `9a67770f826d34c1c2ed4206ccf2abed0e24358faf2c378826d5b22cbacaa18f`
- Scheduler: `89fe2e43d617413f4f208445ce4d927eaff007219b0da549983c758992d92301`
- RLS/policies: `186762249c633b700e4932d885bfc7e911d728169e0a22c5ab5691f9baf6c9e5`
- Grants: `e56bed15922465f2316d657214fbb8c7b32ce397a84df464411cff6db3c6b639`
- Capital One protected fingerprint:
  `cc04334cc4436ddda4c9436a0b8c7dc167a27f6f117e8e352ebab3d77145db04`
- Fidelity protected fingerprint:
  `2a7ab4ff273283da8d3dec3ada84e6281a271c740d1ccbb206d4f2dc90998b29`

These are server-computed redacted digests; no production row, credential,
resume text, job description, or personal identifier is embedded.

## Frozen roster and initial truth

Morgan Stanley / `eightfold:morganstanley`, JPMorgan Chase /
`oracle:jpmc:CX_1001`, and Goldman Sachs / `goldman_higher:roles` all begin
Unsupported with `pending_current_live_contract_proof`, no source row, no
scheduling, and no monitoring. Bank of America, Citi, BlackRock, Wells Fargo,
UBS, Barclays, and Charles Schwab retain their seven exact fail-closed reasons.
The manifest contains both exact ten-row initial and terminal-outcome matrices.

## One-use verifier boundary

- Manifest ID: `03850000-0000-4000-8000-000000000005`
- Run ID: `03850000-0000-4000-8000-000000000501`
- Fixtures: `eightfold_fixture`, `oracle_fixture`, `goldman_fixture`
- Literal company/job/observation UUIDs: nine exact IDs in the manifest
- Faults: `incomplete_observation`, `provider_schema_error`,
  `provider_timeout`, `clean_recovery`
- Bounds: 20 minutes, 12 exercise calls, 3 companies, 3 jobs,
  12 observations, 300 provider requests, 5,000 rows, 1,200,000 ms deadline
- Cadence: 10-minute Active interval, 1-minute cron, 4-minute initial stagger,
  120-second stop-scheduling reserve, ≤15-minute natural-poll proof

The RPC signatures cannot accept a company/job ID, provider/source identity,
URL, host, path, or network destination. Begin collision-checks and seeds only
literal disposable rows. Exercise requires fixed enums plus expected-version
CAS and keeps every owned job open. Finish requires all exact versions, deletes
only ownership-joined observations/jobs/companies, consumes/deletes the run,
revokes service-role execution on all three RPCs, and requires zero residue.
Public, anonymous, and authenticated execution is revoked.

## Exact future mutation inventory

After a fresh exact-release approval, only these actions may run:

1. Re-run the non-TTY linked dry run and require it to propose only `0040`.
2. Non-TTY `web/node_modules/.bin/supabase db push --linked` with
   `SUPABASE_ACCESS_TOKEN`, applying only repaired `0040`.
3. Deploy only `verify-board` (JWT true), `observe-connectors` (JWT false), and
   `poll-tick` (JWT false) from the source-only candidate.
4. Assert exact hosted migration/function/web/catalog/grant/cadence/protected
   parity, including both service-role terminal-transition branches.
5. In Plan 06 only, observe each exact candidate’s primary source; terminalize
   positive Experimental or precise negative Unsupported outcomes; collect
   three activation windows and ≤15-minute natural polling where eligible.
6. Begin the one-use authority once, exercise only the four fixed faults on the
   three disposable fixtures, and always finish through guarded CAS cleanup.
7. Prove zero fixture residue and irreversible RPC grant revocation.

Any drift, partial push, failed restore, unexpected migration, missing
credential, extra function/source/action, or cleanup failure stops without PASS.
