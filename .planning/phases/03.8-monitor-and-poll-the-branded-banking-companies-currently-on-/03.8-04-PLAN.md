---
phase: 03.8-monitor-and-poll-the-branded-banking-companies-currently-on-
plan: 04
type: execute
wave: 4
depends_on: [03.8-03]
files_modified:
  - supabase/migrations/0040_phase_03_8_branded_connectors.sql
  - web/tests/phase-03-8-activation.test.ts
  - web/tests/phase-03-8-migration.test.ts
  - web/src/lib/watchlist.test.ts
autonomous: true
requirements: [DISC-01, DISC-07, DISC-08, DISC-09]
must_haves:
  truths:
    - "D-01, D-02, and D-22: all ten frozen companies end with one truthful exact candidate or Unsupported catalog disposition; no other company enters the phase."
    - "D-03: Capital One and Fidelity remain exact Active Workday sources with unchanged claim behavior and no Phase 03.8 observation or admission."
    - "D-17 through D-23: migration 0040 admits an exact candidate to Experimental 0/3 only from current positive live proof and atomically terminalizes every failed/unproven candidate as Unsupported; no source can remain indefinitely Experimental or terminal Degraded."
    - "D-16, D-18, D-24, D-26, and DISC-01: Experimental and Active claims are separate and service-role-only; Active sources use a ten-minute cadence, one-minute claim cron, at most four-minute initial stagger, and 120-second execution reserve."
  artifacts:
    - path: "supabase/migrations/0040_phase_03_8_branded_connectors.sql"
      provides: "Forward-only catalog, exact-source terminalization, scope provenance, observation, activation, claim, and scheduler schema"
      contains: "finalize_branded_connector_candidate"
    - path: "web/tests/phase-03-8-migration.test.ts"
      provides: "Static registry/catalog/constraint/grant/claim/parity proof"
    - path: "web/src/lib/watchlist.test.ts"
      provides: "Truthful ten-target and Capital One/Fidelity presentation regression"
  key_links:
    - from: "source_coverage_catalog"
      to: "companies"
      via: "idempotent exact candidate reconciliation without Disabled resurrection"
      pattern: "eightfold:morganstanley|oracle:jpmc:CX_1001|goldman_higher:roles"
    - from: "connector_observations"
      to: "companies.activation_state"
      via: "positive three-window exact-source activation"
      pattern: "record_connector_observation"
    - from: "live verifier proof"
      to: "source_coverage_catalog and companies"
      via: "service-role exact-source positive-admit or negative-Unsupported transition"
      pattern: "finalize_branded_connector_candidate"
---

<objective>
Encode truthful catalog outcomes, proof-gated exact Experimental admission, terminal Unsupported disposition, positive activation, durable scope provenance, and bounded claims in one forward migration.

Purpose: Make database state enforce the roster, unsupported evidence, no-zero activation, and lifecycle/scheduling isolation rather than relying on application convention.
Output: Migration 0040 plus terminal-transition, activation, schedule/provenance parity, and Watchlist-state tests.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-RESEARCH.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
@.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-03-SUMMARY.md

<interfaces>
Migration `0037_us_workday_dashboard_queue.sql` owns server-timed connector observations, three-window activation, and Active due claims. Migration `0036_workday_fidelity_integrity_repair.sql` demonstrates protected-row before/after parity. Migration `0013_source_coverage_catalog.sql` defines truthful `experimental` versus `unsupported_with_reason` catalog states.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify positive exact-source activation and independent claims</name>
  <files>web/tests/phase-03-8-activation.test.ts, supabase/migrations/0040_phase_03_8_branded_connectors.sql</files>
  <read_first>
    - web/tests/phase-03-8-activation.test.ts
    - supabase/migrations/0040_phase_03_8_branded_connectors.sql
    - web/tests/activation-policy.test.ts
    - supabase/migrations/0037_us_workday_dashboard_queue.sql
    - supabase/functions/observe-connectors/index.ts
    - supabase/functions/poll-tick/index.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "Zero, partial, non-credible, warning-bearing, mismatched-count, replayed, same-window, Disabled, wrong-provider, or wrong-source evidence never advances progress."
    - "Exactly three positive distinct server-timed windows activate only a migration-admitted exact candidate source."
    - "A service-role exact-source terminal RPC admits current positive live proof to Experimental 0/3 or records a precise Unsupported outcome with no operational authority; anonymous/authenticated roles cannot execute it."
    - "Active polling is statically pinned to a ten-minute source cadence, one-minute cron, maximum four-minute initial stagger, and 120-second execution reserve."
  </behavior>
  <action>
    Start migration `0040_phase_03_8_branded_connectors.sql` as one forward transaction after 0039. Extend provider/source constraints NULL-safely for only `eightfold`, `oracle_recruiting`, and `goldman_higher`. Add constrained `jobs.scope_evidence jsonb` for the bounded exact source key, normalized provider category label, matched D-12 term, `US` detail-country code, external-job evidence digest, and no full payload/credentials.

    Create `finalize_branded_connector_candidate(p_source_key, p_outcome, p_reason, p_evidence_digest)` as a `security definer`, empty-search-path, service-role-only transition over the three literal provider/source/company tuples. Under a row/advisory lock, `admit_experimental` requires a current verifier-owned positive proof digest and atomically reconciles catalog plus operational company to Experimental `0/3`, scheduled false without resurrecting Disabled; `unsupported` requires a bounded exact reason code and atomically leaves/sets catalog `unsupported_with_reason`, nulls operational source authority, deletes verifier-owned observations, and removes any non-Active candidate row. It must reject transition of an already Active source, wrong company/source, replay, arbitrary reason, or caller-supplied network fields. This RPC is the only Plan 06 admission/failure-disposition path; migration application itself admits no candidate.

    Replace `record_connector_observation` forward so every Phase 03.8 source requires `p_job_count > 0`, exact count equality, complete/credible evidence, zero warnings, unique observation ID/window, Experimental state, and a literal provider/source activation allowlist. Create `claim_due_experimental_connectors(batch_size)` with server time, exact source allowlist, `FOR UPDATE SKIP LOCKED`, and independent locks. Keep `claim_due_companies` Active-only, set a newly activated source's first `next_poll_at` to server activation time plus a deterministic 0–4 minute offset, and after each successful claim make it next due exactly ten minutes later. Schedule `poll-tick` every minute and preserve the 120-second application stop-scheduling deadline. Preserve `search_path = ''`, revoke all three RPCs from public/authenticated, and grant only service role.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- phase-03-8-activation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "Activation tests cover every ineligible/replay/same-window/Disabled/cross-company case and exact three-window success."
    - "Zero eligible jobs cannot increment any Phase 03.8 source."
    - "Positive terminalization alone creates Experimental 0/3; negative terminalization creates precise Unsupported with no claimable operational source; Active rows and Disabled rows cannot be overwritten."
    - "Experimental claims cannot return Active/Disabled/unknown rows; Active claims cannot return Experimental rows."
    - "Static tests pin 10-minute Active cadence, 1-minute cron, 4-minute maximum initial stagger, 120-second reserve, service-role-only grants, and worst-case 13-minute discovery-to-feed arithmetic."
  </acceptance_criteria>
  <done>SQL alone owns positive staged activation and separate independently safe execution lanes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile the frozen catalog and preserve protected sources</name>
  <files>supabase/migrations/0040_phase_03_8_branded_connectors.sql, web/tests/phase-03-8-migration.test.ts, web/src/lib/watchlist.test.ts</files>
  <read_first>
    - supabase/migrations/0040_phase_03_8_branded_connectors.sql
    - web/tests/phase-03-8-migration.test.ts
    - web/src/lib/watchlist.test.ts
    - supabase/migrations/0013_source_coverage_catalog.sql
    - supabase/migrations/0036_workday_fidelity_integrity_repair.sql
    - supabase/migrations/0037_us_workday_dashboard_queue.sql
    - supabase/functions/_shared/branded-identities.ts
    - supabase/functions/_shared/workday-identities.ts
    - web/src/lib/watchlist.ts
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-CONTEXT.md
    - .planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-PATTERNS.md
  </read_first>
  <behavior>
    - "Morgan Stanley, JPMorgan, and Goldman remain precise Unsupported rows after migration deployment; deterministic fixtures never admit production sources."
    - "Only the service-role terminal RPC can move a current positive exact-source live proof to Experimental, and every negative branch atomically stays/returns Unsupported."
    - "The seven researched negative targets retain exact current Unsupported reasons and null source/company operational authority."
    - "A deliberately Disabled exact source stays Disabled and is never resurrected by migration replay."
    - "Capital One and Fidelity protected bytes/state/claim eligibility are identical before and after."
  </behavior>
  <action>
    Complete migration 0040 with a ten-row frozen roster assertion. Update current primary-provider evidence and exact Unsupported reasons: Bank of America `primary_portal_html_only_no_structured_machine_contract`; Citi, BlackRock, Barclays, and Charles Schwab `radancy_results_require_html_parsing`; Wells Fargo `primary_portal_managed_challenge_no_bypass`; UBS `structured_endpoint_requires_html_bootstrap_session`. Keep Morgan Stanley, JPMorgan, and Goldman `unsupported_with_reason` using exact `pending_current_live_contract_proof` evidence and no operational authority until Plan 06 invokes the terminal RPC; local fixtures and migration bytes are never production admission proof. Snapshot Capital One/Fidelity and assert exact identity, Active state, catalog, and Active-claim parity before commit. Add the observation schedule plus the exact one-minute Active poll cron using Vault/pg_net; SQL owns the ten-minute cadence and maximum four-minute initial stagger.

    Make `phase-03-8-migration.test.ts` independently compare registry, SQL constraints, catalog rows, positive/negative terminal transitions, `scope_evidence` constraint, claims, activation allowlist, grants, exact cadence/stagger/cron constants, and protected Workday bytes. Update only Watchlist test fixtures/assertions: initial candidates are Unsupported/pending proof; positively terminalized rows merge once and show Experimental 0–3/off then Active/scheduled; negatively terminalized rows are Unsupported/unmonitored with no operational authority; Capital One/Fidelity remain exact Active Workday regression rows. Do not change Watchlist production UI unless a test exposes a compatibility defect.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- phase-03-8-migration.test.ts src/lib/watchlist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "All ten frozen names have one exact current catalog outcome and no out-of-roster row is admitted."
    - "Seven negative targets have their exact reason, null source/company authority, Disabled/Unsupported health, scheduled false, and monitored false."
    - "Migration deployment pre-admits no candidate; hosted positive proof uses the exact terminal RPC to start Experimental 0/3, while every negative proof finishes Unsupported without indefinite Experimental/Degraded state."
    - "`jobs.scope_evidence` accepts only bounded exact source/category/matched-term/US/digest provenance and is written atomically with a job."
    - "Capital One/Fidelity exact bytes, Active state, polling eligibility, and Watchlist presentation remain unchanged."
    - "Migration is forward-only, transactional, contains no destructive table operation, preserves RLS, and has service-only grants."
  </acceptance_criteria>
  <done>The database and Watchlist truthfully represent every frozen company without activation quotas or protected-source drift.</done>
</task>

</tasks>

## Artifacts this phase produces

- Forward-only `supabase/migrations/0040_phase_03_8_branded_connectors.sql`.
- Database support for the three provider IDs and exact source keys.
- Constrained durable per-job `scope_evidence` provenance.
- Service-role-only `finalize_branded_connector_candidate` with exact positive-admit and negative-Unsupported branches.
- Forward `record_connector_observation` positive-evidence rule and exact activation allowlist.
- `public.claim_due_experimental_connectors(integer)` plus exact 10-minute Active cadence, 1-minute cron, 4-minute maximum initial stagger, and observation cron.
- Ten exact initial catalog dispositions/reasons; candidates remain Unsupported until current hosted proof terminalizes them.
- Static terminal-transition, activation, migration, provenance, grant, schedule, protected-source parity, and Watchlist-state tests.
- No deployed migration edit, speculative identity, UI redesign, or secondary portal.

<threat_model>
ASVS level 1 applies; every high-severity threat below is blocking until its mitigation is implemented and verified.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Migration constants → production catalog/companies | Forward SQL mutates operational authorization and visible status. |
| Service runner → observation/claim RPCs | Privileged functions control activation and scheduling. |
| Observation rows → Active state | Evidence must be positive, exact-source, unique-window, and server-timed. |
| Catalog row → Watchlist presentation | Unsupported evidence must not drift into monitored state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03.8-04-01 | Elevation of Privilege | provider/source constraints | high | mitigate | NULL-safe literal provider/source tuples and migration parity tests. |
| T-03.8-04-02 | Tampering | observation activation | high | mitigate | Positive complete count, server clock, row lock, unique ID/window, three-window cap, exact source allowlist. |
| T-03.8-04-03 | Spoofing | cross-company observation | high | mitigate | Company row provider/source must match exact release allowlist under lock. |
| T-03.8-04-04 | Denial of Service | claim/scheduler | high | mitigate | Bounded batch size, `SKIP LOCKED`, staggered due order, separate Experimental/Active lanes. |
| T-03.8-04-05 | Tampering | incomplete polling closure | high | mitigate | Activation requires positive evidence; Active claim does not alter lifecycle closure gate; migration tests assert failed observations close none. |
| T-03.8-04-06 | Elevation of Privilege | RPC grants | high | mitigate | Empty search path, public/authenticated revoke, service-role-only execute. |
| T-03.8-04-07 | Tampering | catalog/operational drift | high | mitigate | Exact ten-row matrix, null authority for Unsupported, registry/SQL/catalog parity tests. |
| T-03.8-04-08 | Tampering | Capital One/Fidelity | high | mitigate | Transactional before/after snapshot and abort-on-drift postconditions. |
| T-03.8-04-09 | Repudiation | migration rollout/restore | high | mitigate | Forward-only checksum-bound migration; no edit or manual repair; hosted parity required in Plan 05. |
| T-03.8-04-10 | Elevation of Privilege | terminal disposition RPC | high | mitigate | Exact tuple/outcome/reason allowlists, live evidence digest, locks, Active/Disabled guards, service-only grant, hosted positive/negative assertions. |
| T-03.8-04-11 | Tampering | per-job scope provenance | high | mitigate | Constrained bounded JSON shape written atomically with jobs and joined by exact source/external ID digest. |
| T-03.8-04-SC | Tampering | dependency supply chain | low | accept | SQL and existing Vitest only; no package install. |
</threat_model>

<verification>
- Run activation, migration, and Watchlist focused suites, then `cd web && npm test`.
- Validate migration syntax against the project's available Supabase runtime without applying it to production.
- Confirm migration 0040 is the only new migration and deployed migrations 0001–0039 are unmodified.
</verification>

<success_criteria>
- Every D-01 target has an exact evidence-backed disposition and no activation quota is encoded.
- Candidate sources enter Experimental automatically only after current positive live proof, fail atomically to Unsupported otherwise, and activate only after three positive clean server windows.
- Unsupported, Disabled, Capital One, and Fidelity states remain truthfully isolated.
</success_criteria>

<output>
Create `.planning/phases/03.8-monitor-and-poll-the-branded-banking-companies-currently-on-/03.8-04-SUMMARY.md` when done.
</output>
