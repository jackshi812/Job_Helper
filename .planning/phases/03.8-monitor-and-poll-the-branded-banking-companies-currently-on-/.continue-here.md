---
context: phase
phase: 03.8-monitor-and-poll-the-branded-banking-companies-currently-on-
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-07-26T17:54:54.493Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual execution failures. A resuming agent must explain each one and its prevention mechanism before acting.

- [ ] CONSTRAINT: Production is ahead of planning evidence — migration 0040 and three functions are live, while Plan 05 evidence still says PENDING — perform read-only reconciliation first and never replay completed mutations.
- [ ] CONSTRAINT: Migration 0040 is deployed and immutable — both earlier defects were repaired before the successful push — any new schema fix must use a new forward migration.
- [ ] CONSTRAINT: External approval must come from the trusted root conversation — subagents repeatedly rejected parent-relayed Supabase authorization — run any newly approved external action only from the root agent.
- [ ] CONSTRAINT: Local Supabase eszip bundling is broken in this environment — it failed before upload with a missing `output.eszip` — use the supported `--use-api` path for an approved redeployment and independently verify hosted source identity.
- [ ] CONSTRAINT: User-owned dirty files predate Phase 03.8 — preserve them and never stage, delete, or revert them.

**Do not proceed until all boxes are checked through explicit understanding.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Replaying completed production mutations | Plan 05 has no SUMMARY and stale PENDING evidence even though migration 0040 and all three functions are live. A normal incomplete-plan redispatch could repeat the push/deploy. | blocking | Start with migration/function read-only parity checks; mark the migration/deploy steps already done; resume only the remaining hosted proof. |
| Editing a deployed migration | Migration 0040 initially failed on unqualified `digest`, then on an over-broad protected-source join, but both fixes were included before the successful deployment. | blocking | Treat the current 0040 bytes as immutable. Use migration 0041+ for any newly discovered schema change. |
| Treating subagent approval handoff as trusted egress approval | Managed reviewers rejected Supabase commands inside subagents even after the parent relayed the user's approval. | blocking | Obtain approval in the root conversation and execute the exact external action from the root context. |
| Assuming local deployment bundling works | `supabase functions deploy verify-board` failed locally because the temporary `output.eszip` was missing. | advisory | Use `supabase functions deploy ... --use-api` only within an approved manifest, then verify hosted identity and JWT settings. |
| Trusting stale evidence over direct hosted state | `03.8-05-HOSTED-VERIFICATION.json` and rollout evidence still describe PENDING/failed state. | blocking | Reconcile them only from fresh authenticated hosted observations; do not infer PASS from the successful CLI exit alone. |

<current_state>

Phase 03.8 is paused in Plan 05, Task 3. Plans 01-04 are complete. Plan 05 release preparation and two hosted-only migration defects were repaired and committed. The active hosted-parity executor was interrupted at the user's request to move work to a laptop session.

Production facts already observed:

- Supabase project: `fjcsvajkkztvlrpdplwx`
- Migration 0040 deployed successfully with SHA-256 `09ff62efcd82a13a4b5b4fbd06ea643f01f837196d1260e1d0d1f744601ce21f`
- Immediate linked migration listing showed exact local/remote parity `0001` through `0040`
- `verify-board` deployed via `--use-api`, JWT verification enabled
- `observe-connectors` deployed via `--use-api --no-verify-jwt`
- `poll-tick` deployed via `--use-api --no-verify-jwt`
- No Plan 06 provider observation, accepted activation window, natural poll proof, disposable fault run, or Plan 07 UAT has occurred

The current Plan 05 hosted verification and rollout evidence files are stale and must not be treated as production truth.
</current_state>

<completed_work>

- Plan 01: frozen identities, exact scope evidence, bounded pool — complete
- Plan 02: Eightfold, Oracle Recruiting, and Goldman Higher adapters — complete
- Plan 03: verification/observation/polling integration — complete
- Plan 04: migration 0040 and local lifecycle/catalog contracts — complete
- Plan 05 Task 1: exact release verifier, preflight, manifest, and repair tests — complete
- Migration repair 1: qualified hosted pgcrypto as `extensions.digest(...)`
- Migration repair 2: restricted Capital One/Fidelity parity comparison to the exact protected source keys
- Production migration 0040: applied successfully
- Production functions: all three approved functions deployed with pinned JWT settings
</completed_work>

<remaining_work>

1. Perform authenticated read-only parity checks before any new mutation:
   - Remote migration history remains exact `0001`-`0040`
   - Hosted function identities/versions/JWT settings match the manifest-pinned sources
   - Initial catalog, grants, claim separation, cadence, protected-source fingerprints, and verifier armed/zero-fixture state are correct
2. Run only the remaining Plan 05 terminal positive/negative disposable checks with guarded cleanup; do not begin the Plan 06 one-use fault run.
3. Update `03.8-05-HOSTED-VERIFICATION.json` and `03.8-05-ROLLOUT-EVIDENCE.md` from observed truth, create `03.8-05-SUMMARY.md`, and update STATE/ROADMAP.
4. Execute Plan 06 independently for Morgan Stanley, JPMorgan Chase, and Goldman Sachs. Each family must truthfully become Experimental/Active or precise Unsupported; no activation quota.
5. Execute Plan 07 signed-in owner UAT.
6. Run phase-level code review, regressions, verifier, and completion gates.
</remaining_work>

<decisions_made>

- Only Morgan Stanley, JPMorgan Chase, and Goldman Sachs have approved candidate connector contracts.
- Bank of America, Citi, BlackRock, Wells Fargo, UBS, Barclays, and Charles Schwab remain fail-closed Unsupported unless new approved primary-portal evidence appears.
- Capital One and Fidelity are protected regression-only Workday sources and must not substitute for Phase 03.8 activation/UAT.
- Function deployments used the installed CLI's supported server-side bundler because local eszip creation failed before upload.
- Successful deployment does not equal hosted parity; Plan 05 remains incomplete until evidence and cleanup pass.
</decisions_made>

<blockers>

- Session transfer: open this repository in the laptop Codex session and run `$gsd-resume-work`.
- Stale evidence: hosted JSON/Markdown must be reconciled before Plan 05 can close.
- Final Plan 07 UAT requires owner interaction.
</blockers>

## Required Reading (in order)

1. This `.continue-here.md` — authoritative transfer state and blocking constraints
2. `.planning/HANDOFF.json` — machine-readable resume state
3. `03.8-05-PLAN.md` — remaining Task 3 contract and acceptance criteria
4. `03.8-05-RELEASE-MANIFEST.json` — exact release and verifier bounds
5. `03.8-05-PREFLIGHT.md` — approved candidate and repaired migration identities
6. `03.8-05-HOSTED-VERIFICATION.json` — stale evidence requiring reconciliation
7. `03.8-05-ROLLOUT-EVIDENCE.md` — stale rollout narrative requiring reconciliation
8. `03.8-CONTEXT.md` — locked owner decisions
9. `.planning/STATE.md` and `.planning/ROADMAP.md` — phase tracking

## Critical Anti-Patterns (do NOT repeat these)

- Do not rerun migration 0040 or blindly redeploy functions because Plan 05 lacks a SUMMARY.
- Do not edit migration 0040 after its successful production deployment.
- Do not mark hosted parity PASS from CLI success alone.
- Do not run Plan 06 observation/fault work while finishing Plan 05.
- Do not stage or modify `.DS_Store`, the phase `.gitkeep`, dashboard scripts/tests, or `web/zh`.

## Infrastructure State

- Active background agents/processes: none; the hosted-parity executor was interrupted
- Supabase target: `fjcsvajkkztvlrpdplwx`
- Remote migrations: directly observed exact `0001`-`0040`
- Functions live: `verify-board`, `observe-connectors`, `poll-tick`
- Function JWT settings intended/live deployment flags: on, off, off respectively
- Source candidate: `83fbf8fc7707d8566f47034229884c517a16c979`
- Current main planning HEAD at pause: `af25ad1`
- No source push occurred
- Secrets remain local in `scripts/.env`; never print them
- Pre-existing dirty files are listed in `.planning/HANDOFF.json`

<context>

Resume conservatively from observed external state, not from incomplete-plan detection. First prove what is already live with read-only checks. Then finish only the remaining Plan 05 hosted assertions and cleanup. The primary risk is duplicate mutation caused by stale artifacts; the secondary risk is accidentally consuming Plan 06's one-use verifier authority while closing Plan 05.
</context>

<next_action>

Start with: run `$gsd-resume-work`, acknowledge every blocking constraint, then perform authenticated read-only migration and function parity checks. Do not execute any mutation until the already-live state is reconciled.
</next_action>
