---
phase: 02.1-source-coverage-expansion
plan: "04"
type: execute
wave: 3
depends_on: ["02.1-02"]
files_modified:
  - web/tests/public-connectors.e2e.test.ts
  - web/tests/detect.test.ts
  - supabase/functions/_shared/adapters/types.ts
  - supabase/functions/_shared/adapters/smartrecruiters.ts
  - supabase/functions/_shared/adapters/recruitee.ts
  - supabase/functions/_shared/connectors.ts
  - supabase/functions/_shared/detect.ts
  - supabase/migrations/0014_public_connectors.sql
autonomous: true
requirements: [DISC-07, DISC-09]
must_haves:
  truths:
    - "SmartRecruiters and Recruitee URLs verify through exact safe detection and the shared registry."
    - "Both adapters reconcile bounded pages/counts before declaring a complete credible observation."
    - "Existing Greenhouse, Lever, and Ashby detection and polling remain intact."
    - "Active claimed SmartRecruiters/Recruitee rows reach their exact adapters through pollConnector; unknown providers fail closed and Experimental Workday remains unclaimable."
  artifacts:
    - path: "supabase/functions/_shared/adapters/smartrecruiters.ts"
      provides: "Bounded SmartRecruiters adapter"
    - path: "supabase/functions/_shared/adapters/recruitee.ts"
      provides: "Documented Recruitee Careers Site adapter"
    - path: "web/tests/public-connectors.e2e.test.ts"
      provides: "URL-to-registry-to-observation provider journey"
    - path: "supabase/migrations/0014_public_connectors.sql"
      provides: "Closed companies.ats_type and jobs.source expansion for SmartRecruiters/Recruitee only"
      contains: "smartrecruiters"
  key_links:
    - from: "supabase/functions/_shared/detect.ts"
      to: "supabase/functions/_shared/connectors.ts"
      via: "typed identities select exact registry entries"
      pattern: "smartrecruiters|recruitee"
    - from: "supabase/functions/poll-tick/index.ts"
      to: "supabase/functions/_shared/connectors.ts"
      via: "Plan 02 pollConnector dispatch consumes the new registry entries"
      pattern: "pollConnector"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Add SmartRecruiters and Recruitee as reusable public connector journeys, without mixing their adapter/detection work with activation-policy persistence.

Purpose: These are the two additional shared platforms supported by safe public contracts.
Output: Failing-then-passing connector tests, bounded adapters, exact detection, and registry entries.

Decisions: D-01 through D-04, D-06, D-07, D-10 through D-14.
</objective>

<execution_context>@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.1-source-coverage-expansion/02.1-CONTEXT.md
@.planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
@.planning/phases/02.1-source-coverage-expansion/02.1-02-SUMMARY.md
<interfaces>Plan 02 provides `ProviderConnector`, `PollObservation`, and a closed registry consumed by both verification and polling. New providers must require no provider-specific branch in either Edge Function.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Write failing public connector journeys</name>
  <files>web/tests/public-connectors.e2e.test.ts, web/tests/detect.test.ts</files>
  <read_first>
    - web/tests/detect.test.ts
    - web/tests/adapters.test.ts
    - supabase/functions/_shared/connectors.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
  </read_first>
  <behavior>
    - Exact approved SmartRecruiters and `uturn.recruitee.com` URLs detect; HTTP, credentials, ports, lookalikes, path escapes, and extra segments reject before network.
    - Complete reconciled pages are credible; blocked, rate-limited, WAF, malformed, oversized, partial, and count-mismatch results are not.
    - Tests use live-shaped fixtures and make no network request.
    - Active claimed SmartRecruiters/Recruitee fixtures dispatch to their exact adapter; unknown providers and Experimental Workday reach no adapter.
  </behavior>
  <action>Create the URL-to-registry-to-observation test and extend the existing detection matrix. Add scheduled-dispatch integration cases against Plan 02's `pollConnector` path: active SmartRecruiters/Recruitee must call their exact adapters, an unknown provider must fail before fetch, and an Experimental Workday fixture must remain rejected/unclaimable. Confirm RED for absent providers while existing provider cases stay green.</action>
  <verify><automated>cd web && npx vitest run tests/public-connectors.e2e.test.ts tests/detect.test.ts tests/adapters.test.ts</automated></verify>
  <acceptance_criteria>
    - Positive and negative host/path matrices are explicit.
    - Pagination, schema, cap, content-type, and error classes are covered.
    - No test calls a live host.
  </acceptance_criteria>
  <done>The two connector journeys are RED for missing production behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded SmartRecruiters and Recruitee adapters</name>
  <files>web/tests/public-connectors.e2e.test.ts, supabase/functions/_shared/adapters/types.ts, supabase/functions/_shared/adapters/smartrecruiters.ts, supabase/functions/_shared/adapters/recruitee.ts, supabase/migrations/0014_public_connectors.sql</files>
  <read_first>
    - supabase/functions/_shared/adapters/lever.ts
    - supabase/functions/_shared/adapters/ashby.ts
    - web/tests/public-connectors.e2e.test.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
  </read_first>
  <behavior>
    - SmartRecruiters reconciles offset/limit pages and `totalFound`; Recruitee validates `/api/offers/` wrapper/records.
    - Required page/detail failure preserves safe returned jobs but makes closure credibility false.
  </behavior>
  <action>Extend `NormalizedJob.source` in `adapters/types.ts` with exactly `smartrecruiters` and `recruitee`. Implement provider-local DTO guards and native-fetch adapters with 15-second timeout, byte/job/page caps, per-host concurrency at most two, stable errors, and `Retry-After`-aware bounded retry. Revalidate redirects and construct targets only from conservative identities. Pin the representative Recruitee tenant; fail its proof if the contract changes instead of silently substituting another tenant. Create ordered migration `0014_public_connectors.sql` that replaces the closed `companies.ats_type` and `jobs.source` checks with prior values plus exactly SmartRecruiters/Recruitee, preserving all rows/uniqueness and adding transactional parity assertions. Do not add Workday/unsupported values or push hosted schema. Add no dependency and parse no arbitrary HTML.</action>
  <verify><automated>cd web && npx vitest run tests/public-connectors.e2e.test.ts tests/adapters.test.ts tests/source-observation.e2e.test.ts</automated></verify>
  <acceptance_criteria>
    - Both adapters return `PollObservation`, never bare arrays.
    - Complete/credible is impossible after count/page/detail invariant failure.
    - Error text and logs remain bounded/redacted.
    - `NormalizedJob.source`, registry provider IDs, `companies.ats_type`, and `jobs.source` contain the same two new executable providers and no unsupported target.
  </acceptance_criteria>
  <done>Both adapters safely normalize public jobs and degrade incomplete observations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire exact detection and registry dispatch</name>
  <files>web/tests/public-connectors.e2e.test.ts, web/tests/detect.test.ts, supabase/functions/_shared/connectors.ts, supabase/functions/_shared/detect.ts</files>
  <read_first>
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/_shared/detect.ts
    - supabase/functions/_shared/adapters/smartrecruiters.ts
    - supabase/functions/_shared/adapters/recruitee.ts
  </read_first>
  <action>Add closed provider identities and exact/suffix-safe detector rules, then register both adapters for common verification/poll dispatch. SmartRecruiters and Recruitee begin Experimental; this plan does not count or promote observations. Complete the scheduled integration cases proving that after activation an active claimed row reaches its exact adapter through Plan 02's `pollConnector`, while unknown and Experimental Workday fixtures make no provider call. Assert registry/type/database parity for every executable provider. Preserve D-03 existing providers and D-07 prohibition on generic HTML.</action>
  <verify><automated>cd web && npx vitest run tests/public-connectors.e2e.test.ts tests/detect.test.ts tests/adapters.test.ts && npm run build</automated></verify>
  <acceptance_criteria>
    - Registry/detector/source type parity is exhaustive.
    - Unknown providers cannot fall through to another adapter.
    - Existing provider regressions and build pass.
    - Integration tests prove active SmartRecruiters/Recruitee dispatch and unknown/Experimental Workday fail-closed behavior.
  </acceptance_criteria>
  <done>A user can verify either public provider through the same safe connector path.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL → detector | Untrusted identity enters exact matching. |
| Edge → provider APIs | Requests, redirects, and untrusted responses cross the network. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-11 | Spoofing / SSRF | Detection and redirects | high | mitigate | HTTPS/exact hosts, conservative identities, fixed templates, redirect and private-address validation. |
| T-02.1-12 | Tampering | Pagination/payloads | high | mitigate | Explicit guards, count reconciliation, and closure-ineligible partial results. |
| T-02.1-13 | Denial of Service | Payload/fan-out | medium | mitigate | Byte/page/job caps, timeout, concurrency limit, and bounded retry. |
| T-02.1-SC | Tampering | Package supply chain | low | accept | Native APIs and pinned dependencies only; no install. |
</threat_model>

<verification>Run focused and full tests/build; inspect provider/detector/registry parity and confirm no unsupported platform or arbitrary HTML path is executable.</verification>
<success_criteria>
- SmartRecruiters and Recruitee are reusable, bounded shared connectors.
- Existing providers do not regress.
- All incomplete/error observations remain closure-ineligible.
</success_criteria>

## Artifacts this phase produces
- `web/tests/public-connectors.e2e.test.ts` — public provider vertical contract.
- `supabase/functions/_shared/adapters/smartrecruiters.ts` — bounded Posting API adapter.
- `supabase/functions/_shared/adapters/recruitee.ts` — bounded Careers Site API adapter.
- `supabase/functions/_shared/detect.ts` and `connectors.ts` — exact identity and dispatch entries.
- `supabase/functions/_shared/adapters/types.ts` — normalized-source union expanded for the two implemented public connectors.
- `supabase/migrations/0014_public_connectors.sql` — strict company/job source checks and parity assertions.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-04-SUMMARY.md` when done.</output>
