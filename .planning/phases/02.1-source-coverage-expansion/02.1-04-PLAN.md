---
phase: 02.1-source-coverage-expansion
plan: "04"
type: execute
wave: 3
depends_on: ["02.1-02"]
files_modified:
  - web/tests/public-connectors.integration.test.ts
  - web/tests/detect.test.ts
  - supabase/functions/_shared/adapters/types.ts
  - supabase/functions/_shared/adapters/smartrecruiters.ts
  - supabase/functions/_shared/adapters/recruitee.ts
  - supabase/functions/_shared/connectors.ts
  - supabase/functions/_shared/detect.ts
  - supabase/migrations/0014_public_connectors.sql
autonomous: true
requirements: [DISC-07, DISC-09]
must_haves:
  truths:
    - "SmartRecruiters and Recruitee URLs verify through exact safe detection and the shared registry."
    - "Both adapters reconcile bounded pages/counts before declaring a complete credible observation."
    - "Existing Greenhouse, Lever, and Ashby detection and polling remain intact."
    - "Active claimed SmartRecruiters/Recruitee rows reach their exact adapters through pollConnector; unknown and synthetic non-active provider rows fail closed."
    - "If SmartRecruiters anonymous access is no longer available at hosted execution time, Plan 07's service-role verifier owns the bounded catalog `unsupported_with_reason` write, proves it unclaimable/not monitored, and phase acceptance proceeds honestly under D-04."
    - "If the pinned Recruitee tenant disappears, no tenant is silently substituted: Plan 07 records the safe unsupported state and stops at a blocking decision checkpoint for a specifically evidenced replacement or explicit D-04 acceptance."
  artifacts:
    - path: "supabase/functions/_shared/adapters/smartrecruiters.ts"
      provides: "Bounded SmartRecruiters adapter"
    - path: "supabase/functions/_shared/adapters/recruitee.ts"
      provides: "Documented Recruitee Careers Site adapter"
    - path: "web/tests/public-connectors.integration.test.ts"
      provides: "URL-to-registry-to-observation provider journey"
    - path: "supabase/migrations/0014_public_connectors.sql"
      provides: "Closed companies.ats_type and jobs.source expansion for SmartRecruiters/Recruitee only"
      contains: "smartrecruiters"
  key_links:
    - from: "supabase/functions/_shared/detect.ts"
      to: "supabase/functions/_shared/connectors.ts"
      via: "typed identities select exact registry entries"
      pattern: "smartrecruiters|recruitee"
    - from: "supabase/functions/poll-tick/index.ts"
      to: "supabase/functions/_shared/connectors.ts"
      via: "Plan 02 pollConnector dispatch consumes the new registry entries"
      pattern: "pollConnector"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Add SmartRecruiters and Recruitee as reusable public connector journeys, without mixing their adapter/detection work with activation-policy persistence.

Purpose: These are the two additional shared platforms supported by safe public contracts.
Output: Failing-then-passing connector tests, bounded adapters, exact detection, and registry entries.

Decisions: D-01 through D-04, D-06, D-07, D-10 through D-14.
</objective>

<execution_context>@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.1-source-coverage-expansion/02.1-CONTEXT.md
@.planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
@.planning/phases/02.1-source-coverage-expansion/02.1-02-SUMMARY.md
<interfaces>Plan 02 provides `ProviderConnector`, `PollObservation`, and a closed registry consumed by both verification and polling. New providers must require no provider-specific branch in either Edge Function.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Write failing public connector journeys</name>
  <files>web/tests/public-connectors.integration.test.ts, web/tests/detect.test.ts</files>
  <read_first>
    - web/tests/detect.test.ts
    - web/tests/adapters.test.ts
    - supabase/functions/_shared/connectors.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
  </read_first>
  <behavior>
    - Exact approved SmartRecruiters and `uturn.recruitee.com` URLs detect; HTTP, credentials, ports, lookalikes, path escapes, and extra segments reject before network.
    - Complete reconciled pages are credible; blocked, rate-limited, WAF, malformed, oversized, partial, and count-mismatch results are not.
    - Tests use live-shaped fixtures and make no network request.
    - Active claimed SmartRecruiters/Recruitee fixtures dispatch to their exact adapter; unknown providers and a synthetic non-active provider fixture reach no adapter.
    - Every provider slug/path segment, including pre-existing Ashby, must pass the same conservative `strictSlug` rule; encoded separators and lax Ashby slugs reject before network.
  </behavior>
  <action>Create the fixture-driven URL-to-registry-to-observation integration test and extend the existing detection matrix. Add scheduled-dispatch cases against Plan 02's `pollConnector` path: active SmartRecruiters/Recruitee must call their exact adapters, an unknown provider must fail before fetch, and a synthetic non-active provider row must remain rejected/unclaimable without inventing Workday shapes before Plan 06. Confirm RED for absent providers while existing provider cases stay green.</action>
  <verify><automated>cd web && npx vitest run tests/public-connectors.integration.test.ts tests/detect.test.ts tests/adapters.test.ts</automated></verify>
  <acceptance_criteria>
    - Positive and negative host/path matrices are explicit.
    - Pagination, schema, cap, content-type, and error classes are covered.
    - No test calls a live host.
  </acceptance_criteria>
  <done>The two connector journeys are RED for missing production behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded SmartRecruiters and Recruitee adapters</name>
  <files>web/tests/public-connectors.integration.test.ts, supabase/functions/_shared/adapters/types.ts, supabase/functions/_shared/adapters/smartrecruiters.ts, supabase/functions/_shared/adapters/recruitee.ts, supabase/migrations/0014_public_connectors.sql</files>
  <read_first>
    - supabase/functions/_shared/adapters/lever.ts
    - supabase/functions/_shared/adapters/ashby.ts
    - web/tests/public-connectors.integration.test.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
  </read_first>
  <behavior>
    - SmartRecruiters reconciles offset/limit pages and `totalFound`; Recruitee validates `/api/offers/` wrapper/records.
    - Required page/detail failure preserves safe returned jobs but makes closure credibility false.
  </behavior>
  <action>Extend `NormalizedJob.source` in `adapters/types.ts` with exactly `smartrecruiters` and `recruitee`. Implement provider-local DTO guards and native-fetch adapters with 15-second timeout, byte/job/page caps, per-host concurrency at most two, stable errors, and `Retry-After`-aware bounded retry. Revalidate redirects and construct targets only from conservative identities. Require exact pagination/count reconciliation: concurrent listing churn yields a partial, closure-ineligible observation retried in a later window; do not add an unproven epsilon. Pin the representative Recruitee tenant. If it disappears or loses the documented public contract, do not silently substitute a tenant: route the bounded evidence to Plan 07, which owns the platform unsupported record and blocking decision checkpoint for one specifically evidenced replacement or explicit D-04 unsupported acceptance. Treat SmartRecruiters' documented API-key requirement versus current anonymous success as an explicit hosted execution-time fork: if anonymous verification still passes, keep the connector Experimental for the activation soak; if it returns authentication/prohibition or otherwise loses a safely pollable public contract, create no schedulable SmartRecruiters company and route the observed fallback to Plan 07, whose service-role verifier is the executable owner for upserting the bounded canonical/evidence catalog row with `unsupported_with_reason`, proving it unclaimable/not monitored, and restoring on aborted verification. Let DISC-07 pass under D-04 as an infeasible target rather than substituting credentials or another tenant. Create ordered migration `0014_public_connectors.sql` that replaces the closed `companies.ats_type` and `jobs.source` checks with prior values plus the implemented provider values, preserving all rows/uniqueness and adding transactional parity assertions that include normal insert, exact-ID reopen, and repost/source-conversion write paths. Do not add Workday/unsupported catalog values or push hosted schema. Add no dependency and parse no arbitrary HTML. Addresses review concerns: Recruitee tenant loss, exact pagination, and provider conversion parity.</action>
  <verify><automated>cd web && npx vitest run tests/public-connectors.integration.test.ts tests/adapters.test.ts tests/source-observation.integration.test.ts</automated></verify>
  <acceptance_criteria>
    - Both adapters return `PollObservation`, never bare arrays.
    - Complete/credible is impossible after count/page/detail invariant failure.
    - Error text and logs remain bounded/redacted.
    - `NormalizedJob.source`, registry provider IDs, `companies.ats_type`, and `jobs.source` contain the same two new executable providers and no unsupported target.
    - SmartRecruiters has an explicit anonymous-access fallback disposition and executable owner: Plan 07 service-role-upserts bounded catalog evidence, proves no schedule/monitoring label, and attempts no API-key or bypass.
    - Recruitee tenant loss has an explicit fail-closed owner and decision checkpoint; no unreviewed replacement tenant can be selected automatically.
    - Constraint parity covers insert, reopen, and repost/source-conversion values.
  </acceptance_criteria>
  <done>Both adapters safely normalize public jobs and degrade incomplete observations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire exact detection and registry dispatch</name>
  <files>web/tests/public-connectors.integration.test.ts, web/tests/detect.test.ts, supabase/functions/_shared/connectors.ts, supabase/functions/_shared/detect.ts</files>
  <read_first>
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/_shared/detect.ts
    - supabase/functions/_shared/adapters/smartrecruiters.ts
    - supabase/functions/_shared/adapters/recruitee.ts
  </read_first>
  <action>Add closed provider identities and exact/suffix-safe detector rules, then register both adapters for common verification/poll dispatch. Apply the conservative slug validator uniformly to Greenhouse, Lever, Ashby, SmartRecruiters, and Recruitee so the pre-existing Ashby exception is closed rather than inherited. SmartRecruiters and Recruitee begin Experimental when their public contracts verify; this plan does not count or promote observations. Complete the scheduled integration cases proving that after activation an active claimed row reaches its exact adapter through Plan 02's `pollConnector`, while unknown and synthetic non-active provider rows make no provider call. Assert registry/type/database parity for every executable provider. Preserve D-03 existing providers, D-04 honest unsupported fallback, and D-07 prohibition on generic HTML. Addresses review concern: Ashby must not retain a lax slug path during detector rework.</action>
  <verify><automated>cd web && npx vitest run tests/public-connectors.integration.test.ts tests/detect.test.ts tests/adapters.test.ts && npm run build</automated></verify>
  <acceptance_criteria>
    - Registry/detector/source type parity is exhaustive.
    - Unknown providers cannot fall through to another adapter.
    - Existing provider regressions and build pass.
    - Integration tests prove active SmartRecruiters/Recruitee dispatch and unknown and synthetic non-active provider rows fail closed.
  </acceptance_criteria>
  <done>A user can verify either public provider through the same safe connector path.</done>
</task>
</tasks>

## Review disposition

Warning-bearing observations remain deliberately ineligible for promotion; relaxing that invariant would weaken D-10/D-11 without provider-specific evidence, so Plan 07 observes and reports warning frequency rather than silently accepting warnings. SmartRecruiters count reconciliation also remains exact: concurrent churn is a safe partial/degraded retry, not closure evidence. Recruitee tenant replacement is never automatic; the hosted owner must either obtain explicit approval for one evidence-backed allowlist entry or retain an unsupported disposition under D-04.

<threat_model>
**ASVS Level:** L1 — opportunistic coverage of primary trust boundaries and high-impact components.

## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL → detector | Untrusted identity enters exact matching. |
| Edge → provider APIs | Requests, redirects, and untrusted responses cross the network. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-11 | Spoofing / SSRF | Detection and redirects | high | mitigate | HTTPS/exact hosts, conservative identities, fixed templates, redirect and private-address validation. |
| T-02.1-12 | Tampering | Pagination/payloads | high | mitigate | Explicit guards, count reconciliation, and closure-ineligible partial results. |
| T-02.1-13 | Denial of Service | Payload/fan-out | medium | mitigate | Byte/page/job caps, timeout, concurrency limit, and bounded retry. |
| T-02.1-SC | Tampering | Package supply chain | low | accept | Native APIs and pinned dependencies only; no install. |
</threat_model>

<verification>Run focused and full tests/build; inspect provider/detector/registry parity and confirm no unsupported platform or arbitrary HTML path is executable.</verification>
<success_criteria>
- SmartRecruiters and Recruitee are reusable, bounded shared connectors.
- Existing providers do not regress.
- All incomplete/error observations remain closure-ineligible.
</success_criteria>

## Artifacts this phase produces
- `web/tests/public-connectors.integration.test.ts` — fixture-driven public provider vertical contract (no live network).
- `supabase/functions/_shared/adapters/smartrecruiters.ts` — bounded Posting API adapter.
- `supabase/functions/_shared/adapters/recruitee.ts` — bounded Careers Site API adapter.
- `supabase/functions/_shared/detect.ts` and `connectors.ts` — exact identity and dispatch entries.
- `supabase/functions/_shared/adapters/types.ts` — normalized-source union expanded for the two implemented public connectors.
- `supabase/migrations/0014_public_connectors.sql` — strict company/job source checks and parity assertions.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-04-SUMMARY.md` when done.</output>
