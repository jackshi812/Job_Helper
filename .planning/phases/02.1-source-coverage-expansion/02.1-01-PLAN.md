---
phase: 02.1-source-coverage-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/tests/source-observation.e2e.test.ts
  - web/tests/lifecycle.test.ts
  - supabase/functions/_shared/adapters/types.ts
  - supabase/functions/_shared/lifecycle.ts
  - supabase/functions/poll-tick/index.ts
autonomous: true
requirements: [DISC-09]
must_haves:
  truths:
    - "Existing Greenhouse, Lever, and Ashby observations ingest and reopen jobs, while closure occurs only after a complete credible observation."
    - "Partial pagination, detail failure, malformed/WAF content, rate limiting, timeout, and implausible emptiness retain last-known jobs and report Degraded."
    - "Adzuna never masks or repairs the health of a failed direct source."
  artifacts:
    - path: "supabase/functions/_shared/adapters/types.ts"
      provides: "PollObservation completeness and closure-credibility contract"
      exports: ["PollObservation"]
    - path: "supabase/functions/_shared/lifecycle.ts"
      provides: "Observation-gated lifecycle planning"
      exports: ["planCompanySync"]
    - path: "web/tests/source-observation.e2e.test.ts"
      provides: "Existing-provider observation-to-lifecycle regression"
  key_links:
    - from: "supabase/functions/poll-tick/index.ts"
      to: "supabase/functions/_shared/lifecycle.ts"
      via: "PollObservation passed to planCompanySync"
      pattern: "planCompanySync.*observation"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Make false closure unreachable before any provider expansion by carrying explicit completeness and credibility from an existing connector observation into lifecycle writes.

Purpose: New paginated sources can return plausible-looking partial data; a bare job array cannot safely authorize closure.
Output: A failing-then-passing vertical observation regression, `PollObservation`, closure-safe lifecycle planning, and existing-provider poll wiring.

Decisions: D-03, D-10, D-11, D-12.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-source-coverage-expansion/02.1-CONTEXT.md
@.planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
@.planning/phases/02.1-source-coverage-expansion/02.1-PATTERNS.md
@.planning/phases/02-watchlist-ingestion-monitoring/02-02-SUMMARY.md

<interfaces>
`NormalizedJob` in `adapters/types.ts` is the immutable first-sight payload. `planCompanySync(existing, returned, nowIso, graceMs?)` currently consumes a bare array. `poll-tick` owns per-company isolation, health updates, exact-ID reopen, inserts, and closure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing existing-provider observation journey</name>
  <files>web/tests/source-observation.e2e.test.ts, web/tests/lifecycle.test.ts</files>
  <read_first>
    - supabase/functions/_shared/adapters/types.ts
    - supabase/functions/_shared/lifecycle.ts
    - supabase/functions/poll-tick/index.ts
    - web/tests/lifecycle.test.ts
  </read_first>
  <behavior>
    - A complete credible Greenhouse/Lever/Ashby observation sees, inserts, reopens, and may close a grace-expired missing job.
    - Partial, unknown, malformed, WAF HTML, 429, timeout, detail failure, count mismatch, and implausibly empty observations produce no close IDs.
    - Safely normalized jobs from a partial observation may advance seen/insert state without advancing source success or authorizing closure.
  </behavior>
  <action>Create fixture-driven tests that span an existing provider result, `PollObservation`, and lifecycle planning. Preserve all current lifecycle cases. Confirm RED because the observation contract is absent; do not weaken the cases to the current array signature.</action>
  <verify><automated>cd web && npx vitest run tests/source-observation.e2e.test.ts tests/lifecycle.test.ts</automated></verify>
  <acceptance_criteria>
    - One positive case proves closure remains possible after credible complete evidence.
    - Every D-10/D-11 failure class proves zero closure.
    - The command fails only for missing production behavior.
  </acceptance_criteria>
  <done>The happy path and retention safety contract are executable and RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry credible observations through the polling lifecycle</name>
  <files>web/tests/source-observation.e2e.test.ts, web/tests/lifecycle.test.ts, supabase/functions/_shared/adapters/types.ts, supabase/functions/_shared/lifecycle.ts, supabase/functions/poll-tick/index.ts</files>
  <read_first>
    - web/tests/source-observation.e2e.test.ts
    - supabase/functions/_shared/adapters/greenhouse.ts
    - supabase/functions/_shared/adapters/lever.ts
    - supabase/functions/_shared/adapters/ashby.ts
    - supabase/functions/_shared/lifecycle.ts
    - supabase/functions/poll-tick/index.ts
  </read_first>
  <behavior>
    - Existing provider adapters are wrapped into complete observations without changing normalization, deduplication, reopen, snapshot, heartbeat, or cadence behavior.
    - Closure requires `completeness = complete`, `credibleForClosure = true`, reconciled provider evidence, and the existing grace rule.
    - Non-credible observations retain last success, store a bounded stable error code/detail, and never use Adzuna as a direct-source fallback.
  </behavior>
  <action>Add `PollObservation` with jobs, completeness, closure credibility, page/count evidence, and bounded warnings. Update lifecycle planning to consume it. In `poll-tick`, explicitly dispatch only the three existing providers, wrap their successful results, and route ambiguous responses to Degraded without closure per D-10 through D-12. Retain exact-ID reopen, immutable snapshots, fingerprint deduplication, per-company `allSettled` isolation, no-work heartbeat, and the approved Adzuna cadence.</action>
  <verify><automated>cd web && npx vitest run tests/source-observation.e2e.test.ts tests/lifecycle.test.ts tests/adapters.test.ts tests/dedup.test.ts</automated></verify>
  <acceptance_criteria>
    - A non-empty partial observation cannot close omitted jobs.
    - Bounded diagnostics exclude bodies, secrets, query strings, and applicant links.
    - Existing provider and lifecycle regressions pass.
  </acceptance_criteria>
  <done>The observation journey is GREEN and failed or incomplete direct sources retain every last-known job.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the safety slice against the full local build</name>
  <files>web/tests/source-observation.e2e.test.ts, web/tests/lifecycle.test.ts</files>
  <read_first>
    - web/tests/source-observation.e2e.test.ts
    - web/tests/lifecycle.test.ts
    - web/package.json
  </read_first>
  <action>Run the complete deterministic suite and production build, then tighten only observation/lifecycle assertions needed to catch regressions found by the broader suite. Do not add provider expansion, schema, UI, or hosted work.</action>
  <verify><automated>cd web && npm test && npm run build</automated></verify>
  <acceptance_criteria>
    - Full tests and build pass.
    - No package manifest or lockfile changes.
    - The plan remains limited to five files and one vertical safety outcome.
  </acceptance_criteria>
  <done>The pre-expansion ingestion path is closure-safe and locally regression-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider payload → lifecycle | Untrusted completeness evidence can change persisted job state. |
| Poll worker → database | Service-role writes can reopen, insert, degrade, or close jobs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-01 | Tampering | `PollObservation` and lifecycle | high | mitigate | Validate shape/count evidence and require explicit complete credible state before closure. |
| T-02.1-02 | Denial of Service | Provider failure handling | medium | mitigate | Preserve per-company isolation, bounded diagnostics, and existing timeouts while retaining last-known data. |
| T-02.1-03 | Information Disclosure | Error persistence/logging | medium | mitigate | Store stable bounded codes/copy only; exclude response bodies, secrets, queries, and applicant links. |
| T-02.1-SC | Tampering | Dependency graph | low | accept | No dependency or package change occurs; existing pinned tooling is reused. |
</threat_model>

<verification>Run `cd web && npm test && npm run build`; inspect the diff to confirm every closure call is guarded by the observation contract and no direct-source fallback was added.</verification>

<success_criteria>
- Credible complete observations preserve all existing lifecycle capabilities.
- Every ambiguous observation retains jobs, reports Degraded, and skips closure.
- Existing Greenhouse, Lever, Ashby, heartbeat, deduplication, and Adzuna behavior remains green.
</success_criteria>

## Artifacts this phase produces

- `web/tests/source-observation.e2e.test.ts` — existing-provider observation journey and degradation proof.
- `supabase/functions/_shared/adapters/types.ts` — `PollObservation` contract.
- `supabase/functions/_shared/lifecycle.ts` — credible-observation closure gate.
- `supabase/functions/poll-tick/index.ts` — fail-closed health and lifecycle wiring.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-01-SUMMARY.md` when done.</output>
