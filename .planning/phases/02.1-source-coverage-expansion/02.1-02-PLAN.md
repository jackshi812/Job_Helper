---
phase: 02.1-source-coverage-expansion
plan: "02"
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - supabase/functions/_shared/connectors.ts
  - supabase/functions/poll-tick/index.ts
  - supabase/functions/verify-board/index.ts
  - web/tests/source-observation.integration.test.ts
  - web/src/lib/watchlist.ts
  - web/src/lib/watchlist.test.ts
  - supabase/migrations/0012_connector_state.sql
autonomous: true
requirements: [PREF-05, DISC-09]
must_haves:
  truths:
    - "A signed-in user can still verify and add Greenhouse, Lever, or Ashby, now receiving a canonical careers URL and authoritative connector state."
    - "Browser clients cannot forge activation, verification progress, or health fields."
    - "Only active connectors are claimable by scheduled polling."
    - "Every claimed active company is polled through exhaustive `pollConnector` registry dispatch; unknown providers fail closed before fetch."
  artifacts:
    - path: "supabase/functions/_shared/connectors.ts"
      provides: "Closed connector registry shared by verification and polling"
      exports: ["providerRegistry", "verifyConnector", "pollConnector"]
    - path: "supabase/migrations/0012_connector_state.sql"
      provides: "Canonical link, source identity, activation/health state, and active-only claims"
      contains: "activation_state"
    - path: "web/src/lib/watchlist.ts"
      provides: "Authoritative verify-and-save client flow and connector-state model"
    - path: "supabase/functions/poll-tick/index.ts"
      provides: "Active-only scheduled registry dispatch with no provider fallback"
  key_links:
    - from: "supabase/functions/verify-board/index.ts"
      to: "supabase/functions/_shared/connectors.ts"
      via: "verifyConnector uses the same provider entry later used by polling"
      pattern: "verifyConnector"
    - from: "web/src/lib/watchlist.ts"
      to: "supabase/functions/verify-board/index.ts"
      via: "signed-in function invocation returns the server-written company"
      pattern: "functions.invoke.*verify-board"
    - from: "supabase/functions/poll-tick/index.ts"
      to: "supabase/functions/_shared/connectors.ts"
      via: "claimed active rows call pollConnector; unknown registry keys fail closed"
      pattern: "pollConnector"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Deliver a server-authoritative verified-add slice: existing supported URLs are detected, live-verified, saved with canonical link/state, and scheduled only when active.

Purpose: Connector expansion cannot safely proceed while browser clients can write operational state or verification and polling can disagree about dispatch.
Output: Closed registry, additive connector-state migration, authoritative verify-and-save function, and updated client model.

Decisions: D-03, D-08, D-09, D-13, D-14.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-source-coverage-expansion/02.1-CONTEXT.md
@.planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md
@.planning/phases/02.1-source-coverage-expansion/02.1-01-SUMMARY.md

<interfaces>
Plan 01 provides `PollObservation`. Existing `detect.ts` returns an allowlisted ATS identity and constructed polling endpoint. `verify-board` is gateway-JWT protected. `addCompany` currently invokes verification and then inserts from the browser; this plan moves persistence behind the server boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify authoritative verified-add and state ownership</name>
  <files>web/src/lib/watchlist.test.ts</files>
  <read_first>
    - web/src/lib/watchlist.ts
    - web/src/lib/watchlist.test.ts
    - supabase/functions/verify-board/index.ts
    - supabase/migrations/0005_watchlist.sql
  </read_first>
  <behavior>
    - A valid existing-provider URL returns the company row written by `verify-board`; the browser performs no follow-up insert.
    - Existing rows expose canonical HTTPS URL, active state, health, and deterministic source key.
    - Invalid URLs save nothing; authenticated clients retain shared select/delete but direct company insert/update is denied.
    - Any gateway-authenticated user may intentionally invoke verify-board, whose service-role write creates/updates the shared company for both users.
  </behavior>
  <action>Extend the watchlist client tests around the revised function response and explicit connector-state columns. Confirm RED before implementation. Keep remove-and-re-add semantics and shared authenticated select/delete behavior unchanged. Add the intended shared-watchlist case: either authenticated account may call JWT-protected `verify-board`, and its server-owned insert/update is visible to both accounts while direct authenticated insert/update remains denied.</action>
  <verify><automated>cd web && npx vitest run src/lib/watchlist.test.ts</automated></verify>
  <acceptance_criteria>
    - Tests prove one authenticated server invocation performs verify-and-save for the shared watchlist.
    - Tests do not mock a browser insert after verification.
    - Existing invalid-input and remove behavior remains covered.
    - The authorization intent is explicit: authenticated select/delete remains; authenticated direct insert/update is revoked.
  </acceptance_criteria>
  <done>The authoritative verified-add behavior is RED and unambiguous.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the registry-backed verified-add journey</name>
  <files>supabase/functions/_shared/connectors.ts, supabase/functions/verify-board/index.ts, web/src/lib/watchlist.ts, web/src/lib/watchlist.test.ts, supabase/migrations/0012_connector_state.sql</files>
  <read_first>
    - supabase/functions/_shared/detect.ts
    - supabase/functions/_shared/adapters/types.ts
    - supabase/functions/verify-board/index.ts
    - supabase/migrations/0005_watchlist.sql
    - supabase/migrations/0008_claim_exclusive.sql
    - web/src/lib/watchlist.ts
  </read_first>
  <behavior>
    - Greenhouse, Lever, and Ashby registry entries own verification and polling wrappers with no implicit fallback.
    - Existing rows backfill as active with deterministic source keys and directly searchable HTTPS canonical links without changing IDs.
    - JWT-protected `verify-board` uses service role only after live verification and returns the persisted row.
  </behavior>
  <action>Create an exhaustive registry for the three existing providers using Plan 01 observations. Add migration 0012 with strict `careers_url`, deterministic unique `source_key`, nullable `site_token`, `activation_state` (`experimental|active|disabled`), progress/verification fields, bounded error code, and observation count. Backfill existing rows as active without changing company/job IDs. Use an exact PostgreSQL authorization mechanism rather than relying on row-level policies for column ownership: `revoke insert, update on table public.companies from authenticated`, retain/grant authenticated `select, delete`, and let only JWT-protected `verify-board` perform company insert/update with the service role. Preserve the intended shared semantics: either authenticated user can request verification, and the server-written company is readable/removable by both authenticated users. Require `activation_state = 'active'` inside `claim_due_companies`. Update `verify-board` to verify through the registry and perform the service-role insert/update; update `addCompany` to consume the returned row. Never fetch `careers_url`; polling targets remain server-constructed typed identities. Do not push hosted schema yet.</action>
  <verify><automated>cd web && npx vitest run src/lib/watchlist.test.ts tests/source-observation.integration.test.ts tests/detect.test.ts && npm run build && rg -n "revoke insert, update on table public.companies from authenticated|grant select, delete on table public.companies to authenticated" ../supabase/migrations/0012_connector_state.sql</automated></verify>
  <acceptance_criteria>
    - Registry lookup is exhaustive and has no Ashby/default fallback.
    - Migration checks remain closed; authenticated direct company insert/update is revoked while select/delete is retained.
    - Tests state and exercise the intended authenticated-user-driven verify request followed by a service-role shared-table write.
    - Active-only eligibility exists in the claim SQL, independently of application dispatch.
    - Existing company IDs, job links, and provider behavior survive the backfill.
  </acceptance_criteria>
  <done>A user can add an existing board end to end while connector identity and operational state remain authoritative server data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route scheduled claims through exhaustive registry dispatch</name>
  <files>web/tests/source-observation.integration.test.ts, supabase/functions/_shared/connectors.ts, supabase/functions/poll-tick/index.ts</files>
  <read_first>
    - web/tests/source-observation.integration.test.ts
    - supabase/functions/_shared/connectors.ts
    - supabase/functions/poll-tick/index.ts
    - supabase/migrations/0012_connector_state.sql
  </read_first>
  <behavior>
    - A claimed active provider calls its exact registry connector and carries the returned PollObservation into lifecycle processing.
    - Unknown provider keys fail closed without Ashby/default dispatch or network access.
    - Experimental and disabled rows are rejected by the SQL claim boundary and by the application guard if injected directly.
  </behavior>
  <action>Replace `poll-tick`'s provider conditionals with `pollConnector(company)` from the closed registry before any new provider can activate. Keep the database active-only claim filter and add an application-level activation guard so a directly injected Experimental/disabled row cannot reach an adapter. Extend the source-observation integration test for exact registry dispatch, unknown-provider fail-closed behavior, and non-active rejection. Preserve all Plan 01 lifecycle/health ordering. Plan 04 will extend these same integration cases with active SmartRecruiters/Recruitee, and Plan 06 will retain the Experimental Workday assertion. Run the full suite/build and leave the linked schema push to Plan 07.</action>
  <verify><automated>cd web && npx vitest run tests/source-observation.integration.test.ts tests/adapters.test.ts tests/lifecycle.test.ts && npm test && npm run build</automated></verify>
  <acceptance_criteria>
    - `poll-tick` contains no provider-specific dispatch branch or default adapter fallback.
    - Active claimed rows enter `pollConnector`; unknown and non-active rows make no provider call.
    - Tests/build pass without package changes.
    - No hosted mutation occurs in this plan.
  </acceptance_criteria>
  <done>The verified-add slice and scheduled polling path share one exhaustive registry and are ready for provider expansion.</done>
</task>

</tasks>

## Review disposition

Rewriting `watchlist.test.ts` away from the browser-insert mock is intentional contract migration, not duplicate coverage: the old assertion would preserve the authorization flaw. Plan 01's temporary three-provider dispatch is likewise replaced here in the immediately dependent wave so no new provider can ever rely on the staging branch.

<threat_model>
**ASVS Level:** L1 — opportunistic coverage of primary trust boundaries and high-impact components.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → verify-board | Authenticated but untrusted URL enters live verification. |
| Edge → provider | Server makes an outbound call to an allowlisted third party. |
| Browser → companies | Shared users manage sources but must not control operational state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-04 | Spoofing / SSRF | Verified-add targets | high | mitigate | Require HTTPS, exact approved hosts, no credentials/ports, constructed endpoints, redirect revalidation, and private-address rejection. |
| T-02.1-05 | Elevation of Privilege | Activation and health columns | high | mitigate | Revoke authenticated `insert, update` on `companies`, retain only shared `select, delete`, and let JWT-protected verification plus service role own writes; claim RPC independently requires active. |
| T-02.1-06 | Tampering | Source identity/backfill | high | mitigate | Deterministic unique keys, strict checks, transactional assertions, and preservation tests for existing IDs. |
| T-02.1-SC | Tampering | Dependency graph | low | accept | No package installation or dependency change is planned. |
</threat_model>

<verification>Run the complete web test/build and inspect the migration/registry diff for strict provider parity, active-only claims, and revocation of browser operational writes.</verification>

<success_criteria>
- Existing supported boards still verify and save through one signed-in action.
- Canonical links and activation/health state are persisted separately from polling identity.
- Browser clients cannot forge state and the scheduler cannot claim non-active rows.
</success_criteria>

## Artifacts this phase produces

- `supabase/functions/_shared/connectors.ts` — closed registry for existing providers.
- `supabase/migrations/0012_connector_state.sql` — canonical URL, identity, activation/health, authorization, and claim changes.
- `supabase/functions/verify-board/index.ts` — authoritative registry-backed verify-and-save flow.
- `supabase/functions/poll-tick/index.ts` — active-only exhaustive `pollConnector` scheduling path.
- `web/tests/source-observation.integration.test.ts` — scheduled registry dispatch and fail-closed integration proof.
- `web/src/lib/watchlist.ts` — connector-state model and server-returned add flow.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-02-SUMMARY.md` when done.</output>
