---
context: phase
phase: 02.1-source-coverage-expansion
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-07-18T03:56:25.120Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual hosted-verification failures. Explain each constraint and its prevention mechanism before continuing execution.

- [ ] CONSTRAINT: Short resumable hosted proof — A `--resume` invocation must never sleep or loop across a future eligibility window. Run one bounded opportunity and return PENDING/COMPLETE.
- [ ] CONSTRAINT: Deploy imported adapter fixes before hosted proof — `verify-board` imports the Workday adapter changed in `4241ffa`. Confirm that exact fix is live with JWT enforcement before trusting another hosted result.
- [ ] CONSTRAINT: Gateway versus handler rejection — Modern `sb_secret_` credentials are rejected by the Supabase gateway and therefore have no handler-entry headers. Do not require in-function headers for that rejection class.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Long-running soak process | The first Workday hosted attempt spent about two minutes in sequential detail fan-out; a soak loop would violate the phase contract and obscure cleanup. | blocking | Run one bounded `--resume` invocation only. If it reports PENDING, stop and resume at `next_eligible_at`; never use sleep or a polling loop. |
| Testing stale deployed code | Hosted proof after `4241ffa` is invalid unless `verify-board`, which imports the adapter, is redeployed with JWT enforcement. | blocking | Inspect deployed function state/version first and redeploy only `verify-board` if the approved post-fix deployment did not finish. |
| Treating gateway rejection as handler rejection | The verifier initially expected auth-stage headers for a modern `sb_secret_` key, but the gateway rejects it before handler entry. | blocking | Preserve commit `4241ffa` classification: gateway signature plus absent handler marker proves non-entry; handler headers apply only to in-function rejection. |
| Phase-wide schema drift false positive | The per-wave gate scanned future planned migrations and demanded pushes before those files existed. | advisory | Do not infer current drift from the old warning. Migrations `0012`-`0016` were pushed in Plan 07; verify remote migration state directly if needed. |

<current_state>
Phase 02.1 is paused in Plan 07 Task 3. Plans 01-06 are complete. Plan 07 Task 1 is committed as `5366175`; Task 2 pushed migrations `0012`-`0016` and initially deployed `verify-board` and `poll-tick`. The first hosted evidence attempt found two verifier defects, fixed and committed as `4241ffa`. A targeted redeploy of only `verify-board` from that fix was explicitly approved and launched, but its completion was not captured before pause. There is no Plan 07 SUMMARY yet, so the plan is incomplete.

No verifier/deployment process and no `.planning/async-jobs` manifest were detected at pause. The only uncommitted files are unrelated user work: `.DS_Store`, `scripts/agent-dashboard.mjs`, and `scripts/agent-dashboard.test.mjs`.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Build a resumable non-destructive hosted verifier — Done in `5366175`.
- Task 2: Push migrations and deploy affected Edge Functions — Migrations `0012`-`0016` pushed in order; initial functions confirmed. Post-fix `verify-board` redeploy outcome requires confirmation.
- Task 3: In progress — first bounded run proved the twelve-row catalog, most bearer rejection paths, the real-user positive control, and Recruitee activation/replay/contention. Bugs were corrected in `4241ffa`; 266 tests, build, lint, and zero-network dry run passed.
</completed_work>

<remaining_work>

- Confirm whether the approved `verify-board` redeploy from `4241ffa` completed and that `verify_jwt=true` remains set.
- Run exactly one short hosted `--resume` opportunity; never sleep/loop across windows.
- Continue separate short invocations until three distinct accepted windows per feasible connector and the fourth-Workday-window no-mutation proof are complete.
- Complete controlled degradation/heartbeat recovery, authorization baselines, catalog parity, cleanup, and idempotence assertions.
- Run hosted `verify-watchlist.ts` and `verify-pipeline.ts`, then full tests/build/lint.
- Create and commit `02.1-07-SUMMARY.md`; update STATE/ROADMAP.
- Execute Plan 08 only after Plan 07 is complete; Plan 08 owns origin push, matching Cloudflare SHA, and deployed UI UAT.
</remaining_work>

<decisions_made>

- User approved pushing migrations `0012`-`0016` and deploying both initial functions to project `fjcsvajkkztvlrpdplwx`.
- User later approved redeploying only `verify-board` after commit `4241ffa`; do not redeploy `poll-tick` or push schema again without a new reason.
- Modern `sb_secret_` rejection is gateway-level, not an in-function rejection.
- Hosted verification remains resumable and non-destructive: one short invocation per server-time opportunity, exact-prefix TTL fixtures, cleanup in `finally`, and no deletion of pre-existing seeds.
- Workday remains fixed to Capital One `wd12/capitalone/Capital_One`, Experimental, unclaimable, and capped at three accepted observations.
</decisions_made>

<blockers>
- External state uncertainty: the approved post-`4241ffa` `verify-board` redeploy was in progress when execution was paused. Verify its result instead of assuming success or failure.
- Time-window dependency: Task 3 requires multiple server-derived windows. Last recorded progress was Recruitee `1/3`, with its prior `next_eligible_at` at `2026-07-18T03:30:00Z`; SmartRecruiters and Workday were eligible with zero durable observations at that snapshot.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable checkpoint and exact next action.
2. `.planning/phases/02.1-source-coverage-expansion/02.1-07-PLAN.md` — hosted proof contract and acceptance criteria.
3. `scripts/verify-source-coverage.ts` — resumable verifier and current hosted probes.
4. `scripts/verify-watchlist.ts` and `scripts/verify-pipeline.ts` — post-`0012` legacy verification and seed-safety rules.
5. `.planning/phases/02.1-source-coverage-expansion/02.1-06-SUMMARY.md` — Workday invariants inherited by the hosted proof.
6. `.planning/STATE.md` and `.planning/ROADMAP.md` — project position; note STATE still reflects Plan 06 because Plan 07 is incomplete.

## Critical Anti-Patterns (do NOT repeat these)

- Long soak/loop: do not run a process that waits across eligibility windows; one bounded `--resume` per turn.
- Stale function proof: confirm `4241ffa` is deployed before accepting hosted Workday results.
- Wrong auth evidence: gateway rejection is proven by gateway signature plus absent handler entry, not nonexistent handler-stage headers.
- Secret exposure: `SUPABASE_JWT_SECRET` is present in gitignored `scripts/.env`; check presence only and never print or commit it.

## Infrastructure State

- Supabase project: `fjcsvajkkztvlrpdplwx` (`Linkedin`).
- Remote schema: migrations `0012` through `0016` were pushed in order.
- Functions before post-fix redeploy: `verify-board` ACTIVE with JWT enforcement; `poll-tick` ACTIVE with gateway JWT disabled and cron-secret validation intact.
- Post-fix deployment: `verify-board`-only redeploy from `4241ffa` was approved/launched; final outcome unknown at pause.
- Gitignored credential: `SUPABASE_JWT_SECRET` is present and non-empty in `scripts/.env`.
- Background processes: none detected for verifier, schema push, or function deployment.
- Async jobs: none.
- Last durable hosted snapshot: SmartRecruiters absent/0/eligible; Recruitee Experimental progress 1, ledger 1, prior next eligibility `2026-07-18T03:30:00Z`; Workday absent/0/eligible; temporary SmartRecruiters rows 0.

<context>
The local implementation is stable: commit `4241ffa` follows the initial verifier commit `5366175`, and its 266-test/build/lint/dry-run gates passed. The main risk is not code uncertainty but accidentally resuming against stale deployed `verify-board` code or turning the server-window proof into one blocking process. Re-establish deployed state first, then advance one bounded opportunity at a time and preserve cleanup evidence after every invocation.
</context>

<next_action>
Start with: confirm no verifier process is running, inspect the deployed `verify-board` state to determine whether the approved `4241ffa` redeploy completed with `verify_jwt=true`, and then run exactly one bounded `node --env-file=scripts/.env --experimental-strip-types scripts/verify-source-coverage.ts --resume` invocation.
</next_action>
