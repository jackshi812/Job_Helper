---
phase: 02.1-source-coverage-expansion
verified: 2026-07-18T16:01:40Z
status: gaps_found
score: 6/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "Hosted verification is non-destructive, rerunnable, and cannot corrupt pre-existing production job lifecycle state."
    status: failed
    reason: "verify-pipeline probe 15 mutates an arbitrary real open job, discards its restore snapshot after a successful reopen, and uses an unconditional non-CAS reopen on failure. A concurrent scheduler/provider removal can therefore leave a timestamp changed or resurrect a legitimately closed job."
    artifacts:
      - path: "scripts/verify-pipeline.ts"
        issue: "Lines 470-511 select and mutate a real seed-company job; line 511 disables success-path restoration; lines 563-569 restore without compare-and-swap protection."
    missing:
      - "Replace probe 15 with an invocation-scoped disposable company/job fixture owned completely by the verifier."
      - "Delete the fixture in finally and assert no marked rows remain."
      - "Rerun the hosted pipeline verifier after the safe fixture implementation."
  - truth: "A successful Experimental connector observation is shown as healthy using verification/soak evidence, while activation remains separate and scheduled polling stays off."
    status: failed
    reason: "Accepted observations update last_verified_at but not last_success_at, while the Watchlist mapper ignores last_verified_at and degrades every row whose last_success_at is null. Capital One Workday is intentionally unscheduled, so its successful 3/3 verification state renders Degraded forever."
    artifacts:
      - path: "supabase/migrations/0015_activation_windows.sql"
        issue: "Lines 197-209 persist last_verified_at and clear errors but do not persist a health-success timestamp usable by the Watchlist."
      - path: "web/src/lib/watchlist.ts"
        issue: "Lines 105-110 and 127-131 derive health and displayed success only from last_success_at, ignoring last_verified_at for Experimental rows."
    missing:
      - "Use an explicit observation-success timestamp, or map successful Experimental rows to last_verified_at when progress is positive and no error exists."
      - "Add a regression for Workday Experimental 3/3 with last_success_at null, last_verified_at set, and no error; expected health is OK with the verification timestamp."
---

# Phase 02.1: Source Coverage Expansion Verification Report

**Phase Goal:** As a job seeker, I want to monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, so that I can broaden my job coverage without trusting brittle or failing sources.

**Verified:** 2026-07-18T16:01:40Z  
**Status:** gaps_found  
**Re-verification:** No — initial goal-backward verification

## User Flow Coverage

| Step | Expected | Evidence | Status |
|---|---|---|---|
| Open Watchlist | Real operational rows and the twelve-company evidence catalog render together | `listCompanies()` queries `companies` and `source_coverage_catalog`; `mergeCoverageRows()` reconciles only non-null source keys | ✓ VERIFIED |
| Open a career site | Each valid canonical HTTPS URL is an underlined, safely named new-tab link | `Watchlist.tsx:71-87`; hosted catalog/asset evidence; unsafe URLs become `Unavailable` | ✓ VERIFIED |
| Understand coverage | Activation, progress, health, provider evidence, and unsupported reasons are visible separately | Seven-column table and catalog mapping exist, but successful Experimental Workday is misclassified as Degraded | ✗ FAILED |
| Add/verify a connector | A real authenticated user reaches the exact registry connector and server-owned persistence | `verify-board/index.ts:195-299`; auth and registry tests; hosted bearer matrix | ✓ VERIFIED |
| Trust failed observations | Partial, failed, changed, or implausibly empty observations retain jobs and cannot authorize closure | `lifecycle.ts:39-47,75-115`; `poll-tick/index.ts:217-288`; failure fixtures and hosted sentinel proof | ✓ VERIFIED |
| Trust activation | Separate database windows, replay/cap checks, and active-only claims govern scheduling | Migration 0015 plus hosted three-window/fourth-window evidence | ✓ VERIFIED |
| Re-run verification safely | Hosted proof must not modify arbitrary production lifecycle rows | `verify-pipeline.ts:470-511,563-569` violates the non-destructive contract | ✗ FAILED |
| Judge deployed interaction | Light/dark, narrow scroll, destination quality, focus, and announcements are exercised on the SHA-proven deployment | Browser-dependent Plan 08 D5 remains pending | ? HUMAN |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | The Watchlist exposes safe canonical links for operational and catalog entries | ✓ VERIFIED | HTTPS parsing, accessible new-tab attributes, twelve seeded URLs, live asset markers, and production smoke evidence agree. |
| 2 | Existing ATS support remains intact while additional feasible platforms are handled honestly | ✓ VERIFIED | Closed six-provider registry; 268 tests; Recruitee Active, SmartRecruiters unsupported without bypass, Workday fixed-identity Experimental, and credentialed/unstable targets catalog-only. |
| 3 | The fixed twelve-company finance matrix has canonical links, provider evidence, and non-monitorable unsupported dispositions | ✓ VERIFIED | Migration 0013, mapper tests, dry-run matrix, hosted catalog proof, and zero-claimability rules agree. |
| 4 | Failed/partial/implausibly empty direct observations retain jobs and cannot authorize closure or receive Adzuna health repair | ✓ VERIFIED | `PollObservation`, `isCredibleCompleteObservation`, lifecycle tests, controlled hosted sentinel proof, and direct-source health writes are wired. |
| 5 | New connectors stage through server-time evidence and only approved stable providers become schedulable | ✓ VERIFIED | RPC uses database time, unique windows, a three-row cap, active-only SQL claim, and application-level rejection; hosted replay/contention/fourth-Workday proofs passed. |
| 6 | Real-user authorization and server ownership block browser/invalid-token state forgery | ✓ VERIFIED | `auth.getUser` and authenticated-role checks precede provider/service-role work; grants revoke authenticated insert/update; hosted invalid-bearer baselines passed. |
| 7 | Hosted verification is non-destructive and safely rerunnable | ✗ FAILED | Probe 15 mutates an arbitrary production job and lacks safe success/failure restoration. |
| 8 | Successful Experimental verification is presented as healthy while polling remains off | ✗ FAILED | `last_verified_at` is persisted but ignored by the health mapper; unscheduled Workday remains falsely Degraded. |

**Score:** 6/8 goal-level truths verified. Across literal PLAN frontmatter, 44/46 truths pass; the two failed concerns are the non-destructive verifier contract and truthful Experimental health.

### Roadmap Success Criteria

| Criterion | Status | Evidence |
|---|---|---|
| Clickable Link column opens stored canonical careers URLs safely | ✓ AUTOMATED / HUMAN DESTINATION CHECK PENDING | Native anchor contract, safe URL parser, asset markers, catalog matrix. |
| Existing providers plus feasible representative platforms are preserved/verified honestly | ✓ VERIFIED | Existing regressions pass; Recruitee Active; SmartRecruiters unsupported; Capital One Workday Experimental; other targets documented unsupported under D-04. |
| Twelve finance companies have direct monitoring where safe and explicit unsupported evidence otherwise | ✓ VERIFIED | Catalog seed/matrix and hosted claimability checks. |
| Failed/changed/empty observations retain jobs and show bounded degradation without closure | ✓ VERIFIED | Lifecycle gating, mapper copy, controlled hosted degradation proof. |
| New connectors pass separate verification opportunities before scheduling | ✓ VERIFIED WITH UI GAP | Database behavior is correct, but successful Experimental health presentation is not. |

## Required Artifacts

The structural artifact checker found all **22/22** PLAN-declared artifacts present and substantive. Manual Level 2-4 inspection found two semantic defects despite the clean existence result.

| Artifact group | Expected | Status | Details |
|---|---|---|---|
| `adapters/types.ts`, `lifecycle.ts`, `poll-tick` | Closure-safe observation-to-lifecycle flow | ✓ VERIFIED | Complete/credible/count/page/warning evidence gates closure; partial safe rows can ingest. |
| `connectors.ts`, `detect.ts`, `verify-board` | Exact, authenticated, closed registry path | ✓ VERIFIED | Server-constructed targets, exact identities, real-user auth, and service-owned writes are wired. |
| Migrations `0012`-`0016` | Closed state/catalog/source/activation/Workday schema | ✓ VERIFIED | Present, ordered, remotely applied per hosted evidence; strict grants/checks/claims remain closed. |
| SmartRecruiters/Recruitee adapters | Bounded reconciled public observations | ⚠ WARNING | Page/count/byte/job/per-request limits exist; SmartRecruiters lacks an invocation-wide detail/time budget. |
| Workday adapter | Fixed Capital One list/detail contract | ✓ VERIFIED | Exact source key, fixed origin, validated relative paths, sequential detail access, and Experimental-only dispatch exclusion. |
| `watchlist.ts`, `Watchlist.tsx` | Real catalog/company data and truthful separate state | ✗ PARTIAL | Data flows, but successful Experimental rows with no scheduled poll are falsely Degraded. |
| `ConfirmDialog.tsx` | Focus-managed accessible removal confirmation | ⚠ WARNING | Trap, cancel-first opt-in, pending state, and restoration exist; removal failure alert is outside the active aria-modal. |
| Hosted verification scripts | Rerunnable non-destructive proof | ✗ FAILED | Source coverage/watchlist drivers are scoped, but pipeline probe 15 mutates a real job unsafely. |
| `02.1-08-SUMMARY.md` and production asset | SHA-bound production evidence | ✓ VERIFIED | Deployment `1e773fd3-49e6-4649-83cf-8ac950c083af`, app SHA `72ed51e...`, asset SHA-256 `d114c742...`, and all four markers recorded and locally reproduced. |

## Key Link Verification

The deterministic checker verified all **14/14** file-backed links. Its two Plan 08 external links are outside its relative-path model; independent production evidence verifies both.

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `poll-tick` | `lifecycle.ts` | `PollObservation` into `planCompanySync` | ✓ WIRED | Closure consumes the explicit observation contract. |
| `verify-board` | registry/Auth/database RPC | auth resolution, `verifyConnector`, service write, activation RPC | ✓ WIRED | Invalid users exit before provider/service work. |
| Watchlist page | `watchlist.ts` | React Query and pure presentation helpers | ⚠ WIRED WITH BUG | Real data flows, but mapper ignores `last_verified_at` for Experimental health. |
| detector | registry/adapters | typed provider identity | ✓ WIRED | No default provider fallback. |
| activation verifier | `record_connector_observation` | server-returned windows/progress | ✓ WIRED | No client timestamp or activation target. |
| hosted verifier | `poll-tick` | disposable degradation fixture | ✓ WIRED | Source-coverage failure fixture is scoped; separate legacy pipeline probe remains unsafe. |
| `origin/main` | Cloudflare production | exact commit metadata | ✓ EXTERNAL EVIDENCE | Approved app SHA equals successful production metadata. |
| deployed asset | Watchlist UI | immutable bundle markers/hash | ✓ EXTERNAL EVIDENCE | Local production build reproduces the recorded JS SHA-256 and required markers. |

## Data-Flow Trace (Level 4)

| Artifact | Data | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `Watchlist.tsx` | `companiesQuery.data` | `companies` + `source_coverage_catalog` through `mergeCoverageRows` | Yes | ⚠ FLOWING, presentation bug |
| `verify-board` | authoritative company/activation result | bearer-resolved user → exact provider → service-role insert/RPC → persisted reload | Yes | ✓ FLOWING |
| `poll-tick` | normalized observation/lifecycle writes | active SQL claim → registry adapter → `PollObservation` → jobs/health/closure | Yes | ✓ FLOWING |
| Production Watchlist | compiled UI | approved commit → successful Cloudflare deployment → immutable JS asset | Yes | ✓ FLOWING |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full deterministic regression | `cd web && npm test` | 20 files, 268 tests passed | ✓ PASS |
| Production build | `cd web && npm run build` | TypeScript/Vite build passed; known chunk warning only | ✓ PASS |
| Lint | `cd web && npm run lint` | Exit 0; pre-existing Fast Refresh warning only | ✓ PASS |
| Hosted verifier definition/no-network contract | `node --experimental-strip-types --check scripts/verify-source-coverage.ts && node --experimental-strip-types scripts/verify-source-coverage.ts --dry-run` | All assertions PASS; `network_calls=0 database_calls=0` | ✓ PASS |
| Experimental Workday health mapping | Source/test inspection | No test maps `last_success_at: null`, successful `last_verified_at`, and 3/3 progress to OK; code maps it Degraded | ✗ FAIL |
| Pipeline verifier restoration | Source inspection | Success discards restore snapshot; failure restoration has no compare-and-swap | ✗ FAIL |

## Probe Execution

No conventional `scripts/**/tests/probe-*.sh` files are declared. The phase uses TypeScript verification drivers.

| Probe/driver | Result | Status |
|---|---|---|
| `verify-source-coverage.ts --dry-run` | Independently rerun; all local definitions valid, zero network/database calls | ✓ PASS |
| `verify-source-coverage.ts --resume` | Execution record reports hosted COMPLETE twice and idempotent counts | ✓ PRIOR HOSTED EVIDENCE |
| `verify-watchlist.ts` | Execution record reports post-0012 hosted pass | ✓ PRIOR HOSTED EVIDENCE |
| `verify-pipeline.ts` | Execution record reports hosted pass, but probe 15 is unsafe by construction | ✗ EVIDENCE DRIVER GAP |

The verifier did not rerun stateful hosted drivers because doing so would repeat the unsafe mutation identified in CR-01.

## Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| PREF-05 | 02, 03, 07, 08 | Safe clickable careers Link | ? NEEDS HUMAN | Code, catalog, production asset, and smoke evidence pass; actual destination quality, viewport, contrast, and keyboard behavior remain Plan 08 D5. |
| DISC-07 | 04-08 | Feasible representative source platforms with existing-provider preservation | ✓ SATISFIED | Recruitee Active; SmartRecruiters honest unsupported; Workday fixed Experimental; Oracle/iCIMS/SuccessFactors/Eightfold documented non-executable; existing adapters pass. |
| DISC-08 | 03, 06-08 | Fixed finance-company validation set | ✓ SATISFIED | Twelve exact rows plus provider evidence/unsupported reasons; unsupported rows are not claimable/monitored. |
| DISC-09 | 01, 02, 04-08 | Retention, degradation, and staged activation | ✗ BLOCKED | Lifecycle safety and activation work, but Experimental success is presented as Degraded and one hosted evidence driver is destructive. |

No orphaned Phase 02.1 requirement IDs were found.

## Code Review Finding Adjudication

| Finding | Independent verdict | Must-have impact |
|---|---|---|
| CR-01 unsafe arbitrary production-job mutation/restore | **Confirmed — BLOCKER.** Lines 470-511 and 563-569 match the reported behavior. | Fails Plan 07's rerunnable/non-destructive verifier truth and undermines hosted regression evidence. |
| CR-02 successful Experimental Workday renders Degraded | **Confirmed — BLOCKER.** Migration 0015 writes `last_verified_at`; mapper/HealthStatus use only `last_success_at`. | Fails the phase goal's honest state contract and UI-SPEC Experimental/OK mapping. |
| WR-01 SmartRecruiters lacks invocation-wide detail budget | **Confirmed — WARNING.** Per-request timeout and concurrency two exist, but the loop can run hundreds of sequential batches. | Does not falsify current hosted disposition because SmartRecruiters is unsupported/unclaimable, but weakens the promised bounded adapter if access returns. |
| WR-02 modal removal error is outside `aria-modal` | **Confirmed — WARNING.** `ConfirmDialog` swallows the rejection while the only alert is behind the modal. | Accessibility/error-path risk; carry into gap closure or Plan 08 D5 UAT, but not a separate roadmap blocker. |

## Anti-Patterns and Disconfirmation Pass

| Check | Result | Assessment |
|---|---|---|
| `TBD` / `FIXME` / `XXX` in 30 reviewed files | None | ✓ CLEAN |
| Placeholder scan | URL input example only | ℹ Intentional input hint, not a stub |
| Partial requirement found | Experimental success/health mapping | ✗ Real production bug missed by passing tests |
| Misleading passing test found | Activation test uses a persisted Experimental company with `last_success_at: null` but never sends it through Watchlist mapping | ✗ Green test does not prove truthful UI health |
| Uncovered error path found | SmartRecruiters detail fan-out exhaustion and modal removal rejection | ⚠ Warnings with no dedicated coverage |

## Human Verification Still Required After Gap Closure

### Plan 08 D5 — deployed visual and interaction judgment

**Test:** On the SHA-proven production Watchlist, inspect light and dark themes and a narrow viewport; open representative employer links; operate Watchlist removal with keyboard; compare Resume deletion and Settings type-to-confirm; leave the page through a background refresh.

**Expected:** Links reach the intended employers in a new tab; all seven columns remain available through the labeled horizontal scroll; contrast/focus rings are legible; Watchlist removal initially focuses **Keep company**, shows **Removing…** only while pending, handles failure accessibly, cancels safely, and restores focus; Resume/Settings defaults remain unchanged; background refresh neither moves focus nor repeatedly announces unchanged state.

**Why human:** Destination quality, viewport behavior, theme contrast, native keyboard/focus behavior, and assistive-technology announcements cannot be accepted from bundle strings or source inspection alone. This item remains pending, but `gaps_found` takes precedence until the two blockers above are fixed.

## Gaps Summary

Two blockers prevent Phase 02.1 from being accepted:

1. The legacy hosted pipeline verifier can corrupt real production job state and must be converted to a disposable, fully owned fixture before it is rerun.
2. Successful Experimental connectors, especially unscheduled Capital One Workday at 3/3, are falsely presented as Degraded because the UI ignores verification success time.

Two confirmed warnings should be handled in the same closure work if practical: add a total SmartRecruiters detail/time budget and move removal failures inside the active dialog. These warnings do not independently change the canonical status because the two blockers already require `gaps_found`.

Neither blocker is assigned to Phase 3 or Phase 4, so neither qualifies for deferral. After code fixes and safe regression/hosted verification, rerun Phase 02.1 verification; if the blockers close, the remaining route is `human_needed` for Plan 08 D5.

---

_Verified: 2026-07-18T16:01:40Z_  
_Verifier: the agent (gsd-verifier, generic-agent workaround)_
