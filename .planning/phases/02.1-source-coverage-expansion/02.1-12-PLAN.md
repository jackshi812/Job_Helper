---
phase: 02.1-source-coverage-expansion
plan: "12"
type: execute
wave: 2
depends_on: ["02.1-09", "02.1-10", "02.1-11"]
files_modified:
  - scripts/verify-pipeline.ts
  - web/tests/pipeline-verifier-safety.test.ts
autonomous: false
requirements: [PREF-05, DISC-07, DISC-08, DISC-09]
gap_closure: true
must_haves:
  truths:
    - "No external push, function deployment, hosted mutation, or production deployment occurs without explicit approval naming the exact SHA and targets."
    - "Approval explicitly covers both stateful hosted commands, every provider/network call, every durable and temporary production mutation class, Adzuna budget consumption, cleanup boundaries, and residue/reconciliation assertions."
    - "The pipeline verifier emits an internal entry, post-drain, per-probe, and final evidence envelope so mutations that occur inside the command are attainable and auditable."
    - "The safe hosted pipeline verifier passes and leaves zero marked fixtures; temporary claim fields are CAS-restored while legitimate provider, discovery, health, budget, and heartbeat effects are retained and explicitly reconciled."
    - "The exact approved SHA is deployed to Cloudflare and the live asset contains the repaired health/modal contracts."
    - "SmartRecruiters budget code is tied to the reviewed SHA and Supabase function deployment records by a deterministic Edge source manifest, without changing its unsupported/unclaimable disposition."
  artifacts:
    - path: "scripts/verify-pipeline.ts"
      provides: "Typed internal mutation journal and evidence envelope spanning pre-mutation entry through final reconciliation"
    - path: "web/tests/pipeline-verifier-safety.test.ts"
      provides: "Regression coverage for complete mutation-class accounting, attainable snapshot order, restoration, and residue gates"
    - path: ".planning/phases/02.1-source-coverage-expansion/02.1-12-SUMMARY.md"
      provides: "Approval, exact SHA, Supabase/Cloudflare deployment identities, hosted evidence envelopes, durable-diff reconciliation, and zero-residue proof"
  key_links:
    - from: "approved git SHA"
      to: "Cloudflare Pages production"
      via: "origin/main and deployment commit equality plus immutable asset hash"
      pattern: "sha256"
    - from: "scripts/verify-pipeline.ts"
      to: "hosted production database"
      via: "internal entry/post-drain/final snapshots, typed mutation journal, invocation-owned fixture, CAS restoration, and allowed-diff reconciliation"
      pattern: "PipelineEvidenceEnvelope|PIPELINE_MUTATION_CLASSES|phase-02.1-reopen-probe"
---

## Phase Goal

**As a** job seeker, **I want to** monitor representative major-employer career platforms with honest active, experimental, degraded, and unsupported states, **so that** I can broaden my job coverage without trusting brittle or failing sources.

<objective>
Approval-gate the repaired release, deploy the exact code, and gather safe hosted and SHA-bound production evidence.

Purpose: Local green tests do not prove deployed semantics, but production mutation must remain explicit and reviewable.
Output: A tested internal mutation-evidence envelope, approved deployments/runs, reconciled hosted proof, immutable frontend evidence, and a UAT-ready release.

Decision preservation: D-01 through D-15 remain enforced, including D-04 unsupported honesty, D-11 lifecycle safety, D-13 activation gates, and the existing exact-SHA production acceptance decision.
</objective>

<execution_context>
@/Users/jackshi/.codex/gsd-core/workflows/execute-plan.md
@/Users/jackshi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-source-coverage-expansion/02.1-09-SUMMARY.md
@.planning/phases/02.1-source-coverage-expansion/02.1-10-SUMMARY.md
@.planning/phases/02.1-source-coverage-expansion/02.1-11-SUMMARY.md
@.planning/phases/02.1-source-coverage-expansion/02.1-08-SUMMARY.md
@.planning/phases/02.1-source-coverage-expansion/02.1-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make every pipeline-verifier mutation internally auditable</name>
  <files>scripts/verify-pipeline.ts, web/tests/pipeline-verifier-safety.test.ts</files>
  <read_first>
    - scripts/verify-pipeline.ts
    - web/tests/pipeline-verifier-safety.test.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-09-SUMMARY.md
    - .planning/phases/02.1-source-coverage-expansion/02.1-VERIFICATION.md
  </read_first>
  <behavior>
    - The command captures `entry` before `ensureSeeds` or any database/function write, `post_drain` immediately after its internal due-work drain, named before/after snapshots around each later mutation class, and `final` after cleanup.
    - `PIPELINE_MUTATION_CLASSES` is exhaustive for optional seed creation, seed/all-active polling timestamps, provider lifecycle/health writes, two discovery sweeps, Adzuna quota/job writes, claim CAS restoration, pipeline/discovery heartbeat changes, the PlanetScale fixture, and the denied-RLS insert cleanup path.
    - The envelope classifies each observed diff as expected durable, temporary-must-restore, fixture-must-delete, or unexplained; unexplained diffs, failed CAS restoration, and fixture residue make the command fail.
    - Evidence records identifiers, changed field names, counts, timestamps, response summaries, and hashes for large text, but never service keys, cron/heartbeat secrets, user passwords, or authorization headers.
  </behavior>
  <action>Extend the Plan 09-safe verifier with exported/testable `PIPELINE_MUTATION_CLASSES`, `PipelineEvidenceSnapshot`, `PipelineMutationRecord`, and `PipelineEvidenceEnvelope` contracts plus focused capture/reconcile helpers. Capture the attainable `entry` snapshot before `ensureSeeds`; it must include whether each Stripe/Palantir/Ramp seed identity exists, all active-company identity/activation/claim/health fields, relevant job identity/lifecycle/first-sight hashes, and the complete pipeline heartbeat state. Because the command itself drains due work, capture `post_drain` internally immediately after that drain and journal the entry-to-post-drain durable provider effects instead of asking an external process to snapshot between those steps. Journal every subsequent real mutation class with named before/after data: optional `verify-board` seed creation; seed and forced-company `last_polled_at` resets; poll-tick claims/provider network calls; provider job insert/update/reopen/close; company `last_polled_at`, `last_success_at`, `consecutive_failures`, and bounded error changes; pipeline tick/success heartbeat; the first and second discovery-sweep calls; Adzuna request/date counters, status, inserted/refreshed/deduplicated job IDs/fingerprints, and discovery heartbeat; concurrent-claim timestamps and per-row CAS restoration; the all-active no-work timestamp override and per-row CAS restoration; the denied publishable-key insert and any unexpectedly returned ID cleanup; and all PlanetScale company/returned-job fixture writes/deletes. Mark optional seeds, legitimate provider/discovery job lifecycle, health, quota counters, and heartbeat advances as expected durable effects; do not restore them. Mark claim-probe and all-active timestamp overrides temporary and require exact CAS restoration unless a newer concurrent value wins, in which case record the conflict and fail without overwriting it. Mark PlanetScale and unexpected RLS rows as invocation-owned and require exact deletion plus zero marker/source-key/company-ID/returned-ID residue. Emit a deterministic redacted JSON envelope to stdout between unique begin/end sentinels before exit, including the exact invocation ID, command, snapshot timestamps, mutation records, allowed durable diffs, restoration results, residue counts, and unexplained diffs. Exit nonzero unless every mutation belongs to the exhaustive registry, every temporary mutation is restored safely, and every fixture class is residue-free. Expand the existing safety test to prove capture order, registry coverage, durable-vs-temporary classification, redaction, CAS-conflict failure, fixture-residue failure, and nonzero unexplained-diff behavior without a hosted call.</action>
  <verify><automated>cd web && npx vitest run tests/pipeline-verifier-safety.test.ts</automated></verify>
  <acceptance_criteria>
    - Entry evidence is captured before the first possible write, and post-drain evidence is captured inside the command at the only attainable point.
    - Every production mutation class in probes 1-17 has one exhaustive registry entry, a before/after record, an allowed disposition, and an acceptance predicate.
    - Legitimate provider/discovery/health/quota/heartbeat changes are retained and reported; only invocation-owned fixtures are deleted and only temporary timestamps are CAS-restored.
    - Envelope redaction and unexplained-diff, restoration-conflict, and residue failure gates pass locally.
  </acceptance_criteria>
  <done>The local verifier can prove exactly what it mutates from command entry through cleanup, with no impossible external snapshot step.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Approve the exact release and complete hosted mutation inventory</name>
  <files></files>
  <read_first>
    - .planning/phases/02.1-source-coverage-expansion/02.1-09-SUMMARY.md
    - .planning/phases/02.1-source-coverage-expansion/02.1-10-SUMMARY.md
    - .planning/phases/02.1-source-coverage-expansion/02.1-11-SUMMARY.md
    - scripts/verify-pipeline.ts
    - scripts/verify-source-coverage.ts
    - supabase/config.toml
  </read_first>
  <action>Run only read-only preflight before asking: show git status while excluding unrelated user changes, the exact gap-fix commits/HEAD SHA, focused/full test/build/lint results, no pending migration, linked project/migration parity, affected Edge functions, Cloudflare account/project/production branch, the exact expanded push/deploy commands, and the two literal hosted commands. Present one approval inventory that does not call any verifier read-only or mutation-free. The inventory must say `git push origin main` changes origin; the exact linked-project `supabase functions deploy verify-board ...` and `supabase functions deploy poll-tick ...` calls replace production Edge bundles and make provider network/database writes available; and the connected Cloudflare `main` build publishes the frontend. For `node --env-file=scripts/.env --experimental-strip-types scripts/verify-pipeline.ts`, enumerate: optional persistent Stripe/Palantir/Ramp creation through verify-board; Greenhouse/Lever/Ashby network calls; resetting seed and later all-active `last_polled_at`; claims and poll-tick provider processing; durable provider job insertion/update/reopen/close and immutable-field checks; company poll/success/failure/error health changes; pipeline `last_tick_at`/`last_success_at` heartbeats; four concurrent claim RPCs with CAS restoration; the no-work all-active timestamp override with CAS restoration; two discovery sweeps; Adzuna network requests and daily budget/date/status counter consumption; durable Adzuna insert/refresh/dedupe/job-status effects; discovery `last_discovery_at`/`discovery_status` heartbeat changes; a denied RLS insert that is deleted if unexpectedly accepted; and the collision-free PlanetScale company/jobs that are always deleted. For `node --env-file=scripts/.env --experimental-strip-types scripts/verify-source-coverage.ts --resume`, enumerate: real-user authorization calls and SmartRecruiters/Recruitee/Workday provider network calls; persistent connector company/catalog/activation-observation/progress/state/last-verified changes; activation RPC replay/contention and Workday cap calls; claim RPC timestamp changes with CAS restoration; the marked Recruitee controlled-failure company/job and exact-prefix cleanup; poll-tick company degradation/recovery and pipeline heartbeat advances. State that both discovery sweeps consume Adzuna allowance even when results deduplicate or no fresh row appears. Distinguish durable expected effects (optional seeds, legitimate provider/discovery jobs, connector/activation evidence, company health, budget counters, and heartbeats) from temporary fields that must CAS-restore and fixtures that must delete. Explain that entry/post-drain evidence for the first command is captured internally, while the second command receives attainable external pre/post snapshots. Do not include `.DS_Store`, `scripts/agent-dashboard.mjs`, or `scripts/agent-dashboard.test.mjs` in any commit/push. Decline stops before every external mutation.</action>
  <verify><automated>git status --short && git log --oneline --decorate -12 && cd web && npm test && npm run build && npm run lint && npx supabase migration list --linked</automated></verify>
  <acceptance_criteria>
    - The prompt names the exact SHA, origin/main, linked Supabase project/functions, Cloudflare production target, all exact commands, provider network calls, and Adzuna request/budget consequences.
    - Every mutation class has its durable, temporary-restoration, or fixture-cleanup disposition and a concrete evidence predicate.
    - Preflight proves there is no schema push and no unrelated user file is included.
    - No push, deploy, provider call, Adzuna request, or hosted database mutation occurs before explicit approval.
  </acceptance_criteria>
  <done>User explicitly approves the exact SHA, targets, commands, provider/quota consequences, durable changes, temporary restoration, and fixture cleanup.</done>
  <decision>Approve pushing/deploying the exact gap-fix SHA and running both fully enumerated stateful hosted verifiers.</decision>
  <options>
    <option id="approve"><name>Approve exact release and runs</name><pros>Produces SHA-bound production proof with internally attainable mutation evidence.</pros><cons>Pushes and replaces production bundles; calls Greenhouse, Lever, Ashby, SmartRecruiters, Recruitee, Workday, and Adzuna; consumes Adzuna daily requests; may persist missing seed/connector companies, accepted activation evidence, legitimate provider/discovery job lifecycle and health changes, and heartbeat/quota advances; temporarily changes claim timestamps under CAS restoration; and creates then deletes PlanetScale, denied-RLS-if-any, and Recruitee fixtures.</cons></option>
    <option id="decline"><name>Decline all external changes</name><pros>Performs no push, deploy, provider call, Adzuna request, or hosted database mutation.</pros><cons>Phase remains gaps_found and production evidence is not refreshed.</cons></option>
  </options>
  <resume-signal>Select approve or decline.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy exact SHA and reconcile both approved hosted runs</name>
  <files></files>
  <read_first>
    - scripts/verify-pipeline.ts
    - scripts/verify-source-coverage.ts
    - .planning/phases/02.1-source-coverage-expansion/02.1-VERIFICATION.md
  </read_first>
  <action>After approval, re-check HEAD equality and a clean reviewed-file diff, push only the approved commits to `origin/main`, and build a deterministic source manifest from the approved-SHA contents of each deployed function entry plus all transitive local `_shared` imports, including connectors, SmartRecruiters adapter/types, lifecycle, and registry adapters. Record Git blob hashes and SHA-256 of the sorted manifest. Run only the exact approved linked-project deploy commands for `verify-board` and `poll-tick`, preserve their JWT/real-user and cron-secret boundaries, record `supabase functions list --output json` versions/deployed-at data, and do not run `db push`. Wait at most 15 minutes in intervals no longer than 60 seconds for the connected Cloudflare `main` deployment to report the exact SHA; record deployment ID, immutable JS URL/hash, and frontend-only health/modal/prior Phase 02.1 markers. Fail on SHA, source-manifest, project, branch, auth-boundary, deployment-record, or asset drift.

Then execute only the two literal stateful hosted commands approved in Task 2, serially. For `verify-pipeline.ts`, capture its sentinel-delimited internal envelope from the exact run and require: `entry` precedes seed creation; `post_drain` follows the internal drain; every registry class is present as executed or explicitly not-applicable; optional seed creation is identity-valid; every seed/all-active timestamp write is journaled; every provider job insert/update/reopen/close and company health change is attributed to a named poll and retains immutable source/external/first-sight data; both discovery sweeps report Adzuna request/date counter deltas, response status, inserted/refreshed/deduplicated IDs/fingerprints and job-status effects; pipeline and discovery heartbeat deltas match responses; each concurrent-claim and all-active temporary timestamp is either exactly CAS-restored or produces a blocking conflict without overwrite; PlanetScale and any unexpectedly accepted RLS fixture have zero marker/source-key/company-ID/returned-ID residue. Accept legitimate durable seed/provider/discovery/health/quota/heartbeat changes and include them in allowed diffs; do not demand that real lifecycle, health, quota, or heartbeat values revert. Reject an unregistered diff, immutable-field drift, failed cleanup, silent restoration conflict, or missing envelope section.

Immediately before `verify-source-coverage.ts --resume`, take an attainable read-only snapshot of connector/catalog identities, activation observations/progress/last-verified/health fields, all relevant provider job identities/lifecycle/first-sight hashes, Workday job count, every active company claim timestamp, the full pipeline heartbeat, and zero exact-prefix controlled-failure fixtures. Run the approved command once. In the after snapshot, allow only persistent verified connector/catalog creation, accepted activation observations/progress/last-verified evidence, legitimate provider/health writes, and pipeline heartbeat advances; require replay/contention/fourth-Workday-window calls to add no forbidden evidence; require every claim timestamp changed solely for the unclaimable proof to be CAS-restored; require zero Recruitee marker company/job residue; require Capital One Experimental 3/3, unclaimable, zero Workday jobs, no error, and mapped OK with verification time; and require SmartRecruiters unsupported/unclaimable. Reject unrelated catalog/company/job/ledger/heartbeat diffs. Run the full local test/build/lint gate. Record the exact approved commands, raw redacted pipeline envelope, source-coverage external snapshots, allowed durable diffs, restoration predicates/results, zero-residue counts, deployment IDs, manifest/hash/function versions, and frontend asset hash in the summary.</action>
  <verify><automated>node --env-file=scripts/.env --experimental-strip-types scripts/verify-pipeline.ts && node --env-file=scripts/.env --experimental-strip-types scripts/verify-source-coverage.ts --resume && cd web && npm test && npm run build && npm run lint</automated></verify>
  <acceptance_criteria>
    - Safe hosted pipeline verifier exits zero with a complete entry/post-drain/per-probe/final envelope, probe 15 PASS, exact temporary-field restoration, and zero marked residue.
    - Optional seed creation, seed/all-active polling, provider lifecycle/health, both discovery sweeps, Adzuna quota/job/status effects, claim CAS restoration, and pipeline/discovery heartbeat changes are each reconciled; no legitimate durable effect is falsely called residue.
    - Source verifier reaches COMPLETE with connector/activation/provider/claim/controlled-failure/heartbeat effects reconciled to its external before/after envelope; Workday is Experimental 3/3 and UI-health OK; SmartRecruiters is still unsupported.
    - Exact production SHA, Edge source-manifest/function-version proof, and immutable frontend asset proof accompany the hosted results.
  </acceptance_criteria>
  <done>Automated and hosted evidence closes both blockers and both warnings on the exact deployed release.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1; block on high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---:|---|---|
| T-02.1-12-01 | Tampering | local git -> origin/Cloudflare | high | mitigate | Explicit approval, exact SHA equality, branch/project checks, immutable asset hash |
| T-02.1-12-02 | Elevation of Privilege | local CLI -> Supabase | high | mitigate | Read-only identity preflight and exact linked project/function list |
| T-02.1-12-03 | Tampering | pipeline verifier -> production DB | high | mitigate | Entry/post-drain/per-probe/final envelope, exhaustive mutation registry, allowed durable diffs, CAS restoration, owned-fixture cleanup, and unexplained-diff failure |
| T-02.1-12-04 | Denial of Service | verifier -> ATS/Adzuna providers and daily quota | high | mitigate | Approval names every provider call, two discovery sweeps, and Adzuna budget consumption; observed request counters/status must reconcile exactly |
| T-02.1-12-05 | Tampering | claim/no-work probes -> company timestamps | high | mitigate | Per-row before/after records and compare-and-set restoration; concurrent conflict blocks without overwrite |
| T-02.1-12-06 | Tampering | provider/discovery polls -> real jobs and company health | high | mitigate | Expected durable lifecycle/health changes are tied to named responses; immutable identities/first-sight hashes must not drift |
| T-02.1-12-07 | Tampering | disposable fixtures -> production DB | high | mitigate | Collision preflight, invocation ownership, finally deletion, and zero marker/source-key/company/job residue |
| T-02.1-12-08 | Information Disclosure | evidence envelope -> logs/summary | high | mitigate | Deterministic redaction forbids credentials, secrets, passwords, authorization headers, and raw large descriptions |
</threat_model>

<verification>Tested internal mutation accounting; approved exact-SHA deployment; two explicitly stateful hosted verifiers with complete durable/temporary/fixture reconciliation; production smoke; and the full local quality gate.</verification>

<success_criteria>
- External changes occur only after explicit approval.
- Deployed SHA/assets match reviewed repairs.
- Hosted proof accounts for every expected durable change, restores every temporary claim mutation safely, and leaves no owned fixture residue.
- Workday and SmartRecruiters remain honestly classified.
</success_criteria>

## Artifacts this phase produces

- Approved gap-fix release SHA and origin/main equality record.
- Supabase function deployment identities and preserved auth-boundary evidence.
- Approved-SHA Edge source manifest/blob hashes tied to Supabase function versions and deployment timestamps.
- Cloudflare deployment ID, immutable asset URL, and SHA-256.
- Pipeline entry/post-drain/per-probe/final mutation envelope with allowed durable diffs, CAS restoration results, and zero-fixture evidence.
- Source-coverage external before/after snapshot with activation/provider/claim/heartbeat reconciliation and zero controlled-failure residue.

<output>Create `.planning/phases/02.1-source-coverage-expansion/02.1-12-SUMMARY.md` when done.</output>
