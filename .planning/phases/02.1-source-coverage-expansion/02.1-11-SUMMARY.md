---
phase: 02.1-source-coverage-expansion
plan: "11"
subsystem: public-connectors
tags: [supabase, edge-functions, smartrecruiters, budgets, lifecycle, vitest]

requires:
  - phase: 02.1-source-coverage-expansion
    provides: Closure-safe PollObservation semantics and the SmartRecruiters public adapter from Plans 01-04
provides:
  - Exact production SmartRecruiters defaults of 40 detail requests and 60 seconds per adapter invocation
  - Remaining-time-capped provider requests and retries across list pagination and detail enrichment
  - Stable closure-ineligible partial observations when either invocation budget is exhausted
affects: [02.1-12-production-rerun, smartrecruiters, provider-runtime-safety]

tech-stack:
  added: []
  patterns:
    - One adapter-entry deadline governs every provider request and retry
    - Detail count exhaustion retains validated list rows while denying closure credibility

key-files:
  created:
    - .planning/phases/02.1-source-coverage-expansion/02.1-11-SUMMARY.md
  modified:
    - supabase/functions/_shared/adapters/smartrecruiters.ts
    - web/tests/adapters.test.ts

key-decisions:
  - "Use performance.now by default and an injectable clock in tests so the 60-second invocation deadline is monotonic and deterministic."
  - "Allow the final count slot to run as a one-request batch, preserving the exact 40-request ceiling while keeping maximum detail concurrency at two."

patterns-established:
  - "Provider-wide deadline: compute once at adapter entry, cap each request timeout/retry by remaining time, and refuse new work at zero."
  - "Budget degradation: return detail_budget_exceeded with safe partial rows, never complete or credible closure evidence."

requirements-completed: [DISC-07, DISC-09]

coverage:
  - id: D1
    description: "SmartRecruiters stops before detail request 41 or the next request after 60,000 milliseconds, retains validated rows, and emits only detail_budget_exceeded."
    requirement: DISC-07
    verification:
      - kind: unit
        ref: "web/tests/adapters.test.ts#SmartRecruiters invocation budgets"
        status: pass
      - kind: integration
        ref: "web/tests/public-connectors.integration.test.ts#SmartRecruiters bounded observation"
        status: pass
    human_judgment: false
  - id: D2
    description: "Budget-exhausted SmartRecruiters observations ingest safe rows but cannot close an omitted open job, while small boards remain complete and credible."
    requirement: DISC-09
    verification:
      - kind: integration
        ref: "web/tests/adapters.test.ts#stops before detail request 41 and retains safe partial rows"
        status: pass
      - kind: other
        ref: "cd web && npx vitest run tests/adapters.test.ts tests/public-connectors.integration.test.ts tests/source-observation.integration.test.ts tests/lifecycle.test.ts"
        status: pass
      - kind: other
        ref: "cd web && npm test && npm run build && npm run lint"
        status: pass
    human_judgment: false

duration: 5 min
completed: 2026-07-18
status: complete
---

# Phase 02.1 Plan 11: SmartRecruiters Invocation Budget Summary

**SmartRecruiters now returns truthful partial evidence within a 40-detail/60-second invocation envelope instead of risking Edge runtime termination on large boards.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-07-18T17:18:59Z
- **Completed:** 2026-07-18T17:23:45Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added exact exported production defaults of 40 detail requests and 60,000 milliseconds with deterministic test-only overrides.
- Applied one adapter-entry deadline to list pagination, detail enrichment, request abort timeouts, and retries while preserving page size 100, 20 pages, 1,000 jobs, 15-second per-request timeout, and two-wide detail concurrency.
- Returned validated partial rows with the bounded `detail_budget_exceeded` warning when either limit is reached; exhausted observations remain closure-ineligible and safe rows still ingest.
- Added count, elapsed-time, zero-extra-fetch, override, happy-path, and lifecycle-composition regressions without sleeping or contacting a provider.

## Task Commits

1. **Task 1 RED: Specify total SmartRecruiters detail budgets** - `bcba012` (test)
2. **Task 1 GREEN: Enforce invocation-wide count and time budgets** - `049ba97` (fix)
3. **Task 2: Prove exhausted observations stay closure-safe** - `c82d0e5` (test)

## Files Created/Modified

- `supabase/functions/_shared/adapters/smartrecruiters.ts` - Production defaults, injectable monotonic clock, invocation deadline, remaining-time request caps, and bounded detail fan-out.
- `web/tests/adapters.test.ts` - Exact default, count/time exhaustion, no-extra-fetch, partial-row, happy-path, and lifecycle safety regressions.

## Decisions Made

- Used `performance.now()` for the production deadline because elapsed duration, not wall-clock time, is the safety boundary; tests inject a deterministic clock.
- Kept the maximum concurrency at two while permitting a final one-request batch when only one of the 40 logical detail slots remains.
- Reused the existing `incomplete()` observation boundary so warnings remain capped at 64 characters and exhausted results cannot authorize closure.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The production build retains the pre-existing Vite chunk-size warning, and lint retains the pre-existing Fast Refresh warning in `AuthProvider.tsx`; both commands exit successfully.
- The repository git index required managed escalation for commits; approved commits completed normally with hooks.

## Known Stubs

None.

## User Setup Required

None - this plan changed only local adapter code and tests. It deployed no function and made no hosted request.

## Next Phase Readiness

- Plan 12 may deploy this bounded adapter with the other approved gap fixes and run exact-SHA hosted verification.
- SmartRecruiters remains honestly unsupported and unclaimable; this safety hardening does not change its provider disposition.

## Verification Results

- RED gate: four new budget tests failed for missing defaults/options/enforcement while six existing adapter tests stayed green.
- Focused adapter/lifecycle gate: 4 files, 75 tests passed.
- Full deterministic regression: 22 files, 285 tests passed.
- Production TypeScript/Vite build passed with only the unchanged chunk-size warning.
- Oxlint passed with only the unchanged Fast Refresh warning.

## Self-Check: PASSED

- Commits `bcba012`, `049ba97`, and `c82d0e5` exist on `main` in RED, GREEN, and lifecycle-proof order.
- Both declared implementation/test files and this summary exist.
- Coverage classification reports 2/2 deliverables fully auto-covered with passing evidence.
- The focused 75-test gate, full 285-test suite, production build, lint, and diff whitespace check pass.
- No package, schema, provider disposition, scheduled dispatch, deployment, secret, or hosted state changed.
- Unrelated `.DS_Store` and agent-dashboard files remain untouched and uncommitted.

---
*Phase: 02.1-source-coverage-expansion*
*Completed: 2026-07-18*
