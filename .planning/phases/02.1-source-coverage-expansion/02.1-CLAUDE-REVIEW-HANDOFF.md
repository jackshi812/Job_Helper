---
phase: "02.1"
purpose: claude_plan_review
review_commit: abf1349
mode: read_only
---

# Claude Review Handoff — Phase 02.1

Review Phase 02.1 at commit `abf1349` as an independent, source-grounded plan reviewer. Do not edit files, execute the plans, deploy, push, or create a review commit.

## Read first

1. `.planning/PROJECT.md`
2. `.planning/ROADMAP.md` — Phase 02.1 section
3. `.planning/REQUIREMENTS.md` — PREF-05 and DISC-07 through DISC-09
4. `.planning/phases/02.1-source-coverage-expansion/02.1-CONTEXT.md`
5. `.planning/phases/02.1-source-coverage-expansion/02.1-RESEARCH.md`
6. `.planning/phases/02.1-source-coverage-expansion/02.1-PATTERNS.md`
7. `.planning/phases/02.1-source-coverage-expansion/02.1-UI-SPEC.md`
8. All eight `.planning/phases/02.1-source-coverage-expansion/02.1-*-PLAN.md` files
9. Every source file, migration, route, and test referenced by those plans

## Review standard

- Verify plan claims against the current repository; do not review plan prose in isolation.
- Cite concrete `path/to/file:line` evidence and explain the runtime mechanism.
- Check completeness, dependency ordering, same-wave file ownership, security, failure behavior, scope, and executable verification.
- Treat unsupported or undocumented source integrations as fail-closed; do not recommend generic HTML scraping as an implicit fallback.
- Confirm the plans cover PREF-05, DISC-07, DISC-08, DISC-09, and decisions D-01 through D-15.
- If repository evidence is unavailable, label the point an open question instead of asserting a defect.

## Re-audit these prior risk areas

1. SmartRecruiters and Recruitee parity across registry identities, TypeScript source unions, `companies.ats_type`, `jobs.source`, adapters, detection, and ordered migrations.
2. Scheduled `poll-tick` dispatch reaches the shared connector registry for active providers, rejects unknown providers, and leaves Experimental Workday unclaimable.
3. Activation requires three distinct database-time polling windows; replayed or same-window calls cannot promote a connector.
4. Failed, ambiguous, blocked, partial, or implausibly empty observations retain last-known jobs and cannot close listings.
5. Hosted verification pushes migrations before assertions, is resumable/idempotent, uses server eligibility boundaries, and limits individual waits to 60 seconds.
6. Production UI acceptance requires explicit approval, a push to `origin/main`, a Cloudflare deployment matching the reviewed commit SHA, and deployed asset proof before UAT.
7. The finance catalog labels only safely pollable sources as monitored and provides canonical links, evidence, and explicit unsupported reasons for the remainder.

## Required response

Return exactly one top-level verdict:

- `## REVIEW PASSED`
- `## ISSUES FOUND`

For issues, provide a structured list containing plan, task, severity (`blocker`, `warning`, or `info`), evidence, impact, and a specific fix. Finish with a short coverage summary and overall risk level. Do not modify the repository.

## Suggested invocation

From the repository root:

```bash
claude -p "Read .planning/phases/02.1-source-coverage-expansion/02.1-CLAUDE-REVIEW-HANDOFF.md in full and perform the requested read-only review. Output only the review verdict and findings."
```
