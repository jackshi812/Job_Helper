---
context: phase
phase: 04-resume-tailoring-tracker
task: null
total_tasks: 0
status: paused
last_updated: 2026-07-25T20:00:49.824Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Production history isolation — local `main` and production `origin/main` intentionally have different histories — future releases must be built as isolated fast-forward candidates from `origin/main`; never push local `main` directly or force-push production.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Releasing local planning history | The approved local Phase 03.7 commit could only reach production through a destructive force-push because production uses a clean release history. | blocking | Start an isolated worktree at `origin/main`, apply only the approved source diff, prove tree identity, rerun gates, obtain exact-SHA approval, and fast-forward push. |
| Assuming direct Cloudflare control | Wrangler cannot query or deploy non-interactively because `CLOUDFLARE_API_TOKEN` is unavailable. | advisory | Use the GitHub-triggered Pages path and verify `origin/main`, the production asset path, and live/local SHA-256 equality. |
| Claiming browser UAT without a browser | The browser-control runtime had no available browser. | advisory | Keep authenticated UI checks PENDING until the owner observes and explicitly approves them. |
| Touching unrelated worktree dirt | `.DS_Store`, the two agent-dashboard scripts, and `web/zh` predate this phase. | advisory | Stage only explicit task files and preserve those paths unchanged. |

<current_state>
Phase 03.7 is fully complete. The default production route is Watchlist Jobs, `/all-jobs` preserves the combined feed, exact release `baf81fcaa72443995f227b118d10eec90da25679` is live, its JavaScript and CSS are byte-identical to the approved build, and the owner approved all three signed-in UAT checks. Project state has advanced to Phase 4, but Phase 4 discussion, research, context, and planning have not started.
</current_state>

<completed_work>

Completed Tasks:
- Recovered the Phase 03.3 Plan 04 execution anomaly using the owner-selected mark-and-skip path; preserved partial evidence and explicitly did not claim missing historical UAT.
- Replaced the paused source-expansion Phase 03.7 scope with Watchlist-First Jobs Dashboard.
- Implemented one reusable Dashboard: `/` is watchlist-only and `/all-jobs` is the existing combined feed.
- Passed 52 test files / 1,127 tests, production build, lint, and diff checks.
- Created clean production candidate `baf81fcaa72443995f227b118d10eec90da25679` from `origin/main`, proved its `web` tree matches the approved implementation, and fast-forward deployed it.
- Verified production JavaScript SHA-256 `8e28857fb560b5997865201c75226069e71a973a5fe66451726d52a7d7a41f1f` and CSS SHA-256 `5e4b68a4dd156623177584706be0bfc5f367d9102cd15ea5324204d1a36b8eaf`.
- Recorded 3/3 owner-approved signed-in UAT checks and closed Phase 03.7 with no remaining gaps.
</completed_work>

<remaining_work>

- Discuss Phase 4 Resume Tailoring & Tracker; no Phase 4 decisions are locked yet.
- After discussion, research DOCX formatting fidelity, truthful edit validation, PDF generation, review UX, application-stage modeling, manually added jobs, notes, and artifact linking.
- Plan and execute Phase 4 only after the owner approves its product boundary.
</remaining_work>

<decisions_made>

- Watchlist Jobs is the default because watched companies have higher priority.
- All Jobs retains watched and externally discovered preference-pass jobs.
- The watchlist-only view keeps its company filter; Phase 03.7 did not create an external-only page or company directory.
- Source membership uses the normalized job-to-company relationship and fails closed; employer-name guessing is forbidden.
- Phase 03.7 reused the existing feed and Dashboard without schema, ingestion, provider, or persistence changes.
- The former PwC/Citi/Google/Amazon/Microsoft source-expansion scope was replaced and remains unimplemented.
- Phase 03.3 Plan 04 is superseded; no historical rollout replay is permitted.
</decisions_made>

<blockers>
- None. Phase 4 is intentionally waiting for discussion, not technically blocked.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable resume state and exact release identities.
2. `.planning/STATE.md` — current Phase 4 boundary and milestone status.
3. `.planning/ROADMAP.md` § Phase 4 — goal, requirements, and success criteria.
4. `.planning/PROJECT.md` — product scope and core value.
5. `.planning/REQUIREMENTS.md` — RESU-02..05 and TRAK-01..04 definitions.
6. `.planning/phases/03.7-watchlist-first-jobs-dashboard/03.7-VERIFICATION.md` — completed release proof.
7. `.planning/phases/03.7-watchlist-first-jobs-dashboard/03.7-UAT.md` — owner-approved production observations.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat local `main` as the production release branch → build future releases from `origin/main` in an isolated clean worktree and fast-forward only.
- Do not reuse an approval after a candidate SHA changes → request approval for the exact clean production candidate.
- Do not infer authenticated browser behavior from unit tests or public asset strings → require owner observation when browser control remains unavailable.
- Do not stage or modify `.DS_Store`, `scripts/agent-dashboard.mjs`, `scripts/agent-dashboard.test.mjs`, or `web/zh`.

## Infrastructure State

- Production web: `https://job-helper-qs9.pages.dev`.
- Production release branch: `origin/main` at `baf81fcaa72443995f227b118d10eec90da25679`.
- Local planning branch: `main` at `06792e16bddef1358aac70b8153a484ebeacc11b`.
- Supabase/schema/functions/ingestion: unchanged by Phase 03.7.
- Cloudflare API token: unavailable to the non-interactive environment; deploy through the GitHub Pages integration.
- Background processes: none known.
- Async jobs: none.
- Uncommitted user-owned paths: `.DS_Store`, `scripts/agent-dashboard.mjs`, `scripts/agent-dashboard.test.mjs`, and `web/zh`.

<context>
The owner asked to stop after completing and approving Phase 03.7. Resume at the clean Phase 4 discussion boundary. The fastest safe path is to clarify the smallest vertical Phase 4 MVP before research or implementation; do not revive the replaced source-expansion work unless the owner explicitly asks later.
</context>

<next_action>
Start with: acknowledge the production-history isolation constraint, then run `$gsd-discuss-phase 4` to collect Phase 4 product decisions.
</next_action>
