VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key_here
# Web-push VAPID public key (base64url uncompressed P-256 point). Generate with
# `node scripts/generate-vapid-keys.mjs`; put the REAL value in web/.env.local and
# the Cloudflare Pages env, not here. Consumed by web/src/lib/push.ts (Plan 03-07).
VITE_VAPID_PUBLIC_KEY=
